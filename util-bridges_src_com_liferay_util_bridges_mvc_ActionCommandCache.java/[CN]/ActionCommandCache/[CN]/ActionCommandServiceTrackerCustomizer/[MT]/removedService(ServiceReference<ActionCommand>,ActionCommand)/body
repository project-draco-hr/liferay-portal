{
  Registry registry=RegistryUtil.getRegistry();
  registry.ungetService(serviceReference);
  String actionCommandName=(String)serviceReference.getProperty("action.command.name");
  _actionCommandCache.remove(actionCommandName);
  for (  List<ActionCommand> chains : _actionCommandChainCache.values()) {
    chains.remove(actionCommand);
  }
}

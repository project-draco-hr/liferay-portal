{
  DLSync newDLSync=addDLSync();
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(DLSync.class,DLSync.class.getClassLoader());
  dynamicQuery.add(RestrictionsFactoryUtil.eq("fileId",newDLSync.getFileId()));
  List<DLSync> result=_persistence.findWithDynamicQuery(dynamicQuery);
  assertEquals(1,result.size());
  DLSync existingDLSync=result.get(0);
  assertEquals(existingDLSync,newDLSync);
}

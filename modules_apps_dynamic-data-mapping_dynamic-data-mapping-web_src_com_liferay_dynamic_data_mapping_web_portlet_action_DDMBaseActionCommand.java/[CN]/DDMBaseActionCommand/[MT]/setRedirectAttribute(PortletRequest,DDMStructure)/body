{
  String redirect=getRedirect(portletRequest);
  boolean saveAndContinue=ParamUtil.getBoolean(portletRequest,"saveAndContinue");
  if (saveAndContinue) {
    redirect=getSaveAndContinueRedirect(portletRequest,structure,redirect);
  }
  portletRequest.setAttribute(WebKeys.REDIRECT,redirect);
}

{
  if (!PropsValues.LAYOUT_USER_PUBLIC_LAYOUTS_ENABLED && user.hasPublicLayouts()) {
    Group userGroup=user.getGroup();
    LayoutLocalServiceUtil.deleteLayouts(userGroup.getGroupId(),false);
  }
}

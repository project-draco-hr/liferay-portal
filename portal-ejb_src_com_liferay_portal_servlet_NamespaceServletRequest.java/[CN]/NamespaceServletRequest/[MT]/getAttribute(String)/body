{
  Object value=null;
  if (reservedParams.contains(name) || PortalClassLoaderUtil.isPortalClassLoader()) {
    value=super.getAttribute(name);
  }
  if (value == null) {
    value=super.getAttribute(_namespace + name);
  }
  return value;
}

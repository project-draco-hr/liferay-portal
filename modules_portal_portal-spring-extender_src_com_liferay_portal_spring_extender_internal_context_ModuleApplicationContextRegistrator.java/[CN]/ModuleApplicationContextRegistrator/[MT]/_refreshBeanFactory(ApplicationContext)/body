{
  try {
    BeanReferenceRefreshUtil.refresh(applicationContext.getAutowireCapableBeanFactory());
  }
 catch (  Exception e) {
    _logger.log(Logger.LOG_ERROR,"Unexpected error while refreshing " + applicationContext.getDisplayName() + " may cause memory "+ "leaks on multiple redeployments");
  }
}

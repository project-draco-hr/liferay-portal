{
  try {
    BeanReferenceRefreshUtil.refresh(applicationContext.getAutowireCapableBeanFactory());
  }
 catch (  Exception e) {
    _logger.log(Logger.LOG_ERROR,"Unable to refresh " + applicationContext.getDisplayName() + ". This may cause memory leaks on multiple redeployments.");
  }
}

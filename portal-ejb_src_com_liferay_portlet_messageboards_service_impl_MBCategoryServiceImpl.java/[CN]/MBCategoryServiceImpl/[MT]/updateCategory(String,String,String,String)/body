{
  MBCategoryPermission.check(getPermissionChecker(),categoryId,ActionKeys.UPDATE);
  return MBCategoryLocalServiceUtil.updateCategory(getUser().getCompanyId(),categoryId,parentCategoryId,name,description);
}

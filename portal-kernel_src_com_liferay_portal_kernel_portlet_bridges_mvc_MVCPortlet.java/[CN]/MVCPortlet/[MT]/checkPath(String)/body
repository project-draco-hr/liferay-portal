{
  if (Validator.isNotNull(path) && (!path.startsWith(templatePath) || !PortalUtil.isValidResourceId(path) || !Validator.isFilePath(path,false))) {
    throw new PortletException("Path " + path + " is not accessible by this portlet");
  }
}

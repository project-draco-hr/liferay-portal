{
  Repository repository=findByPrimaryKey(repositoryId);
  Session session=null;
  try {
    session=openSession();
    Repository[] array=new RepositoryImpl[3];
    array[0]=getByUuid_PrevAndNext(session,repository,uuid,orderByComparator,true);
    array[1]=repository;
    array[2]=getByUuid_PrevAndNext(session,repository,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  List<BreadcrumbEntry> entries=new ArrayList<BreadcrumbEntry>();
  LayoutSet parentLayoutSet=_getParentLayoutSet(themeDisplay.getLayout().getLayoutSet());
  if (parentLayoutSet != null) {
    _addGroupsBreadcrumbEntries(entries,themeDisplay,parentLayoutSet,true);
  }
  return entries;
}

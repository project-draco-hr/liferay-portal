{
  List<BreadcrumbEntry> entries=new ArrayList<BreadcrumbEntry>();
  _addGroupsEntries(entries,themeDisplay,themeDisplay.getLayout().getLayoutSet(),false);
  BreadcrumbEntry entry=null;
  if (entries.size() > 0) {
    entry=entries.get(0);
  }
  return entry;
}

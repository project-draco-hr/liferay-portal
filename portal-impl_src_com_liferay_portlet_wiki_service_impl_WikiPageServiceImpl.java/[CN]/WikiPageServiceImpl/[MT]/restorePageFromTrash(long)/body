{
  WikiPage page=wikiPageLocalService.getPage(resourcePrimKey);
  WikiPagePermission.check(getPermissionChecker(),page,ActionKeys.DELETE);
  wikiPageLocalService.restorePageFromTrash(getUserId(),page);
}

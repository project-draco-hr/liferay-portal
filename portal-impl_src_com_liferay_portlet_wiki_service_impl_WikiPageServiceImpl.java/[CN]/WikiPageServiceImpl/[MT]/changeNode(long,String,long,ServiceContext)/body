{
  WikiPagePermission.check(getPermissionChecker(),nodeId,title,ActionKeys.DELETE);
  WikiNodePermission.check(getPermissionChecker(),newNodeId,ActionKeys.ADD_PAGE);
  wikiPageLocalService.changeNode(getUserId(),nodeId,title,newNodeId,serviceContext);
}

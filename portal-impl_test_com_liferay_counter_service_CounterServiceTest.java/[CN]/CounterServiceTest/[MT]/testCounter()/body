{
  long counterValue=0;
  try {
    _counterServiceTestService.testCounterService();
  }
 catch (  CounterServiceTestException e) {
    counterValue=e.getCounterValue();
  }
  assertTrue(counterValue < nextLong());
}

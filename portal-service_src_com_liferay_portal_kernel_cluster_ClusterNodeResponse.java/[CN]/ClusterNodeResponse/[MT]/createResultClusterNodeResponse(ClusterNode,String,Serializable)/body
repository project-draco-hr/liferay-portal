{
  if ((result != null) && !(result instanceof Serializable)) {
    return new ClusterNodeResponse(clusterNode,uuid,null,new ClusterException("Return value is not serializable"));
  }
  return new ClusterNodeResponse(clusterNode,uuid,result,null);
}

{
  if ((result != null) && !(result instanceof Serializable)) {
    return new ClusterNodeResponse(clusterNode,clusterMessageType,uuid,multicast,null,new ClusterException("Return value is not serializable"));
  }
  return new ClusterNodeResponse(clusterNode,clusterMessageType,uuid,multicast,result,null);
}

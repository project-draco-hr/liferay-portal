{
  if ((result != null) && !(result instanceof Serializable)) {
    return new ClusterNodeResponse(clusterNode,clusterMessageType,uuid,null,new ClusterException("Return value is not serializable"));
  }
  return new ClusterNodeResponse(clusterNode,clusterMessageType,uuid,result,null);
}

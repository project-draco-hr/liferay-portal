{
  try {
    String result=HttpUtil.URLtoString(url);
    if (result.contains(entryURL)) {
      return;
    }
  }
 catch (  Exception e) {
  }
  try {
    _commentManager.deleteComment(commentId);
  }
 catch (  Exception e) {
    _log.error("Error trying to delete trackback comment " + commentId,e);
  }
}

{
  String xml=(String)oldValue;
  String portletId=(String)_upgradePortletIdColumn.getOldValue();
  PortletPreferences preferences=PortletPreferencesSerializer.fromDefaultXML(xml);
  processPreferences(portletId,preferences);
  return PortletPreferencesSerializer.toXML((PortletPreferencesImpl)preferences);
}

{
  String xml=(String)oldValue;
  String portletId=(String)_upgradePortletIdColumn.getOldValue();
  PortletPreferences prefs=PortletPreferencesSerializer.fromDefaultXML(xml);
  processPrefs(portletId,prefs);
  return PortletPreferencesSerializer.toXML((PortletPreferencesImpl)prefs);
}

{
  DoPrivileged annotation=beanClass.getAnnotation(DoPrivileged.class);
  while ((annotation == null) && (beanClass=beanClass.getSuperclass()) != null) {
    annotation=beanClass.getAnnotation(DoPrivileged.class);
  }
  if (annotation != null) {
    return true;
  }
  return false;
}

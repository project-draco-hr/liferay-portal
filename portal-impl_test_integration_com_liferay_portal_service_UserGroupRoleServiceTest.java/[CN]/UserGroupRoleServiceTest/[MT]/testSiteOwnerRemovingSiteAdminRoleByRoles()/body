{
  Group site=ServiceTestUtil.addGroup(0,"site");
  User siteOwnerOperator=addGroupOwnerUser(site);
  User siteAdminUpdated=addGroupAdminUser(site);
  Role siteAdminRole=RoleLocalServiceUtil.getRole(TestPropsValues.getCompanyId(),RoleConstants.SITE_ADMINISTRATOR);
  deleteUserGroupRoleByRoles(site.getGroupId(),siteAdminRole.getRoleId(),siteOwnerOperator,siteAdminUpdated);
  Assert.assertFalse(UserGroupRoleLocalServiceUtil.hasUserGroupRole(siteAdminUpdated.getUserId(),site.getGroupId(),siteAdminRole.getRoleId()));
}

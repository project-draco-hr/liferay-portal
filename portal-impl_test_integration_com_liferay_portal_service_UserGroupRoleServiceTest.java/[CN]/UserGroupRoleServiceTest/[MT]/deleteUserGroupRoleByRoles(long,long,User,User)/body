{
  PermissionChecker permissionChecker=PermissionCheckerFactoryUtil.create(operatorUser);
  PermissionThreadLocal.setPermissionChecker(permissionChecker);
  long[] roleIds={roleId};
  UserGroupRoleServiceUtil.deleteUserGroupRoles(updatedUser.getUserId(),groupId,roleIds);
}

{
  try {
    createNpmrcFile();
    createPackageJsonFile();
    super.executeNode();
  }
  finally {
    Project project=getProject();
    project.delete(getNpmrcFile(),getPackageJsonFile());
  }
}

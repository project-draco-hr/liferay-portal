{
  for (  Map.Entry<String,ObjectValuePair<Servlet,Long>> entry : _servlets.entrySet()) {
    if (servlet == entry.getValue().getKey()) {
      _servlets.remove(entry.getKey());
      break;
    }
  }
}

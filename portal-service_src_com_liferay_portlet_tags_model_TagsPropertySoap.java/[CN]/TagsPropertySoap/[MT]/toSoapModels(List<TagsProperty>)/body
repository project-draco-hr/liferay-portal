{
  List<TagsPropertySoap> soapModels=new ArrayList<TagsPropertySoap>(models.size());
  for (  TagsProperty model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new TagsPropertySoap[soapModels.size()]);
}

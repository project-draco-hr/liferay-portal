{
  if (Validator.isNull(_provider)) {
    ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
    if (Validator.isNull(_provider)) {
      _provider=_getMapProviderKey(themeDisplay.getCompanyId(),themeDisplay.getSiteGroupId());
    }
  }
}

{
  for (int i=0; i < cmdLine.length; i++) {
    if (cmdLine[i].trim().length() == 0) {
      cmdLine[i]=StringPool.UNDERLINE;
    }
  }
  try {
    Future<?> future=ProcessUtil.execute(ProcessUtil.LOGGING_OUTPUT_PROCESSOR,cmdLine);
    future.get();
  }
 catch (  Exception e) {
    _log.error(e);
  }
}

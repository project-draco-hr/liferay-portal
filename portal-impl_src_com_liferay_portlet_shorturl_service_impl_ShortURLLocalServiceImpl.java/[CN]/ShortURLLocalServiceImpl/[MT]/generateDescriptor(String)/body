{
  if (Validator.isNull(description)) {
    return "";
  }
  String descriptor=HtmlUtil.escapeURL(description.replaceAll(" ","-"));
  if (!descriptor.startsWith("/")) {
    return "/" + descriptor;
  }
 else {
    return descriptor;
  }
}

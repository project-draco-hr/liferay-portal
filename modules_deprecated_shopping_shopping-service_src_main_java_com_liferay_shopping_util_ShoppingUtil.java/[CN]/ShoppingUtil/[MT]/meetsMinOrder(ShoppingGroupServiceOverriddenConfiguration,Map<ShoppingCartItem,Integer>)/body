{
  if ((shoppingGroupServiceOverriddenConfiguration.getMinOrder() > 0) && (calculateSubtotal(items) < shoppingGroupServiceOverriddenConfiguration.getMinOrder())) {
    return false;
  }
 else {
    return true;
  }
}

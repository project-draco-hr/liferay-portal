{
  WorkflowLog workflowLog=getWorkflowLog(workflowTask.getWorkflowInstanceId());
  if (workflowLog == null) {
    return LanguageUtil.get(_liferayPortletRequest.getHttpServletRequest(),_NEVER);
  }
 else {
    return _dateFormatDateTime.format(workflowLog.getCreateDate());
  }
}

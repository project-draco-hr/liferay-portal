{
  DDLRecordSetPermission.check(getPermissionChecker(),recordSetId,ActionKeys.UPDATE);
  return ddlRecordSetLocalService.updateRecordSet(groupId,ddmStructureId,recordSetId,nameMap,descriptionMap,minDisplayRows,serviceContext);
}

{
  DDLPermission.check(getPermissionChecker(),groupId,ActionKeys.ADD_RECORD_SET);
  return ddlRecordSetLocalService.addRecordSet(getUserId(),groupId,ddmStructureId,recordSetKey,autoRecordSetKey,nameMap,description,serviceContext);
}

{
  if (_log.isDebugEnabled()) {
    _log.debug("Remote node executed");
  }
  ServerSocket serverSocket=createServerSocket(_START_PORT);
  EhcacheStreamServerThread ehcacheStreamServerThread=new EhcacheStreamServerThread(serverSocket,cacheName);
  ehcacheStreamServerThread.start();
  return serverSocket.getLocalSocketAddress();
}

{
  _serverSocket=serverSocket;
  _portalCacheManager=portalCacheManager;
  _cacheNames=cacheNames;
  setDaemon(true);
  setName(EhcacheStreamServerThread.class.getName() + " - " + cacheNames);
  setPriority(Thread.NORM_PRIORITY);
}

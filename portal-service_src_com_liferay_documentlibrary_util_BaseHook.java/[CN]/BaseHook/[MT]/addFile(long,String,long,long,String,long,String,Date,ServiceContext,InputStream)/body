{
  HookWriterThread writer=new HookWriterThread(this);
  writer.addFile(companyId,portletId,groupId,repositoryId,fileName,fileEntryId,properties,modifiedDate,serviceContext,is);
  Thread thread=new Thread(writer);
  thread.start();
}

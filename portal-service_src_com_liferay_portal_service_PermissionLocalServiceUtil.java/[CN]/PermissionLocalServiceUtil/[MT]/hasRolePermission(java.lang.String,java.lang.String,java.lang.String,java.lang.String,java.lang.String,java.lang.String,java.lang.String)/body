{
  PermissionLocalService permissionLocalService=PermissionLocalServiceFactory.getService();
  return permissionLocalService.hasRolePermission(roleId,companyId,name,typeId,scope,primKey,actionId);
}

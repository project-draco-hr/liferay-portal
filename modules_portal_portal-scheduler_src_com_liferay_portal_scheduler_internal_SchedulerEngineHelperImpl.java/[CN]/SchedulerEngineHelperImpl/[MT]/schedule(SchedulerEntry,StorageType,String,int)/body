{
  Message message=new Message();
  message.put(SchedulerEngine.MESSAGE_LISTENER,schedulerEntry.getEventMessageListener());
  message.put(SchedulerEngine.PORTLET_ID,portletId);
  schedule(schedulerEntry.getTrigger(),storageType,schedulerEntry.getDescription(),DestinationNames.SCHEDULER_DISPATCH,message,exceptionsMaxSize);
}

{
  List<Long> folderIds=dlFolderService.getFolderIds(groupId,rootFolderId);
  if (folderIds.size() == 0) {
    return 0;
  }
  return dlFileEntryFinder.countByG_U_F_M(groupId,userId,folderIds,mimeTypes,new QueryDefinition(status));
}

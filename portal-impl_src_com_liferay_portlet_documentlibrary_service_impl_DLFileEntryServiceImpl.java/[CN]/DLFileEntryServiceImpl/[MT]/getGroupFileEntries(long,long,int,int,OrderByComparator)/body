{
  if (userId <= 0) {
    return dlFileEntryPersistence.filterFindByGroupId(groupId,start,end,obc);
  }
 else {
    return dlFileEntryPersistence.filterFindByG_U(groupId,userId,start,end,obc);
  }
}

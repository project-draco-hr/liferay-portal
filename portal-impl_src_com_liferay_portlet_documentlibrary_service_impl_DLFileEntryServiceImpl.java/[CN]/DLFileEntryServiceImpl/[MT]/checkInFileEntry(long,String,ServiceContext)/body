{
  try {
    if (!hasFileEntryLock(fileEntryId)) {
      throw new PrincipalException();
    }
  }
 catch (  NoSuchFileEntryException nsfee) {
  }
  dlFileEntryLocalService.checkInFileEntry(getUserId(),fileEntryId,lockUuid,serviceContext);
}

{
  String lockId=DLUtil.getLockId(folderId,name);
  boolean hasLock=lockService.hasLock(DLFileEntry.class.getName(),lockId,getUserId());
  if (!hasLock) {
    hasLock=dlFolderService.hasInheritableLock(folderId);
  }
  return hasLock;
}

{
  runSQL("update ResourceCode set name = '" + newCodeName + "' where"+ " name = '"+ oldCodeName+ "';");
  runSQL("update ResourceAction set name = '" + newCodeName + "' where"+ " name = '"+ oldCodeName+ "';");
  runSQL("update ResourcePermission set name = '" + newCodeName + "' where"+ " name = '"+ oldCodeName+ "';");
}

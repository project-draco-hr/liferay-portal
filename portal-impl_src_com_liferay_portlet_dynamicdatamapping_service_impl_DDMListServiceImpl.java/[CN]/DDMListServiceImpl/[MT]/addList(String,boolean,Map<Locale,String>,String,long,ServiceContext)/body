{
  DDMPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_LIST);
  return ddmListLocalService.addList(getUserId(),listKey,autoListKey,nameMap,description,structureId,serviceContext);
}

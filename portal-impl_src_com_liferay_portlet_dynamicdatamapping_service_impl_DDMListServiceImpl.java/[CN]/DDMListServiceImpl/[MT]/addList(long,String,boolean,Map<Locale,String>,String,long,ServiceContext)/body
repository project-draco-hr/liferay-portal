{
  DDMPermission.check(getPermissionChecker(),groupId,ActionKeys.ADD_LIST);
  return ddmListLocalService.addList(getUserId(),groupId,listKey,autoListKey,nameMap,description,structureId,serviceContext);
}

{
  DDMPermission.check(getPermissionChecker(),groupId,ActionKeys.ADD_LIST);
  return ddmListLocalService.addList(getUserId(),groupId,structureId,listKey,autoListKey,nameMap,description,serviceContext);
}

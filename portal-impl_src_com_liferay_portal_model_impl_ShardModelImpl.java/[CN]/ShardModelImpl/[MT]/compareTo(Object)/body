{
  if (obj == null) {
    return -1;
  }
  ShardImpl shard=(ShardImpl)obj;
  long pk=shard.getPrimaryKey();
  if (getPrimaryKey() < pk) {
    return -1;
  }
 else   if (getPrimaryKey() > pk) {
    return 1;
  }
 else {
    return 0;
  }
}

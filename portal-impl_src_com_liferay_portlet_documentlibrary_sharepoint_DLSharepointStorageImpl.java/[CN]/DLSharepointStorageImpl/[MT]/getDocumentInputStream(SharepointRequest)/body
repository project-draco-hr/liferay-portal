{
  DLFileEntry fileEntry=getFileEntry(sharepointRequest);
  return DLFileEntryLocalServiceUtil.getFileAsStream(sharepointRequest.getCompanyId(),sharepointRequest.getUserId(),fileEntry.getGroupId(),fileEntry.getFolderId(),fileEntry.getName());
}

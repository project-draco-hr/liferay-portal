{
  DDMTemplate template=_ddmTemplateLocalService.fetchDDMTemplate(GetterUtil.getLong(primKey));
  if (template == null) {
    return;
  }
  template.setModifiedDate(new Date());
  _ddmTemplateLocalService.updateDDMTemplate(template);
}

{
  Session session=null;
  try {
    session=openSession();
    TagsVocabulary tagsVocabulary=(TagsVocabulary)session.get(TagsVocabularyImpl.class,new Long(vocabularyId));
    if (tagsVocabulary == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No TagsVocabulary exists with the primary key " + vocabularyId);
      }
      throw new NoSuchVocabularyException("No TagsVocabulary exists with the primary key " + vocabularyId);
    }
    return remove(tagsVocabulary);
  }
 catch (  NoSuchVocabularyException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

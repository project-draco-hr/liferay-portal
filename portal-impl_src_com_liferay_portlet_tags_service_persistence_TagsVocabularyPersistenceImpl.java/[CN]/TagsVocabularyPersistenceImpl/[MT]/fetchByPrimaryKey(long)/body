{
  Session session=null;
  try {
    session=openSession();
    return (TagsVocabulary)session.get(TagsVocabularyImpl.class,new Long(vocabularyId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

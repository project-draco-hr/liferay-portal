{
  TagsVocabulary result=(TagsVocabulary)EntityCacheUtil.getResult(TagsVocabularyModelImpl.ENTITY_CACHE_ENABLED,TagsVocabulary.class,vocabularyId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      TagsVocabulary tagsVocabulary=(TagsVocabulary)session.get(TagsVocabularyImpl.class,new Long(vocabularyId));
      cacheResult(tagsVocabulary);
      return tagsVocabulary;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (TagsVocabulary)result;
  }
}

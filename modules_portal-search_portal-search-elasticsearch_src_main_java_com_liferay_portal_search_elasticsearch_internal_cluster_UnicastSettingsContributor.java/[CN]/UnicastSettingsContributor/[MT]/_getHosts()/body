{
  String[] hosts=_clusterSettingsContext.getHosts();
  String port=elasticsearchConfiguration.discoveryZenPingUnicastHostsPort();
  port=CharPool.COLON + port;
  for (int i=0; i < hosts.length; i++) {
    hosts[i]=hosts[i] + port;
  }
  return hosts;
}

{
  String column=null;
  if (col == null) {
    column="null";
  }
 else   if (col instanceof Boolean) {
    org.hibernate.type.BooleanType booleanType=new org.hibernate.type.BooleanType();
    String s=booleanType.objectToSQLString(col,HibernateUtil.getDialect());
    column=StringPool.APOSTROPHE + s + StringPool.APOSTROPHE;
  }
 else {
    column=StringPool.APOSTROPHE + col + StringPool.APOSTROPHE;
  }
  sb.append(column);
  if (last) {
    sb.append(")" + StringPool.NEW_LINE);
  }
 else {
    sb.append(StringPool.COMMA + StringPool.SPACE);
  }
}

{
  try {
    selenium.connectToEmailAccount(TestPropsValues.EMAIL_ADDRESS_1,TestPropsValues.EMAIL_PASSWORD_1);
    selenium.sendEmail("ThisIsNotCorrectEmailAddress","Email Test","This is a test message.");
  }
 catch (  Throwable t) {
    assertEquals(t.getMessage(),"Invalid Addresses");
  }
}

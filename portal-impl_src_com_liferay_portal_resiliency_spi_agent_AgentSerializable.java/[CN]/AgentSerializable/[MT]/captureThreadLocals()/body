{
  _threadLocalDistributors=ThreadLocalDistributorRegistry.getThreadLocalDistributors();
  for (  ThreadLocalDistributor threadLocalDistributor : _threadLocalDistributors) {
    threadLocalDistributor.capture();
  }
}

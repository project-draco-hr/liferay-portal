{
  if (_documentStarted) {
    return _reader.read(chars,offset,length);
  }
  int read=0;
  for (read=0; read < length; read++) {
    int c=read();
    if (c == -1) {
      if (read == 0) {
        return -1;
      }
      return read;
    }
    chars[offset + read]=(char)c;
  }
  return read;
}

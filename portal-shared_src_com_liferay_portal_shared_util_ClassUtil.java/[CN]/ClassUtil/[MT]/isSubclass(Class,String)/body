{
  if (a == null || s == null) {
    return false;
  }
  if (a.getName().equals(s)) {
    return true;
  }
  for (Class x=a; x != null; x=x.getSuperclass()) {
    if (x.getClass().getName().equals(s)) {
      return true;
    }
  }
  return false;
}

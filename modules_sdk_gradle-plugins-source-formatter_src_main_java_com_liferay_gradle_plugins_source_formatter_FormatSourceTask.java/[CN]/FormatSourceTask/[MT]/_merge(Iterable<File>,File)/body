{
  StringBuilder sb=new StringBuilder();
  int i=0;
  for (  File file : files) {
    if (i > 0) {
      sb.append(",");
    }
    sb.append(FileUtil.relativize(file,startFile));
    i++;
  }
  return sb.toString();
}

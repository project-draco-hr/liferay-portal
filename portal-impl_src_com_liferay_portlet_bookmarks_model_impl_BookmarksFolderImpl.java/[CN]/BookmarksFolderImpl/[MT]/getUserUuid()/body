{
  if (_userUuid != null) {
    return _userUuid;
  }
 else {
    try {
      User user=UserUtil.findByPrimaryKey(getUserId());
      return user.getUuid();
    }
 catch (    NoSuchUserException e) {
      return StringPool.BLANK;
    }
  }
}

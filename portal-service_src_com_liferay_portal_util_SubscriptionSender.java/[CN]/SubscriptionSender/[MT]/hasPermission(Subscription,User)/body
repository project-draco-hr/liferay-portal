{
  PermissionChecker permissionChecker=PermissionCheckerFactoryUtil.create(user,true);
  return SubscriptionPermissionUtil.contains(permissionChecker,subscription.getClassName(),subscription.getClassPK());
}

{
  String className=subscription.getClassName();
  long classPK=subscription.getClassPK();
  try {
    return SubscriptionPermissionUtil.contains(user,className,classPK,ActionKeys.SUBSCRIBE);
  }
 catch (  Exception e) {
    return false;
  }
}

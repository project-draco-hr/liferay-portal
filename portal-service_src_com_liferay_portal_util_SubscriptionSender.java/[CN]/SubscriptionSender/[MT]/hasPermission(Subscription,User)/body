{
  String className=subscription.getClassName();
  long classPK=subscription.getClassPK();
  try {
    return SubscriptionPermissionUtil.contains(user,className,classPK);
  }
 catch (  Exception e) {
    return false;
  }
}

{
  try {
    int count=JournalStructureLocalServiceUtil.searchCount(_companyId,_groupId,_structureId,_name,_description,_andOperator);
    pageContext.setAttribute(_var,count);
    return SKIP_BODY;
  }
 catch (  Exception e) {
    throw new JspException(e);
  }
}

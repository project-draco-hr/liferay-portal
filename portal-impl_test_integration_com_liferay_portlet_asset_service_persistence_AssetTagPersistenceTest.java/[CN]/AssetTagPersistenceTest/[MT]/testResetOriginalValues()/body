{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  AssetTag newAssetTag=addAssetTag();
  _persistence.clearCache();
  AssetTagModelImpl existingAssetTagModelImpl=(AssetTagModelImpl)_persistence.findByPrimaryKey(newAssetTag.getPrimaryKey());
  Assert.assertTrue(Validator.equals(existingAssetTagModelImpl.getUuid(),existingAssetTagModelImpl.getOriginalUuid()));
  Assert.assertEquals(existingAssetTagModelImpl.getGroupId(),existingAssetTagModelImpl.getOriginalGroupId());
  Assert.assertEquals(existingAssetTagModelImpl.getGroupId(),existingAssetTagModelImpl.getOriginalGroupId());
  Assert.assertTrue(Validator.equals(existingAssetTagModelImpl.getName(),existingAssetTagModelImpl.getOriginalName()));
}

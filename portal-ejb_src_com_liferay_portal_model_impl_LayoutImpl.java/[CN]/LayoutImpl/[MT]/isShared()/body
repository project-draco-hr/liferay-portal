{
  if (!_privateLayout) {
    return true;
  }
 else {
    Group group=getGroup();
    String className=group.getClassName();
    if (className.equals(User.class.getName())) {
      return false;
    }
 else {
      return true;
    }
  }
}

{
  if ((ownerId != null) && (ownerId.startsWith(PRIVATE) || ownerId.startsWith(PUBLIC))) {
    return GetterUtil.getLong(ownerId.substring(PRIVATE.length(),ownerId.length()));
  }
 else {
    return 0;
  }
}

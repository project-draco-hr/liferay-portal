{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(sourceId));
  }
  com.liferay.portlet.tags.model.TagsSource tagsSource=getPersistence().remove(sourceId);
  if (listener != null) {
    listener.onAfterRemove(tagsSource);
  }
  return tagsSource;
}

{
  try {
    if (PropsValues.CLUSTER_LINK_ENABLED) {
      _memeorySchedulerClusterEventListener=new MemorySchedulerClusterEventListener();
      ClusterExecutorUtil.addClusterEventListener(_memeorySchedulerClusterEventListener);
      lockMemorySchedulerCluster(false,null);
    }
  }
 catch (  Exception e) {
    throw new SchedulerException("Unable to start scheduler",e);
  }
  _schedulerEngine.start();
}

{
  List plugins=findPluginsByGroupIdAndArtifactId(groupId,artifactId);
  if (plugins == null) {
    plugins=new ArrayList();
    _groupAndArtifactIndex.put(groupId + StringPool.SLASH + artifactId,plugins);
  }
  plugins.add(pluginPackage);
}

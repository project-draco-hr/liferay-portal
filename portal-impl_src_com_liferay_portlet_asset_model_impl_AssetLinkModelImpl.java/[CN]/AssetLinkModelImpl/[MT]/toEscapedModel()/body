{
  if (isEscapedModel()) {
    return (AssetLink)this;
  }
 else {
    AssetLink model=new AssetLinkImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setLinkId(getLinkId());
    model.setCompanyId(getCompanyId());
    model.setUserId(getUserId());
    model.setUserName(HtmlUtil.escape(getUserName()));
    model.setModifiedDate(getModifiedDate());
    model.setEntryId1(getEntryId1());
    model.setEntryId2(getEntryId2());
    model.setTypeId(getTypeId());
    model.setWeight(getWeight());
    model=(AssetLink)Proxy.newProxyInstance(AssetLink.class.getClassLoader(),new Class[]{AssetLink.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

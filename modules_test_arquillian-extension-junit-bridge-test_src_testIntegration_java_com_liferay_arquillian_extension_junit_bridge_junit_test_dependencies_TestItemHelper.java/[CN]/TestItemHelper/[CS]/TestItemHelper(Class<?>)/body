{
  _path=Paths.get(System.getProperty("user.home"),clazz.getName() + ".log");
  try {
    Files.deleteIfExists(_path);
  }
 catch (  IOException ioe) {
    throw new UncheckedIOException(ioe);
  }
}

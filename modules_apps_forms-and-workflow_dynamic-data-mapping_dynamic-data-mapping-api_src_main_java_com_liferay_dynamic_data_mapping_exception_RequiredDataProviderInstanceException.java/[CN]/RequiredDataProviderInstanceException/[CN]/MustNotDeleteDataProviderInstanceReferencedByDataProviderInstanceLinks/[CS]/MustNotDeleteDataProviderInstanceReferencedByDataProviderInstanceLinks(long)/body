{
  super(String.format("Data provider instance %s cannot be deleted because it " + "is referenced by one or more data provider instance " + "links",dataProviderInstanceId));
  this.dataProviderInstanceId=dataProviderInstanceId;
}

{
  String companyId=PortalUtil.getCompanyId(req);
  ServletContext portalCtx=ctx.getContext(CompanyPropsUtil.get(companyId,PropsUtil.PORTAL_CTX));
  if (portalCtx == null) {
    portalCtx=ctx;
  }
  return portalCtx;
}

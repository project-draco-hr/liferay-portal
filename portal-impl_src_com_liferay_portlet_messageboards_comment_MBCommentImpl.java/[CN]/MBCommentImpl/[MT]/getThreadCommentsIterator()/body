{
  List<MBMessage> messages=_treeWalker.getMessages();
  int[] range=_treeWalker.getChildrenRange(getMessage());
  return new MBCommentIterator(messages,range[0],range[1],_treeWalker,_pathThemeImages);
}

{
  List<SchedulerResponse> schedulerResponses=_getScheduledJobs(groupName,jobType);
  for (  SchedulerResponse schedulerResponse : schedulerResponses) {
    _unregisterMessageListener(schedulerResponse);
  }
}

{
  SchedulerResponse schedulerResponse=_schedulerEngine.getScheduledJob(jobName,groupName);
  if ((schedulerResponse.getGroupName() == null) && (schedulerResponse.getJobName() == null) && (schedulerResponse.getTrigger() == null)) {
    return null;
  }
  return schedulerResponse;
}

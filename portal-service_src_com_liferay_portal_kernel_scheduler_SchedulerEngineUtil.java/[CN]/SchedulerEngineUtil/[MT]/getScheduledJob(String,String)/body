{
  SchedulerRequest schedulerRequest=_instance._getScheduledJob(jobName,groupName);
  if (schedulerRequest.getTrigger() == null && schedulerRequest.getJobName() == null && schedulerRequest.getGroupName() == null) {
    return null;
  }
  return schedulerRequest;
}

{
  SchedulerRequest schedulerRequest=_schedulerEngine.getScheduledJob(jobName,groupName);
  if ((schedulerRequest.getGroupName() == null) && (schedulerRequest.getJobName() == null) && (schedulerRequest.getTrigger() == null)) {
    return null;
  }
  return schedulerRequest;
}

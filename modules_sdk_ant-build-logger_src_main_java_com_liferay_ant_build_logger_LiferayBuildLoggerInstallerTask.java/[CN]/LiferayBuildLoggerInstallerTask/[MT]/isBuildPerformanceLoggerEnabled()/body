{
  Project project=getProject();
  Object buildPerformanceLoggerEnabled=project.getProperty(BUILD_PERFORMANCE_LOGGER_ENABLED);
  if ((buildPerformanceLoggerEnabled != null) && buildPerformanceLoggerEnabled.equals("true")) {
    return true;
  }
  return false;
}

{
  Project currentProject=getProject();
  Object buildPerformanceLoggerEnabled=currentProject.getProperty(BUILD_PERFORMANCE_LOGGER_ENABLED);
  if (buildPerformanceLoggerEnabled.equals("true")) {
    return true;
  }
 else {
    return false;
  }
}

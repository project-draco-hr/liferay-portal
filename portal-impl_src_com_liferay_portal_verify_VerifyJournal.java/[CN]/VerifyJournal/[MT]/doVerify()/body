{
  verifyContent();
  verifyCreateAndModifiedDates();
  verifyDynamicElements();
  updateFolderAssets();
  verifyOracleNewLine();
  verifyAssets();
  verifyPermissionsAndAssets();
  verifySearch();
  verifyTree();
  verifyURLTitle();
}

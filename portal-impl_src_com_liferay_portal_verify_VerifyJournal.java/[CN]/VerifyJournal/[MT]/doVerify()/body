{
  verifyContent();
  verifyDynamicElement();
  verifyCreateAndModifiedDates();
  updateFolderAssets();
  verifyOracleNewLine();
  verifyPermissionsAndAssets();
  verifySearch();
  verifyTree();
  verifyURLTitle();
}

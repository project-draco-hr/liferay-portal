{
  CalendarPermission.check(getPermissionChecker(),calendarId,CalendarActionKeys.UPDATE);
  return calendarLocalService.updateCalendar(calendarId,nameMap,descriptionMap,timeZoneId,color,defaultCalendar,enableComments,enableRatings,serviceContext);
}

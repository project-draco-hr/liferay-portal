{
  Session session=null;
  try {
    session=openSession();
    return (MBMailingList)session.get(MBMailingListImpl.class,new Long(mailingListId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBMailingList mbMailingList) method. Use update(MBMailingList mbMailingList, boolean merge) instead.");
  }
  return update(mbMailingList,false);
}

{
  try {
    DDMStorageLink ddmStorageLink=DDMStorageLinkLocalServiceUtil.getStorageLinkByClassPK(classPK);
    return getStorageAdapter(ddmStorageLink.getStorageType());
  }
 catch (  StorageException se) {
    throw se;
  }
catch (  Exception e) {
    throw new StorageException(e);
  }
}

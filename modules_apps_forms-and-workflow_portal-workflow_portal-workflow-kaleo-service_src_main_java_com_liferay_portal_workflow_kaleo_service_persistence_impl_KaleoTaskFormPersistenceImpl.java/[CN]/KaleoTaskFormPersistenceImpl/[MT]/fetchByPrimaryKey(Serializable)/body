{
  Serializable serializable=entityCache.getResult(KaleoTaskFormModelImpl.ENTITY_CACHE_ENABLED,KaleoTaskFormImpl.class,primaryKey);
  if (serializable == nullModel) {
    return null;
  }
  KaleoTaskForm kaleoTaskForm=(KaleoTaskForm)serializable;
  if (kaleoTaskForm == null) {
    Session session=null;
    try {
      session=openSession();
      kaleoTaskForm=(KaleoTaskForm)session.get(KaleoTaskFormImpl.class,primaryKey);
      if (kaleoTaskForm != null) {
        cacheResult(kaleoTaskForm);
      }
 else {
        entityCache.putResult(KaleoTaskFormModelImpl.ENTITY_CACHE_ENABLED,KaleoTaskFormImpl.class,primaryKey,nullModel);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(KaleoTaskFormModelImpl.ENTITY_CACHE_ENABLED,KaleoTaskFormImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return kaleoTaskForm;
}

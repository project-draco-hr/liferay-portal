{
  if (!_locksMap.containsKey(getPrimaryKeyString(classObj,key)) && LockLocalServiceUtil.isLocked(classObj.getName(),key)) {
    Lock lock=LockLocalServiceUtil.getLock(classObj.getName(),key);
    addLocks(classObj.getName(),key,lock);
  }
}

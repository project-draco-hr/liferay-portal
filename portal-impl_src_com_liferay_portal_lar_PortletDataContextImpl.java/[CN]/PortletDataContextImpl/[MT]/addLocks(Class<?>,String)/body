{
  if (!_locksMap.containsKey(getPrimaryKeyString(clazz,key)) && LockHelperUtil.isLocked(clazz.getName(),key)) {
    Lock lock=LockHelperUtil.getLock(clazz.getName(),key);
    addLocks(clazz.getName(),key,lock);
  }
}

{
  List<Element> referenceElements=getReferenceElements(parentElement,clazz,groupId,uuid,0,referenceType);
  if (!referenceElements.isEmpty()) {
    return referenceElements.get(0);
  }
  return null;
}

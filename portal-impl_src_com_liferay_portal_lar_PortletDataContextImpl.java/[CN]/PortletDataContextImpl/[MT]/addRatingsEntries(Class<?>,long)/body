{
  List<RatingsEntry> ratingsEntries=RatingsEntryLocalServiceUtil.getEntries(clazz.getName(),classPK);
  if (ratingsEntries.size() == 0) {
    return;
  }
  _ratingsEntriesMap.put(getPrimaryKeyString(clazz,classPK),ratingsEntries);
}

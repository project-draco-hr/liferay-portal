{
  for (  DeletionEntry deletionEntry : deletionEntries) {
    String path=getDeletionEntryPath(deletionEntry.getClassNameId(),deletionEntry.getClassPK());
    addZipEntry(path,deletionEntry);
    DeletionEntryLocalServiceUtil.deleteDeletionEntry(deletionEntry.getEntryId());
  }
}

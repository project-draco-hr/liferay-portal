{
  Object object=fromXML(getZipEntryAsString(path));
  Element classNameElement=element.element("class-name");
  if (classNameElement != null) {
    BeanPropertiesUtil.setProperty(object,"className",classNameElement.getText());
  }
  return object;
}

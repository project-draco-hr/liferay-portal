{
  List<Element> referenceElements=getReferenceElements(parentElement,clazz,groupId,uuid,classPK,referenceType);
  if (!referenceElements.isEmpty()) {
    return referenceElements.get(0);
  }
  return null;
}

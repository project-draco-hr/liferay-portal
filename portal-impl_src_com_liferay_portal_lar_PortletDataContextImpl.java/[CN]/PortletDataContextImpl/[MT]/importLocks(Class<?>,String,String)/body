{
  Lock lock=_locksMap.get(getPrimaryKeyString(classObj,key));
  if (lock == null) {
    return;
  }
  long userId=getUserId(lock.getUserUuid());
  LockLocalServiceUtil.lock(userId,classObj.getName(),newKey,lock.getOwner(),lock.getInheritable(),lock.getExpirationDate().getTime());
}

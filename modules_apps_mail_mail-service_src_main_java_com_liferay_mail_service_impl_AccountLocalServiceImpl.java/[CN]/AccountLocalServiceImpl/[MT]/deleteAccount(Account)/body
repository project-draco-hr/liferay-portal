{
  accountPersistence.remove(account);
  folderLocalService.deleteFolders(account.getAccountId());
  Indexer<Account> indexer=IndexerRegistryUtil.getIndexer(Account.class);
  indexer.delete(account);
  return account;
}

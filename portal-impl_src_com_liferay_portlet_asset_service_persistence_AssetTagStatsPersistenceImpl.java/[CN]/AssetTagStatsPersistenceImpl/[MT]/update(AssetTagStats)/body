{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetTagStats assetTagStats) method. Use update(AssetTagStats assetTagStats, boolean merge) instead.");
  }
  return update(assetTagStats,false);
}

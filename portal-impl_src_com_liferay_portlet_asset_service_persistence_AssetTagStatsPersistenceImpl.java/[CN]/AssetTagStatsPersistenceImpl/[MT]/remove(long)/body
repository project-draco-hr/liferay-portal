{
  Session session=null;
  try {
    session=openSession();
    AssetTagStats assetTagStats=(AssetTagStats)session.get(AssetTagStatsImpl.class,new Long(tagStatsId));
    if (assetTagStats == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No AssetTagStats exists with the primary key " + tagStatsId);
      }
      throw new NoSuchTagStatsException("No AssetTagStats exists with the primary key " + tagStatsId);
    }
    return remove(assetTagStats);
  }
 catch (  NoSuchTagStatsException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

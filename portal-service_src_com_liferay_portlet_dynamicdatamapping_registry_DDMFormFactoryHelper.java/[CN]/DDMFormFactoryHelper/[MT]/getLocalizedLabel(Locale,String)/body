{
  DDMForm ddmForm=_clazz.getAnnotation(DDMForm.class);
  ResourceBundle resourceBundle=getResourceBundle(ddmForm,locale);
  return LanguageUtil.get(resourceBundle,StringUtil.extractLast(label,StringPool.PERCENT));
}

{
  if (_imageSize <= 0) {
    try {
      Image largeImage=ImageLocalUtil.getImage(getLargeImageId());
      _imageSize=largeImage.getSize();
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
  return _imageSize;
}

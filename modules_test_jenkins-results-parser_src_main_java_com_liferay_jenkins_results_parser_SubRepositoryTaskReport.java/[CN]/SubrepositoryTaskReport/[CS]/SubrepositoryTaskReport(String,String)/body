{
  this.buildURL=buildURL;
  this.name=name;
  testReportJSONObject=JenkinsResultsParserUtil.toJSONObject(JenkinsResultsParserUtil.getLocalURL(buildURL + "testReport/api/json"));
  if (testReportJSONObject.getInt("failCount") > 0) {
    result="FAILURE";
  }
 else {
    result="SUCCESS";
  }
}

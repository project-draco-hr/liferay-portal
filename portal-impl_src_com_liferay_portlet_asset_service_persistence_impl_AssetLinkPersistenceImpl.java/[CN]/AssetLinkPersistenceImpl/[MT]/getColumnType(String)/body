{
  Integer type=AssetLinkModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ AssetLinkModelImpl.TABLE_NAME);
  }
  return type;
}

{
  if (Validator.equals(_command,Constants.ADD) || Validator.equals(_command,Constants.ADD_DYNAMIC) || Validator.equals(_command,Constants.ADD_MULTIPLE)|| Validator.equals(_command,Constants.ADD_WEBDAV)) {
    return true;
  }
 else {
    return false;
  }
}

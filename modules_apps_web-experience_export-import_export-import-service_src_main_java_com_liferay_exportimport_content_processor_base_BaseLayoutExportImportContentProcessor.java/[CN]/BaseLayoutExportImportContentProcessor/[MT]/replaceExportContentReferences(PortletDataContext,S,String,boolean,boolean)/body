{
  String processedContent=replaceExportLayoutReferences(portletDataContext,stagedModel,content);
  processedContent=replaceExportLinksToLayouts(portletDataContext,stagedModel,processedContent);
  return processedContent;
}

{
  Copy copy=super.addTaskDeploy(project,liferayExtension);
  copy.rename(new Closure<String>(null){
    @SuppressWarnings("unused") public String doCall(    String fileName){
      return getDeployedFileName(project,fileName);
    }
  }
);
  return copy;
}

{
  Folder folder=DLAppServiceUtil.addFolder(repositoryId,DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,folderName,StringPool.BLANK,new ServiceContext());
  assertNotNull(folder);
  folder=DLAppServiceUtil.getFolder(folder.getFolderId());
  assertNotNull(folder);
  return folder;
}

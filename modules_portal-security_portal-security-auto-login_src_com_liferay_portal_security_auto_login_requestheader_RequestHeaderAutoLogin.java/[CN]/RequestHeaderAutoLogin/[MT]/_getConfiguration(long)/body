{
  try {
    RequestHeaderAutoLoginConfiguration configuration=_settingsFactory.getSettings(RequestHeaderAutoLoginConfiguration.class,new CompanyServiceSettingsLocator(companyId,RequestHeaderAutoLoginConstants.SERVICE_NAME));
    return configuration;
  }
 catch (  SettingsException se) {
    _log.error("Unable to get RequestHeaderAutoLogin configuration",se);
  }
  return null;
}

{
  ResourceLocalServiceUtil.addModelResources(question.getCompanyId(),question.getGroupId(),question.getUserId(),PollsQuestion.class.getName(),question.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

{
  PollsVoteUtil.removeByQuestionId(question.getQuestionId());
  PollsChoiceUtil.removeByQuestionId(question.getQuestionId());
  ResourceLocalServiceUtil.deleteResource(question.getCompanyId(),PollsQuestion.class.getName(),Resource.TYPE_CLASS,Resource.SCOPE_INDIVIDUAL,question.getPrimaryKey().toString());
  PollsQuestionUtil.remove(question.getQuestionId());
}

{
  int count=countByG_DDMTK(groupId,DDMTemplateKey);
  if (count == 0) {
    return null;
  }
  List<JournalArticle> list=findByG_DDMTK(groupId,DDMTemplateKey,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

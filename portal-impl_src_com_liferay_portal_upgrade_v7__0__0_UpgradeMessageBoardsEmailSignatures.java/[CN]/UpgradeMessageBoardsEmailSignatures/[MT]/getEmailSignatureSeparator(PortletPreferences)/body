{
  GroupPortletSettings groupPortletSettings=new GroupPortletSettings(portletPreferences);
  boolean emailHtmlFormat=MBUtil.getEmailHtmlFormat(groupPortletSettings);
  if (emailHtmlFormat) {
    return "<br />--<br />";
  }
 else {
    return "\n--\n";
  }
}

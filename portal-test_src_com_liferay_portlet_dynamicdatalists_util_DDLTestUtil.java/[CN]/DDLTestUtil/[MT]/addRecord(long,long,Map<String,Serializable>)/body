{
  long userId=TestPropsValues.getUserId();
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext(groupId);
  return DDLRecordLocalServiceUtil.addRecord(userId,groupId,recordSetId,DDLRecordConstants.DISPLAY_INDEX_DEFAULT,fieldsMap,serviceContext);
}

{
  if (isEscapedModel()) {
    return (MBMessageFlag)this;
  }
 else {
    return (MBMessageFlag)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (MBMessageFlag)this;
  }
 else {
    return (MBMessageFlag)Proxy.newProxyInstance(MBMessageFlag.class.getClassLoader(),new Class[]{MBMessageFlag.class},new AutoEscapeBeanHandler(this));
  }
}

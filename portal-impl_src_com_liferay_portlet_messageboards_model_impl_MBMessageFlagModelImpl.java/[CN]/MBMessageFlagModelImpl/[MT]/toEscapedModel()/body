{
  if (isEscapedModel()) {
    return (MBMessageFlag)this;
  }
 else {
    MBMessageFlag model=new MBMessageFlagImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setMessageFlagId(getMessageFlagId());
    model.setUserId(getUserId());
    model.setMessageId(getMessageId());
    model.setFlag(getFlag());
    model=(MBMessageFlag)Proxy.newProxyInstance(MBMessageFlag.class.getClassLoader(),new Class[]{MBMessageFlag.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

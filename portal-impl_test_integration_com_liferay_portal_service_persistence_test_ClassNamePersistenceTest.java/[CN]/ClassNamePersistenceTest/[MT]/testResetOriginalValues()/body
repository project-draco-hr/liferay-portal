{
  ClassName newClassName=addClassName();
  _persistence.clearCache();
  ClassName existingClassName=_persistence.findByPrimaryKey(newClassName.getPrimaryKey());
  Assert.assertTrue(Objects.equals(existingClassName.getValue(),ReflectionTestUtil.invoke(existingClassName,"getOriginalValue",new Class<?>[0])));
}

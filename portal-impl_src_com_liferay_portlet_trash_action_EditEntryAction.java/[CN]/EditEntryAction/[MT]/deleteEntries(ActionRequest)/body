{
  long entryId=ParamUtil.getLong(actionRequest,"entryId");
  if (entryId > 0) {
    deleteEntry(entryId);
  }
 else {
    long[] deleteEntryIds=StringUtil.split(ParamUtil.getString(actionRequest,"deleteEntryIds"),0L);
    for (int i=0; i < deleteEntryIds.length; i++) {
      deleteEntry(deleteEntryIds[i]);
    }
  }
}

{
  Props props=mock(Props.class);
  when(props.get(PropsKeys.XML_SECURITY_ENABLED)).thenReturn(Boolean.TRUE.toString());
  when(props.getArray(PropsKeys.XML_SECURITY_WHITELIST)).thenReturn(new String[]{DDMStructureTestUtil.class.getName(),DDMXMLImplTest.class.getName()});
  PropsUtil.setProps(props);
}

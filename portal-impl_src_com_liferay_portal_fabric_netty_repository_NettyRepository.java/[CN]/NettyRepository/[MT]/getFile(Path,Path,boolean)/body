{
  if (localFilePath == null) {
    return getFile(remoteFilePath,RepositoryHelperUtil.getRepositoryFilePath(repositoryPath,remoteFilePath),deleteAfterFetch,true);
  }
  return getFile(remoteFilePath,localFilePath,deleteAfterFetch,false);
}

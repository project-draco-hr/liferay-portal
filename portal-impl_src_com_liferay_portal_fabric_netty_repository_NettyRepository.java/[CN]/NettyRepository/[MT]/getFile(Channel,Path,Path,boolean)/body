{
  if (localFilePath == null) {
    return getFile(channel,remoteFilePath,RepositoryHelperUtil.getRepositoryFilePath(repositoryPath,remoteFilePath),deleteAfterFetch,true);
  }
  return getFile(channel,remoteFilePath,localFilePath,deleteAfterFetch,false);
}

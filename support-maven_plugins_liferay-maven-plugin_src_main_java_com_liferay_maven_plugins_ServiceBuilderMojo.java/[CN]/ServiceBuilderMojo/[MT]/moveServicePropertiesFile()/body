{
  File servicePropertiesFile=new File(implDir,"service.properties");
  if (servicePropertiesFile.exists()) {
    FileUtil.move(servicePropertiesFile,new File(resourcesDir,"service.properties"));
  }
}

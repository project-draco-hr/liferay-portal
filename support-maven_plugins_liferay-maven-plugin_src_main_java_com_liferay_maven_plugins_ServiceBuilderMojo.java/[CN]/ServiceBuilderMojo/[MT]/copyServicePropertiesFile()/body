{
  File servicePropertiesFile=new File(resourcesDir,"service.properties");
  if (servicePropertiesFile.exists()) {
    FileUtil.copyFile(servicePropertiesFile,new File(implDir,"service.properties"));
  }
}

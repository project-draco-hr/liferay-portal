{
  String emailMessageUpdatedSubject=typedSettings.getValue("emailMessageUpdatedSubject");
  if (Validator.isNotNull(emailMessageUpdatedSubject)) {
    return emailMessageUpdatedSubject;
  }
  return ContentUtil.get(typedSettings.getValue(PropsKeys.MESSAGE_BOARDS_EMAIL_MESSAGE_UPDATED_SUBJECT));
}

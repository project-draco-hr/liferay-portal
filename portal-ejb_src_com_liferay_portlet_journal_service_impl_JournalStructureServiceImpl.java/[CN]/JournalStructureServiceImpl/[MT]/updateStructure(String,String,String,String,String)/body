{
  User user=getUser();
  JournalStructurePermission.check(getPermissionChecker(),user.getCompanyId(),groupId,structureId,ActionKeys.UPDATE);
  return JournalStructureLocalServiceUtil.updateStructure(user.getCompanyId(),groupId,structureId,name,description,xsd);
}

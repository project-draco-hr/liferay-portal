{
  User user=getUser();
  JournalStructurePermission.check(getPermissionChecker(),user.getCompanyId(),structureId,ActionKeys.UPDATE);
  return JournalStructureLocalServiceUtil.updateStructure(user.getCompanyId(),structureId,name,description,xsd);
}

{
  Session session=null;
  try {
    session=openSession();
    return (DLFileRank)session.get(DLFileRankImpl.class,new Long(fileRankId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  for (  DLFileRank dlFileRank : dlFileRanks) {
    if (EntityCacheUtil.getResult(DLFileRankModelImpl.ENTITY_CACHE_ENABLED,DLFileRank.class,dlFileRank.getPrimaryKey(),this) == null) {
      cacheResult(dlFileRank);
    }
  }
}

{
  if (stringResponse.isCalledGetOutputStream()) {
    UnsyncByteArrayOutputStream unsyncByteArrayInputStream=stringResponse.getUnsyncByteArrayOutputStream();
    write(response,unsyncByteArrayInputStream.unsafeGetByteArray(),unsyncByteArrayInputStream.size());
  }
 else {
    write(response,stringResponse.getString());
  }
}

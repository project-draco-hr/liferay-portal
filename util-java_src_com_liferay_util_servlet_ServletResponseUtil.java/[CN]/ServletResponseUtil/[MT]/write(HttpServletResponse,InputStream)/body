{
  OutputStream os=null;
  try {
    if (!res.isCommitted()) {
      os=res.getOutputStream();
      int c=is.read();
      while (c != -1) {
        os.write(c);
        c=is.read();
      }
    }
  }
  finally {
    cleanUp(os,is);
  }
}

{
  int bufferSize=10;
  UnsyncBufferedInputStream ubis=new UnsyncBufferedInputStream(new ByteArrayInputStream(new byte[bufferSize]));
  ubis.close();
  assertTrue(ubis.inputStream == null);
  assertTrue(ubis.buffer == null);
}

{
  try {
    PortletURL viewInContextURL=getURLView(liferayPortletResponse,WindowState.MAXIMIZED);
    return viewInContextURL.toString();
  }
 catch (  Exception e) {
    return noSuchEntryRedirect;
  }
}

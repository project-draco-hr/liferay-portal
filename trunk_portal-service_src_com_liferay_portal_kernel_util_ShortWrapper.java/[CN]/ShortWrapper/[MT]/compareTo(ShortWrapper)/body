{
  if (shortWrapper == null) {
    return 1;
  }
  if (getValue() > shortWrapper.getValue()) {
    return 1;
  }
 else   if (getValue() < shortWrapper.getValue()) {
    return -1;
  }
 else {
    return 0;
  }
}

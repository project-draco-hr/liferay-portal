{
  try {
    return ResourceBundleUtil.getBundle(getResourceBundleBaseName(_clazz),locale,_clazz.getClassLoader());
  }
 catch (  Exception e) {
    return ResourceBundleUtil.getBundle(getResourceBundleBaseName(_clazz),locale,PortalClassLoaderUtil.getClassLoader());
  }
}

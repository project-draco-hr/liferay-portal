{
  for (  String categoryName : _CATEGORY_NAMES) {
    AssetCategory category=AssetCategoryLocalServiceUtil.addCategory(TestPropsValues.getUserId(),categoryName,vocabularyId,ServiceTestUtil.getServiceContext());
    _categoryIds=ArrayUtil.append(_categoryIds,category.getCategoryId());
  }
}

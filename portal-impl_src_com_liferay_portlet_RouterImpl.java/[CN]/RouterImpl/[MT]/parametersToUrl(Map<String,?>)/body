{
  for (  Route route : _routes) {
    String url=route.parametersToUrl(parameters);
    if (url != null) {
      return url;
    }
  }
  throw new RouteNotFoundException("No route could be found to use for parameters " + MapUtil.toString(parameters));
}

{
  if (value == null) {
    return false;
  }
  String prefix=StringPool.DOLLAR + StringPool.OPEN_CURLY_BRACE + protocol+ StringPool.COLON;
  if (value.startsWith(prefix) && value.endsWith(StringPool.CLOSE_CURLY_BRACE)) {
    return true;
  }
  return false;
}

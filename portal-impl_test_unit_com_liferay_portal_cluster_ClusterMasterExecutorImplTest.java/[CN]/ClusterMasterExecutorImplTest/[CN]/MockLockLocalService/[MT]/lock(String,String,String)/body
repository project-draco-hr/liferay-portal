{
  if (_lock == null) {
    Lock lock=new LockImpl();
    lock.setKey(key);
    if (_override) {
      lock.setOwner(_value);
    }
 else {
      lock.setOwner(owner);
    }
    _lock=lock;
  }
  return _lock;
}

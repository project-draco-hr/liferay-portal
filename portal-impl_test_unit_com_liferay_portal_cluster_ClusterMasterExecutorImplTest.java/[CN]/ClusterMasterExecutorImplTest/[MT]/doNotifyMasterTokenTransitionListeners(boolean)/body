{
  ClusterMasterExecutorImpl clusterMasterExecutorImpl=new ClusterMasterExecutorImpl();
  ClusterMasterTokenTransitionListener clusterMasterTokenTransitionListener=null;
  if (acquired) {
    clusterMasterTokenTransitionListener=new MockClusterMasterTokenTransitionListener(){
      @Override public void masterTokenReleased(){
        Assert.fail();
      }
    }
;
  }
 else {
    clusterMasterTokenTransitionListener=new MockClusterMasterTokenTransitionListener(){
      @Override public void masterTokenAcquired(){
        Assert.fail();
      }
    }
;
  }
  clusterMasterExecutorImpl.registerClusterMasterTokenTransitionListener(clusterMasterTokenTransitionListener);
  clusterMasterExecutorImpl.notifyMasterTokenTransitionListeners(acquired);
}

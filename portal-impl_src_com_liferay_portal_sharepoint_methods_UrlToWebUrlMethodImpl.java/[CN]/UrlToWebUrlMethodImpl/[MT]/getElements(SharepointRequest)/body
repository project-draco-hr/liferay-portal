{
  List<ResponseElement> elements=new ArrayList<ResponseElement>();
  String url=sharepointRequest.getParameterValue("url");
  if (_log.isInfoEnabled()) {
    _log.info("URL is " + url);
  }
  if (Validator.isNotNull(url)) {
    elements.add(new Property("webUrl",_SHAREPOINT));
    int webdavPos=url.indexOf(_WEBDAV);
    if (webdavPos > -1) {
      elements.add(new Property("fileUrl",url.substring(webdavPos + _WEBDAV.length())));
    }
 else {
      elements.add(new Property("fileUrl",url.substring(_SHAREPOINT.length())));
    }
  }
  return elements;
}

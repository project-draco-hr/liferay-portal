{
  String portletId=PortletProviderUtil.getPortletId(MDRRule.class.getName(),PortletProvider.Action.EDIT);
  return contains(permissionChecker,MDRConstants.SERVICE_NAME,portletId,classPK,actionId);
}

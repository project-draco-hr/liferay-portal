{
  JournalArticleDisplay articleDisplay=getDisplay(groupId,articleId,templateId,viewMode,languageId,themeDisplay,1,xmlRequest);
  return getContent(articleDisplay,renderResponse,request,response);
}

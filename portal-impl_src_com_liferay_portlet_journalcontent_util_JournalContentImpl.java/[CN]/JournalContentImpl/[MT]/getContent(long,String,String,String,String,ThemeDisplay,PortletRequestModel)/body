{
  JournalArticleDisplay articleDisplay=getDisplay(groupId,articleId,ddmTemplateKey,viewMode,languageId,themeDisplay,1,portletRequestModel);
  if (articleDisplay != null) {
    return articleDisplay.getContent();
  }
 else {
    return null;
  }
}

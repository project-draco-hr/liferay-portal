{
  try {
    if (_TEMPLATE_FILE_NAME.equals(string)) {
      return new ByteArrayInputStream(_TEST_TEMPLATE_CONTENT.getBytes());
    }
    throw new ResourceNotFoundException("Unable to find " + string);
  }
 catch (  Exception e) {
    throw new ResourceNotFoundException(e);
  }
}

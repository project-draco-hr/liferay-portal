{
  if (workflowInstance.getEndDate() == null) {
    return LanguageUtil.get(workflowInstanceRequestHelper.getRequest(),"never");
  }
  return dateFormatDateTime.format(workflowInstance.getEndDate());
}

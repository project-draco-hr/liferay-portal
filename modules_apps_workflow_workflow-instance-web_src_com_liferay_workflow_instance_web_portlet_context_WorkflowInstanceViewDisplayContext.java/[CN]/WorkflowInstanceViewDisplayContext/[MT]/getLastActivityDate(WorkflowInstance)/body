{
  WorkflowLog workflowLog=getWorkflowLog(workflowInstance);
  return (workflowLog == null) ? LanguageUtil.get(_liferayPortletRequest.getHttpServletRequest(),"never") : _dateFormatDateTime.format(workflowLog.getCreateDate());
}

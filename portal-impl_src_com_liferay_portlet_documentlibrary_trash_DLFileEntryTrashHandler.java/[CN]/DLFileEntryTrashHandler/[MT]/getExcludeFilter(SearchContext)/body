{
  BooleanFilter excludeFilter=new BooleanFilter();
  excludeFilter.addRequiredTerm(Field.ENTRY_CLASS_NAME,DLFileEntryConstants.getClassName());
  excludeFilter.addRequiredTerm(Field.HIDDEN,true);
  return excludeFilter;
}

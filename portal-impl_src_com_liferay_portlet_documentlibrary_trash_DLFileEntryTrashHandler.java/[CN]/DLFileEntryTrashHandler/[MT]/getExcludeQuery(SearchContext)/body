{
  BooleanQuery excludeQuery=BooleanQueryFactoryUtil.create(searchContext);
  excludeQuery.addRequiredTerm(Field.ENTRY_CLASS_NAME,DLFileEntryConstants.getClassName());
  excludeQuery.addRequiredTerm(Field.HIDDEN,true);
  return excludeQuery;
}

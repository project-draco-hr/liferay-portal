{
  long userId=TestPropsValues.getUserId();
  String generatedScreenName=_screenNameGenerator.generate(TestPropsValues.getCompanyId(),userId,"postfix@liferay.com");
  Assert.assertEquals("postfix." + userId,generatedScreenName);
}

{
  String screenName=_screenNameGenerator.generate(TestPropsValues.getCompanyId(),TestPropsValues.getUserId(),"postfix@liferay.com");
  Assert.assertEquals("postfix." + TestPropsValues.getUserId(),screenName);
}

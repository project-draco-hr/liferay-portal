{
  String generatedScreenName=_screenNameGenerator.generate(TestPropsValues.getCompanyId(),TestPropsValues.getUserId(),"test@liferay.com");
  Assert.assertNotSame("test",generatedScreenName);
  Assert.assertEquals("test.1",generatedScreenName);
}

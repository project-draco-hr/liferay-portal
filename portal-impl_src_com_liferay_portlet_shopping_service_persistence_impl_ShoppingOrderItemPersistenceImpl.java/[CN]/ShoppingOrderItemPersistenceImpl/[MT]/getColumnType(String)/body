{
  Integer type=ShoppingOrderItemModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ShoppingOrderItemModelImpl.TABLE_NAME);
  }
  return type;
}

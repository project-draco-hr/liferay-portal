{
  Map<Integer,SocialActivityDefinition> socialActivityDefinitions=_socialActivityDefinitions.get(activityDefinition.getModelName());
  if (socialActivityDefinitions != null) {
    socialActivityDefinitions.remove(activityDefinition.getActivityKey());
  }
}

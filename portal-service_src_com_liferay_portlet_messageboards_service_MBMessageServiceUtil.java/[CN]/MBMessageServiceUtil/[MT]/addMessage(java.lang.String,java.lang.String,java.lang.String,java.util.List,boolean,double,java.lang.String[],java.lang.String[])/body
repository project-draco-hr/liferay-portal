{
  MBMessageService mbMessageService=MBMessageServiceFactory.getService();
  return mbMessageService.addMessage(categoryId,subject,body,files,anonymous,priority,communityPermissions,guestPermissions);
}

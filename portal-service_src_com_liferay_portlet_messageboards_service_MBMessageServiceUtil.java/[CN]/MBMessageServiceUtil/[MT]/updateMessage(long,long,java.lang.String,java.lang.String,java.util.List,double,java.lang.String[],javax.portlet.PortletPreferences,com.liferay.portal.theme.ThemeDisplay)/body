{
  MBMessageService mbMessageService=MBMessageServiceFactory.getService();
  return mbMessageService.updateMessage(messageId,categoryId,subject,body,files,priority,tagsEntries,prefs,themeDisplay);
}

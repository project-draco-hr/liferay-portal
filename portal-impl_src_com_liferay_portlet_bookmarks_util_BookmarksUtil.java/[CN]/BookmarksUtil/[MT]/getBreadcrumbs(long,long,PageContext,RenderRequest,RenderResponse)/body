{
  if (entryId > 0) {
    BookmarksEntry entry=BookmarksEntryLocalServiceUtil.getEntry(entryId);
    return getBreadcrumbs(entry.getFolder(),entry,pageContext,req,res);
  }
 else {
    BookmarksFolder folder=null;
    try {
      folder=BookmarksFolderLocalServiceUtil.getFolder(folderId);
    }
 catch (    Exception e) {
    }
    return getBreadcrumbs(folder,null,pageContext,req,res);
  }
}

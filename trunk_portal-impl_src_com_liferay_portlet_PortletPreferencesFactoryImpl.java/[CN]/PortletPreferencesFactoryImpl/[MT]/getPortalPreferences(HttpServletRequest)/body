{
  ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
  return getPortalPreferences(request.getSession(),themeDisplay.getCompanyId(),themeDisplay.getUserId(),themeDisplay.isSignedIn());
}

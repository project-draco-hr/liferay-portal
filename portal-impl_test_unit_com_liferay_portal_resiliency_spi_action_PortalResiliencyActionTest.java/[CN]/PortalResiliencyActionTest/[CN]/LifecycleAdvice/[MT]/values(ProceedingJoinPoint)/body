{
  SPIAgent.Lifecycle[] lifecycles=(SPIAgent.Lifecycle[])proceedingJoinPoint.proceed();
  SPIAgent.Lifecycle[] newLifecycles=new SPIAgent.Lifecycle[lifecycles.length + 1];
  System.arraycopy(lifecycles,0,newLifecycles,0,lifecycles.length);
  int ordinal=newLifecycles.length - 1;
  newLifecycles[ordinal]=ReflectionUtil.newEnumElement(SPIAgent.Lifecycle.class,new Class<?>[]{String.class},"UNKNOWN",ordinal,"UNKNOWN");
  return newLifecycles;
}

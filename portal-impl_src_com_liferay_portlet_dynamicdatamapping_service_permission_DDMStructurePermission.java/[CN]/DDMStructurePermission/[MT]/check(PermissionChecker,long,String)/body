{
  if (!contains(permissionChecker,structureId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDMStructure.class.getName(),structureId,actionId);
  }
}

{
  if (!contains(permissionChecker,structure,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDMStructure.class.getName(),structure.getStructureId(),actionId);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Vocabulary vocabulary) method. Use update(Vocabulary vocabulary, boolean merge) instead.");
  }
  return update(vocabulary,false);
}

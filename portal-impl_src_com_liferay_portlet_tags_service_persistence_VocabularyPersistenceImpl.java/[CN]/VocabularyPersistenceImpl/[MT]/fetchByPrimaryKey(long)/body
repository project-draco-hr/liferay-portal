{
  Session session=null;
  try {
    session=openSession();
    return (Vocabulary)session.get(VocabularyImpl.class,new Long(vocabularyId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

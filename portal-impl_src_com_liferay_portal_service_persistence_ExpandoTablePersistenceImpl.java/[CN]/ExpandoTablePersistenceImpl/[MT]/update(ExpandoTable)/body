{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoTable expandoTable) method. To improve performance, call update(ExpandoTable expandoTable, boolean merge).");
  }
  return update(expandoTable,false);
}

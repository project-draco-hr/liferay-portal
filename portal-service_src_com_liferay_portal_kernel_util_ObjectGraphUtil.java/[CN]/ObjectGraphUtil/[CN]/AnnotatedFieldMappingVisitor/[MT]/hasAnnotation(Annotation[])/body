{
  for (  Annotation annotation : annotations) {
    for (    Class<? extends Annotation> annotationClass : _annotationClasses) {
      if (annotationClass.isInstance(annotation)) {
        return true;
      }
    }
  }
  return false;
}

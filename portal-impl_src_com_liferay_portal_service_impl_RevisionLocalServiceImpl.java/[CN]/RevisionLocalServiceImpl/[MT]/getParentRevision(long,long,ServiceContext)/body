{
  long parentRevisionId=ParamUtil.getLong(serviceContext,"parentRevisionId");
  Revision parentRevision=null;
  if (parentRevisionId > 0) {
    parentRevision=revisionPersistence.findByPrimaryKey(parentRevisionId);
  }
  if (parentRevision == null) {
    List<Revision> revisions=revisionPersistence.findByB_P(branchId,plid,0,1);
    if (!revisions.isEmpty()) {
      parentRevision=revisions.get(0);
    }
  }
  return parentRevision;
}

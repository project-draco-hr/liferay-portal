{
  ThemeDisplay themeDisplay=(ThemeDisplay)liferayPortletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  return getURLAdd(liferayPortletRequest,liferayPortletResponse,themeDisplay.getScopeGroupId(),0);
}

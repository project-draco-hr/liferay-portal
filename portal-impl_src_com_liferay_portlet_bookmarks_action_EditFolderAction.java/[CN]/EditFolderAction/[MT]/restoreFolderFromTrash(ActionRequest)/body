{
  long[] restoreEntryIds=StringUtil.split(ParamUtil.getString(actionRequest,"restoreFolderIds"),0L);
  for (  long restoreEntryId : restoreEntryIds) {
    BookmarksFolderServiceUtil.restoreFolderFromTrash(restoreEntryId);
  }
}

{
  UserGroup userGroup=MembershipPolicyTestUtil.addUserGroup();
  UserGroupServiceUtil.updateUserGroup(userGroup.getUserGroupId(),userGroup.getName(),userGroup.getDescription(),ServiceTestUtil.getServiceContext());
  Assert.assertTrue(isVerify());
}

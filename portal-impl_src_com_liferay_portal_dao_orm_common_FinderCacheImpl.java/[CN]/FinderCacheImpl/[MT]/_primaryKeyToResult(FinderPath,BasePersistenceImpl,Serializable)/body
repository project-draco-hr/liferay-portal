{
  if (primaryKey instanceof List<?>) {
    Set<Serializable> cachedSet=new HashSet<Serializable>((List<Serializable>)primaryKey);
    if (cachedSet.isEmpty()) {
      return (Serializable)Collections.emptyList();
    }
    try {
      Map<Serializable,Serializable> results=basePersistenceImpl.fetchByPrimaryKeys(cachedSet);
      if (results.size() < cachedSet.size()) {
        return null;
      }
      List<Serializable> list=new ArrayList<Serializable>(results.values());
      return (Serializable)Collections.unmodifiableList(list);
    }
 catch (    Exception e) {
    }
  }
 else   if (BaseModel.class.isAssignableFrom(finderPath.getResultClass())) {
    return EntityCacheUtil.loadResult(finderPath.isEntityCacheEnabled(),finderPath.getResultClass(),primaryKey,basePersistenceImpl);
  }
  return primaryKey;
}

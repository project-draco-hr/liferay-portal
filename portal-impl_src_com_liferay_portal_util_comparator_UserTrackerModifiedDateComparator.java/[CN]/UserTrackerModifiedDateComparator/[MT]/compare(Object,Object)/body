{
  UserTracker userTracker1=(UserTracker)obj1;
  UserTracker userTracker2=(UserTracker)obj2;
  int value=DateUtil.compareTo(userTracker1.getModifiedDate(),userTracker2.getModifiedDate());
  if (_asc) {
    return value;
  }
 else {
    return -value;
  }
}

{
  Map<String,String> headers=new HashMap<String,String>();
  headers.put(HttpHeaders.CONTENT_TYPE,"xyz-foo/bar-512");
  Tuple tuple=service(Method.MKCOL,"mkcolbody",headers,_TEST_CONTENT.getBytes());
  assertEquals(HttpServletResponse.SC_UNSUPPORTED_MEDIA_TYPE,getStatusCode(tuple));
}

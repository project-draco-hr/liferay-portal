{
  ddlEntryPersistence.remove(entry);
  resourceLocalService.deleteResource(entry.getCompanyId(),DDLEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,entry.getEntryId());
  ddlEntryItemLocalService.deleteEntryItems(entry.getEntryId());
}

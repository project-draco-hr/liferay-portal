{
  if (liveGroup.hasLocalOrRemoteStagingGroup() || liveGroup.isStaged()) {
    return;
  }
  typeSettingsProperties.putAll(PropertiesParamUtil.getProperties(serviceContext,StagingConstants.STAGED_PREFIX));
}

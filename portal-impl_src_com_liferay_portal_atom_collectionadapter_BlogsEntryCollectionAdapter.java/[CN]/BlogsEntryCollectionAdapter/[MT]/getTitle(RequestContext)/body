{
  String portletTitle=null;
  try {
    portletTitle=createFeedTitleFromPortletName(requestContext,PortletKeys.BLOGS);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return portletTitle;
}

{
  DDLEntryPermission.check(getPermissionChecker(),groupId,entryKey,ActionKeys.UPDATE);
  return ddlEntryLocalService.updateEntry(groupId,ddmStructureId,entryKey,nameMap,description,serviceContext);
}

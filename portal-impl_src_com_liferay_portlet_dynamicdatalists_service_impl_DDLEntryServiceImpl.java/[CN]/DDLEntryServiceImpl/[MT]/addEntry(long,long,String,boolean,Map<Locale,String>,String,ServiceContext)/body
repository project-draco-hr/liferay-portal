{
  DDLPermission.check(getPermissionChecker(),groupId,ActionKeys.ADD_LIST);
  return ddlEntryLocalService.addEntry(getUserId(),groupId,ddmStructureId,entryKey,autoEntryKey,nameMap,description,serviceContext);
}

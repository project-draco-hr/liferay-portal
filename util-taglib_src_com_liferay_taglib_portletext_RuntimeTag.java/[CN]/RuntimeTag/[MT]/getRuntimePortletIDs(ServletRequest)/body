{
  RuntimePortletIDs runtimePortletIds=(RuntimePortletIDs)servletRequest.getAttribute(WebKeys.RUNTIME_PORTLET_IDS);
  if (runtimePortletIds != null) {
    return runtimePortletIds.getRuntimePortletIDs();
  }
  return null;
}

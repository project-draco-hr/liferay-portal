{
  ActionAdapter actionAdapter=(ActionAdapter)StrutsActionRegistryUtil.getAction("StrutsActionRegistryUtilTest1");
  Assert.assertNotNull(actionAdapter);
  _atomicState.reset();
  actionAdapter.execute(null,null,null,null);
  Assert.assertTrue(_atomicState.isSet());
  PortletActionAdapter portletActionAdapter=(PortletActionAdapter)StrutsActionRegistryUtil.getAction("StrutsActionRegistryUtilTest2");
  Assert.assertNotNull(portletActionAdapter);
  _atomicState.reset();
  portletActionAdapter.isCheckMethodOnProcessAction();
  Assert.assertTrue(_atomicState.isSet());
}

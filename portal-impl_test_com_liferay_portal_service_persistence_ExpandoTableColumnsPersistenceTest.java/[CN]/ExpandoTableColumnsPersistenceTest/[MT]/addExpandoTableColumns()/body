{
  ExpandoTableColumnsPK pk=new ExpandoTableColumnsPK(nextLong(),nextLong());
  ExpandoTableColumns expandoTableColumns=_persistence.create(pk);
  _persistence.update(expandoTableColumns);
  return expandoTableColumns;
}

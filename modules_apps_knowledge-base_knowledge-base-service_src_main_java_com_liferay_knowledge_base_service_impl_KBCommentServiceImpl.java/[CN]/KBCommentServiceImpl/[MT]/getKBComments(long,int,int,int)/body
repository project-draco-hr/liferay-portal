{
  if (AdminPermission.contains(getPermissionChecker(),groupId,ActionKeys.VIEW_SUGGESTIONS)) {
    return kbCommentPersistence.findByG_S(groupId,status,start,end);
  }
  return Collections.emptyList();
}

{
  if (SuggestionPermission.contains(getPermissionChecker(),groupId,className,classPK,ActionKeys.VIEW_SUGGESTIONS)) {
    return kbCommentLocalService.getKBCommentsCount(className,classPK,status);
  }
  return 0;
}

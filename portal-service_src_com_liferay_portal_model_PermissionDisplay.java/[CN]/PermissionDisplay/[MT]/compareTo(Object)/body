{
  if (obj == null) {
    return -1;
  }
  PermissionDisplay permissionDisplay=(PermissionDisplay)obj;
  int value=getResourceLabel().compareTo(permissionDisplay.getResourceLabel());
  if (value == 0) {
    value=getActionLabel().compareTo(permissionDisplay.getActionLabel());
  }
  return value;
}

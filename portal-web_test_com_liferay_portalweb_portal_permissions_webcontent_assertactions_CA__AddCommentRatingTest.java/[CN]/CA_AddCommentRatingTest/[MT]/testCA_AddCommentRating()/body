{
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Web Content Display Permissions Test Page")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.clickAt("link=Web Content Display Permissions Test Page",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  selenium.clickAt("//td[1]/ul/li[2]/a[1]",RuntimeVariables.replace(""));
  Thread.sleep(5000);
  assertTrue(selenium.isTextPresent("+1"));
  selenium.clickAt("//td[1]/ul/li[2]/a[2]",RuntimeVariables.replace(""));
  Thread.sleep(5000);
  assertTrue(selenium.isTextPresent("-1"));
  selenium.clickAt("//td[1]/ul/li[2]/a[2]",RuntimeVariables.replace(""));
  Thread.sleep(5000);
  assertTrue(selenium.isTextPresent("\u00b10"));
}

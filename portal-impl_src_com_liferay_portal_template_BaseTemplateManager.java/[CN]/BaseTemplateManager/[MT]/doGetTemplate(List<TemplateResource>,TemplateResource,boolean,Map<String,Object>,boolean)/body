{
  if (templateResources == null || templateResources.isEmpty()) {
    throw new IllegalArgumentException("TemplateResource parameter is empty");
  }
  return doGetTemplate(templateResources.get(0),errorTemplateResource,restricted,helperUtilities,privileged);
}

{
  List<String> relativePaths=new ArrayList<>(pathFiles.size());
  for (  File pathFile : pathFiles) {
    relativePaths.add(_getRelativePath(baseFile.toPath(),pathFile.toPath()));
  }
  return relativePaths;
}

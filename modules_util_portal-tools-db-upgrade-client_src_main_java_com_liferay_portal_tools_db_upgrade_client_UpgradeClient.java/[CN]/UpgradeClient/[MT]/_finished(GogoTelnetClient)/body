{
  System.out.print("Checking to see if all upgrades steps have completed...");
  String unfinished=gogoTelnetClient.send("upgrade:dryRun");
  String upgradeSteps=gogoTelnetClient.send("upgrade:list | grep Registered | grep step");
  if (unfinished.contains("true") || upgradeSteps.contains("true")) {
    System.out.println(" one of your upgrades is still running or failed.");
    return false;
  }
 else {
    System.out.println(" done.");
    return true;
  }
}

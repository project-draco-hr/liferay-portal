{
  FormsPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_STRUCTURE_ENTRY);
  return formsStructureEntryLocalService.addStructureEntry(getUserId(),groupId,structureId,autoStrucureId,name,description,xsd,serviceContext);
}

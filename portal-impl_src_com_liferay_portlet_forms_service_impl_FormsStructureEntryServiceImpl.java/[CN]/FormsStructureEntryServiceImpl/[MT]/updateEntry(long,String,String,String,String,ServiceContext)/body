{
  FormsStructureEntryPermission.check(getPermissionChecker(),groupId,formStructureId,ActionKeys.UPDATE);
  return formsStructureEntryLocalService.updateEntry(groupId,formStructureId,name,description,xsd,serviceContext);
}

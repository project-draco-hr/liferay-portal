{
  if (Validator.isNotNull(subject)) {
    while (StringUtil.startsWith(subject,prefix + ":") || StringUtil.startsWith(subject,prefix + ">")) {
      subject=subject.substring(3).trim();
    }
  }
  return subject;
}

{
  try {
    BatchSessionUtil.setEnabled(true);
    _convert();
  }
 catch (  Exception e) {
    _log.fatal(e,e);
  }
 finally {
    CacheRegistry.clear();
    BatchSessionUtil.setEnabled(false);
  }
}

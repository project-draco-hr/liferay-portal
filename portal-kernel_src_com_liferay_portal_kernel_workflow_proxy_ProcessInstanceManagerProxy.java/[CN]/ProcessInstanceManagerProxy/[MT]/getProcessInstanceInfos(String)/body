{
  try {
    return (List<ProcessInstanceInfo>)_synchronousMessageSender.send(ProcessInstanceRequest.createGetRequest(workflowDefinitionName));
  }
 catch (  MessageBusException ex) {
    throw new WorkflowException("Unable to get processInstances for " + "workflowDefinitionName:" + workflowDefinitionName,ex);
  }
}

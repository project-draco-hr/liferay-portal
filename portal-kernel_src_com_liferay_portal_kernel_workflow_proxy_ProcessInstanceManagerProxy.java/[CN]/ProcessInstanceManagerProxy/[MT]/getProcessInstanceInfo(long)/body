{
  try {
    return (ProcessInstanceInfo)_synchronousMessageSender.send(ProcessInstanceRequest.createGetRequest(processInstanceId));
  }
 catch (  MessageBusException ex) {
    throw new WorkflowException("Unable to get processInstance by id:" + processInstanceId,ex);
  }
}

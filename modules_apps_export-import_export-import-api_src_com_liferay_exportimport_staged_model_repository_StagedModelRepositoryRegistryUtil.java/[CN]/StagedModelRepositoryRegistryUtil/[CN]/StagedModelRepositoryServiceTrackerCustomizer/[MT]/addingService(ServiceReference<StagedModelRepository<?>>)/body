{
  StagedModelRepository<?> stagedModelRepository=_bundleContext.getService(serviceReference);
  String modelName=GetterUtil.getString(serviceReference.getProperty("model.class.name"));
  _stagedModelRepositories.put(modelName,stagedModelRepository);
  return stagedModelRepository;
}

{
  HttpSession ses=event.getSession();
  SharedSessionAttributeCache cache=SharedSessionAttributeCache.getInstance(ses);
  if (cache.contains(event.getName())) {
    cache.setAttribute(event.getName(),event.getValue());
  }
}

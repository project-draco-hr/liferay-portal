{
  if (permissionChecker.hasOwnerPermission(formStructure.getCompanyId(),FormStructure.class.getName(),formStructure.getId(),formStructure.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(formStructure.getGroupId(),FormStructure.class.getName(),formStructure.getId(),actionId);
}

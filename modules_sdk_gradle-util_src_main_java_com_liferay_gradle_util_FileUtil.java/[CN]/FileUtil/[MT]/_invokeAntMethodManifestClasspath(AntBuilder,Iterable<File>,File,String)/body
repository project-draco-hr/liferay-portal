{
  Map<String,Object> args=new HashMap<>();
  args.put("jarfile",jarFile);
  args.put("maxParentLevels",99);
  args.put("property",property);
  Closure<Void> closure=new Closure<Void>(null){
    @SuppressWarnings("unused") public void doCall(){
      _invokeAntMethodClasspath(antBuilder,merge(files,File.pathSeparator));
    }
  }
;
  antBuilder.invokeMethod("manifestclasspath",new Object[]{args,closure});
}

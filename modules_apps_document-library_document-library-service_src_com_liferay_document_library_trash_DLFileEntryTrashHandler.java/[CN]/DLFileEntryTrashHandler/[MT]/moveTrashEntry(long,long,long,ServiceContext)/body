{
  DocumentRepository documentRepository=getDocumentRepository(classPK);
  RepositoryTrashUtil.moveFileEntryFromTrash(userId,documentRepository.getRepositoryId(),classPK,containerModelId,serviceContext);
}

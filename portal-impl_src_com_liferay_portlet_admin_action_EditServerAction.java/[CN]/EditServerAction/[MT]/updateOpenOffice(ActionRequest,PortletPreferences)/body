{
  boolean enabled=ParamUtil.getBoolean(actionRequest,"enabled");
  int port=ParamUtil.getInteger(actionRequest,"port");
  preferences.setValue(PropsKeys.OPENOFFICE_SERVER_ENABLED,String.valueOf(enabled));
  preferences.setValue(PropsKeys.OPENOFFICE_SERVER_PORT,String.valueOf(port));
  preferences.store();
}

{
  String className=getClassName(classedModel);
  long primaryKey=getPrimaryKey(classedModel);
  getSocialActivityLocalService().addUniqueActivity(userId,classedModel.getGroupId(),className,primaryKey,type,extraData,receiverUserId);
}

{
  String className=getClassName(classedModel);
  long primaryKey=getPrimaryKey(classedModel);
  getSocialActivityLocalService().addUniqueActivity(userId,groupId,className,primaryKey,type,extraData,receiverUserId);
}

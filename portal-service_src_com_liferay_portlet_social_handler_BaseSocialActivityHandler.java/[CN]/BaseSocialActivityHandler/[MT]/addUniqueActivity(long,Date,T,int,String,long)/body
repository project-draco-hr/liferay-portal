{
  String className=getClassName(classedModel);
  long primaryKey=getPrimaryKey(classedModel);
  getSocialActivityLocalService().addUniqueActivity(userId,classedModel.getGroupId(),createDate,className,primaryKey,type,extraData,receiverUserId);
}

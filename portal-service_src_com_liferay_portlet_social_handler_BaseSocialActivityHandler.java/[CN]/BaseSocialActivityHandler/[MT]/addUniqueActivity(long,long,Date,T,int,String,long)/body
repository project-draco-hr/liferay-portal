{
  String className=getClassName(classedModel);
  long primaryKey=getPrimaryKey(classedModel);
  getSocialActivityLocalService().addUniqueActivity(userId,groupId,createDate,className,primaryKey,type,extraData,receiverUserId);
}

{
  try {
    _persistence.countByC_C_S(RandomTestUtil.nextLong(),RandomTestUtil.nextLong(),RandomTestUtil.nextDouble());
    _persistence.countByC_C_S(0L,0L,0D);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

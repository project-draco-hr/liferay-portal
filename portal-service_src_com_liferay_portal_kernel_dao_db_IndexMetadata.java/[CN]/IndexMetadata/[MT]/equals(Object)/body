{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof IndexMetadata)) {
    return false;
  }
  IndexMetadata indexMetadata=(IndexMetadata)obj;
  if (Validator.equals(getTableName(),indexMetadata.getTableName()) && Arrays.equals(getColumnNames(),indexMetadata.getColumnNames())) {
    return true;
  }
  return false;
}

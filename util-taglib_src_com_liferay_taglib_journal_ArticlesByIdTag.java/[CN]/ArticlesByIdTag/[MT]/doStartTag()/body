{
  List<JournalArticle> articles;
  try {
    articles=JournalArticleLocalServiceUtil.getArticles(_groupId,_articleId);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,articles);
  return SKIP_BODY;
}

{
  IGImage image=igImageLocalService.getImageByFolderIdAndNameWithExtension(groupId,folderId,nameWithExtension);
  IGImagePermission.check(getPermissionChecker(),image,ActionKeys.VIEW);
  return image;
}

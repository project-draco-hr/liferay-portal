{
  IGFolderPermission.check(getPermissionChecker(),groupId,folderId,ActionKeys.ADD_IMAGE);
  return igImageLocalService.addImage(null,getUserId(),groupId,folderId,name,description,file,contentType,serviceContext);
}

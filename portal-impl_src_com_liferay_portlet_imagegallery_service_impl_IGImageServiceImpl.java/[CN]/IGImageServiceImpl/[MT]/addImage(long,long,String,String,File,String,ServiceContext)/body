{
  IGFolderPermission.check(getPermissionChecker(),groupId,folderId,ActionKeys.ADD_IMAGE);
  return igImageLocalService.addImage(getUserId(),groupId,folderId,name,description,file,contentType,serviceContext);
}

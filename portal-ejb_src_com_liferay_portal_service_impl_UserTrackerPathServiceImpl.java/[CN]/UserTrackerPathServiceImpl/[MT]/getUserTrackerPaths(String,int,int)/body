{
  UserTracker userTracker=UserTrackerUtil.findByPrimaryKey(userTrackerId);
  if (!hasAdministrator(userTracker.getCompanyId())) {
    throw new PrincipalException();
  }
  return UserTrackerPathLocalServiceUtil.getUserTrackerPaths(userTrackerId,begin,end);
}

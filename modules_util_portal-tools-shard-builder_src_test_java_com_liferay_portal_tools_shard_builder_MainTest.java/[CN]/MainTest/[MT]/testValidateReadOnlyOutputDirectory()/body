{
  File readOnlyDir=temporaryFolder.newFolder();
  readOnlyDir.setReadable(false);
  readOnlyDir.setWritable(false);
  String[] arguments={"-P","foo.properties","-S",_SCHEMA_NAME,"-C",_COMPANY_ID,"-O",readOnlyDir.getAbsolutePath()};
  Main.main(arguments);
}

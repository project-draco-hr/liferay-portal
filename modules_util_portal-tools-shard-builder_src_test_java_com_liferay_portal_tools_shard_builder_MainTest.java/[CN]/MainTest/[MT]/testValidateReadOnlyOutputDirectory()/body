{
  File readOnlyDir=temporaryFolder.newFolder();
  readOnlyDir.setReadable(false);
  readOnlyDir.setWritable(false);
  String[] args={"-P","foo.properties","-S",_SCHEMA_NAME,"-C",_COMPANY_ID,"-O",readOnlyDir.getAbsolutePath()};
  Main.main(args);
}

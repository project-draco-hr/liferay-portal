{
  String[] args={"-P","foobar.properties","-S",_DEFAULT_SCHEMA_NAME,"-C",_DEFAULT_COMPANY_ID,"-O","neverMindPath"};
  try {
    Main.main(args);
    Assert.fail();
  }
 catch (  ParameterException pe) {
    Assert.assertEquals("File parameter -P does not exist",pe.getMessage());
  }
}

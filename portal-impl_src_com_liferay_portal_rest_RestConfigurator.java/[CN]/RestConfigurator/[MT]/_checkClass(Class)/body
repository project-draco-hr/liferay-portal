{
  return (clazz.isAnonymousClass() == false) && (clazz.isArray() == false) && (clazz.isEnum() == false)&& (clazz.isInterface() == false)&& (clazz.isLocalClass() == false)&& ((clazz.isMemberClass() ^ Modifier.isStatic(clazz.getModifiers())) == false)&& (clazz.isPrimitive() == false);
}

{
  assertEquals("The maps are different sizes",expected.size(),actual.size());
  for (  String name : expected.keySet()) {
    assertEquals("The values for key '" + name + "' are different",MapUtil.getString(expected,name),MapUtil.getString(actual,name));
  }
}

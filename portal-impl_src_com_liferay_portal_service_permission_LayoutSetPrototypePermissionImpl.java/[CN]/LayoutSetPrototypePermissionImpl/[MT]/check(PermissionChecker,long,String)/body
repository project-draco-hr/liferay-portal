{
  if (!contains(permissionChecker,layoutSetPrototypeId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,LayoutSetPrototype.class.getName(),layoutSetPrototypeId,actionId);
  }
}

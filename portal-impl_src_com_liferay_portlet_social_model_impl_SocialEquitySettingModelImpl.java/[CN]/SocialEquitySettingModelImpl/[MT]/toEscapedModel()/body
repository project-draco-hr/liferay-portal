{
  if (isEscapedModel()) {
    return (SocialEquitySetting)this;
  }
 else {
    return (SocialEquitySetting)Proxy.newProxyInstance(SocialEquitySetting.class.getClassLoader(),new Class[]{SocialEquitySetting.class},new AutoEscapeBeanHandler(this));
  }
}

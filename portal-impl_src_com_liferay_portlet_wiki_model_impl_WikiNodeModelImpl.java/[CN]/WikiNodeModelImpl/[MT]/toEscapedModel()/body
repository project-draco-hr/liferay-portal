{
  if (isEscapedModel()) {
    return (WikiNode)this;
  }
 else {
    return (WikiNode)Proxy.newProxyInstance(WikiNode.class.getClassLoader(),new Class[]{WikiNode.class},new AutoEscapeBeanHandler(this));
  }
}

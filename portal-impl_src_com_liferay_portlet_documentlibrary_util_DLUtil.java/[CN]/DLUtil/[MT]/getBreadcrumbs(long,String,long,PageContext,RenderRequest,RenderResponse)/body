{
  if ((folderId > 0) && Validator.isNotNull(name)) {
    DLFileEntry fileEntry=DLFileEntryLocalServiceUtil.getFileEntry(folderId,name);
    return getBreadcrumbs(fileEntry.getFolder(),fileEntry,rootFolderId,pageContext,renderRequest,renderResponse);
  }
 else {
    DLFolder folder=null;
    try {
      folder=DLFolderLocalServiceUtil.getFolder(folderId);
    }
 catch (    Exception e) {
    }
    return getBreadcrumbs(folder,null,rootFolderId,pageContext,renderRequest,renderResponse);
  }
}

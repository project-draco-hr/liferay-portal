{
  MockServiceReference mockServiceReference=(MockServiceReference)object;
  if (_ranking != mockServiceReference._ranking) {
    if (_ranking < mockServiceReference._ranking) {
      return -1;
    }
    return 1;
  }
  if (_id == mockServiceReference._id) {
    return 0;
  }
  if (_id < mockServiceReference._id) {
    return 1;
  }
  return -1;
}

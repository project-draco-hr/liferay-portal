{
  UpgradePortletId upgradePortletId=new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"85",SiteMapPortletKeys.SITE_MAP}};
    }
  }
;
  _releaseLocalService.updateRelease("com.liferay.site.map.web",Collections.<UpgradeProcess>singletonList(upgradePortletId),1,0,false);
}

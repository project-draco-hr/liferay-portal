{
  if (obj == null) {
    return false;
  }
  UserGroupGroupRolePK pk=null;
  try {
    pk=(UserGroupGroupRolePK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((userGroupId == pk.userGroupId) && (groupId == pk.groupId) && (roleId == pk.roleId)) {
    return true;
  }
 else {
    return false;
  }
}

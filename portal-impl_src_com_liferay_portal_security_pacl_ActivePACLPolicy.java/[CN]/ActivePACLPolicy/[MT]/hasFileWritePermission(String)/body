{
  Permission permission=new FilePermission(fileName,_FILE_PERMISSION_WRITE);
  for (  Permission writeFilePermission : _writeFilePermissions) {
    if (writeFilePermission.implies(permission)) {
      return true;
    }
  }
  return false;
}

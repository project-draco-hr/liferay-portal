{
  Permission permission=new FilePermission(fileName,_FILE_PERMISSION_EXECUTE);
  for (  Permission executeFilePermission : _executeFilePermissions) {
    if (executeFilePermission.implies(permission)) {
      return true;
    }
  }
  return false;
}

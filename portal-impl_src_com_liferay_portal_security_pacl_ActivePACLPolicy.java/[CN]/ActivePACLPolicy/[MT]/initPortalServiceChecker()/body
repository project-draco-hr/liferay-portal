{
  _portalServiceChecker=(PortalServiceChecker)getChecker(PortalServicePermission.class);
  if (_portalServiceChecker == null) {
    _portalServiceChecker=new PortalServiceChecker();
    initChecker(_portalServiceChecker);
  }
}

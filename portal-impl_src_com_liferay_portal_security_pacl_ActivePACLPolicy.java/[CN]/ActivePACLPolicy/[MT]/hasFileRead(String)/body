{
  Permission permission=new FilePermission(fileName,_FILE_PERMISSION_READ);
  for (  Permission readFilePermission : _readFilePermissions) {
    if (readFilePermission.implies(permission)) {
      return true;
    }
  }
  return false;
}

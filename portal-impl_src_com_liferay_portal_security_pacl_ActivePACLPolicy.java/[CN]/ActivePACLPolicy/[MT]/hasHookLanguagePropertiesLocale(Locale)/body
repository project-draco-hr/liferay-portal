{
  if (_hookLanguagePropertiesLanguageIds.contains(locale.getLanguage())) {
    return true;
  }
  if (_hookLanguagePropertiesLanguageIds.contains(locale.getLanguage() + "_" + locale.getCountry())) {
    return true;
  }
  return false;
}

{
  String script=ddmTemplate.getScript();
  Document document=SAXReaderUtil.read(script);
  return getHTML(pageContext,document.getRootElement(),fields,StringPool.BLANK,ddmTemplate.getMode(),false,locale);
}

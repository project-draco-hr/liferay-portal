{
  int total=0;
  String fieldName=StringUtil.extractFirst(fieldsDisplayValues[offset],DDMImpl.INSTANCE_SEPARATOR);
  for (; offset < fieldsDisplayValues.length; offset++) {
    String fieldNameValue=StringUtil.extractFirst(fieldsDisplayValues[offset],DDMImpl.INSTANCE_SEPARATOR);
    if (fieldNameValue.equals(fieldName)) {
      total++;
    }
    if (fieldNameValue.equals(parentFieldName)) {
      break;
    }
  }
  return total;
}

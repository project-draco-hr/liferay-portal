{
  String defaultFieldsDisplayValue=null;
  if (fields != null) {
    Field fieldsDisplayField=fields.get(DDMImpl.FIELDS_DISPLAY_NAME);
    if (fieldsDisplayField != null) {
      defaultFieldsDisplayValue=(String)fieldsDisplayField.getValue();
    }
  }
  return ParamUtil.getString((HttpServletRequest)pageContext.getRequest(),DDMImpl.FIELDS_DISPLAY_NAME,defaultFieldsDisplayValue);
}

{
  Session session=null;
  try {
    session=openSession();
    return (Organization)session.get(OrganizationImpl.class,new Long(organizationId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

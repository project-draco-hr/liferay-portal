{
  for (  Organization organization : organizations) {
    if (EntityCacheUtil.getResult(OrganizationModelImpl.ENTITY_CACHE_ENABLED,Organization.class,organization.getPrimaryKey(),this) == null) {
      cacheResult(organization);
    }
  }
}

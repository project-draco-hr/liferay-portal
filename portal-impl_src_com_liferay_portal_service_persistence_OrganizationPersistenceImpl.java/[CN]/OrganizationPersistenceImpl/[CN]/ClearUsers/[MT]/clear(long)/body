{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(organizationId,com.liferay.portal.model.User.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(organizationId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(organizationId,com.liferay.portal.model.User.class.getName());
    }
  }
}

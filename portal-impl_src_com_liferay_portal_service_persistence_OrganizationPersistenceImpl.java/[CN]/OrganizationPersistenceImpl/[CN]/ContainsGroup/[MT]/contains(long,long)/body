{
  List<Integer> results=execute(new Object[]{new Long(organizationId),new Long(groupId)});
  if (results.size() > 0) {
    Integer count=results.get(0);
    if (count.intValue() > 0) {
      return true;
    }
  }
  return false;
}

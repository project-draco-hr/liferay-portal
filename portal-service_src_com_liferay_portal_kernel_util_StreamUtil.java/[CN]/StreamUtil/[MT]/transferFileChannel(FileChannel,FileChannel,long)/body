{
  if (length <= 0) {
    length=inputFileChannel.size() - inputFileChannel.position();
  }
  long position=0;
  while (position < length) {
    position+=inputFileChannel.transferTo(position,length - position,outputFileChannel);
  }
}

{
  Group group=(Group)obj;
  try {
    if (group.isCompany() || PortalUtil.isSystemGroup(group.getGroupKey())) {
      return true;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return super.isDisabled(obj);
}

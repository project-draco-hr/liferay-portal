{
  if (isEscapedModel()) {
    return (SocialEquityUser)this;
  }
 else {
    return (SocialEquityUser)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

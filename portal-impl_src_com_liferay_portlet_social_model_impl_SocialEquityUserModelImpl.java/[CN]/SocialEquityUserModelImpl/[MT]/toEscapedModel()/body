{
  if (isEscapedModel()) {
    return (SocialEquityUser)this;
  }
 else {
    return (SocialEquityUser)Proxy.newProxyInstance(SocialEquityUser.class.getClassLoader(),new Class[]{SocialEquityUser.class},new AutoEscapeBeanHandler(this));
  }
}

{
  try {
    clearAssets.clear(pk);
    for (    com.liferay.portlet.asset.model.Asset asset : assets) {
      addAsset.add(pk,asset.getPrimaryKey());
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    FinderCacheUtil.clearCache("Assets_AssetTags");
  }
}

{
  PreparedStatement ps=null;
  ResultSet rs=null;
  try {
    ps=connection.prepareStatement("select fileEntryTypeId from DLFileEntryType " + "where name = ? AND companyId = ?");
    ps.setString(1,DLFileEntryTypeConstants.NAME_IG_IMAGE);
    ps.setLong(2,companyId);
    rs=ps.executeQuery();
    if (rs.next()) {
      return rs.getLong("fileEntryTypeId");
    }
    return 0;
  }
  finally {
    DataAccess.cleanUp(ps,rs);
  }
}

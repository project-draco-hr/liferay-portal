{
  PropsValues.LOCALES_ENABLED=PropsUtil.getArray(PropsKeys.LOCALES_ENABLED);
  LanguageUtil.init();
  CompanyTestUtil.resetCompanyLocales(TestPropsValues.getCompanyId(),_availableLocales,LocaleUtil.getDefault());
}

{
  PollsVote existingVote=PollsVoteLocalServiceUtil.fetchPollsVoteByUuidAndGroupId(uuid,groupId);
  Map<Long,Long> voteIds=(Map<Long,Long>)portletDataContext.getNewPrimaryKeysMap(PollsVote.class);
  voteIds.put(voteId,existingVote.getVoteId());
}

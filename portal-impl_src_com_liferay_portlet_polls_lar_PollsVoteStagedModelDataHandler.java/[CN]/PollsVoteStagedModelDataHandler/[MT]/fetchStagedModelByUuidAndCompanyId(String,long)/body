{
  List<PollsVote> votes=PollsVoteLocalServiceUtil.getPollsVotesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<PollsVote>());
  if (ListUtil.isEmpty(votes)) {
    return null;
  }
  return votes.get(0);
}

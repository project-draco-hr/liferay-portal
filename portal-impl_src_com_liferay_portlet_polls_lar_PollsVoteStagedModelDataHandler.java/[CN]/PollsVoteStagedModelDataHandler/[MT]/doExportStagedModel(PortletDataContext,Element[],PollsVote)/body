{
  Element questionsElement=elements[0];
  Element choicesElement=elements[1];
  StagedModelDataHandlerUtil.exportStagedModel(portletDataContext,new Element[]{questionsElement,choicesElement},vote.getChoice());
  Element votesElement=elements[2];
  Element voteElement=votesElement.addElement("vote");
  portletDataContext.addClassedModel(voteElement,StagedModelPathUtil.getPath(vote),vote,PollsPortletDataHandler.NAMESPACE);
}

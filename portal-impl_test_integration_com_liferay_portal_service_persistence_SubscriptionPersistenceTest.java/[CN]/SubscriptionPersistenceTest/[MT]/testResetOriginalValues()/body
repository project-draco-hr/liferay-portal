{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  Subscription newSubscription=addSubscription();
  _persistence.clearCache();
  SubscriptionModelImpl existingSubscriptionModelImpl=(SubscriptionModelImpl)_persistence.findByPrimaryKey(newSubscription.getPrimaryKey());
  Assert.assertEquals(existingSubscriptionModelImpl.getCompanyId(),existingSubscriptionModelImpl.getOriginalCompanyId());
  Assert.assertEquals(existingSubscriptionModelImpl.getUserId(),existingSubscriptionModelImpl.getOriginalUserId());
  Assert.assertEquals(existingSubscriptionModelImpl.getClassNameId(),existingSubscriptionModelImpl.getOriginalClassNameId());
  Assert.assertEquals(existingSubscriptionModelImpl.getClassPK(),existingSubscriptionModelImpl.getOriginalClassPK());
}

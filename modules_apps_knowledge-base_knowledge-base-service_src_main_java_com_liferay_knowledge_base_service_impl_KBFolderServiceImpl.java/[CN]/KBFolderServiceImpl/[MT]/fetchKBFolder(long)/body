{
  KBFolder kbFolder=kbFolderLocalService.fetchKBFolder(kbFolderId);
  if (kbFolder != null) {
    KBFolderPermission.check(getPermissionChecker(),kbFolder,KBActionKeys.VIEW);
  }
  return kbFolder;
}

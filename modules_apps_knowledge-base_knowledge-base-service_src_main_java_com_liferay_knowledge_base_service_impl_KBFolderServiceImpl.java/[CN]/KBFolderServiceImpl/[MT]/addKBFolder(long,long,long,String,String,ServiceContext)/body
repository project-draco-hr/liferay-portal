{
  KBFolderPermission.check(getPermissionChecker(),groupId,parentResourcePrimKey,ActionKeys.ADD_KB_FOLDER);
  return kbFolderLocalService.addKBFolder(getUserId(),groupId,parentResourceClassNameId,parentResourcePrimKey,name,description,serviceContext);
}

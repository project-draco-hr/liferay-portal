{
  for (  ShoppingItemPrice shoppingItemPrice : shoppingItemPrices) {
    if (EntityCacheUtil.getResult(ShoppingItemPriceModelImpl.ENTITY_CACHE_ENABLED,ShoppingItemPriceImpl.class,shoppingItemPrice.getPrimaryKey()) == null) {
      cacheResult(shoppingItemPrice);
    }
 else {
      shoppingItemPrice.resetOriginalValues();
    }
  }
}

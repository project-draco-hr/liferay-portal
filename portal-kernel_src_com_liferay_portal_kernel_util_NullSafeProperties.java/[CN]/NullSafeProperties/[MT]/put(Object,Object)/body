{
  if (key == null) {
    return null;
  }
 else {
    if (value == null) {
      return super.remove(key);
    }
 else {
      value=_encodeNewlines((String)value);
      return super.put(key,value);
    }
  }
}

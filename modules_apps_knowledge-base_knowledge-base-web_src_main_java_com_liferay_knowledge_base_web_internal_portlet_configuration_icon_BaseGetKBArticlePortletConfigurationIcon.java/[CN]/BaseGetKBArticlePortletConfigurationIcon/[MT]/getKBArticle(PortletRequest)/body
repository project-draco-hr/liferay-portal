{
  KBArticle kbArticle=(KBArticle)portletRequest.getAttribute(KBWebKeys.KNOWLEDGE_BASE_KB_ARTICLE);
  if (kbArticle != null) {
    return kbArticle;
  }
  return (KBArticle)portletRequest.getAttribute(KBWebKeys.KNOWLEDGE_BASE_PARENT_KB_ARTICLE);
}

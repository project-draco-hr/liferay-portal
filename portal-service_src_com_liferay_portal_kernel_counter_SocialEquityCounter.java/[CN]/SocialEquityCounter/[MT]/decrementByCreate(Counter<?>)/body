{
  if (delta.getValue() instanceof SocialEquityValue) {
    return new SocialEquityCounter(new SocialEquityValue(_value.getK() - ((SocialEquityValue)delta.getValue()).getK(),_value.getB() - ((SocialEquityValue)delta.getValue()).getB()));
  }
  throw new IllegalArgumentException("Invalid type " + delta.getValue().getClass().getName() + ". Expected type is SocialEquity.");
}

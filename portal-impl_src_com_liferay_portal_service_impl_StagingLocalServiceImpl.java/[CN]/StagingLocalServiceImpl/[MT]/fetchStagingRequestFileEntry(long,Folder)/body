{
  try {
    return PortletFileRepositoryUtil.getPortletFileEntry(folder.getGroupId(),folder.getFolderId(),getAssembledFileEntryName(stagingRequestId));
  }
 catch (  NoSuchFileException nsfe) {
    return null;
  }
}

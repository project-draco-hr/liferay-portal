{
  try {
    PortletFileRepositoryUtil.deleteFolder(folderId);
  }
 catch (  NoSuchFolderException e) {
    if (_log.isDebugEnabled()) {
      _log.debug("Staging request already cleaned up: " + folderId,e);
    }
  }
}

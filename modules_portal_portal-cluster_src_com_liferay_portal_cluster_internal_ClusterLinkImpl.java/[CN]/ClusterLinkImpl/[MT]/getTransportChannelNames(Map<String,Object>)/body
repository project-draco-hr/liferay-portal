{
  Properties transportNames=new Properties();
  int prefixLength=ClusterPropsKeys.CHANNEL_NAME_TRANSPORT_PREFIX.length();
  for (  String key : properties.keySet()) {
    if (key.startsWith(ClusterPropsKeys.CHANNEL_NAME_TRANSPORT_PREFIX)) {
      transportNames.put(key.substring(prefixLength + 1),properties.get(key));
    }
  }
  if (transportNames.isEmpty()) {
    transportNames=_props.getProperties(PropsKeys.CLUSTER_LINK_CHANNEL_NAME_TRANSPORT,true);
  }
  return transportNames;
}

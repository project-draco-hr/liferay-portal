{
  String name=ReleaseInfo.getName();
  if (!name.contains("Community")) {
    return;
  }
  componentContext.enableComponent(SinglePortalCacheBootstrapLoaderFactory.class.getName());
  componentContext.enableComponent(SinglePortalCacheReplicatorFactory.class.getName());
}

{
  if ((type == TYPE_UNI_CHILD) || (type == TYPE_UNI_ENEMY) || (type == TYPE_UNI_FOLLOWER)|| (type == TYPE_UNI_PARENT)|| (type == TYPE_UNI_SUBORDINATE)|| (type == TYPE_UNI_SUPERVISOR)) {
    return true;
  }
 else {
    return false;
  }
}

{
  UserGroupGroupRolePK pk=new UserGroupGroupRolePK(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
  UserGroupGroupRole userGroupGroupRole=_persistence.create(pk);
  _persistence.update(userGroupGroupRole);
  return userGroupGroupRole;
}

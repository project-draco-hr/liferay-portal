{
  Object obj=null;
  try {
    obj=cache.getFromCache(key);
    cache.flushEntry(key);
  }
 catch (  NeedsRefreshException nfe) {
  }
 finally {
    if (obj == null) {
      cache.cancelUpdate(key);
    }
  }
  return obj;
}

{
  if (isEscapedModel()) {
    return (DDMStorageLink)this;
  }
 else {
    return (DDMStorageLink)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  PACLPolicy paclPolicy=getPACLPolicy(_logCheckListen.isDebugEnabled());
  if (_logCheckListen.isInfoEnabled()) {
    _logCheckListen.info("Checking permissions to listen on port " + port + " with PACL policy "+ paclPolicy);
  }
  if (paclPolicy != null) {
    if (!paclPolicy.isSocketListen(port)) {
      throw new SecurityException("Attempted to listen on port " + port);
    }
  }
  super.checkListen(port);
}

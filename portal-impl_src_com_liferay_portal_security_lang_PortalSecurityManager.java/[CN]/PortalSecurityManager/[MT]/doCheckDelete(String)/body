{
  PACLPolicy paclPolicy=getPACLPolicy(_logDoCheckDelete.isDebugEnabled());
  if (paclPolicy != null) {
    if (!paclPolicy.hasFileDelete(fileName)) {
      throw new SecurityException("Attempted to delete file " + fileName);
    }
  }
}

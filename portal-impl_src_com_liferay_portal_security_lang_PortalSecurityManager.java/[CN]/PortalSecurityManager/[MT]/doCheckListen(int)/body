{
  PACLPolicy paclPolicy=getPACLPolicy(_logDoCheckListen.isDebugEnabled());
  if (paclPolicy != null) {
    if (!paclPolicy.hasSocketListen(port)) {
      throw new SecurityException("Attempted to listen on port " + port);
    }
  }
}

{
  AssetCategoryPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),parentCategoryId,ActionKeys.ADD_CATEGORY);
  return assetCategoryLocalService.addCategory(null,getUserId(),parentCategoryId,titleMap,vocabularyId,categoryProperties,serviceContext);
}

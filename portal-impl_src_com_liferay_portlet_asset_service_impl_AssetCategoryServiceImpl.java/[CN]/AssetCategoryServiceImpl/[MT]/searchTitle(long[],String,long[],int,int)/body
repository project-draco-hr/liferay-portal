{
  BaseModelSearchResult<AssetCategory> categories=assetCategoryLocalService.searchCategories(CompanyThreadLocal.getCompanyId(),groupIds,title,vocabularyIds,start,end);
  return toJSONArray(categories.getBaseModels());
}

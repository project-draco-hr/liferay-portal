{
  User user=getUser();
  BaseModelSearchResult<AssetCategory> results=assetCategoryLocalService.searchCategories(user.getCompanyId(),groupIds,title,vocabularyIds,start,end);
  return new AssetCategoryDisplay(results.getBaseModels(),results.getLength(),start,end);
}

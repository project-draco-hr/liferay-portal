{
  if (UserLocalServiceUtil.hasUserGroupUser(userGroupGroup.getGroupId(),permissionChecker.getUserId()) || GroupPermissionUtil.contains(permissionChecker,userGroupGroup.getGroupId(),ActionKeys.VIEW)) {
    return true;
  }
 else {
    return false;
  }
}

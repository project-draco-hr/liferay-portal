{
  if (GroupPermissionUtil.contains(permissionChecker,userGroupGroup.getGroupId(),ActionKeys.VIEW) || UserLocalServiceUtil.hasUserGroupUser(userGroupGroup.getGroupId(),permissionChecker.getUserId())) {
    return true;
  }
 else {
    return false;
  }
}

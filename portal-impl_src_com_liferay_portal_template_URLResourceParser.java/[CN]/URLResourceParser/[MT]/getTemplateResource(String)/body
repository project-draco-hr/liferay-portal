{
  try {
    URL url=getURL(templateId);
    if (url != null) {
      return new URLTemplateResource(templateId,url);
    }
  }
 catch (  IOException e) {
    throw new TemplateException(e);
  }
  return null;
}

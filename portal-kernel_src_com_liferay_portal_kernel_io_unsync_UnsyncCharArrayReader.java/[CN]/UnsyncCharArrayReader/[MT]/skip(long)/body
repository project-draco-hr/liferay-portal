{
  if (buffer == null) {
    throw new IOException("Stream closed");
  }
  if (skip < 0) {
    return 0;
  }
  if ((index + skip) > capacity) {
    skip=capacity - index;
  }
  index+=skip;
  return skip;
}

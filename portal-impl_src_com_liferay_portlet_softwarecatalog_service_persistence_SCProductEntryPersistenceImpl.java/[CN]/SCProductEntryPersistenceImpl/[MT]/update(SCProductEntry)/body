{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCProductEntry scProductEntry) method. To improve performance, call update(SCProductEntry scProductEntry, boolean merge).");
  }
  return update(scProductEntry,false);
}

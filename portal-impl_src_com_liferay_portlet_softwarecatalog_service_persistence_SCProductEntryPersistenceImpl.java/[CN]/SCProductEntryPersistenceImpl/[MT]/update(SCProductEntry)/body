{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCProductEntry scProductEntry) method. Use update(SCProductEntry scProductEntry, boolean merge) instead.");
  }
  return update(scProductEntry,false);
}

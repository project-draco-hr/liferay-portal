{
  SCProductEntry scProductEntry=findByPrimaryKey(productEntryId);
  Session session=null;
  try {
    session=openSession();
    SCProductEntry[] array=new SCProductEntryImpl[3];
    array[0]=getByGroupId_PrevAndNext(session,scProductEntry,groupId,orderByComparator,true);
    array[1]=scProductEntry;
    array[2]=getByGroupId_PrevAndNext(session,scProductEntry,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

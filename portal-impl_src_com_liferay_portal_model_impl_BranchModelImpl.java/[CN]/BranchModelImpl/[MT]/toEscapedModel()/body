{
  if (isEscapedModel()) {
    return (Branch)this;
  }
 else {
    return (Branch)Proxy.newProxyInstance(Branch.class.getClassLoader(),new Class[]{Branch.class},new AutoEscapeBeanHandler(this));
  }
}

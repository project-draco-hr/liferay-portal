{
  if (parentName.equals(ThemeBuilder.STYLED)) {
    return _styledJarFile;
  }
  if (parentName.equals(ThemeBuilder.UNSTYLED)) {
    return _unstyledJarFile;
  }
  throw new IllegalArgumentException("Unsupported parent name " + parentName);
}

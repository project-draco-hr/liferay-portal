{
  String formURL=getPublishedFormURL();
  if (formURL.isEmpty()) {
    return formURL;
  }
  StringBundler sb=new StringBundler(2);
  sb.append(formURL);
  sb.append("/preview");
  return sb.toString();
}

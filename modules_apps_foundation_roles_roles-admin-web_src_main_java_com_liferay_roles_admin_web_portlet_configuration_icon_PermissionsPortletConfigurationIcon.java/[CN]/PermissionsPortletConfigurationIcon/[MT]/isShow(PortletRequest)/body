{
  try {
    ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
    long roleId=getRoleId(portletRequest);
    Role role=_roleService.fetchRole(roleId);
    String roleName=role.getName();
    return !roleName.equals(RoleConstants.OWNER) && RolePermissionUtil.contains(themeDisplay.getPermissionChecker(),roleId,ActionKeys.PERMISSIONS);
  }
 catch (  Exception e) {
  }
  return false;
}

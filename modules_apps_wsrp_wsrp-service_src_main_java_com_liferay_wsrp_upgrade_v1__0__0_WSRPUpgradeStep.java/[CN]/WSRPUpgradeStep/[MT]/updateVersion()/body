{
  final DB db=DBManagerUtil.getDB();
  try {
    db.runSQL("update WSRP_WSRPProducer set version = '2.0' where version " + "is null or version = ''");
  }
 catch (  Exception e) {
    new UpgradeException(e);
  }
}

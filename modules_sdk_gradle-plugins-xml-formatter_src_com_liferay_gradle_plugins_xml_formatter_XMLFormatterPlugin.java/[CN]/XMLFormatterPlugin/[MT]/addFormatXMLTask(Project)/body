{
  TaskContainer taskContainer=project.getTasks();
  Task task=taskContainer.create(FORMAT_XML_TASK_NAME,FormatXMLTask.class);
  task.setDescription("Runs Liferay XML Formatter to format files.");
  return task;
}

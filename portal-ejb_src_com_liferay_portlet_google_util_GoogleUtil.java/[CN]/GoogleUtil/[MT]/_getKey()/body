{
  if (_licenseKeys.size() > 0) {
    _licenseCount++;
    if (_licenseCount >= 1000) {
      _useNewKey();
      return _getKey();
    }
 else {
      return (String)_licenseKeys.get(0);
    }
  }
 else {
    return null;
  }
}

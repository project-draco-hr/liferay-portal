{
  _licenseKeys=new Vector();
  for (int i=0; ; i++) {
    String key=PropsUtil.get(PropsUtil.GOOGLE_LICENSE + i);
    if (key == null) {
      break;
    }
 else {
      _licenseKeys.add(key);
    }
  }
}

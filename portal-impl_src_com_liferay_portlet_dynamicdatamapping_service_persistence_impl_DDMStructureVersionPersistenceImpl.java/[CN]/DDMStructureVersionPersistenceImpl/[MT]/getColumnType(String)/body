{
  Integer type=DDMStructureVersionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DDMStructureVersionModelImpl.TABLE_NAME);
  }
  return type;
}

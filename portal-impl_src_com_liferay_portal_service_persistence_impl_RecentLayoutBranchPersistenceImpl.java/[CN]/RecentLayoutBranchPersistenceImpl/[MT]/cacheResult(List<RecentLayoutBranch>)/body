{
  for (  RecentLayoutBranch recentLayoutBranch : recentLayoutBranchs) {
    if (entityCache.getResult(RecentLayoutBranchModelImpl.ENTITY_CACHE_ENABLED,RecentLayoutBranchImpl.class,recentLayoutBranch.getPrimaryKey()) == null) {
      cacheResult(recentLayoutBranch);
    }
 else {
      recentLayoutBranch.resetOriginalValues();
    }
  }
}

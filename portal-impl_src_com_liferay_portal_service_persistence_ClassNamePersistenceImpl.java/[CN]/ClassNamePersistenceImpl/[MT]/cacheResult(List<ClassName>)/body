{
  for (  ClassName className : classNames) {
    if (EntityCacheUtil.getResult(ClassNameModelImpl.ENTITY_CACHE_ENABLED,ClassName.class,className.getPrimaryKey(),this) == null) {
      cacheResult(className);
    }
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ClassName className) method. To improve performance, call update(ClassName className, boolean merge).");
  }
  return update(className,false);
}

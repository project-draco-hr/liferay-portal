{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ClassName className) method. Use update(ClassName className, boolean merge) instead.");
  }
  return update(className,false);
}

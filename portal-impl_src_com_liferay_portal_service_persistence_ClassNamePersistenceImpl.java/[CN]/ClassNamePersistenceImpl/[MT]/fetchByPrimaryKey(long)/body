{
  Session session=null;
  try {
    session=openSession();
    return (ClassName)session.get(ClassNameImpl.class,new Long(classNameId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  categoriesPropertyLocalService.deleteProperties(entry.getEntryId());
  resourceLocalService.deleteResource(entry.getCompanyId(),CategoriesEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,entry.getEntryId());
  categoriesEntryPersistence.remove(entry);
}

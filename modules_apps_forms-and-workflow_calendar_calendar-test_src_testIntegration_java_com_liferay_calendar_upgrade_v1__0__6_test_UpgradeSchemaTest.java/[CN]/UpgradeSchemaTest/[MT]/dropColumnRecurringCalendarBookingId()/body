{
  try (Connection con=DataAccess.getUpgradeOptimizedConnection()){
    connection=con;
    if (hasColumn("CalendarBooking","recurringCalendarBookingId")) {
      runSQL("alter table CalendarBooking drop column " + "recurringCalendarBookingId");
    }
  }
 }

{
  String[] contentFlags=new String[]{"compileJSP","Unable to compile JSPs"};
  for (  String contentFlag : contentFlags) {
    if (content.contains(contentFlag)) {
      return true;
    }
  }
  return false;
}

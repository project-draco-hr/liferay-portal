{
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(AddressImpl.TABLE_NAME,AddressImpl.TABLE_COLUMNS,new PKUpgradeColumnImpl(),new ClassNameUpgradeColumnImpl());
  upgradeTable.updateTable();
  CounterLocalServiceUtil.reset(Address.class.getName());
  DBUtil.getInstance().executeSQL(_UPGRADE_SCHEMA);
}

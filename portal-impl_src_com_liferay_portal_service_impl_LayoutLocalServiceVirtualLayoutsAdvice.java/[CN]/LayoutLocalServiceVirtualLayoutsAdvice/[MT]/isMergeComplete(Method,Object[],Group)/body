{
  if (MergeLayoutPrototypesThreadLocal.isMergeComplete(method.getName(),arguments) && (!group.isUser() || PropsValues.USER_GROUPS_COPY_LAYOUTS_TO_USER_PERSONAL_SITE)) {
    return true;
  }
 else {
    return false;
  }
}

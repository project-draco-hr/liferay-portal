{
  if (isEscapedModel()) {
    return (RatingsEntry)this;
  }
 else {
    return (RatingsEntry)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

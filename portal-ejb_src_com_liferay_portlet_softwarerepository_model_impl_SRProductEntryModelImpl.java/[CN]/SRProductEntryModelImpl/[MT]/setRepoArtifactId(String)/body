{
  if (((repoArtifactId == null) && (_repoArtifactId != null)) || ((repoArtifactId != null) && (_repoArtifactId == null)) || ((repoArtifactId != null) && (_repoArtifactId != null) && !repoArtifactId.equals(_repoArtifactId))) {
    if (!XSS_ALLOW_REPOARTIFACTID) {
      repoArtifactId=XSSUtil.strip(repoArtifactId);
    }
    _repoArtifactId=repoArtifactId;
  }
}

{
  Class<?> clazz=getClass();
  ClassLoader classLoader=clazz.getClassLoader();
  Document document=SAXReaderUtil.read(classLoader.getResource("com/liferay/portal/wab/extender/internal/dependencies" + "/default-web.xml"));
  _defaultWebXmlRootElement=document.getRootElement();
}

{
  _bundleContext=bundleContext;
  _logger=new Logger(bundleContext);
  _httpTunnelExtenderConfiguration=Configurable.createConfigurable(HttpTunnelExtenderConfiguration.class,properties);
  start(bundleContext);
}

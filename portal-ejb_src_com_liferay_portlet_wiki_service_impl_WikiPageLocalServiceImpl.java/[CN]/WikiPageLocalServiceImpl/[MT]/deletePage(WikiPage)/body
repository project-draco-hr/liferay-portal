{
  try {
    Indexer.deletePage(page.getCompanyId(),page.getNodeId(),page.getTitle());
  }
 catch (  IOException ioe) {
    _log.error("Deleting index " + page.getPrimaryKey(),ioe);
  }
  TagsAssetLocalServiceUtil.deleteAsset(WikiPage.class.getName(),page.getResourcePK().toString());
  ResourceLocalServiceUtil.deleteResource(page.getCompanyId(),WikiPage.class.getName(),ResourceImpl.SCOPE_INDIVIDUAL,page.getResourcePK().toString());
  WikiPageUtil.removeByN_T(page.getNodeId(),page.getTitle());
}

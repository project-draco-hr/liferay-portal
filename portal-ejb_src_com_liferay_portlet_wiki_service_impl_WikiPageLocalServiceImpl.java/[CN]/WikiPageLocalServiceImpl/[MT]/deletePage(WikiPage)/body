{
  try {
    Indexer.deletePage(page.getCompanyId(),page.getNodeId(),page.getTitle());
  }
 catch (  IOException ioe) {
    _log.error(ioe.getMessage());
  }
  ResourceLocalServiceUtil.deleteResource(page.getCompanyId(),WikiPage.class.getName(),Resource.TYPE_CLASS,Resource.SCOPE_INDIVIDUAL,page.getResourcePK().toString());
  WikiPageUtil.removeByN_T(page.getNodeId(),page.getTitle());
}

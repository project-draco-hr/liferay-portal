{
  PortletURL portletURL=liferayPortletResponse.createLiferayPortletURL(PortletKeys.CALENDAR,PortletRequest.RENDER_PHASE);
  portletURL.setParameter("struts_action","/calendar/view_event");
  portletURL.setParameter("eventId",String.valueOf(_event.getEventId()));
  portletURL.setWindowState(windowState);
  return portletURL;
}

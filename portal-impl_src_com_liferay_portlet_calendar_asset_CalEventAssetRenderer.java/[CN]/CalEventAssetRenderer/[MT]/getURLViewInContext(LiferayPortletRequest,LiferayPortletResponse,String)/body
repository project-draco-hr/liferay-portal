{
  ThemeDisplay themeDisplay=(ThemeDisplay)liferayPortletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  return themeDisplay.getPathMain() + "/calendar/find_event?noSuchEntryRedirect=" + HttpUtil.encodeURL(noSuchEntryRedirect)+ "&eventId="+ _event.getEventId();
}

{
  try (PreparedStatement ps=connection.prepareStatement("select rootMessageId from MBThread where threadId = " + mbThreadId);ResultSet rs=ps.executeQuery()){
    if (rs.next()) {
      return rs.getLong("rootMessageId");
    }
    return -1;
  }
 }

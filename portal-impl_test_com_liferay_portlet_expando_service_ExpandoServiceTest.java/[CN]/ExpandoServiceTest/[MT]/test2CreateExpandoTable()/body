{
  ExpandoTable table=ExpandoTableServiceUtil.addTable(ExpandoServiceTest.class.getName(),_TABLENAME);
  assertNotNull(table);
  assertEquals(table.getClassName(),ExpandoServiceTest.class.getName());
  assertEquals(table.getName(),_TABLENAME);
}

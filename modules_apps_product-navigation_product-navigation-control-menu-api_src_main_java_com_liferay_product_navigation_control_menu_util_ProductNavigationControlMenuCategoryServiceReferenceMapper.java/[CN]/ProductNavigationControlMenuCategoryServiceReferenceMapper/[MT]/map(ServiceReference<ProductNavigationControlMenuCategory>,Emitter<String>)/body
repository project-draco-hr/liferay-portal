{
  String controlMenuCategoryKey=(String)serviceReference.getProperty("product.navigation.control.menu.category.key");
  if (Validator.isNull(controlMenuCategoryKey)) {
    _log.error("Unable to register control menu category because of missing " + "service property " + "\"product.navigation.control.menu.category.key\"");
  }
 else {
    emitter.emit(controlMenuCategoryKey);
  }
}

{
  Class.forName("org.hsqldb.jdbcDriver");
  Connection con=DriverManager.getConnection("jdbc:hsqldb:mem:testSampleSQLBuilderDB;shutdown=true","sa","");
  DBLoader.loadHypersonic(con,_sqlDir + "/portal-minimal/portal-minimal-hypersonic.sql");
  DBLoader.loadHypersonic(con,_sqlDir + "/indexes/indexes-hypersonic.sql");
  DBLoader.loadHypersonic(con,_outputDir + "/sample-hypersonic.sql");
  Statement statement=con.createStatement();
  statement.execute("SHUTDOWN COMPACT");
  statement.close();
  con.close();
}

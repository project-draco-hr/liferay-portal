{
  GroupPermissionUtil.check(getPermissionChecker(),groupId,ActionKeys.ASSIGN_REVIEWER);
  tasksReviewLocalService.updateReviewers(getUserId(),groupId,proposalId,stage,reviewerIds,removeReviewerIds);
}

{
  TasksProposal proposal=tasksProposalPersistence.findByPrimaryKey(proposalId);
  GroupPermissionUtil.check(getPermissionChecker(),proposal.getGroupId(),ActionKeys.APPROVE_PROPOSAL);
  return tasksReviewLocalService.approveReview(getUserId(),proposalId,stage);
}

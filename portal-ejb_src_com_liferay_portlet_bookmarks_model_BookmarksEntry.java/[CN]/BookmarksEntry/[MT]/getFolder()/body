{
  BookmarksFolder folder=null;
  try {
    folder=BookmarksFolderLocalServiceUtil.getFolder(getFolderId());
  }
 catch (  Exception e) {
    folder=new BookmarksFolder();
    _log.error(e);
  }
  return folder;
}

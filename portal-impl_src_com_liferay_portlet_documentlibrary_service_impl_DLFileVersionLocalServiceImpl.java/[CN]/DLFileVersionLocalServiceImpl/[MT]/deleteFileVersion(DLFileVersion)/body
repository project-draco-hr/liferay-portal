{
  dlFileVersionPersistence.remove(fileVersion);
  workflowInstanceLinkLocalService.deleteWorkflowInstanceLink(fileVersion.getCompanyId(),fileVersion.getGroupId(),DLFileEntry.class.getName(),fileVersion.getFileVersionId());
}

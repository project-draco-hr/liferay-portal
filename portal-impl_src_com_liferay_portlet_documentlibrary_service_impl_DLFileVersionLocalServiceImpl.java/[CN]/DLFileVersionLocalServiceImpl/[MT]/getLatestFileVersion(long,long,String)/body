{
  List<DLFileVersion> fileVersions=dlFileVersionPersistence.findByG_F_N(groupId,folderId,name,0,1,new FileVersionComparator());
  if (fileVersions.size() == 0) {
    throw new NoSuchFileVersionException();
  }
  return fileVersions.get(0);
}

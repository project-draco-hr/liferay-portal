{
  if (status == StatusConstants.ANY) {
    return dlFileVersionPersistence.findByG_F_N(groupId,folderId,name);
  }
 else {
    return dlFileVersionPersistence.findByG_F_N_S(groupId,folderId,name,status);
  }
}

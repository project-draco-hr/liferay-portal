{
  DLFileVersion fileVersion=dlFileVersionPersistence.findByG_F_N_V(groupId,folderId,name,version);
  fileVersion.setDescription(description);
  dlFileVersionPersistence.update(fileVersion,false);
  return fileVersion;
}

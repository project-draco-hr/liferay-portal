{
  if (_log.isInfoEnabled()) {
    _log.info("Path " + path);
  }
  String encodedURL=res.encodeURL(path);
  if (_log.isInfoEnabled()) {
    _log.info("Encoded URL " + encodedURL);
  }
  return encodedURL;
}

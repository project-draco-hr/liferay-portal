{
  _clusterNodes.clear();
  for (int i=0; i < nodeNumber; i++) {
    String clusterNodeId=_CLUSTER_NODE_ID_PREFIX + i;
    _clusterNodes.put(clusterNodeId,new ClusterNode(clusterNodeId,_localhostInetAddress));
  }
}

{
  _bytes=new byte[_RESPONSE_MESSAGE.length];
  int offset=0;
  while (offset < _RESPONSE_MESSAGE.length) {
    int bytesRead=inputStream.read(_bytes,offset,_RESPONSE_MESSAGE.length - offset);
    if (bytesRead == -1) {
      break;
    }
    offset+=bytesRead;
  }
}

{
  ContentReference contentReference=ContentReference.fromInputStream(sourceFileName,mimeType,is,size);
  _modelValidator.validate(contentReference);
  return super.updateFileEntry(userId,fileEntryId,sourceFileName,mimeType,title,description,changeLog,majorVersion,is,size,serviceContext);
}

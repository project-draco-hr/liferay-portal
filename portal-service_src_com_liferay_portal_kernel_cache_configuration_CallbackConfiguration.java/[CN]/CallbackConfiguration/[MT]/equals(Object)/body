{
  if (this == object) {
    return true;
  }
  if (!(object instanceof CallbackConfiguration)) {
    return false;
  }
  CallbackConfiguration callbackConfiguration=(CallbackConfiguration)object;
  if ((_callbackFactory == callbackConfiguration._callbackFactory) && _properties.equals(callbackConfiguration._properties)) {
    return true;
  }
  return false;
}

{
  JarResource jarResource=new JarResource("META-INF/spring/context.dependencies","dependencies/META-INF/spring/empty.dependencies");
  Jar jar=analyze(Collections.<String>emptyList(),jarResource);
  Resource resource=jar.getResource("OSGI-INF/context/context.dependencies");
  String collect=IO.collect(resource.openInputStream());
  Assert.assertEquals("",collect);
}

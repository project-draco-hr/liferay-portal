{
  Jar jar=analyze(Arrays.asList(_PACKAGE_NAME_FILTER));
  Resource resource=jar.getResource("OSGI-INF/context/context.dependencies");
  String value=IO.collect(resource.openInputStream());
  Assert.assertEquals(_RELEASE_INFO + "java.lang.String (service.ranking=1)\n",value);
}

{
  if (isEscapedModel()) {
    return (UserGroupGroupRole)this;
  }
 else {
    UserGroupGroupRole model=new UserGroupGroupRoleImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setUserGroupId(getUserGroupId());
    model.setGroupId(getGroupId());
    model.setRoleId(getRoleId());
    model=(UserGroupGroupRole)Proxy.newProxyInstance(UserGroupGroupRole.class.getClassLoader(),new Class[]{UserGroupGroupRole.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

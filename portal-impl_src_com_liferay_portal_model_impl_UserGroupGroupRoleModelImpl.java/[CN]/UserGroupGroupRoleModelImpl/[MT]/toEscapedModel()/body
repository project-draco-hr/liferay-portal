{
  if (isEscapedModel()) {
    return (UserGroupGroupRole)this;
  }
 else {
    return (UserGroupGroupRole)Proxy.newProxyInstance(UserGroupGroupRole.class.getClassLoader(),new Class[]{UserGroupGroupRole.class},new AutoEscapeBeanHandler(this));
  }
}

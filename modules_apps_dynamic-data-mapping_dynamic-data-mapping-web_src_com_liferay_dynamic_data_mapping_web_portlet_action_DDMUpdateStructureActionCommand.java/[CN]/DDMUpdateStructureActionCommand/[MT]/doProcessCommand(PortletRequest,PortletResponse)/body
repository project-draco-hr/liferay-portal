{
  DDMStructure structure=updateStructure(portletRequest);
  String redirect=ParamUtil.getString(portletRequest,DDMConstants.REDIRECT);
  redirect=super.setRedirectAttribute(portletRequest,redirect);
  boolean saveAndContinue=ParamUtil.getBoolean(portletRequest,DDMConstants.SAVE_AND_CONTINUE);
  if (saveAndContinue) {
    redirect=getSaveAndContinueRedirect(portletRequest,structure,redirect);
    portletRequest.setAttribute(WebKeys.REDIRECT,redirect);
  }
}

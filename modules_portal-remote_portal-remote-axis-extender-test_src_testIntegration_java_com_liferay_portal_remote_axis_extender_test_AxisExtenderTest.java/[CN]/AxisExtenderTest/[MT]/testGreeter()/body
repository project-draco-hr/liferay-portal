{
  URL url=new URL(_url,"/o/com.liferay.portal.remote.axis.extender.test/api/axis/" + "CalcService?wsdl");
  CalcServiceSoapService calcServiceSoapServiceLocator=new CalcServiceSoapServiceLocator();
  CalcServiceSoap calcServiceSoapPort=calcServiceSoapServiceLocator.getCalcServiceSoapPort(url);
  Assert.assertEquals(5,calcServiceSoapPort.sum(2,3));
}

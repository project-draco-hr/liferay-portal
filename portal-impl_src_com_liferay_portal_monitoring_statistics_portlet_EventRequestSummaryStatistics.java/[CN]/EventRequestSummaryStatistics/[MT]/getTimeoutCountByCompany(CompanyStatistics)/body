{
  long timeoutCount=0;
  for (  RequestStatistics requestStatistics : companyStatistics.getEventRequestStatisticsSet()) {
    timeoutCount+=requestStatistics.getTimeoutCount();
  }
  return timeoutCount;
}

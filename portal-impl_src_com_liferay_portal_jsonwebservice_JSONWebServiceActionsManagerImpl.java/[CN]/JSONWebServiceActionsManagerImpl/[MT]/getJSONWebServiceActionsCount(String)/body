{
  int count=0;
  for (int i=0; i < _jsonWebServiceActionConfigs.size(); i++) {
    JSONWebServiceActionConfig jsonWebServiceActionConfig=_jsonWebServiceActionConfigs.get(i);
    if (contextName.equals(jsonWebServiceActionConfig.getContextName())) {
      count++;
    }
  }
  return count;
}

{
  int count=0;
  for (  JSONWebServiceActionConfig jsonWebServiceActionConfig : _jsonWebServiceActionConfigs) {
    String actionContextPath=jsonWebServiceActionConfig.getContextPath();
    if (contextPath.equals(actionContextPath)) {
      count++;
    }
  }
  return count;
}

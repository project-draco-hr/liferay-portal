{
  List<JSONWebServiceActionMapping> mappings=new ArrayList<JSONWebServiceActionMapping>(_jsonWebServiceActionConfigs.size());
  for (  JSONWebServiceActionConfig jsonWebServiceActionConfig : _jsonWebServiceActionConfigs) {
    mappings.add(jsonWebServiceActionConfig);
  }
  return mappings;
}

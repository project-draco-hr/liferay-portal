{
  if (isEscapedModel()) {
    return (FormsStructureEntryLink)this;
  }
 else {
    return (FormsStructureEntryLink)Proxy.newProxyInstance(FormsStructureEntryLink.class.getClassLoader(),new Class[]{FormsStructureEntryLink.class},new AutoEscapeBeanHandler(this));
  }
}

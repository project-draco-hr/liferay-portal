{
  Session session=null;
  try {
    session=openSession();
    return (ClassNameMapper)session.get(ClassNameMapperImpl.class,new Long(classNameMapperId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

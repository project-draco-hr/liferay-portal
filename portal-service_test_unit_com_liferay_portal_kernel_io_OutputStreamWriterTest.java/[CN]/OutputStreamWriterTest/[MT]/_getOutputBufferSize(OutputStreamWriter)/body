{
  Field field=ReflectionUtil.getDeclaredField(OutputStreamWriter.class,"_outputByteBuffer");
  ByteBuffer outputBuffer=(ByteBuffer)field.get(outputStreamWriter);
  return outputBuffer.capacity();
}

{
  try (PreparedStatement ps=connection.prepareStatement("select id_ from JournalStructure where structureId = ?")){
    ps.setString(1,structureId);
    try (ResultSet rs=ps.executeQuery()){
      if (rs.next()) {
        return rs.getLong("id_");
      }
      return 0;
    }
   }
 }

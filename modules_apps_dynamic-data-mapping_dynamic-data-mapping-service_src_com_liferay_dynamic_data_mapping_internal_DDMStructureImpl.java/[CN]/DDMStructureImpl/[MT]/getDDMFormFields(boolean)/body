{
  List<com.liferay.portlet.dynamicdatamapping.model.DDMFormField> fields=_structure.getDDMFormFields(includeTransientFields);
  List<DDMFormField> ddmFormFields=new ArrayList<>();
  try {
    for (    com.liferay.portlet.dynamicdatamapping.model.DDMFormField field : fields) {
      ddmFormFields.add(BeanPropertiesUtil.deepCopyProperties(field,DDMFormField.class));
    }
  }
 catch (  Exception e) {
  }
  return ddmFormFields;
}

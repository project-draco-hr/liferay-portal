{
  if (isEscapedModel()) {
    return (DDMListEntry)this;
  }
 else {
    return (DDMListEntry)Proxy.newProxyInstance(DDMListEntry.class.getClassLoader(),new Class[]{DDMListEntry.class},new AutoEscapeBeanHandler(this));
  }
}

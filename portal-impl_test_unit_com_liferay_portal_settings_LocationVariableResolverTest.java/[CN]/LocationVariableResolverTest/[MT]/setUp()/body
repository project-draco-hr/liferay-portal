{
  FileUtil fileUtil=new FileUtil();
  fileUtil.setFile(new FileImpl());
  _mockResourceManager=new MockResourceManager("En un lugar de la Mancha...");
  _mockSettingsFactory=mock(SettingsFactory.class);
  _locationVariableResolver=new LocationVariableResolver(_mockResourceManager,_mockSettingsFactory);
}

{
  LocationVariableResolver locationVariableResolver=new LocationVariableResolver(null);
  Assert.assertFalse(locationVariableResolver.isLocationVariable("this is obviously not a location variable"));
}

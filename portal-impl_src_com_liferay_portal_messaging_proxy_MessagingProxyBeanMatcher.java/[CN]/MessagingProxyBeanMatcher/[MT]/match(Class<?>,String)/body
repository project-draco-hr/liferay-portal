{
  if (_beanClass.isAssignableFrom(beanClass) && StringUtil.wildcardMatches(beanName,_beanNamePattern,CharPool.QUESTION,CharPool.STAR,CharPool.PERCENT,true)) {
    return true;
  }
  return false;
}

{
  if (this == object) {
    return true;
  }
  if (!(object instanceof IndexerRequest)) {
    return false;
  }
  IndexerRequest indexerRequest=(IndexerRequest)object;
  if (Validator.equals(_indexer,indexerRequest._indexer) && Validator.equals(_modelClass,indexerRequest._modelClass) && Validator.equals(_methodHandler,indexerRequest._methodHandler)) {
    return true;
  }
  return false;
}

{
  try {
    removePluginPackage(moduleId,repositoryURL);
  }
 catch (  IOException ioe) {
  }
  addPluginPackage(moduleId,name,version,modifiedDate,author,types,tags,licenses,liferayVersions,shortDescription,longDescription,changeLog,pageURL,repositoryURL,status,installedVersion);
}

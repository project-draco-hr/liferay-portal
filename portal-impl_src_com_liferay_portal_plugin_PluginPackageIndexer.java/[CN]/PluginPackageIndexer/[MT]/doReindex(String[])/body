{
  SearchEngineUtil.deletePortletDocuments(CompanyConstants.SYSTEM,PORTLET_ID);
  for (  PluginPackage pluginPackage : PluginPackageUtil.getAllAvailablePluginPackages()) {
    doReindex(pluginPackage);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (PresenceStatuses)session.get(PresenceStatusesImpl.class,new Long(presenceStatusId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

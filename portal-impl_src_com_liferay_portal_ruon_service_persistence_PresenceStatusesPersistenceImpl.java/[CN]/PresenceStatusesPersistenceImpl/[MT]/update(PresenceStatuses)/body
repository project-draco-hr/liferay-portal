{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PresenceStatuses presenceStatuses) method. Use update(PresenceStatuses presenceStatuses, boolean merge) instead.");
  }
  return update(presenceStatuses,false);
}

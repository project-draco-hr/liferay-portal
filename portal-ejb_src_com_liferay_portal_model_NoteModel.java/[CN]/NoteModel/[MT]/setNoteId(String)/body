{
  if (((noteId == null) && (_noteId != null)) || ((noteId != null) && (_noteId == null)) || ((noteId != null) && (_noteId != null) && !noteId.equals(_noteId))) {
    if (!XSS_ALLOW_NOTEID) {
      noteId=XSSUtil.strip(noteId);
    }
    _noteId=noteId;
    setModified(true);
  }
}

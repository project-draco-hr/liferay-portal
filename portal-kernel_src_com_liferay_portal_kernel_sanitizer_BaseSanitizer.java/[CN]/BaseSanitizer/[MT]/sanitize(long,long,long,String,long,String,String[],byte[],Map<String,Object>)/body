{
  ByteArrayOutputStream byteArrayOutputStream=new ByteArrayOutputStream();
  sanitize(companyId,groupId,userId,className,classPK,contentType,modes,new ByteArrayInputStream(byteArray),byteArrayOutputStream,options);
  return byteArrayOutputStream.toByteArray();
}

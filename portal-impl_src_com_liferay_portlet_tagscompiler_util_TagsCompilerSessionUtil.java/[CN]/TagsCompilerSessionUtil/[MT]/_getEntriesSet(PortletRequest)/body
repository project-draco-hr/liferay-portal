{
  PortletSession ses=req.getPortletSession();
  Set<String> entriesSet=(Set<String>)ses.getAttribute(WebKeys.TAGS_COMPILER_ENTRIES,PortletSession.APPLICATION_SCOPE);
  if (entriesSet == null) {
    entriesSet=new TreeSet<String>();
    ses.setAttribute(WebKeys.TAGS_COMPILER_ENTRIES,entriesSet,PortletSession.APPLICATION_SCOPE);
  }
  return entriesSet;
}

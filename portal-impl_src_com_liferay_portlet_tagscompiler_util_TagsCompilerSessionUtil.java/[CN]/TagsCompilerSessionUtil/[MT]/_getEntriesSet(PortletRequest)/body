{
  PortletSession portletSession=portletRequest.getPortletSession();
  Set<String> entriesSet=(Set<String>)portletSession.getAttribute(WebKeys.TAGS_COMPILER_ENTRIES,PortletSession.APPLICATION_SCOPE);
  if (entriesSet == null) {
    entriesSet=new TreeSet<String>();
    portletSession.setAttribute(WebKeys.TAGS_COMPILER_ENTRIES,entriesSet,PortletSession.APPLICATION_SCOPE);
  }
  return entriesSet;
}

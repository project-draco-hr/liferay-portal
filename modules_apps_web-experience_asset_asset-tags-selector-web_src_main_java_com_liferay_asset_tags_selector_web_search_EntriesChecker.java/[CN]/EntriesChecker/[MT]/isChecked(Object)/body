{
  AssetTag tag=(AssetTag)obj;
  String[] selectedTags=StringUtil.split(ParamUtil.getString(_portletRequest,"selectedTags"));
  if (ArrayUtil.isEmpty(selectedTags)) {
    return false;
  }
  if (!ArrayUtil.contains(selectedTags,tag.getName())) {
    return false;
  }
  return true;
}

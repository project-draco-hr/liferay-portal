{
  if (!passwordPolicy.isDefaultPolicy()) {
    return;
  }
  try {
    _sacpEntryLocalService.checkDefaultProfile(passwordPolicy.getCompanyId());
  }
 catch (  PortalException pe) {
    throw new ModelListenerException(pe);
  }
}

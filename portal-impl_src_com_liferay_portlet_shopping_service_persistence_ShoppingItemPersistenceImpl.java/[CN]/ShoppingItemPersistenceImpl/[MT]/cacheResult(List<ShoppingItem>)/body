{
  for (  ShoppingItem shoppingItem : shoppingItems) {
    if (EntityCacheUtil.getResult(ShoppingItemModelImpl.ENTITY_CACHE_ENABLED,ShoppingItem.class,shoppingItem.getPrimaryKey(),this) == null) {
      cacheResult(shoppingItem);
    }
  }
}

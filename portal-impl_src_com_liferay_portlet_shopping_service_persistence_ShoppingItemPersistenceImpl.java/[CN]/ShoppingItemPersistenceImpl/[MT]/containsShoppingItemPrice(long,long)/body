{
  Object[] finderArgs=new Object[]{pk,shoppingItemPricePK};
  Boolean value=(Boolean)FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_SHOPPINGITEMPRICE,finderArgs,this);
  if (value == null) {
    try {
      value=Boolean.valueOf(containsShoppingItemPrice.contains(pk,shoppingItemPricePK));
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_SHOPPINGITEMPRICE,finderArgs,value);
    }
 catch (    Exception e) {
      FinderCacheUtil.removeResult(FINDER_PATH_CONTAINS_SHOPPINGITEMPRICE,finderArgs);
      throw processException(e);
    }
  }
  return value.booleanValue();
}

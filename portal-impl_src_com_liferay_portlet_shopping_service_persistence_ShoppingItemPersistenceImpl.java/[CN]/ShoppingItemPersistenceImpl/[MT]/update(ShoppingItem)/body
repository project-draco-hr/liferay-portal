{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingItem shoppingItem) method. To improve performance, call update(ShoppingItem shoppingItem, boolean merge).");
  }
  return update(shoppingItem,false);
}

{
  data=null;
  try {
    data=rsvc.parse(_reader,name);
    initDocument();
    return true;
  }
 catch (  Exception e) {
    throw new ParseErrorException("Unable to parse Velocity template");
  }
 finally {
    if (_reader != null) {
      _reader.close();
    }
  }
}

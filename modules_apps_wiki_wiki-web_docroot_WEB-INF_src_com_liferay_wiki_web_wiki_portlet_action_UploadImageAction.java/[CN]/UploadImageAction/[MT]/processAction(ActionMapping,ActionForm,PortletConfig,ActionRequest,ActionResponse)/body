{
  long resourcePrimKey=ParamUtil.getLong(actionRequest,"resourcePrimKey");
  UploadHandler uploadHandler=new ImageWikiUploadHandler(resourcePrimKey);
  uploadHandler.upload(actionRequest,actionResponse);
  return;
}

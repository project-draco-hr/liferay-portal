{
  String portletTitle=null;
  try {
    portletTitle=AtomUtil.createFeedTitleFromPortletName(requestContext,PortletKeys.BLOGS);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return portletTitle;
}

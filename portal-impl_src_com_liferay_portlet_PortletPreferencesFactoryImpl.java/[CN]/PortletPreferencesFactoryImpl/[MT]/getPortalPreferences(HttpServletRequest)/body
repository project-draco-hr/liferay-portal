{
  ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
  return getPortalPreferences(themeDisplay.getCompanyId(),themeDisplay.getUserId(),themeDisplay.isSignedIn(),request.getSession());
}

{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyId);
    return CompanyStatistics.getRequestStatistics().getRollingAverageTime();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get average response time for: " + companyId,e);
  }
}

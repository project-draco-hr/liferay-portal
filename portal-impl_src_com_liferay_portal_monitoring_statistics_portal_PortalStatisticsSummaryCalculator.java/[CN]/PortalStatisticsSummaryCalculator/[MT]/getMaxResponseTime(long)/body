{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyId);
    return CompanyStatistics.getRequestStatistics().getMaxTime();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get max response time for: " + companyId,e);
  }
}

{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyWebId);
    return CompanyStatistics.getRequestStatistics().getNumRequests();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get num requests for: " + companyWebId,e);
  }
}

{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyWebId);
    return CompanyStatistics.getRequestStatistics().getNumSuccesses();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get num successes for: " + companyWebId,e);
  }
}

{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyId);
    return CompanyStatistics.getRequestStatistics().getNumRequests();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get num requests for: " + companyId,e);
  }
}

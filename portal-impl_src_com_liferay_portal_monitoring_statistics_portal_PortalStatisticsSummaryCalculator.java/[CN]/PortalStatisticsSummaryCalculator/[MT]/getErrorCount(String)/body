{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyWebId);
    return CompanyStatistics.getRequestStatistics().getNumErrors();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get num errors for: " + companyWebId,e);
  }
}

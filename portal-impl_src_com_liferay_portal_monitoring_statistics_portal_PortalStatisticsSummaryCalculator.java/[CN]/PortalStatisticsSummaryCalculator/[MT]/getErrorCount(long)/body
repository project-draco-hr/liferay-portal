{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyId);
    return CompanyStatistics.getRequestStatistics().getNumErrors();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get num errors for: " + companyId,e);
  }
}

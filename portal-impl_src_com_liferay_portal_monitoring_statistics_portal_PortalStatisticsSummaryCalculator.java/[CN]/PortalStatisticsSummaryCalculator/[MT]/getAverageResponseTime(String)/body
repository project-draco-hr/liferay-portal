{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyWebId);
    return CompanyStatistics.getRequestStatistics().getRollingAverageTime();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get average response time for: " + companyWebId,e);
  }
}

{
  try {
    CompanyStatistics CompanyStatistics=_serverStatistics.getCompanyStatistics(companyId);
    return CompanyStatistics.getRequestStatistics().getNumSuccesses();
  }
 catch (  Exception e) {
    throw new MonitoringException("Unable to get num timeouts for: " + companyId,e);
  }
}

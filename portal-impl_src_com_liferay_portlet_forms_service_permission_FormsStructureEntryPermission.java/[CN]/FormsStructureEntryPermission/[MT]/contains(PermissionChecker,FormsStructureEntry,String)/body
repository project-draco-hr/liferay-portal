{
  if (permissionChecker.hasOwnerPermission(structureEntry.getCompanyId(),FormsStructureEntry.class.getName(),structureEntry.getStructureEntryId(),structureEntry.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(structureEntry.getGroupId(),FormsStructureEntry.class.getName(),structureEntry.getStructureEntryId(),actionId);
}

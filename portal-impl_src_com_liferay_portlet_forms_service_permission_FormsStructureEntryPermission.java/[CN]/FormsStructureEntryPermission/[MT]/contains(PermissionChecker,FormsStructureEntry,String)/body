{
  if (permissionChecker.hasOwnerPermission(entry.getCompanyId(),FormsStructureEntry.class.getName(),entry.getId(),entry.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(entry.getGroupId(),FormsStructureEntry.class.getName(),entry.getId(),actionId);
}

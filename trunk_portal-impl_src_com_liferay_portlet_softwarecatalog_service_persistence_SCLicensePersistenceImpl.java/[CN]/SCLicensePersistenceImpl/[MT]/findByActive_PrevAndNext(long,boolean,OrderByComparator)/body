{
  SCLicense scLicense=findByPrimaryKey(licenseId);
  Session session=null;
  try {
    session=openSession();
    SCLicense[] array=new SCLicenseImpl[3];
    array[0]=getByActive_PrevAndNext(session,scLicense,active,orderByComparator,true);
    array[1]=scLicense;
    array[2]=getByActive_PrevAndNext(session,scLicense,active,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

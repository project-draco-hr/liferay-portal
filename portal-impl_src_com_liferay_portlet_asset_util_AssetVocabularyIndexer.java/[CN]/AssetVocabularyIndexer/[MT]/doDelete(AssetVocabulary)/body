{
  Document document=new DocumentImpl();
  document.addUID(CLASS_NAME,assetVocabulary.getVocabularyId());
  SearchEngineUtil.deleteDocument(getSearchEngineId(),assetVocabulary.getCompanyId(),document.get(Field.UID),isCommitImmediately());
}

{
  AssetVocabulary vocabulary=(AssetVocabulary)obj;
  Document document=new DocumentImpl();
  document.addUID(CLASS_NAME,vocabulary.getVocabularyId());
  SearchEngineUtil.deleteDocument(getSearchEngineId(),vocabulary.getCompanyId(),document.get(Field.UID),isCommitImmediately());
}

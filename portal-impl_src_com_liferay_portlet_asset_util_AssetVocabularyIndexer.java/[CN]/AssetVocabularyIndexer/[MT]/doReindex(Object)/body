{
  AssetVocabulary assetVocabulary=(AssetVocabulary)obj;
  Document document=getDocument(assetVocabulary);
  SearchEngineUtil.updateDocument(getSearchEngineId(),assetVocabulary.getCompanyId(),document);
}

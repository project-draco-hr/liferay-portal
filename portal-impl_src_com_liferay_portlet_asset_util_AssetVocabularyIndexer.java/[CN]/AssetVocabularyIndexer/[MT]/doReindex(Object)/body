{
  AssetVocabulary vocabulary=(AssetVocabulary)obj;
  Document document=getDocument(vocabulary);
  if (document != null) {
    SearchEngineUtil.updateDocument(getSearchEngineId(),vocabulary.getCompanyId(),document,isCommitImmediately());
  }
}

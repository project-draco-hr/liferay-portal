{
synchronized (ses) {
    SharedSessionAttributeCache cache=(SharedSessionAttributeCache)ses.getAttribute(_SESSION_KEY);
    if (cache == null) {
      cache=new SharedSessionAttributeCache();
      ses.setAttribute(_SESSION_KEY,cache);
    }
    return cache;
  }
}

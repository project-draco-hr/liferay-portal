{
  if (jsonRpcRequest == null) {
    return;
  }
  Iterator<Map.Entry<String,String>> iterator=jsonRpcRequest.parametersIterator();
  while (iterator.hasNext()) {
    Map.Entry<String,String> param=iterator.next();
    ObjectValuePair<String,Object> objectValuePair=new ObjectValuePair<String,Object>(param.getKey(),param.getValue());
    _parameters.add(objectValuePair);
  }
}

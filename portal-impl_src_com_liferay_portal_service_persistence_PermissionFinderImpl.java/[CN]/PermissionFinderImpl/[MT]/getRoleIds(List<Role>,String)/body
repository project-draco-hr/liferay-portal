{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < roles.size(); i++) {
    sb.append(table);
    sb.append(".roleId = ?");
    if ((i + 1) < roles.size()) {
      sb.append(" OR ");
    }
  }
  return sb.toString();
}

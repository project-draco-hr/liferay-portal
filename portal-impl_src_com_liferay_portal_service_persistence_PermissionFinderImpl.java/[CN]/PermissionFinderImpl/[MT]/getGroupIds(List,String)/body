{
  StringMaker sm=new StringMaker();
  for (int i=0; i < groups.size(); i++) {
    sm.append(table);
    sm.append(".groupId = ?");
    if ((i + 1) < groups.size()) {
      sm.append(" OR ");
    }
  }
  return sm.toString();
}

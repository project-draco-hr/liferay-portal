{
  if (isNew) {
    Object[] args=new Object[]{scProductEntry.getRepoGroupId(),scProductEntry.getRepoArtifactId()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_RG_RA,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_RG_RA,args,scProductEntry);
  }
 else {
    SCProductEntryModelImpl scProductEntryModelImpl=(SCProductEntryModelImpl)scProductEntry;
    if ((scProductEntryModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_RG_RA.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{scProductEntry.getRepoGroupId(),scProductEntry.getRepoArtifactId()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_RG_RA,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_RG_RA,args,scProductEntry);
    }
  }
}

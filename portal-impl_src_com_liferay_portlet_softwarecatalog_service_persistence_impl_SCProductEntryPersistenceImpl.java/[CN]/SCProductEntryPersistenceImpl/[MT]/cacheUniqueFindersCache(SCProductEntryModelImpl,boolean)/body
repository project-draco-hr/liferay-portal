{
  if (isNew) {
    Object[] args=new Object[]{scProductEntryModelImpl.getRepoGroupId(),scProductEntryModelImpl.getRepoArtifactId()};
    finderCache.putResult(FINDER_PATH_COUNT_BY_RG_RA,args,Long.valueOf(1));
    finderCache.putResult(FINDER_PATH_FETCH_BY_RG_RA,args,scProductEntryModelImpl);
  }
 else {
    if ((scProductEntryModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_RG_RA.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{scProductEntryModelImpl.getRepoGroupId(),scProductEntryModelImpl.getRepoArtifactId()};
      finderCache.putResult(FINDER_PATH_COUNT_BY_RG_RA,args,Long.valueOf(1));
      finderCache.putResult(FINDER_PATH_FETCH_BY_RG_RA,args,scProductEntryModelImpl);
    }
  }
}

{
  for (  SCProductEntry scProductEntry : scProductEntries) {
    if (entityCache.getResult(SCProductEntryModelImpl.ENTITY_CACHE_ENABLED,SCProductEntryImpl.class,scProductEntry.getPrimaryKey()) == null) {
      cacheResult(scProductEntry);
    }
 else {
      scProductEntry.resetOriginalValues();
    }
  }
}

{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByGroupId_PrevAndNext(productEntryId,groupId,orderByComparator);
  }
  SCProductEntry scProductEntry=findByPrimaryKey(productEntryId);
  Session session=null;
  try {
    session=openSession();
    SCProductEntry[] array=new SCProductEntryImpl[3];
    array[0]=filterGetByGroupId_PrevAndNext(session,scProductEntry,groupId,orderByComparator,true);
    array[1]=scProductEntry;
    array[2]=filterGetByGroupId_PrevAndNext(session,scProductEntry,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

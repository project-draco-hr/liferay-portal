{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(fileEntryId));
  }
  com.liferay.portlet.documentlibrary.model.DLFileEntry dlFileEntry=getPersistence().remove(fileEntryId);
  if (listener != null) {
    listener.onAfterRemove(dlFileEntry);
  }
  return dlFileEntry;
}

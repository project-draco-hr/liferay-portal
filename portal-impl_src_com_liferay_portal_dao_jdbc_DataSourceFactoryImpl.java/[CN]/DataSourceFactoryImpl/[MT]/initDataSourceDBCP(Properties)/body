{
  DataSource basicDataSource=BasicDataSourceFactory.createDataSource(properties);
  registerConnectionPoolMetrics(new DBCPConnectionPoolMetrics((BasicDataSource)basicDataSource));
  return basicDataSource;
}

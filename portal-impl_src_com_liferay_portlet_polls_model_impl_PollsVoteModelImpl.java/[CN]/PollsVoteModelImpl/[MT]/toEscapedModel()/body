{
  if (isEscapedModel()) {
    return (PollsVote)this;
  }
 else {
    return (PollsVote)Proxy.newProxyInstance(PollsVote.class.getClassLoader(),new Class[]{PollsVote.class},new AutoEscapeBeanHandler(this));
  }
}

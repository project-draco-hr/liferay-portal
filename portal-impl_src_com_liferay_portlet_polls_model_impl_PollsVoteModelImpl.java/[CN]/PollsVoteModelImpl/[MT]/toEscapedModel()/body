{
  if (isEscapedModel()) {
    return (PollsVote)this;
  }
 else {
    PollsVote model=new PollsVoteImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setVoteId(getVoteId());
    model.setUserId(getUserId());
    model.setQuestionId(getQuestionId());
    model.setChoiceId(getChoiceId());
    model.setVoteDate(getVoteDate());
    model=(PollsVote)Proxy.newProxyInstance(PollsVote.class.getClassLoader(),new Class[]{PollsVote.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

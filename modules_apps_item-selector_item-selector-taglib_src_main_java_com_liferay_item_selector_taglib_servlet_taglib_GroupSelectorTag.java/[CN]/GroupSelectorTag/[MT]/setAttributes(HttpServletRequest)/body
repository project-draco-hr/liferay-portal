{
  request.setAttribute("liferay-item-selector:group-selector:groupCount",getGroupCount(request));
  request.setAttribute("liferay-item-selector:group-selector:groups",getGroups(request));
  request.setAttribute("liferay-item-selector:group-selector:itemSelector",ItemSelectorUtil.getItemSelector());
}

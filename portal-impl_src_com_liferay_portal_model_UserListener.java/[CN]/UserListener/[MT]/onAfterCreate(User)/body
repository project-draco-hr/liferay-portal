{
  try {
    if (!user.isDefaultUser() && !LDAPUserTransactionThreadLocal.getOriginatesFromLDAP()) {
      PortalLDAPUtil.exportToLDAP(user);
    }
  }
 catch (  Exception e) {
    throw new ModelListenerException(e);
  }
}

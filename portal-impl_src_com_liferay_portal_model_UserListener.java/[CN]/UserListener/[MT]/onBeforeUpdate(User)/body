{
  if (!(user instanceof UserModelImpl)) {
    return;
  }
  UserModelImpl userModel=(UserModelImpl)user;
  LDAPUserTransactionThreadLocal.setOriginalEmailAddress(userModel.getOriginalEmailAddress());
}

{
  Properties props=PropsUtil.getProperties("workflow.definition.name.",true);
  if (props != null) {
    for (    Object key : props.keySet()) {
      _workflowDefinitionNames.put((String)key,props.getProperty((String)key));
    }
  }
}

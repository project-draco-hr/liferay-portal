{
  WikiPagePermissionChecker.check(getPermissionChecker(),nodeId,title,ActionKeys.DELETE);
  WikiNodePermissionChecker.check(getPermissionChecker(),newNodeId,ActionKeys.ADD_PAGE);
  wikiPageLocalService.changeNode(getUserId(),nodeId,title,newNodeId,serviceContext);
}

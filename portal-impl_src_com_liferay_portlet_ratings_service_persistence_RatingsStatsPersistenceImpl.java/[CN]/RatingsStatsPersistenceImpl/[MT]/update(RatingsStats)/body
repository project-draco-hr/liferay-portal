{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(RatingsStats ratingsStats) method. To improve performance, call update(RatingsStats ratingsStats, boolean merge).");
  }
  return update(ratingsStats,false);
}

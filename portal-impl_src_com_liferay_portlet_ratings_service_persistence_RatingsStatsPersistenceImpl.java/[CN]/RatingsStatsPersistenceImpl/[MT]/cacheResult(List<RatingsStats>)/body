{
  for (  RatingsStats ratingsStats : ratingsStatses) {
    if (EntityCacheUtil.getResult(RatingsStatsModelImpl.ENTITY_CACHE_ENABLED,RatingsStats.class,ratingsStats.getPrimaryKey(),this) == null) {
      cacheResult(ratingsStats);
    }
  }
}

{
  if (isEscapedModel()) {
    return (JournalFeed)this;
  }
 else {
    return (JournalFeed)Proxy.newProxyInstance(JournalFeed.class.getClassLoader(),new Class[]{JournalFeed.class},new AutoEscapeBeanHandler(this));
  }
}

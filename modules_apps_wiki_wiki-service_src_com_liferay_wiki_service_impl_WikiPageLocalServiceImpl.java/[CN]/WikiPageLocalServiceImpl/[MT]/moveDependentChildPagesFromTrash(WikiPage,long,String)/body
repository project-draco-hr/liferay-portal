{
  List<WikiPage> childPages=getChildren(nodeId,true,trashTitle,WorkflowConstants.STATUS_IN_TRASH);
  for (  WikiPage childPage : childPages) {
    childPage.setParentTitle(page.getTitle());
    wikiPagePersistence.update(childPage);
    if (childPage.isInTrashImplicitly()) {
      moveDependentFromTrash(childPage,page.getNodeId(),page.getTitle());
    }
  }
}

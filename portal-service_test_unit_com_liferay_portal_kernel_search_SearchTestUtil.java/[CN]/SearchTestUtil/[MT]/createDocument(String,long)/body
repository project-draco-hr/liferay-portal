{
  Document document=new DocumentImpl();
  document.add(new Field(Field.ENTRY_CLASS_NAME,entryClassName));
  document.add(new Field(Field.ENTRY_CLASS_PK,String.valueOf(entryClassPk)));
  return document;
}

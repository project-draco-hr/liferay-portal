{
  SRProductVersionPermission.check(getPermissionChecker(),productVersionId,ActionKeys.UPDATE);
  return SRProductVersionLocalServiceUtil.updateProductVersion(productVersionId,version,changeLog,downloadPageURL,directDownloadURL,repoStoreArtifact,frameworkVersionIds);
}

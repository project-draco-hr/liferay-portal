{
  MBThread thread=MBThreadLocalServiceUtil.getThread(classPK);
  if ((thread.getCategoryId() > 0) && (MBCategoryLocalServiceUtil.fetchMBCategory(thread.getCategoryId()) == null)) {
    return false;
  }
  return !thread.isInTrashContainer();
}

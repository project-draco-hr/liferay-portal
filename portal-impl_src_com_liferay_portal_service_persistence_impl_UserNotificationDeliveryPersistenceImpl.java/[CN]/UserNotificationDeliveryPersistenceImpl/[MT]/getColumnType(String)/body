{
  Integer type=UserNotificationDeliveryModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ UserNotificationDeliveryModelImpl.TABLE_NAME);
  }
  return type;
}

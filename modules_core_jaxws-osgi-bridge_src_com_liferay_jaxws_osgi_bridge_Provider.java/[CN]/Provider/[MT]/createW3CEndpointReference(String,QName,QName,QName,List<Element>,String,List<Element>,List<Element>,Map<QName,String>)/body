{
  javax.xml.ws.spi.Provider provider=_getProvider();
  return provider.createW3CEndpointReference(address,interfaceName,serviceName,portName,metadata,wsdlDocumentLocation,referenceParameters,elements,attributes);
}

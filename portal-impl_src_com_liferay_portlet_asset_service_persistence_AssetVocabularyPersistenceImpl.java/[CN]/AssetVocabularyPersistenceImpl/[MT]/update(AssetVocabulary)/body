{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetVocabulary assetVocabulary) method. Use update(AssetVocabulary assetVocabulary, boolean merge) instead.");
  }
  return update(assetVocabulary,false);
}

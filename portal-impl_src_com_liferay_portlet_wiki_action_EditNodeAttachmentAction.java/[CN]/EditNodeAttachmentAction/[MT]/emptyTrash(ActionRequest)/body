{
  long nodeId=ParamUtil.getLong(actionRequest,"nodeId");
  List<WikiPage> wikiPages=WikiPageLocalServiceUtil.getPages(nodeId,QueryUtil.ALL_POS,QueryUtil.ALL_POS);
  for (  WikiPage wikiPage : wikiPages) {
    String[] deletedAttachmentsFiles=wikiPage.getDeletedAttachmentsFiles();
    if (deletedAttachmentsFiles.length > 0) {
      WikiPageServiceUtil.emptyPageAttachments(nodeId,wikiPage.getTitle());
    }
  }
}

{
  int count=countByC_KDI(companyId,kaleoDefinitionId);
  if (count == 0) {
    return null;
  }
  List<KaleoNode> list=findByC_KDI(companyId,kaleoDefinitionId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  Set<String> settingsIds=_settingsDefinitionSettingsDescriptor.getSettingsIds();
  Collection<String> expectedSettingsIds=Arrays.asList("settingsId.1","settingsId.2");
  Assert.assertEquals(settingsIds.size(),expectedSettingsIds.size());
  Assert.assertTrue(settingsIds.containsAll(expectedSettingsIds));
}

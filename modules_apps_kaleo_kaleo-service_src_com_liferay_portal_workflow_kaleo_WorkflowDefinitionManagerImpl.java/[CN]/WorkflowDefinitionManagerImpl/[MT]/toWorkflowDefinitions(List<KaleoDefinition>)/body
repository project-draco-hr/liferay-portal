{
  List<WorkflowDefinition> workflowDefinitions=new ArrayList<>(kaleoDefinitions.size());
  for (  KaleoDefinition kaleoDefinition : kaleoDefinitions) {
    workflowDefinitions.add(WorkflowModelUtil.toWorkflowDefinition(kaleoDefinition));
  }
  return workflowDefinitions;
}

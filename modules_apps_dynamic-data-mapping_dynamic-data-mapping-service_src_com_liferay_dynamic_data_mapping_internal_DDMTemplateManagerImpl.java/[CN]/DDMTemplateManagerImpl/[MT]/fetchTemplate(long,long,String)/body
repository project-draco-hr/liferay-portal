{
  com.liferay.portlet.dynamicdatamapping.model.DDMTemplate template=_ddmTemplateLocalService.fetchTemplate(groupId,classNameId,templateKey);
  if (template == null) {
    return null;
  }
  return new DDMTemplateImpl(template);
}

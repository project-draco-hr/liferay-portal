{
  List<DDMTemplate> ddmTemplates=new ArrayList<>();
  for (  com.liferay.dynamic.data.mapping.model.DDMTemplate ddmTemplate : _ddmTemplateLocalService.getTemplates(groupIds,classNameId,classPK)) {
    ddmTemplates.add(new DDMTemplateImpl(ddmTemplate));
  }
  return ddmTemplates;
}

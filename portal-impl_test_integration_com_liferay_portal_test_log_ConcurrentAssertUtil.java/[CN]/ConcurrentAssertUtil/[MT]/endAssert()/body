{
  _assertingThread=null;
  for (  Map.Entry<Thread,String> entry : _concurrentErrors.entrySet()) {
    Assert.fail("Caught concurrent error : " + entry.getValue() + " at thread : "+ entry.getKey());
  }
  _concurrentErrors.clear();
}

{
  SystemProperties.set("default.liferay.home",_getDefaultLiferayHome());
  _configuration=new ConfigurationImpl(PropsUtil.class.getClassLoader(),PropsFiles.PORTAL);
  SystemProperties.set(PropsKeys.LIFERAY_HOME,_get(PropsKeys.LIFERAY_HOME));
  SystemProperties.set(PropsKeys.RESOURCE_REPOSITORIES_ROOT,_get(PropsKeys.RESOURCE_REPOSITORIES_ROOT));
}

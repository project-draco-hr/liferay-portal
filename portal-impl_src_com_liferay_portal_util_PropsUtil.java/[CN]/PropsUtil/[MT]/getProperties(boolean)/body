{
  Properties properties=_instance._getProperties();
  if (!includeSystem) {
    return properties;
  }
  Properties systemCompanyProperties=_instance._getProperties(CompanyConstants.SYSTEM);
  Properties mergedProperties=(Properties)systemCompanyProperties.clone();
  mergedProperties.putAll(properties);
  return mergedProperties;
}

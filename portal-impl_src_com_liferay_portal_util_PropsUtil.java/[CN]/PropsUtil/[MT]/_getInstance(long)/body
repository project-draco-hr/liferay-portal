{
  ClassLoader classLoader=PropsUtil.class.getClassLoader();
  if (companyId > CompanyConstants.SYSTEM) {
    return ExtPropertiesLoader.getInstance(classLoader,PropsFiles.PORTAL,companyId);
  }
 else {
    return ExtPropertiesLoader.getInstance(classLoader,PropsFiles.PORTAL);
  }
}

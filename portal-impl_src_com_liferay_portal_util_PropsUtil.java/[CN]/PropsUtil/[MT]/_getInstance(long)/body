{
  if (companyId > CompanyConstants.SYSTEM) {
    return ExtPropertiesLoader.getInstance(PropsFiles.PORTAL,companyId);
  }
 else {
    return ExtPropertiesLoader.getInstance(PropsFiles.PORTAL);
  }
}

{
  if (_fixedIssues != null) {
    return _fixedIssues;
  }
  Properties properties=getProperties();
  _fixedIssues=StringUtil.split(properties.getProperty(Patcher.FIXED_ISSUES));
  return _fixedIssues;
}

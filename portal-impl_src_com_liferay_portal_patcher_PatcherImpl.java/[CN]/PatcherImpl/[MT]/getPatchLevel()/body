{
  if (_patchLevel != null) {
    return _patchLevel;
  }
  Properties properties=getProperties();
  _patchLevel=StringUtil.split(properties.getProperty(PROPERTY_PATCH_LEVEL));
  return _patchLevel;
}

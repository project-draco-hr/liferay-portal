{
  if (_installedPatches != null) {
    return _installedPatches;
  }
  String property=getProperties().getProperty(Patcher.INSTALLED_PATCHES);
  _installedPatches=property.split(",");
  return _installedPatches;
}

{
  if (_installedPatches != null) {
    return _installedPatches;
  }
  Properties properties=getProperties();
  _installedPatches=StringUtil.split(properties.getProperty(Patcher.INSTALLED_PATCHES));
  return _installedPatches;
}

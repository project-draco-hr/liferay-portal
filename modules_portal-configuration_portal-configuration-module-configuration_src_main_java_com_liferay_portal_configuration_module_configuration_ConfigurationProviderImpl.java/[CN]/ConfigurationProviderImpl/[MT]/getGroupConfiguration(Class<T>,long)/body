{
  String configurationPid=getConfigurationPid(clazz);
  String settingsId=_getConfigurationSettingsId(clazz);
  return getConfiguration(clazz,new GroupServiceSettingsLocator(groupId,settingsId,configurationPid));
}

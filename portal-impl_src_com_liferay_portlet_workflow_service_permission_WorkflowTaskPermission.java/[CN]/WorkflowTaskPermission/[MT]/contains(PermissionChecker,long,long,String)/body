{
  if (permissionChecker.hasPermission(0,WorkflowTask.class.getName(),taskId,actionId)) {
    return true;
  }
 else   if (permissionChecker.hasPermission(0,WorkflowInstance.class.getName(),instanceId,actionId)) {
    return true;
  }
  return false;
}

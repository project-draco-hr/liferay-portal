{
  String extension=FileUtil.getExtension(name);
  if (Validator.isNotNull(extension) && !title.toLowerCase().endsWith(StringPool.PERIOD + extension)) {
    title+=StringPool.PERIOD + extension;
  }
  return title;
}

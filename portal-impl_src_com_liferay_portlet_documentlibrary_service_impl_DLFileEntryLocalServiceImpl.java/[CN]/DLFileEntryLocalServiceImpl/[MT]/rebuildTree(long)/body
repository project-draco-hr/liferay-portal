{
  List<DLFileEntry> fileEntries=dlFileEntryPersistence.findByCompanyId(companyId);
  for (  DLFileEntry fileEntry : fileEntries) {
    fileEntry.setTreePath(fileEntry.buildTreePath());
    dlFileEntryPersistence.update(fileEntry);
  }
}

{
  if (bytes == null) {
    throw new FileSizeException();
  }
  InputStream is=new UnsyncByteArrayInputStream(bytes);
  return addFileEntry(userId,groupId,folderId,name,title,description,changelog,extraSettings,is,bytes.length,serviceContext);
}

{
  if (hasFileEntryLock(userId,fileEntryId)) {
    return LockHelperUtil.getLock(DLFileEntry.class.getName(),fileEntryId);
  }
  return LockHelperUtil.lock(userId,DLFileEntry.class.getName(),fileEntryId,null,false,DLFileEntryImpl.LOCK_EXPIRATION_TIME);
}

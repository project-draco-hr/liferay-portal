{
  String extension=getExtension(title,serviceContext);
  String extraSettings=StringPool.BLANK;
  return updateFileEntry(userId,fileEntryId,sourceFileName,extension,mimeType,title,description,changeLog,majorVersion,extraSettings,is,size,serviceContext);
}

{
  if (_total == 0) {
    _cur=DEFAULT_CUR;
    return;
  }
  if (isRecalculateCur()) {
    if ((_total % _delta) == 0) {
      _cur=(_total / _delta);
    }
 else {
      _cur=(_total / _delta) + 1;
    }
  }
}

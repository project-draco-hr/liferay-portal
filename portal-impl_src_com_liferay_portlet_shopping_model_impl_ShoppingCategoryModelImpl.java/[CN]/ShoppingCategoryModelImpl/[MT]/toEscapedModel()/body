{
  if (isEscapedModel()) {
    return (ShoppingCategory)this;
  }
 else {
    return (ShoppingCategory)Proxy.newProxyInstance(ShoppingCategory.class.getClassLoader(),new Class[]{ShoppingCategory.class},new AutoEscapeBeanHandler(this));
  }
}

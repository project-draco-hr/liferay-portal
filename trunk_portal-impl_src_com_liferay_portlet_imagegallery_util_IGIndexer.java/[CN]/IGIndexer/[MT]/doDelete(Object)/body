{
  IGImage image=(IGImage)obj;
  Document document=new DocumentImpl();
  document.addUID(PORTLET_ID,image.getImageId());
  SearchEngineUtil.deleteDocument(image.getCompanyId(),document.get(Field.UID));
}

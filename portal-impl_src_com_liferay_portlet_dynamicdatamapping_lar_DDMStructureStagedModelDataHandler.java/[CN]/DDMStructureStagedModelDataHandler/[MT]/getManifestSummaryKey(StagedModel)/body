{
  if (stagedModel == null) {
    return DDMStructure.class.getName();
  }
  DDMStructure structure=(DDMStructure)stagedModel;
  return ManifestSummary.getManifestSummaryKey(DDMStructure.class.getName(),structure.getClassName());
}

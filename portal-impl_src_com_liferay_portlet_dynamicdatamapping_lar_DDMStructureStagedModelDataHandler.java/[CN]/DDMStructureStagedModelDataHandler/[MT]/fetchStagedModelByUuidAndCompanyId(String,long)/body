{
  List<DDMStructure> structures=DDMStructureLocalServiceUtil.getDDMStructuresByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<DDMStructure>());
  if (ListUtil.isEmpty(structures)) {
    return null;
  }
  return structures.get(0);
}

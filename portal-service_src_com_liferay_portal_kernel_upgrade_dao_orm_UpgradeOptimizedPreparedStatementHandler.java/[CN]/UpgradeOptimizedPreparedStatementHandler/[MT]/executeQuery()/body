{
  ResultSet resultSet=_statement.executeQuery();
  return (ResultSet)ProxyUtil.newProxyInstance(Thread.currentThread().getContextClassLoader(),new Class[]{ResultSet.class},new UpgradeOptimizedResultSetHandler(resultSet));
}

{
  List<Role> roles=layoutCache.getGroupRoles_1to4(groupId);
  Element groupElement=exportRoles(companyId,resourceName,ResourceConstants.SCOPE_GROUP,String.valueOf(groupId),parentElement,entityName + "-roles",roles);
  if (groupElement.elements().isEmpty()) {
    parentElement.remove(groupElement);
  }
}

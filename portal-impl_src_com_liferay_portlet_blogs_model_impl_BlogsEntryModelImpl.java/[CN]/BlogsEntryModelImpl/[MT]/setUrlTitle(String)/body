{
  if (((urlTitle == null) && (_urlTitle != null)) || ((urlTitle != null) && (_urlTitle == null)) || ((urlTitle != null) && (_urlTitle != null) && !urlTitle.equals(_urlTitle))) {
    if (!XSS_ALLOW_URLTITLE) {
      urlTitle=XSSUtil.strip(urlTitle);
    }
    _urlTitle=urlTitle;
  }
}

{
  if (isEscapedModel()) {
    return (BlogsEntry)this;
  }
 else {
    return (BlogsEntry)Proxy.newProxyInstance(BlogsEntry.class.getClassLoader(),new Class[]{BlogsEntry.class},new AutoEscapeBeanHandler(this));
  }
}

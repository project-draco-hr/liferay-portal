{
  String searchConfiguration=portletPreferences.getValue("searchConfiguration",StringPool.BLANK);
  for (  String[] name : _CLASS_NAMES) {
    searchConfiguration=StringUtil.replace(searchConfiguration,name[0],name[1]);
  }
  portletPreferences.setValue("searchConfiguration",searchConfiguration);
}

{
  try {
    doImportMissingReference(portletDataContext,referenceElement);
  }
 catch (  Exception e) {
    if (!StringUtil.equalsIgnoreCase(referenceElement.attributeValue("type"),portletDataContext.REFERENCE_TYPE_DEPENDENCY_DISPOSABLE)) {
      throw e;
    }
  }
}

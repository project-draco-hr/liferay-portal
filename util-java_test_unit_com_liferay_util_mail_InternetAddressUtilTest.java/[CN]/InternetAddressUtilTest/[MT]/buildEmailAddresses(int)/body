{
  InternetAddress[] internetAddresses=new InternetAddress[numOfAddress];
  for (int i=0; i < numOfAddress; i++) {
    internetAddresses[i]=new InternetAddress(String.valueOf(i) + _INTERNET_ADDRESS_SUFFIX);
  }
  return internetAddresses;
}

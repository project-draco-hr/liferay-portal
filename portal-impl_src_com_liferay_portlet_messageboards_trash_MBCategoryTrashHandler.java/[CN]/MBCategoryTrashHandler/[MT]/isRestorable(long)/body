{
  MBCategory category=MBCategoryLocalServiceUtil.getCategory(classPK);
  if ((category.getParentCategoryId() > 0) && (MBCategoryLocalServiceUtil.fetchMBCategory(category.getParentCategoryId()) == null)) {
    return false;
  }
  return !category.isInTrashContainer();
}

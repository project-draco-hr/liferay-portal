{
  Lock readLock=_channelsRWLock.readLock();
  try {
    readLock.lock();
    for (    Channel channel : _channels.values()) {
      channel.flush();
    }
  }
  finally {
    readLock.unlock();
  }
}

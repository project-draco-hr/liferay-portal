{
  Lock writeLock=_channelsRWLock.writeLock();
  try {
    writeLock.lock();
    Channel channel=_channels.remove(userId);
    if (channel != null) {
      channel.close();
    }
    return channel;
  }
  finally {
    writeLock.unlock();
  }
}

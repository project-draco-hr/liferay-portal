{
  Lock writeLock=_channelsRWLock.writeLock();
  try {
    writeLock.lock();
    for (    Channel channel : _channels.values()) {
      channel.close();
    }
    _channels.clear();
  }
  finally {
    writeLock.unlock();
  }
}

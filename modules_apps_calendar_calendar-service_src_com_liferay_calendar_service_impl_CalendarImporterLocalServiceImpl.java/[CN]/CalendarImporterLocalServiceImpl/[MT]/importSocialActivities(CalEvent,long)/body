{
  List<SocialActivity> socialActivities=socialActivityPersistence.findByC_C(classNameLocalService.getClassNameId(CalEvent.class),calEvent.getEventId());
  for (  SocialActivity socialActivity : socialActivities) {
    importSocialActivity(socialActivity,calendarBookingId);
  }
}

{
  ResourceAction oldResourceAction=resourceActionPersistence.fetchByN_A(oldResourceModelName,oldActionId);
  boolean hasActionId=resourcePermissionLocalService.hasActionId(resourcePermission,oldResourceAction);
  if (!hasActionId) {
    return 0;
  }
  return getActionId(newResourceModelName,newActionId);
}

{
  if (messagePayload instanceof Serializable) {
    handleNotification((Serializable)messagePayload);
  }
 else {
    if (_log.isWarnEnabled()) {
      _log.warn("Unable to process message content of type " + messagePayload.getClass().getName());
    }
  }
}

{
  AssetTag assetTag=fetchByPrimaryKey(tagId);
  if (assetTag == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + tagId);
    }
    throw new NoSuchTagException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + tagId);
  }
  return assetTag;
}

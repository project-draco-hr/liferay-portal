{
  RemotePluginPackageRepository repository=(RemotePluginPackageRepository)_repositoryCache.get(repositoryURL);
  if (repository != null) {
    return repository;
  }
  return _loadRepository(repositoryURL);
}

{
  DDLRecordSet ddlRecordSet=(DDLRecordSet)EntityCacheUtil.getResult(DDLRecordSetModelImpl.ENTITY_CACHE_ENABLED,DDLRecordSetImpl.class,recordSetId,this);
  if (ddlRecordSet == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (ddlRecordSet == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      ddlRecordSet=(DDLRecordSet)session.get(DDLRecordSetImpl.class,Long.valueOf(recordSetId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (ddlRecordSet == null)) {
        EntityCacheUtil.putResult(DDLRecordSetModelImpl.ENTITY_CACHE_ENABLED,DDLRecordSetImpl.class,recordSetId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(ddlRecordSet);
      }
      closeSession(session);
    }
  }
  return ddlRecordSet;
}

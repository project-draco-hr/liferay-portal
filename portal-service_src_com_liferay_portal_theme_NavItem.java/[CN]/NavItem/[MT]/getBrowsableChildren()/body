{
  if (_browsableChildren == null) {
    List<NavItem> children=getChildren();
    _browsableChildren=ListUtil.filter(children,new PredicateFilter<NavItem>(){
      @Override public boolean filter(      NavItem navItem){
        return navItem.isBrowsable();
      }
    }
);
  }
  return _browsableChildren;
}

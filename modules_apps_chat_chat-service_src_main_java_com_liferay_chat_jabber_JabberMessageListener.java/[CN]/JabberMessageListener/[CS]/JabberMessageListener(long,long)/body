{
  _companyId=companyId;
  _userId=userId;
  try {
    chatGroupServiceConfiguration=ConfigurationProviderUtil.getCompanyConfiguration(ChatGroupServiceConfiguration.class,_companyId);
  }
 catch (  ConfigurationException ce) {
    _log.error("Unable to load chat group service configuration",ce);
  }
}

{
  _companyId=companyId;
  _userId=userId;
  try {
    _chatGroupServiceConfiguration=ConfigurationProviderUtil.getCompanyConfiguration(ChatGroupServiceConfiguration.class,_companyId);
  }
 catch (  ConfigurationException e) {
    _log.error("Unable to load ChatGroupServiceConfiguration",e);
  }
}

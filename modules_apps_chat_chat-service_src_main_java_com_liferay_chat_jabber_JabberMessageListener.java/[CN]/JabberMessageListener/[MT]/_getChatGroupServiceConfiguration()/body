{
  try {
    return ConfigurationProviderUtil.getCompanyConfiguration(ChatGroupServiceConfiguration.class,_companyId);
  }
 catch (  ConfigurationException ce) {
    _log.error("Unable to load chat group service configuration",ce);
  }
  return null;
}

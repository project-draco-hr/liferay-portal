{
  InputStream inputStream=getResource("/META-INF/definitions/no-incoming-task.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No incoming transition found for task task",error);
}

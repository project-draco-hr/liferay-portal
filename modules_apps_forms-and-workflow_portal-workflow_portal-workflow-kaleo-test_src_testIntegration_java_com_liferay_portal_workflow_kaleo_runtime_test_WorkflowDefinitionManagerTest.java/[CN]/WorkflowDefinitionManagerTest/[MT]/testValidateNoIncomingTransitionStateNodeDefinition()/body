{
  InputStream inputStream=getResource("/META-INF/definitions/no-incoming-state.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No incoming transition found for state state",error);
}

{
  InputStream inputStream=getResource("/META-INF/definitions/unbalanced-fork-and-join.xml");
  String error=assertInvalid(inputStream);
  assertEquals("There are unbalanced fork and join nodes",error);
}

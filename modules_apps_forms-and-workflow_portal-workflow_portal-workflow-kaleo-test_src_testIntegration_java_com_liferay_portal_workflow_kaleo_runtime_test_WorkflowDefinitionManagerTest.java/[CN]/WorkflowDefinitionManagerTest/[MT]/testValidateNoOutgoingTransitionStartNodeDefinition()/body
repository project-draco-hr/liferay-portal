{
  InputStream inputStream=getResource("/META-INF/definitions/no-outgoing-start-node.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No outgoing transition found for initial state start",error);
}

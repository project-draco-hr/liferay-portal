{
  InputStream inputStream=getResource("/META-INF/definitions/no-incoming-condition.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No incoming transition found for condition condition",error);
}

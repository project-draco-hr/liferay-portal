{
  InputStream inputStream=getResource("/META-INF/definitions/invalid-transition.xml");
  String error=assertInvalid(inputStream);
  assertEquals("Unable to find target node for transition end",error);
}

{
  InputStream inputStream=getResource("/META-INF/definitions/less-than-two-outgoing-fork.xml");
  String error=assertInvalid(inputStream);
  assertEquals("Less than 2 outgoing transitions found for fork fork",error);
}

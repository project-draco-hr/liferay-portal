{
  InputStream inputStream=getResource("less-than-two-outgoing-condition.xml");
  String error=assertInvalid(inputStream);
  assertEquals("Less than 2 outgoing transitions found for condition condition",error);
}

{
  InputStream inputStream=getResource("/META-INF/definitions/no-incoming-fork.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No incoming transition found for fork fork",error);
}

{
  FormatXMLTask formatXMLTask=GradleUtil.addTask(project,FORMAT_XML_TASK_NAME,FormatXMLTask.class);
  formatXMLTask.setDescription("Runs Liferay XML Formatter to format files.");
  return formatXMLTask;
}

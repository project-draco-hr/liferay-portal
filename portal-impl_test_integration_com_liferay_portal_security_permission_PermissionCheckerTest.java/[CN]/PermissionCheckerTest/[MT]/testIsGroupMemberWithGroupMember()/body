{
  User user=UserTestUtil.addUser();
  UserLocalServiceUtil.addGroupUser(_group.getGroupId(),user.getUserId());
  Assert.assertTrue(_getPermissionChecker(user).isGroupMember(_group.getGroupId()));
}

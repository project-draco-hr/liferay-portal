{
  User user=UserTestUtil.addUser(_group.getGroupId(),LocaleUtil.getDefault());
  PermissionChecker permissionChecker=_getPermissionChecker(user);
  Assert.assertFalse(permissionChecker.isGroupOwner(_group.getGroupId()));
}

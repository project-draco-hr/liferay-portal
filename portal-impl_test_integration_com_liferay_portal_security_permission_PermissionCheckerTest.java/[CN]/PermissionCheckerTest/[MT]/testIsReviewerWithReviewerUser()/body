{
  User user=UserTestUtil.addUser();
  Role reviewerRole=addReviewerRole();
  UserLocalServiceUtil.setRoleUsers(reviewerRole.getRoleId(),new long[]{user.getUserId()});
  Assert.assertTrue(_getPermissionChecker(user).isReviewer(user.getCompanyId(),_group.getGroupId()));
}

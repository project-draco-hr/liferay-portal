{
  TagsEntry entry=TagsEntryLocalServiceUtil.getEntry(entryId);
  TagsEntryPermission.check(getPermissionChecker(),name,entry.getGroupId(),ActionKeys.UPDATE);
  return tagsEntryLocalService.updateEntry(entryId,parentEntryName,name,vocabularyName);
}

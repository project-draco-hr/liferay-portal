{
  TagsEntryPermission.check(getPermissionChecker(),entryId,ActionKeys.UPDATE);
  return tagsEntryLocalService.updateEntry(getUserId(),entryId,parentEntryName,name,vocabularyName,properties,serviceContext);
}

{
  TagsEntry entry=TagsEntryLocalServiceUtil.getEntry(entryId);
  TagsEntryPermission.check(getPermissionChecker(),entry.getEntryId(),ActionKeys.UPDATE);
  return tagsEntryLocalService.updateEntry(getUserId(),entryId,parentEntryName,name,vocabularyName,properties);
}

{
  TagsEntryPermission.check(getPermissionChecker(),fromEntryId,ActionKeys.VIEW);
  TagsEntryPermission.check(getPermissionChecker(),toEntryId,ActionKeys.UPDATE);
  tagsEntryLocalService.mergeEntries(fromEntryId,toEntryId);
}

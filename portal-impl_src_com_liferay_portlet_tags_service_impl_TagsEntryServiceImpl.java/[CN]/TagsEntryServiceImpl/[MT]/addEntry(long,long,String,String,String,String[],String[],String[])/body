{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.TAGS_ADMIN,ActionKeys.ADD_ENTRY);
  return tagsEntryLocalService.addEntry(getUserId(),groupId,parentEntryName,name,vocabularyName,properties,communityPermissions,guestPermissions);
}

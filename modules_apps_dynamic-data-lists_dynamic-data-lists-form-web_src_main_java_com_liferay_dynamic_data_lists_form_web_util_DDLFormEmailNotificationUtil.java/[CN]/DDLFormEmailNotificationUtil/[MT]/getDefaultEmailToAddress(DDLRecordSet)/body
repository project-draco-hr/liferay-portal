{
  String emailToAddress=StringPool.BLANK;
  User userFormCreator=UserLocalServiceUtil.fetchUser(recordSet.getUserId());
  if (userFormCreator != null) {
    emailToAddress=userFormCreator.getEmailAddress();
  }
  return emailToAddress;
}

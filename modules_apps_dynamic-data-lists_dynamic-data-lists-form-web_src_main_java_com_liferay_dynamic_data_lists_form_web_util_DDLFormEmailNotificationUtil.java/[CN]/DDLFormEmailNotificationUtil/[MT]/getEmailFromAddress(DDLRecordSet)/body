{
  DDLRecordSetSettings recordSettings=recordSet.getSettingsObj();
  String defaultEmailFromAddress=PrefsPropsUtil.getString(recordSet.getCompanyId(),PropsKeys.ADMIN_EMAIL_FROM_ADDRESS);
  return GetterUtil.getString(recordSettings.emailFromAddress(),defaultEmailFromAddress);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetCategory assetCategory) method. Use update(AssetCategory assetCategory, boolean merge) instead.");
  }
  return update(assetCategory,false);
}

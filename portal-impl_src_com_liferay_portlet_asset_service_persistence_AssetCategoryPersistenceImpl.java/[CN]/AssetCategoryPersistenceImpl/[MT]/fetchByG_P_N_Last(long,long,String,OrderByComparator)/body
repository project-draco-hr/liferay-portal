{
  int count=countByG_P_N(groupId,parentCategoryId,name);
  List<AssetCategory> list=findByG_P_N(groupId,parentCategoryId,name,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

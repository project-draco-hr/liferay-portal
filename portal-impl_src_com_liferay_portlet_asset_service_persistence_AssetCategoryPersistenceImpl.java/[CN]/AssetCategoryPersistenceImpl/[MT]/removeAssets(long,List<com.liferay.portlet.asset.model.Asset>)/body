{
  try {
    for (    com.liferay.portlet.asset.model.Asset asset : assets) {
      removeAsset.remove(pk,asset.getPrimaryKey());
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    FinderCacheUtil.clearCache("Assets_AssetCategories");
  }
}

{
  String sql="UPDATE AssetCategory SET leftcategoryId = (leftcategoryId + ?) WHERE (groupId = ?) AND (leftcategoryId > ?) AND (categoryId NOT IN (" + StringUtil.merge(childrenCategoryIds) + "))";
  SqlUpdate _sqlUpdate=SqlUpdateFactoryUtil.getSqlUpdate(getDataSource(),sql,new int[]{java.sql.Types.BIGINT,java.sql.Types.BIGINT,java.sql.Types.BIGINT});
  _sqlUpdate.update(new Object[]{delta,groupId,leftCategoryId});
}

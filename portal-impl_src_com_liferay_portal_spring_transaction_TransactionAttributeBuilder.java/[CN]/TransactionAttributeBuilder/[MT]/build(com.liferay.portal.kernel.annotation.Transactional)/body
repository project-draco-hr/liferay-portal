{
  if (transactional == null) {
    return null;
  }
  return _build(transactional.enabled(),transactional.isolation().value(),transactional.propagation().value(),transactional.readOnly(),transactional.timeout(),transactional.rollbackFor(),transactional.rollbackForClassName(),transactional.noRollbackFor(),transactional.noRollbackForClassName());
}

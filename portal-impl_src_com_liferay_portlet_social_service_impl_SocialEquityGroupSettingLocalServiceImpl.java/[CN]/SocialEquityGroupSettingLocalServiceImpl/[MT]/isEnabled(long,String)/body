{
  if (isEnabled(groupId,className,SocialEquitySettingConstants.TYPE_INFORMATION) && isEnabled(groupId,className,SocialEquitySettingConstants.TYPE_PARTICIPATION)) {
    return true;
  }
 else {
    return false;
  }
}

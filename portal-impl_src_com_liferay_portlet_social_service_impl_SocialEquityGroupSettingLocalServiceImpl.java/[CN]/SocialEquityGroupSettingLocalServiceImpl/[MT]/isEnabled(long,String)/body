{
  boolean isInformationEquity=isEnabled(groupId,className,SocialEquitySettingConstants.TYPE_INFORMATION);
  boolean isParticipationEquity=isEnabled(groupId,className,SocialEquitySettingConstants.TYPE_PARTICIPATION);
  return (isInformationEquity && isParticipationEquity);
}

{
  return addStructure(TestPropsValues.getGroupId(),className,0,getSampleStructureDefinition("name",new Locale[]{LocaleUtil.US},defaultLocale),defaultLocale,ServiceContextTestUtil.getServiceContext());
}

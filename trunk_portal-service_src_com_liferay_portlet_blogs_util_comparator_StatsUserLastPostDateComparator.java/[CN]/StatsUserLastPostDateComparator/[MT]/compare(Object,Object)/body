{
  BlogsStatsUser statsUser1=(BlogsStatsUser)obj1;
  BlogsStatsUser statsUser2=(BlogsStatsUser)obj2;
  int value=DateUtil.compareTo(statsUser1.getLastPostDate(),statsUser2.getLastPostDate());
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

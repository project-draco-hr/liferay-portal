{
  IndexName indexName=new IndexName(_indexNameBuilder.getIndexName(_companyId));
  IndexCreationHelper indexCreationHelper=Mockito.mock(IndexCreationHelper.class);
  _elasticsearchFixture.createIndex(indexName,indexCreationHelper);
}

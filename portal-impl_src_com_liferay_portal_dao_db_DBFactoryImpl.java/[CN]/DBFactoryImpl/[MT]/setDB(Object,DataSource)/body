{
  _db=getDB(dialect,dataSource);
  if (_log.isDebugEnabled()) {
    Class<?> dbClazz=_db.getClass();
    Class<?> dialectClazz=dialect.getClass();
    _log.debug("Using DB implementation " + dbClazz.getName() + " for "+ dialectClazz.getName());
  }
}

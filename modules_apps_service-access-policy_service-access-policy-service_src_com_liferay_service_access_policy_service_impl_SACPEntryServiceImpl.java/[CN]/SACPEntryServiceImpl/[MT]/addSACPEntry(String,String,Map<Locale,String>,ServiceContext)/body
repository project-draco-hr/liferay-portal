{
  PortletPermissionUtil.check(getPermissionChecker(),SAPConstants.SERVICE_NAME,SAPActionKeys.ACTION_ADD_SAP_ENTRY);
  return sacpEntryLocalService.addSACPEntry(getUserId(),allowedServiceSignatures,false,name,titleMap,serviceContext);
}

{
  Stack<String> stack=_getCompanyServiceStack();
  if (stack.isEmpty()) {
    return PropsValues.SHARD_DEFAULT_NAME;
  }
  return GetterUtil.getString(stack.peek(),PropsValues.SHARD_DEFAULT_NAME);
}

{
  Shard shard=fetchByPrimaryKey(shardId);
  if (shard == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No Shard exists with the primary key " + shardId);
    }
    throw new NoSuchShardException("No Shard exists with the primary key " + shardId);
  }
  return shard;
}

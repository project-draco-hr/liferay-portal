{
  Session session=null;
  try {
    session=openSession();
    return (SRProductEntry)session.get(SRProductEntryImpl.class,new Long(productEntryId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

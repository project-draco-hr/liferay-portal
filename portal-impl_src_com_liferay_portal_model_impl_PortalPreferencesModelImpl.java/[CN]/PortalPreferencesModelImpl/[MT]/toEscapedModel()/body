{
  if (isEscapedModel()) {
    return (PortalPreferences)this;
  }
 else {
    return (PortalPreferences)Proxy.newProxyInstance(PortalPreferences.class.getClassLoader(),new Class[]{PortalPreferences.class},new AutoEscapeBeanHandler(this));
  }
}

{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(imageId));
  }
  com.liferay.portlet.imagegallery.model.IGImage igImage=getPersistence().remove(imageId);
  if (listener != null) {
    listener.onAfterRemove(igImage);
  }
  return igImage;
}

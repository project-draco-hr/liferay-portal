{
  DiscussionPermission discussionPermission=commentManager.getDiscussionPermission(getPermissionChecker());
  long groupId=getGroupId(className,classPK);
  long companyId=getCompanyId(groupId);
  discussionPermission.checkViewPermission(companyId,groupId,className,classPK);
  return commentManager.getCommentsCount(className,classPK);
}

{
  try {
    Registry registry=RegistryUtil.getRegistry();
    return registry.getServices(DLFileVersionDisplayContextFactory.class,null);
  }
 catch (  Exception e) {
    throw new SystemException(e);
  }
}

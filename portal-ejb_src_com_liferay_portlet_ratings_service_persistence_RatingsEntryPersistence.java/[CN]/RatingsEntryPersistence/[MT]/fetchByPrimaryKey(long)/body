{
  Session session=null;
  try {
    session=openSession();
    return (RatingsEntry)session.get(RatingsEntry.class,new Long(entryId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

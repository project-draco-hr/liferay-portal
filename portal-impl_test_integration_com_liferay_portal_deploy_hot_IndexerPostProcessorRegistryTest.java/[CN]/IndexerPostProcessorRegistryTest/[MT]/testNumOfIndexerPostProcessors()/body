{
  Field serviceTrackerField=ReflectionUtil.getDeclaredField(IndexerPostProcessorRegistry.class,"_serviceTracker");
  ServiceTracker<?,?> serviceTracker=(ServiceTracker<?,?>)serviceTrackerField.get(_registry);
  assertEquals(4,serviceTracker.getServices().length);
}

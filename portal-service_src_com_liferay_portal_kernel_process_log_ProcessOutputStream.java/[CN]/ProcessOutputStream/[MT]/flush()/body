{
  if (index > 0) {
    byte[] logData=toByteArray();
    LoggingProcessCallable loggingProcessCallable=new LoggingProcessCallable(logData,_errorLog);
    writeProcessCallable(loggingProcessCallable);
    reset();
  }
}

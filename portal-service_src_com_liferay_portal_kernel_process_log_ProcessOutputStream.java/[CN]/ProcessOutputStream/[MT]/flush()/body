{
  if (index > 0) {
    byte[] bytes=toByteArray();
    LoggingProcessCallable loggingProcessCallable=new LoggingProcessCallable(bytes,_error);
    writeProcessCallable(loggingProcessCallable);
    reset();
  }
}

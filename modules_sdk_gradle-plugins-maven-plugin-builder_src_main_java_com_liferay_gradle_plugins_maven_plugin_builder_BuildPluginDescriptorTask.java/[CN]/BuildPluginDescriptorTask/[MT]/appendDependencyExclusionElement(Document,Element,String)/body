{
  String[] tokens=parseDependencyNotation(dependencyNotation);
  String groupId=tokens[0];
  String artifactId=tokens[1];
  appendDependencyExclusionElement(doc,exclusionsElement,groupId,artifactId);
}

{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(RepositoryEntry.class,_dynamicQueryClassLoader);
  dynamicQuery.add(RestrictionsFactoryUtil.eq("repositoryEntryId",RandomTestUtil.nextLong()));
  List<RepositoryEntry> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

{
  String className="TEST_CLASS_NAME";
  ExpandoBridge expandoBridge1=DoPrivilegedUtil.wrap(new ExpandoBridgeImpl(TestPropsValues.getCompanyId(),className));
  ExpandoBridge expandoBridge2=DoPrivilegedUtil.wrap(new ExpandoBridgeImpl(TestPropsValues.getCompanyId(),className));
  Assert.assertTrue(expandoBridge1.equals(expandoBridge2));
}

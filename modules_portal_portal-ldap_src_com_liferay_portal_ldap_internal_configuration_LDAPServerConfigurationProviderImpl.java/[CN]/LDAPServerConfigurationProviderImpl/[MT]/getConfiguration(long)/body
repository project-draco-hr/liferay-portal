{
  List<LDAPServerConfiguration> ldapServerConfigurations=getConfigurations(companyId);
  if (ldapServerConfigurations.isEmpty()) {
    throw new IllegalArgumentException("No LDAPServerConfiguration found for companyId " + companyId);
  }
  return ldapServerConfigurations.get(0);
}

{
  try {
    DLService dlService=DLServiceFactory.getService();
    dlService.addFile(companyId,portletId,groupId,repositoryId,fileName,byteArray);
  }
 catch (  PortalException pe) {
    throw pe;
  }
catch (  SystemException se) {
    throw se;
  }
catch (  Exception e) {
    throw new SystemException(e);
  }
}

{
  try {
    DLService dlService=DLServiceFactory.getService();
    return dlService.search(companyId,portletId,groupId,repositoryIds,keywords);
  }
 catch (  SystemException se) {
    throw se;
  }
catch (  Exception e) {
    throw new SystemException(e);
  }
}

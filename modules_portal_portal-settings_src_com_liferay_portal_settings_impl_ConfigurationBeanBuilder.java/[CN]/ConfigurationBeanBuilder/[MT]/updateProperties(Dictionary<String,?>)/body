{
  if (_log.isInfoEnabled()) {
    _log.info("Configuration with configurationPid " + SettingsDefinitionUtil.getConfigurationPid(_settingsDefinition) + " changed");
  }
  _configurable=Configurable.createConfigurable(_settingsDefinition.getConfigurationBeanClass(),properties);
}

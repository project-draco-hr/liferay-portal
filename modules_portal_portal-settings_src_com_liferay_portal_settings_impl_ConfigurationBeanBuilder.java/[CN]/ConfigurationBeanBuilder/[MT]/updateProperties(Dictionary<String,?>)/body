{
  if (_log.isInfoEnabled()) {
    String pid=SettingsDefinitionUtil.getConfigurationPid(_settingsDefinition);
    _log.info("Configuration with configurationPid " + pid + " changed");
  }
  _configurable=Configurable.createConfigurable(_settingsDefinition.getConfigurationBeanClass(),properties);
}

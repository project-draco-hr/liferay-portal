{
  TaskContainer taskContainer=project.getTasks();
  Task task=taskContainer.create("buildService",ServiceBuilderTask.class);
  task.setDescription("Runs Liferay Service Builder.");
  task.setGroup("build");
  return task;
}

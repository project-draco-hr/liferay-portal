{
  TaskContainer taskContainer=project.getTasks();
  Task task=taskContainer.create(BUILD_SERVICE_TASK_NAME,BuildServiceTask.class);
  task.setDescription("Runs Liferay Service Builder.");
  task.setGroup("build");
  return task;
}

{
  try {
    List<DLFileRank> fileRanks=DLFileRankLocalServiceUtil.getFileRanks(group.getGroupId(),TestPropsValues.getUserId());
    if (fileRanks.size() == 1) {
      return fileRanks.get(0);
    }
    return null;
  }
 catch (  Exception e) {
    return null;
  }
}

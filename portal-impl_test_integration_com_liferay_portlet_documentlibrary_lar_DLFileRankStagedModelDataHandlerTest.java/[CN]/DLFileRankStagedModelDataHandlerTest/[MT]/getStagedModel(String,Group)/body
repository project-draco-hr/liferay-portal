{
  try {
    List<DLFileRank> dlFileRanks=DLFileRankLocalServiceUtil.getFileRanks(group.getGroupId(),TestPropsValues.getUserId());
    if (dlFileRanks.size() == 1) {
      return dlFileRanks.get(0);
    }
    return null;
  }
 catch (  Exception e) {
    return null;
  }
}

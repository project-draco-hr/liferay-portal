{
  List<StagedModel> dependendtStagedModels=dependentStagedModelsMap.get(FileEntry.class.getSimpleName());
  FileEntry fileEntry=(FileEntry)dependendtStagedModels.get(0);
  return DLAppTestUtil.addDLFileRank(group.getGroupId(),fileEntry.getFileEntryId());
}

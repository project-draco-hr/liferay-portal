{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ReportsEntry reportsEntry) method. Use update(ReportsEntry reportsEntry, boolean merge) instead.");
  }
  return update(reportsEntry,false);
}

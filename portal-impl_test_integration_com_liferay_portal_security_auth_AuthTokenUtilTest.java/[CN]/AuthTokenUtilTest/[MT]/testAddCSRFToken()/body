{
  MockHttpServletRequest request=new MockHttpServletRequest();
  LiferayPortletURL liferayPortletURL=new PortletURLImpl(request,PortletKeys.PORTAL,0,PortletRequest.ACTION_PHASE);
  AuthTokenUtil.addCSRFToken(request,liferayPortletURL);
  Assert.assertEquals("TEST_TOKEN",liferayPortletURL.getParameter("p_auth"));
}

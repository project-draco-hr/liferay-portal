{
  List<JSONWSComment> comments=new ArrayList<>();
  while (threadDiscussionCommentIterator.hasNext()) {
    comments.add(new JSONWSComment(threadDiscussionCommentIterator.next()));
  }
  return comments;
}

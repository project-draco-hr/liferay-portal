{
  Object[] finderArgs=new Object[]{new Long(passwordPolicyId)};
  Long count=(Long)FinderCacheUtil.getResult(FINDER_PATH_COUNT_BY_PASSWORDPOLICYID,finderArgs,this);
  if (count == null) {
    Session session=null;
    try {
      session=openSession();
      StringBuilder query=new StringBuilder();
      query.append("SELECT COUNT(passwordPolicyRel) ");
      query.append("FROM PasswordPolicyRel passwordPolicyRel WHERE ");
      query.append("passwordPolicyRel.passwordPolicyId = ?");
      query.append(" ");
      Query q=session.createQuery(query.toString());
      QueryPos qPos=QueryPos.getInstance(q);
      qPos.add(passwordPolicyId);
      count=(Long)q.uniqueResult();
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (count == null) {
        count=Long.valueOf(0);
      }
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_PASSWORDPOLICYID,finderArgs,count);
      closeSession(session);
    }
  }
  return count.intValue();
}

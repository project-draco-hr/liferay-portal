{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PasswordPolicyRel passwordPolicyRel) method. To improve performance, call update(PasswordPolicyRel passwordPolicyRel, boolean merge).");
  }
  return update(passwordPolicyRel,false);
}

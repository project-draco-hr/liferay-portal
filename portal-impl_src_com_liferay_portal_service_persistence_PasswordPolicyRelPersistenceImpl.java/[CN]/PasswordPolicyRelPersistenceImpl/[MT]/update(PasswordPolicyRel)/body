{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PasswordPolicyRel passwordPolicyRel) method. Use update(PasswordPolicyRel passwordPolicyRel, boolean merge) instead.");
  }
  return update(passwordPolicyRel,false);
}

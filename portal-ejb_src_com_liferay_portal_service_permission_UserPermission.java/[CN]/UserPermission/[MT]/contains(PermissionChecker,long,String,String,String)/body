{
  PermissionCheckerImpl permissionCheckerImpl=(PermissionCheckerImpl)permissionChecker;
  String organizationActionId=actionId + "_USER";
  if (actionId.equals(ActionKeys.ADD_USER)) {
    organizationActionId=actionId;
  }
  if (permissionCheckerImpl.getUserId() == userId) {
    return true;
  }
 else   if (permissionChecker.hasPermission(0,User.class.getName(),userId,actionId)) {
    return true;
  }
 else   if (Validator.isNotNull(organizationId) && permissionChecker.hasPermission(0,Organization.class.getName(),organizationId,organizationActionId)) {
    return true;
  }
 else   if (Validator.isNotNull(locationId) && permissionChecker.hasPermission(0,"com.liferay.portal.model.Location",locationId,organizationActionId)) {
    return true;
  }
  return false;
}

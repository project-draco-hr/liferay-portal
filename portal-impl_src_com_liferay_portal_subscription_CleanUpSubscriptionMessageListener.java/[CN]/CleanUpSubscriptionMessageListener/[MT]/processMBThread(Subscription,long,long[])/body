{
  MBThread thread=MBThreadLocalServiceUtil.fetchThread(subscription.getClassPK());
  if ((thread != null) && ((thread.getGroupId() == groupId) || !ArrayUtil.contains(groupIds,thread.getGroupId()))) {
    SubscriptionLocalServiceUtil.deleteSubscription(subscription.getSubscriptionId());
  }
}

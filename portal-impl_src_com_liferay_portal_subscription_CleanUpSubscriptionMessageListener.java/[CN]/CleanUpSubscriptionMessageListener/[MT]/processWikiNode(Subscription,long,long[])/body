{
  WikiNode node=WikiNodeLocalServiceUtil.fetchWikiNode(subscription.getClassPK());
  if ((node != null) && ((node.getGroupId() == groupId) || !ArrayUtil.contains(groupIds,node.getGroupId()))) {
    SubscriptionLocalServiceUtil.deleteSubscription(subscription.getSubscriptionId());
  }
}

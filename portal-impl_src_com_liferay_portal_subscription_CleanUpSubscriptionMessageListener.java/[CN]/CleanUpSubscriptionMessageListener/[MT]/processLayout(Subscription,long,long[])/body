{
  Layout layout=LayoutLocalServiceUtil.fetchLayout(subscription.getClassPK());
  if ((layout != null) && ((layout.getGroupId() == groupId) || !ArrayUtil.contains(groupIds,layout.getGroupId()))) {
    SubscriptionLocalServiceUtil.deleteSubscription(subscription.getSubscriptionId());
  }
}

{
  if (isEscapedModel()) {
    return (LayoutSet)this;
  }
 else {
    return (LayoutSet)Proxy.newProxyInstance(LayoutSet.class.getClassLoader(),new Class[]{LayoutSet.class},new AutoEscapeBeanHandler(this));
  }
}

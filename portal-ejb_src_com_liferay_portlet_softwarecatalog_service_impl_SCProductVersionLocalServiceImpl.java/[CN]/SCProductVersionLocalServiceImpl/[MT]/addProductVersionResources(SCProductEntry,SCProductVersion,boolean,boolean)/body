{
  ResourceLocalServiceUtil.addResources(productVersion.getCompanyId(),productEntry.getGroupId(),productVersion.getUserId(),SCProductVersion.class.getName(),productVersion.getPrimaryKey(),false,addCommunityPermissions,addGuestPermissions);
}

{
  ResourceLocalServiceUtil.addModelResources(productVersion.getCompanyId(),productEntry.getGroupId(),productVersion.getUserId(),SCProductVersion.class.getName(),productVersion.getPrimaryKey(),communityPermissions,guestPermissions);
}

{
  Iterator itr=SCProductVersionUtil.findByProductEntryId(productEntryId).iterator();
  while (itr.hasNext()) {
    SCProductVersion productVersion=(SCProductVersion)itr.next();
    deleteProductVersion(productVersion);
  }
}

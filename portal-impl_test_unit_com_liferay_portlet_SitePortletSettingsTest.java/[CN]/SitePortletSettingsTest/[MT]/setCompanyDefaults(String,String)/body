{
  PortletPreferences companyDefaultSettings=new PortletPreferencesImpl();
  companyDefaultSettings.setValue(name,value);
  SitePortletSettings sitePortletSettings=(SitePortletSettings)portletSettings;
  sitePortletSettings.setCompanyDefaults(companyDefaultSettings);
}

{
  Profile profile=_bundleContext.getService(serviceReference);
  for (  String portalProfileName : profile.getSupportedPortalProfileNames()) {
    if (_portalProfileNames.contains(portalProfileName)) {
      profile.activate();
      break;
    }
  }
  return null;
}

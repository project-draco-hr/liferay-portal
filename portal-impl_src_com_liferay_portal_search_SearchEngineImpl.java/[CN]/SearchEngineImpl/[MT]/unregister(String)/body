{
  SearchRequest searchRequest=new SearchRequest();
  searchRequest.setCommand(SearchRequest.COMMAND_UNREGISTER);
  searchRequest.setId(name);
  MessageBusUtil.sendMessage(DestinationNames.SEARCH_WRITER,searchRequest);
}

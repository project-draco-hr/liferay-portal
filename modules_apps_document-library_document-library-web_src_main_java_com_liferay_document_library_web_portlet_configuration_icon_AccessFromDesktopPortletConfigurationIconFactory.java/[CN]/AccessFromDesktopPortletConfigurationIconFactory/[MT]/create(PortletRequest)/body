{
  try {
    Folder folder=ActionUtil.getFolder(portletRequest);
    return new AccessFromDesktopPortletConfigurationIcon(getServletContext(),getJspPath(),portletRequest,folder);
  }
 catch (  Exception e) {
  }
  return null;
}

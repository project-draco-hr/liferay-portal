{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(MeetupsEntry.class,_dynamicQueryClassLoader);
  dynamicQuery.add(RestrictionsFactoryUtil.eq("meetupsEntryId",RandomTestUtil.nextLong()));
  List<MeetupsEntry> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

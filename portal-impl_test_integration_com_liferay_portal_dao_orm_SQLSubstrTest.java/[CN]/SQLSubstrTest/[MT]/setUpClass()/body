{
  _db=DBManagerUtil.getDB();
  _db.runSQL("create table TestSubStr (id LONG not null primary key, data " + "VARCHAR(10) null)");
  _db.runSQL("insert into TestSubStr values (1, 'EXAMPLE'" + ")");
}

{
  if (isEscapedModel()) {
    return (SCProductScreenshot)this;
  }
 else {
    return (SCProductScreenshot)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

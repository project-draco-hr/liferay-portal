{
  if (!isFactory()) {
    return false;
  }
  if (Validator.equals(getScope(),Scope.COMPANY.toString()) && Validator.equals(getLabelAttribute(),PROPERTY_KEY_COMPANY_ID)) {
    return true;
  }
  return false;
}

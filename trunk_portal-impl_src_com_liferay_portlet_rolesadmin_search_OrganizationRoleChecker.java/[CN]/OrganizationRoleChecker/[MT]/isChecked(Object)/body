{
  Organization organization=(Organization)obj;
  try {
    return GroupLocalServiceUtil.hasRoleGroup(_role.getRoleId(),organization.getGroup().getGroupId());
  }
 catch (  Exception e) {
    _log.error(e,e);
    return false;
  }
}

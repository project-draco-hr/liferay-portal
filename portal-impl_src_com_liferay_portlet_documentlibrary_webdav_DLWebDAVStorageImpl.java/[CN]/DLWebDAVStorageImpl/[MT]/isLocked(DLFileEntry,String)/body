{
  long parentFolderId=fileEntry.getFolderId();
  String fileName=fileEntry.getName();
  if (Validator.isNull(lockUuid)) {
    return DLFileEntryServiceUtil.hasFileEntryLock(parentFolderId,fileName);
  }
 else {
    try {
      boolean verified=DLFileEntryServiceUtil.verifyFileEntryLock(parentFolderId,fileName,lockUuid);
      return !verified;
    }
 catch (    NoSuchLockException nsle) {
      return false;
    }
  }
}

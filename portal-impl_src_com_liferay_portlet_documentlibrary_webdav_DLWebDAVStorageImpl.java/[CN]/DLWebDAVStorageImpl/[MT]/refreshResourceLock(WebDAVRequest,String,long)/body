{
  Resource resource=getResource(webDavRequest);
  Lock lock=null;
  if (resource instanceof DLFileEntryResourceImpl) {
    try {
      lock=DLFileEntryServiceUtil.refreshFileEntryLock(uuid,timeout);
    }
 catch (    Exception e) {
      throw new WebDAVException(e);
    }
  }
  return lock;
}

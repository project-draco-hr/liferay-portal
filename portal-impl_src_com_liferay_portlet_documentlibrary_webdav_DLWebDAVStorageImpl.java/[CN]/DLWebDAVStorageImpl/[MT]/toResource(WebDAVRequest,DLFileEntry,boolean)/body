{
  String parentPath=getRootPath() + webDavReq.getPath();
  String name=StringPool.BLANK;
  if (appendPath) {
    name=fileEntry.getTitleWithExtension();
  }
  return new DLFileEntryResourceImpl(webDavReq,fileEntry,parentPath,name);
}

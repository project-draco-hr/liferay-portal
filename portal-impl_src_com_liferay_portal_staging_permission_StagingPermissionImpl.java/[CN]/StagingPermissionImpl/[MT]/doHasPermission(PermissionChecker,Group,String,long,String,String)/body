{
  if (!actionId.equals(ActionKeys.VIEW) && !actionId.equals(ActionKeys.DELETE) && group.hasLocalOrRemoteStagingGroup()&& (Validator.isNull(portletId) || group.isStagedPortlet(portletId))) {
    return false;
  }
 else {
    return null;
  }
}

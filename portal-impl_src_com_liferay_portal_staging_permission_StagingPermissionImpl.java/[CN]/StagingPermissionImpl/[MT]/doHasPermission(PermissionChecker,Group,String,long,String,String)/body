{
  if (!actionId.equals(ActionKeys.VIEW) && !actionId.equals(ActionKeys.DELETE) && group.hasRemoteOrLocalStagingGroup()&& group.isStagedPortlet(portletId)) {
    return false;
  }
 else {
    return null;
  }
}

{
  Integer type=PasswordPolicyModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ PasswordPolicyModelImpl.TABLE_NAME);
  }
  return type;
}

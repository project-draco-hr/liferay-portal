{
  if (isEscapedModel()) {
    return (TasksReview)this;
  }
 else {
    return (TasksReview)Proxy.newProxyInstance(TasksReview.class.getClassLoader(),new Class[]{TasksReview.class},new AutoEscapeBeanHandler(this));
  }
}

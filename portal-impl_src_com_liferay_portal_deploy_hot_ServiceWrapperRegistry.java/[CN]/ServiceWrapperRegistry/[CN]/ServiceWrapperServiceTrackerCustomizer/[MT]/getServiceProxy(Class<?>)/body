{
  Object service=null;
  try {
    service=PortalBeanLocatorUtil.locate(serviceTypeClass.getName());
  }
 catch (  BeanLocatorException ble) {
    Registry registry=RegistryUtil.getRegistry();
    service=registry.getService(serviceTypeClass);
  }
  return service;
}

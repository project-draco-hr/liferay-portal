{
  Matcher matcher=_oldDocumentLibraryURLPattern.matcher(url);
  if (!matcher.find()) {
    return null;
  }
  long groupId=GetterUtil.getLong(matcher.group(2));
  return DLAppLocalServiceUtil.getFileEntryByUuidAndGroupId(matcher.group(1),groupId);
}

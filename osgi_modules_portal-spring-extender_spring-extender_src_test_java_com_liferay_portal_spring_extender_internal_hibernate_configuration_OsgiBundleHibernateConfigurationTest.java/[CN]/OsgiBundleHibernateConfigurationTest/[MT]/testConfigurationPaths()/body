{
  String[] configurationResources=_osgiBundleHibernateConfiguration.getConfigurationResources();
  Assert.assertEquals(1,configurationResources.length);
  Assert.assertEquals("META-INF/module-hbm.xml",configurationResources[0]);
}

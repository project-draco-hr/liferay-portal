{
  super(String.format("Password must not make a user model listener fail: " + modelListenerException.getMessage()),PASSWORD_INVALID);
  this.userId=userId;
  this.modelListenerException=modelListenerException;
}

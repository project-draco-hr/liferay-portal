{
  super(String.format("Password for user %s must not contain any dictionary " + "words such as: %s",userId,_getDictionaryWordsString(dictionaryWords)),PASSWORD_CONTAINS_TRIVIAL_WORDS);
  _userId=userId;
  _dictionaryWords=dictionaryWords;
}

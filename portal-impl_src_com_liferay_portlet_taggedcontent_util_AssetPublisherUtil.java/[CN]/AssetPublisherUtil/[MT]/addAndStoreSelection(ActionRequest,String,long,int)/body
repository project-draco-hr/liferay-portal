{
  String referringPortletResource=ParamUtil.getString(req,"referringPortletResource");
  if (Validator.isNull(referringPortletResource)) {
    return;
  }
  TagsAsset asset=TagsAssetLocalServiceUtil.getAsset(className,classPK);
  PortletPreferences prefs=PortletPreferencesFactoryUtil.getPortletSetup(req,referringPortletResource);
  addSelection(className,asset.getAssetId(),assetOrder,prefs);
  prefs.store();
}

{
switch (level) {
case ERROR:
    if (_log.isErrorEnabled()) {
      _log.error(error.format(level,_simpleMessageFormatter));
    }
  break;
case WARNING:
if (_log.isWarnEnabled()) {
  _log.warn(error.format(level,_simpleMessageFormatter));
}
break;
default :
break;
}
}

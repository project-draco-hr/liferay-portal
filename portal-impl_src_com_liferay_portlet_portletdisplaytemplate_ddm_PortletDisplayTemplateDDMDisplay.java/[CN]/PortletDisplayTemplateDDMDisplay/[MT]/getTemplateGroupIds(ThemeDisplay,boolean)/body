{
  long[] groupIds=new long[]{PortletDisplayTemplateUtil.getDDMTemplateGroupId(themeDisplay.getScopeGroupId())};
  if (showGlobalScope) {
    groupIds=PortalUtil.getSiteAndCompanyGroupIds(themeDisplay);
  }
  return groupIds;
}

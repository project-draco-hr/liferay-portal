{
  try {
    Method activateMethod=clazz.getDeclaredMethod(methodName,parameterTypes);
    activateMethod.setAccessible(true);
    activateMethod.invoke(instance,parameters);
  }
 catch (  Exception e) {
    throw new IllegalStateException(e);
  }
}

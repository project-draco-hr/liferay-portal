{
  if ((!mbMessage.isApproved() && !mbMessage.isInTrash()) || (mbMessage.isDiscussion() && mbMessage.isRoot())) {
    return;
  }
  Document document=getDocument(mbMessage);
  SearchEngineHelperUtil.updateDocument(getSearchEngineId(),mbMessage.getCompanyId(),document,isCommitImmediately());
}

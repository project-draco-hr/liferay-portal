{
  Integer type=PortletItemModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ PortletItemModelImpl.TABLE_NAME);
  }
  return type;
}

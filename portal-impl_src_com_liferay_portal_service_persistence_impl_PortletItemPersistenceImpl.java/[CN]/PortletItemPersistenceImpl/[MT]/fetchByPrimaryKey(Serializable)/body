{
  PortletItem portletItem=(PortletItem)entityCache.getResult(PortletItemModelImpl.ENTITY_CACHE_ENABLED,PortletItemImpl.class,primaryKey);
  if (portletItem == _nullPortletItem) {
    return null;
  }
  if (portletItem == null) {
    Session session=null;
    try {
      session=openSession();
      portletItem=(PortletItem)session.get(PortletItemImpl.class,primaryKey);
      if (portletItem != null) {
        cacheResult(portletItem);
      }
 else {
        entityCache.putResult(PortletItemModelImpl.ENTITY_CACHE_ENABLED,PortletItemImpl.class,primaryKey,_nullPortletItem);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(PortletItemModelImpl.ENTITY_CACHE_ENABLED,PortletItemImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return portletItem;
}

{
  ServletContext servletContext=ModifiableServletContext.createInstance(servletContextEvent.getServletContext(),_wabBundleProcessor);
  try {
    _servletContextListener.contextDestroyed(new ServletContextEvent(servletContext));
  }
 catch (  Exception e) {
    _exception=e;
  }
}

{
  for (  Release release : releases) {
    if (EntityCacheUtil.getResult(ReleaseModelImpl.ENTITY_CACHE_ENABLED,ReleaseImpl.class,release.getPrimaryKey(),this) == null) {
      cacheResult(release);
    }
  }
}

{
  for (  Release release : releases) {
    if (EntityCacheUtil.getResult(ReleaseModelImpl.ENTITY_CACHE_ENABLED,Release.class,release.getPrimaryKey(),this) == null) {
      cacheResult(release);
    }
  }
}

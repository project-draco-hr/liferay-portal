{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Release release) method. To improve performance, call update(Release release, boolean merge).");
  }
  return update(release,false);
}

{
  JarResource jarResource=new JarResource("dependencies/META-INF/spring/empty.dependencies","META-INF/spring/context.dependencies");
  Jar jar=analyze(Collections.<String>emptyList(),jarResource);
  Resource resource=jar.getResource("OSGI-INF/context/context.dependencies");
  String value=read(resource);
  Assert.assertEquals(_RELEASE_INFO,value);
}

{
  ObjectValuePair<TriggerState,Exception> jobState=(ObjectValuePair<TriggerState,Exception>)message.get(SchedulerEngine.JOB_STATE);
  jobState.setKey(TriggerState.ERROR);
  jobState.setValue(exception);
}

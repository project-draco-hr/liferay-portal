{
  if (isEscapedModel()) {
    return (DDMList)this;
  }
 else {
    return (DDMList)Proxy.newProxyInstance(DDMList.class.getClassLoader(),new Class[]{DDMList.class},new AutoEscapeBeanHandler(this));
  }
}

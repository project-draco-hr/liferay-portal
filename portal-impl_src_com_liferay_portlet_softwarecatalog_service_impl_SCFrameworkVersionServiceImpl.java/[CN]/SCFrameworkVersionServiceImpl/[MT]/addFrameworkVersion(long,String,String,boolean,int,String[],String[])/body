{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.SOFTWARE_CATALOG,ActionKeys.ADD_FRAMEWORK_VERSION);
  return scFrameworkVersionLocalService.addFrameworkVersion(getUserId(),plid,name,url,active,priority,communityPermissions,guestPermissions);
}

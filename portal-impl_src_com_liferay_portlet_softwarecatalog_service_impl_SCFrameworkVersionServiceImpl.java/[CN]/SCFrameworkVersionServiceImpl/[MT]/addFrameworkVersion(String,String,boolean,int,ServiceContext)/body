{
  PortletPermissionUtil.check(getPermissionChecker(),serviceContext.getPlid(),PortletKeys.SOFTWARE_CATALOG,ActionKeys.ADD_FRAMEWORK_VERSION);
  return scFrameworkVersionLocalService.addFrameworkVersion(getUserId(),name,url,active,priority,serviceContext);
}

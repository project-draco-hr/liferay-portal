{
  List<String> relativePaths=new ArrayList<>();
  for (  File file : files) {
    relativePaths.add(getRelativePath(file));
  }
  return StringUtil.merge(relativePaths.toArray(new String[relativePaths.size()]),",");
}

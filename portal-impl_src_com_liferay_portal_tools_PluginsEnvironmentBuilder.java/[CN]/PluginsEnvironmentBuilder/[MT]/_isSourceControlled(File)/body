{
  boolean controlled=true;
  if (!libDir.exists()) {
    controlled=false;
  }
  try {
    _execSVNCommand(_SVN_INFO + libDir);
  }
 catch (  Exception e) {
    controlled=false;
  }
  return controlled;
}

{
  if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.LOGIN_DIALOG_DISABLED,PropsValues.LOGIN_DIALOG_DISABLED)) {
    return true;
  }
  if (_instance._ssoMap.isEmpty()) {
    return false;
  }
  return _instance._isLoginRedirectRequired(companyId);
}

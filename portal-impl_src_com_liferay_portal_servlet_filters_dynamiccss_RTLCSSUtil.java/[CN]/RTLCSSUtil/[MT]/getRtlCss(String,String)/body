{
  String rtlCss=css;
  try {
    RTLCSSConverter rtlCSSConverter=new RTLCSSConverter();
    rtlCss=rtlCSSConverter.process(rtlCss);
  }
 catch (  Exception e) {
    _log.error("Unable to generate RTL version for " + fileName + StringPool.COMMA_AND_SPACE+ e.getMessage());
  }
  return rtlCss;
}

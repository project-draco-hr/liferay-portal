{
  if (((orderId == null) && (_orderId != null)) || ((orderId != null) && (_orderId == null)) || ((orderId != null) && (_orderId != null) && !orderId.equals(_orderId))) {
    if (!XSS_ALLOW_ORDERID) {
      orderId=XSSUtil.strip(orderId);
    }
    _orderId=orderId;
    setModified(true);
  }
}

{
  long[] classNameIds=PortletDisplayTemplateHandlerRegistryUtil.getClassNameIds();
  for (  long classNameId : classNameIds) {
    List<DDMTemplate> ddmTemplates=DDMTemplateLocalServiceUtil.getTemplates(portletDataContext.getScopeGroupId(),classNameId);
    for (    DDMTemplate ddmTemplate : ddmTemplates) {
      StagedModelDataHandlerUtil.exportStagedModel(portletDataContext,portletDisplayTemplatesElement,ddmTemplate);
    }
  }
}

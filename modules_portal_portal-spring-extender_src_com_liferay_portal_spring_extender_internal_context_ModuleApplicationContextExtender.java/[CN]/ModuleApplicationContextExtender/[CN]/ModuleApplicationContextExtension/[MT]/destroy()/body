{
  for (  ServiceRegistration<UpgradeStep> upgradeStepsRegistration : _upgradeStepsRegistrations) {
    upgradeStepsRegistration.unregister();
  }
  if (_component != null) {
    _dependencyManager.remove(_component);
  }
}

{
  byte[] fileContent;
  try {
    fileContent=FileUtil.getBytes(file);
  }
 catch (  IOException ex) {
    fileContent=null;
  }
  _content=Base64.encode(fileContent);
  _name=file.getName();
  _size=file.length();
}

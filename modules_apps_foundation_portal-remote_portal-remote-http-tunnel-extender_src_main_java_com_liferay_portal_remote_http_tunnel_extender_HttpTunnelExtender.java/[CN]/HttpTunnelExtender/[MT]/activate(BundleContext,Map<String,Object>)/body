{
  _bundleContext=bundleContext;
  _httpTunnelExtenderConfiguration=ConfigurableUtil.createConfigurable(HttpTunnelExtenderConfiguration.class,properties);
  _logger=new Logger(bundleContext);
  start(bundleContext);
}

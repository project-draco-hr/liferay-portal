{
  Session session=null;
  try {
    session=openSession();
    if (categoriesVocabulary.isCachedModel() || BatchSessionUtil.isEnabled()) {
      Object staleObject=session.get(CategoriesVocabularyImpl.class,categoriesVocabulary.getPrimaryKeyObj());
      if (staleObject != null) {
        session.evict(staleObject);
      }
    }
    session.delete(categoriesVocabulary);
    session.flush();
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST);
  CategoriesVocabularyModelImpl categoriesVocabularyModelImpl=(CategoriesVocabularyModelImpl)categoriesVocabulary;
  FinderCacheUtil.removeResult(FINDER_PATH_FETCH_BY_G_N,new Object[]{new Long(categoriesVocabularyModelImpl.getOriginalGroupId()),categoriesVocabularyModelImpl.getOriginalName()});
  EntityCacheUtil.removeResult(CategoriesVocabularyModelImpl.ENTITY_CACHE_ENABLED,CategoriesVocabularyImpl.class,categoriesVocabulary.getPrimaryKey());
  return categoriesVocabulary;
}

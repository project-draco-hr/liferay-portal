{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(CategoriesVocabulary categoriesVocabulary) method. Use update(CategoriesVocabulary categoriesVocabulary, boolean merge) instead.");
  }
  return update(categoriesVocabulary,false);
}

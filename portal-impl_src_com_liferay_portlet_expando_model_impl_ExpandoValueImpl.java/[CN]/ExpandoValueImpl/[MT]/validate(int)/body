{
  ExpandoColumn column=ExpandoColumnLocalServiceUtil.getColumn(getColumnId());
  if (column.getType() != type) {
    throw new ValueDataException("Column " + getColumnId() + " has type "+ ExpandoColumnImpl.getTypeLabel(column.getType())+ " and is not compatbile with type "+ ExpandoColumnImpl.getTypeLabel(type));
  }
}

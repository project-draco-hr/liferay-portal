{
  ExpandoColumn column=getColumn();
  if (column == null) {
    return false;
  }
  if ((column.getType() == ExpandoColumnConstants.STRING_ARRAY_LOCALIZED) || (column.getType() == ExpandoColumnConstants.STRING_LOCALIZED)) {
    return true;
  }
  return false;
}

{
  if (!isColumnLocalized()) {
    return null;
  }
  List<Locale> locales=new ArrayList<Locale>();
  for (  String languageId : LocalizationUtil.getAvailableLocales(getData())) {
    locales.add(LocaleUtil.fromLanguageId(languageId));
  }
  return locales;
}

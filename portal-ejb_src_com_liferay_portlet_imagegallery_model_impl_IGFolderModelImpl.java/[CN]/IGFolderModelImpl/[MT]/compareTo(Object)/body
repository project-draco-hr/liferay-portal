{
  if (obj == null) {
    return -1;
  }
  IGFolderImpl igFolder=(IGFolderImpl)obj;
  int value=0;
  value=getFolderId().compareTo(igFolder.getFolderId());
  if (value != 0) {
    return value;
  }
  value=getName().toLowerCase().compareTo(igFolder.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  String topicId=ParamUtil.getString(req,"topicId");
  String categoryId=ParamUtil.getString(req,"categoryId");
  String name=ParamUtil.getString(req,"name");
  String description=ParamUtil.getString(req,"description");
  boolean addCommunityPermissions=ParamUtil.getBoolean(req,"addCommunityPermissions");
  boolean addGuestPermissions=ParamUtil.getBoolean(req,"addGuestPermissions");
  if (Validator.isNull(topicId)) {
    CaptchaUtil.check(req);
    MBTopicServiceUtil.addTopic(categoryId,name,description,addCommunityPermissions,addGuestPermissions);
  }
 else {
    MBTopicServiceUtil.updateTopic(topicId,categoryId,name,description);
  }
}

{
  DLFileEntry dlFileEntry=(DLFileEntry)baseModel;
  FileEntry fileEntry=DLAppTestUtil.updateFileEntry(serviceContext.getScopeGroupId(),dlFileEntry.getFileEntryId(),null,keywords);
  return (DLFileEntry)fileEntry.getModel();
}

{
  DLFileEntry dlFileEntry=(DLFileEntry)baseModel;
  FileEntry fileEntry=DLAppTestUtil.updateFileEntry(serviceContext.getScopeGroupId(),dlFileEntry.getFileEntryId(),null,dlFileEntry.getMimeType(),keywords,true,serviceContext);
  return (DLFileEntry)fileEntry.getModel();
}

{
  DLFolder dlFolder=(DLFolder)parentBaseModel;
  FileEntry fileEntry=DLAppTestUtil.addFileEntry(dlFolder.getGroupId(),dlFolder.getFolderId(),keywords + ".txt",keywords,approved);
  return (DLFileEntry)fileEntry.getModel();
}

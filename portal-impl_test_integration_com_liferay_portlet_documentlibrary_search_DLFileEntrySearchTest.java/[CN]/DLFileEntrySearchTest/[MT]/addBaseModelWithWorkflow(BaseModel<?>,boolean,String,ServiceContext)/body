{
  DLFolder dlFolder=(DLFolder)parentBaseModel;
  FileEntry fileEntry=DLAppTestUtil.addFileEntry(dlFolder.getFolderId(),keywords + ".txt",keywords,approved,serviceContext);
  return (DLFileEntry)fileEntry.getModel();
}

{
  PermissionImporter permissionImporter=new PermissionImporter();
  permissionImporter.importPortletPermissions(new LayoutCache(),TestPropsValues.getCompanyId(),importGroup.getGroupId(),TestPropsValues.getUserId(),importLayout,portletElement,portletId);
}

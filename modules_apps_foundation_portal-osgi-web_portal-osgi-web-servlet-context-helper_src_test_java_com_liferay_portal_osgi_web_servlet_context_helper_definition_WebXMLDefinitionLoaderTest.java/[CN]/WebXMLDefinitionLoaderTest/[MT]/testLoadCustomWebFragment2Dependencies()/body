{
  EntryLoaderMockBundle bundle=new EntryLoaderMockBundle("dependencies/custom-web-fragment2.xml");
  WebXMLDefinitionLoader webXMLDefinitionLoader=new WebXMLDefinitionLoader(bundle,SAXParserFactory.newInstance(),new Logger(null));
  Order ordering=new OrderImpl();
  EnumMap<Order.Path,String[]> routes=ordering.getRoutes();
  routes.put(Order.Path.AFTER,new String[]{"fragment1"});
  testLoadDependencies(webXMLDefinitionLoader,0,0,0,bundle.getEntry(),"fragment2",ordering,null);
}

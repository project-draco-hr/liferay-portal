{
  try {
    _persistence.countByG_NotC(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
    _persistence.countByG_NotC(0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

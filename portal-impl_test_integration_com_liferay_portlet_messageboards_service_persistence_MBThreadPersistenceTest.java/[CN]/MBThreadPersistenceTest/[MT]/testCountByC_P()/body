{
  try {
    _persistence.countByC_P(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDouble());
    _persistence.countByC_P(0L,0D);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  try {
    _persistence.countByL_P(ServiceTestUtil.nextDate(),ServiceTestUtil.nextDouble());
    _persistence.countByL_P(ServiceTestUtil.nextDate(),0D);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  if (!PortalRunMode.isTestMode()) {
    return super.createSearchReaderDestination(searchWriterDestinationName);
  }
  SynchronousDestination synchronousDestination=new SynchronousDestination();
  synchronousDestination.setName(searchWriterDestinationName);
  return synchronousDestination;
}

{
  if (!contains(permissionChecker,template,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDMTemplate.class.getName(),template.getTemplateId(),actionId);
  }
}

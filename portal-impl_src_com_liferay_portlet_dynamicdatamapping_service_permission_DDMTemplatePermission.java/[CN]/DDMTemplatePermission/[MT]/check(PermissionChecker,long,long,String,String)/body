{
  if (!contains(permissionChecker,groupId,templateId,portletId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDMTemplate.class.getName(),templateId,actionId);
  }
}

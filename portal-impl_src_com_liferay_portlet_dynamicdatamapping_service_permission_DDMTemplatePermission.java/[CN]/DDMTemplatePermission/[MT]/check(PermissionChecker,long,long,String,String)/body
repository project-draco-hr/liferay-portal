{
  if (!contains(permissionChecker,groupId,templateId,portletId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,DDMTemplate.class.getName(),templateId,actionId);
  }
}

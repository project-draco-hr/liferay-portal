{
  String[] names=CustomSQLUtil.keywords(name);
  String[] realNames=CustomSQLUtil.keywords(realName);
  String[] descriptions=CustomSQLUtil.keywords(description);
  return findByC_PG_C_N_D(companyId,parentGroupId,parentGroupIdComparator,classNameIds,names,realNames,descriptions,params,andOperator,start,end,obc);
}

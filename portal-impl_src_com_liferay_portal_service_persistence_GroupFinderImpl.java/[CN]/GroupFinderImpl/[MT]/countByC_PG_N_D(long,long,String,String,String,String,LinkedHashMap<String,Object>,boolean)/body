{
  name=StringUtil.lowerCase(name);
  description=StringUtil.lowerCase(description);
  String[] names=CustomSQLUtil.keywords(name);
  String[] realNames=CustomSQLUtil.keywords(realName);
  String[] descriptions=CustomSQLUtil.keywords(description);
  return countByC_C_PG_N_D(companyId,_getGroupOrganizationClassNameIds(),parentGroupId,parentGroupIdComparator,names,realNames,descriptions,params,andOperator);
}

{
  String[] names=null;
  String[] realNames=null;
  String[] descriptions=null;
  boolean andOperator=false;
  if (Validator.isNotNull(keywords)) {
    names=CustomSQLUtil.keywords(keywords);
    realNames=CustomSQLUtil.keywords(keywords);
    descriptions=CustomSQLUtil.keywords(keywords);
  }
 else {
    andOperator=true;
  }
  return findByC_C_PG_N_D(companyId,_getGroupOrganizationClassNameIds(),parentGroupId,parentGroupIdComparator,names,realNames,descriptions,params,andOperator,start,end,obc);
}

{
  lock.lock();
  try {
    return doGetNotificationEvents(flush);
  }
 catch (  ChannelException ce) {
    throw ce;
  }
catch (  Exception e) {
    throw new ChannelException(e);
  }
 finally {
    lock.unlock();
  }
}

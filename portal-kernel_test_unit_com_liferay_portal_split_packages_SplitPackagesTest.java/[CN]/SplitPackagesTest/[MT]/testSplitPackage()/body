{
  Set<String> portalImplPackageNames=_getPackageNames(Paths.get("portal-impl/src"));
  Set<String> portalServicePackageNames=_getPackageNames(Paths.get("portal-kernel/src"));
  portalImplPackageNames.retainAll(portalServicePackageNames);
  Assert.assertTrue("Detected split packages in portal-kernel and portal-impl: " + portalImplPackageNames,portalImplPackageNames.isEmpty());
}

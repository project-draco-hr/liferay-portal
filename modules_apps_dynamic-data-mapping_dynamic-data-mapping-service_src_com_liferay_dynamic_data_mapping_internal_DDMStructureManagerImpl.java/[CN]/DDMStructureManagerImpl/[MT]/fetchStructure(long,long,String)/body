{
  com.liferay.portlet.dynamicdatamapping.model.DDMStructure structure=_ddmStructureLocalService.fetchStructure(groupId,classNameId,structureKey);
  if (structure == null) {
    return null;
  }
  return new DDMStructureImpl(structure);
}

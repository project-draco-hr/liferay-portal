{
  if (fileName == null) {
    return null;
  }
  int pos=fileName.lastIndexOf(StringPool.PERIOD);
  if (pos != -1) {
    return fileName.substring(pos + 1,fileName.length());
  }
 else {
    return null;
  }
}

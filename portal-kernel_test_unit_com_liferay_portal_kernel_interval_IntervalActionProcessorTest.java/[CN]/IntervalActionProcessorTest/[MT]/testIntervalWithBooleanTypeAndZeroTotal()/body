{
  IntervalActionProcessor<Boolean> intervalSearcher=new IntervalActionProcessor<>(0,1);
  intervalSearcher.setPerformIntervalActionMethod(new IntervalActionProcessor.PerformIntervalActionMethod<Boolean>(){
    @Override public Boolean performIntervalAction(    int start,    int end){
      return false;
    }
  }
);
  Assert.assertNull(intervalSearcher.performIntervalActions());
}

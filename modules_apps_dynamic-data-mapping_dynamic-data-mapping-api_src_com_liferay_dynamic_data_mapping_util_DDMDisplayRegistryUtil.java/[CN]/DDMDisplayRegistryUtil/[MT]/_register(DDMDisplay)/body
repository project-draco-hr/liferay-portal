{
  Registry registry=RegistryUtil.getRegistry();
  ServiceRegistration<DDMDisplay> serviceRegistration=registry.registerService(DDMDisplay.class,ddmDisplay);
  _serviceRegistrations.put(ddmDisplay,serviceRegistration);
}

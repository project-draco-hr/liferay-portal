{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(CategoriesProperty categoriesProperty) method. Use update(CategoriesProperty categoriesProperty, boolean merge) instead.");
  }
  return update(categoriesProperty,false);
}

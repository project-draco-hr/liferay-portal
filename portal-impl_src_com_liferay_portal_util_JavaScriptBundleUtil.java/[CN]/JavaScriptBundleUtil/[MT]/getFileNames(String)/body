{
  String[] fileNames=(String[])SingleVMPoolUtil.get(CACHE_NAME,bundleId);
  if (fileNames == null) {
    fileNames=_instance._getFileNames(bundleId);
    SingleVMPoolUtil.put(CACHE_NAME,bundleId,fileNames);
  }
  return fileNames;
}

{
  Integer type=BackgroundTaskModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ BackgroundTaskModelImpl.TABLE_NAME);
  }
  return type;
}

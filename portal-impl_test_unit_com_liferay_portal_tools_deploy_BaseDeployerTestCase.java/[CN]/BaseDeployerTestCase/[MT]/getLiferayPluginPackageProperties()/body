{
  InputStream inputStream=new FileInputStream(WEB_INF_FOLDER + "/liferay-plugin-package.properties");
  String stringProperties=StringUtil.read(inputStream);
  Properties properties=PropertiesUtil.load(stringProperties);
  Assert.assertFalse("Test properties have not been loaded properly.",properties.isEmpty());
  return properties;
}

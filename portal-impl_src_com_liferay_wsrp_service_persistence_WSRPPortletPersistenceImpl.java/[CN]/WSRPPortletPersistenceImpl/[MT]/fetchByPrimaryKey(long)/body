{
  Session session=null;
  try {
    session=openSession();
    return (WSRPPortlet)session.get(WSRPPortletImpl.class,new Long(portletId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WSRPPortlet wsrpPortlet) method. Use update(WSRPPortlet wsrpPortlet, boolean merge) instead.");
  }
  return update(wsrpPortlet,false);
}

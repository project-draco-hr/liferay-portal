{
  for (long i=0; i < sourceIndexInput.length(); i++) {
    if (sourceIndexInput.readByte() != newIndexInput.readByte()) {
      fail("Files content do not match, name : " + fileName + ", position : "+ i);
    }
  }
  sourceIndexInput.close();
  newIndexInput.close();
}

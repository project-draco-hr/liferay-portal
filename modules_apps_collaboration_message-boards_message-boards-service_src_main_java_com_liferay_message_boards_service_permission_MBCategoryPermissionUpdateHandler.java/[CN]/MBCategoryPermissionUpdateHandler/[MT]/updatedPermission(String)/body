{
  MBCategory mbCategory=_mbCategoryLocalService.fetchMBCategory(GetterUtil.getLong(primKey));
  if (mbCategory == null) {
    return;
  }
  mbCategory.setModifiedDate(new Date());
  _mbCategoryLocalService.updateMBCategory(mbCategory);
}

{
  Group group=groupPersistence.fetchByPrimaryKey(repositoryId);
  if (group == null) {
    Repository repository=repositoryPersistence.fetchByPrimaryKey(repositoryId);
    if (repository == null) {
      throw new RepositoryException("Repository ID invalid " + repositoryId);
    }
  }
}

{
  com.liferay.portal.kernel.repository.Repository repositoryImpl=new LiferayRepository(folderId,fileEntryId,fileVersionId);
  checkRepository(repositoryImpl.getRepositoryId());
  return repositoryImpl;
}

{
  com.liferay.portal.kernel.repository.Repository repositoryImpl=repositoryLocalService.getRepositoryImpl(folderId,fileEntryId,fileVersionId);
  checkRepository(repositoryImpl.getRepositoryId());
  return repositoryImpl;
}

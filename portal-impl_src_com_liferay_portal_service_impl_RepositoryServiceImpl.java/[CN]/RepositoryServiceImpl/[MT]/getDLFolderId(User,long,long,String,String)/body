{
  DLFolder dlFolder=dlRepositoryLocalService.addFolder(user.getUserId(),groupId,repositoryId,DLFolderConstants.MAPPED_PARENT_FOLDER_ID,name,description,new ServiceContext());
  return dlFolder.getFolderId();
}

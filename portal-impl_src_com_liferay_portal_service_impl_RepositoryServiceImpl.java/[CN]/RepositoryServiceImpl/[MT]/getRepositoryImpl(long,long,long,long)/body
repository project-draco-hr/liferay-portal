{
  com.liferay.portal.kernel.repository.Repository repositoryImpl=repositoryLocalService.getRepositoryImpl(folderId,fileEntryId,fileVersionId,fileShortcutId);
  checkRepository(repositoryImpl.getRepositoryId(),folderId,fileEntryId,fileVersionId);
  return repositoryImpl;
}

{
  Registry registry=RegistryUtil.getRegistry();
  _serviceTracker=registry.trackServices(ClusterMasterExecutor.class,new ClusterMasterExecutorServiceTrackerCustomizer());
  _serviceTracker.open();
}

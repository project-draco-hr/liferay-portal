{
  ClusterMasterExecutor clusterMasterExecutor=_serviceTracker.getService();
  if ((clusterMasterExecutor == null) || !_portalInitialized) {
    return;
  }
  clusterMasterExecutor.initialize();
  SchedulerEngineHelperUtil.start();
}

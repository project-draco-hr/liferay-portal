{
  Integer type=SocialActivityModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SocialActivityModelImpl.TABLE_NAME);
  }
  return type;
}

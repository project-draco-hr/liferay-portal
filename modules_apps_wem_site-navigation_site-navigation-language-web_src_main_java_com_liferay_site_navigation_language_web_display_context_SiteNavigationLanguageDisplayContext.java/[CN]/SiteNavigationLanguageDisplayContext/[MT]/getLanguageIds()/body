{
  if (_languageIds != null) {
    return _languageIds;
  }
  _languageIds=StringUtil.split(_siteNavigationLanguagePortletInstanceConfiguration.languageIds());
  if (ArrayUtil.isEmpty(_languageIds)) {
    _languageIds=getAvailableLanguageIds();
  }
  return _languageIds;
}

{
  BlogsCategory model=new BlogsCategoryImpl();
  model.setCategoryId(getCategoryId());
  model.setCompanyId(getCompanyId());
  model.setUserId(getUserId());
  model.setUserName(Html.escape(getUserName()));
  model.setCreateDate(getCreateDate());
  model.setModifiedDate(getModifiedDate());
  model.setParentCategoryId(getParentCategoryId());
  model.setName(Html.escape(getName()));
  model.setDescription(Html.escape(getDescription()));
  if (true) {
    model=(BlogsCategory)Proxy.newProxyInstance(BlogsCategory.class.getClassLoader(),new Class[]{BlogsCategory.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

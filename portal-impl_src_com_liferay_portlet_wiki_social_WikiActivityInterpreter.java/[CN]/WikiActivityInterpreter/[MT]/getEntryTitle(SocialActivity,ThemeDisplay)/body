{
  WikiPageResource pageResource=WikiPageResourceLocalServiceUtil.getPageResource(activity.getClassPK());
  WikiPage page=WikiPageLocalServiceUtil.getPage(pageResource.getNodeId(),pageResource.getTitle());
  if (!page.isInTrash()) {
    return TrashUtil.getOriginalTitle(page.getTitle());
  }
  return page.getTitle();
}

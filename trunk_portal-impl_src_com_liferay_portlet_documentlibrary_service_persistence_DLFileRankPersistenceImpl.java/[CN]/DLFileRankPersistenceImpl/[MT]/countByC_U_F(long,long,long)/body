{
  Object[] finderArgs=new Object[]{companyId,userId,fileEntryId};
  Long count=(Long)FinderCacheUtil.getResult(FINDER_PATH_COUNT_BY_C_U_F,finderArgs,this);
  if (count == null) {
    StringBundler query=new StringBundler(4);
    query.append(_SQL_COUNT_DLFILERANK_WHERE);
    query.append(_FINDER_COLUMN_C_U_F_COMPANYID_2);
    query.append(_FINDER_COLUMN_C_U_F_USERID_2);
    query.append(_FINDER_COLUMN_C_U_F_FILEENTRYID_2);
    String sql=query.toString();
    Session session=null;
    try {
      session=openSession();
      Query q=session.createQuery(sql);
      QueryPos qPos=QueryPos.getInstance(q);
      qPos.add(companyId);
      qPos.add(userId);
      qPos.add(fileEntryId);
      count=(Long)q.uniqueResult();
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (count == null) {
        count=Long.valueOf(0);
      }
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_U_F,finderArgs,count);
      closeSession(session);
    }
  }
  return count.intValue();
}

{
  Assert.assertEquals(expectedInstanceId,ddmFormFieldValue.getInstanceId());
  Value value=ddmFormFieldValue.getValue();
  Assert.assertEquals(expectedEnValue,value.getString(LocaleUtil.US));
  Assert.assertEquals(expectedPtValue,value.getString(LocaleUtil.BRAZIL));
}

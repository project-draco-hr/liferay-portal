{
  for (  Object object : list) {
    List<Object> listObject=_convertIterableToList(object);
    if (listObject != null) {
      Object value=_addVariableStatementList(variableStatement,listObject,results);
      results.add(value);
    }
 else {
      Object value=_addVariableStatement(variableStatement,object);
      results.add(value);
    }
  }
  return results;
}

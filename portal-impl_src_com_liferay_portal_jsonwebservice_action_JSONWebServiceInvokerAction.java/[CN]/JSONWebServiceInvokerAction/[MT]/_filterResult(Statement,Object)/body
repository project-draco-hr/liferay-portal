{
  List<Object> listResult=_convertIterableToList(result);
  if (listResult != null) {
    result=_filterResultList(statement,listResult,new ArrayList<Object>());
  }
 else {
    result=_filterResultObject(statement,result);
  }
  return result;
}

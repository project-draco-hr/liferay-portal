{
  User user=PortalUtil.getSelectedUser(portletRequest);
  long[] roleIds=new long[0];
  if (user != null) {
    roleIds=user.getRoleIds();
  }
  return getRequestPrimaryKeys(portletRequest,roleIds,"addRoleIds","deleteRoleIds");
}

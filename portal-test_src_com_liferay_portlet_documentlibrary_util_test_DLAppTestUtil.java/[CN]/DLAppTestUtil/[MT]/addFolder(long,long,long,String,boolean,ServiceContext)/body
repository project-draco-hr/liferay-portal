{
  String name=PrincipalThreadLocal.getName();
  try {
    PrincipalThreadLocal.setName(userId);
    return addFolder(repositoryId,parentFolderId,folderName,deleteExisting,serviceContext);
  }
  finally {
    PrincipalThreadLocal.setName(name);
  }
}

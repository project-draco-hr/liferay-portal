{
  long userId=TestPropsValues.getUserId();
  String mappedId=RandomTestUtil.randomString();
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(groupId);
  return addRepositoryEntry(userId,groupId,repositoryId,mappedId,serviceContext);
}

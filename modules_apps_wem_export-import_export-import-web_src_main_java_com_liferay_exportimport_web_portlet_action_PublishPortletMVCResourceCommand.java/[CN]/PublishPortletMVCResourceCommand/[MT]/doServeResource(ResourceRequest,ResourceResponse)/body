{
  String cmd=ParamUtil.getString(resourceRequest,Constants.CMD);
  if (!cmd.equals(Constants.PUBLISH)) {
    return;
  }
  PortletRequestDispatcher portletRequestDispatcher=getPortletRequestDispatcher(resourceRequest,"/publish_portlet_processes.jsp");
  resourceRequest=ActionUtil.getWrappedResourceRequest(resourceRequest,null);
  portletRequestDispatcher.include(resourceRequest,resourceResponse);
}

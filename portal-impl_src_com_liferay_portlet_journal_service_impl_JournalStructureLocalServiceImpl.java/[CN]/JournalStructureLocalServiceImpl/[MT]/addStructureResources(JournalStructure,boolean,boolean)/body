{
  ResourceLocalServiceUtil.addResources(structure.getCompanyId(),structure.getGroupId(),structure.getUserId(),JournalStructure.class.getName(),structure.getStructureId(),false,addCommunityPermissions,addGuestPermissions);
}

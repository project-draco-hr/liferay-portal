{
  for (  JournalStructure structure : journalStructurePersistence.findByGroupId(groupId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new JournalStructureIDComparator())) {
    deleteStructure(structure);
  }
}

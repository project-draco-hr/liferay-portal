{
  Element delayElement=timerElement.addElement(elementName);
  addTextElement(delayElement,"duration",String.valueOf(delayDuration.getDuration()));
  addTextElement(delayElement,"scale",delayDuration.getDurationScale().getValue());
}

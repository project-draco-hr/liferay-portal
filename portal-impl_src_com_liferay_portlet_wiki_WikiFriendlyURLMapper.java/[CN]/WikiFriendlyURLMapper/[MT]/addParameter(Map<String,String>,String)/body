{
  if (!routeParameters.containsKey(name)) {
    return;
  }
  String nodeName=routeParameters.get(name);
  nodeName=StringUtil.replace(nodeName,_ESCAPED_CHARS,_UNESCAPED_CHARS);
  routeParameters.put(name,nodeName);
}

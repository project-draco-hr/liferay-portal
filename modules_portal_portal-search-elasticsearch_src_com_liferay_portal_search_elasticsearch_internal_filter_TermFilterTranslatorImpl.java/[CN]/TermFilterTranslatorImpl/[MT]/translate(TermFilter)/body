{
  TermFilterBuilder termFilterBuilder=FilterBuilders.termFilter(termFilter.getField(),termFilter.getValue());
  if (termFilter.isCached() != null) {
    termFilterBuilder.cache(termFilter.isCached());
  }
  return termFilterBuilder;
}

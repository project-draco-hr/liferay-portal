{
  com.liferay.dynamic.data.mapping.model.DDMTemplate ddmTemplate=_ddmTemplateLocalService.fetchTemplate(groupId,classNameId,templateKey);
  if (ddmTemplate == null) {
    return null;
  }
  return new DDMTemplateImpl(ddmTemplate);
}

{
  for (  Map.Entry<PortalCacheListener<K,V>,CacheListenerScope> entry : _cacheListeners.entrySet()) {
    PortalCacheListener<K,V> cacheListener=entry.getKey();
    if (_shouldDeliver(cacheListener,entry.getValue())) {
      cacheListener.notifyEntryEvicted(portalCache,key,value,timeToLive);
    }
  }
}

{
  int count=0;
  for (  long organizationId : organizationIds) {
    LinkedHashMap<String,Object> params=new LinkedHashMap<>();
    params.put("usersOrgs",organizationId);
    count+=UserLocalServiceUtil.searchCount(companyId,null,status,params);
  }
  return count;
}

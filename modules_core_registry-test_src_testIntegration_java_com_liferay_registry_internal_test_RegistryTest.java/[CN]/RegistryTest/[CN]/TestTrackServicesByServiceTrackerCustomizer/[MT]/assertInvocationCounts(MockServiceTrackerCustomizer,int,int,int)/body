{
  Assert.assertEquals(expectedAddingInvocationCount,mockServiceTrackerCustomizer.getAddingInvocationCount());
  Assert.assertEquals(expectedModifiedInvocationCount,mockServiceTrackerCustomizer.getModifiedInvocationCount());
  Assert.assertEquals(expectedRemovedInvocationCount,mockServiceTrackerCustomizer.getRemovedInvocationCount());
}

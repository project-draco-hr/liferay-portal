{
  if (_out == null) {
    _hasError=true;
  }
 else {
    try {
      _out.write(s);
    }
 catch (    InterruptedIOException x) {
      Thread.currentThread().interrupt();
    }
catch (    IOException x) {
      _hasError=true;
    }
  }
}

{
  if (_out == null) {
    _hasError=true;
  }
 else {
    try {
      _out.write(c);
    }
 catch (    InterruptedIOException x) {
      Thread.currentThread().interrupt();
    }
catch (    IOException x) {
      _hasError=true;
    }
  }
}

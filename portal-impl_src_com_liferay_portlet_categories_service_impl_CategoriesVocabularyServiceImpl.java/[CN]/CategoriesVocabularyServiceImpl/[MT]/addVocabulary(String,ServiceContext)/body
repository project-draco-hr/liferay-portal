{
  CategoriesPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_VOCABULARY);
  return categoriesVocabularyLocalService.addVocabulary(getUserId(),name,serviceContext);
}

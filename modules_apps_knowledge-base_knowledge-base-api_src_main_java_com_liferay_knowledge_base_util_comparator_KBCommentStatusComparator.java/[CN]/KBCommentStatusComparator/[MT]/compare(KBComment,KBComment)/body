{
  int value=0;
  if (kbComment1.getStatus() < kbComment2.getStatus()) {
    value=-1;
  }
 else   if (kbComment1.getStatus() > kbComment2.getStatus()) {
    value=1;
  }
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

{
  try {
    Layout layout=LayoutLocalServiceUtil.getLayout(plid);
    if (AssetUtil.isDefaultAssetPublisher(layout,portletId,StringPool.BLANK)) {
      JournalArticleLocalServiceUtil.deleteLayoutArticleReferences(layout.getGroupId(),layout.getUuid());
    }
    SubscriptionLocalServiceUtil.deleteSubscriptions(layout.getCompanyId(),PortletPreferences.class.getName(),AssetPublisherUtil.getSubscriptionClassPK(plid,portletId));
  }
 catch (  Exception e) {
    throw new PortletLayoutListenerException(e);
  }
}

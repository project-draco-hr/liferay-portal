{
  StringMaker sm=new StringMaker();
  sm.append(CACHE_NAME);
  sm.append(StringPool.POUND);
  sm.append(nodeId);
  sm.append(title);
  if (postfix != null) {
    sm.append(StringPool.POUND);
    sm.append(postfix);
  }
  return sm.toString();
}

{
  if ((array == null) || (array.length == 0)) {
    return new HashSet();
  }
  Set<Long> set=new HashSet<Long>(array.length);
  for (int i=0; i < array.length; i++) {
    set.add(array[i]);
  }
  return set;
}

{
  if ((array == null) || (array.size() == 0)) {
    return new HashSet();
  }
  Set set=new HashSet(array.size());
  for (int i=0; i < array.size(); i++) {
    set.add(array.get(i));
  }
  return set;
}

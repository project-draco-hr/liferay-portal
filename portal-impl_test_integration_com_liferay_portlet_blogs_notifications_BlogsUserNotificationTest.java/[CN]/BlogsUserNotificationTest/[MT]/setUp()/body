{
  super.setUp();
  _group=GroupTestUtil.addGroup();
  _user=TestPropsValues.getUser();
  BlogsEntryLocalServiceUtil.subscribe(_user.getUserId(),_group.getGroupId());
  addUserNotificationDeliveries();
  setActiveAllUserNotificationDeliveries(true);
  _logRecords=JDKLoggerTestUtil.configureJDKLogger(LoggerMockMailServiceImpl.class.getName(),Level.INFO);
}

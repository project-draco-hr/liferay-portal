{
  _displayTerms=displayTerms;
  _searchTerms=searchTerms;
  _curParam=curParam;
  _curValue=ParamUtil.get(req,_curParam,1);
  setDelta(delta);
  _iteratorURL=iteratorURL;
  _iteratorURL.setParameter(DisplayTerms.AND_OPERATOR,Boolean.toString(ParamUtil.get(req,DisplayTerms.AND_OPERATOR,true)));
  _headerNames=ListUtil.copy(headerNames);
  _emptyResultsMessage=emptyResultsMessage;
}

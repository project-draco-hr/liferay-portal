{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof AttributeImpl)) {
    return false;
  }
  org.dom4j.Attribute attribute=((AttributeImpl)obj).getWrappedAttribute();
  return _attribute.equals(attribute);
}

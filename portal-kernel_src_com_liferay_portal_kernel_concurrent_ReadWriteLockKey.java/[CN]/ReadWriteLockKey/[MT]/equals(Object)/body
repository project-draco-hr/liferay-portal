{
  if (obj == null) {
    return false;
  }
  if (getClass() != obj.getClass()) {
    return false;
  }
  final ReadWriteLockKey<T> other=(ReadWriteLockKey<T>)obj;
  if (this._key != other._key && (this._key == null || !this._key.equals(other._key))) {
    return false;
  }
  return true;
}

{
  List<DLDocumentMetadataSet> metadataSets=dlDocumentMetadataSetPersistence.findByFileVersionId(fileVersionId);
  for (  DLDocumentMetadataSet metadataSet : metadataSets) {
    deleteMetadataSet(metadataSet);
  }
}

{
  Map map=super.getParameterMap();
  if (_filteredParams == null) {
    return map;
  }
  Iterator itr=_filteredParams.iterator();
  while (itr.hasNext()) {
    map.remove(itr.next());
  }
  return map;
}

{
  if (_HYPERSONIC) {
    return Arrays.asList(new String[]{"portal:jdbc.default.url=".concat(_databaseURL),"portal:jdbc.default.username=sa","portal:jdbc.default.password="});
  }
  return Collections.emptyList();
}

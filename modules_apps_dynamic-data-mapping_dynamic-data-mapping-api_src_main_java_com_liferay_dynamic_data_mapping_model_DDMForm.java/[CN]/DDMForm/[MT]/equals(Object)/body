{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof DDMForm)) {
    return false;
  }
  DDMForm ddmForm=(DDMForm)obj;
  if (Validator.equals(_availableLocales,ddmForm._availableLocales) && Validator.equals(_defaultLocale,ddmForm._defaultLocale) && Validator.equals(_ddmFormFields,ddmForm._ddmFormFields)) {
    return true;
  }
  return false;
}

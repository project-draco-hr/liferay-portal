{
  FileEntry fileEntry=DLAppTestUtil.addFileEntry(_group.getGroupId(),DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,true,StringUtil.randomString(20));
  String name=StringUtil.randomString(20).concat(BLACKLIST_CHARS[0]);
  try {
    DLAppTestUtil.updateFileEntry(_group.getGroupId(),fileEntry.getFileEntryId(),name,name);
    Assert.fail(name + " is not a valid name.");
  }
 catch (  Exception e) {
    Assert.assertTrue(e instanceof FileNameException);
  }
}

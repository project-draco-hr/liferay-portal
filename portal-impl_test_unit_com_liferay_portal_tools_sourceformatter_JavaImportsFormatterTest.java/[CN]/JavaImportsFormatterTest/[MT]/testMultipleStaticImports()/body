{
  String original="import static org.junit.Assert.assertEquals;" + "\n\n" + "import static org.mockito.Mockito.when;";
  String expected=original + "\n";
  _assertFormat("multiple static imports should be preserved",original,expected);
}

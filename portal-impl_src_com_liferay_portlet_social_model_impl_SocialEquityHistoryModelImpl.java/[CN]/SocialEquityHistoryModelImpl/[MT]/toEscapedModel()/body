{
  if (isEscapedModel()) {
    return (SocialEquityHistory)this;
  }
 else {
    return (SocialEquityHistory)Proxy.newProxyInstance(SocialEquityHistory.class.getClassLoader(),new Class[]{SocialEquityHistory.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (SocialEquityHistory)this;
  }
 else {
    return (SocialEquityHistory)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

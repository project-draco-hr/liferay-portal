{
  try {
    _persistence.countByBNS_BNU(StringPool.BLANK,ServiceTestUtil.nextLong());
    _persistence.countByBNS_BNU(StringPool.NULL,0L);
    _persistence.countByBNS_BNU((String)null,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

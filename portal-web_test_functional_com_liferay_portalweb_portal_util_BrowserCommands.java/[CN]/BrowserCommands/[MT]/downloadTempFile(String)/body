{
  if (!_BROWSER_TYPE.equals("*iehta") && !_BROWSER_TYPE.equals("*iexplore")) {
    return;
  }
  try {
    Runtime runtime=Runtime.getRuntime();
    String command=_BROWSER_COMMANDS_DIR + "download_file.exe";
    runtime.exec(command);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

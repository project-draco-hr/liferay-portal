{
  if (!_BROWSER_TYPE.equals("*iehta") && !_BROWSER_TYPE.equals("*iexplore")) {
    return;
  }
  try {
    Runtime runtime=Runtime.getRuntime();
    String[] commands={_BROWSER_COMMANDS_DIR + "set_browser_option.exe",_OUTPUT_DIR};
    runtime.exec(commands);
    Thread.sleep(10000);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

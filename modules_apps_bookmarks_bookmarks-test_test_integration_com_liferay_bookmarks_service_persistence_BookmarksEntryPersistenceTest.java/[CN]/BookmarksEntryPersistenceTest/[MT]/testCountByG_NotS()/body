{
  try {
    _persistence.countByG_NotS(RandomTestUtil.nextLong(),RandomTestUtil.nextInt());
    _persistence.countByG_NotS(0L,0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

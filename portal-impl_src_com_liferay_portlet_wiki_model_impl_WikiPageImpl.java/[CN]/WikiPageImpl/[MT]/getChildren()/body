{
  List children=null;
  try {
    children=WikiPageLocalServiceUtil.getChildren(getNodeId(),getTitle());
  }
 catch (  Exception e) {
    children=new ArrayList();
    _log.error(e);
  }
  return children;
}

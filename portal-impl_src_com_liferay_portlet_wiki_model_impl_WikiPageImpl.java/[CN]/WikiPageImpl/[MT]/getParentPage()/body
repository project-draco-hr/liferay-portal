{
  if (Validator.isNull(getParent())) {
    return null;
  }
  WikiPage page=null;
  try {
    page=WikiPageLocalServiceUtil.getPage(getNodeId(),getParent());
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return page;
}

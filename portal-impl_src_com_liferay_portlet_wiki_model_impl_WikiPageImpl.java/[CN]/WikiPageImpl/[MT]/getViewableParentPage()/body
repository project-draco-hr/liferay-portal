{
  if (Validator.isNull(getParentTitle())) {
    return null;
  }
  WikiPage page=null;
  try {
    page=WikiPageServiceUtil.getPage(getGroupId(),getNodeId(),getParentTitle());
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return page;
}

{
  if (_attachmentFolderId > 0) {
    return _attachmentFolderId;
  }
  _attachmentFolderId=WikiPageAttachmentUtil.getPageFolderId(getGroupId(),getUserId(),getNodeId(),getResourcePrimKey());
  return _attachmentFolderId;
}

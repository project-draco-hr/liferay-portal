{
  HttpPrincipal httpPrincipal=null;
  if (authenticated) {
    httpPrincipal=new HttpPrincipal(TestProps.get("http.url"),TestProps.get("http.user.id"),TestProps.get("http.password"));
  }
 else {
    httpPrincipal=new HttpPrincipal(TestProps.get("http.url"));
  }
  return httpPrincipal;
}

{
  Calendar calendar=Calendar.getInstance();
  Date endDate=CalendarUtil.getLTDate(calendar);
  calendar.set(Calendar.MONTH,1);
  calendar.set(Calendar.DATE,1);
  calendar.add(Calendar.YEAR,-2);
  searchCount(CalendarUtil.getGTDate(calendar),endDate,_USERS_COUNT_LAST_24_HOURS + _USERS_COUNT_LAST_WEEK + _USERS_COUNT_LAST_MONTH+ _USERS_COUNT_LAST_YEAR+ _USERS_COUNT_LAST_TWO_YEARS);
}

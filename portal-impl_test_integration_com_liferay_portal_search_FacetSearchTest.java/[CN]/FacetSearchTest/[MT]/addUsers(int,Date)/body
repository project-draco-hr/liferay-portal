{
  for (int i=0; i < count; i++) {
    User user=UserTestUtil.addUser(RandomTestUtil.randomString(),false,RandomTestUtil.randomString(),_randomLastName,new long[]{TestPropsValues.getGroupId()});
    user.setModifiedDate(modifiedDate);
    UserLocalServiceUtil.updateUser(user);
  }
}

{
  Calendar calendar=Calendar.getInstance();
  Date endDate=CalendarUtil.getLTDate(calendar);
  calendar.add(Calendar.MONTH,-1);
  searchCount(CalendarUtil.getGTDate(calendar),endDate,_USER_COUNT_LAST_24_HRS + _USER_COUNT_LAST_WEEK + _USER_COUNT_LAST_MONTH);
}

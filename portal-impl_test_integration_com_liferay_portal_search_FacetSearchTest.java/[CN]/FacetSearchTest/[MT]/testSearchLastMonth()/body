{
  Calendar calendar=Calendar.getInstance();
  Date endDate=CalendarUtil.getLTDate(calendar);
  calendar.add(Calendar.MONTH,-1);
  searchCount(CalendarUtil.getGTDate(calendar),endDate,_USERS_COUNT_LAST_24_HOURS + _USERS_COUNT_LAST_WEEK + _USERS_COUNT_LAST_MONTH);
}

{
  String channelSystemProperties=GetterUtil.getString(properties.get(ClusterPropsKeys.CHANNEL_SYSTEM_PROPERTIES));
  if (Validator.isNull(channelSystemProperties)) {
    channelSystemProperties=GetterUtil.getString(_props.get(PropsKeys.CLUSTER_LINK_CHANNEL_SYSTEM_PROPERTIES));
  }
  initSystemProperties(channelSystemProperties);
  String autodetectAddress=GetterUtil.getString(properties.get(ClusterPropsKeys.AUTODETECT_ADDRESS));
  if (Validator.isNull(autodetectAddress)) {
    autodetectAddress=GetterUtil.getString(_props.get(PropsKeys.CLUSTER_LINK_AUTODETECT_ADDRESS));
  }
  initBindAddress(autodetectAddress);
}

{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(fileRankId));
  }
  com.liferay.portlet.documentlibrary.model.DLFileRank dlFileRank=getPersistence().remove(fileRankId);
  if (listener != null) {
    listener.onAfterRemove(dlFileRank);
  }
  return dlFileRank;
}

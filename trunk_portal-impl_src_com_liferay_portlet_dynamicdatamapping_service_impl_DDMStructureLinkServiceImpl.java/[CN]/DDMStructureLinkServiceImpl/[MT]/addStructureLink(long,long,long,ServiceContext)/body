{
  DDMPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_STRUCTURE);
  return ddmStructureLinkLocalService.addStructureLink(classNameId,classPK,structureId,serviceContext);
}

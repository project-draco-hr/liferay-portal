{
  Integer type=ShoppingOrderModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ShoppingOrderModelImpl.TABLE_NAME);
  }
  return type;
}

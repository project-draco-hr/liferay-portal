{
  if (isEscapedModel()) {
    return (WorkflowDefinitionLink)this;
  }
 else {
    return (WorkflowDefinitionLink)Proxy.newProxyInstance(WorkflowDefinitionLink.class.getClassLoader(),new Class[]{WorkflowDefinitionLink.class},new AutoEscapeBeanHandler(this));
  }
}

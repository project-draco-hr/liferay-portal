{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  return AssetPublisherUtil.getAssetEntries(preferences,themeDisplay.getLayout(),themeDisplay.getScopeGroupId(),true);
}

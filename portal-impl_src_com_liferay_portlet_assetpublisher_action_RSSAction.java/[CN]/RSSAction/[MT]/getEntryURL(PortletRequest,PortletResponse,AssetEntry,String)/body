{
  if ("viewInPortlet".equals(assetLinkBehavior)) {
    return getEntryURLViewInContext(portletRequest,portletResponse,entry);
  }
 else {
    return getEntryURLAssetPublisher(portletRequest,portletResponse,entry);
  }
}

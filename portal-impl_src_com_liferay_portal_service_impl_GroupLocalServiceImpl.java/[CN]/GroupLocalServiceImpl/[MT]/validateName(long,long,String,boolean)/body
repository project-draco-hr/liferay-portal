{
  if (Validator.isNull(name) || Validator.isNumber(name) || name.contains(StringPool.STAR)|| name.contains(ORGANIZATION_NAME_SUFFIX)) {
    throw new GroupNameException();
  }
  try {
    Group group=groupFinder.findByC_N(companyId,name);
    if ((groupId <= 0) || (group.getGroupId() != groupId)) {
      throw new DuplicateGroupException();
    }
  }
 catch (  NoSuchGroupException nsge) {
  }
  if (site) {
    Company company=companyLocalService.getCompany(companyId);
    if (name.equals(company.getName())) {
      throw new DuplicateGroupException();
    }
  }
}

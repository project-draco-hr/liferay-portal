{
  TreePathUtil.rebuildTree(companyId,GroupConstants.DEFAULT_PARENT_GROUP_ID,StringPool.SLASH,new TreeModelFinder<Group>(){
    @Override public List<Group> findTreeModels(    long previousId,    long companyId,    long parentPrimaryKey,    int size){
      return groupPersistence.findByG_C_P(previousId,companyId,parentPrimaryKey,QueryUtil.ALL_POS,size,new GroupIdComparator(true));
    }
    @Override public void rebuildDependentModelsTreePaths(    long parentPrimaryKey,    String treePath){
    }
    @Override public void reindexTreeModels(    List<TreeModel> treeModels){
    }
  }
);
}

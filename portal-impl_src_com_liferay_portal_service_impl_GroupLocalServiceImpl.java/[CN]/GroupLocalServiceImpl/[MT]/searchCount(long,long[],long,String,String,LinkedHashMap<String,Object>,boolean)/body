{
  String[] names=CustomSQLUtil.keywords(name);
  String realName=getRealName(companyId,name);
  String[] realNames=CustomSQLUtil.keywords(realName);
  String[] descriptions=CustomSQLUtil.keywords(description);
  return groupFinder.countByC_C_PG_N_D(companyId,classNameIds,parentGroupId,names,realNames,descriptions,params,andOperator);
}

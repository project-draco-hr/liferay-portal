{
  if (obc == null) {
    obc=new GroupNameComparator(true);
  }
  String realName=getRealName(companyId,name);
  return groupFinder.findByC_N_D(companyId,name,realName,description,params,andOperator,start,end,obc);
}

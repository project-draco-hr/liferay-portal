{
  Iterator itr=scProductEntryPersistence.findByGroupId(groupId).iterator();
  while (itr.hasNext()) {
    SCProductEntry productEntry=(SCProductEntry)itr.next();
    deleteProductEntry(productEntry);
  }
}

{
  if (SearchEngineUtil.isIndexReadOnly()) {
    return;
  }
  SCProductEntry productEntry=scProductEntryPersistence.fetchByPrimaryKey(productEntryId);
  if (productEntry == null) {
    return;
  }
  reindex(productEntry);
}

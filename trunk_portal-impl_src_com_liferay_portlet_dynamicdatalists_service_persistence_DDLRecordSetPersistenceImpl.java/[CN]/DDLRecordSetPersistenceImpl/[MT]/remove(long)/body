{
  Session session=null;
  try {
    session=openSession();
    DDLRecordSet ddlRecordSet=(DDLRecordSet)session.get(DDLRecordSetImpl.class,Long.valueOf(recordSetId));
    if (ddlRecordSet == null) {
      if (_log.isWarnEnabled()) {
        _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + recordSetId);
      }
      throw new NoSuchRecordSetException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + recordSetId);
    }
    return ddlRecordSetPersistence.remove(ddlRecordSet);
  }
 catch (  NoSuchRecordSetException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

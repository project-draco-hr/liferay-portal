{
  Map<Locale,String> nameMap=new HashMap<Locale,String>();
  nameMap.put(Locale.US,name);
  return JournalStructureLocalServiceUtil.addStructure(TestPropsValues.getUserId(),groupId,structureId,false,null,nameMap,null,xsd,getServiceContext());
}

{
  List<BookmarksEntry> entries=BookmarksEntryLocalServiceUtil.getBookmarksEntriesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<BookmarksEntry>());
  if (ListUtil.isEmpty(entries)) {
    return null;
  }
  return entries.get(0);
}

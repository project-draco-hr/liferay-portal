{
  for (  RecentLayoutRevision recentLayoutRevision : recentLayoutRevisions) {
    if (entityCache.getResult(RecentLayoutRevisionModelImpl.ENTITY_CACHE_ENABLED,RecentLayoutRevisionImpl.class,recentLayoutRevision.getPrimaryKey()) == null) {
      cacheResult(recentLayoutRevision);
    }
 else {
      recentLayoutRevision.resetOriginalValues();
    }
  }
}

{
  int count=_counter.getAndIncrement();
  if (count >= _priorities.size()) {
    throw new IllegalStateException("Can't create more than " + _priorities.size() + " channels.");
  }
  return new ClusterLinkPortalCacheClusterChannel(_destination,_priorities.get(count));
}

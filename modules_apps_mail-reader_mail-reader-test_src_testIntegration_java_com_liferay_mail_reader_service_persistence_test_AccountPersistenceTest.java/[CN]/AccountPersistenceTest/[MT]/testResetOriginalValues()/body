{
  Account newAccount=addAccount();
  _persistence.clearCache();
  Account existingAccount=_persistence.findByPrimaryKey(newAccount.getPrimaryKey());
  Assert.assertEquals(Long.valueOf(existingAccount.getUserId()),ReflectionTestUtil.<Long>invoke(existingAccount,"getOriginalUserId",new Class<?>[0]));
  Assert.assertTrue(Objects.equals(existingAccount.getAddress(),ReflectionTestUtil.invoke(existingAccount,"getOriginalAddress",new Class<?>[0])));
}

{
  boolean found=false;
  String testStagedModelDataHandlerClassName=TestStagedModelDataHandler.class.getName();
  List<StagedModelDataHandler<?>> stagedModelDataHandlers=StagedModelDataHandlerRegistryUtil.getStagedModelDataHandlers();
  for (  StagedModelDataHandler<?> stagedModelDataHandler : stagedModelDataHandlers) {
    Class<? extends StagedModelDataHandler> clazz=stagedModelDataHandler.getClass();
    String className=clazz.getName();
    if (testStagedModelDataHandlerClassName.equals(className)) {
      found=true;
    }
  }
  Assert.assertTrue(found);
}

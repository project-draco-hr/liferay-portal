{
  SyncFilePersistence syncFilePersistence=SyncFileService.getSyncFilePersistence();
  syncFilePersistence.executeRaw("ALTER TABLE `SyncFile` ADD COLUMN userId LONG BEFORE version;");
  syncFilePersistence.executeRaw("ALTER TABLE `SyncFile` ADD COLUMN userName VARCHAR(16777216) " + "BEFORE version;");
}

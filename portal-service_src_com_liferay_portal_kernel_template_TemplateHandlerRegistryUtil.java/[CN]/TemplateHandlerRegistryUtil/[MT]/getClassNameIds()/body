{
  long[] classNameIds=new long[_instance._templateHandlers.size()];
  int i=0;
  for (  Map.Entry<String,TemplateHandler> entry : _instance._templateHandlers.entrySet()) {
    TemplateHandler templateHandler=entry.getValue();
    classNameIds[i++]=PortalUtil.getClassNameId(templateHandler.getClassName());
  }
  return classNameIds;
}

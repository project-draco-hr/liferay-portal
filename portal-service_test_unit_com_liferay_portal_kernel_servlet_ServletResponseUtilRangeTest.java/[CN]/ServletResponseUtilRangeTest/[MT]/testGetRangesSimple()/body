{
  long length=1000;
  String rangeHeader="bytes=0-999";
  setupRange(request,rangeHeader);
  List<Range> ranges=ServletResponseUtil.getRanges(request,response,length);
  Assert.assertEquals(ranges.size(),1);
  assertRange(ranges.get(0),0,999,1000);
}

{
  if (!contains(permissionChecker,article,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),JournalArticle.class.getName(),article.getArticleId(),actionId);
  }
}

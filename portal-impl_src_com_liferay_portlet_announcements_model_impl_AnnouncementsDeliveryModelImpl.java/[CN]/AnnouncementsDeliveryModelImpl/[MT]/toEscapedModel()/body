{
  if (isEscapedModel()) {
    return (AnnouncementsDelivery)this;
  }
 else {
    return (AnnouncementsDelivery)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (AnnouncementsDelivery)this;
  }
 else {
    return (AnnouncementsDelivery)Proxy.newProxyInstance(AnnouncementsDelivery.class.getClassLoader(),new Class[]{AnnouncementsDelivery.class},new AutoEscapeBeanHandler(this));
  }
}

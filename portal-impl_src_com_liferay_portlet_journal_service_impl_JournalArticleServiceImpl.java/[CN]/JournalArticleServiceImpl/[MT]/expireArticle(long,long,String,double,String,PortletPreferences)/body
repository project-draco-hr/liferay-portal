{
  JournalArticlePermission.check(getPermissionChecker(),companyId,groupId,articleId,ActionKeys.EXPIRE);
  JournalArticleLocalServiceUtil.expireArticle(companyId,groupId,articleId,version,articleURL,prefs);
}

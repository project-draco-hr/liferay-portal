{
  JournalArticlePermission.check(getPermissionChecker(),groupId,articleId,ActionKeys.VIEW);
  return JournalArticleLocalServiceUtil.getArticleContent(groupId,articleId,version,languageId,themeDisplay);
}

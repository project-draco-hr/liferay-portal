{
  JournalPermission.check(getPermissionChecker(),groupId,ActionKeys.EXPIRE);
  return journalArticleLocalService.updateStatus(getUserId(),groupId,articleId,version,WorkflowConstants.STATUS_EXPIRED,articleURL,serviceContext);
}

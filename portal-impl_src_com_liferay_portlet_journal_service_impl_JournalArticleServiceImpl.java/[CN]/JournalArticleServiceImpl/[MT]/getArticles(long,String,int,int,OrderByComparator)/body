{
  List<JournalArticle> articles=journalArticleLocalService.getArticles(groupId,articleId,start,end,obc);
  JournalArticlePermission.check(getPermissionChecker(),groupId,articleId,ActionKeys.VIEW);
  return articles;
}

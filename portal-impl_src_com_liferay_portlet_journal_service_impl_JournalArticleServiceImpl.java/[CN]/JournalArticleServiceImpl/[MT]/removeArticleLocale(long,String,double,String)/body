{
  JournalArticle article=journalArticleLocalService.getArticle(groupId,articleId,version);
  JournalArticlePermission.check(getPermissionChecker(),article,ActionKeys.UPDATE);
  return journalArticleLocalService.removeArticleLocale(article,languageId);
}

{
  JournalArticlePermission.check(getPermissionChecker(),companyId,groupId,articleId,ActionKeys.UPDATE);
  return JournalArticleLocalServiceUtil.removeArticleLocale(companyId,groupId,articleId,version,languageId);
}

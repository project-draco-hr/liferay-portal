{
  Iterator itr=JournalArticleUtil.findByCompanyId(companyId).iterator();
  while (itr.hasNext()) {
    JournalArticle article=(JournalArticle)itr.next();
    removeArticleLocale(companyId,article.getGroupId(),article.getArticleId(),article.getVersion(),languageId);
  }
}

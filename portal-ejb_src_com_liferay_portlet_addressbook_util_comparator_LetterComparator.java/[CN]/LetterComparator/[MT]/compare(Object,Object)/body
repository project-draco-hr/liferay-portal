{
  Recipient r1=(Recipient)obj1;
  Recipient r2=(Recipient)obj2;
  String name1=r1.getRecipientName().substring(0,1);
  String name2=r2.getRecipientName().substring(0,1);
  return name1.compareToIgnoreCase(name2);
}

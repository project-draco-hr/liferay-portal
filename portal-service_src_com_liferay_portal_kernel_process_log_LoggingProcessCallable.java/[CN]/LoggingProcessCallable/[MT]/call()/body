{
  try {
    if (_errorLog) {
      System.err.write(_logData);
    }
 else {
      System.out.write(_logData);
    }
  }
 catch (  IOException ioe) {
    _log.error("Failed to output log message : " + new String(_logData),ioe);
  }
  return StringPool.BLANK;
}

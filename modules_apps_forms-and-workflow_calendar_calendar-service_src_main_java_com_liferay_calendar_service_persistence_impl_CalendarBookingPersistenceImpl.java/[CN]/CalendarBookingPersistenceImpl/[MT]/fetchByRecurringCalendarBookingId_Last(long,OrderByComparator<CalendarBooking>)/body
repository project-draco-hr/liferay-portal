{
  int count=countByRecurringCalendarBookingId(recurringCalendarBookingId);
  if (count == 0) {
    return null;
  }
  List<CalendarBooking> list=findByRecurringCalendarBookingId(recurringCalendarBookingId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  String title=StringPool.BLANK;
  if (structure != null) {
    title=LanguageUtil.format(locale,"templates-for-structure-x",structure.getName(locale),false);
  }
 else {
    title=getDefaultViewTemplateTitle(locale);
  }
  return title;
}

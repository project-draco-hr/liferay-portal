{
  if (!_fileEntries.contains(fileEntry.getFileEntryId()) && _isSupportedVideo(fileEntry)) {
    _fileEntries.add(fileEntry.getFileEntryId());
    MessageBusUtil.sendMessage(DestinationNames.DOCUMENT_LIBRARY_VIDEO_PROCESSOR,fileEntry);
  }
}

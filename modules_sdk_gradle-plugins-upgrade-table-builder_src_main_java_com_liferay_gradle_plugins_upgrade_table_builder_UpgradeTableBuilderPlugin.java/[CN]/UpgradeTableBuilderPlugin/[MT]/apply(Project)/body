{
  Configuration upgradeTableBuilderConfiguration=addUpgradeTableBuilderConfiguration(project);
  addBuildUpgradeTableTask(project);
  configureTasksBuildUpgradeTable(project,upgradeTableBuilderConfiguration);
}

{
  Configuration upgradeTableBuilderConfiguration=addConfigurationUpgradeTableBuilder(project);
  addTaskBuildUpgradeTable(project);
  configureTasksBuildUpgradeTable(project,upgradeTableBuilderConfiguration);
}

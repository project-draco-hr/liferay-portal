{
  if (isEscapedModel()) {
    return (RepositoryEntry)this;
  }
 else {
    return (RepositoryEntry)Proxy.newProxyInstance(RepositoryEntry.class.getClassLoader(),new Class[]{RepositoryEntry.class},new AutoEscapeBeanHandler(this));
  }
}

{
  try {
    return DLStoreUtil.getFile(_companyId,CompanyConstants.SYSTEM,filePath);
  }
 catch (  PortalException pe) {
    _log.error(String.format("Unable to find the binary file with path \"%s\" " + "referenced by %s",filePath,getModelInfo()));
    throw pe;
  }
}

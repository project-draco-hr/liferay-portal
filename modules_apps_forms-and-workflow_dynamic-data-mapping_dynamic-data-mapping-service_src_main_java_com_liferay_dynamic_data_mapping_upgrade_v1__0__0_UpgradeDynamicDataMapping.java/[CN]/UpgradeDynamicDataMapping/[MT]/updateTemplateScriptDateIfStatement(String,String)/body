{
  String oldTemplateScriptSB="<#if (" + dateFieldName + "_Data > 0)>";
  String newTemplateScriptSB="<#if (validator.isNotNull(" + dateFieldName + "_Data))>";
  return StringUtil.replace(script,oldTemplateScriptSB,newTemplateScriptSB);
}

{
  String oldTemplateScript="<#if (" + dateFieldName + "_Data > 0)>";
  String newTemplateScript="<#if (validator.isNotNull(" + dateFieldName + "_Data))>";
  return StringUtil.replace(script,oldTemplateScript,newTemplateScript);
}

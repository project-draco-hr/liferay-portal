{
  try {
    _started.await(_stopTimeout,TimeUnit.MILLISECONDS);
  }
 catch (  InterruptedException ie) {
    _logger.log(Logger.LOG_ERROR,String.format("The wait for bundle {0}/{1} being started " + "before destruction has been interrupted.",_bundle.getSymbolicName(),_bundle.getBundleId()),ie);
  }
  _webBundleDeployer.doStop(_bundle);
}

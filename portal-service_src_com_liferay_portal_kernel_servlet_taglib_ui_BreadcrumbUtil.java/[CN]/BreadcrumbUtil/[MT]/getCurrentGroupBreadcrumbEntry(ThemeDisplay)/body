{
  List<BreadcrumbEntry> breadcrumbEntries=new ArrayList<BreadcrumbEntry>();
  _addGroupsBreadcrumbEntries(breadcrumbEntries,themeDisplay,themeDisplay.getLayout().getLayoutSet(),false);
  BreadcrumbEntry breadcrumbEntry=null;
  if (breadcrumbEntries.size() > 0) {
    breadcrumbEntry=breadcrumbEntries.get(0);
  }
  return breadcrumbEntry;
}

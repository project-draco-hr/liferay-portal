{
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(PhoneImpl.TABLE_NAME,PhoneImpl.TABLE_COLUMNS,new PKUpgradeColumnImpl(),new ClassNameUpgradeColumnImpl());
  upgradeTable.updateTable();
  CounterLocalServiceUtil.reset(Phone.class.getName());
  DBUtil.getInstance().executeSQL(_UPGRADE_SCHEMA);
}

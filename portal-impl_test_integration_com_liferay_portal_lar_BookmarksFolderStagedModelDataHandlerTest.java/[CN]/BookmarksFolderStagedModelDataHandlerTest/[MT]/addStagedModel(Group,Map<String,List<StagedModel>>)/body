{
  List<StagedModel> folderList=dependentStagedModels.get(BookmarksFolder.class.getName());
  BookmarksFolder parentFolder=(BookmarksFolder)folderList.get(0);
  return BookmarksTestUtil.addFolder(group.getGroupId(),parentFolder.getFolderId(),"Test Folder");
}

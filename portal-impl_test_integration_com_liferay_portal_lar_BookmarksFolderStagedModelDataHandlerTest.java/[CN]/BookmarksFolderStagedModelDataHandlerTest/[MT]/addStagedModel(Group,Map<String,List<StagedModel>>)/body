{
  List<StagedModel> stagedModels=stagedModelsMap.get(BookmarksFolder.class.getName());
  BookmarksFolder folder=(BookmarksFolder)stagedModels.get(0);
  return BookmarksTestUtil.addFolder(group.getGroupId(),folder.getFolderId(),ServiceTestUtil.randomString());
}

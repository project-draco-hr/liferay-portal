{
  ModuleConfig moduleConfig=getModuleConfig(renderRequest);
  mapping=(ActionMapping)moduleConfig.findActionConfig(getPath(renderRequest));
  PortletAction action=(PortletAction)InstancePool.get(mapping.getType());
  return action.render(mapping,form,portletConfig,renderRequest,renderResponse);
}

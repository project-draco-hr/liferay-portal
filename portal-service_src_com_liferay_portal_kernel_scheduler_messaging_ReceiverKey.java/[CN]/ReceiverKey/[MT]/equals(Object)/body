{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ReceiverKey)) {
    return false;
  }
  ReceiverKey receiverKey=(ReceiverKey)obj;
  if (Validator.equals(_jobName,receiverKey._jobName) && Validator.equals(_groupName,receiverKey._groupName)) {
    return true;
  }
  return false;
}

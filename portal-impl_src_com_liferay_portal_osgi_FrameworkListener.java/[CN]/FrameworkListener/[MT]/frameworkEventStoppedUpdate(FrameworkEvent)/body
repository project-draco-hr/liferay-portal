{
  Bundle bundle=frameworkEvent.getBundle();
  Log log=LogFactoryUtil.getLog(bundle.getSymbolicName());
  if (!log.isInfoEnabled()) {
    return;
  }
  log.info("[STOPPED_UPDATE] " + frameworkEvent.getSource(),frameworkEvent.getThrowable());
}

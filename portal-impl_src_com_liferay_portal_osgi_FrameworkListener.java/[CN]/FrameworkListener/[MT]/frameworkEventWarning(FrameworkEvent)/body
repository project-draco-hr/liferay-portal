{
  Bundle bundle=frameworkEvent.getBundle();
  Log log=LogFactoryUtil.getLog(bundle.getSymbolicName());
  if (!log.isWarnEnabled()) {
    return;
  }
  log.warn("[WARNING] " + frameworkEvent.getSource(),frameworkEvent.getThrowable());
}

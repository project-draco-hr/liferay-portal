{
  Bundle bundle=frameworkEvent.getBundle();
  Log log=LogFactoryUtil.getLog(bundle.getSymbolicName());
  if (log.isErrorEnabled()) {
    log.error("[ERROR] " + frameworkEvent.getSource(),frameworkEvent.getThrowable());
  }
}

{
  Document doc=getUserDocument(companyId,userId,screenName,emailAddress,firstName,middleName,lastName,jobTitle,active,groupIds,organizationIds,roleIds,userGroupIds,expandoBridge);
  SearchEngineUtil.addDocument(companyId,doc);
}

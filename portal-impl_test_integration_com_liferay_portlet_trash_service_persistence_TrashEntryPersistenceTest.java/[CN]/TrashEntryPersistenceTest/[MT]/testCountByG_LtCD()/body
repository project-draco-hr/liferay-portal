{
  try {
    _persistence.countByG_LtCD(RandomTestUtil.nextLong(),RandomTestUtil.nextDate());
    _persistence.countByG_LtCD(0L,RandomTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

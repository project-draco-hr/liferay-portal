{
  try {
    _persistence.countByG_LtCD(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDate());
    _persistence.countByG_LtCD(0L,ServiceTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  DLFileEntryMetadata dlFileEntryMetadata=getDLFileEntryMetadata();
  if (dlFileEntryMetadata == null) {
    return new DDMFormValues(null);
  }
  return StorageEngineUtil.getDDMFormValues(dlFileEntryMetadata.getDDMStorageId());
}

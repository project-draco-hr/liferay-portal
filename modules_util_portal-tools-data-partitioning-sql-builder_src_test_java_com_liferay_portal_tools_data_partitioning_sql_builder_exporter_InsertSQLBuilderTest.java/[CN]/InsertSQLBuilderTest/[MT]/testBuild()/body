{
  String expected="insert into Foo values (a, b, c, d);\n";
  String[] fields={"a","b","c","d"};
  InsertSQLBuilder insertSQLBuilder=new InsertSQLBuilder();
  Assert.assertEquals(expected,insertSQLBuilder.build(fields,"Foo"));
}

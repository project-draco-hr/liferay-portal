{
  List<SocialActivity> activities=socialActivityFinder.findByU_R(userId,userId,QueryUtil.ALL_POS,QueryUtil.ALL_POS);
  for (  SocialActivity activity : activities) {
    socialActivityPersistence.remove(activity);
  }
}

{
  if (PropsValues.SOCIAL_ACTIVITY_SETS_ENABLED) {
    socialActivitySetLocalService.decrementActivityCount(classNameId,classPK);
  }
  socialActivityPersistence.removeByC_C(classNameId,classPK);
  socialActivityCounterLocalService.deleteActivityCounters(classNameId,classPK);
}

{
  List<TagsAssetSoap> soapModels=new ArrayList<TagsAssetSoap>(models.size());
  for (  TagsAsset model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new TagsAssetSoap[soapModels.size()]);
}

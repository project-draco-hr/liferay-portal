{
  addUsers();
  addRequiredSites();
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext();
  UserServiceUtil.addGroupUsers(getRequiredSiteIds()[0],getUserIds(),serviceContext);
  Assert.assertTrue(getPropagateMembershipMethodFlag());
}

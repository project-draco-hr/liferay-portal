{
  addRequiredSites();
  User user=MembershipPolicyTestUtil.addUser(null,null,getRequiredSiteIds(),null);
  UserServiceUtil.unsetGroupUsers(getRequiredSiteIds()[0],new long[]{user.getUserId()},_getServiceContext());
}

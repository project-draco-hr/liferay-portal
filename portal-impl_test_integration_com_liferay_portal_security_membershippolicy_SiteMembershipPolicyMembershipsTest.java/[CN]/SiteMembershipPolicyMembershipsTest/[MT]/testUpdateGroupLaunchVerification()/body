{
  Group group=MembershipPolicyTestUtil.addGroup();
  GroupServiceUtil.updateGroup(group.getGroupId(),group.getParentGroupId(),"TestSiteNewName",group.getDescription(),group.getType(),group.getFriendlyURL(),group.isActive(),_getServiceContext());
  Assert.assertTrue(getVerifyMethodFlag());
}

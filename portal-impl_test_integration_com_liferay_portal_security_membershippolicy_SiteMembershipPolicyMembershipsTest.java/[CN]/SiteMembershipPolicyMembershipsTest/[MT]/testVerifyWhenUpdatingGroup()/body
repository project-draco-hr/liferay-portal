{
  Group group=MembershipPolicyTestUtil.addGroup();
  GroupServiceUtil.updateGroup(group.getGroupId(),group.getParentGroupId(),ServiceTestUtil.randomString(),group.getDescription(),group.getType(),group.getFriendlyURL(),group.isActive(),group.isManualMembership(),ServiceTestUtil.getServiceContext());
  Assert.assertTrue(isVerify());
}

{
  RequestDispatcher requestDispatcher=doGetRequestDispatcher(servletContext,path);
  if (PACLPolicyManager.isActive() && PortalSecurityManagerThreadLocal.isEnabled()) {
    requestDispatcher=new PACLRequestDispatcherWrapper(servletContext,requestDispatcher);
  }
  return requestDispatcher;
}

{
  SyncFile syncFile=SyncFileService.addSyncFile(null,null,null,filePathName,null,null,parentFolderId,repositoryId,SyncFile.STATE_SYNCED,syncAccountId,SyncFile.TYPE_FILE);
  if (typePK == 0) {
    syncFile.setTypePK(syncFile.getSyncFileId());
    SyncFileService.update(syncFile);
  }
  return syncFile;
}

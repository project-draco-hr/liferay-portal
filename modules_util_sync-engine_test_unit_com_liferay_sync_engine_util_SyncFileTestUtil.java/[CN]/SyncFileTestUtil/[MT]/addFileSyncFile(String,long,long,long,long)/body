{
  SyncFile syncFile=SyncFileService.addSyncFile(null,null,null,filePathName,null,null,parentFolderId,repositoryId,syncAccountId,SyncFile.TYPE_FILE);
  if (typePK == 0) {
    typePK=syncFile.getSyncFileId();
  }
  syncFile.setTypePK(typePK);
  SyncFileService.update(syncFile);
  return syncFile;
}

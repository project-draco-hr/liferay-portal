{
  String[] keywords=PropsUtil.getArray(PropsUtil.LAYOUT_FRIENDLY_URL_KEYWORDS);
  for (int i=0; i < keywords.length; i++) {
    if ((LayoutFriendlyURLException.validate(friendlyURL) > -1) || (friendlyURL.indexOf(keywords[i]) != -1)) {
      return false;
    }
  }
  return true;
}

{
  int status=getStatus();
  if (status == 0) {
    return "downloading-xuggler";
  }
 else   if (status == 1) {
    return "copying-xuggler";
  }
 else   if (status == 2) {
    return "completed";
  }
  return "unknown";
}

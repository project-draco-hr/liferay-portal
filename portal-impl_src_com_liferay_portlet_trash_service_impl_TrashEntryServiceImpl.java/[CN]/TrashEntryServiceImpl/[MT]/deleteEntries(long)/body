{
  List<TrashEntry> entries=trashEntryPersistence.findByGroupId(groupId);
  for (  TrashEntry entry : entries) {
    try {
      deleteEntry(entry);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
}

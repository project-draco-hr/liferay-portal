{
  List<TrashEntry> entries=trashEntryLocalService.getEntries(groupId);
  for (  TrashEntry entry : entries) {
    try {
      deleteEntry(entry);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
}

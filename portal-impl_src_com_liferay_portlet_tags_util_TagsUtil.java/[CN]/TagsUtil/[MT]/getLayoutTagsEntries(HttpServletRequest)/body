{
  Set<String> entries=(Set<String>)request.getAttribute(WebKeys.TAGS_LAYOUT_ENTRIES);
  if (entries == null) {
    entries=new HashSet<String>();
    request.setAttribute(WebKeys.TAGS_LAYOUT_ENTRIES,entries);
  }
  return entries;
}

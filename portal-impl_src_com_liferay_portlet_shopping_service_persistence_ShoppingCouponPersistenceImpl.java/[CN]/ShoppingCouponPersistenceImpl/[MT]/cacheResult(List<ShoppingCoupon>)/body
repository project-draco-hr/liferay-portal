{
  for (  ShoppingCoupon shoppingCoupon : shoppingCoupons) {
    if (EntityCacheUtil.getResult(ShoppingCouponModelImpl.ENTITY_CACHE_ENABLED,ShoppingCoupon.class,shoppingCoupon.getPrimaryKey(),this) == null) {
      cacheResult(shoppingCoupon);
    }
  }
}

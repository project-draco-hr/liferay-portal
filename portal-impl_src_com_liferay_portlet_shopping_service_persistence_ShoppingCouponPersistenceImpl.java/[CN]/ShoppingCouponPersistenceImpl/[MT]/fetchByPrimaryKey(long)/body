{
  ShoppingCoupon shoppingCoupon=(ShoppingCoupon)EntityCacheUtil.getResult(ShoppingCouponModelImpl.ENTITY_CACHE_ENABLED,ShoppingCouponImpl.class,couponId,this);
  if (shoppingCoupon == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (shoppingCoupon == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      shoppingCoupon=(ShoppingCoupon)session.get(ShoppingCouponImpl.class,Long.valueOf(couponId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (shoppingCoupon == null)) {
        EntityCacheUtil.putResult(ShoppingCouponModelImpl.ENTITY_CACHE_ENABLED,ShoppingCouponImpl.class,couponId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(shoppingCoupon);
      }
      closeSession(session);
    }
  }
  return shoppingCoupon;
}

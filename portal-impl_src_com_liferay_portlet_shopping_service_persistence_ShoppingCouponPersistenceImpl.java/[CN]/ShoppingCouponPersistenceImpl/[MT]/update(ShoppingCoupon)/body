{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingCoupon shoppingCoupon) method. To improve performance, call update(ShoppingCoupon shoppingCoupon, boolean merge).");
  }
  return update(shoppingCoupon,false);
}

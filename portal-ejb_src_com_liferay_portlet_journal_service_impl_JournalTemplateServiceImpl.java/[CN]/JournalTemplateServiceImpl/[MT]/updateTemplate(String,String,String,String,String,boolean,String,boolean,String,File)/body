{
  User user=getUser();
  JournalTemplatePermission.check(getPermissionChecker(),user.getCompanyId(),templateId,ActionKeys.UPDATE);
  return JournalTemplateLocalServiceUtil.updateTemplate(user.getCompanyId(),templateId,structureId,name,description,xsl,formatXsl,langType,smallImage,smallImageURL,smallFile);
}

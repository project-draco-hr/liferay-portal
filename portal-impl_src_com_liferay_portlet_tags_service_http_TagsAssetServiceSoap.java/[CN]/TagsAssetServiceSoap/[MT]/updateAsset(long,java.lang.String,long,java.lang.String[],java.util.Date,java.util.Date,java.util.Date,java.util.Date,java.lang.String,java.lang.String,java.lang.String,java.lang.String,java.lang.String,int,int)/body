{
  try {
    com.liferay.portlet.tags.model.TagsAsset returnValue=TagsAssetServiceUtil.updateAsset(groupId,className,classPK,entryNames,startDate,endDate,publishDate,expirationDate,mimeType,title,description,summary,url,height,width);
    return com.liferay.portlet.tags.model.TagsAssetSoap.toSoapModel(returnValue);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

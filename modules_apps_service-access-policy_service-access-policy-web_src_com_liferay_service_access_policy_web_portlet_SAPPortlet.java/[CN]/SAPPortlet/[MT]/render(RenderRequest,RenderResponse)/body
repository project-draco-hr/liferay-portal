{
  String mvcPath=ParamUtil.getString(renderRequest,"mvcPath");
  if (mvcPath.equals("/edit_entry.jsp")) {
    Set<Map<String,String>> remoteServiceClassNames=getRemoteServiceClassNames();
    renderRequest.setAttribute(SAPWebKeys.REMOTE_SERVICES_CLASS_NAMES,remoteServiceClassNames);
  }
  super.render(renderRequest,renderResponse);
}

{
  CalendarBooking calendarBooking=_calendarBookingLocalService.fetchCalendarBooking(GetterUtil.getLong(primKey));
  if (calendarBooking == null) {
    return;
  }
  calendarBooking.setModifiedDate(new Date());
  _calendarBookingLocalService.updateCalendarBooking(calendarBooking);
}

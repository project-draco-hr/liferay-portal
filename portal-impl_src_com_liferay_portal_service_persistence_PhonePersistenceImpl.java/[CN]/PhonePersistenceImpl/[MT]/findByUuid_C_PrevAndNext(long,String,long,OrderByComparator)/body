{
  Phone phone=findByPrimaryKey(phoneId);
  Session session=null;
  try {
    session=openSession();
    Phone[] array=new PhoneImpl[3];
    array[0]=getByUuid_C_PrevAndNext(session,phone,uuid,companyId,orderByComparator,true);
    array[1]=phone;
    array[2]=getByUuid_C_PrevAndNext(session,phone,uuid,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Phone result=(Phone)EntityCacheUtil.getResult(PhoneModelImpl.ENTITY_CACHE_ENABLED,Phone.class,phoneId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      Phone phone=(Phone)session.get(PhoneImpl.class,new Long(phoneId));
      cacheResult(phone);
      return phone;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (Phone)result;
  }
}

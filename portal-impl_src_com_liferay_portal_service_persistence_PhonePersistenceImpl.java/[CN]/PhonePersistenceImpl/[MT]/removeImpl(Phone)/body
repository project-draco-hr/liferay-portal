{
  phone=toUnwrappedModel(phone);
  Session session=null;
  try {
    session=openSession();
    if (phone.isCachedModel()) {
      phone=(Phone)session.get(PhoneImpl.class,phone.getPrimaryKeyObj());
    }
    session.delete(phone);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(phone);
  return phone;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Phone phone) method. To improve performance, call update(Phone phone, boolean merge).");
  }
  return update(phone,false);
}

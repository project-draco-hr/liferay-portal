{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof UserGroupRoleCacheModel)) {
    return false;
  }
  UserGroupRoleCacheModel userGroupRoleCacheModel=(UserGroupRoleCacheModel)obj;
  if (userGroupRolePK.equals(userGroupRoleCacheModel.userGroupRolePK) && (mvccVersion == userGroupRoleCacheModel.mvccVersion)) {
    return true;
  }
  return false;
}

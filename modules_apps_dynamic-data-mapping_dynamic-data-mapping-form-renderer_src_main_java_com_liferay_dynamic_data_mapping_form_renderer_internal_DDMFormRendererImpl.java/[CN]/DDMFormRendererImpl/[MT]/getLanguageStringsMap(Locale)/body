{
  Map<String,String> stringsMap=new HashMap<>();
  ResourceBundle portalResourceBundle=ResourceBundleUtil.getBundle("content.Language",locale,PortalClassLoaderUtil.getClassLoader());
  List<ResourceBundle> resourceBundles=new ArrayList<>();
  resourceBundles.add(portalResourceBundle);
  collectResourceBundles(getClass(),resourceBundles,locale);
  ResourceBundle[] resourceBundlesArray=resourceBundles.toArray(new ResourceBundle[resourceBundles.size()]);
  ResourceBundle resourceBundle=new AggregateResourceBundle(resourceBundlesArray);
  stringsMap.put("next",LanguageUtil.get(resourceBundle,"next"));
  stringsMap.put("previous",LanguageUtil.get(resourceBundle,"previous"));
  return stringsMap;
}

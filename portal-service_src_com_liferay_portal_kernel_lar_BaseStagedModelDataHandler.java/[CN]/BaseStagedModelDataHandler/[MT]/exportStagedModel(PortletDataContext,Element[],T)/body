{
  String path=StagedModelPathUtil.getPath(stagedModel);
  if (portletDataContext.isPathProcessed(path)) {
    return;
  }
  try {
    doExportStagedModel(portletDataContext,elements,(T)stagedModel.clone());
  }
 catch (  Exception e) {
    throw new PortletDataException(e);
  }
}

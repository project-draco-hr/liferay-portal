{
  if (obj == null) {
    return -1;
  }
  AnnouncementEntryImpl announcementEntry=(AnnouncementEntryImpl)obj;
  int value=0;
  if (getPriority() < announcementEntry.getPriority()) {
    value=-1;
  }
 else   if (getPriority() > announcementEntry.getPriority()) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  value=DateUtil.compareTo(getModifiedDate(),announcementEntry.getModifiedDate());
  if (value != 0) {
    return value;
  }
  return 0;
}

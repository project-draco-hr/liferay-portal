{
  String keywords=GetterUtil.getString(Http.getParameter(url,"keywords"));
  int startPage=GetterUtil.getInteger(Http.getParameter(url,"p"),1);
  int itemsPerPage=GetterUtil.getInteger(Http.getParameter(url,"c"),SearchContainer.DEFAULT_DELTA);
  return search(req,keywords,startPage,itemsPerPage);
}

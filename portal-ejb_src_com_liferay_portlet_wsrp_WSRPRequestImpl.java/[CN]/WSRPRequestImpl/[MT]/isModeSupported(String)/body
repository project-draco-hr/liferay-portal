{
  if (wsrpMode == null)   throw new IllegalArgumentException("A mode must not be null");
  return _portletRequest.isPortletModeAllowed(Modes.getJsrPortletModeFromWsrpMode(Modes.fromString(wsrpMode)));
}

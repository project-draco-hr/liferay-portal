{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ShardCacheModel)) {
    return false;
  }
  ShardCacheModel shardCacheModel=(ShardCacheModel)obj;
  if ((shardId == shardCacheModel.shardId) && (mvccVersion == shardCacheModel.mvccVersion)) {
    return true;
  }
  return false;
}

{
  ModelListener listener=null;
  if (Validator.isNotNull(_LISTENER)) {
    try {
      listener=(ModelListener)Class.forName(_LISTENER).newInstance();
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(nodeId));
  }
  com.liferay.portlet.wiki.model.WikiNode wikiNode=getPersistence().remove(nodeId);
  if (listener != null) {
    listener.onAfterRemove(wikiNode);
  }
  return wikiNode;
}

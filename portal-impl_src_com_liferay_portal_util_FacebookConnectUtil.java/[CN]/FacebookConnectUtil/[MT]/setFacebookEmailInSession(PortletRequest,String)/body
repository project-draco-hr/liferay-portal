{
  HttpServletRequest request=PortalUtil.getOriginalServletRequest(((LiferayPortletRequest)portletRequest).getHttpServletRequest());
  request.getSession().setAttribute(WebKeys.FACEBOOK_USER_EMAIL_ADDRESS,email);
}

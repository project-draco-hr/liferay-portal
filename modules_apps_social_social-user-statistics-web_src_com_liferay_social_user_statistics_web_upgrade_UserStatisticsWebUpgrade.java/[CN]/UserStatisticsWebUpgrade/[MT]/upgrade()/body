{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradePortletId());
  _releaseLocalService.updateRelease("com.liferay.social.user.statistics.web",upgradeProcesses,1,1,false);
}

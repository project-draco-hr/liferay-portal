{
  if (getTopicId().equals(Company.SYSTEM)) {
    return null;
  }
  MBCategory category=null;
  try {
    category=MBCategoryLocalServiceUtil.getCategory(getCategoryId());
  }
 catch (  Exception e) {
    category=new MBCategory();
    _log.error(e);
  }
  return category;
}

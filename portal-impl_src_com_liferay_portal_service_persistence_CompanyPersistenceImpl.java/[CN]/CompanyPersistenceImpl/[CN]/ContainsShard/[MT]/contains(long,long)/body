{
  List<Integer> results=_mappingSqlQuery.execute(new Object[]{new Long(companyId),new Long(shardId)});
  if (results.size() > 0) {
    Integer count=results.get(0);
    if (count.intValue() > 0) {
      return true;
    }
  }
  return false;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Company company) method. To improve performance, call update(Company company, boolean merge).");
  }
  return update(company,false);
}

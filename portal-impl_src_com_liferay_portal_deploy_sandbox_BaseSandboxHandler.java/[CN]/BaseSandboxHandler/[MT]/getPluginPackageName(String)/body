{
  String pluginPackageName=dirName.substring(0,dirName.length() - 6);
  pluginPackageName=StringUtil.replace(pluginPackageName,new String[]{StringPool.UNDERLINE,StringPool.MINUS},new String[]{StringPool.SPACE,StringPool.SPACE});
  return StringUtil.upperCaseFirstLetter(pluginPackageName);
}

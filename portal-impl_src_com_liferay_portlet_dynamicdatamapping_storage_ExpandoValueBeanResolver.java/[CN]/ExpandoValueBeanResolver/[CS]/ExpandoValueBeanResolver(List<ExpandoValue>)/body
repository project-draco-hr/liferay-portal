{
  _expandoValueMap=new HashMap<String,ExpandoValue>();
  try {
    for (    ExpandoValue expandoValue : expandoValues) {
      _expandoValueMap.put(expandoValue.getColumn().getName(),expandoValue);
    }
  }
 catch (  Exception e) {
    _log.error(e.getMessage(),e);
  }
}

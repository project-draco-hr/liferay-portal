{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  Address address=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  if (address != null) {
    AddressLocalServiceUtil.deleteAddress(address);
  }
}

{
  if (!SPIUtil.isSPI()) {
    return;
  }
  Set<String> searchEngineIds=SearchEngineHelperUtil.getSearchEngineIds();
  for (  String searchEngineId : searchEngineIds) {
    String destinationName=SearchEngineHelperUtil.getSearchWriterDestinationName(searchEngineId);
    Destination destination=MessageBusUtil.getDestination(destinationName);
    destination.unregisterMessageListeners();
    SPI spi=SPIUtil.getSPI();
    destination.register(new IntrabandBridgeMessageListener(spi.getRegistrationReference()));
  }
}

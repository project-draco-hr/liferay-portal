{
  List<Resource> resources=new ArrayList<>();
  resources.add(DDMWebDavUtil.toResource(webDAVRequest,DDMWebDavUtil.TYPE_STRUCTURES,getRootPath(),true));
  resources.add(DDMWebDavUtil.toResource(webDAVRequest,DDMWebDavUtil.TYPE_TEMPLATES,getRootPath(),true));
  return resources;
}

{
  IGDisplayContextFactory igDisplayContextFactory=null;
  if (_bundleContext != null) {
    igDisplayContextFactory=_bundleContext.getService(serviceReference);
  }
  _igDisplayContextFactories.put(serviceReference,igDisplayContextFactory);
}

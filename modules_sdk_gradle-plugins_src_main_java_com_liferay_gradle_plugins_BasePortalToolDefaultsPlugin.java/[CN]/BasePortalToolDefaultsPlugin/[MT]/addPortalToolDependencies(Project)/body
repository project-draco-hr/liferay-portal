{
  String name=getPortalToolName();
  String version=GradleUtil.getProperty(project,name + ".version",getPortalToolVersion());
  if (Validator.isNotNull(version)) {
    GradleUtil.addDependency(project,getPortalToolConfigurationName(),getPortalToolGroup(),name,version);
  }
}

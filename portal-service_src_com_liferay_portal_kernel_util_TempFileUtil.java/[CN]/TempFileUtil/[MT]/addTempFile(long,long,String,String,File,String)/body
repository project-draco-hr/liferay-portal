{
  Folder folder=addTempFolder(groupId,userId,tempFolderName);
  return PortletFileRepositoryUtil.addPortletFileEntry(groupId,userId,StringPool.BLANK,0,PortletKeys.DOCUMENT_LIBRARY,folder.getFolderId(),file,fileName,mimeType,false);
}

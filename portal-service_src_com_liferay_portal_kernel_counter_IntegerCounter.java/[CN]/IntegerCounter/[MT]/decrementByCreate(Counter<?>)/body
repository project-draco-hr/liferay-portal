{
  if (delta.getValue() instanceof Integer) {
    return decrementByCreate((Integer)delta.getValue());
  }
  throw new IllegalArgumentException("Invalid type " + delta.getValue().getClass().getName() + ". Expected type is java.lang.Integer.");
}

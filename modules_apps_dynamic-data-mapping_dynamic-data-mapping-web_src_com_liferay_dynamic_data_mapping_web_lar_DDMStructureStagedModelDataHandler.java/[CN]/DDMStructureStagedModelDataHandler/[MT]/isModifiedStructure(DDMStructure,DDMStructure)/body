{
  if (DateUtil.compareTo(structure.getModifiedDate(),existingStructure.getModifiedDate()) > 0) {
    return true;
  }
  if (!Validator.equals(structure.getNameMap(),existingStructure.getNameMap())) {
    return true;
  }
  if (!Validator.equals(structure.getDescriptionMap(),existingStructure.getDescriptionMap())) {
    return true;
  }
  if (!Validator.equals(structure.getDefinition(),existingStructure.getDefinition())) {
    return true;
  }
  if (!Validator.equals(structure.getStorageType(),existingStructure.getStorageType())) {
    return true;
  }
  if (!Validator.equals(structure.getType(),existingStructure.getType())) {
    return true;
  }
  return false;
}

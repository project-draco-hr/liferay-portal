{
  if (_textObj == null) {
    try {
      Hook hook=new DLHook();
      _textObj=hook.getImageAsBytes(this);
    }
 catch (    Exception e) {
      _log.error("Error reading image " + getImageId(),e);
    }
  }
  return _textObj;
}

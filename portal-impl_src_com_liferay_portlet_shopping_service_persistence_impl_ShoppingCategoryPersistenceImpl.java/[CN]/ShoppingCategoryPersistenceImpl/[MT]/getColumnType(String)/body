{
  Integer type=ShoppingCategoryModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ShoppingCategoryModelImpl.TABLE_NAME);
  }
  return type;
}

{
  ClassLoader[] classLoaders=new ClassLoader[servletContextNames.length];
  for (int i=0; i < servletContextNames.length; i++) {
    classLoaders[i]=ClassLoaderPool.getClassLoader(servletContextNames[i]);
  }
  return classLoaders;
}

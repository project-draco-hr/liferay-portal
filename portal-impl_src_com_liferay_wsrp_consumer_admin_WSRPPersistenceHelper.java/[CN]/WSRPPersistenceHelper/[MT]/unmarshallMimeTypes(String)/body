{
  try {
    ByteArrayInputStream inputStream=new ByteArrayInputStream(mimeString.getBytes());
    JAXBElement<MimeTypes> rootElement=(JAXBElement<MimeTypes>)_unmarshaller.unmarshal(inputStream);
    return rootElement.getValue();
  }
 catch (  JAXBException ex) {
    throw new WSRPConsumerException(ex.getMessage(),ex);
  }
}

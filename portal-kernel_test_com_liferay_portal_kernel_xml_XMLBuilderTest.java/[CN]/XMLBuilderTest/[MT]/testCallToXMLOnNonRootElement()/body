{
  BuilderElement rootElement=XMLBuilder.createRootElement("root");
  BuilderElement childElement=XMLBuilder.addElement(rootElement,"child");
  try {
    childElement.toXMLString();
    fail();
  }
 catch (  IllegalStateException ise) {
  }
}

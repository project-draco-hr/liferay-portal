{
  Assert.assertSame(_mockDataSource,dataSource);
  Assert.assertEquals("DELETE FROM " + _mappingTableName + " WHERE "+ _leftColumnName+ " = ? AND "+ _rightColumnName+ " = ?",sql);
  Assert.assertArrayEquals(new int[]{java.sql.Types.BIGINT,java.sql.Types.BIGINT},types);
}

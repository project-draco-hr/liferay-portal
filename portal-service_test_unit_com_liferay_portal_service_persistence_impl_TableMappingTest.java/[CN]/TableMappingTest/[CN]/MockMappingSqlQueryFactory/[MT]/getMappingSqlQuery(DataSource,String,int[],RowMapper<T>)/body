{
  int count=_counter++;
  if (count == 0) {
    return (MappingSqlQuery<T>)new MockGetRightPrimaryKeysByLeftPrimaryKeyMappingSqlQuery(dataSource,sql,types,RowMapper.ID);
  }
  if (count == 1) {
    return (MappingSqlQuery<T>)new MockGetLeftPrimaryKeysByRightPrimaryKeyMappingSqlQuery(dataSource,sql,types,RowMapper.ID);
  }
  return null;
}

{
  Assert.assertSame(_mockDataSource,dataSource);
  Assert.assertEquals("SELECT " + _leftColumnName + " FROM "+ _mappingTableName+ " WHERE "+ _rightColumnName+ "=?",sql);
  Assert.assertArrayEquals(new int[]{java.sql.Types.BIGINT},types);
  Assert.assertSame(RowMapper.PRIMARY_KEY,rowMapper);
}

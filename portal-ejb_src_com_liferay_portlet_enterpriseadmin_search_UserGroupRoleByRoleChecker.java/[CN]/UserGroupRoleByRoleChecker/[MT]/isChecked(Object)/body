{
  Role role=(Role)obj;
  try {
    return UserGroupRoleLocalServiceUtil.hasUserGroupRole(_user.getUserId(),role.getRoleId(),_group.getGroupId());
  }
 catch (  Exception e) {
    _log.error(e);
    return false;
  }
}

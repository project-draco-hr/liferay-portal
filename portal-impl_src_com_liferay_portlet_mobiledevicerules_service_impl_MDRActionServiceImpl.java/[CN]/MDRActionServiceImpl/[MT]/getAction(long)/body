{
  MDRAction action=mdrActionLocalService.getMDRAction(actionId);
  MDRRuleGroupInstancePermissionUtil.check(getPermissionChecker(),action.getGroupId(),action.getRuleGroupInstanceId(),ActionKeys.VIEW);
  return action;
}

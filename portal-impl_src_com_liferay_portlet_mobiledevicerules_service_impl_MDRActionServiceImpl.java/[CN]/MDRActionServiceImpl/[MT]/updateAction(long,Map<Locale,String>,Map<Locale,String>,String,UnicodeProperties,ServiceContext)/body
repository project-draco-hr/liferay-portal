{
  MDRAction action=mdrActionLocalService.getMDRAction(actionId);
  MDRRuleGroupInstancePermissionUtil.check(getPermissionChecker(),action.getGroupId(),action.getRuleGroupInstanceId(),ActionKeys.UPDATE);
  return mdrActionLocalService.updateAction(actionId,nameMap,descriptionMap,type,typeSettingsProperties,serviceContext);
}

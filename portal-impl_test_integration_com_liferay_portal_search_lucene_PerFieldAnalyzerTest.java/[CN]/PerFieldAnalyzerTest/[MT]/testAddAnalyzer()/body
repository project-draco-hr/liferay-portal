{
  try {
    String fieldName="testFieldName";
    Analyzer analyzer=mock(Analyzer.class);
    Fieldable fieldable=mock(Fieldable.class);
    when(analyzer.getPositionIncrementGap(fieldName)).thenReturn(1);
    when(analyzer.getOffsetGap(Mockito.any(Fieldable.class))).thenReturn(1);
    when(fieldable.name()).thenReturn(fieldName);
    _analyzer.addAnalyzer(fieldName,analyzer);
    int positionIncrementGap=_analyzer.getPositionIncrementGap(fieldName);
    int offsetGap=_analyzer.getOffsetGap(fieldable);
    Assert.assertEquals(analyzer.getPositionIncrementGap(fieldName),positionIncrementGap);
    Assert.assertEquals(analyzer.getOffsetGap(fieldable),offsetGap);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

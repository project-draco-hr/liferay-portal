{
  if (!contains(permissionChecker,folder,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),JournalFolder.class.getName(),folder.getFolderId(),actionId);
  }
}

{
  DLLocalService dlLocalService=DLLocalServiceFactory.getService();
  dlLocalService.updateFile(companyId,portletId,groupId,repositoryId,fileName,versionNumber,sourceFileName,properties,tagsEntries,is);
}

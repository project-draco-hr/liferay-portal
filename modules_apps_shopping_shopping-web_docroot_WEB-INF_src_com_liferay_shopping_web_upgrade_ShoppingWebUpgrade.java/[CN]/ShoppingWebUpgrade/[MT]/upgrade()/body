{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradeAdminPortlets());
  _releaseLocalService.updateRelease("com.liferay.shopping.web",upgradeProcesses,1,1,false);
}

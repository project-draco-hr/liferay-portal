{
  Boolean valid=null;
  try {
    valid=(Boolean)PortalClassInvoker.invoke(true,_IS_VALID_METHOD_KEY,emailAddress);
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn(e);
    }
  }
  if (valid == null) {
    return false;
  }
 else {
    return valid.booleanValue();
  }
}

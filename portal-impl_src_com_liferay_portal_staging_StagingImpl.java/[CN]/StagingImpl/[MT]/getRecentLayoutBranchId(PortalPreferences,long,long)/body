{
  try {
    return getRecentLayoutAttribute(portalPreferences,layoutSetBranchId,plid,getRecentLayoutBranchIdKey(layoutSetBranchId,plid));
  }
 catch (  JSONException je) {
    _log.warn("Staging preferences are not in JSON format");
  }
  return 0;
}

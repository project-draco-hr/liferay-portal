{
  long userId=PortalUtil.getUserId(portletRequest);
  Map<String,String[]> parameterMap=getStagingParameters(portletRequest);
  LayoutLocalServiceUtil.publishPortletInBackground(userId,portletId,sourcePlid,targetPlid,sourceGroupId,targetGroupId,portletId,parameterMap,null,null);
}

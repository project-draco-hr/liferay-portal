{
  ClassName className=ClassNameServiceHttp.fetchClassName(httpPrincipal,String.valueOf(group.getClassNameId()));
  if (Validator.equals(className.getClassName(),Company.class.getName())) {
    return true;
  }
  return false;
}

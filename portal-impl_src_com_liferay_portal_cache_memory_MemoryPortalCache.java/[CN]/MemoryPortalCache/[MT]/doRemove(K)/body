{
  V value=_concurrentMap.remove(key);
  if (value != null) {
    for (    CacheListener<K,V> cacheListener : _cacheListeners) {
      cacheListener.notifyEntryRemoved(this,key,value,DEFAULT_TIME_TO_LIVE);
    }
  }
}

{
  try {
    portletURL.setWindowState(windowState);
  }
 catch (  WindowStateException wse) {
    if (_log.isWarnEnabled()) {
      _log.warn("Unable to set notification's URL window state to " + windowState,wse);
    }
  }
}

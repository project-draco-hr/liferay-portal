{
  if (!isStagingGroup(folder.getGroupId())) {
    dlSyncLocalService.addSync(folder.getFolderId(),folder.getCompanyId(),folder.getRepositoryId(),folder.getParentFolderId(),DLSyncConstants.TYPE_FOLDER,folder.getName(),"-1");
  }
}

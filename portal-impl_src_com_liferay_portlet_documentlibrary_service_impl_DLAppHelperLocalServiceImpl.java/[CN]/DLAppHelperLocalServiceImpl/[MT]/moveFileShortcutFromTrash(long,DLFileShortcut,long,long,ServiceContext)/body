{
  if (dlFileShortcut.isInTrash()) {
    restoreFileShortcutFromTrash(userId,dlFileShortcut);
  }
  return dlAppService.updateFileShortcut(dlFileShortcut.getFileShortcutId(),newFolderId,toFileEntryId,serviceContext);
}

{
  if (isEscapedModel()) {
    return (BookmarksFolder)this;
  }
 else {
    return (BookmarksFolder)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

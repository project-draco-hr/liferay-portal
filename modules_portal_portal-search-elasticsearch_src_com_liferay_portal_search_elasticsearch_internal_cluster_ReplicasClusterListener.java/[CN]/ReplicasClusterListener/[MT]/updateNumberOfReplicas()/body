{
  if (!_replicasClusterContext.isElasticsearchEmbeddedCluster()) {
    return;
  }
  try {
    ReplicasManager replicasManager=_replicasClusterContext.getReplicasManager();
    replicasManager.updateNumberOfReplicas(getNumberOfReplicas(),_replicasClusterContext.getElasticsearchTargetIndexNames());
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn("Cannot update indices for number of replicas",e);
    }
  }
}

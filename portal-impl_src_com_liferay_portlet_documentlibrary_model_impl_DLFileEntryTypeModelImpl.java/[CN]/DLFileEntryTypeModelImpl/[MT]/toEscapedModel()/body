{
  if (isEscapedModel()) {
    return (DLFileEntryType)this;
  }
 else {
    return (DLFileEntryType)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

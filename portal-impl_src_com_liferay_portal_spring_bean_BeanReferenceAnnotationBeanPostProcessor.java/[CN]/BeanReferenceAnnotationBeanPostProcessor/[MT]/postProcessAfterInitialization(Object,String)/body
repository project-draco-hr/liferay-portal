{
  if (bean instanceof ServiceBeanIdentifier) {
    ((ServiceBeanIdentifier)bean).setIdentifier(beanName);
  }
 else   if (beanName.endsWith("Service")) {
    _log.error("Service bean " + beanName + " does not implement "+ ServiceBeanIdentifier.class.getName()+ " interface. This "+ "will make it unable to be invoked across cluster.");
  }
  return bean;
}

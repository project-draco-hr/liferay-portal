{
  liferaySelenium.assertElementPresent(locator);
  if (liferaySelenium.isNotPartialText(locator,pattern)) {
    String text=liferaySelenium.getText(locator);
    BaseTestCase.fail(text + " does not contain " + pattern+ " at "+ locator);
  }
}

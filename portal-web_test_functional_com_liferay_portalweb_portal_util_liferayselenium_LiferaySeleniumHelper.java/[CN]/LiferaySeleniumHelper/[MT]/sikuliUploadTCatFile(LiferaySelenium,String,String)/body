{
  String tcatAdminFile=TestPropsValues.TCAT_ADMIN_REPOSITORY + "/" + value;
  if (OSDetector.isWindows()) {
    tcatAdminFile=tcatAdminFile.replace("/","\\");
  }
  sikuliType(liferaySelenium,image,tcatAdminFile);
  _screen.type(Key.ENTER);
}

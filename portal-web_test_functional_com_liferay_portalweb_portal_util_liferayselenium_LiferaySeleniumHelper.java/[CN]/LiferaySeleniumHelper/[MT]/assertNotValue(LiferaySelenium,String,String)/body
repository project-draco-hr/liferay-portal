{
  liferaySelenium.assertElementPresent(locator);
  if (liferaySelenium.isValue(locator,pattern)) {
    String value=liferaySelenium.getValue(locator);
    BaseTestCase.fail("Pattern " + pattern + " matches "+ value+ " at "+ locator);
  }
}

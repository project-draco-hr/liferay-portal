{
  _screenshotErrorCount++;
  File file=new File(liferaySelenium.getProjectDir() + "portal-web/test-results/" + "functional/screenshots/"+ "ScreenshotBeforeAction"+ _screenshotErrorCount+ ".jpg");
  file.mkdirs();
  Robot robot=new Robot();
  Toolkit toolkit=Toolkit.getDefaultToolkit();
  Rectangle rectangle=new Rectangle(toolkit.getScreenSize());
  BufferedImage bufferedImage=robot.createScreenCapture(rectangle);
  ImageIO.write(bufferedImage,"jpg",file);
}

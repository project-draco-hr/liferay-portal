{
  try {
    registerRepositoryRegistryPlugin(_liferayRepositoryRegistryPlugin);
  }
 catch (  PortalException pe) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to register " + LiferayRepositoryRegistryPlugin.class.getName(),pe);
    }
  }
}

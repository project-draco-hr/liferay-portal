{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof MBean)) {
    return false;
  }
  MBean mBean=(MBean)obj;
  if (Validator.equals(_domainName,mBean._domainName) && Validator.equals(_mBeanName,mBean._mBeanName)) {
    return true;
  }
  return false;
}

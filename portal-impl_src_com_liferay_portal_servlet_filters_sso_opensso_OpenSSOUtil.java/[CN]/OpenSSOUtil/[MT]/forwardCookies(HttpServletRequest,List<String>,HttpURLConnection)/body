{
  StringBuffer cookies=new StringBuffer();
  for (  String cName : cookieNames) {
    String cValue=CookieUtil.get(req,cName);
    cookies.append(cName).append('=').append(cValue).append(';');
  }
  if (cookies.length() > 0) {
    conn.setRequestProperty("Cookie",cookies.toString());
  }
}

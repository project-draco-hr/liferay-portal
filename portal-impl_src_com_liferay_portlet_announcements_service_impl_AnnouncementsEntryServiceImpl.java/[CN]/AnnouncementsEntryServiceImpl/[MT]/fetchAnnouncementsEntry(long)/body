{
  AnnouncementsEntry entry=announcementsEntryLocalService.fetchAnnouncementsEntry(entryId);
  if (entry != null) {
    AnnouncementsEntryPermission.check(getPermissionChecker(),entry,ActionKeys.VIEW);
  }
  return entry;
}

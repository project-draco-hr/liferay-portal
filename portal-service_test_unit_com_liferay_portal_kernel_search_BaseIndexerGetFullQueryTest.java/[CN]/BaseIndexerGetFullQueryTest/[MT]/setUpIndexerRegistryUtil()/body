{
  mockStatic(IndexerRegistryUtil.class,Mockito.CALLS_REAL_METHODS);
  List<Indexer> indexers=Arrays.<Indexer>asList(new DLFileEntryIndexer(),new MBMessageIndexer());
  PowerMockito.when(IndexerRegistryUtil.class,"getIndexers").thenReturn(indexers);
}

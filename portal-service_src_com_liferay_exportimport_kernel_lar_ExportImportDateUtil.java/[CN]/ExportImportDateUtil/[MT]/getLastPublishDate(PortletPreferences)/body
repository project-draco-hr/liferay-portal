{
  long lastPublishDate=GetterUtil.getLong(jxPortletPreferences.getValue(_LAST_PUBLISH_DATE,StringPool.BLANK));
  if (lastPublishDate == 0) {
    return null;
  }
  return new Date(lastPublishDate);
}

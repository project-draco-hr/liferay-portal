{
  Integer type=CalendarNotificationTemplateModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ CalendarNotificationTemplateModelImpl.TABLE_NAME);
  }
  return type;
}

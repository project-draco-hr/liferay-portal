{
  try {
    String plid="PRI.3.1";
    long parentFolderId=-1;
    String name="Test Folder";
    String description="This is a test folder.";
    String[] communityPermissions=new String[0];
    String[] guestPermissions=new String[0];
    BookmarksFolderServiceUtil.addFolder(plid,parentFolderId,name,description,communityPermissions,guestPermissions);
  }
 catch (  Exception e) {
    fail(e);
  }
}

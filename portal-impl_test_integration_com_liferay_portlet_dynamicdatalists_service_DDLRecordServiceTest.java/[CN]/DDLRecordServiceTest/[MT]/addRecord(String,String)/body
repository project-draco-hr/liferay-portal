{
  Fields fields=new Fields();
  Field nameField=new Field(recordSet.getDDMStructureId(),"name",name);
  fields.put(nameField);
  Field descriptionField=new Field(recordSet.getDDMStructureId(),"description",description);
  fields.put(descriptionField);
  addRecord(recordSet.getRecordSetId(),fields);
}

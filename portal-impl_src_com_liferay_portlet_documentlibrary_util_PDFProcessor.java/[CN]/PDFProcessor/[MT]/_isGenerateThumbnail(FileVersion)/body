{
  long companyId=fileVersion.getCompanyId();
  String thumbnailFilePath=getThumbnailFilePath(fileVersion);
  if (PropsValues.DL_FILE_ENTRY_THUMBNAIL_ENABLED && !DLStoreUtil.hasFile(companyId,REPOSITORY_ID,thumbnailFilePath)) {
    return true;
  }
 else {
    return false;
  }
}

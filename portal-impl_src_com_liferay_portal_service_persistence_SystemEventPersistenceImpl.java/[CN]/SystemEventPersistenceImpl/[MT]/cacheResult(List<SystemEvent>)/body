{
  for (  SystemEvent systemEvent : systemEvents) {
    if (EntityCacheUtil.getResult(SystemEventModelImpl.ENTITY_CACHE_ENABLED,SystemEventImpl.class,systemEvent.getPrimaryKey()) == null) {
      cacheResult(systemEvent);
    }
 else {
      systemEvent.resetOriginalValues();
    }
  }
}

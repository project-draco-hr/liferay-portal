{
  try {
    CalEventServiceUtil.deleteEvent(eventId,extCalServerUrl,extCalId,extCalUserId,extCalPassword,extCalSessionId);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

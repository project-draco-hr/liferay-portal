{
  UpgradePortletId upgradePortletId=new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"125",UsersAdminPortletKeys.USERS_ADMIN}};
    }
  }
;
  _releaseLocalService.updateRelease("com.liferay.users.admin.web",Collections.<UpgradeProcess>singletonList(upgradePortletId),1,1,false);
}

{
  try {
    Thread thread=new Thread(){
      @Override public ClassLoader getContextClassLoader(){
        return super.getContextClassLoader();
      }
      @Override public void run(){
      }
    }
;
    thread.start();
    Assert.fail();
  }
 catch (  SecurityException se) {
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PollsQuestion pollsQuestion) method. To improve performance, call update(PollsQuestion pollsQuestion, boolean merge).");
  }
  return update(pollsQuestion,false);
}

{
  for (  PollsQuestion pollsQuestion : pollsQuestions) {
    if (EntityCacheUtil.getResult(PollsQuestionModelImpl.ENTITY_CACHE_ENABLED,PollsQuestion.class,pollsQuestion.getPrimaryKey(),this) == null) {
      cacheResult(pollsQuestion);
    }
  }
}

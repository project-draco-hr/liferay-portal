{
  if (sql == null) {
    return sql;
  }
  if (_vendorMySQL) {
    DB db=DBFactoryUtil.getDB();
    if (!db.isSupportsStringCaseSensitiveQuery()) {
      sql=_removeLower(sql);
    }
  }
  return sql;
}

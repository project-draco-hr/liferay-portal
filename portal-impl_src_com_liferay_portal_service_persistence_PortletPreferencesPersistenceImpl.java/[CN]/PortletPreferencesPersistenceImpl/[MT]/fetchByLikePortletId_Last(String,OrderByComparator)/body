{
  int count=countByLikePortletId(portletId);
  List<PortletPreferences> list=findByLikePortletId(portletId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

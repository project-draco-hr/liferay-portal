{
  Iterator<PortletPreferences> itr=findByO_O_P(ownerId,ownerType,plid).iterator();
  while (itr.hasNext()) {
    PortletPreferences portletPreferences=itr.next();
    remove(portletPreferences);
  }
}

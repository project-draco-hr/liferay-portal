{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PortletPreferences portletPreferences) method. To improve performance, call update(PortletPreferences portletPreferences, boolean merge).");
  }
  return update(portletPreferences,false);
}

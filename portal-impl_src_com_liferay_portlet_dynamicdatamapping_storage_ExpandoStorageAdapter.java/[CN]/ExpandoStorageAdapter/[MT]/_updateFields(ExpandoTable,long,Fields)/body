{
  Iterator<Field> itr=fields.iterator();
  while (itr.hasNext()) {
    Field field=itr.next();
    ExpandoValueServiceUtil.addValue(table.getCompanyId(),ExpandoStorageAdapter.class.getName(),table.getName(),field.getName(),classPK,field.getValue());
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (ExpandoRowValues)session.get(ExpandoRowValuesImpl.class,expandoRowValuesPK);
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

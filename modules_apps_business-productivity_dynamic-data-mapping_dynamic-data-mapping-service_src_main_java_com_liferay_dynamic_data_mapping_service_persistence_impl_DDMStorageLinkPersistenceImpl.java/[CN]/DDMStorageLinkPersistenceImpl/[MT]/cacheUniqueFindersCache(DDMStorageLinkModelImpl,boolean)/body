{
  if (isNew) {
    Object[] args=new Object[]{ddmStorageLinkModelImpl.getClassPK()};
    finderCache.putResult(FINDER_PATH_COUNT_BY_CLASSPK,args,Long.valueOf(1));
    finderCache.putResult(FINDER_PATH_FETCH_BY_CLASSPK,args,ddmStorageLinkModelImpl);
  }
 else {
    if ((ddmStorageLinkModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_CLASSPK.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{ddmStorageLinkModelImpl.getClassPK()};
      finderCache.putResult(FINDER_PATH_COUNT_BY_CLASSPK,args,Long.valueOf(1));
      finderCache.putResult(FINDER_PATH_FETCH_BY_CLASSPK,args,ddmStorageLinkModelImpl);
    }
  }
}

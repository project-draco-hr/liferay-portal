{
  Object[] args=new Object[]{socialActivityAchievementModelImpl.getGroupId(),socialActivityAchievementModelImpl.getUserId(),socialActivityAchievementModelImpl.getName()};
  finderCache.removeResult(FINDER_PATH_COUNT_BY_G_U_N,args);
  finderCache.removeResult(FINDER_PATH_FETCH_BY_G_U_N,args);
  if ((socialActivityAchievementModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_G_U_N.getColumnBitmask()) != 0) {
    args=new Object[]{socialActivityAchievementModelImpl.getOriginalGroupId(),socialActivityAchievementModelImpl.getOriginalUserId(),socialActivityAchievementModelImpl.getOriginalName()};
    finderCache.removeResult(FINDER_PATH_COUNT_BY_G_U_N,args);
    finderCache.removeResult(FINDER_PATH_FETCH_BY_G_U_N,args);
  }
}

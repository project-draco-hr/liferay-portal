{
  int count=countByG_P_S(groupId,parentFolderId,status);
  List<DLFolder> list=findByG_P_S(groupId,parentFolderId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

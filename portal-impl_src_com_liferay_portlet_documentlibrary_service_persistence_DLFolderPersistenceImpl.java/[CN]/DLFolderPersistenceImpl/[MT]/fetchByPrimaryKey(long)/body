{
  DLFolder result=(DLFolder)EntityCacheUtil.getResult(DLFolderModelImpl.ENTITY_CACHE_ENABLED,DLFolder.class,folderId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      DLFolder dlFolder=(DLFolder)session.get(DLFolderImpl.class,new Long(folderId));
      if (dlFolder != null) {
        cacheResult(dlFolder);
      }
      return dlFolder;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (DLFolder)result;
  }
}

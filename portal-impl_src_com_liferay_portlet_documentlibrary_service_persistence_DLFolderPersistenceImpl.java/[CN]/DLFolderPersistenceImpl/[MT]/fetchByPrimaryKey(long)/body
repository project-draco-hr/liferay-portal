{
  DLFolder dlFolder=(DLFolder)EntityCacheUtil.getResult(DLFolderModelImpl.ENTITY_CACHE_ENABLED,DLFolderImpl.class,folderId,this);
  if (dlFolder == _nullDLFolder) {
    return null;
  }
  if (dlFolder == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      dlFolder=(DLFolder)session.get(DLFolderImpl.class,Long.valueOf(folderId));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (!hasException && (dlFolder == null)) {
        EntityCacheUtil.putResult(DLFolderModelImpl.ENTITY_CACHE_ENABLED,DLFolderImpl.class,folderId,_nullDLFolder);
      }
 else {
        cacheResult(dlFolder);
      }
      closeSession(session);
    }
  }
  return dlFolder;
}

{
  DLFolder dlFolder=(DLFolder)EntityCacheUtil.getResult(DLFolderModelImpl.ENTITY_CACHE_ENABLED,DLFolderImpl.class,folderId,this);
  if (dlFolder == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (dlFolder == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      dlFolder=(DLFolder)session.get(DLFolderImpl.class,Long.valueOf(folderId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (dlFolder == null)) {
        EntityCacheUtil.putResult(DLFolderModelImpl.ENTITY_CACHE_ENABLED,DLFolderImpl.class,folderId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(dlFolder);
      }
      closeSession(session);
    }
  }
  return dlFolder;
}

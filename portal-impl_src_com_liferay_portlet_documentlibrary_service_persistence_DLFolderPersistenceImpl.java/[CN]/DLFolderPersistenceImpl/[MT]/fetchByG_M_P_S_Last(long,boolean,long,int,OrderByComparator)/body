{
  int count=countByG_M_P_S(groupId,mountPoint,parentFolderId,status);
  List<DLFolder> list=findByG_M_P_S(groupId,mountPoint,parentFolderId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  DLFolder dlFolder=findByPrimaryKey(folderId);
  Session session=null;
  try {
    session=openSession();
    DLFolder[] array=new DLFolderImpl[3];
    array[0]=getByG_P_S_M_PrevAndNext(session,dlFolder,groupId,parentFolderId,status,mountPoint,orderByComparator,true);
    array[1]=dlFolder;
    array[2]=getByG_P_S_M_PrevAndNext(session,dlFolder,groupId,parentFolderId,status,mountPoint,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

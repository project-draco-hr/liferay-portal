{
  int count=countByG_M_P_H(groupId,mountPoint,parentFolderId,hidden);
  List<DLFolder> list=findByG_M_P_H(groupId,mountPoint,parentFolderId,hidden,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(DLFolder dlFolder) method. To improve performance, call update(DLFolder dlFolder, boolean merge).");
  }
  return update(dlFolder,false);
}

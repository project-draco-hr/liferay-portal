{
  if (hasUserPreferenes()) {
    Layout layout=getLayout();
    long plid=layout.getPlid();
    _portalPreferences.resetValues(PersonalizedPages.namespacePlid(plid));
    _portalPreferences.setValue(PersonalizedPages.namespacePlid(plid),_MODIFIED_DATE,_dateFormat.format(new Date()));
  }
}

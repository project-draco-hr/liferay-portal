{
  if (!hasUserPreferenes()) {
    return;
  }
  _portalPreferences.setValue(PersonalizedPages.namespace(getLayout().getPlid()),key,value);
  _portalPreferences.setValue(PersonalizedPages.namespace(getLayout().getPlid()),_MODIFIED_DATE,_dateFormat.format(new Date()));
}

{
  if (!hasUserPreferenes()) {
    return;
  }
  _portalPreferences.setValue(PersonalizedPages.namespacePlid(getPlid()),key,value);
  _portalPreferences.setValue(PersonalizedPages.namespacePlid(getPlid()),_MODIFIED_DATE,_dateFormat.format(new Date()));
}

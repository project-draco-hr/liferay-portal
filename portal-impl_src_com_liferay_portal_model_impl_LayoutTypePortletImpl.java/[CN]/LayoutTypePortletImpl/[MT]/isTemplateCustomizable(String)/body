{
  if (!hasSourcePrototypeLayout()) {
    return true;
  }
  String customizable=getSourcePrototypeLayoutProperty(CustomizedPages.namespaceColumnId(columnId));
  return GetterUtil.getBoolean(customizable,false);
}

{
  if (isSupportsAlterColumnType()) {
    runSQL("alter_column_type CalEvent remindBy INTEGER");
  }
 else {
    UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(CalEventTable.TABLE_NAME,CalEventTable.TABLE_COLUMNS);
    upgradeTable.setCreateSQL(CalEventTable.TABLE_SQL_CREATE);
    upgradeTable.updateTable();
  }
}

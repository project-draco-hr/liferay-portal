{
  Set<AssetTag> groupsTags=new TreeSet<AssetTag>(new AssetTagNameComparator());
  for (  long groupId : groupIds) {
    List<AssetTag> groupTags=getGroupTags(groupId);
    groupsTags.addAll(groupTags);
  }
  return new ArrayList(groupsTags);
}

{
  IGImage igImage=findByPrimaryKey(imageId);
  Session session=null;
  try {
    session=openSession();
    IGImage[] array=new IGImageImpl[3];
    array[0]=getByG_U_PrevAndNext(session,igImage,groupId,userId,orderByComparator,true);
    array[1]=igImage;
    array[2]=getByG_U_PrevAndNext(session,igImage,groupId,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

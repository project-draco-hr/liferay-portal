{
  Class<?>[] declaredClasses=clazz.getDeclaredClasses();
  int declaredClassesLength=declaredClasses.length;
  if (_log.isDebugEnabled()) {
    _log.debug("Loading " + clazz.getName() + " and "+ declaredClassesLength+ " inner classes");
  }
}

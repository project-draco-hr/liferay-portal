{
  super(persistence.getDataSource(),"INSERT INTO SCFrameworkVersions_SCProductVersions (productVersionId, frameworkVersionId) VALUES (?, ?)");
  _persistence=persistence;
  declareParameter(new SqlParameter(Types.BIGINT));
  declareParameter(new SqlParameter(Types.BIGINT));
  compile();
}

{
  if (status == WorkflowConstants.STATUS_ANY) {
    return ddlRecordPersistence.findByRecordSetId(recordSetId,start,end,orderByComparator);
  }
 else {
    return ddlRecordPersistence.findByR_S(recordSetId,status,start,end,orderByComparator);
  }
}

{
  ddlRecordPersistence.remove(record);
  StorageEngineUtil.deleteByClass(record.getClassPK());
  workflowInstanceLinkLocalService.deleteWorkflowInstanceLinks(record.getCompanyId(),record.getGroupId(),DDLRecord.class.getName(),record.getRecordId());
}

{
  setChangeLogHeader(new Callable<String>(){
    @Override public String call() throws Exception {
      Project project=getProject();
      return "Module Version " + project.getVersion();
    }
  }
);
  setTicketIdPrefixes("CLDSVCS","LPS","SOS","SYNC");
}

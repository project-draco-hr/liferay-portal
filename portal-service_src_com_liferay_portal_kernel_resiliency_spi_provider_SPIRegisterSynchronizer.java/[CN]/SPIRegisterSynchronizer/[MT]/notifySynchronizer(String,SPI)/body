{
  SynchronousQueue<SPI> spiSynchronizer=_synchronizerRegistry.remove(spiUUID);
  if (spiSynchronizer == null) {
    throw new IllegalStateException("No such SPI synchronizer with uuid : " + spiUUID);
  }
  spiSynchronizer.put(spi);
}

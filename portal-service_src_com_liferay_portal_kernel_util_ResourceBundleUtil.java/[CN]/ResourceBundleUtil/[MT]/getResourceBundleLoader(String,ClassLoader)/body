{
  return new ResourceBundleLoader(){
    @Override public ResourceBundle loadResourceBundle(    String languageId){
      return ResourceBundleUtil.getBundle(baseName,LocaleUtil.fromLanguageId(languageId),classLoader);
    }
  }
;
}

{
  JournalFolder folder=getJournalFolder(classPK);
  if ((folder.getParentFolderId() > 0) && (JournalFolderLocalServiceUtil.fetchFolder(folder.getParentFolderId()) == null)) {
    return false;
  }
  return !folder.isInTrashContainer();
}

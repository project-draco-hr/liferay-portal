{
  long companyId=getCompanyId(portletRequest);
  String domain=getValidPortalDomain(companyId,portletRequest.getServerName());
  return getPortalURL(domain,portletRequest.getServerPort(),secure);
}

{
  if (!PropsValues.WEB_SERVER_FORWARDED_PORT_ENABLED) {
    return request.getServerPort();
  }
  return GetterUtil.getInteger(request.getHeader(PropsValues.WEB_SERVER_FORWARDED_PORT_HEADER),request.getServerPort());
}

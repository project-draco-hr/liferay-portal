{
  if (!PropsValues.WEB_SERVER_FORWARDED_PORT_ENABLED) {
    return request.getServerPort();
  }
  int serverPort=GetterUtil.getInteger(request.getHeader(PropsValues.WEB_SERVER_FORWARDED_PORT_HEADER),request.getServerPort());
  return serverPort;
}

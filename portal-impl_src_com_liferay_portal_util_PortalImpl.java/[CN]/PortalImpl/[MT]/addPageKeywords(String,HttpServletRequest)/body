{
  String requestKeywords=(String)request.getAttribute(WebKeys.PAGE_KEYWORDS);
  if (requestKeywords != null) {
    keywords=requestKeywords + StringPool.BLANK + keywords;
  }
  request.setAttribute(WebKeys.PAGE_KEYWORDS,keywords);
}

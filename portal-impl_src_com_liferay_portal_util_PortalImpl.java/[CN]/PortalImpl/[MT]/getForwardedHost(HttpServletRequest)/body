{
  if (!PropsValues.WEB_SERVER_FORWARDED_HOST_ENABLED) {
    return request.getServerName();
  }
  String serverName=GetterUtil.get(request.getHeader(PropsValues.WEB_SERVER_FORWARDED_HOST_HEADER),request.getServerName());
  return serverName;
}

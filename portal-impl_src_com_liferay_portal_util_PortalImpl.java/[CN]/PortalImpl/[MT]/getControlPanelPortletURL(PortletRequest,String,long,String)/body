{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  Layout layout=null;
  try {
    long plid=getControlPanelPlid(themeDisplay.getCompanyId());
    layout=LayoutLocalServiceUtil.getLayout(plid);
  }
 catch (  PortalException pe) {
    _log.error("Unable to determine control panel layout",pe);
    return null;
  }
  VirtualLayout virtualLayout=new VirtualLayout(layout,themeDisplay.getScopeGroup());
  portletRequest.setAttribute(WebKeys.LAYOUT,virtualLayout);
  LiferayPortletURL liferayPortletURL=new PortletURLImpl(portletRequest,portletId,virtualLayout.getPlid(),lifecycle);
  try {
    liferayPortletURL.setWindowState(WindowState.MAXIMIZED);
  }
 catch (  WindowStateException wse) {
    _log.error(wse);
  }
  return liferayPortletURL;
}

{
  if (_portalPort == -1) {
    _portalPortLock.lock();
    try {
      if (_portalPort == -1) {
        _portalPort=request.getServerPort();
      }
    }
  finally {
      _portalPortLock.unlock();
    }
  }
}

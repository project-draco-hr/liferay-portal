{
  String actions=permission.getActions();
  String name=permission.getName();
  if (!_hostsAndPorts.implies(permission)) {
    throwSecurityException(_log,"Attempted " + actions + " for address "+ name);
  }
}

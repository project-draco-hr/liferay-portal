{
  byte[] b=new byte[1];
  write(closePipeWriteFd,b,1);
synchronized (changeLock) {
    boolean eventsAdded=cancelImpl(pathWatchKey,true);
    if (eventsAdded)     queueKey(pathWatchKey);
    int nread=read(closePipeReadFd,b,1);
    assert(nread == 1);
  }
}

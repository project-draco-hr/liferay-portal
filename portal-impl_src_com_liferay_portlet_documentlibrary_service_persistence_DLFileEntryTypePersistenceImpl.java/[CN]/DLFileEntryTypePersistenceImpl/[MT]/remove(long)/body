{
  Session session=null;
  try {
    session=openSession();
    DLFileEntryType dlFileEntryType=(DLFileEntryType)session.get(DLFileEntryTypeImpl.class,Long.valueOf(fileEntryTypeId));
    if (dlFileEntryType == null) {
      if (_log.isWarnEnabled()) {
        _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + fileEntryTypeId);
      }
      throw new NoSuchFileEntryTypeException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + fileEntryTypeId);
    }
    return dlFileEntryTypePersistence.remove(dlFileEntryType);
  }
 catch (  NoSuchFileEntryTypeException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

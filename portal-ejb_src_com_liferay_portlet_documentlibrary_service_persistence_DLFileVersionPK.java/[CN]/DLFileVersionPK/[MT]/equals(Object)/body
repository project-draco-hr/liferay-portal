{
  if (obj == null) {
    return false;
  }
  DLFileVersionPK pk=null;
  try {
    pk=(DLFileVersionPK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((folderId.equals(pk.folderId)) && (name.equals(pk.name)) && (version == pk.version)) {
    return true;
  }
 else {
    return false;
  }
}

{
  _findActionHelper=new BaseStrutsPortletFindActionHelper(){
    @Override public long getGroupId(    long primaryKey) throws Exception {
      return FindStrutsAction.this.getGroupId(primaryKey);
    }
    @Override public String getPrimaryKeyParameterName(){
      return FindStrutsAction.this.getPrimaryKeyParameterName();
    }
    @Override public String getStrutsAction(    HttpServletRequest request,    String portletId){
      return FindStrutsAction.this.getStrutsAction(request,portletId);
    }
    @Override public String[] initPortletIds(){
      return FindStrutsAction.this.initPortletIds();
    }
    @Override public PortletURL processPortletURL(    HttpServletRequest request,    PortletURL portletURL) throws Exception {
      return FindStrutsAction.this.processPortletURL(request,portletURL);
    }
    @Override public void setPrimaryKeyParameter(    PortletURL portletURL,    long primaryKey) throws Exception {
      FindStrutsAction.this.setPrimaryKeyParameter(portletURL,primaryKey);
    }
    @Override protected PortletPageFinder getPortletPageFinder(){
      return FindStrutsAction.this._portletPageFinder;
    }
  }
;
}

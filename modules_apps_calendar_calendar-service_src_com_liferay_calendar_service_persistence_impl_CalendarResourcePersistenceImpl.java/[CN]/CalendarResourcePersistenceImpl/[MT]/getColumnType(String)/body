{
  Integer type=CalendarResourceModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ CalendarResourceModelImpl.TABLE_NAME);
  }
  return type;
}

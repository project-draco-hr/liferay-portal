{
  Session session=null;
  try {
    session=openSession();
    return (OrgGroupPermission)session.get(OrgGroupPermissionImpl.class,orgGroupPermissionPK);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

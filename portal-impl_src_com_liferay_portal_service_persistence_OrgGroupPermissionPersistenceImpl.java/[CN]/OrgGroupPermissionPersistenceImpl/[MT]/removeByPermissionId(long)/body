{
  Iterator<OrgGroupPermission> itr=findByPermissionId(permissionId).iterator();
  while (itr.hasNext()) {
    OrgGroupPermission orgGroupPermission=itr.next();
    remove(orgGroupPermission);
  }
}

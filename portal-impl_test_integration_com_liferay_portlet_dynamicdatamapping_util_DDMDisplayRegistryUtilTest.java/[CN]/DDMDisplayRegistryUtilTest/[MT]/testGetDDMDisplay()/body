{
  String testClassName=TestDDMDisplay.class.getName();
  DDMDisplay dDMDisplay=DDMDisplayRegistryUtil.getDDMDisplay(testClassName);
  Class<?> clazz=dDMDisplay.getClass();
  Assert.assertEquals(testClassName,clazz.getName());
}

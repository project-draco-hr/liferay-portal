{
  List<Element> stagedModelsElements=new ArrayList<Element>();
  for (  String className : stagedModelsMap.keySet()) {
    Element stagedModelsElement=SAXReaderUtil.createElement(className);
    stagedModelsElements.add(stagedModelsElement);
  }
  if (!stagedModelsMap.containsKey(getStagedModelClassName())) {
    Element stagedModelsElement=SAXReaderUtil.createElement(getStagedModelClassName());
    stagedModelsElements.add(stagedModelsElement);
  }
  return stagedModelsElements.toArray(new Element[stagedModelsElements.size()]);
}

{
  List<Element> stagedModelElements=new ArrayList<Element>();
  for (  String className : dependentStagedModels.keySet()) {
    Element relatedStagedModelElement=new ElementImpl(DocumentHelper.createElement(className));
    stagedModelElements.add(relatedStagedModelElement);
  }
  if (!dependentStagedModels.containsKey(getStagedModelClassName())) {
    Element relatedStagedModelElement=new ElementImpl(DocumentHelper.createElement(getStagedModelClassName()));
    stagedModelElements.add(relatedStagedModelElement);
  }
  return stagedModelElements.toArray(new Element[stagedModelElements.size()]);
}

{
  if (!isCommentableStagedModel()) {
    return;
  }
  MBTestUtil.addDiscussionMessage(stagingGroup.getGroupId(),ExportImportClassedModelUtil.getClassName(stagedModel),ExportImportClassedModelUtil.getClassPK(stagedModel));
}

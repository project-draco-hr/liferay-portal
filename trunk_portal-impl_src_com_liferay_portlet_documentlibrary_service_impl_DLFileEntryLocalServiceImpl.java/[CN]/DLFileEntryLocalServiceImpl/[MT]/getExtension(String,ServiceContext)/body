{
  String sourceFileName=(String)serviceContext.getAttribute("sourceFileName");
  if (Validator.isNull(sourceFileName)) {
    sourceFileName=title;
  }
  return FileUtil.getExtension(sourceFileName);
}

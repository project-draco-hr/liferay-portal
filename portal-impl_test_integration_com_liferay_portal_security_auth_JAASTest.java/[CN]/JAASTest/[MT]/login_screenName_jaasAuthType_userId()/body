{
  _jaasAuthTypeField.set(null,"userId");
  User user=TestPropsValues.getUser();
  String screenName=user.getScreenName();
  LoginContext loginContext=new LoginContext(_PORTAL_REALM,new JAASCallbackHandler(screenName,user.getPassword()));
  try {
    loginContext.login();
    Assert.fail();
  }
 catch (  Exception e) {
  }
}

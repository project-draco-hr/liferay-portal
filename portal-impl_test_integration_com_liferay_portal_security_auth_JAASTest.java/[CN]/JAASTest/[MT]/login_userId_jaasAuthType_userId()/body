{
  _jaasAuthTypeField.set(null,"userId");
  User user=TestPropsValues.getUser();
  String userIdString=String.valueOf(user.getUserId());
  LoginContext loginContext=new LoginContext(_PORTAL_REALM,new JAASCallbackHandler(userIdString,user.getPassword()));
  try {
    loginContext.login();
  }
 catch (  Exception e) {
    Assert.fail();
  }
  validateSubject(loginContext.getSubject(),userIdString);
}

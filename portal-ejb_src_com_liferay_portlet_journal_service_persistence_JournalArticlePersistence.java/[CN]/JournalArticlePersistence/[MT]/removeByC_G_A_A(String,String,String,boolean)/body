{
  Iterator itr=findByC_G_A_A(companyId,groupId,articleId,approved).iterator();
  while (itr.hasNext()) {
    JournalArticle journalArticle=(JournalArticle)itr.next();
    remove(journalArticle);
  }
}

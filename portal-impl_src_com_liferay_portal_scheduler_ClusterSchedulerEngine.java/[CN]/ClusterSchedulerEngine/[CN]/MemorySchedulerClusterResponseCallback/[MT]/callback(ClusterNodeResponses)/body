{
  try {
    ClusterNodeResponse clusterNodeResponse=clusterNodeResponses.getClusterResponse(_address);
    List<SchedulerResponse> schedulerResponses=(List<SchedulerResponse>)clusterNodeResponse.getResult();
    _doMasterToSlave(schedulerResponses);
  }
 catch (  Exception e) {
    _log.error("Unable to load memory clustered jobs from cluster node " + _address.getDescription(),e);
  }
}

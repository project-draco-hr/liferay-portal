{
  ByteArrayOutputStream byetArrayOutputStream=new ByteArrayOutputStream();
  ObjectOutputStream objectOutputStream=new ObjectOutputStream(byetArrayOutputStream);
  objectOutputStream.writeObject(object);
  objectOutputStream.close();
  byte[] orginalBytes=byetArrayOutputStream.toByteArray();
  return Base64.encode(orginalBytes);
}

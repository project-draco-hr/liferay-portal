{
  byte[] orginalBytes=Base64.decode(serializedString);
  UnsyncByteArrayInputStream byetArrayInputStream=new UnsyncByteArrayInputStream(orginalBytes);
  ObjectInputStream objectInputStream=new ObjectInputStream(byetArrayInputStream);
  Object object=objectInputStream.readObject();
  objectInputStream.close();
  return object;
}

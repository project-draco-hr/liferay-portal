{
  Session session=null;
  try {
    session=openSession();
    return (AnnouncementFlag)session.get(AnnouncementFlagImpl.class,new Long(announcementFlagId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserGroupRole userGroupRole) method. To improve performance, call update(UserGroupRole userGroupRole, boolean merge).");
  }
  return update(userGroupRole,false);
}

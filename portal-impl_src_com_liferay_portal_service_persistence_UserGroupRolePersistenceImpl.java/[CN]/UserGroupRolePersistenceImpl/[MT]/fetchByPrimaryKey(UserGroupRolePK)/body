{
  Session session=null;
  try {
    session=openSession();
    return (UserGroupRole)session.get(UserGroupRoleImpl.class,userGroupRolePK);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

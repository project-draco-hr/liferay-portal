{
  UserGroupRole userGroupRole=(UserGroupRole)EntityCacheUtil.getResult(UserGroupRoleModelImpl.ENTITY_CACHE_ENABLED,UserGroupRoleImpl.class,userGroupRolePK,this);
  if (userGroupRole == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (userGroupRole == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      userGroupRole=(UserGroupRole)session.get(UserGroupRoleImpl.class,userGroupRolePK);
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (userGroupRole == null)) {
        EntityCacheUtil.putResult(UserGroupRoleModelImpl.ENTITY_CACHE_ENABLED,UserGroupRoleImpl.class,userGroupRolePK,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(userGroupRole);
      }
      closeSession(session);
    }
  }
  return userGroupRole;
}

{
  for (  UserGroupRole userGroupRole : userGroupRoles) {
    if (EntityCacheUtil.getResult(UserGroupRoleModelImpl.ENTITY_CACHE_ENABLED,UserGroupRole.class,userGroupRole.getPrimaryKey(),this) == null) {
      cacheResult(userGroupRole);
    }
  }
}

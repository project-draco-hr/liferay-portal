{
  if (isEscapedModel()) {
    return (IGImage)this;
  }
 else {
    return (IGImage)Proxy.newProxyInstance(IGImage.class.getClassLoader(),new Class[]{IGImage.class},new AutoEscapeBeanHandler(this));
  }
}

{
  Method testMethod=testContext.getMethod();
  TransactionContext transactionContext=_transactionContextCache.remove(testMethod);
  if ((transactionContext != null) && !transactionContext._transactionStatus.isCompleted()) {
    rollbackTransaction(transactionContext);
  }
}

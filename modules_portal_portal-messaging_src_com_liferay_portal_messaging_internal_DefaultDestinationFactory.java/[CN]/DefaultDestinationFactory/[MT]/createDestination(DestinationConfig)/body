{
  String type=destinationConfig.getDestinationType();
  DestinationPrototype destinationPrototype=_destinationPrototypes.get(type);
  if (destinationPrototype == null) {
    throw new IllegalArgumentException("No prototype configured for : " + type);
  }
  return destinationPrototype.createDestination(destinationConfig);
}

{
  if (((eventId == null) && (_eventId != null)) || ((eventId != null) && (_eventId == null)) || ((eventId != null) && (_eventId != null) && !eventId.equals(_eventId))) {
    if (!XSS_ALLOW_EVENTID) {
      eventId=XSSUtil.strip(eventId);
    }
    _eventId=eventId;
  }
}

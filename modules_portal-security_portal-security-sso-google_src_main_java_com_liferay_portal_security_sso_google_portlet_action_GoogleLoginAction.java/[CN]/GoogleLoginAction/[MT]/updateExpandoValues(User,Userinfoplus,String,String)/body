{
  ExpandoValueLocalServiceUtil.addValue(user.getCompanyId(),User.class.getName(),ExpandoTableConstants.DEFAULT_TABLE_NAME,"googleAccessToken",user.getUserId(),accessToken);
  ExpandoValueLocalServiceUtil.addValue(user.getCompanyId(),User.class.getName(),ExpandoTableConstants.DEFAULT_TABLE_NAME,"googleRefreshToken",user.getUserId(),refreshToken);
  ExpandoValueLocalServiceUtil.addValue(user.getCompanyId(),User.class.getName(),ExpandoTableConstants.DEFAULT_TABLE_NAME,"googleUserId",user.getUserId(),userinfoplus.getId());
}

{
  Configuration configuration=new Configuration();
  try {
    String[] resources=getConfigurationResources();
    for (    String resource : resources) {
      try {
        readResource(configuration,resource);
      }
 catch (      Exception e2) {
        if (_log.isWarnEnabled()) {
          _log.warn(e2,e2);
        }
      }
    }
    if (Validator.isNull(PropsValues.HIBERNATE_DIALECT)) {
      String dialect=determineDialect();
      configuration.setProperty("hibernate.dialect",dialect);
    }
    Properties hibernateProperties=PropsUtil.getProperties("hibernate.",false);
    hibernateProperties.setProperty("net.sf.ehcache.configurationResourceName",PropsUtil.get("net.sf.ehcache.configurationResourceName"));
    configuration.setProperties(hibernateProperties);
  }
 catch (  Exception e1) {
    _log.error(e1,e1);
  }
  return configuration;
}

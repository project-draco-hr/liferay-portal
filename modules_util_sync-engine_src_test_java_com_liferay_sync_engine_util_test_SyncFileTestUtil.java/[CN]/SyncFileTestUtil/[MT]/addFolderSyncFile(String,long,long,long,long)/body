{
  Files.createDirectory(Paths.get(filePathName));
  SyncFile syncFile=SyncFileService.addSyncFile(null,null,false,null,filePathName,null,null,parentFolderId,repositoryId,SyncFile.STATE_SYNCED,0,syncAccountId,SyncFile.TYPE_FOLDER);
  if (typePK == 0) {
    syncFile.setTypePK(syncFile.getSyncFileId());
    SyncFileService.update(syncFile);
  }
  return syncFile;
}

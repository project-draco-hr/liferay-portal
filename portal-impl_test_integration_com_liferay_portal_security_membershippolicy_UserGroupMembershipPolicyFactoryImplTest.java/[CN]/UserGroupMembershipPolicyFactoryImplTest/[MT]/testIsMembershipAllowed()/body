{
  try {
    boolean value=UserGroupMembershipPolicyUtil.isMembershipAllowed(1,1);
    Assert.assertTrue(value);
    value=UserGroupMembershipPolicyUtil.isMembershipAllowed(2,2);
    Assert.assertFalse(value);
  }
 catch (  PortalException e) {
    Assert.fail();
  }
}

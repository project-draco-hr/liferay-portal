{
  SSLConnectionSocketFactory sslConnectionSocketFactory=_sslSocketFactoryBuilder.build();
  Registry<ConnectionSocketFactory> schemeRegistry=createSchemeRegistry(sslConnectionSocketFactory);
  return new PoolingHttpClientConnectionManager(schemeRegistry);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AnnouncementsEntry announcementsEntry) method. To improve performance, call update(AnnouncementsEntry announcementsEntry, boolean merge).");
  }
  return update(announcementsEntry,false);
}

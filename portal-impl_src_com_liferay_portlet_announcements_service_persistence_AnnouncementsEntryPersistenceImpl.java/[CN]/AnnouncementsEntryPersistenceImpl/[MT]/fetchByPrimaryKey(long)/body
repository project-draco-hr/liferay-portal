{
  AnnouncementsEntry announcementsEntry=(AnnouncementsEntry)EntityCacheUtil.getResult(AnnouncementsEntryModelImpl.ENTITY_CACHE_ENABLED,AnnouncementsEntryImpl.class,entryId,this);
  if (announcementsEntry == _nullAnnouncementsEntry) {
    return null;
  }
  if (announcementsEntry == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      announcementsEntry=(AnnouncementsEntry)session.get(AnnouncementsEntryImpl.class,Long.valueOf(entryId));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (!hasException && (announcementsEntry == null)) {
        EntityCacheUtil.putResult(AnnouncementsEntryModelImpl.ENTITY_CACHE_ENABLED,AnnouncementsEntryImpl.class,entryId,_nullAnnouncementsEntry);
      }
 else {
        cacheResult(announcementsEntry);
      }
      closeSession(session);
    }
  }
  return announcementsEntry;
}

{
  AnnouncementsEntry announcementsEntry=fetchByPrimaryKey(entryId);
  if (announcementsEntry == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No AnnouncementsEntry exists with the primary key " + entryId);
    }
    throw new NoSuchEntryException("No AnnouncementsEntry exists with the primary key " + entryId);
  }
  return announcementsEntry;
}

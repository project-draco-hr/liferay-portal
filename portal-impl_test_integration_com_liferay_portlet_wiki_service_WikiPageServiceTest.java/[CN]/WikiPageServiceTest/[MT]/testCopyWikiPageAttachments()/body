{
  WikiPage page=WikiTestUtil.addPage(TestPropsValues.getUserId(),_group.getGroupId(),_node.getNodeId(),ServiceTestUtil.randomString(),true);
  WikiTestUtil.addWikiAttachment(page.getUserId(),page.getNodeId(),page.getTitle(),getClass());
  List<FileEntry> fileEntries=page.getAttachmentsFileEntries();
  WikiPage copy=WikiTestUtil.copyPage(page,true,ServiceTestUtil.getServiceContext(_group.getGroupId()));
  List<FileEntry> copiedFileEntries=copy.getAttachmentsFileEntries();
  Assert.assertEquals(fileEntries.size(),copiedFileEntries.size());
  FileEntry fileEntry=fileEntries.get(0);
  FileEntry copiedFileEntry=copiedFileEntries.get(0);
  Assert.assertEquals(fileEntry.getTitle(),copiedFileEntry.getTitle());
  Assert.assertEquals(fileEntry.getExtension(),copiedFileEntry.getExtension());
  Assert.assertEquals(fileEntry.getMimeType(),copiedFileEntry.getMimeType());
  Assert.assertEquals(fileEntry.getSize(),copiedFileEntry.getSize());
}

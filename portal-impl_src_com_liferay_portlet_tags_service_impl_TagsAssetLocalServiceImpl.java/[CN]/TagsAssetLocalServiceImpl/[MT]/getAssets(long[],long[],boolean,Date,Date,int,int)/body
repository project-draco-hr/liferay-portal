{
  if (andOperator) {
    return TagsAssetFinder.findByAndEntryIds(entryIds,notEntryIds,publishDate,expirationDate,begin,end);
  }
 else {
    return TagsAssetFinder.findByOrEntryIds(entryIds,notEntryIds,publishDate,expirationDate,begin,end);
  }
}

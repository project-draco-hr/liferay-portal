{
  if (andOperator) {
    return TagsAssetFinder.countByAndEntryIds(entryIds,notEntryIds,publishDate,expirationDate);
  }
 else {
    return TagsAssetFinder.countByOrEntryIds(entryIds,notEntryIds,publishDate,expirationDate);
  }
}

{
  if (andOperator) {
    return TagsAssetFinder.countByAndEntryIds(groupId,entryIds,notEntryIds,excludeZeroViewCount,publishDate,expirationDate);
  }
 else {
    return TagsAssetFinder.countByOrEntryIds(groupId,entryIds,notEntryIds,excludeZeroViewCount,publishDate,expirationDate);
  }
}

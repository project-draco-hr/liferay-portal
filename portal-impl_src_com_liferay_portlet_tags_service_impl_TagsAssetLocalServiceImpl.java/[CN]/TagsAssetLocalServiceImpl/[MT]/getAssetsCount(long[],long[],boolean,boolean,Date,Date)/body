{
  if (andOperator) {
    return TagsAssetFinder.countByAndEntryIds(entryIds,notEntryIds,excludeZeroViewCount,publishDate,expirationDate);
  }
 else {
    return TagsAssetFinder.countByOrEntryIds(entryIds,notEntryIds,excludeZeroViewCount,publishDate,expirationDate);
  }
}

{
  StorageAdapter storageAdapter=StorageAdapterRegistryUtil.getStorageAdapter(storageType);
  if (storageAdapter != null) {
    return storageAdapter;
  }
  return StorageAdapterRegistryUtil.getDefaultStorageAdapter();
}

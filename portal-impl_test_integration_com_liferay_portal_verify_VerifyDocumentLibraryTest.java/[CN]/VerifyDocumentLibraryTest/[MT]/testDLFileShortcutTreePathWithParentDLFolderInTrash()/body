{
  Group group=GroupTestUtil.addGroup();
  Folder grandparentFolder=DLAppTestUtil.addFolder(group.getGroupId(),DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,ServiceTestUtil.randomString());
  Folder parentFolder=DLAppTestUtil.addFolder(group.getGroupId(),grandparentFolder.getFolderId(),ServiceTestUtil.randomString());
  DLAppTestUtil.addFileEntry(group.getGroupId(),parentFolder.getFolderId(),false,ServiceTestUtil.randomString());
  DLAppServiceUtil.moveFolderToTrash(parentFolder.getFolderId());
  DLFolderLocalServiceUtil.deleteFolder(grandparentFolder.getFolderId(),false);
  doVerify();
}

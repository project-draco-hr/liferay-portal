{
  String name=StringUtil.randomString(20) + PropsValues.DL_CHAR_BLACKLIST[0];
  try {
    DLAppTestUtil.addFolder(_group.getGroupId(),DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,name);
    Assert.fail(name + " is an invalid name");
  }
 catch (  Exception e) {
    Assert.assertTrue(e instanceof FolderNameException);
  }
}

{
  for (  FutureTask<Void> futureTask : futureTasks) {
    futureTask.get();
  }
  Assert.notNull(ResourcePermissionLocalServiceUtil.fetchResourcePermission(_COMPANY_ID,_NAME,_SCOPE,_PRIM_KEY,_ROLE_ID));
}

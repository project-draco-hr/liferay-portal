{
  List<JournalFeed> feeds=new ArrayList<JournalFeed>(contentFeeds.size());
  for (  ContentFeed contentFeed : contentFeeds) {
    MirageFeed mirageFeed=(MirageFeed)contentFeed;
    feeds.add(mirageFeed.getFeed());
  }
  return feeds;
}

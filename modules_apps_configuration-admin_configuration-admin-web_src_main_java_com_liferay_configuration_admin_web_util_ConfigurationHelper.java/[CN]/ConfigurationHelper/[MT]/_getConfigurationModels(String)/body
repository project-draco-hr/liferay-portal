{
  Map<String,ConfigurationModel> configurationModelMap=new HashMap<>();
  Bundle[] bundles=_bundleContext.getBundles();
  for (  Bundle bundle : bundles) {
    _collectConfigurationModels(bundle,configurationModelMap,locale,false);
    _collectConfigurationModels(bundle,configurationModelMap,locale,true);
  }
  return configurationModelMap;
}

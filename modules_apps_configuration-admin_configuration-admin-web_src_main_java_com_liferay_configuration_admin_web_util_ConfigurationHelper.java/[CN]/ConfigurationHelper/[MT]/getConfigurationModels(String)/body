{
  Set<ConfigurationModel> configurationModels=new TreeSet(new ConfigurationModelComparator());
  configurationModels.addAll(_categorizedConfigurationModels.get(configurationCategory));
  return new ArrayList<>(configurationModels);
}

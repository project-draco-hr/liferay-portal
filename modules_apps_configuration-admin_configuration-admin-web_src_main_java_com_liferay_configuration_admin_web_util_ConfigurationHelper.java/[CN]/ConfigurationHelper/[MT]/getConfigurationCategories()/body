{
  Set<String> configurationCategories=new TreeSet(getConfigurationCategoryComparator());
  configurationCategories.addAll(_categorizedConfigurationModels.keySet());
  return new ArrayList<>(configurationCategories);
}

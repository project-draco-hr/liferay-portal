{
  UserThread newUserThread=addUserThread();
  _persistence.clearCache();
  UserThread existingUserThread=_persistence.findByPrimaryKey(newUserThread.getPrimaryKey());
  Assert.assertEquals(Long.valueOf(existingUserThread.getUserId()),ReflectionTestUtil.<Long>invoke(existingUserThread,"getOriginalUserId",new Class<?>[0]));
  Assert.assertEquals(Long.valueOf(existingUserThread.getMbThreadId()),ReflectionTestUtil.<Long>invoke(existingUserThread,"getOriginalMbThreadId",new Class<?>[0]));
}

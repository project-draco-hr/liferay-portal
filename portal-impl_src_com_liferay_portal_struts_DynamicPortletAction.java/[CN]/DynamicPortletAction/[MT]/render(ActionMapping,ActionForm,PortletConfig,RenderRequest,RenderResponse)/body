{
  ModuleConfig moduleConfig=getModuleConfig(renderRequest);
  actionMapping=(ActionMapping)moduleConfig.findActionConfig(getPath(renderRequest));
  PortletAction action=(PortletAction)InstancePool.get(actionMapping.getType());
  return action.render(actionMapping,actionForm,portletConfig,renderRequest,renderResponse);
}

{
  ModuleConfig moduleConfig=getModuleConfig(actionRequest);
  mapping=(ActionMapping)moduleConfig.findActionConfig(getPath(actionRequest));
  PortletAction action=(PortletAction)InstancePool.get(mapping.getType());
  action.processAction(mapping,form,portletConfig,actionRequest,actionResponse);
}

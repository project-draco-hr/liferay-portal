{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.countByC_LtD(companyId,new Date());
  }
 else {
    return blogsEntryPersistence.countByC_LtD_S(companyId,new Date(),status);
  }
}

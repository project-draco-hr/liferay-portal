{
  try {
    doInvokeUndeploy(hotDeployEvent);
  }
 catch (  Throwable t) {
    throwHotDeployException(hotDeployEvent,"Error unregistering JSONWebServices for " + hotDeployEvent.getServletContextName(),t);
  }
}

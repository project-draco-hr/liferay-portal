{
  String className=getClassName(model);
  long primaryKey=getPrimaryKey(model);
  getSocialActivityLocalService().addUniqueActivity(userId,model.getGroupId(),className,primaryKey,type,extraData,receiverUserId);
}

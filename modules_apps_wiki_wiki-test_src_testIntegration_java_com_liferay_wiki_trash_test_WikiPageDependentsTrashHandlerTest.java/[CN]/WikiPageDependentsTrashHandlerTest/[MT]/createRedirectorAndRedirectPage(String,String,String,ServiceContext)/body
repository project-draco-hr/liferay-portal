{
  WikiTestUtil.addPage(TestPropsValues.getUserId(),_node.getNodeId(),oldTitle,RandomTestUtil.randomString(),parentPageTitle,true,serviceContext);
  WikiPageLocalServiceUtil.renamePage(TestPropsValues.getUserId(),_node.getNodeId(),oldTitle,newTitle,serviceContext);
}

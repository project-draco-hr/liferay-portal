{
  Matcher matcher=_pattern.matcher(pluginVersion);
  if (!matcher.find()) {
    return false;
  }
  if (pluginVersion.startsWith("6.2") && (Integer.parseInt(matcher.group(1)) < minimumVersion)) {
    return false;
  }
  return true;
}

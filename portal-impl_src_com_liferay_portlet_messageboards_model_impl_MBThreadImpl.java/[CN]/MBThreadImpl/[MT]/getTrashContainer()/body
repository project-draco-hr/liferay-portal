{
  try {
    MBCategory category=MBCategoryLocalServiceUtil.getCategory(getCategoryId());
    if (category.isInTrash()) {
      return category;
    }
    return category.getTrashContainer();
  }
 catch (  Exception e) {
    return null;
  }
}

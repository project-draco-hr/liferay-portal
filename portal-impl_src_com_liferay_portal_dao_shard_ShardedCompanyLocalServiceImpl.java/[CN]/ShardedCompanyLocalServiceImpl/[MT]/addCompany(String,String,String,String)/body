{
  shardName=ShardUtil.getShardSelector().getShardId(webId,virtualHost,mx,shardName);
  ShardUtil.pushCompanyService(shardName);
  try {
    return super.addCompany(webId,virtualHost,mx,shardName);
  }
  finally {
    ShardUtil.popCompanyService();
  }
}

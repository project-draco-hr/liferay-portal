{
  WikiDisplayContextFactory wikiDisplayContextFactory=null;
  if (_bundleContext != null) {
    wikiDisplayContextFactory=_bundleContext.getService(serviceReference);
  }
  _wikiDisplayContextFactories.put(serviceReference,wikiDisplayContextFactory);
}

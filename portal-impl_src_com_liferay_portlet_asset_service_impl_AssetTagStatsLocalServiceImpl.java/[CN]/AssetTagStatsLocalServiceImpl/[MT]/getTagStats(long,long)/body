{
  AssetTagStats tagStats=assetTagStatsPersistence.fetchByC_T(classNameId,tagId);
  if (tagStats == null) {
    tagStats=assetTagStatsLocalService.addTagStats(classNameId,tagId);
  }
  return tagStats;
}

{
  if (templateId.contains(TemplateResource.JOURNAL_SEPARATOR) || templateId.contains(TemplateResource.SERVLET_SEPARATOR) || templateId.contains(TemplateResource.THEME_LOADER_SEPARATOR)) {
    return null;
  }
  Class<?> clazz=getClass();
  ClassLoader classLoader=clazz.getClassLoader();
  if (_log.isDebugEnabled()) {
    _log.debug("Loading " + templateId);
  }
  return classLoader.getResource(templateId);
}

{
  for (  WSRPConsumer wsrpConsumer : wsrpConsumers) {
    if (entityCache.getResult(WSRPConsumerModelImpl.ENTITY_CACHE_ENABLED,WSRPConsumerImpl.class,wsrpConsumer.getPrimaryKey()) == null) {
      cacheResult(wsrpConsumer);
    }
 else {
      wsrpConsumer.resetOriginalValues();
    }
  }
}

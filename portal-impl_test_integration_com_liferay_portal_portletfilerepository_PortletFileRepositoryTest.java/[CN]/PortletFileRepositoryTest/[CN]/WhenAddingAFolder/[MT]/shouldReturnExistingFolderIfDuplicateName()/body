{
  String name=RandomTestUtil.randomString();
  Folder folder=_addPortletFolder(_group.getGroupId(),_portletId,_folder.getFolderId(),name);
  Folder newFolder=_addPortletFolder(_group.getGroupId(),_portletId,_folder.getFolderId(),name);
  Assert.assertEquals(newFolder,folder);
}

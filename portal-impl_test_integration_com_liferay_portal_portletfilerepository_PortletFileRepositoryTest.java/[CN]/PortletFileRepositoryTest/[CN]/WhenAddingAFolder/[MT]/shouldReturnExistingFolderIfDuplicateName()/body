{
  String name=RandomTestUtil.randomString();
  Folder folder=_addPortletFolder(_portletId,_folder.getFolderId(),name);
  Folder newFolder=_addPortletFolder(_portletId,_folder.getFolderId(),name);
  Assert.assertEquals(newFolder,folder);
}

{
  try {
    ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
    return RolePermissionUtil.contains(themeDisplay.getPermissionChecker(),getRoleId(portletRequest),ActionKeys.UPDATE);
  }
 catch (  Exception e) {
  }
  return false;
}

{
  String coRepoId=companyId + StringPool.PERIOD + repositoryId;
  FileUtil.deltree(PropsUtil.get(PropsUtil.DL_ROOT_DIR) + coRepoId);
  FileUtil.deltree(PropsUtil.get(PropsUtil.DL_VERSION_ROOT_DIR) + coRepoId);
}

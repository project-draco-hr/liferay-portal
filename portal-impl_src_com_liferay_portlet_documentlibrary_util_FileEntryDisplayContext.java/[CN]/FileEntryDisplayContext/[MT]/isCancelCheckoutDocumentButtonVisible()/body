{
  if ((_fileEntryDisplayContextHelper.hasUpdatePermission() && _fileEntryDisplayContextHelper.isCheckedOut() && _fileEntryDisplayContextHelper.isLockedByMe()) || (_fileEntryDisplayContextHelper.isCheckedOut() && _fileEntryDisplayContextHelper.hasOverrideCheckoutPermission())) {
    return true;
  }
  return false;
}

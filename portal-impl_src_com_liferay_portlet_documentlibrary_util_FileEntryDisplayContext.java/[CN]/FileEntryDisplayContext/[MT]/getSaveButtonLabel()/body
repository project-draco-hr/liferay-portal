{
  String saveButtonLabel="save";
  FileVersion fileVersion=_fileEntryDisplayContextHelper.getFileVersion();
  if ((fileVersion == null) || _fileEntryDisplayContextHelper.isDraft() || _fileEntryDisplayContextHelper.isApproved()) {
    saveButtonLabel="save-as-draft";
  }
  return saveButtonLabel;
}

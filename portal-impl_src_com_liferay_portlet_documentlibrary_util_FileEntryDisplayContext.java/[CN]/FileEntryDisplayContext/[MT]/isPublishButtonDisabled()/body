{
  if ((_fileEntryDisplayContextHelper.isCheckedOut() && !_fileEntryDisplayContextHelper.isFileEntryLockedByMe()) || (_fileEntryDisplayContextHelper.isPending() && _isDLFileEntryDraftsEnabled())) {
    return true;
  }
  return false;
}

{
  if ((_fileEntryDisplayContextHelper.isCheckedOut() && !_fileEntryDisplayContextHelper.isLockedByMe()) || (_fileEntryDisplayContextHelper.isPending() && _isDLFileEntryDraftsEnabled())) {
    return true;
  }
  return false;
}

{
  if (_fileEntryDisplayContextHelper.hasUpdatePermission() && _fileEntryDisplayContextHelper.isSupportsLocking() && !_fileEntryDisplayContextHelper.isCheckedOut()) {
    return true;
  }
  return false;
}

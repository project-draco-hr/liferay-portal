{
  if (_fileEntryDisplayContextHelper.hasUpdatePermission() && !_fileEntryDisplayContextHelper.isCheckedOut() && _fileEntryDisplayContextHelper.isSupportsLocking()) {
    return true;
  }
  return false;
}

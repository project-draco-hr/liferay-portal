{
  if (_fileEntryDisplayContextHelper.hasDeletePermission() && _fileEntryDisplayContextHelper.isDLFileEntry() && !_isFileEntryCheckedOutByOther()&& _isTrashEnabled()) {
    return true;
  }
  return false;
}

{
  super.setUp();
  User siteAdmin=UserTestUtil.addGroupUser(group,RoleConstants.SITE_ADMINISTRATOR);
  _commentedEntry=BlogsTestUtil.addEntry(siteAdmin.getUserId(),group,true);
  siteMember=UserTestUtil.addGroupUser(group,RoleConstants.SITE_MEMBER);
}

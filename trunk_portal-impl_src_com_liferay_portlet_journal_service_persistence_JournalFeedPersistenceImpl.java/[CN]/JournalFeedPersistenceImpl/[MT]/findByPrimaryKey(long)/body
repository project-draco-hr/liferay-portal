{
  JournalFeed journalFeed=fetchByPrimaryKey(id);
  if (journalFeed == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + id);
    }
    throw new NoSuchFeedException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + id);
  }
  return journalFeed;
}

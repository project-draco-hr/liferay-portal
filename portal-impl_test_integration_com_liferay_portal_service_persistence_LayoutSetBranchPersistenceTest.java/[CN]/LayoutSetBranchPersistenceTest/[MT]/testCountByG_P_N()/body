{
  try {
    _persistence.countByG_P_N(RandomTestUtil.nextLong(),RandomTestUtil.randomBoolean(),StringPool.BLANK);
    _persistence.countByG_P_N(0L,RandomTestUtil.randomBoolean(),StringPool.NULL);
    _persistence.countByG_P_N(0L,RandomTestUtil.randomBoolean(),(String)null);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

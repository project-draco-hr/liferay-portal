{
  HttpServletRequest request=PortalUtil.getHttpServletRequest(portletRequest);
  if (SessionMessages.isEmpty(portletRequest) && SessionMessages.isEmpty(request)) {
    return true;
  }
  return false;
}

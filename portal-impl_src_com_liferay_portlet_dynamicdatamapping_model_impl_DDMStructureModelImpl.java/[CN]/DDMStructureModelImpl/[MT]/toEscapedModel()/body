{
  if (isEscapedModel()) {
    return (DDMStructure)this;
  }
 else {
    return (DDMStructure)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (DDMStructure)this;
  }
 else {
    return (DDMStructure)Proxy.newProxyInstance(DDMStructure.class.getClassLoader(),new Class[]{DDMStructure.class},new AutoEscapeBeanHandler(this));
  }
}

{
  List<String> list=new ArrayList<>();
  list.add("aaa");
  list.add("bbb");
  list.add("ccc");
  List<Object> list2=ListUtil.toList(list);
  Assert.assertArrayEquals(new Object[]{"aaa","bbb","ccc"},list2.toArray());
}

{
  MBCategory newMBCategory=addMBCategory();
  _persistence.clearCache();
  MBCategory existingMBCategory=_persistence.findByPrimaryKey(newMBCategory.getPrimaryKey());
  Assert.assertTrue(Validator.equals(existingMBCategory.getUuid(),ReflectionTestUtil.invoke(existingMBCategory,"getOriginalUuid",new Class<?>[0])));
  Assert.assertEquals(existingMBCategory.getGroupId(),ReflectionTestUtil.invoke(existingMBCategory,"getOriginalGroupId",new Class<?>[0]));
}

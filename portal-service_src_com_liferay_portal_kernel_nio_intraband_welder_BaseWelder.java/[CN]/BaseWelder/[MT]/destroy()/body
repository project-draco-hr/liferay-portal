{
  if (state != State.WELDED) {
    throw new IllegalStateException("Can not destroy a welder at state " + state);
  }
  registrationReference.cancelRegistration();
  doDestroy();
  state=State.DESTROYED;
}

{
  Set<String> tokenIgnoreActions=_serviceRegistrations.keySet();
  String[] tokenIgnoreActionsArray=tokenIgnoreActions.toArray(new String[tokenIgnoreActions.size()]);
  _unregister(tokenIgnoreActionsArray);
}

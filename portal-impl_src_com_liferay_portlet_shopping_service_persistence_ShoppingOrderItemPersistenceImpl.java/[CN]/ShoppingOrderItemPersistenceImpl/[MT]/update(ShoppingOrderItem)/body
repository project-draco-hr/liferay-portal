{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingOrderItem shoppingOrderItem) method. Use update(ShoppingOrderItem shoppingOrderItem, boolean merge) instead.");
  }
  return update(shoppingOrderItem,false);
}

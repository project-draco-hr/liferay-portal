{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingOrderItem shoppingOrderItem) method. To improve performance, call update(ShoppingOrderItem shoppingOrderItem, boolean merge).");
  }
  return update(shoppingOrderItem,false);
}

{
  String className=getClassName();
  String classPK=getClassPK();
  if (Validator.isNotNull(className) && Validator.isNotNull(classPK) && className.equals(Organization.class.getName())) {
    return true;
  }
 else {
    return false;
  }
}

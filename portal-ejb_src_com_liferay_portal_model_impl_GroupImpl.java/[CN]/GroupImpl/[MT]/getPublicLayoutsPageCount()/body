{
  try {
    LayoutSet layoutSet=LayoutSetLocalServiceUtil.getLayoutSet(LayoutImpl.PUBLIC + getGroupId());
    return layoutSet.getPageCount();
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return 0;
}

{
  if (!_reIndex) {
    return;
  }
  UnicodeProperties properties=getAttributeProperties(name);
  if ((getAttributeType(name) == ExpandoColumnConstants.STRING) && GetterUtil.getBoolean(properties.getProperty(ExpandoBridgeIndexer.INDEXABLE))) {
    reIndex();
  }
}

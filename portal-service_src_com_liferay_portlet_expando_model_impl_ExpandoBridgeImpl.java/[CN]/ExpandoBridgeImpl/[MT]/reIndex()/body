{
  if (!_reIndex) {
    return;
  }
  Indexer indexerInstance=IndexerRegistryUtil.getRegisteredIndexer(_className);
  if (indexerInstance != null) {
    try {
      indexerInstance.reIndex(_className,_classPK);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
}

{
  if (obj == null) {
    return false;
  }
  if (!(obj instanceof MethodKey)) {
    return false;
  }
  MethodKey methodKey=(MethodKey)obj;
  if ((_declaringClass == methodKey._declaringClass) && _methodName.equals(methodKey._methodName) && (_parameterTypes.length == methodKey._parameterTypes.length)) {
    for (int i=0; i < _parameterTypes.length; i++) {
      if (_parameterTypes[i] != methodKey._parameterTypes[i]) {
        return false;
      }
    }
    return true;
  }
  return false;
}

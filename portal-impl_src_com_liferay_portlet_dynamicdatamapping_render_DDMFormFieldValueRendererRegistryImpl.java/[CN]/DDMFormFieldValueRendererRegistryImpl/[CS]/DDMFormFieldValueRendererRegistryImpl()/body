{
  Registry registry=RegistryUtil.getRegistry();
  Class<?> clazz=getClass();
  Filter filter=registry.getFilter("(&(objectClass=" + DDMFormFieldValueRenderer.class.getName() + ")(!(objectClass="+ clazz.getName()+ ")))");
  _serviceTracker=registry.trackServices(filter,new DDMFormFieldValueRendererServiceTrackerCustomizer());
  _serviceTracker.open();
}

{
  if (Validator.equals(categoryId,ShoppingCategoryImpl.DEFAULT_PARENT_CATEGORY_ID)) {
    return PortletPermission.contains(permissionChecker,plid,PortletKeys.SHOPPING,actionId);
  }
 else {
    return contains(permissionChecker,categoryId,actionId);
  }
}

{
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(PortletPreferencesImpl.TABLE_NAME,PortletPreferencesImpl.TABLE_COLUMNS,new PKUpgradeColumnImpl());
  upgradeTable.updateTable();
  DBUtil.getInstance().executeSQL(_UPGRADE_SCHEMA);
}

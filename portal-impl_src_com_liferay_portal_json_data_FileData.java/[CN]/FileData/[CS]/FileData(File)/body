{
  byte[] bytes=null;
  try {
    bytes=FileUtil.getBytes(file);
  }
 catch (  IOException ioe) {
    bytes=null;
  }
  _content=Base64.encode(bytes);
  _name=file.getName();
  _size=file.length();
}

{
  try {
    WikiPage page=WikiPageLocalServiceUtil.getPage(_classPK);
    Folder folder=page.addAttachmentsFolder();
    return PortletFileRepositoryUtil.getPortletFileEntry(themeDisplay.getScopeGroupId(),folder.getFolderId(),fileName);
  }
 catch (  PortalException pe) {
    return null;
  }
}

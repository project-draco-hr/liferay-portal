{
  PortalPermissionUtil.check(getPermissionChecker(),ActionKeys.ADD_USER_GROUP);
  User user=getUser();
  return UserGroupLocalServiceUtil.addUserGroup(user.getUserId(),user.getCompanyId(),name,description);
}

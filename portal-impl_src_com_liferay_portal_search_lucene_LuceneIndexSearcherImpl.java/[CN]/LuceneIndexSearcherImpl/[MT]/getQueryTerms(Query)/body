{
  String[] queryTerms=new String[0];
  try {
    queryTerms=LuceneUtil.getQueryTerms(QueryTranslator.translate(query));
  }
 catch (  ParseException pe) {
    _log.error("Query: " + query,pe);
  }
  return queryTerms;
}

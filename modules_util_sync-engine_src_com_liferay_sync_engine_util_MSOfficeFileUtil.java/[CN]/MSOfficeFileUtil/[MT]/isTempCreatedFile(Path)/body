{
  String fileName=String.valueOf(filePath.getFileName());
  if ((fileName.startsWith("~$") || (fileName.startsWith("~") && fileName.endsWith(".tmp"))) && !Files.isDirectory(filePath)) {
    return true;
  }
  return false;
}

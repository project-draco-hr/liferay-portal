{
  LayoutSet layoutSet=group.getPublicLayoutSet();
  List<Layout> initialMergeFailFriendlyURLLayouts=SitesUtil.getMergeFailFriendlyURLLayouts(layoutSet);
  setLinkEnabled(true);
  LayoutTestUtil.addLayout(group.getGroupId(),RandomTestUtil.randomString(),false);
  LayoutTestUtil.addLayout(_layoutSetPrototypeGroup.getGroupId(),RandomTestUtil.randomString(),true);
  propagateChanges(group);
  layoutSet=LayoutSetLocalServiceUtil.getLayoutSet(layoutSet.getLayoutSetId());
  List<Layout> mergeFailFriendlyURLLayouts=SitesUtil.getMergeFailFriendlyURLLayouts(layoutSet);
  Assert.assertEquals(initialMergeFailFriendlyURLLayouts.size() + 1,mergeFailFriendlyURLLayouts.size());
}

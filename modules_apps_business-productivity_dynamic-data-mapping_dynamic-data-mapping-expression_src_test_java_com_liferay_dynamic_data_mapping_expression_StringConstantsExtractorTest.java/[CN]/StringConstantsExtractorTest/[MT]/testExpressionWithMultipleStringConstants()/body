{
  String expressionString="equals(Country, \"Brazil\") || equals(Country, \"US\")";
  List<String> variableNames=_stringConstantsExtractor.extract(expressionString);
  Assert.assertEquals("Brazil",variableNames.get(0));
  Assert.assertEquals("US",variableNames.get(1));
}

{
  int total=_ddlRecordSetService.searchCount(_ddlFormAdminRequestHelper.getCompanyId(),_ddlFormAdminRequestHelper.getScopeGroupId(),getKeywords(),DDLRecordSetConstants.SCOPE_FORMS);
  recordSetSearch.setTotal(total);
}

{
  Website website=(Website)EntityCacheUtil.getResult(WebsiteModelImpl.ENTITY_CACHE_ENABLED,WebsiteImpl.class,websiteId,this);
  if (website == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (website == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      website=(Website)session.get(WebsiteImpl.class,Long.valueOf(websiteId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (website == null)) {
        EntityCacheUtil.putResult(WebsiteModelImpl.ENTITY_CACHE_ENABLED,WebsiteImpl.class,websiteId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(website);
      }
      closeSession(session);
    }
  }
  return website;
}

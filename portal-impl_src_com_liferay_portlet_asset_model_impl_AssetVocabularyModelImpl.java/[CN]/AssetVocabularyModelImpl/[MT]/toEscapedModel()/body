{
  if (isEscapedModel()) {
    return (AssetVocabulary)this;
  }
 else {
    return (AssetVocabulary)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

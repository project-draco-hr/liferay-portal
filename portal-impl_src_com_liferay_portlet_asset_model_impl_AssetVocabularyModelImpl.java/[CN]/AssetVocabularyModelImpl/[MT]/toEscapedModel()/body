{
  if (isEscapedModel()) {
    return (AssetVocabulary)this;
  }
 else {
    return (AssetVocabulary)Proxy.newProxyInstance(AssetVocabulary.class.getClassLoader(),new Class[]{AssetVocabulary.class},new AutoEscapeBeanHandler(this));
  }
}

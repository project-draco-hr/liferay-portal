{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(Team.class,_dynamicQueryClassLoader);
  dynamicQuery.add(RestrictionsFactoryUtil.eq("teamId",RandomTestUtil.nextLong()));
  List<Team> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

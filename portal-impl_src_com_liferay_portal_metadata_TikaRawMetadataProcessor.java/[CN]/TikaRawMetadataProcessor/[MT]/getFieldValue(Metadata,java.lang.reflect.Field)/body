{
  Object fieldValue=null;
  try {
    fieldValue=beanField.get(metadata);
  }
 catch (  IllegalAccessException e) {
    if (_log.isDebugEnabled()) {
      _log.debug("The property " + beanField.getName() + " will not be added to the metatada set");
    }
  }
  return fieldValue;
}

{
  try {
    WikiNode node=ActionUtil.getNode(renderRequest);
    renderRequest.setAttribute(WikiWebKeys.WIKI_NODE,node);
    WikiPage page=ActionUtil.getPage(renderRequest);
    renderRequest.setAttribute(WikiWebKeys.WIKI_PAGE,page);
    ActionUtil.compareVersions(renderRequest,renderResponse,_wikiEngineRenderer);
    renderRequest.setAttribute(WikiWebKeys.WIKI_ENGINE_RENDERER,_wikiEngineRenderer);
  }
 catch (  Exception e) {
    if (e instanceof NoSuchPageException) {
      SessionErrors.add(renderRequest,e.getClass());
      return "/wiki/error.jsp";
    }
 else {
      throw new PortletException(e);
    }
  }
  return "/wiki/compare_versions.jsp";
}

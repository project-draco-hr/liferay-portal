{
  if (_connector.equals(CONNECTOR_IFRAME) || renderRequest.getWindowState().equals(WindowState.MAXIMIZED)) {
    invokeApplication(renderRequest,renderResponse);
  }
 else {
    renderNormalWindowState(renderRequest,renderResponse);
  }
}

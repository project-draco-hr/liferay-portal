{
  UpgradePortletId upgradePortletId=new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"184",SitesDirectoryPortletKeys.SITES_DIRECTORY}};
    }
  }
;
  _releaseLocalService.updateRelease("com.liferay.site.navigation.directory.web",Collections.<UpgradeProcess>singletonList(upgradePortletId),1,1,false);
}

{
  if ((tagIds.length == 0) && (notTagIds.length == 0)) {
    return assetFinder.findAssets(groupId,classNameIds,null,null,null,null,excludeZeroViewCount,publishDate,expirationDate,start,end);
  }
 else   if (andOperator) {
    return assetFinder.findByAndTagIds(groupId,classNameIds,tagIds,notTagIds,null,null,null,null,excludeZeroViewCount,publishDate,expirationDate,start,end);
  }
 else {
    return assetFinder.findByOrTagIds(groupId,classNameIds,tagIds,notTagIds,null,null,null,null,excludeZeroViewCount,publishDate,expirationDate,start,end);
  }
}

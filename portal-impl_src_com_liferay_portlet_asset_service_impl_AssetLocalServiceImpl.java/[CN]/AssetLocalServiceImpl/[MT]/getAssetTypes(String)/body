{
  AssetType[] assetTypes=new AssetType[AssetUtil.ASSET_TYPE_CLASS_NAMES.length];
  for (int i=0; i < AssetUtil.ASSET_TYPE_CLASS_NAMES.length; i++) {
    assetTypes[i]=getAssetType(AssetUtil.ASSET_TYPE_CLASS_NAMES[i],languageId);
  }
  return assetTypes;
}

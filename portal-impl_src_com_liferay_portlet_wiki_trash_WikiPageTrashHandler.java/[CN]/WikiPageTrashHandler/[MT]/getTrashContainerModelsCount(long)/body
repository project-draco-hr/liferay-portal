{
  WikiPage page=WikiPageLocalServiceUtil.getPage(classPK);
  List<WikiPage> childPages=WikiPageLocalServiceUtil.getTrashedChildren(page.getNodeId(),true,page.getTitle());
  return childPages.size();
}

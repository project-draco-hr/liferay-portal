{
  if (groupId == TestPropsValues.getGroupId()) {
    return getServiceContext(TestPropsValues.getCompanyId(),groupId,userId);
  }
 else {
    Group group=GroupLocalServiceUtil.getGroup(groupId);
    return getServiceContext(group.getCompanyId(),group.getGroupId(),userId);
  }
}

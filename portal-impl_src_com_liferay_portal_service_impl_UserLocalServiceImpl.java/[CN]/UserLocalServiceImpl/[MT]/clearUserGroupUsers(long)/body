{
  userGroupPersistence.clearUsers(userGroupId);
  try {
    UserIndexer.updateUsers(userLocalService.getUserGroupUsers(userGroupId));
  }
 catch (  SearchException se) {
    _log.error("Indexing users of user group " + String.valueOf(userGroupId),se);
  }
  PermissionCacheUtil.clearCache();
}

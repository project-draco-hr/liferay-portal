{
  try {
    doSendPasswordResetLink(companyId,emailAddress,remoteAddr,remoteHost,userAgent,fromName,fromAddress,subject,body,serviceContext);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

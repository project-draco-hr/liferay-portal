{
  String[] parts=StringUtil.split(s,delimeter);
  int i=1;
  while (i < parts.length) {
    s=StringUtil.replaceFirst(s,delimeter + parts[i] + delimeter,StringPool.BLANK);
    i=i + 2;
  }
  return s;
}

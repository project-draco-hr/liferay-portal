{
  if (obj == null) {
    return -1;
  }
  BlogsCategory blogsCategory=(BlogsCategory)obj;
  int value=0;
  value=getName().compareTo(blogsCategory.getName());
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  if (obj == null) {
    return -1;
  }
  BlogsCategory blogsCategory=(BlogsCategory)obj;
  int value=0;
  value=getName().toLowerCase().compareTo(blogsCategory.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  QueryDefinition queryDefinition=new QueryDefinition();
  queryDefinition.setStatus(WorkflowConstants.STATUS_ANY);
  Assert.assertEquals(4,doCountBy_G_U_F_R_M_BothRepositories(_folderDefaultRepository.getUserId(),null,queryDefinition));
}

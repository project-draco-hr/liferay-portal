{
  List<Long> repositoryIds=ListUtil.toList(new long[]{_folderNewRepository.getRepositoryId()});
  List<Long> folderIds=ListUtil.toList(new long[]{_folderNewRepository.getFolderId()});
  return doFindBy_G_U_R_F_M(userId,repositoryIds,folderIds,mimeType,queryDefinition);
}

{
  QueryDefinition queryDefinition=new QueryDefinition();
  queryDefinition.setStatus(WorkflowConstants.STATUS_ANY);
  Assert.assertEquals(2,doCountBy_G_U_F_R_M_DefaultRepository(_folderDefaultRepository.getUserId(),null,queryDefinition));
}

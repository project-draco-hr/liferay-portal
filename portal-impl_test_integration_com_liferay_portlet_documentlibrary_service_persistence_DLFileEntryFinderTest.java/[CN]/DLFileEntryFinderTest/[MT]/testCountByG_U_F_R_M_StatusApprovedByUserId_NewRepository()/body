{
  QueryDefinition queryDefinition=new QueryDefinition();
  queryDefinition.setStatus(WorkflowConstants.STATUS_APPROVED);
  Assert.assertEquals(1,doCountBy_G_U_F_R_M_NewRepository(_folderDefaultRepository.getUserId(),null,queryDefinition));
}

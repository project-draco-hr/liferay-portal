{
  List<Long> folderIds=ListUtil.toList(new long[]{_folder.getFolderId()});
  String[] mimeTypes=null;
  if (mimeType != null) {
    mimeTypes=new String[]{mimeType};
  }
  return DLFileEntryFinderUtil.countByG_U_F_M(_group.getGroupId(),userId,folderIds,mimeTypes,queryDefinition);
}

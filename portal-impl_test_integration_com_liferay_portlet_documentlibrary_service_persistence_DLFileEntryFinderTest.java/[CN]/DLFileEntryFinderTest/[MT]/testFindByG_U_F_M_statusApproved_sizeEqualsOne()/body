{
  QueryDefinition queryDefinition=getQueryDefinition(WorkflowConstants.STATUS_APPROVED);
  List<DLFileEntry> dlFileEntries=doFindBy_G_U_F_M(_folder.getUserId(),null,queryDefinition);
  Assert.assertEquals(1,dlFileEntries.size());
}

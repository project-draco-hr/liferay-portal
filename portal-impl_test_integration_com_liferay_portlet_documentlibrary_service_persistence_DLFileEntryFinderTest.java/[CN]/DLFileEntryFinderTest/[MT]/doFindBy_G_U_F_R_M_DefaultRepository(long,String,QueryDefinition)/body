{
  List<Long> folderIds=ListUtil.toList(new long[]{_folderDefaultRepository.getFolderId()});
  List<Long> repositoryIds=ListUtil.toList(new long[]{_folderDefaultRepository.getRepositoryId()});
  return doFindBy_G_U_F_R_M(userId,folderIds,repositoryIds,mimeType,queryDefinition);
}

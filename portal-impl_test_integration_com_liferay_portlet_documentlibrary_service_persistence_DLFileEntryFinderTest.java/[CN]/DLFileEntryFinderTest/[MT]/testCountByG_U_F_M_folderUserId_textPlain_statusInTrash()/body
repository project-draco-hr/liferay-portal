{
  QueryDefinition queryDefinition=getQueryDefinition(WorkflowConstants.STATUS_IN_TRASH,true);
  Assert.assertEquals(0,doCountBy_G_U_F_M(_folder.getUserId(),ContentTypes.TEXT_PLAIN,queryDefinition));
}

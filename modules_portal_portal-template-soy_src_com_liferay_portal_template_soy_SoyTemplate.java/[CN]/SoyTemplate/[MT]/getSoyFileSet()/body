{
  SoyFileSet soyFileSet=null;
  if (_privileged) {
    soyFileSet=AccessController.doPrivileged(new TemplatePrivilegedExceptionAction(templateResource));
  }
 else {
    _builder.add(getTemplateResourceURL(templateResource));
    soyFileSet=_builder.build();
  }
  return soyFileSet;
}

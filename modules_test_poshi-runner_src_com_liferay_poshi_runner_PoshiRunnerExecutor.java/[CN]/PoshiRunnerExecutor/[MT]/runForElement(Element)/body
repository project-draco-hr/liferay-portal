{
  String list=PoshiRunnerVariablesUtil.replaceCommandVars(element.attributeValue("list"));
  String param=PoshiRunnerVariablesUtil.replaceCommandVars(element.attributeValue("param"));
  if (!list.isEmpty()) {
    String[] listArray=list.split(",");
    for (    String item : listArray) {
      PoshiRunnerVariablesUtil.putIntoCommandMap(param,item);
      parseElement(element);
    }
  }
}

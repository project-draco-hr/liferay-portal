{
  _log.debug("Borrowing:\t" + _instance._pool.getNumIdle() + "\t"+ _instance._pool.getNumActive());
  PermissionChecker permissionChecker=(PermissionChecker)_instance._pool.borrowObject();
  permissionChecker.init(user,signedIn);
  return permissionChecker;
}

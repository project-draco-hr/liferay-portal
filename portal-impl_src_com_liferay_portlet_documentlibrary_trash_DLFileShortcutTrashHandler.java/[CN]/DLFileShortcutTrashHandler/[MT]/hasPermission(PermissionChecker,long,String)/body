{
  FileShortcut fileShortcut=getFileShortcut(classPK);
  if (fileShortcut.isInHiddenFolder() && actionId.equals(ActionKeys.VIEW)) {
    return false;
  }
  return DLFileShortcutPermission.contains(permissionChecker,classPK,actionId);
}

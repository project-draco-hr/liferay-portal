{
  InputStream is=servletContext.getResourceAsStream("/WEB-INF/classes/META-INF/portlet-hbm.xml");
  if (is == null) {
    return;
  }
  Document doc=SAXReaderUtil.read(is);
  Element root=doc.getRootElement();
  List<Element> classEls=root.elements("class");
  for (  Element classEl : classEls) {
    String name=classEl.attributeValue("name");
    CacheRegistry.unregister(name);
  }
  CacheRegistry.clear();
  EntityCacheUtil.clearCache();
  FinderCacheUtil.clearCache();
}

{
  InputStream inputStream=servletContext.getResourceAsStream("/META-INF/" + path);
  if (inputStream == null) {
    inputStream=servletContext.getResourceAsStream("/WEB-INF/" + path);
  }
  return inputStream;
}

{
  if (parameters.length != 1) {
    throw new IllegalArgumentException(String.format("Expected 1 parameters, received %d",parameters.length));
  }
  return Validator.isEmailAddress(parameters[0].toString());
}

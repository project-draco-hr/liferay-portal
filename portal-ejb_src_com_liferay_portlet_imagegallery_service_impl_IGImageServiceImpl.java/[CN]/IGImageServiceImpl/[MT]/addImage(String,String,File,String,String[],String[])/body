{
  IGFolderPermission.check(getPermissionChecker(),folderId,ActionKeys.ADD_IMAGE);
  return IGImageLocalServiceUtil.addImage(getUserId(),folderId,description,file,contentType,communityPermissions,guestPermissions);
}

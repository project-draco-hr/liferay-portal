{
  try (Connection con=DataAccess.getConnection();Statement statement=con.createStatement();ResultSet rs=statement.executeQuery(_READ_RELEASE_MODIFIED_DATE)){
    Assert.assertTrue(rs.next());
    Timestamp timestamp=rs.getTimestamp("modifiedDate");
    Assert.assertFalse(rs.next());
    return timestamp.getTime();
  }
 }

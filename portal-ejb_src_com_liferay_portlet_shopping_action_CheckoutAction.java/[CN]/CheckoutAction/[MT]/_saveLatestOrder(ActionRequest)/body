{
  ShoppingCart cart=ShoppingCartServiceUtil.getCart(req.getPortletSession().getId(),PortalUtil.getCompanyId(req));
  ShoppingOrder order=ShoppingOrderServiceUtil.saveLatestOrder(cart);
  req.setAttribute(WebKeys.SHOPPING_ORDER,order);
}

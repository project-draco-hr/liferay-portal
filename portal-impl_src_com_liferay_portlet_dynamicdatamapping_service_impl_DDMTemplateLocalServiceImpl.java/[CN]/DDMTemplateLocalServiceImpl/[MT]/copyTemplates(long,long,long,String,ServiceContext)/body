{
  List<DDMTemplate> newTemplates=new ArrayList<DDMTemplate>();
  List<DDMTemplate> oldTemplates=getTemplates(structureId,type);
  for (  DDMTemplate oldTemplate : oldTemplates) {
    DDMTemplate newTemplate=addTemplate(userId,oldTemplate.getGroupId(),newStructureId,oldTemplate.getNameMap(),oldTemplate.getDescriptionMap(),oldTemplate.getType(),oldTemplate.getMode(),oldTemplate.getLanguage(),oldTemplate.getScript(),serviceContext);
    newTemplates.add(newTemplate);
  }
  return newTemplates;
}

{
  Hits hits=LuceneSearchEngineUtil.search(searchRequest.getCompanyId(),searchRequest.getQuery(),searchRequest.getSorts(),searchRequest.getStart(),searchRequest.getEnd());
  message.setPayload(hits);
  _messageSender.send(message.getResponseDestination(),message);
}

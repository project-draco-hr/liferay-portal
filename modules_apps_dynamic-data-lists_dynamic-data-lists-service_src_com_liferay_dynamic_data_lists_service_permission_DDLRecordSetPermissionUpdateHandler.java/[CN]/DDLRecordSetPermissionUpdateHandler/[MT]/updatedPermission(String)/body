{
  DDLRecordSet recordSet=_ddlRecordSetLocalService.fetchDDLRecordSet(GetterUtil.getLong(primKey));
  if (recordSet == null) {
    return;
  }
  recordSet.setModifiedDate(new Date());
  _ddlRecordSetLocalService.updateDDLRecordSet(recordSet);
}

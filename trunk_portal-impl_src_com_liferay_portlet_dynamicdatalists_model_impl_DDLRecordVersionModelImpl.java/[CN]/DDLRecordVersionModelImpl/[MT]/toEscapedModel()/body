{
  if (isEscapedModel()) {
    return (DDLRecordVersion)this;
  }
 else {
    return (DDLRecordVersion)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

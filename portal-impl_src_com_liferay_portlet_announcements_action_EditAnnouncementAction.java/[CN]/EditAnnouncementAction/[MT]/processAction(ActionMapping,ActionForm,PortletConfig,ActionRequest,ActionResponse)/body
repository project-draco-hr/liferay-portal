{
  String cmd=ParamUtil.getString(req,Constants.CMD);
  PortletPreferences prefs=PortletPreferencesFactoryUtil.getPortletSetup(req,PortletKeys.ANNOUNCEMENTS);
  try {
    if (cmd.equals(Constants.ADD) || cmd.equals(Constants.UPDATE)) {
      updateAnnouncement(req,prefs);
    }
 else     if (cmd.equals(Constants.DELETE)) {
      deleteAnnouncement(req);
    }
    String redirect=ParamUtil.getString(req,"redirect");
    res.sendRedirect(redirect);
  }
 catch (  Exception e) {
    if (e instanceof AnnouncementContentException || e instanceof AnnouncementDisplayDateException || e instanceof AnnouncementExpirationDateException|| e instanceof AnnouncementTitleException|| e instanceof NoSuchEntryException|| e instanceof PrincipalException) {
      SessionErrors.add(req,e.getClass().getName());
    }
 else {
      throw e;
    }
  }
}

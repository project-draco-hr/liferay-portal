{
  List<StagedModel> stagedModels=new ArrayList<StagedModel>();
  stagedModels.add(BlogsTestUtil.addEntry(TestPropsValues.getUserId(),group,true));
  stagedModels.add(BlogsTestUtil.addEntry(TestPropsValues.getUserId(),group,false));
  return stagedModels;
}

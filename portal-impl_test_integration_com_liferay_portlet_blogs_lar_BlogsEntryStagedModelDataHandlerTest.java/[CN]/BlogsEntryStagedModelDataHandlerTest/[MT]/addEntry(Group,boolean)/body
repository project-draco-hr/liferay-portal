{
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(group,TestPropsValues.getUserId());
  if (approved) {
    return BlogsEntryLocalServiceUtil.addEntry(TestPropsValues.getUserId(),RandomTestUtil.randomString(),RandomTestUtil.randomString(),serviceContext);
  }
  return BlogsTestUtil.addEntry(TestPropsValues.getUserId(),RandomTestUtil.randomString(),false,false,serviceContext);
}

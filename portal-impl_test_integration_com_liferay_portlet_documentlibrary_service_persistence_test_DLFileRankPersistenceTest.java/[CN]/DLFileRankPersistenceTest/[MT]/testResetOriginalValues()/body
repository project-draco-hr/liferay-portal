{
  DLFileRank newDLFileRank=addDLFileRank();
  _persistence.clearCache();
  DLFileRank existingDLFileRank=_persistence.findByPrimaryKey(newDLFileRank.getPrimaryKey());
  Assert.assertEquals(existingDLFileRank.getCompanyId(),ReflectionTestUtil.invoke(existingDLFileRank,"getOriginalCompanyId",new Class<?>[0]));
  Assert.assertEquals(existingDLFileRank.getUserId(),ReflectionTestUtil.invoke(existingDLFileRank,"getOriginalUserId",new Class<?>[0]));
  Assert.assertEquals(existingDLFileRank.getFileEntryId(),ReflectionTestUtil.invoke(existingDLFileRank,"getOriginalFileEntryId",new Class<?>[0]));
}

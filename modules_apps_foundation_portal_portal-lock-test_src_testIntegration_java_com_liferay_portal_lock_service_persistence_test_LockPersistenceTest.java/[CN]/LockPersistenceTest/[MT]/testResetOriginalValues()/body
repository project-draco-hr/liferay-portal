{
  Lock newLock=addLock();
  _persistence.clearCache();
  Lock existingLock=_persistence.findByPrimaryKey(newLock.getPrimaryKey());
  Assert.assertTrue(Validator.equals(existingLock.getClassName(),ReflectionTestUtil.invoke(existingLock,"getOriginalClassName",new Class<?>[0])));
  Assert.assertTrue(Validator.equals(existingLock.getKey(),ReflectionTestUtil.invoke(existingLock,"getOriginalKey",new Class<?>[0])));
}

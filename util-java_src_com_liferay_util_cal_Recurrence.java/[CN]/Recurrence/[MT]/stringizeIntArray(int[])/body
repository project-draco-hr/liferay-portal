{
  if (a == null) {
    return "null";
  }
  StringBuffer buffer=new StringBuffer();
  int i;
  buffer.append("[");
  for (i=0; i < a.length; i++) {
    if (i != 0) {
      buffer.append(",");
    }
    buffer.append(a[i]);
  }
  buffer.append("]");
  return buffer.toString();
}

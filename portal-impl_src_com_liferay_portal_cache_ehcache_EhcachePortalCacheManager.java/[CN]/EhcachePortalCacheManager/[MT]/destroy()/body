{
  try {
    PortalCacheProvider.unregisterPortalCacheManager(_cacheManager.getName());
    _portalCaches.clear();
    _cacheManager.shutdown();
  }
  finally {
    if (_managementService != null) {
      _managementService.dispose();
    }
  }
}

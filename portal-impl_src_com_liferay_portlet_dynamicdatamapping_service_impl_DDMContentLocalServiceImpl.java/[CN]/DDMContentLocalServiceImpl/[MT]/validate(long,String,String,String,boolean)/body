{
  if (!autoContentKey) {
    validate(contentKey);
    DDMContent content=ddmContentPersistence.fetchByG_C(groupId,contentKey);
    if (content != null) {
      throw new ContentDuplicateContentKeyException();
    }
  }
  validate(groupId,name,xml);
}

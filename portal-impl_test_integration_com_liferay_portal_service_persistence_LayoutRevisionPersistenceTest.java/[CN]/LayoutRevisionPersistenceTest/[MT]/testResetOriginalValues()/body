{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  LayoutRevision newLayoutRevision=addLayoutRevision();
  _persistence.clearCache();
  LayoutRevisionModelImpl existingLayoutRevisionModelImpl=(LayoutRevisionModelImpl)_persistence.findByPrimaryKey(newLayoutRevision.getPrimaryKey());
  assertEquals(existingLayoutRevisionModelImpl.getLayoutSetBranchId(),existingLayoutRevisionModelImpl.getOriginalLayoutSetBranchId());
  assertEquals(existingLayoutRevisionModelImpl.getHead(),existingLayoutRevisionModelImpl.getOriginalHead());
  assertEquals(existingLayoutRevisionModelImpl.getPlid(),existingLayoutRevisionModelImpl.getOriginalPlid());
}

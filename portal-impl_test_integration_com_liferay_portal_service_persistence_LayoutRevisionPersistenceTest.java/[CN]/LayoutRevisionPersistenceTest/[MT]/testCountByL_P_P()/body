{
  try {
    _persistence.countByL_P_P(RandomTestUtil.nextLong(),RandomTestUtil.nextLong(),RandomTestUtil.nextLong());
    _persistence.countByL_P_P(0L,0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

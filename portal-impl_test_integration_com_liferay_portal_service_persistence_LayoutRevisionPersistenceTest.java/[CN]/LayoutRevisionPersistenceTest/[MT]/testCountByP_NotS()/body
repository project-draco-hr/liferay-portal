{
  try {
    _persistence.countByP_NotS(ServiceTestUtil.nextLong(),ServiceTestUtil.nextInt());
    _persistence.countByP_NotS(0L,0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

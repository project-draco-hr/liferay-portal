{
  int count=countByG_U_C_S(groupId,userId,categoryId,status);
  if (count == 0) {
    return null;
  }
  List<MBMessage> list=findByG_U_C_S(groupId,userId,categoryId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

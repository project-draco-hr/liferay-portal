{
  for (  MBMessage mbMessage : mbMessages) {
    if (EntityCacheUtil.getResult(MBMessageModelImpl.ENTITY_CACHE_ENABLED,MBMessage.class,mbMessage.getPrimaryKey(),this) == null) {
      cacheResult(mbMessage);
    }
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBMessage mbMessage) method. To improve performance, call update(MBMessage mbMessage, boolean merge).");
  }
  return update(mbMessage,false);
}

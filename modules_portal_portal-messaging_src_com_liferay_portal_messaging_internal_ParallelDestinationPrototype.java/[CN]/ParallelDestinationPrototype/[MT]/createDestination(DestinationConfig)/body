{
  ParallelDestination parallelDestination=new ParallelDestination();
  parallelDestination.setName(destinationConfig.getDestinationName());
  parallelDestination.setMaximumQueueSize(destinationConfig.getMaximumQueueSize());
  parallelDestination.setRejectedExecutionHandler(destinationConfig.getRejectedExecutionHandler());
  parallelDestination.setWorkersCoreSize(destinationConfig.getWorkersCoreSize());
  parallelDestination.setWorkersMaxSize(destinationConfig.getWorkersMaxSize());
  parallelDestination.afterPropertiesSet();
  return parallelDestination;
}

{
  revisionLocalService.deleteRevisionsByBranch(branch.getBranchId());
  resourceLocalService.deleteResource(branch.getCompanyId(),Branch.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,branch.getBranchId());
  branchPersistence.remove(branch);
}

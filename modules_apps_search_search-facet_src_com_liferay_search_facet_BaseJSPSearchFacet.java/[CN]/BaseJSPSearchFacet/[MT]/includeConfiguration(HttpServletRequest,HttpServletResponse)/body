{
  if (Validator.isNull(getConfigurationJspPath())) {
    return;
  }
  RequestDispatcher requestDispatcher=_servletContext.getRequestDispatcher(getConfigurationJspPath());
  try {
    requestDispatcher.include(request,response);
  }
 catch (  ServletException se) {
    _log.error("Unable to include JSP",se);
    throw new IOException("Unable to include " + getDisplayJspPath(),se);
  }
}

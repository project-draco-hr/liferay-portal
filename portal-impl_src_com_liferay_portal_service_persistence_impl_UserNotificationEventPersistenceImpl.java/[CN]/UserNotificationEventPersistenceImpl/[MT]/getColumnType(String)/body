{
  Integer type=UserNotificationEventModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ UserNotificationEventModelImpl.TABLE_NAME);
  }
  return type;
}

{
  String pluginVersion=syncContext.getPluginVersion();
  Matcher matcher=_pattern.matcher(pluginVersion);
  if (!matcher.find()) {
    return false;
  }
  if (pluginVersion.startsWith("6.2") && (Integer.parseInt(matcher.group(1)) < 3)) {
    return false;
  }
  return true;
}

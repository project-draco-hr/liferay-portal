{
  _flushTest(new Callable<Void>(){
    @Override public Void call() throws Exception {
      _session.merge(_className1);
      _session.merge(_className2);
      SQLQuery query=_session.createSynchronizedSQLQuery("SELECT * FROM ClassName_");
      query.list();
      return null;
    }
  }
);
}

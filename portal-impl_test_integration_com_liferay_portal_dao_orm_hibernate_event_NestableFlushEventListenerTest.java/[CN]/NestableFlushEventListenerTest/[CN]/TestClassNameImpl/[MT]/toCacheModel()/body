{
  SQLQuery query=_session.createSynchronizedSQLQuery("SELECT * FROM Release_");
  try {
    query.list();
  }
 catch (  ORMException orme) {
    if (!_isCausedByException(orme,StaleStateException.class)) {
      throw orme;
    }
  }
  return super.toCacheModel();
}

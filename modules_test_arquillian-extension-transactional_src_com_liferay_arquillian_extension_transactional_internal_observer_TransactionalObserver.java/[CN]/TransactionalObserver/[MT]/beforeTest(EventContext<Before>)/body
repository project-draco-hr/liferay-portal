{
  _transactionAttribute=getTransactionAttribute(eventContext.getEvent());
  if (_transactionAttribute != null) {
    _transactionStatus=TransactionInvokerUtil.start(_transactionAttribute);
  }
  eventContext.proceed();
}

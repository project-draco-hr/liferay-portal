{
  long[] userIds=addUsers();
  long[] standardSiteIds=addStandardSites();
  long[] standardRoleIds=addStandardRoles();
  Assert.assertTrue(SiteMembershipPolicyUtil.isRoleAllowed(userIds[0],standardSiteIds[0],standardRoleIds[0]));
}

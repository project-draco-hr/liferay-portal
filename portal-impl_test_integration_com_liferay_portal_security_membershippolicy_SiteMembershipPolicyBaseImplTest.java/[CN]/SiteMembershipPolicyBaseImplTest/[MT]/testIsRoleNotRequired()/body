{
  long[] userIds=addUsers();
  long[] standardSiteIds=addStandardSites();
  long[] standardRoleIds=addStandardRoles();
  Assert.assertFalse(SiteMembershipPolicyUtil.isRoleRequired(userIds[0],standardSiteIds[0],standardRoleIds[0]));
}

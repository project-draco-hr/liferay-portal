{
  if (isEscapedModel()) {
    return (SocialRequest)this;
  }
 else {
    return (SocialRequest)Proxy.newProxyInstance(SocialRequest.class.getClassLoader(),new Class[]{SocialRequest.class},new AutoEscapeBeanHandler(this));
  }
}

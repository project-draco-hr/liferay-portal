{
  if (isEscapedModel()) {
    return (SocialRequest)this;
  }
 else {
    return (SocialRequest)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isNew) {
    Object[] args=new Object[]{announcementsFlag.getUserId(),announcementsFlag.getEntryId(),announcementsFlag.getValue()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_U_E_V,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_U_E_V,args,announcementsFlag);
  }
 else {
    AnnouncementsFlagModelImpl announcementsFlagModelImpl=(AnnouncementsFlagModelImpl)announcementsFlag;
    if ((announcementsFlagModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_U_E_V.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{announcementsFlag.getUserId(),announcementsFlag.getEntryId(),announcementsFlag.getValue()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_U_E_V,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_U_E_V,args,announcementsFlag);
    }
  }
}

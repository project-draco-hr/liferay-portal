{
  for (long i=0; i < sourceIndexInput.length(); i++) {
    Assert.assertEquals(fileName + " has different source and target byte value at " + i,sourceIndexInput.readByte(),targetIndexInput.readByte());
  }
  sourceIndexInput.close();
  targetIndexInput.close();
}

{
  PortletPreferences siteDefaultSettings=new PortletPreferencesImpl();
  siteDefaultSettings.setValue(name,value);
  InstancePortletSettings instancePortletSettings=(InstancePortletSettings)portletSettings;
  instancePortletSettings.setSiteDefaults(siteDefaultSettings);
}

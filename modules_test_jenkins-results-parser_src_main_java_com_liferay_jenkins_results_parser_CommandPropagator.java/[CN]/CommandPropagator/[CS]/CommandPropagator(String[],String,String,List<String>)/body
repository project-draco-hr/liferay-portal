{
  for (  String fileName : fileNames) {
    _commandPropagatorTasks.add(new CommandPropagatorTask(sourceDirName + "/" + fileName,targetDirName + "/" + fileName));
  }
  _targetSlaves.addAll(targetSlaves);
}

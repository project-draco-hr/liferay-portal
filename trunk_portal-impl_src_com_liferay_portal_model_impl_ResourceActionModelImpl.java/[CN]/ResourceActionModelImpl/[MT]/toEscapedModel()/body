{
  if (isEscapedModel()) {
    return (ResourceAction)this;
  }
 else {
    return (ResourceAction)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

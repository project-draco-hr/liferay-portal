{
  if (_lifecycle.equals(PortletRequest.ACTION_PHASE) || _lifecycle.equals(PortletRequest.EVENT_PHASE)) {
    return new NullServletOutputStream();
  }
 else {
    return super.getOutputStream();
  }
}

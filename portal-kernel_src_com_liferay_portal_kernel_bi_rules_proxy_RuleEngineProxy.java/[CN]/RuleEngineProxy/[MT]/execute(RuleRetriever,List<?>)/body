{
  ExecutionRequestMessage executionRequestMessage=new ExecutionRequestMessage(ExecutionRequestType.EXECUTE,ruleRetriever);
  executionRequestMessage.addFact(facts);
  _executionRequestMessageSender.send(executionRequestMessage);
}

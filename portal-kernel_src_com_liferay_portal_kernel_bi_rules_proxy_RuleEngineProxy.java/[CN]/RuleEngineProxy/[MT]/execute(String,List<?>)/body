{
  ExecutionRequestMessage requestMessage=new ExecutionRequestMessage(ExecutionRequestType.EXECUTE,domainName);
  requestMessage.addFact(facts);
  Message message=new Message();
  message.setPayload(requestMessage);
  _asyncExecutionSender.send(message);
}

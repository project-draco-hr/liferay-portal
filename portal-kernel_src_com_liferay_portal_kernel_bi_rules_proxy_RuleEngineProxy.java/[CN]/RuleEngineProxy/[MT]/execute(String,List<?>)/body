{
  ExecutionRequestMessage executionRequestMessage=new ExecutionRequestMessage(ExecutionRequestType.EXECUTE,domainName);
  executionRequestMessage.addFact(facts);
  _executionRequestMessageSender.send(executionRequestMessage);
}

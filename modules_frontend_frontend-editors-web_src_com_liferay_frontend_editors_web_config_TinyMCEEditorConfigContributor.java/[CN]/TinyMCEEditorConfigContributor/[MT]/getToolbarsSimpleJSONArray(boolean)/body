{
  JSONArray jsonArray=JSONFactoryUtil.createJSONArray();
  String firstRowButtons="bold italic underline strikethrough | bullist numlist | table | " + "link unlink image";
  if (showSource) {
    firstRowButtons+=" code";
  }
  jsonArray.put(firstRowButtons);
  return jsonArray;
}

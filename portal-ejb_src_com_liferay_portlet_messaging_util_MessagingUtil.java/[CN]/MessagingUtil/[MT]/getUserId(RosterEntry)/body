{
  String userId=entry.getUser();
  String serverName=GetterUtil.getString(PropsUtil.get(PropsUtil.JABBER_XMPP_SERVER_NAME),"localhost");
  userId=StringUtil.replace(userId,"@" + serverName,"");
  return userId;
}

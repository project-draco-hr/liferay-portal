{
  int count=_backgroundTaskManager.getBackgroundTasksCount(CompanyConstants.SYSTEM,new String[]{ReindexPortalBackgroundTaskExecutor.class.getName(),ReindexSingleIndexerBackgroundTaskExecutor.class.getName()},false);
  if (count == 0) {
    return false;
  }
  return super.hasAccessPermission(request);
}

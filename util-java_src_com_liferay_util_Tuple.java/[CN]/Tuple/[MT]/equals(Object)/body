{
  if (!(x instanceof Tuple)) {
    return false;
  }
  Tuple tuple=(Tuple)x;
  if (tuple._objArr.length != _objArr.length) {
    return false;
  }
  for (int i=0; i < _objArr.length; i++) {
    if ((tuple._objArr != null) && (_objArr[i] != null) && !_objArr[i].equals(tuple._objArr[i])) {
      return false;
    }
 else     if ((tuple._objArr[i]) == null || (_objArr[i] == null)) {
      return false;
    }
  }
  return true;
}

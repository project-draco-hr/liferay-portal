{
  SAPEntry sapEntry=sapEntryLocalService.fetchSAPEntry(companyId,name);
  if (sapEntry != null) {
    SAPEntryPermission.check(getPermissionChecker(),sapEntry,ActionKeys.VIEW);
  }
  return sapEntry;
}

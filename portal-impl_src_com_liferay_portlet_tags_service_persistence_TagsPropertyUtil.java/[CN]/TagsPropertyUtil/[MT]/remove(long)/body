{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(propertyId));
  }
  com.liferay.portlet.tags.model.TagsProperty tagsProperty=getPersistence().remove(propertyId);
  if (listener != null) {
    listener.onAfterRemove(tagsProperty);
  }
  return tagsProperty;
}

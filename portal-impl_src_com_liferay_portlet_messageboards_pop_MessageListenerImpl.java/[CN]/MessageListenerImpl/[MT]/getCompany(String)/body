{
  int pos=recipient.indexOf(StringPool.AT) + MBUtil.POP_SERVER_SUBDOMAIN_LENGTH + 1;
  if (MBUtil.POP_SERVER_SUBDOMAIN_LENGTH > 0) {
    pos++;
  }
  String mx=recipient.substring(pos);
  return CompanyLocalServiceUtil.getCompanyByMx(mx);
}

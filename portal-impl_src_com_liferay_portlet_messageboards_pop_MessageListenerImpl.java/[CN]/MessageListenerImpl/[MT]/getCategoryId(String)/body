{
  int pos=recipient.indexOf(CharPool.AT);
  String target=recipient.substring(MBUtil.MESSAGE_POP_PORTLET_PREFIX.length() + getOffset(),pos);
  String[] parts=StringUtil.split(target,StringPool.PERIOD);
  return GetterUtil.getLong(parts[0]);
}

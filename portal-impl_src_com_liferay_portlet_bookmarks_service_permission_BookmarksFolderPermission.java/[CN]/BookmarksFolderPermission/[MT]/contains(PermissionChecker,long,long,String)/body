{
  if (folderId == BookmarksFolderImpl.DEFAULT_PARENT_FOLDER_ID) {
    return PortletPermissionUtil.contains(permissionChecker,plid,PortletKeys.BOOKMARKS,actionId);
  }
 else {
    return contains(permissionChecker,folderId,actionId);
  }
}

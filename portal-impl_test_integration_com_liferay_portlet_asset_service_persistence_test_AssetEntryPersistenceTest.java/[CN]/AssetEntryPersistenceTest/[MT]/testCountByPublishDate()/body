{
  try {
    _persistence.countByPublishDate(RandomTestUtil.nextDate());
    _persistence.countByPublishDate(RandomTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

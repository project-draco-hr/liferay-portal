{
  TestAuthPublicPath testAuthPublicPath=new TestAuthPublicPath();
  Assert.assertTrue(AuthPublicPathRegistry.contains(testAuthPublicPath.path()));
  Assert.assertFalse(AuthPublicPathRegistry.contains("/unregistered/unknown/path"));
}

{
  String sql="SELECT count(content) FROM Content content WHERE " + "content.companyId = ? AND content.repositoryId = ? AND " + "content.path = ? AND content.version = ?";
  Session session=null;
  try {
    session=openSession();
    Query q=session.createQuery(sql);
    QueryPos qPos=QueryPos.getInstance(q);
    qPos.add(companyId);
    qPos.add(repositoryId);
    qPos.add(path);
    qPos.add(version);
    return ((Number)q.uniqueResult()).intValue();
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

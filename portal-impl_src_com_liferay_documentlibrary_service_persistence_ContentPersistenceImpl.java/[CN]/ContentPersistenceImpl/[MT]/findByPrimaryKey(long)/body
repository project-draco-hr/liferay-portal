{
  Content content=fetchByPrimaryKey(contentId);
  if (content == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No Content exists with the primary key " + contentId);
    }
    throw new NoSuchContentException("No Content exists with the primary key " + contentId);
  }
  return content;
}

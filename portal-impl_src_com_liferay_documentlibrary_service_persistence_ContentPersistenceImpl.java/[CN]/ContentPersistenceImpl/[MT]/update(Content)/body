{
  Session session=null;
  try {
    session=openSession();
    session.saveOrUpdate(content);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    session.saveOrUpdate(content);
    session.flush();
    session.clear();
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (!AssetUtil.isValidWord(key)) {
    throw new CategoryPropertyKeyException("Key is not a valid word {key=" + key + "}");
  }
  if (!AssetUtil.isValidWord(value)) {
    throw new CategoryPropertyValueException("Value is not a valid word {value=" + value + "}");
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (ExpandoTable)session.get(ExpandoTableImpl.class,new Long(tableId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

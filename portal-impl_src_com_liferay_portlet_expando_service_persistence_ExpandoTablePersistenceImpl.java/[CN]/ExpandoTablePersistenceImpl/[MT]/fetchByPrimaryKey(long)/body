{
  ExpandoTable expandoTable=(ExpandoTable)EntityCacheUtil.getResult(ExpandoTableModelImpl.ENTITY_CACHE_ENABLED,ExpandoTableImpl.class,tableId);
  if (expandoTable == _nullExpandoTable) {
    return null;
  }
  if (expandoTable == null) {
    Session session=null;
    try {
      session=openSession();
      expandoTable=(ExpandoTable)session.get(ExpandoTableImpl.class,Long.valueOf(tableId));
      if (expandoTable != null) {
        cacheResult(expandoTable);
      }
 else {
        EntityCacheUtil.putResult(ExpandoTableModelImpl.ENTITY_CACHE_ENABLED,ExpandoTableImpl.class,tableId,_nullExpandoTable);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(ExpandoTableModelImpl.ENTITY_CACHE_ENABLED,ExpandoTableImpl.class,tableId);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return expandoTable;
}

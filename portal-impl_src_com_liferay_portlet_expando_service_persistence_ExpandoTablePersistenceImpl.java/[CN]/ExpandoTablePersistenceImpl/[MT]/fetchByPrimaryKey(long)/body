{
  ExpandoTable expandoTable=(ExpandoTable)EntityCacheUtil.getResult(ExpandoTableModelImpl.ENTITY_CACHE_ENABLED,ExpandoTableImpl.class,tableId,this);
  if (expandoTable == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (expandoTable == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      expandoTable=(ExpandoTable)session.get(ExpandoTableImpl.class,Long.valueOf(tableId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (expandoTable == null)) {
        EntityCacheUtil.putResult(ExpandoTableModelImpl.ENTITY_CACHE_ENABLED,ExpandoTableImpl.class,tableId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(expandoTable);
      }
      closeSession(session);
    }
  }
  return expandoTable;
}

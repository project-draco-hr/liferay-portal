{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoTable expandoTable) method. Use update(ExpandoTable expandoTable, boolean merge) instead.");
  }
  return update(expandoTable,false);
}

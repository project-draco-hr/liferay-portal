{
  if (isEscapedModel()) {
    return (Organization)this;
  }
 else {
    return (Organization)Proxy.newProxyInstance(Organization.class.getClassLoader(),new Class[]{Organization.class},new AutoEscapeBeanHandler(this));
  }
}

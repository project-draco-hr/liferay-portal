{
  for (  TagsProperty tagsProperty : tagsProperties) {
    if (EntityCacheUtil.getResult(TagsPropertyModelImpl.ENTITY_CACHE_ENABLED,TagsProperty.class,tagsProperty.getPrimaryKey(),this) == null) {
      cacheResult(tagsProperty);
    }
  }
}

{
  TagsProperty tagsProperty=(TagsProperty)EntityCacheUtil.getResult(TagsPropertyModelImpl.ENTITY_CACHE_ENABLED,TagsPropertyImpl.class,propertyId,this);
  if (tagsProperty == null) {
    Session session=null;
    try {
      session=openSession();
      tagsProperty=(TagsProperty)session.get(TagsPropertyImpl.class,new Long(propertyId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (tagsProperty != null) {
        cacheResult(tagsProperty);
      }
      closeSession(session);
    }
  }
  return tagsProperty;
}

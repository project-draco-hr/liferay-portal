{
  TagsProperty result=(TagsProperty)EntityCacheUtil.getResult(TagsPropertyModelImpl.ENTITY_CACHE_ENABLED,TagsPropertyImpl.class,propertyId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      TagsProperty tagsProperty=(TagsProperty)session.get(TagsPropertyImpl.class,new Long(propertyId));
      if (tagsProperty != null) {
        cacheResult(tagsProperty);
      }
      return tagsProperty;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (TagsProperty)result;
  }
}

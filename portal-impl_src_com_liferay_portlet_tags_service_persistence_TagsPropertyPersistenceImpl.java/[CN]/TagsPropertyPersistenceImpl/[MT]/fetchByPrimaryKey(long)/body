{
  Session session=null;
  try {
    session=openSession();
    return (TagsProperty)session.get(TagsPropertyImpl.class,new Long(propertyId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TagsProperty tagsProperty) method. To improve performance, call update(TagsProperty tagsProperty, boolean merge).");
  }
  return update(tagsProperty,false);
}

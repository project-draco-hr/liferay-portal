{
  try {
    _persistence.countByC_LtCD(RandomTestUtil.nextLong(),RandomTestUtil.nextDate());
    _persistence.countByC_LtCD(0L,RandomTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

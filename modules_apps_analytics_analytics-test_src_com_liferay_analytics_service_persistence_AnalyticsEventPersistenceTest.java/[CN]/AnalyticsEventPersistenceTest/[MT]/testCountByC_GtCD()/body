{
  try {
    _persistence.countByC_GtCD(RandomTestUtil.nextLong(),RandomTestUtil.nextDate());
    _persistence.countByC_GtCD(0L,RandomTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

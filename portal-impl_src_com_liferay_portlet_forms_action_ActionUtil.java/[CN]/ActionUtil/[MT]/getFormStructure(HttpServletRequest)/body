{
  long groupId=ParamUtil.getLong(request,"groupId");
  String formStructureId=ParamUtil.getString(request,"formStructureId");
  FormStructure formStructure=null;
  if (Validator.isNotNull(formStructureId)) {
    formStructure=FormStructureServiceUtil.fetchByG_F(groupId,formStructureId);
  }
  request.setAttribute(WebKeys.FORM_STRUCTURE,formStructure);
}

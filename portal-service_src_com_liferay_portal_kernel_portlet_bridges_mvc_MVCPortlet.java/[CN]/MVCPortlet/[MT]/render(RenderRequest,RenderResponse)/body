{
  String mvcRenderCommandName=ParamUtil.getString(renderRequest,"mvcRenderCommandName");
  MVCRenderCommand mvcRenderCommand=(MVCRenderCommand)_mvcRenderCommandCache.getMVCCommand(mvcRenderCommandName);
  if (mvcRenderCommand != MVCRenderCommand.EMPTY) {
    String mvcPath=mvcRenderCommand.render(renderRequest,renderResponse);
    if (Validator.isNotNull(mvcPath)) {
      renderRequest.setAttribute(MVC_PATH,mvcPath);
    }
  }
  super.render(renderRequest,renderResponse);
}

{
  if (!contains(permissionChecker,layoutBranch,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),LayoutBranch.class.getName(),layoutBranch.getLayoutBranchId(),actionId);
  }
}

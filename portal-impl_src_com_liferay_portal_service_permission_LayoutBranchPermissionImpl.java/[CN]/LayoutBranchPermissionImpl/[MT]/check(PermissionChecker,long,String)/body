{
  if (!contains(permissionChecker,layoutBranchId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),LayoutBranch.class.getName(),layoutBranchId,actionId);
  }
}

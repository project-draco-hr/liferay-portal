{
  try {
    if (!LDAPUserTransactionThreadLocal.isOriginatesFromLDAP()) {
      PortalLDAPExporterUtil.exportToLDAP(contact);
    }
  }
 catch (  Exception e) {
    throw new ModelListenerException(e);
  }
}

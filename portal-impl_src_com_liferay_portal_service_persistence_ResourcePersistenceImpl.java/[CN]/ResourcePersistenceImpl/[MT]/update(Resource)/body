{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Resource resource) method. To improve performance, call update(Resource resource, boolean merge).");
  }
  return update(resource,false);
}

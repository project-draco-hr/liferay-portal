{
  for (  Resource resource : resources) {
    if (EntityCacheUtil.getResult(ResourceModelImpl.ENTITY_CACHE_ENABLED,Resource.class,resource.getPrimaryKey(),this) == null) {
      cacheResult(resource);
    }
  }
}

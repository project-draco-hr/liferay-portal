{
  super(persistence.getDataSource(),"INSERT INTO Groups_UserGroups (groupId, userGroupId) VALUES (?, ?)");
  _persistence=persistence;
  declareParameter(new SqlParameter(Types.VARCHAR));
  declareParameter(new SqlParameter(Types.VARCHAR));
  compile();
}

{
  if (syncVersion == null) {
    syncVersion=getLatestSyncVersion();
  }
  if ((syncVersion != null) && (syncVersion.getBuildNumber() > ReleaseInfo.getBuildNumber())) {
    return true;
  }
  return false;
}

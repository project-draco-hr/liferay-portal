{
  Registry registry=RegistryUtil.getRegistry();
  Indexer indexer=registry.getService(serviceReference);
  Set<String> classNames=_aggregrateClassNames((String[])serviceReference.getProperty("indexer.classNames"),indexer.getClassNames());
  for (  String className : classNames) {
    _indexers.put(className,indexer);
  }
  return indexer;
}

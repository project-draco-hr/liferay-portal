{
  Session session=null;
  try {
    session=openSession();
    return (SCFrameworkVersion)session.get(SCFrameworkVersionImpl.class,new Long(frameworkVersionId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

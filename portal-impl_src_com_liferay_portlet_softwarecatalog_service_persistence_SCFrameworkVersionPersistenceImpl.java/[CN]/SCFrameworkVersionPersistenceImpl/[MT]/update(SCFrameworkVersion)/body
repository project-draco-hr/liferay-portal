{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCFrameworkVersion scFrameworkVersion) method. To improve performance, call update(SCFrameworkVersion scFrameworkVersion, boolean merge).");
  }
  return update(scFrameworkVersion,false);
}

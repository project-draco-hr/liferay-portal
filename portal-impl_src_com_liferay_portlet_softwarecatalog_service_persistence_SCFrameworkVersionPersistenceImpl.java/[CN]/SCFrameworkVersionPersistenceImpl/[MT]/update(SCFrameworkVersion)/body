{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCFrameworkVersion scFrameworkVersion) method. Use update(SCFrameworkVersion scFrameworkVersion, boolean merge) instead.");
  }
  return update(scFrameworkVersion,false);
}

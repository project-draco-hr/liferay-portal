{
  Dictionary<String,Object> mvcActionCommandProperties=new HashMapDictionary<>();
  mvcActionCommandProperties.put("javax.portlet.name",TestUploadPortlet.TEST_UPLOAD_PORTLET);
  mvcActionCommandProperties.put("mvc.command.name",TestUploadPortlet.TEST_MVC_COMMAND_NAME);
  Bundle bundle=FrameworkUtil.getBundle(getClass());
  BundleContext bundleContext=bundle.getBundleContext();
  ServiceRegistration<MVCActionCommand> serviceRegistration=bundleContext.registerService(MVCActionCommand.class,mvcActionCommand,mvcActionCommandProperties);
  serviceRegistrations.add(serviceRegistration);
}

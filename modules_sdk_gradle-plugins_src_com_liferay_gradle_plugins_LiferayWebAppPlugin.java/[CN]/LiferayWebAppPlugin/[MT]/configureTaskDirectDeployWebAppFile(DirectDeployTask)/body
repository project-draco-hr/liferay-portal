{
  if (directDeployTask.getWebAppFile() != null) {
    return;
  }
  War warTask=(War)GradleUtil.getTask(directDeployTask.getProject(),WarPlugin.WAR_TASK_NAME);
  directDeployTask.setWebAppFile(warTask.getArchivePath());
}

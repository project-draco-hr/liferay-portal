{
  List<Log> list=_logs.get(name);
  if (list == null) {
    list=new Vector<Log>();
    _logs.put(name,list);
  }
  Log log=new LogWrapper(_logFactory.getLog(name));
  list.add(log);
  return log;
}

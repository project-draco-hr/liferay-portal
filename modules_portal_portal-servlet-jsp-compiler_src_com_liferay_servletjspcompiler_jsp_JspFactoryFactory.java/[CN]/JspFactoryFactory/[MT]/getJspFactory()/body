{
  ServiceLoader<JspFactory> jspFactories=ServiceLoader.load(JspFactory.class);
  for (  JspFactory jspFactory : jspFactories) {
    return jspFactory;
  }
  return JspFactory.getDefaultFactory();
}

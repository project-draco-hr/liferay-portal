{
  Socket socket=null;
  try {
    socket=new Socket(host,port);
    return socket.getLocalAddress().getHostAddress();
  }
  finally {
    if (socket != null) {
      try {
        socket.close();
      }
 catch (      IOException ioe) {
      }
    }
  }
}

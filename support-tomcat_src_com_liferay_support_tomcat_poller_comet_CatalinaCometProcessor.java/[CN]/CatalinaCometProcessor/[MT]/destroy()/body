{
  super.destroy();
  if (_log.isDebugEnabled()) {
    _log.debug("Destroy comet processor");
  }
  try {
    _cometHandlerPool.closeCometHandlers();
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

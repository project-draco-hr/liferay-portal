{
  Serializable serializable=entityCache.getResult(MDRActionModelImpl.ENTITY_CACHE_ENABLED,MDRActionImpl.class,primaryKey);
  if (serializable == nullModel) {
    return null;
  }
  MDRAction mdrAction=(MDRAction)serializable;
  if (mdrAction == null) {
    Session session=null;
    try {
      session=openSession();
      mdrAction=(MDRAction)session.get(MDRActionImpl.class,primaryKey);
      if (mdrAction != null) {
        cacheResult(mdrAction);
      }
 else {
        entityCache.putResult(MDRActionModelImpl.ENTITY_CACHE_ENABLED,MDRActionImpl.class,primaryKey,nullModel);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(MDRActionModelImpl.ENTITY_CACHE_ENABLED,MDRActionImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return mdrAction;
}

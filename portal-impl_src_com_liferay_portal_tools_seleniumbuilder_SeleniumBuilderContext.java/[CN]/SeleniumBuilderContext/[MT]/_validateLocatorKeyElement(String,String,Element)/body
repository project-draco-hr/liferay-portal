{
  String comparator=element.attributeValue("comparator");
  List<Attribute> attributes=element.attributes();
  for (  Attribute attribute : attributes) {
    String attributeName=attribute.getName();
    if (attributeName.startsWith("locator-key")) {
      String attributeValue=attribute.getValue();
      if (!_isValidLocatorKey(actionName,comparator,attributeValue)) {
        _seleniumBuilderFileUtil.throwValidationException(1010,fileName,element,attributeValue);
      }
    }
  }
}

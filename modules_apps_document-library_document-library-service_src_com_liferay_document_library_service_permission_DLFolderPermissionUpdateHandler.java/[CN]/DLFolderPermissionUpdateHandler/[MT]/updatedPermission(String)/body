{
  DLFolder folder=_dLFolderLocalService.fetchDLFolder(GetterUtil.getLong(primKey));
  if (folder == null) {
    return;
  }
  folder.setModifiedDate(new Date());
  _dLFolderLocalService.updateDLFolder(folder);
}

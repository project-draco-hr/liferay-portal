{
  try {
    doImportDeletions(portletDataContext,portletId,portletPreferences,data);
    return doImportData(portletDataContext,portletId,portletPreferences,data);
  }
 catch (  Exception e) {
    throw new PortletDataException(e);
  }
}

{
  try {
    doExportDeletions(portletDataContext,portletId,portletPreferences);
    return doExportData(portletDataContext,portletId,portletPreferences);
  }
 catch (  Exception e) {
    throw new PortletDataException(e);
  }
}

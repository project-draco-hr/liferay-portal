{
  if (_transactionHandler == null) {
    throw new IllegalStateException(_platformTransactionManager + " does not support " + "programmatic transaction handling");
  }
  _transactionHandler.rollback(_platformTransactionManager,throwable,transactionAttribute,transactionStatus);
}

{
  CommentManager commentManager=getCommentManager();
  return commentManager.addComment(groupId,className,classPK,permissionClassName,permissionClassPK,permissionOwnerId,parentMessageId,subject,body,serviceContextFunction);
}

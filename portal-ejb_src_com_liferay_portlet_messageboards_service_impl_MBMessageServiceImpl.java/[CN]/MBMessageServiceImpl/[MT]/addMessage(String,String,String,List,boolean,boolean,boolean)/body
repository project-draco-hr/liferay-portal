{
  MBCategoryPermission.check(getPermissionChecker(),categoryId,ActionKeys.ADD_MESSAGE);
  return MBMessageLocalServiceUtil.addMessage(getUserId(),categoryId,subject,body,files,anonymous,null,addCommunityPermissions,addGuestPermissions);
}

{
  MBTopicPermission.check(getPermissionChecker(),topicId,ActionKeys.ADD_MESSAGE);
  return MBMessageLocalServiceUtil.addMessage(getUserId(),topicId,threadId,parentMessageId,subject,body,files,anonymous,prefs,addCommunityPermissions,addGuestPermissions);
}

{
  Object obj=findService(serviceClass.getName());
  if (obj != null) {
    obj=Proxy.newProxyInstance(serviceClass.getClassLoader(),new Class[]{serviceClass},new ExceptionSafeBeanHandler(obj));
  }
  return obj;
}

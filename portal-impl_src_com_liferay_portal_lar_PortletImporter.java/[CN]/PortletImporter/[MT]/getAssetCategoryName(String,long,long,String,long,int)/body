{
  AssetCategory assetCategory=null;
  try {
    assetCategory=AssetCategoryUtil.findByG_P_N_V_First(groupId,parentCategoryId,name,vocabularyId,null);
  }
 catch (  NoSuchCategoryException nsce) {
    return name;
  }
  if (Validator.isNotNull(uuid) && uuid.equals(assetCategory.getUuid())) {
    return name;
  }
  name=StringUtil.appendParentheticalSuffix(name,count);
  return getAssetCategoryName(uuid,groupId,parentCategoryId,name,vocabularyId,++count);
}

{
  if ((groupSessions=(Hashtable)_userSession.get(ConsumerConstants.WSRP_GROUPSESSIONS)) == null) {
    groupSessions=new Hashtable();
    _userSession.put(ConsumerConstants.WSRP_GROUPSESSIONS,groupSessions);
  }
  return groupSessions;
}

{
  String key=createKey();
  Map myMap=(Map)this._portletSession.getAttribute(key,PortletSession.APPLICATION_SCOPE);
  if (myMap == null) {
    myMap=new Hashtable();
    this._portletSession.setAttribute(key,myMap,PortletSession.APPLICATION_SCOPE);
  }
  return myMap;
}

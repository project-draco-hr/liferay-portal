{
  Session session=null;
  try {
    session=openSession();
    return (BookmarksFolder)session.get(BookmarksFolderImpl.class,new Long(folderId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

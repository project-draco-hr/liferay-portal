{
  ServiceACProfile serviceACProfile=findByPrimaryKey(serviceACProfileId);
  Session session=null;
  try {
    session=openSession();
    ServiceACProfile[] array=new ServiceACProfileImpl[3];
    array[0]=getByUuid_C_PrevAndNext(session,serviceACProfile,uuid,companyId,orderByComparator,true);
    array[1]=serviceACProfile;
    array[2]=getByUuid_C_PrevAndNext(session,serviceACProfile,uuid,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

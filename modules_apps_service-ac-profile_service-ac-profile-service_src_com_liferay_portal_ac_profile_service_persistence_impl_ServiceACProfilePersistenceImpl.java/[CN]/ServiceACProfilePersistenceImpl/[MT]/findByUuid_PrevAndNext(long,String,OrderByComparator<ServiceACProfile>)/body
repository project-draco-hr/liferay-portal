{
  ServiceACProfile serviceACProfile=findByPrimaryKey(serviceACProfileId);
  Session session=null;
  try {
    session=openSession();
    ServiceACProfile[] array=new ServiceACProfileImpl[3];
    array[0]=getByUuid_PrevAndNext(session,serviceACProfile,uuid,orderByComparator,true);
    array[1]=serviceACProfile;
    array[2]=getByUuid_PrevAndNext(session,serviceACProfile,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

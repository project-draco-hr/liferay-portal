{
  for (  IGImage igImage : igImages) {
    if (EntityCacheUtil.getResult(IGImageModelImpl.ENTITY_CACHE_ENABLED,IGImage.class,igImage.getPrimaryKey(),this) == null) {
      cacheResult(igImage);
    }
  }
}

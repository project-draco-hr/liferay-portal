{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(IGImage igImage) method. To improve performance, call update(IGImage igImage, boolean merge).");
  }
  return update(igImage,false);
}

{
  Iterator itr=_listeners.iterator();
  while (itr.hasNext()) {
    SubEthaMessageListenerImpl curListener=(SubEthaMessageListenerImpl)itr.next();
    if (curListener.equals(listener)) {
      itr.remove();
    }
  }
}

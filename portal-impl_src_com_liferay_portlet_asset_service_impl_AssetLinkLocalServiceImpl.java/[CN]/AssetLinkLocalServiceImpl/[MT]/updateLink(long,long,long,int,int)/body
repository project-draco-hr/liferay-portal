{
  AssetLink assetLink=assetLinkPersistence.fetchByE_E_T(entryId1,entryId2,typeId);
  if (assetLink == null) {
    assetLink=addLink(userId,entryId1,entryId2,typeId,weight);
  }
 else {
    assetLink.setWeight(weight);
    assetLinkPersistence.update(assetLink);
  }
  return assetLink;
}

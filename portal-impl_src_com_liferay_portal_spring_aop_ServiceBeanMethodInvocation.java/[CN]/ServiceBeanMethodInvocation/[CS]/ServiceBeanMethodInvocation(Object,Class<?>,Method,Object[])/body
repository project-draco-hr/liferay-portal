{
  _target=target;
  _targetClass=targetClass;
  _method=method;
  _arguments=arguments;
  if (!_method.isAccessible()) {
    _method.setAccessible(true);
  }
  if ((_method.getDeclaringClass() == Object.class) && "equals".equals(_method.getName())) {
    _equalsMethod=true;
  }
}

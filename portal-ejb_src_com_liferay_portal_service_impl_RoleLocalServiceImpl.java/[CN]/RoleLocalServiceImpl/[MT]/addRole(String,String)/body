{
  validate(null,companyId,name);
  String roleId=Long.toString(CounterServiceUtil.increment(Role.class.getName()));
  Role role=RoleUtil.create(roleId);
  role.setCompanyId(companyId);
  role.setName(name);
  RoleUtil.update(role);
  return role;
}

{
  if (_httpServlet == null) {
    response.sendError(HttpServletResponse.SC_SERVICE_UNAVAILABLE,"Framework unavailable or not configured");
    return;
  }
  _httpServlet.service(request,response);
}

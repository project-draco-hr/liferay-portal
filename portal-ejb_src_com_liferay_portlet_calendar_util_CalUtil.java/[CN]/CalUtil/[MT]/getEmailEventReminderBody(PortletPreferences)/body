{
  String emailEventReminderBody=prefs.getValue("email-event-reminder-body",StringPool.BLANK);
  if (Validator.isNotNull(emailEventReminderBody)) {
    return emailEventReminderBody;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsUtil.CALENDAR_EMAIL_EVENT_REMINDER_BODY));
  }
}

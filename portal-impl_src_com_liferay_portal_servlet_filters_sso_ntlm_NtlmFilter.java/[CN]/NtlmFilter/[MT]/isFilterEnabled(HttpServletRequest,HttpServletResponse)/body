{
  try {
    long companyId=PortalInstances.getCompanyId(request);
    if (AuthSettingsUtil.isNtlmEnabled(companyId) && BrowserSnifferUtil.isIe(request)) {
      return true;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return false;
}

{
  Dictionary<String,String> dictionary=new HashMapDictionary<>();
  boolean shibbolethEnabled=_prefsProps.getBoolean(companyId,LegacyTokenPropsKeys.SHIBBOLETH_AUTH_ENABLED,false);
  if (shibbolethEnabled) {
    addShibbolethTokenKeys(dictionary,companyId);
  }
  if (_log.isDebugEnabled()) {
    _log.debug("Adding Shibboleth Token configuration for company " + companyId + " with properties: "+ dictionary);
  }
  return dictionary;
}

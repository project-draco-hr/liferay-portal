{
  if (!_paclPolicy.isActive()) {
    return true;
  }
  if (_permissionCollection.implies(permission)) {
    return true;
  }
  if (!_paclPolicy.implies(permission)) {
    return false;
  }
  return true;
}

{
  Object[] args=proceedingJoinPoint.getArgs();
  args[0]="UDP(thread_pool.min_threads=1;thread_pool.max_threads=1;" + "oob_thread_pool.enabled=false;):PING:MERGE3:FD_SOCK:" + "FD_ALL:VERIFY_SUSPECT:pbcast.NAKACK2:UNICAST:"+ "pbcast.STABLE:pbcast.GMS:UFC:MFC:FRAG2:RSVP";
  return proceedingJoinPoint.proceed(args);
}

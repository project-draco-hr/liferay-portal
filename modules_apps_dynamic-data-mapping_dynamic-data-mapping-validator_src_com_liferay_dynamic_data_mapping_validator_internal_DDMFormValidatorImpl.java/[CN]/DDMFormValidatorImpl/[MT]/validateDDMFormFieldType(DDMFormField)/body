{
  Set<String> fieldTypeNames=DDMFormFieldTypeRegistryUtil.getDDMFormFieldTypeNames();
  if (!fieldTypeNames.contains(ddmFormField.getType())) {
    throw new DDMFormValidationException("Invalid type set for field " + ddmFormField.getName());
  }
}

{
  if (!contains(permissionChecker,recordSet,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDLRecordSet.class.getName(),recordSet.getRecordSetId(),actionId);
  }
}

{
  if (!contains(permissionChecker,recordSetId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDLRecordSet.class.getName(),recordSetId,actionId);
  }
}

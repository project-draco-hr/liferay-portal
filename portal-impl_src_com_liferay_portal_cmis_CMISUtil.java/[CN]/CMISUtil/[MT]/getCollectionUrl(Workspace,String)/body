{
  for (  Collection coll : ws.getCollections()) {
    String collType=coll.getAttributeValue(_constants.COLLECTION_TYPE);
    if (collType.equals(collectionType)) {
      return coll.getHref().toString();
    }
  }
  return null;
}

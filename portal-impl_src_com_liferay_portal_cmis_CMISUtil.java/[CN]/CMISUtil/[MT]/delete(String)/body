{
  ClientResponse resp=_getClient().delete(url);
  if (ResponseType.select(resp.getStatus()) != ResponseType.SUCCESS) {
    throw new CMISException("Error deleting object at " + url + " "+ resp.getStatusText());
  }
}

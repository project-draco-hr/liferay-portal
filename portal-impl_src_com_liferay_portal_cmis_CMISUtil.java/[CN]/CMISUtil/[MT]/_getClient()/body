{
  try {
    AbderaClient client=new AbderaClient(_abdera);
    client.addCredentials(null,null,null,new UsernamePasswordCredentials(_USERNAME,_PASSWORD));
    return client;
  }
 catch (  Exception e) {
    throw new CMISException(e);
  }
}

{
  for (  SACPEntry sacpEntry : sacpEntries) {
    if (EntityCacheUtil.getResult(SACPEntryModelImpl.ENTITY_CACHE_ENABLED,SACPEntryImpl.class,sacpEntry.getPrimaryKey()) == null) {
      cacheResult(sacpEntry);
    }
 else {
      sacpEntry.resetOriginalValues();
    }
  }
}

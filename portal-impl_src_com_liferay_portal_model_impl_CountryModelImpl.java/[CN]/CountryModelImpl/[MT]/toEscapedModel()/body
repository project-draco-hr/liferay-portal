{
  if (isEscapedModel()) {
    return (Country)this;
  }
 else {
    return (Country)Proxy.newProxyInstance(Country.class.getClassLoader(),new Class[]{Country.class},new AutoEscapeBeanHandler(this));
  }
}

{
  Session session=null;
  try {
    session=openSession();
    AssetEntry assetEntry=(AssetEntry)session.get(AssetEntryImpl.class,new Long(entryId));
    if (assetEntry == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No AssetEntry exists with the primary key " + entryId);
      }
      throw new NoSuchEntryException("No AssetEntry exists with the primary key " + entryId);
    }
    return remove(assetEntry);
  }
 catch (  NoSuchEntryException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

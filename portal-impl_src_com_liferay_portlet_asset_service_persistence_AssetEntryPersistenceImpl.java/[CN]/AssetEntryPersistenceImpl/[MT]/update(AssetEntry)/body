{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetEntry assetEntry) method. Use update(AssetEntry assetEntry, boolean merge) instead.");
  }
  return update(assetEntry,false);
}

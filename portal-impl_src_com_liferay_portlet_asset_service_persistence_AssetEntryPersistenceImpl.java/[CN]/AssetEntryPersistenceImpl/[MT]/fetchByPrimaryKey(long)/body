{
  AssetEntry assetEntry=(AssetEntry)EntityCacheUtil.getResult(AssetEntryModelImpl.ENTITY_CACHE_ENABLED,AssetEntryImpl.class,entryId,this);
  if (assetEntry == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (assetEntry == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      assetEntry=(AssetEntry)session.get(AssetEntryImpl.class,Long.valueOf(entryId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (assetEntry == null)) {
        EntityCacheUtil.putResult(AssetEntryModelImpl.ENTITY_CACHE_ENABLED,AssetEntryImpl.class,entryId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(assetEntry);
      }
      closeSession(session);
    }
  }
  return assetEntry;
}

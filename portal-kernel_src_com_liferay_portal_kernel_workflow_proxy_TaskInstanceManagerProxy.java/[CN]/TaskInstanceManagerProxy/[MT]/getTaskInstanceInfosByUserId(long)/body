{
  try {
    return (List<TaskInstanceInfo>)_synchronousMessageSender.send(TaskInstanceRequest.createGetByUserIdRequest(actorId));
  }
 catch (  MessageBusException ex) {
    throw new WorkflowException("Unable to get taskInstances by " + "actorId:" + actorId,ex);
  }
}

{
  try {
    return (List<TaskInstanceInfo>)_synchronousMessageSender.send(TaskInstanceRequest.createGetByTokenIdRequest(tokenId,finished));
  }
 catch (  MessageBusException ex) {
    throw new WorkflowException("Unable to get taskInstances by " + "tokenId:" + tokenId + " finished:"+ finished,ex);
  }
}

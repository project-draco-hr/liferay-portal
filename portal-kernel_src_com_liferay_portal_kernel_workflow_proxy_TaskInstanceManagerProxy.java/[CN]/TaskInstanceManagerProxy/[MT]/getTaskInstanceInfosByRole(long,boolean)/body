{
  try {
    return (List<TaskInstanceInfo>)_synchronousMessageSender.send(TaskInstanceRequest.createGetByRoleRequest(roleId,finished));
  }
 catch (  MessageBusException ex) {
    throw new WorkflowException("Unable to get taskInstances by " + "roleId:" + roleId + " finished:"+ finished,ex);
  }
}

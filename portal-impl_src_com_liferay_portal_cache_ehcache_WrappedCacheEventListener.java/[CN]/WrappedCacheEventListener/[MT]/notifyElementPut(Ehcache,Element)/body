{
  _cacheListener.notifyEntryPut(_portalCache,element.getKey().toString(),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug(ehcache.getName() + " - notifyElementPut: " + element.getKey());
  }
}

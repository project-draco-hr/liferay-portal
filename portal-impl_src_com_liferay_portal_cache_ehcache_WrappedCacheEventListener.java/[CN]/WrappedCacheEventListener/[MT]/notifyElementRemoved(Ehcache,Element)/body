{
  _cacheListener.notifyEntryRemoved(_portalCache,element.getKey().toString(),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug(ehcache.getName() + " - notifyElementRemoved: " + element.getKey());
  }
}

{
  _cacheListener.notifyEntryUpdated(_portalCache,element.getKey().toString(),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug(ehcache.getName() + " - notifyElementUpdated: " + element.getKey());
  }
}

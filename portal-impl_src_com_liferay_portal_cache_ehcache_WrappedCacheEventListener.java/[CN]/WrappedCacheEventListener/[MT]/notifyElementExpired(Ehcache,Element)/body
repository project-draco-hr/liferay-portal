{
  _cacheListener.notifyEntryExpired(_portalCache,element.getKey().toString(),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug(ehcache.getName() + " - notifyElementExpired: " + element.getKey());
  }
}

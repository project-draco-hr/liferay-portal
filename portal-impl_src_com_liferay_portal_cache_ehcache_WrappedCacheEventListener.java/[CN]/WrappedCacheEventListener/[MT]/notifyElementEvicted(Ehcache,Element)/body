{
  _cacheListener.notifyEntryEvicted(_portalCache,element.getKey().toString(),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug(ehcache.getName() + " - notifyElementEvicted: " + element.getKey());
  }
}

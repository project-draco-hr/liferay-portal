{
  try {
    String javaCommand="javac";
    if (OSDetector.isWindows()) {
      javaCommand="javac.exe";
    }
    File file=new File(System.getProperty("java.home"),"bin".concat(File.separator).concat(javaCommand));
    file.canRead();
    Assert.fail();
  }
 catch (  SecurityException se) {
  }
}

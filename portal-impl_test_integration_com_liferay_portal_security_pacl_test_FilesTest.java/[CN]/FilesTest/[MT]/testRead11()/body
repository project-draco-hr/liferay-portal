{
  String javaCommand="java";
  if (OSDetector.isWindows()) {
    javaCommand="java.exe";
  }
  File file=new File(System.getProperty("java.home"),"bin" + File.separator + javaCommand);
  file.canRead();
}

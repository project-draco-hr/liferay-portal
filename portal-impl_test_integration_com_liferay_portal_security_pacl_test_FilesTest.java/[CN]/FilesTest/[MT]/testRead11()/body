{
  try {
    String javaCommand="java";
    if (OSDetector.isWindows()) {
      javaCommand="java.exe";
    }
    File file=new File(System.getenv("JAVA_HOME"),"bin".concat(File.separator).concat(javaCommand));
    file.canRead();
  }
 catch (  SecurityException se) {
    Assert.fail();
  }
}

{
  try {
    String javaCommand="java";
    if (OSDetector.isWindows()) {
      javaCommand="java.exe";
    }
    File file=new File(System.getenv("JAVA_HOME"),"bin" + File.separator + javaCommand);
    file.canRead();
  }
 catch (  SecurityException se) {
    Assert.fail();
  }
}

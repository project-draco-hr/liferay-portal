{
  PortletPermissionUtil.check(getPermissionChecker(),PortletKeys.WORKFLOW_TASKS,ActionKeys.ASSIGN_USER_TASKS);
  WorkflowTaskManagerUtil.assignWorkflowTaskToUser(getUserId(),workflowTaskId,assigneeUserId,comment,context);
}

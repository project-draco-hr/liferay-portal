{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ResourceCode resourceCode) method. To improve performance, call update(ResourceCode resourceCode, boolean merge).");
  }
  return update(resourceCode,false);
}

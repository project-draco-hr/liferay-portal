{
  super(String.format("Screen name %s for user %s is  not valid because it " + "must be alphanumeric or one the following special " + "characters: %s",screenName,userId,new String(validSpecialChars)));
  _userId=userId;
  _screenName=screenName;
  _validSpecialChars=validSpecialChars;
}

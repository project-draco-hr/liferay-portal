{
  super("Screen name " + screenName + " for user "+ userId+ " is "+ "not valid because it must not be a reserved name for "+ "anonymous users such as: "+ StringUtil.merge(reservedScreenNames));
  _userId=userId;
  _screenName=screenName;
  _reservedScreenNames=reservedScreenNames;
}

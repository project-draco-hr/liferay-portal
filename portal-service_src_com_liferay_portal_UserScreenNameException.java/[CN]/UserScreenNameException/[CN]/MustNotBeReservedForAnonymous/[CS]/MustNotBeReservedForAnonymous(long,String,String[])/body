{
  super(String.format("Screen name %s for user %s is invalid because it must " + "not be a reserved name for anonymous users such as: " + "%s",StringUtil.merge(reservedScreenNames)));
  _userId=userId;
  _screenName=screenName;
  _reservedScreenNames=reservedScreenNames;
}

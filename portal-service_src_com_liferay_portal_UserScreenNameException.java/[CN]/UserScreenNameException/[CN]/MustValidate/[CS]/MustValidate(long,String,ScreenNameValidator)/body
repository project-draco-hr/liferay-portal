{
  super(String.format("Screen name %s for user %s must validate with %s",screenName,userId,ClassUtil.getClassName(screenNameValidator)));
  _userId=userId;
  _screenName=screenName;
  _screenNameValidator=screenNameValidator;
}

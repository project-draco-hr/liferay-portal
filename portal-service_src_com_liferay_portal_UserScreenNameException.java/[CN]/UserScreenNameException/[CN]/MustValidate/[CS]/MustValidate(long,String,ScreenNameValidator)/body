{
  super("Screen name " + screenName + " for user "+ userId+ " does "+ "not validate with "+ ClassUtil.getClassName(screenNameValidator));
  _userId=userId;
  _screenName=screenName;
  _screenNameValidator=screenNameValidator;
}

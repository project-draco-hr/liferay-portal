{
  super(String.format("Screen name %s for user %s is numeric but the portal " + "property \"%s\" is disabled",screenName,userId,PropsKeys.USERS_SCREEN_NAME_ALLOW_NUMERIC));
  _userId=userId;
  _screenName=screenName;
}

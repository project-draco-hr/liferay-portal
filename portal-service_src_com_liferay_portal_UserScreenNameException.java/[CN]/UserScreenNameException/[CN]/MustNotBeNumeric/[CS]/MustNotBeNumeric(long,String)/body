{
  super("Screen name " + screenName + " for user "+ userId+ " is "+ "numeric but the portal property \""+ PropsKeys.USERS_SCREEN_NAME_ALLOW_NUMERIC+ "\" is disabled");
  _userId=userId;
  _screenName=screenName;
}

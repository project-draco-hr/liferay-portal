{
  if (this == obj) {
    return true;
  }
  JournalArticleKey journalArticleKey=(JournalArticleKey)obj;
  if (Validator.equals(journalArticleKey._articleId,_articleId) && Validator.equals(journalArticleKey._groupId,_groupId)) {
    return true;
  }
  return false;
}

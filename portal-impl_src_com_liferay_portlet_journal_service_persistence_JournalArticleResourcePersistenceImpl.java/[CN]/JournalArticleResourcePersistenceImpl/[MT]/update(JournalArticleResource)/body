{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalArticleResource journalArticleResource) method. Use update(JournalArticleResource journalArticleResource, boolean merge) instead.");
  }
  return update(journalArticleResource,false);
}

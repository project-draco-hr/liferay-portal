{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalArticleResource journalArticleResource) method. To improve performance, call update(JournalArticleResource journalArticleResource, boolean merge).");
  }
  return update(journalArticleResource,false);
}

{
  Map<Long,Set<Long>> liveUsers=(Map<Long,Set<Long>>)WebAppPool.get(companyId,WebKeys.LIVE_USERS);
  if (liveUsers == null) {
    liveUsers=new ConcurrentHashMap<Long,Set<Long>>();
    WebAppPool.put(companyId,WebKeys.LIVE_USERS,liveUsers);
  }
  return liveUsers;
}

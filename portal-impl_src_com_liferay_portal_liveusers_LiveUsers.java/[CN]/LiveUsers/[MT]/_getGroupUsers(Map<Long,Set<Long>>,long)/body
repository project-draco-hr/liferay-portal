{
  Set<Long> groupUsers=liveUsers.get(groupId);
  if (groupUsers == null) {
    groupUsers=new MapBackedSet<Long>(new ConcurrentHashMap<Long,Boolean>());
    liveUsers.put(groupId,groupUsers);
  }
  return groupUsers;
}

{
  DDMListItem ddmListItem=findByPrimaryKey(listItemId);
  Session session=null;
  try {
    session=openSession();
    DDMListItem[] array=new DDMListItemImpl[3];
    array[0]=getByUuid_PrevAndNext(session,ddmListItem,uuid,orderByComparator,true);
    array[1]=ddmListItem;
    array[2]=getByUuid_PrevAndNext(session,ddmListItem,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  for (  DDMListItem ddmListItem : ddmListItems) {
    if (EntityCacheUtil.getResult(DDMListItemModelImpl.ENTITY_CACHE_ENABLED,DDMListItemImpl.class,ddmListItem.getPrimaryKey(),this) == null) {
      cacheResult(ddmListItem);
    }
  }
}

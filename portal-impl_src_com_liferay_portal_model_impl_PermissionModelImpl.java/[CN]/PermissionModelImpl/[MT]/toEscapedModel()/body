{
  if (isEscapedModel()) {
    return (Permission)this;
  }
 else {
    return (Permission)Proxy.newProxyInstance(Permission.class.getClassLoader(),new Class[]{Permission.class},new AutoEscapeBeanHandler(this));
  }
}

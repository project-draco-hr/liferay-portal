{
  Bible bible=(Bible)_bibles.get(prefs.getValue("language",StringPool.BLANK));
  if (bible == null) {
    bible=(Bible)_bibles.get(locale.getLanguage());
  }
  if (bible == null) {
    bible=(Bible)_bibles.get("en");
  }
  return bible;
}

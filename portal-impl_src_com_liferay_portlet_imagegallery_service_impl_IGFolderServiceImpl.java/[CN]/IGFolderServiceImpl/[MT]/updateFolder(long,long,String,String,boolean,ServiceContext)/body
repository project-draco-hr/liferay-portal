{
  IGFolder folder=igFolderLocalService.getFolder(folderId);
  IGFolderPermission.check(getPermissionChecker(),folder,ActionKeys.UPDATE);
  return igFolderLocalService.updateFolder(folderId,parentFolderId,name,description,mergeWithParentFolder,serviceContext);
}

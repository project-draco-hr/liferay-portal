{
  List<IGFolder> folders=igFolderLocalService.getFolders(groupId,parentFolderId);
  folders=ListUtil.copy(folders);
  Iterator<IGFolder> itr=folders.iterator();
  while (itr.hasNext()) {
    IGFolder folder=itr.next();
    if (!IGFolderPermission.contains(getPermissionChecker(),folder.getFolderId(),ActionKeys.VIEW)) {
      itr.remove();
    }
  }
  return folders;
}

{
  IGFolderPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),parentFolderId,ActionKeys.ADD_FOLDER);
  return igFolderLocalService.addFolder(null,getUserId(),parentFolderId,name,description,serviceContext);
}

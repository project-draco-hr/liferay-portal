{
  IGFolderPermission.check(getPermissionChecker(),serviceContext.getPlid(),parentFolderId,ActionKeys.ADD_FOLDER);
  return igFolderLocalService.addFolder(parentFolderId,name,description,serviceContext);
}

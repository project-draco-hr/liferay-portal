{
  FormsStructureEntry formsStructureEntry=(FormsStructureEntry)EntityCacheUtil.getResult(FormsStructureEntryModelImpl.ENTITY_CACHE_ENABLED,FormsStructureEntryImpl.class,id,this);
  if (formsStructureEntry == null) {
    Session session=null;
    try {
      session=openSession();
      formsStructureEntry=(FormsStructureEntry)session.get(FormsStructureEntryImpl.class,new Long(id));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (formsStructureEntry != null) {
        cacheResult(formsStructureEntry);
      }
      closeSession(session);
    }
  }
  return formsStructureEntry;
}

{
  for (int i=0; i < organizationIds.length; i++) {
    if (permissionChecker.hasPermission(0,Organization.class.getName(),organizationIds[i],organizationActionId)) {
      return true;
    }
  }
  return false;
}

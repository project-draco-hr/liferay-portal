{
  boolean enabled=false;
  try {
    int pagesCount=_wikiPageLocalService.getPagesCount("classic_wiki");
    if (pagesCount > 0) {
      enabled=true;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return enabled;
}

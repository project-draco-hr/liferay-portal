{
  WikiPageService wikiPageService=WikiPageServiceFactory.getService();
  return wikiPageService.updatePage(nodeId,title,version,content,format,parentTitle,redirectTitle,tagsEntries,prefs,themeDisplay);
}

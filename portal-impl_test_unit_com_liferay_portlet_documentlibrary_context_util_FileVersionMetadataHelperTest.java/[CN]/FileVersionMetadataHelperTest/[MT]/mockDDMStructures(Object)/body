{
  when(_fileVersion.getModel()).thenReturn(model);
  DLFileEntryType dlFileEntryType=mock(DLFileEntryType.class);
  when(_dlFileEntryTypeService.getFileEntryType(Matchers.anyLong())).thenReturn(dlFileEntryType);
  List<DDMStructure> ddmStructures=new ArrayList<>();
  ddmStructures.add(mock(DDMStructure.class));
  ddmStructures.add(mock(DDMStructure.class));
  when(dlFileEntryType.getDDMStructures()).thenReturn(ddmStructures);
}

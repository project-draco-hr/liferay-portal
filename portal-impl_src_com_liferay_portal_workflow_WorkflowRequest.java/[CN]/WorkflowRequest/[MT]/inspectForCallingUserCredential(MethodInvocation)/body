{
  CallingUserId id=invocation.getMethod().getAnnotation(CallingUserId.class);
  if (id == null) {
    return null;
  }
  return UserCredentialFactoryUtil.createCredential((Long)invocation.getArguments()[id.value()]);
}

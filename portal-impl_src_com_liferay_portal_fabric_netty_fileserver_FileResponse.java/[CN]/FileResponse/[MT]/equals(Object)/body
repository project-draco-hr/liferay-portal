{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof FileResponse)) {
    return false;
  }
  FileResponse fileResponse=(FileResponse)obj;
  if (_path.equals(fileResponse._path) && (_size == fileResponse._size) && (_lastModifiedTime == fileResponse._lastModifiedTime)&& (_folder == fileResponse._folder)) {
    return true;
  }
  return false;
}

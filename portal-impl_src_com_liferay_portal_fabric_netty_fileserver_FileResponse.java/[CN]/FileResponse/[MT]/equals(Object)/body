{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof FileResponse)) {
    return false;
  }
  FileResponse fileResponse=(FileResponse)obj;
  if ((_folder == fileResponse._folder) && (_lastModifiedTime == fileResponse._lastModifiedTime) && _path.equals(fileResponse._path)&& (_size == fileResponse._size)) {
    return true;
  }
  return false;
}

{
  _contextPath=ContextPathUtil.getContextPath(servletContext);
  if (_log.isInfoEnabled()) {
    int actionsCount=JSONWebServiceActionsManagerUtil.countJSONWebServiceActions(_contextPath);
    _log.info("Configured " + actionsCount + " actions for "+ _contextPath);
  }
}

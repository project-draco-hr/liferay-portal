{
  JSONWebServiceConfigurator restConfigurator=new JSONWebServiceConfigurator();
  restConfigurator.setRESTActionsManager(JSONWebServiceActionsManagerUtil.getRESTActionsManager());
  try {
    restConfigurator.configure(classLoader);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

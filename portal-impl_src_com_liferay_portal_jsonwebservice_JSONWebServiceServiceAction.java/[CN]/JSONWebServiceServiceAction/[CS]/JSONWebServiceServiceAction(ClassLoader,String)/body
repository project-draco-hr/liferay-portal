{
  _jsonWebServiceConfigurator=new JSONWebServiceConfigurator(portletServletContextName);
  _jsonWebServiceConfigurator.clean();
  try {
    _jsonWebServiceConfigurator.configure(classLoader);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

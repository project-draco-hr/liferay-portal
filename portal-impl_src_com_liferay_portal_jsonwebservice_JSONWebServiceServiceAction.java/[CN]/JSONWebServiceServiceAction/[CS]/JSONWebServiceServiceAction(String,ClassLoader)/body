{
  _jsonWebServiceConfigurator=new JSONWebServiceConfigurator(servletContextName);
  _jsonWebServiceConfigurator.clean();
  try {
    _jsonWebServiceConfigurator.configure(classLoader);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

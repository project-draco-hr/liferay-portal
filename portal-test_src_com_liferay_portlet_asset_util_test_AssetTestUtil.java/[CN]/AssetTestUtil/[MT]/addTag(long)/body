{
  long userId=TestPropsValues.getUserId();
  String[] tagProperties=null;
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(groupId,userId);
  return AssetTagLocalServiceUtil.addTag(userId,RandomTestUtil.randomString(),tagProperties,serviceContext);
}

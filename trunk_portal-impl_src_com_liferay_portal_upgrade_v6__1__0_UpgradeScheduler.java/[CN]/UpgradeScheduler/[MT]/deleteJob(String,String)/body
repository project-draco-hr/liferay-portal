{
  runSQL("delete from QUARTZ_CRON_TRIGGERS where TRIGGER_NAME = '" + jobName + "' and TRIGGER_GROUP = '"+ jobGroup+ "'");
  runSQL("delete from QUARTZ_JOB_DETAILS where JOB_NAME = '" + jobName + "' and JOB_GROUP = '"+ jobGroup+ "'");
  runSQL("delete from QUARTZ_SIMPLE_TRIGGERS where TRIGGER_NAME = '" + jobName + "' and TRIGGER_GROUP = '"+ jobGroup+ "'");
  runSQL("delete from QUARTZ_TRIGGERS where TRIGGER_NAME = '" + jobName + "' and TRIGGER_GROUP = '"+ jobGroup+ "'");
}

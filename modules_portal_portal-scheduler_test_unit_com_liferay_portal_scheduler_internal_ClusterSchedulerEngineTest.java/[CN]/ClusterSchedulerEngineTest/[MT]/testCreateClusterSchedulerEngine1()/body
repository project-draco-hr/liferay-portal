{
  Mockito.when(_props.get(PropsKeys.SCHEDULER_ENABLED)).thenReturn("true");
  _schedulerEngineHelperImpl.setSchedulerEngine(_mockSchedulerEngine);
  _schedulerEngineHelperImpl.activate();
  SchedulerEngine schedulerEngine=_schedulerEngineHelperImpl.getSchedulerEngine();
  Assert.assertNotSame(_mockSchedulerEngine,schedulerEngine);
  Assert.assertEquals(ClusterSchedulerEngine.class,schedulerEngine.getClass());
}

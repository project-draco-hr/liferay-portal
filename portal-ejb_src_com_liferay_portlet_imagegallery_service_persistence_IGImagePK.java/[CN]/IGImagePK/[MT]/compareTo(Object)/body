{
  if (obj == null) {
    return -1;
  }
  IGImagePK pk=(IGImagePK)obj;
  int value=0;
  value=companyId.compareTo(pk.companyId);
  if (value != 0) {
    return value;
  }
  value=imageId.compareTo(pk.imageId);
  if (value != 0) {
    return value;
  }
  return 0;
}

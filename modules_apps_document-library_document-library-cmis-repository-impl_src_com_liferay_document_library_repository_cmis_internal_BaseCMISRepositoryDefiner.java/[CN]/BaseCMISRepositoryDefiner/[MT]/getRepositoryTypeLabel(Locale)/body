{
  ClassLoader classLoader=getClass().getClassLoader();
  ResourceBundle resourceBundle=ResourceBundle.getBundle(LanguageConstants.LANGUAGE_BASE_NAME,locale,classLoader);
  return resourceBundle.getString(_MODEL_RESOURCE_NAME_PREFIX + getClassName());
}

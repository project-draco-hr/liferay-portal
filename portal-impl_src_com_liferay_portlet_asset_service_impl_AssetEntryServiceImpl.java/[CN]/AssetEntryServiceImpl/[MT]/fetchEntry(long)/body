{
  AssetEntry entry=assetEntryLocalService.fetchEntry(entryId);
  if (entry != null) {
    if (AssetEntryPermission.contains(getPermissionChecker(),entry,ActionKeys.VIEW)) {
      return entry;
    }
  }
  return null;
}

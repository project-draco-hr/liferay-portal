{
  long[][] viewableTagIds=getViewableTagIds(tagIds,notTagIds);
  tagIds=viewableTagIds[0];
  notTagIds=viewableTagIds[1];
  return assetEntryLocalService.getEntriesCount(groupId,classNameIds,tagIds,notTagIds,andOperator,excludeZeroViewCount,publishDate,expirationDate);
}

{
  RubyInstanceConfig rubyInstanceConfig=new RubyInstanceConfig();
  rubyInstanceConfig.setLoader(PortalClassLoaderUtil.getClassLoader());
  _ruby=JavaEmbedUtils.initialize(new ArrayList<String>(),rubyInstanceConfig);
}

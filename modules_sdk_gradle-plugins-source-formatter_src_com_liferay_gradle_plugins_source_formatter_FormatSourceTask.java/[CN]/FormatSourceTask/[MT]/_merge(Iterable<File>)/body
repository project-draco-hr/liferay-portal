{
  StringBuilder sb=new StringBuilder();
  int i=0;
  for (  File file : files) {
    if (i > 0) {
      sb.append(",");
    }
    sb.append(FileUtil.getAbsolutePath(file));
    i++;
  }
  return sb.toString();
}

{
  StringBuilder sb=new StringBuilder();
  Project project=getProject();
  int i=0;
  for (  File file : files) {
    if (i > 0) {
      sb.append(",");
    }
    sb.append(project.relativePath(file));
    i++;
  }
  return sb.toString();
}

{
  PortletPreferences prefs=getPreferences();
  String tomcatLibDir=PropsUtil.get(PropsUtil.AUTO_DEPLOY_TOMCAT_LIB_DIR);
  return prefs.getValue(PropsUtil.AUTO_DEPLOY_TOMCAT_LIB_DIR,tomcatLibDir);
}

{
  PortletPreferences prefs=getPreferences();
  String interval=PropsUtil.get(PropsUtil.AUTO_DEPLOY_INTERVAL);
  return GetterUtil.getLong(prefs.getValue(PropsUtil.AUTO_DEPLOY_INTERVAL,interval));
}

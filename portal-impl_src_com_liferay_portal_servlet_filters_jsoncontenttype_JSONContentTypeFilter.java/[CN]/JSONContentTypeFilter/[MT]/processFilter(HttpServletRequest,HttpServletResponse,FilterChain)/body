{
  if (!BrowserSnifferUtil.isIe(request)) {
    processFilter(JSONContentTypeFilter.class.getName(),request,response,filterChain);
  }
 else {
    processFilter(JSONContentTypeFilter.class.getName(),request,new JSONContentTypeResponse(response),filterChain);
  }
}

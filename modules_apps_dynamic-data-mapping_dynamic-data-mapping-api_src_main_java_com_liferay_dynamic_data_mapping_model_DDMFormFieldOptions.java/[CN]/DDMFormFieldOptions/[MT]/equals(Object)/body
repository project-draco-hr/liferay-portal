{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof DDMFormFieldOptions)) {
    return false;
  }
  DDMFormFieldOptions ddmFormFieldOptions=(DDMFormFieldOptions)obj;
  if (Validator.equals(_defaultLocale,ddmFormFieldOptions._defaultLocale) && Validator.equals(_options,ddmFormFieldOptions._options)) {
    return true;
  }
  return false;
}

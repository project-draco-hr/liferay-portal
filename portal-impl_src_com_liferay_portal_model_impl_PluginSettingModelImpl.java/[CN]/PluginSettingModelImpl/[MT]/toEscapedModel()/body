{
  if (isEscapedModel()) {
    return (PluginSetting)this;
  }
 else {
    return (PluginSetting)Proxy.newProxyInstance(PluginSetting.class.getClassLoader(),new Class[]{PluginSetting.class},new AutoEscapeBeanHandler(this));
  }
}

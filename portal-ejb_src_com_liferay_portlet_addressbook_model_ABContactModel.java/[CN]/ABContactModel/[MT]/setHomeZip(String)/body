{
  if (((homeZip == null) && (_homeZip != null)) || ((homeZip != null) && (_homeZip == null)) || ((homeZip != null) && (_homeZip != null) && !homeZip.equals(_homeZip))) {
    if (!XSS_ALLOW_HOMEZIP) {
      homeZip=XSSUtil.strip(homeZip);
    }
    _homeZip=homeZip;
    setModified(true);
  }
}

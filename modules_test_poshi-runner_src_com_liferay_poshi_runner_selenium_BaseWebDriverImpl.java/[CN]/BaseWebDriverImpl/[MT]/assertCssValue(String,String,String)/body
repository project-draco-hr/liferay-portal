{
  WebElement webElement=getWebElement(locator);
  String value=webElement.getCssValue(cssAttribute);
  if (!value.equals(cssValue)) {
    throw new Exception("CSS Value " + value + " does not match "+ cssValue);
  }
}

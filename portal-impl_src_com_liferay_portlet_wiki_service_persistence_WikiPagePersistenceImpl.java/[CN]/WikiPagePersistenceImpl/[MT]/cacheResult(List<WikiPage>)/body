{
  for (  WikiPage wikiPage : wikiPages) {
    if (EntityCacheUtil.getResult(WikiPageModelImpl.ENTITY_CACHE_ENABLED,WikiPage.class,wikiPage.getPrimaryKey(),this) == null) {
      cacheResult(wikiPage);
    }
  }
}

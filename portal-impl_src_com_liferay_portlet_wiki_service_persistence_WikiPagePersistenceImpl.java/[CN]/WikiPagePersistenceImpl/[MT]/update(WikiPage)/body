{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WikiPage wikiPage) method. To improve performance, call update(WikiPage wikiPage, boolean merge).");
  }
  return update(wikiPage,false);
}

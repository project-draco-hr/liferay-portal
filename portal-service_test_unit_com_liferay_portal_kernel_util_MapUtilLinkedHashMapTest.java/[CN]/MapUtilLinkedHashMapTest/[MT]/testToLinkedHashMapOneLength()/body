{
  Map<String,String> map=MapUtil.toLinkedHashMap(new String[]{"one,1"},",");
  Assert.assertFalse(map.isEmpty());
  Assert.assertTrue(map.size() == 1);
  Assert.assertTrue(map.containsValue("1"));
  Assert.assertTrue(map.containsKey("one"));
}

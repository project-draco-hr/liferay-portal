{
  if (condition.getIncomingTransitionsCount() == 0) {
    throw new WorkflowException("No incoming transition found for condition " + condition.getName());
  }
  if (condition.getOutgoingTransitionsCount() < 2) {
    throw new WorkflowException("Less than 2 outgoing transitions found for condition " + condition.getName());
  }
}

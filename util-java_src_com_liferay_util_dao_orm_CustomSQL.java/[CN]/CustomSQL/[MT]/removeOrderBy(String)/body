{
  AtomicReference<String> sqlAtomicReference=new AtomicReference<String>(sql);
  int pos=sqlAtomicReference.get().indexOf(_ORDER_BY_CLAUSE);
  if (pos != -1) {
    sql=sqlAtomicReference.get().substring(0,pos);
  }
  return sql;
}

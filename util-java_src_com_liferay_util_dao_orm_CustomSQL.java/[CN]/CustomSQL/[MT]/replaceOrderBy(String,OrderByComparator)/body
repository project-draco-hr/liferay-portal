{
  if (obc == null) {
    return sql;
  }
  StringBuilder sb=new StringBuilder();
  sb.append(removeOrderBy(sql));
  sb.append(" ORDER BY ");
  sb.append(obc.getOrderBy());
  return sb.toString();
}

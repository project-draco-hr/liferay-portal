{
  if (Validator.isNull(configurationResourcePath)) {
    return null;
  }
  URL configurationResourceURL=EhcacheConfigurationUtil.class.getResource(configurationResourcePath);
  return getConfiguration(configurationResourceURL,clusterAware,usingDefault);
}

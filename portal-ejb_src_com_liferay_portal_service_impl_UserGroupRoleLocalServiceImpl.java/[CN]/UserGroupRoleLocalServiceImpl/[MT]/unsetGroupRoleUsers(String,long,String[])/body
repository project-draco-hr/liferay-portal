{
  for (int i=0; i < userIds.length; i++) {
    String userId=userIds[i];
    try {
      UserGroupRoleUtil.remove(new UserGroupRolePK(userId,roleId,groupId));
    }
 catch (    NoSuchUserGroupRoleException e) {
    }
  }
}

{
  ServletContext servletContext=super.getServletContext();
  if (Validator.isNotNull(_portletId)) {
    try {
      servletContext=getServletContext(servletContext,getServletRequest());
    }
 catch (    SystemException e) {
    }
  }
  return servletContext;
}

{
  if (!contains(permissionChecker,question,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,PollsQuestion.class.getName(),question.getQuestionId(),actionId);
  }
}

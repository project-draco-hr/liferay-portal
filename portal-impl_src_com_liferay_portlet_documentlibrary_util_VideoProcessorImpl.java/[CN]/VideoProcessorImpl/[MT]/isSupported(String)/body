{
  if (Validator.isNull(mimeType)) {
    return false;
  }
  try {
    if (XugglerUtil.isEnabled()) {
      return _videoMimeTypes.contains(mimeType);
    }
  }
 catch (  Exception e) {
  }
  return false;
}

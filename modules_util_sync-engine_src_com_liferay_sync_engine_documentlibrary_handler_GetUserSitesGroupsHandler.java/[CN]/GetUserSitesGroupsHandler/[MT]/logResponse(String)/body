{
  try {
    ObjectMapper objectMapper=new ObjectMapper();
    _remoteSyncSites=objectMapper.readValue(response,new TypeReference<List<SyncSite>>(){
    }
);
    super.logResponse("{\"count\":" + _remoteSyncSites.size() + "}");
  }
 catch (  Exception e) {
    _logger.error(e.getMessage(),e);
  }
}

{
  Path relativePath=_modulesDirPath.relativize(dirPath);
  String projectPathPrefix=relativePath.toString();
  projectPathPrefix=":" + StringUtil.replace(projectPathPrefix,File.separatorChar,CharPool.COLON);
  return gradlePropertiesTemplate.replace("[$PROJECT_PATH_PREFIX$]",projectPathPrefix);
}

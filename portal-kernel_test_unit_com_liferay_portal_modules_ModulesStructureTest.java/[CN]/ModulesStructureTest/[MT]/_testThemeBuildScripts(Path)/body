{
  if (!_contains(dirPath.resolve("package.json"),"\"liferay-theme-tasks\":")) {
    return;
  }
  Path gulpfileJsPath=dirPath.resolve("gulpfile.js");
  if (Files.notExists(gulpfileJsPath)) {
    Assert.fail("Missing " + gulpfileJsPath);
  }
}

{
  if (absolutePath.contains("/ant-bnd/") || absolutePath.contains("/osgi-util/") || absolutePath.contains("/registry-api/")|| absolutePath.contains("/sync-engine-shared/")) {
    return true;
  }
 else {
    return false;
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (ListType)session.get(ListTypeImpl.class,new Integer(listTypeId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

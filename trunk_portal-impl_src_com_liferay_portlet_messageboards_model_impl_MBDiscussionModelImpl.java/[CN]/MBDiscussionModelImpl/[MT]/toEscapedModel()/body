{
  if (isEscapedModel()) {
    return (MBDiscussion)this;
  }
 else {
    return (MBDiscussion)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

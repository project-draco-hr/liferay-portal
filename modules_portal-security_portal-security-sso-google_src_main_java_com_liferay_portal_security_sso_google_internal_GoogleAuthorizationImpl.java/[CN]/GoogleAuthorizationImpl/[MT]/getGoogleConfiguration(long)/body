{
  try {
    GoogleAuthorizationConfiguration googleConfiguration=_configurationFactory.getConfiguration(GoogleAuthorizationConfiguration.class,new CompanyServiceSettingsLocator(companyId,GoogleConstants.SERVICE_NAME));
    return googleConfiguration;
  }
 catch (  ConfigurationException ce) {
    _log.error("Unable to get Google configuration",ce);
  }
  return null;
}

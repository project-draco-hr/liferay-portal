{
  String asterisk="";
  if (addAsterisk) {
    asterisk="*";
  }
  String replacementProperty=_replacementStyles.get(property);
  CSSDeclaration replacementCSSDeclaration=styleRule.getDeclarationOfPropertyNameCaseInsensitive(asterisk + replacementProperty);
  CSSDeclaration cssDeclaration=styleRule.getDeclarationOfPropertyNameCaseInsensitive(asterisk + property);
  if (cssDeclaration != null) {
    cssDeclaration.setProperty(asterisk + replacementProperty);
  }
  if (replacementCSSDeclaration != null) {
    replacementCSSDeclaration.setProperty(asterisk + property);
  }
}

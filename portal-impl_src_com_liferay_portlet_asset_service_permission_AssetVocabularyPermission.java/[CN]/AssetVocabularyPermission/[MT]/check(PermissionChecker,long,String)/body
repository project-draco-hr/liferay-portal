{
  if (!contains(permissionChecker,vocabularyId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),AssetVocabulary.class.getName(),vocabularyId,actionId);
  }
}

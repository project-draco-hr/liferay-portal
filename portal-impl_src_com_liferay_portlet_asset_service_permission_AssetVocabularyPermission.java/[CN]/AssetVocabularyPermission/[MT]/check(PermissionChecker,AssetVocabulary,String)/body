{
  if (!contains(permissionChecker,vocabulary,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),AssetVocabulary.class.getName(),vocabulary.getVocabularyId(),actionId);
  }
}

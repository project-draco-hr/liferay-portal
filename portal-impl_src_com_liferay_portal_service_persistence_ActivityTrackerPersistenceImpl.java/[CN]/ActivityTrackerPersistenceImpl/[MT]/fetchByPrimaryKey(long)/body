{
  Session session=null;
  try {
    session=openSession();
    return (ActivityTracker)session.get(ActivityTrackerImpl.class,new Long(activityTrackerId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ActivityTracker activityTracker) method. Use update(ActivityTracker activityTracker, boolean merge) instead.");
  }
  return update(activityTracker,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ActivityTracker activityTracker) method. To improve performance, call update(ActivityTracker activityTracker, boolean merge).");
  }
  return update(activityTracker,false);
}

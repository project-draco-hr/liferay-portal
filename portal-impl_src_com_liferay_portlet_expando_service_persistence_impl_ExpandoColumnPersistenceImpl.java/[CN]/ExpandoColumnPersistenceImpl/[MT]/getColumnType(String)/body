{
  Integer type=ExpandoColumnModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ExpandoColumnModelImpl.TABLE_NAME);
  }
  return type;
}

{
  if (entryId == TagsEntryConstants.DEFAULT_PARENT_ENTRY_ID) {
    return PortletPermissionUtil.contains(permissionChecker,plid,PortletKeys.TAGS_ADMIN,actionId);
  }
 else {
    return contains(permissionChecker,entryId,actionId);
  }
}

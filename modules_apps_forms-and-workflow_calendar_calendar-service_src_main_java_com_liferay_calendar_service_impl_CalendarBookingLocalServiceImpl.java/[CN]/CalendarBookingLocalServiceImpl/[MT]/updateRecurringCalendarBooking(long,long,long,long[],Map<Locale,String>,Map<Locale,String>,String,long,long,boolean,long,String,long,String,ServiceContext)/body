{
  CalendarBooking calendarBooking=calendarBookingPersistence.findByPrimaryKey(calendarBookingId);
  List<CalendarBooking> relatedCalendarBookings=getRelatedCalendarBookings(calendarBooking);
  List<String> unchangedList=getUnchangedList(calendarBooking,calendarId,titleMap,descriptionMap,location,startTime,endTime,allDay,firstReminder,firstReminderType,secondReminder,secondReminderType);
  updateCalendarBookingsByChanges(userId,calendarId,childCalendarIds,titleMap,descriptionMap,location,startTime,endTime,allDay,firstReminder,firstReminderType,secondReminder,secondReminderType,serviceContext,relatedCalendarBookings,unchangedList);
  return calendarBooking;
}

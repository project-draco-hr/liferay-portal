{
  List<CalendarBooking> recurringCalendarBookings=getRecurringCalendarBookings(calendarBooking);
  List<CalendarBooking> followingRecurringCalendarBookings=new ArrayList<>();
  for (  CalendarBooking recurringCalendarBooking : recurringCalendarBookings) {
    if (recurringCalendarBooking.getStartTime() > startTime) {
      followingRecurringCalendarBookings.add(recurringCalendarBooking);
    }
  }
  return followingRecurringCalendarBookings;
}

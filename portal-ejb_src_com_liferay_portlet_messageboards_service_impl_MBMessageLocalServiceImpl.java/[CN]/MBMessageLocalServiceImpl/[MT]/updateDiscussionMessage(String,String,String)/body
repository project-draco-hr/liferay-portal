{
  String categoryId=Company.SYSTEM;
  List files=new ArrayList();
  double priority=0.0;
  PortletPreferences prefs=null;
  return updateMessage(messageId,categoryId,subject,body,files,priority,prefs);
}

{
  ResourceLocalServiceUtil.addModelResources(message.getCompanyId(),category.getGroupId(),message.getUserId(),MBMessage.class.getName(),message.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

{
  Session session=null;
  try {
    session=openSession();
    return (ShoppingCategory)session.get(ShoppingCategoryImpl.class,new Long(categoryId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

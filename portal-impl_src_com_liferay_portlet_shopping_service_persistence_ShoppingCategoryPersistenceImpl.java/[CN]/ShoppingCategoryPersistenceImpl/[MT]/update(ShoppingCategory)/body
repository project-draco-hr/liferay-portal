{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingCategory shoppingCategory) method. To improve performance, call update(ShoppingCategory shoppingCategory, boolean merge).");
  }
  return update(shoppingCategory,false);
}

{
  long[] folderIds=ParamUtil.getLongValues(request,"rowIdsBookmarksFolder");
  List<BookmarksFolder> folders=new ArrayList<>();
  for (  long folderId : folderIds) {
    if ((folderId > 0) && (folderId != BookmarksFolderConstants.DEFAULT_PARENT_FOLDER_ID)) {
      BookmarksFolder folder=BookmarksFolderServiceUtil.getFolder(folderId);
      folders.add(folder);
    }
  }
  return folders;
}

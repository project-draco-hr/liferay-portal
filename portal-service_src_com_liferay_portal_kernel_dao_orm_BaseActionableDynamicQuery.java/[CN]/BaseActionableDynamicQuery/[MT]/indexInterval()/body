{
  if (Validator.isNull(_searchEngineId) || (_documents == null) || _documents.isEmpty()) {
    return;
  }
  SearchEngineUtil.updateDocuments(_searchEngineId,_companyId,new ArrayList<Document>(_documents));
  _documents.clear();
}

{
  try {
    return updateExportPortletPreferences(portletDataContext,portletPreferences,portletDataContext.getPortletId());
  }
 catch (  Exception e) {
    throw new PortletDataException("Unable to update portlet preferences during export",e);
  }
}

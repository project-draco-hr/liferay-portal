{
  try {
    removePluginPackage(moduleId,repositoryURL);
  }
 catch (  IOException ioe) {
  }
  addPluginPackage(moduleId,name,version,author,types,tags,licenses,liferayVersions,shortDescription,longDescription,changeLog,pageURL,repositoryURL,status,installedVersion);
}

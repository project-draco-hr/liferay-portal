{
  if (_log.isDebugEnabled()) {
    _log.debug("Ehcache stream client thread for cache " + ehcache.getName());
  }
  _ehcache=ehcache;
  setDaemon(true);
  setName(EhcacheStreamClientThread.class.getName() + " - " + ehcache.getName());
  setPriority(Thread.NORM_PRIORITY);
}

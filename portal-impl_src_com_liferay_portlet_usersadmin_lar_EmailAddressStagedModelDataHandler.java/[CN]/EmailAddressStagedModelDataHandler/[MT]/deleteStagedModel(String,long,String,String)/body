{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  EmailAddress emailAddress=EmailAddressLocalServiceUtil.fetchEmailAddressByUuidAndCompanyId(uuid,group.getCompanyId());
  deleteStagedModel(emailAddress);
}

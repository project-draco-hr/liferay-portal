{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  EmailAddress emailAddress=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  EmailAddressLocalServiceUtil.deleteEmailAddress(emailAddress);
}

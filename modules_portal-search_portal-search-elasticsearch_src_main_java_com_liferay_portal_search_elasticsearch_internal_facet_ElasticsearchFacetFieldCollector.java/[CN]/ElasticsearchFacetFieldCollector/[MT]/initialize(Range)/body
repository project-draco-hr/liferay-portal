{
  _aggregation=range;
  for (  Range.Bucket bucket : range.getBuckets()) {
    String key=StringUtil.replace(bucket.getKeyAsString(),StringPool.DASH,_TO_STRING);
    key=StringPool.OPEN_BRACKET.concat(key).concat(StringPool.CLOSE_BRACKET);
    _counts.put(key,(int)bucket.getDocCount());
  }
}

{
  DDMExpressionImpl ddmExpression=new DDMExpressionImpl<>("true",String.class);
  Object result=ddmExpression.toRetunType(false);
  Assert.assertTrue(result instanceof String);
  Assert.assertEquals("false",result);
}

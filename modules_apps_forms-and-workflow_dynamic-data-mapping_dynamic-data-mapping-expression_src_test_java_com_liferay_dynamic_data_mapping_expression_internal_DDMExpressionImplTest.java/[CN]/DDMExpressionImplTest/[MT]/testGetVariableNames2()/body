{
  DDMExpressionImpl ddmExpressionImpl=new DDMExpressionImpl<>("(((1 + 2) * (1 - 2 / x)) + log(1 * 6 - y))",Number.class);
  Set<String> expectedVariableNames=new HashSet<>(Arrays.asList("x","y"));
  Assert.assertEquals(expectedVariableNames,ddmExpressionImpl.getExpressionVariableNames());
}

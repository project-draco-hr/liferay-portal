{
  Document doc=getImageDocument(companyId,groupId,folderId,imageId,name,description,modifiedDate,assetCategoryIds,assetTagNames,expandoBridge);
  SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
}

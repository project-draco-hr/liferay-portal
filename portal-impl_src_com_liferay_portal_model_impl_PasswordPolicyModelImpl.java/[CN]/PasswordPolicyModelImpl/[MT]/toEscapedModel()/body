{
  if (isEscapedModel()) {
    return (PasswordPolicy)this;
  }
 else {
    return (PasswordPolicy)Proxy.newProxyInstance(PasswordPolicy.class.getClassLoader(),new Class[]{PasswordPolicy.class},new AutoEscapeBeanHandler(this));
  }
}

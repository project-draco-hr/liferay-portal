{
  try (LoggingTimer loggingTimer=new LoggingTimer()){
    runSQL("create index IX_8257E37B on Group_ (classNameId, classPK)");
    runSQL("create index IX_B41E3F78 on Group_ " + "(companyId, groupId, parentGroupId)");
  }
 }

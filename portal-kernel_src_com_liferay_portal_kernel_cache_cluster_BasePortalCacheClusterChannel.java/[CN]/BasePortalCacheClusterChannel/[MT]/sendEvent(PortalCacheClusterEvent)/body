{
  if (_started == false) {
synchronized (this) {
      if (_started == false) {
        _dispatchThread.start();
        _started=true;
      }
    }
  }
  try {
    _eventQueue.put(event);
  }
 catch (  InterruptedException ignored) {
  }
}

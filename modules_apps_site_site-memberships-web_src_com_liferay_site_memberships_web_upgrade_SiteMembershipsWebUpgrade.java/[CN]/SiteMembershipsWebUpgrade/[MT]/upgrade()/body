{
  UpgradePortletId upgradePortletId=new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"174",SiteMembershipsPortletKeys.SITE_MEMBERSHIPS_ADMIN}};
    }
  }
;
  _releaseLocalService.updateRelease("com.liferay.site.memberships.web",Collections.<UpgradeProcess>singletonList(upgradePortletId),1,1,false);
}

{
  _dispatchThread=new Thread(this,"PortalCacheClusterChannel dispatch thread-" + _dispatchThreadCounter.getAndIncrement());
  _eventQueue=new CoalescedPipe<>(new PortalCacheClusterEventCoalesceComparator());
}

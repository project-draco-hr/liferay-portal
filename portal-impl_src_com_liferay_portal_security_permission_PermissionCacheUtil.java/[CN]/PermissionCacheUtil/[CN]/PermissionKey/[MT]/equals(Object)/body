{
  PermissionKey permissionKey=(PermissionKey)obj;
  if ((permissionKey._groupId == _groupId) && Validator.equals(permissionKey._name,_name) && Validator.equals(permissionKey._primKey,_primKey)&& Arrays.equals(permissionKey._roleIds,_roleIds)&& Validator.equals(permissionKey._actionId,_actionId)) {
    return true;
  }
  return false;
}

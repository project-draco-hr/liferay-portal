{
  FormStructure structure=formStructurePersistence.findByPrimaryKey(formStructureLink.getFormStructureId());
  resourceLocalService.addModelResources(structure.getCompanyId(),structure.getGroupId(),structure.getUserId(),FormStructureLink.class.getName(),formStructureLink.getFormStructureLinkId(),communityPermissions,guestPermissions);
}

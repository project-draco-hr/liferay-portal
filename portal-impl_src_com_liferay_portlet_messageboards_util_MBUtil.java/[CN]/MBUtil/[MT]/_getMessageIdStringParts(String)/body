{
  int beginOfIds=messageIdString.indexOf(MBUtil.MESSAGE_POP_PORTLET_PREFIX) + MBUtil.MESSAGE_POP_PORTLET_PREFIX.length();
  int endOfIds=messageIdString.indexOf(CharPool.AT);
  return StringUtil.split(messageIdString.substring(beginOfIds,endOfIds),CharPool.PERIOD);
}

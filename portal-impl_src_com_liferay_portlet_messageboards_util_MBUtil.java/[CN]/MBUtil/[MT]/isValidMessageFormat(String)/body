{
  String editorName=PropsUtil.get(BB_CODE_EDITOR_WYSIWYG_IMPL_KEY);
  if (messageFormat.equals("bbcode") && !(editorName.equals("bbcode") || editorName.equals("ckeditor_bbcode"))) {
    return false;
  }
  return true;
}

{
  Settings settings=SettingsFactoryUtil.getGroupServiceSettings(groupId,MBConstants.SERVICE_NAME);
  ParameterMapSettings configurationSettings=new ParameterMapSettings(settings,request.getParameterMap());
  return new MBSettings(configurationSettings);
}

{
  Settings settings=SettingsFactoryUtil.getGroupServiceSettings(groupId,MBConstants.SERVICE_NAME);
  ParameterMapSettings parameterMapSettings=new ParameterMapSettings(request.getParameterMap(),settings);
  return new MBSettings(parameterMapSettings);
}

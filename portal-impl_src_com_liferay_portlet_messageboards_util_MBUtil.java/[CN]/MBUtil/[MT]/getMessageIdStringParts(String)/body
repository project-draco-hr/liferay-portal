{
  int pos=messageIdString.indexOf(CharPool.AT);
  String target=messageIdString.substring(MBUtil.MESSAGE_POP_PORTLET_PREFIX.length() + getMessageIdStringOffset(),pos);
  return StringUtil.split(target,CharPool.PERIOD);
}

{
  return StringPool.LESS_THAN + SMTP_PORTLET_PREFIX + categoryId+ StringPool.PERIOD+ messageId+ StringPool.AT+ PropsUtil.get(PropsUtil.SMTP_SERVER_SUBDOMAIN)+ StringPool.PERIOD+ mx+ StringPool.GREATER_THAN;
}

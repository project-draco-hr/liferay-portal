{
  ShoppingCategory category=shoppingCategoryLocalService.getCategory(categoryId);
  getShoppingCategoryPermission().check(getPermissionChecker(),category,ActionKeys.DELETE);
  shoppingCategoryLocalService.deleteCategory(categoryId);
}

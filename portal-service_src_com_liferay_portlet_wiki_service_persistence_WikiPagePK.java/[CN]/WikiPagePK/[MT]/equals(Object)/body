{
  if (obj == null) {
    return false;
  }
  WikiPagePK pk=null;
  try {
    pk=(WikiPagePK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((nodeId == pk.nodeId) && (title.equals(pk.title)) && (version == pk.version)) {
    return true;
  }
 else {
    return false;
  }
}

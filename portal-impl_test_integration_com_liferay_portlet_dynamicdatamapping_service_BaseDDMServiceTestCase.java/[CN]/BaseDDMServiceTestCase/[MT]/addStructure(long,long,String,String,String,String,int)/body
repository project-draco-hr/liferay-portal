{
  return DDMStructureLocalServiceUtil.addStructure(TestPropsValues.getUserId(),group.getGroupId(),parentStructureId,classNameId,structureKey,getDefaultLocaleMap(name),null,xsd,storageType,type,ServiceTestUtil.getServiceContext(group.getGroupId()));
}

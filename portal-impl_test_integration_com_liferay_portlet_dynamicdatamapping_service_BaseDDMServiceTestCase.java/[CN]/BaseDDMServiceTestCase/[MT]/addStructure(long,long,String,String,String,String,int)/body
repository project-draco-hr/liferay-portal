{
  return DDMStructureLocalServiceUtil.addStructure(TestPropsValues.getUserId(),group.getGroupId(),parentStructureId,classNameId,structureKey,getDefaultLocaleMap(name),null,definition,storageType,type,ServiceContextTestUtil.getServiceContext(group.getGroupId()));
}

{
  return DDMTemplateLocalServiceUtil.addTemplate(TestPropsValues.getUserId(),group.getGroupId(),classNameId,classPK,templateKey,getDefaultLocaleMap(name),null,type,mode,language,script,cacheable,ServiceTestUtil.getServiceContext(group.getGroupId()));
}

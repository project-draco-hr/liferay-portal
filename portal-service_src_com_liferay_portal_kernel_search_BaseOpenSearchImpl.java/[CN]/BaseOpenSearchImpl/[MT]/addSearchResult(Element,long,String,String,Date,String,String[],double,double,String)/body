{
  if (format.equals("rss")) {
    addSearchResultRSS(root,groupId,title,link,updated,summary,tags,ratings,score);
  }
 else {
    addSearchResultAtom(root,groupId,title,link,updated,summary,tags,ratings,score);
  }
}

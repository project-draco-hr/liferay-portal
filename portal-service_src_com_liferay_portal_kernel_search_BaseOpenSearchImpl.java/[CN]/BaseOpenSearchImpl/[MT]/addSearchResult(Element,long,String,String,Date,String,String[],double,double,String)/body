{
  String groupIdValue=String.valueOf(groupId);
  if (format.equals("rss")) {
    addSearchResultRSS(root,groupIdValue,title,link,updated,summary,tags,ratings,score);
  }
 else {
    addSearchResultAtom(root,groupIdValue,title,link,updated,summary,tags,ratings,score);
  }
}

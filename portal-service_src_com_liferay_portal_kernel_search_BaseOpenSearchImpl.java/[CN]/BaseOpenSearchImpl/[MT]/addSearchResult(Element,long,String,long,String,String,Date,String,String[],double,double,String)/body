{
  if (format.equals("rss")) {
    addSearchResultRSS(root,groupId,entryClassName,entryClassPK,title,link,updated,summary,tags,ratings,score);
  }
 else {
    addSearchResultAtom(root,groupId,entryClassName,entryClassPK,title,link,updated,summary,tags,ratings,score);
  }
}

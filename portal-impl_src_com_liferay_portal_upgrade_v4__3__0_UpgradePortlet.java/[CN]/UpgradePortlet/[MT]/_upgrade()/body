{
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(PortletImpl.TABLE_NAME,PortletImpl.TABLE_COLUMNS,new PKUpgradeColumnImpl());
  upgradeTable.updateTable();
  DBUtil.getInstance().executeSQL(_UPGRADE_SCHEMA);
}

{
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(PortletImpl.TABLE_NAME,PortletImpl.TABLE_COLUMNS,new PKUpgradeColumnImpl("id_",false));
  upgradeTable.updateTable();
  runSQL(_UPGRADE_SCHEMA);
}

{
  String url=StringPool.BLANK;
  try {
    url=PermissionsURLTag.doTag(StringPool.BLANK,WikiNode.class.getName(),_node.getName(),null,String.valueOf(_node.getNodeId()),LiferayWindowState.POP_UP.toString(),null,themeDisplay.getRequest());
  }
 catch (  Exception e) {
  }
  return url;
}

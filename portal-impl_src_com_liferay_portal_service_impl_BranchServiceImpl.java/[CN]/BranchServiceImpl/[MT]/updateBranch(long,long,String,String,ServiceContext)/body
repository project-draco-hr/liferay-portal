{
  if (!getPermissionChecker().hasPermission(groupId,Branch.class.getName(),branchId,ActionKeys.UPDATE)) {
    throw new PrincipalException();
  }
  return branchLocalService.updateBranch(branchId,name,description,serviceContext);
}

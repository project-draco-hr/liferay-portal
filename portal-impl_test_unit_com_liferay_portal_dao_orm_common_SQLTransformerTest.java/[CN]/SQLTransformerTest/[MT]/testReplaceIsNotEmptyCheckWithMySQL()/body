{
  String inputSQL=_SQL_PREFIX + _SQL_INPUT_WHERE_INE;
  String expectedSQL=_SQL_PREFIX + "(st.someField IS NOT NULL AND st.someField != '') AND " + "(st.someOtherField IS NOT NULL AND st.someOtherField != '')";
  testReplaceEmptyCheck(MySQLDB.getInstance(),"_replaceIsNotEmptyCheck",inputSQL,expectedSQL);
}

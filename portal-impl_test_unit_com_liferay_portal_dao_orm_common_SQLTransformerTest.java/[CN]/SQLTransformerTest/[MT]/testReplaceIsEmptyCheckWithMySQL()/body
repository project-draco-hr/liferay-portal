{
  String inputSQL=_SQL_PREFIX + _SQL_INPUT_WHERE_IE;
  String expectedSQL=_SQL_PREFIX + "(st.someField IS NOT NULL AND st.someField = '') OR " + "(st.someOtherField IS NOT NULL AND st.someOtherField = '')";
  testReplaceEmptyCheck(MySQLDB.getInstance(),"_replaceIsEmptyCheck",inputSQL,expectedSQL);
}

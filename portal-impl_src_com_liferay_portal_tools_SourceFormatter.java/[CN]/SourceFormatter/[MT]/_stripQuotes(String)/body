{
  String[] parts=StringUtil.split(s,CharPool.QUOTE);
  int i=1;
  while (i < parts.length) {
    s=StringUtil.replaceFirst(s,StringPool.QUOTE + parts[i] + StringPool.QUOTE,StringPool.BLANK);
    i=i + 2;
  }
  return s;
}

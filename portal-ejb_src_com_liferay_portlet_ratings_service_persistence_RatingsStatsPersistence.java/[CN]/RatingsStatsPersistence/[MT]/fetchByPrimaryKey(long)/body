{
  Session session=null;
  try {
    session=openSession();
    return (RatingsStats)session.get(RatingsStatsImpl.class,new Long(statsId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

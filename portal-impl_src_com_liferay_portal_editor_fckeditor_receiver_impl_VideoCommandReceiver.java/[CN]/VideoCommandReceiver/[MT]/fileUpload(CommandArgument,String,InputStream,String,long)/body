{
  if (!XugglerUtil.isEnabled()) {
    return String.valueOf(ServletResponseConstants.SC_VIDEO_PREVIEW_DISABLED_EXCEPTION);
  }
  return super.fileUpload(commandArgument,fileName,inputStream,contentType,size);
}

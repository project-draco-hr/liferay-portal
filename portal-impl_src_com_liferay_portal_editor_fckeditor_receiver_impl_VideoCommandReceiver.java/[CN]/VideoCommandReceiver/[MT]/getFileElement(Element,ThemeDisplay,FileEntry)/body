{
  fileElement=super.getFileElement(fileElement,themeDisplay,fileEntry);
  if (!VideoProcessorUtil.hasVideo(fileEntry.getFileVersion())) {
    fileElement.setAttribute("errorMessage",LanguageUtil.get(themeDisplay.getLocale(),"video-still-generating-try-later"));
  }
  return fileElement;
}

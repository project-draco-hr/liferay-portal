{
  fileElement=super.getFileElement(fileElement,themeDisplay,fileEntry);
  if (!VideoProcessorUtil.hasVideo(fileEntry.getFileVersion())) {
    fileElement.setAttribute("errorMessage",LanguageUtil.get(themeDisplay.getLocale(),"video-preview-has-not-been-generated-yet-please-retry-" + "later"));
  }
  return fileElement;
}

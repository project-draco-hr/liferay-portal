{
  fileElement=super.getFileElement(commandArgument,fileElement,fileEntry);
  if (!VideoProcessorUtil.hasVideo(fileEntry.getFileVersion())) {
    fileElement.setAttribute("errorMessage",LanguageUtil.get(commandArgument.getLocale(),"the-video-preview-is-not-yet-ready.-please-try-again-" + "later"));
  }
  return fileElement;
}

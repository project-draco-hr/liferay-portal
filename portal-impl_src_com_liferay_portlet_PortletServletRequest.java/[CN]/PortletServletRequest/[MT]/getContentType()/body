{
  if (_lifecycle.equals(PortletRequest.ACTION_PHASE) || _lifecycle.equals(PortletRequest.RESOURCE_PHASE) || _isUploadRequest()) {
    return super.getContentType();
  }
 else {
    return null;
  }
}

{
  if (permissionChecker.hasOwnerPermission(vocabulary.getCompanyId(),AssetCategoryVocabulary.class.getName(),vocabulary.getVocabularyId(),vocabulary.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(vocabulary.getGroupId(),AssetCategoryVocabulary.class.getName(),vocabulary.getVocabularyId(),actionId);
}

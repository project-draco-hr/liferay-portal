{
  String portletName=_announcementsRequestHelper.getPortletName();
  if (!portletName.equals(AnnouncementsPortletKeys.ALERTS) || (portletName.equals(AnnouncementsPortletKeys.ALERTS) && AnnouncementsEntryPermission.contains(_announcementsRequestHelper.getPermissionChecker(),_announcementsRequestHelper.getLayout(),AnnouncementsPortletKeys.ANNOUNCEMENTS_ADMIN,ActionKeys.VIEW))) {
    return true;
  }
  return false;
}

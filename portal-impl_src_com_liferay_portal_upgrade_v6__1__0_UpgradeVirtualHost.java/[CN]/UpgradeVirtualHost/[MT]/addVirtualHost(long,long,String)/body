{
  long virtualHostId=increment();
  runSQL("insert into VirtualHost (virtualHostId, companyId, layoutSetId, " + "virtualHostName) values (" + virtualHostId + ", "+ companyId+ ", "+ layoutSetId+ ", '"+ virtualHostName+ "')");
}

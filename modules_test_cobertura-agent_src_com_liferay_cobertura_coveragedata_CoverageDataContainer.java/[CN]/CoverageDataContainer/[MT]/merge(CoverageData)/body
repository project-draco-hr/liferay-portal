{
  CoverageDataContainer otherCoverageDataContainer=(CoverageDataContainer)otherCoverageData;
  Map<Object,CoverageData> otherChildren=otherCoverageDataContainer.children;
  for (  Entry<Object,CoverageData> entry : otherChildren.entrySet()) {
    CoverageData otherChild=entry.getValue();
    CoverageData myChild=children.putIfAbsent(entry.getKey(),otherChild);
    if (myChild != null) {
      myChild.merge(otherChild);
    }
  }
}

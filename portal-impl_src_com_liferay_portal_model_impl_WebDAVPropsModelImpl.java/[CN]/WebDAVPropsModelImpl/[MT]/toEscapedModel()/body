{
  if (isEscapedModel()) {
    return (WebDAVProps)this;
  }
 else {
    return (WebDAVProps)Proxy.newProxyInstance(WebDAVProps.class.getClassLoader(),new Class[]{WebDAVProps.class},new AutoEscapeBeanHandler(this));
  }
}

{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  PortletDisplay portletDisplay=themeDisplay.getPortletDisplay();
  WikiWebComponentProvider wikiWebComponentProvider=WikiWebComponentProvider.getWikiWebComponentProvider();
  SettingsFactory settingsFactory=wikiWebComponentProvider.getSettingsFactory();
  WikiPortletInstanceSettings wikiPortletInstanceSettings=settingsFactory.getSettings(WikiPortletInstanceSettings.class,new PortletInstanceSettingsLocator(themeDisplay.getLayout(),portletDisplay.getId()));
  return wikiPortletInstanceSettings;
}

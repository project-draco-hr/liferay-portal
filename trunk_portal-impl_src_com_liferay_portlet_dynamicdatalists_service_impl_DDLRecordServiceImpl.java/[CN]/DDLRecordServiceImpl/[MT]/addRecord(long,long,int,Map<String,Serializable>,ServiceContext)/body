{
  DDLRecordSetPermission.check(getPermissionChecker(),recordSetId,ActionKeys.ADD_RECORD);
  return ddlRecordLocalService.addRecord(getUserId(),groupId,recordSetId,displayIndex,fieldsMap,serviceContext);
}

{
  if (isEscapedModel()) {
    return (ExpandoTableColumns)this;
  }
 else {
    ExpandoTableColumns model=new ExpandoTableColumnsImpl();
    model.setEscapedModel(true);
    model.setTableId(getTableId());
    model.setColumnId(getColumnId());
    model=(ExpandoTableColumns)Proxy.newProxyInstance(ExpandoTableColumns.class.getClassLoader(),new Class[]{ExpandoTableColumns.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

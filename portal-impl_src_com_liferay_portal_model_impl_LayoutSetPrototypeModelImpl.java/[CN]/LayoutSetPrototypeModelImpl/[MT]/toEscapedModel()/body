{
  if (isEscapedModel()) {
    return (LayoutSetPrototype)this;
  }
 else {
    return (LayoutSetPrototype)Proxy.newProxyInstance(LayoutSetPrototype.class.getClassLoader(),new Class[]{LayoutSetPrototype.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (_instrument && (_classData.getNumberOfValidLines() == 0) && _logger.isWarnEnabled()) {
    _logger.warn("No line number information found for class " + _className + ". Please recompile with debug info.");
  }
}

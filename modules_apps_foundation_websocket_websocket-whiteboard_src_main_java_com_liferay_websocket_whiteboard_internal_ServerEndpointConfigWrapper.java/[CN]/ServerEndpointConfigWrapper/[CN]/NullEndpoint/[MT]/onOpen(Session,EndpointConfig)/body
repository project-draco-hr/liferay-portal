{
  try {
    session.close(new CloseReason(CloseReason.CloseCodes.GOING_AWAY,"Service has gone away"));
  }
 catch (  IOException ioe) {
    _log.log(LogService.LOG_ERROR,"It is not possible close the session",ioe);
  }
}

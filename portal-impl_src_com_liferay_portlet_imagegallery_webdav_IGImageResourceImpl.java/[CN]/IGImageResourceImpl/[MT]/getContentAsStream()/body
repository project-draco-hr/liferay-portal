{
  try {
    Image image=ImageLocalServiceUtil.getImage(_image.getLargeImageId());
    byte[] bytes=image.getTextObj();
    return new UnsyncByteArrayInputStream(bytes);
  }
 catch (  Exception e) {
    throw new WebDAVException(e);
  }
}

{
  if (_log.isDebugEnabled()) {
    _log.debug("Instantiate " + PropsValues.CONTROL_PANEL_DEFAULT_ENTRY_CLASS);
  }
  ClassLoader classLoader=PACLClassLoaderUtil.getPortalClassLoader();
  _originalControlPanelEntry=(ControlPanelEntry)InstanceFactory.newInstance(classLoader,PropsValues.CONTROL_PANEL_DEFAULT_ENTRY_CLASS);
  _controlPanelEntry=_originalControlPanelEntry;
}

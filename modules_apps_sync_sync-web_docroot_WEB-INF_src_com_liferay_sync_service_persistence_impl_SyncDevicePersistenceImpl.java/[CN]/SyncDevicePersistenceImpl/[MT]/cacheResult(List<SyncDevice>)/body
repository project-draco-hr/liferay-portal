{
  for (  SyncDevice syncDevice : syncDevices) {
    if (entityCache.getResult(SyncDeviceModelImpl.ENTITY_CACHE_ENABLED,SyncDeviceImpl.class,syncDevice.getPrimaryKey()) == null) {
      cacheResult(syncDevice);
    }
 else {
      syncDevice.resetOriginalValues();
    }
  }
}

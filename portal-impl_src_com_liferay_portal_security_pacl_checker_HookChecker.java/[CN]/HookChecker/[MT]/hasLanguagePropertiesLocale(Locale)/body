{
  if (_languagePropertiesLanguageIds.contains(locale.getLanguage())) {
    return true;
  }
  if (_languagePropertiesLanguageIds.contains(locale.getLanguage() + "_" + locale.getCountry())) {
    return true;
  }
  return false;
}

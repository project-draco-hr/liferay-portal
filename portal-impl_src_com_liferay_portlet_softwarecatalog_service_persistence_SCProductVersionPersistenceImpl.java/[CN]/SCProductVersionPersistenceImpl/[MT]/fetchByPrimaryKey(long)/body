{
  Session session=null;
  try {
    session=openSession();
    return (SCProductVersion)session.get(SCProductVersionImpl.class,new Long(productVersionId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(productVersionId,com.liferay.portlet.softwarecatalog.model.SCFrameworkVersion.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(productVersionId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(productVersionId,com.liferay.portlet.softwarecatalog.model.SCFrameworkVersion.class.getName());
    }
  }
}

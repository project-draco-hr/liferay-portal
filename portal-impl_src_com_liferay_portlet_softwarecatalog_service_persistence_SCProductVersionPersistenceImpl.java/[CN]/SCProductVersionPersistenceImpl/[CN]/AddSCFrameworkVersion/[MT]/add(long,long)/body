{
  if (!_persistenceImpl.containsSCFrameworkVersion.contains(productVersionId,frameworkVersionId)) {
    _sqlUpdate.update(new Object[]{new Long(productVersionId),new Long(frameworkVersionId)});
  }
}

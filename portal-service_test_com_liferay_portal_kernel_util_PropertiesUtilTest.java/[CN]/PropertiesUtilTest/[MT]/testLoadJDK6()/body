{
  if (JavaProps.isJDK6()) {
    Properties properties=PropertiesUtil.loadJDK6(new UnsyncStringReader(propertiesString));
    for (    String[] property : propertyArray) {
      assertEquals(property[1],properties.get(property[0]));
    }
  }
}

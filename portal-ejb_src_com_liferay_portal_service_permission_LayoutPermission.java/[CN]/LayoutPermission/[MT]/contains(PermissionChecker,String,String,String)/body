{
  String groupId=Layout.getGroupId(ownerId);
  if (GroupPermission.contains(permissionChecker,groupId,ActionKeys.MANAGE_LAYOUTS)) {
    return true;
  }
  return permissionChecker.hasPermission(groupId,Layout.class.getName(),new LayoutPK(layoutId,ownerId).toString(),actionId);
}

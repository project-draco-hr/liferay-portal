{
  if (isEscapedModel()) {
    return (UserGroupRole)this;
  }
 else {
    return (UserGroupRole)Proxy.newProxyInstance(UserGroupRole.class.getClassLoader(),new Class[]{UserGroupRole.class},new AutoEscapeBeanHandler(this));
  }
}

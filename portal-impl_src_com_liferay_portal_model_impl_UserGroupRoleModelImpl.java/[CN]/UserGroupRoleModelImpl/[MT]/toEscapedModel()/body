{
  if (isEscapedModel()) {
    return (UserGroupRole)this;
  }
 else {
    return (UserGroupRole)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  UserGroupRole model=new UserGroupRoleImpl();
  model.setUserId(getUserId());
  model.setGroupId(getGroupId());
  model.setRoleId(getRoleId());
  if (true) {
    model=(UserGroupRole)Proxy.newProxyInstance(UserGroupRole.class.getClassLoader(),new Class[]{UserGroupRole.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

{
  if (PropsValues.SEARCH_PERMISSION_FILTER_ENABLED) {
    try {
      booleanFilter=doGetPermissionBooleanFilter(companyId,groupIds,userId,className,booleanFilter,searchContext);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
  return booleanFilter;
}

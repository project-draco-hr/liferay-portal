{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  SACPEntry newSACPEntry=addSACPEntry();
  _persistence.clearCache();
  SACPEntry existingSACPEntry=_persistence.findByPrimaryKey(newSACPEntry.getPrimaryKey());
  Assert.assertEquals(existingSACPEntry.getCompanyId(),ReflectionTestUtil.invoke(existingSACPEntry,"getOriginalCompanyId",new Class<?>[0]));
  Assert.assertTrue(Validator.equals(existingSACPEntry.getName(),ReflectionTestUtil.invoke(existingSACPEntry,"getOriginalName",new Class<?>[0])));
}

{
  String displayName=dirName.substring(0,dirName.length() - (_pluginType.length() + 1));
  StringBundler sb=new StringBundler(5);
  sb.append(displayName);
  sb.append(SANDBOX_MARKER);
  sb.append(_pluginType);
  return sb.toString();
}

{
  ShoppingCart newShoppingCart=addShoppingCart();
  _persistence.clearCache();
  ShoppingCart existingShoppingCart=_persistence.findByPrimaryKey(newShoppingCart.getPrimaryKey());
  Assert.assertEquals(existingShoppingCart.getGroupId(),ReflectionTestUtil.invoke(existingShoppingCart,"getOriginalGroupId",new Class<?>[0]));
  Assert.assertEquals(existingShoppingCart.getUserId(),ReflectionTestUtil.invoke(existingShoppingCart,"getOriginalUserId",new Class<?>[0]));
}

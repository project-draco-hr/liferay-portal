{
  if (isPortalProperty(oldKey)) {
    _log.error("Portal property \"" + oldKey + "\" was migrated to the module property \""+ newKey+ "\" and it should be defined within the module \""+ moduleName+ "\"");
  }
}

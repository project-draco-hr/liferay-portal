{
  if (portalProperties.containsKey(key)) {
    _log.error("Vaadin framework is not bundled with portal anymore, so " + "portal property \"" + key + "\" might be obsolete. "+ "Please refer to Vaadin documentation, how to configure "+ "Vaadin with the portal.");
  }
}

{
  try (LoggingTimer loggingTimer=new LoggingTimer()){
    if (systemProperties.containsKey(oldKey)) {
      _log.error("System property \"" + oldKey + "\" was modularized to "+ moduleName+ " as \""+ newKey+ "\"");
    }
  }
 }

{
  Map<Long,Integer> actual=new HashMap<>(termCollectors.size());
  for (  TermCollector termCollector : termCollectors) {
    actual.put(Long.valueOf(termCollector.getTerm()),termCollector.getFrequency());
  }
  return actual;
}

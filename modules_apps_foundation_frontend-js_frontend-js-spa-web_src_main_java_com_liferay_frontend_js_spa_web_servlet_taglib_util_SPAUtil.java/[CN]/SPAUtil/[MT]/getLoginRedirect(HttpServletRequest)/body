{
  String loginPortletNamespace=PortalUtil.getPortletNamespace(PropsUtil.get(PropsKeys.AUTH_LOGIN_PORTLET_NAME));
  String loginRedirectParameter=loginPortletNamespace + "redirect";
  return GetterUtil.getString(request.getParameter(loginRedirectParameter));
}

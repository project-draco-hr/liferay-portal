{
  List<String> completeArgs=new ArrayList<>();
  GUtil.addToCollection(completeArgs,getArgs());
  completeArgs.add("deployment.files=" + getDeploymentFileNames());
  completeArgs.add("deployment.output.file=" + FileUtil.getAbsolutePath(getOutputFile()));
  completeArgs.add("deployment.path=" + FileUtil.getAbsolutePath(getDeploymentPath()));
  return completeArgs;
}

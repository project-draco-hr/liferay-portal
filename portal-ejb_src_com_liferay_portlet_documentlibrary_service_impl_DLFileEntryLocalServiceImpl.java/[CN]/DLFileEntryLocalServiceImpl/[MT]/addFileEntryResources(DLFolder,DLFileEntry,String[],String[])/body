{
  ResourceLocalServiceUtil.addModelResources(fileEntry.getCompanyId(),folder.getGroupId(),fileEntry.getUserId(),DLFileEntry.class.getName(),fileEntry.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

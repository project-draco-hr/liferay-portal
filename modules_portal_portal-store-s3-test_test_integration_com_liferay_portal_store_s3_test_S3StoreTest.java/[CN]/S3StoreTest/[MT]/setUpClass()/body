{
  String dlStoreImpl=PropsUtil.get(PropsKeys.DL_STORE_IMPL);
  String s3StoreClassName="com.liferay.portal.store.s3.S3Store";
  Assume.assumeTrue("Property '" + PropsKeys.DL_STORE_IMPL + "' must be equals to '"+ s3StoreClassName+ "'",dlStoreImpl.equals(s3StoreClassName));
}

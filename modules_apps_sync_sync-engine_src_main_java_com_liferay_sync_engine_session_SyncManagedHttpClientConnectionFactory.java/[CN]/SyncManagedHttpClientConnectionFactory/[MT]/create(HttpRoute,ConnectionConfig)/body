{
  if (connectionConfig == null) {
    connectionConfig=ConnectionConfig.DEFAULT;
  }
  return new SyncManagedHttpClientConnection("http-outgoing-" + _counter.getAndIncrement(),connectionConfig.getBufferSize());
}

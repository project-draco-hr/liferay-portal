{
  FileCacheOutputStream fcos=exportPortletInfoToStream(plid,portletId,parameterMap,startDate,endDate);
  try {
    return fcos.getBytes();
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

{
  FileCacheOutputStream fcos=exportPortletInfoAsStream(plid,portletId,parameterMap,startDate,endDate);
  try {
    return fcos.getBytes();
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

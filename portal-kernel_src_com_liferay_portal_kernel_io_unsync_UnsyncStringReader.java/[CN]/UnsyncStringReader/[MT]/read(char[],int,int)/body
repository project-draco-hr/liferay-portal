{
  if (len <= 0) {
    return 0;
  }
  if (index >= capability) {
    return -1;
  }
  int length=len;
  if (index + length > capability) {
    length=capability - index;
  }
  string.getChars(index,index + length,cbuf,off);
  index+=length;
  return length;
}

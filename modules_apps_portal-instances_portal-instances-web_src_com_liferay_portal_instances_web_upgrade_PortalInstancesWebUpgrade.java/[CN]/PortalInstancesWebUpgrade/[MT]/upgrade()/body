{
  UpgradePortletId upgradePortletId=new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"135",PortalInstancesPortletKeys.PORTAL_INSTANCES}};
    }
  }
;
  _releaseLocalService.updateRelease("com.liferay.portal.instances.web",Collections.<UpgradeProcess>singletonList(upgradePortletId),1,1,false);
}

{
  MDRRule rule=mdrRuleLocalService.getMDRRule(ruleId);
  MDRRuleGroupPermissionUtil.check(getPermissionChecker(),rule.getGroupId(),rule.getRuleGroupId(),ActionKeys.UPDATE);
  return mdrRuleLocalService.updateRule(ruleId,nameMap,descriptionMap,type,typeSettings,serviceContext);
}

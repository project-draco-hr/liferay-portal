{
  MDRRule rule=mdrRuleLocalService.fetchRule(ruleId);
  if (rule != null) {
    MDRRuleGroupPermissionUtil.check(getPermissionChecker(),rule.getGroupId(),rule.getRuleGroupId(),ActionKeys.VIEW);
  }
  return rule;
}

{
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Blogs Permissions Page")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.saveScreenShotAndSource();
  selenium.clickAt("link=Blogs Permissions Page",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  selenium.saveScreenShotAndSource();
  assertTrue(selenium.isPartialText("//div[@class='entry-title']/a","Portlet1 Temporary1 Entry1"));
  selenium.clickAt("//div[@class='entry-title']/a",RuntimeVariables.replace("Portlet1 Temporary1 Entry1"));
  selenium.waitForPageToLoad("30000");
  selenium.saveScreenShotAndSource();
  selenium.click(RuntimeVariables.replace("link=Delete"));
  selenium.waitForPageToLoad("30000");
  assertTrue(selenium.getConfirmation().matches("^Are you sure you want to delete this[\\s\\S]$"));
  selenium.saveScreenShotAndSource();
  assertFalse(selenium.isElementPresent("link=Edited1 Portlet1 Temporary1 Entry1"));
  assertFalse(selenium.isElementPresent("link=Portlet1 Temporary1 Entry1"));
}

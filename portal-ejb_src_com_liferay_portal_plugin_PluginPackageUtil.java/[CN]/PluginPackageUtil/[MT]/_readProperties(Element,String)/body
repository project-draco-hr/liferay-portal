{
  Properties result=new Properties();
  if (parent != null) {
    Iterator itr=parent.elements(childTagName).iterator();
    while (itr.hasNext()) {
      Element tagEl=(Element)itr.next();
      result.setProperty(tagEl.attribute("name").getValue(),tagEl.attribute("value").getValue());
    }
  }
  return result;
}

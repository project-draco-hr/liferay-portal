{
  List pluginList=_installedPlugins.findPluginsByGroupIdAndArtifactId(groupId,artifactId);
  if (pluginList == null) {
    return null;
  }
 else {
    return (PluginPackage)pluginList.get(0);
  }
}

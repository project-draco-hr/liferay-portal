{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(SystemEvent.class,SystemEvent.class.getClassLoader());
  dynamicQuery.add(RestrictionsFactoryUtil.eq("systemEventId",ServiceTestUtil.nextLong()));
  List<SystemEvent> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

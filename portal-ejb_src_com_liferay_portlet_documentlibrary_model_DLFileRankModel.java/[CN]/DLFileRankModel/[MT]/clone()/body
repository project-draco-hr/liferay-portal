{
  DLFileRank clone=new DLFileRank();
  clone.setCompanyId(getCompanyId());
  clone.setUserId(getUserId());
  clone.setFolderId(getFolderId());
  clone.setName(getName());
  clone.setCreateDate(getCreateDate());
  return clone;
}

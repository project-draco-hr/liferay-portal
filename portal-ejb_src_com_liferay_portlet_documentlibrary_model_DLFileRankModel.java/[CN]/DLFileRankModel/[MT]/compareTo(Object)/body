{
  if (obj == null) {
    return -1;
  }
  DLFileRank dlFileRank=(DLFileRank)obj;
  int value=0;
  value=getCreateDate().compareTo(dlFileRank.getCreateDate());
  value=value * -1;
  if (value != 0) {
    return value;
  }
  return 0;
}

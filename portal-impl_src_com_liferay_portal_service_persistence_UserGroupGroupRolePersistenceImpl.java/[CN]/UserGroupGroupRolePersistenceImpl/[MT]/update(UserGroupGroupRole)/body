{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserGroupGroupRole userGroupGroupRole) method. Use update(UserGroupGroupRole userGroupGroupRole, boolean merge) instead.");
  }
  return update(userGroupGroupRole,false);
}

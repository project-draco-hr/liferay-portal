{
  if (isEscapedModel()) {
    return (JournalTemplate)this;
  }
 else {
    return (JournalTemplate)Proxy.newProxyInstance(JournalTemplate.class.getClassLoader(),new Class[]{JournalTemplate.class},new AutoEscapeBeanHandler(this));
  }
}

{
  List<Class<? extends BaseModel<?>>> modelClassesName=new ArrayList<>();
  modelClassesName.addAll(ModelMigratorUtil.getModelClassesName(ClassLoaderUtil.getPortalClassLoader(),".*"));
  for (  String servletContextName : ServletContextPool.keySet()) {
    ServletContext servletContext=ServletContextPool.get(servletContextName);
    ClassLoader classLoader=servletContext.getClassLoader();
    modelClassesName.addAll(ModelMigratorUtil.getModelClassesName(classLoader,".*"));
  }
  return modelClassesName;
}

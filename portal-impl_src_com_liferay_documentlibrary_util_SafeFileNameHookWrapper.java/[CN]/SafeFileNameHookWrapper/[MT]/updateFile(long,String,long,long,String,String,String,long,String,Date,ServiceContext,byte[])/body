{
  _hook.updateFile(companyId,portletId,groupId,repositoryId,FileUtil.encodeSafeFileName(fileName),versionNumber,FileUtil.encodeSafeFileName(sourceFileName),fileEntryId,properties,modifiedDate,serviceContext,bytes);
}

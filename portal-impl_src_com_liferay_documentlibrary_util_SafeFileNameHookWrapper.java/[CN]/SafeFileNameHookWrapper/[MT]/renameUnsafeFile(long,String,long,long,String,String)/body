{
  if ((safeFileName != null) && !safeFileName.equals(fileName)) {
    if (_hook.hasFile(companyId,repositoryId,fileName,DLFileEntryConstants.DEFAULT_VERSION)) {
      _hook.updateFile(companyId,portletId,groupId,repositoryId,fileName,safeFileName,true);
    }
  }
}

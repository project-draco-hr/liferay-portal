{
  if (Validator.isNull(mimeType)) {
    return false;
  }
  try {
    if (XugglerUtil.isEnabled()) {
      return _audioMimeTypes.contains(mimeType);
    }
  }
 catch (  Exception e) {
  }
  return false;
}

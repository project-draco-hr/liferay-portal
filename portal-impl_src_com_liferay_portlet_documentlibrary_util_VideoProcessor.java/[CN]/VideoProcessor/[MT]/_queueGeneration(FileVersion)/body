{
  if (!_fileEntries.contains(fileVersion.getFileEntryId()) && _isSupportedVideo(fileVersion)) {
    _fileEntries.add(fileVersion.getFileVersionId());
    MessageBusUtil.sendMessage(DestinationNames.DOCUMENT_LIBRARY_VIDEO_PROCESSOR,fileVersion);
  }
}

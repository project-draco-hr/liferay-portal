{
  long companyId=fileVersion.getCompanyId();
  String fileName=getThumbnailFilePath(fileVersion);
  if (PropsValues.DL_FILE_ENTRY_THUMBNAIL_ENABLED && !DLStoreUtil.hasFile(companyId,REPOSITORY_ID,fileName)) {
    return true;
  }
 else {
    return false;
  }
}

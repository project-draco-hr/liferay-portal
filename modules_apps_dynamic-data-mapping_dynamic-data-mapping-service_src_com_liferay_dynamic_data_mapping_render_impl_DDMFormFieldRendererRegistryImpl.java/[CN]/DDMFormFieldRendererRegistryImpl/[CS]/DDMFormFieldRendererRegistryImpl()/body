{
  Class<?> clazz=getClass();
  Bundle bundle=FrameworkUtil.getBundle(clazz);
  _bundleContext=bundle.getBundleContext();
  Filter filter=null;
  try {
    filter=FrameworkUtil.createFilter("(&(objectClass=" + DDMFormFieldRenderer.class.getName() + ")(!(objectClass="+ clazz.getName()+ ")))");
  }
 catch (  InvalidSyntaxException ise) {
  }
  _serviceTracker=new ServiceTracker<>(_bundleContext,filter,new DDMFormFieldRendererServiceTrackerCustomizer());
  _serviceTracker.open();
}

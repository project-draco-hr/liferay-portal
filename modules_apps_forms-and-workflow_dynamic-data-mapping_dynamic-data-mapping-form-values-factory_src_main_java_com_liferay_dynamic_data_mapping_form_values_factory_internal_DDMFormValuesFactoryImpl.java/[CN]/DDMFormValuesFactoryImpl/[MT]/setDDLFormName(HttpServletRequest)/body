{
  String portletId=PortalUtil.getPortletId(httpServletRequest);
  if (Validator.isNotNull(portletId)) {
    _ddlFormName=StringPool.UNDERLINE + portletId + StringPool.UNDERLINE;
  }
 else {
    _ddlFormName=StringPool.BLANK;
  }
}

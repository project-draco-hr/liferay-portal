{
  if (addPortletNamespace) {
    String portletNamespace=getPortletNamespace(httpServletRequest);
    _ddmFormFieldPrefix=portletNamespace + DDMFormRendererConstants.DDM_FORM_FIELD_NAME_PREFIX;
  }
 else {
    _ddmFormFieldPrefix=DDMFormRendererConstants.DDM_FORM_FIELD_NAME_PREFIX;
  }
}

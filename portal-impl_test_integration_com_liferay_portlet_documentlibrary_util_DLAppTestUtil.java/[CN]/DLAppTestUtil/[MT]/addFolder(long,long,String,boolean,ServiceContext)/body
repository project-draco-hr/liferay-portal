{
  long userId=TestPropsValues.getUserId();
  String description=StringPool.BLANK;
  if (deleteExisting) {
    try {
      DLAppServiceUtil.deleteFolder(serviceContext.getScopeGroupId(),parentFolderId,name);
    }
 catch (    NoSuchFolderException nsfe) {
    }
  }
  return DLAppLocalServiceUtil.addFolder(userId,repositoryId,parentFolderId,name,description,serviceContext);
}

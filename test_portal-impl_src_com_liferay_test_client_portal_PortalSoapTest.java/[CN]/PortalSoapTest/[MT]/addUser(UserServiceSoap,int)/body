{
  boolean autoPassword=true;
  String password1=null;
  String password2=null;
  boolean autoScreenName=true;
  String screenName="";
  String emailAddress="PortalSoapTest" + seed + "@liferay.com";
  String locale=LocaleUtil.getDefault().toString();
  String firstName="PortalSoapTest";
  String middleName="";
  String lastName="PortalSoapTest";
  int prefixId=0;
  int suffixId=0;
  boolean male=true;
  int birthdayMonth=Calendar.JANUARY;
  int birthdayDay=1;
  int birthdayYear=1970;
  String jobTitle=null;
  long[] organizationIds=new long[0];
  boolean sendMail=false;
  UserSoap user=userService.addUser(TestConstants.COMPANY_ID,autoPassword,password1,password2,autoScreenName,screenName,emailAddress,locale,firstName,middleName,lastName,prefixId,suffixId,male,birthdayMonth,birthdayDay,birthdayYear,jobTitle,organizationIds,sendMail);
  System.out.println("Creating user " + emailAddress);
  user=userService.getUserByEmailAddress(TestConstants.COMPANY_ID,emailAddress);
  userService.deleteUser(user.getUserId());
}

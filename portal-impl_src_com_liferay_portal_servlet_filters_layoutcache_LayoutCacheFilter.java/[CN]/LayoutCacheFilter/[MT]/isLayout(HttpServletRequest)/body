{
  if ((_pattern == _PATTERN_FRIENDLY) || (_pattern == _PATTERN_RESOURCE)) {
    return true;
  }
 else {
    String plid=ParamUtil.getString(request,"p_l_id");
    if (Validator.isNotNull(plid)) {
      return true;
    }
 else {
      return false;
    }
  }
}

{
  List<ExpandoTableColumnsSoap> soapModels=new ArrayList<ExpandoTableColumnsSoap>(models.size());
  for (int i=0; i < models.size(); i++) {
    ExpandoTableColumns model=models.get(i);
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new ExpandoTableColumnsSoap[soapModels.size()]);
}

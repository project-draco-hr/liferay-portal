{
  List list=CompanyLocalServiceUtil.getCompanies();
  for (Iterator itr=list.iterator(); itr.hasNext(); ) {
    Company company=(Company)itr.next();
    JournalContentSearchLocalServiceUtil.checkContentSearches(company.getCompanyId());
  }
}

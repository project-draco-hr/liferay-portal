{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradeTable());
  _releaseLocalService.updateRelease("com.liferay.portal.lock.service",upgradeProcesses,1,1,false);
}

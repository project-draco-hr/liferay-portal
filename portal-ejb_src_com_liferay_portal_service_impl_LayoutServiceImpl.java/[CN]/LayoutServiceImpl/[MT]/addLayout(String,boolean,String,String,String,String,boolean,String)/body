{
  GroupPermission.check(getPermissionChecker(),groupId,ActionKeys.MANAGE_LAYOUTS);
  return LayoutLocalServiceUtil.addLayout(groupId,getUserId(),privateLayout,parentLayoutId,name,title,type,hidden,friendlyURL);
}

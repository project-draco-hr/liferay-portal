{
  GroupPermission.check(getPermissionChecker(),groupId,ActionKeys.MANAGE_LAYOUTS);
  return LayoutLocalServiceUtil.addLayout(getUserId(),groupId,privateLayout,parentLayoutId,name,title,type,hidden,friendlyURL);
}

{
  LayoutPermission.check(getPermissionChecker(),layoutId,ownerId,ActionKeys.UPDATE);
  return LayoutLocalServiceUtil.updateLayout(layoutId,ownerId,parentLayoutId,name,languageId,type,hidden,friendlyURL);
}

{
  String version=GradleUtil.getPortalToolVersion(project,_PMD_PORTAL_TOOL_NAME);
  if (Validator.isNotNull(version)) {
    GradleUtil.addDependency(project,"pmd",GradleUtil.PORTAL_TOOL_GROUP,_PMD_PORTAL_TOOL_NAME,version);
  }
}

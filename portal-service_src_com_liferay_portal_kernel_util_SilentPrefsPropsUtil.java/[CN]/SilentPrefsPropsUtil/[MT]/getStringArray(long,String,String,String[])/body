{
  try {
    return PrefsPropsUtil.getStringArray(companyId,name,delimiter,defaultValue);
  }
 catch (  SystemException se) {
  }
  String value=PropsUtil.get(name);
  if (Validator.isNull(value)) {
    return defaultValue;
  }
  return StringUtil.split(value,delimiter);
}

{
  String methodName=StringUtil.extractLast(proceedingJoinPoint.getTarget().getClass().getName(),StringPool.PERIOD);
  Signature signature=proceedingJoinPoint.getSignature();
  methodName+=StringPool.PERIOD + signature.getName() + "()";
  return methodName;
}

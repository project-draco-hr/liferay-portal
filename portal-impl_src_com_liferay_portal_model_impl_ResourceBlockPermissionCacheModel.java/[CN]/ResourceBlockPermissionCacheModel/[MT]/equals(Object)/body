{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ResourceBlockPermissionCacheModel)) {
    return false;
  }
  ResourceBlockPermissionCacheModel resourceBlockPermissionCacheModel=(ResourceBlockPermissionCacheModel)obj;
  if ((resourceBlockPermissionId == resourceBlockPermissionCacheModel.resourceBlockPermissionId) && (mvccVersion == resourceBlockPermissionCacheModel.mvccVersion)) {
    return true;
  }
  return false;
}

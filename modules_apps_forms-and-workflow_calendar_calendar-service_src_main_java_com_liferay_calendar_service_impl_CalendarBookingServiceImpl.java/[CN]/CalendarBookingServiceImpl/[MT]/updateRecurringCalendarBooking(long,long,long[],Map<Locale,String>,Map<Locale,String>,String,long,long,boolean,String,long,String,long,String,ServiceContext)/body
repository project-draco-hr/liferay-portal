{
  CalendarPermission.check(getPermissionChecker(),calendarId,CalendarActionKeys.MANAGE_BOOKINGS);
  return calendarBookingLocalService.updateRecurringCalendarBooking(getUserId(),calendarBookingId,calendarId,childCalendarIds,titleMap,descriptionMap,location,startTime,endTime,allDay,firstReminder,firstReminderType,secondReminder,secondReminderType,serviceContext);
}

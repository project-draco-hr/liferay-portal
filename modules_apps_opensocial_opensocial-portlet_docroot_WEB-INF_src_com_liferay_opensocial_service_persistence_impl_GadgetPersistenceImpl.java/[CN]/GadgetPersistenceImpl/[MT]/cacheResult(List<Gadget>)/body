{
  for (  Gadget gadget : gadgets) {
    if (entityCache.getResult(GadgetModelImpl.ENTITY_CACHE_ENABLED,GadgetImpl.class,gadget.getPrimaryKey()) == null) {
      cacheResult(gadget);
    }
 else {
      gadget.resetOriginalValues();
    }
  }
}

{
  String filterFieldAndValue="\"" + DESTINATION_FILTER_FIELD + "\":\""+ _destinationFilter+ "\"";
  if (message.indexOf(filterFieldAndValue) != -1) {
    _listener.receive(messageId,message);
  }
}

{
  List<DynamicInclude> dynamicIncludes=DynamicIncludeUtil.getDynamicIncludes(getKey());
  if ((dynamicIncludes == null) || dynamicIncludes.isEmpty()) {
    return SKIP_BODY;
  }
  return EVAL_BODY_INCLUDE;
}

{
  if (isEscapedModel()) {
    return (DeletionEntry)this;
  }
 else {
    return (DeletionEntry)Proxy.newProxyInstance(DeletionEntry.class.getClassLoader(),new Class[]{DeletionEntry.class},new AutoEscapeBeanHandler(this));
  }
}

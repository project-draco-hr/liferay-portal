{
  Map<String,ExtRepositoryModelAdapter<?>> extRepositoryAdapters=_getExtRepositoryAdapters();
  T extRepositoryAdapter=(T)extRepositoryAdapters.get(extRepositoryModelKey);
  if (extRepositoryAdapter != null) {
    if (_log.isDebugEnabled()) {
      _log.debug("Hit " + extRepositoryModelKey);
    }
  }
 else {
    if (_log.isDebugEnabled()) {
      _log.debug("Miss " + extRepositoryModelKey);
    }
  }
  return extRepositoryAdapter;
}

{
  ExpandoTable table=expandoTablePersistence.fetchByC_C_N(companyId,classNameId,ExpandoTableConstants.DEFAULT_TABLE_NAME);
  if (table == null) {
    return Collections.EMPTY_LIST;
  }
  return expandoColumnPersistence.findByTableId(table.getTableId());
}

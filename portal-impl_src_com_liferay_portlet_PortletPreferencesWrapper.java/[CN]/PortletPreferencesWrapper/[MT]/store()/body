{
  if (_lifecycle.equals(PortletRequest.RENDER_PHASE) && PropsValues.PORTLET_PREFERENCES_STRICT_STORE) {
    throw new IllegalStateException("Preferences cannot be stored inside a render call");
  }
  _portletPreferences.store();
}

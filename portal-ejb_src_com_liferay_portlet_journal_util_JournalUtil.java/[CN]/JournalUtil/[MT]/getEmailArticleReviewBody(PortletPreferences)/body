{
  String emailArticleReviewBody=prefs.getValue("email-article-review-body",StringPool.BLANK);
  if (Validator.isNotNull(emailArticleReviewBody)) {
    return emailArticleReviewBody;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsUtil.JOURNAL_EMAIL_ARTICLE_REVIEW_BODY));
  }
}

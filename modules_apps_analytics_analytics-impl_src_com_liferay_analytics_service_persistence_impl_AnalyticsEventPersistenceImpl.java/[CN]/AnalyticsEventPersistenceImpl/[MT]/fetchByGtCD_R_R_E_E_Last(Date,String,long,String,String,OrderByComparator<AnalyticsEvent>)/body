{
  int count=countByGtCD_R_R_E_E(createDate,referrerClassName,referrerClassPK,elementKey,type);
  if (count == 0) {
    return null;
  }
  List<AnalyticsEvent> list=findByGtCD_R_R_E_E(createDate,referrerClassName,referrerClassPK,elementKey,type,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

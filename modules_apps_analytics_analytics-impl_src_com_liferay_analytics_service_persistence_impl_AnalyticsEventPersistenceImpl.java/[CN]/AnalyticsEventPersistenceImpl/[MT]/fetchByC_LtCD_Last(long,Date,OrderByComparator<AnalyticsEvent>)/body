{
  int count=countByC_LtCD(companyId,createDate);
  if (count == 0) {
    return null;
  }
  List<AnalyticsEvent> list=findByC_LtCD(companyId,createDate,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

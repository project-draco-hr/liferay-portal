{
  int count=countByGtCD_E_E(createDate,elementKey,type);
  if (count == 0) {
    return null;
  }
  List<AnalyticsEvent> list=findByGtCD_E_E(createDate,elementKey,type,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

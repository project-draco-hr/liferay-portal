{
  List<AnalyticsEvent> list=findByGtCD_R_R_E_E(createDate,referrerClassName,referrerClassPK,elementKey,type,0,1,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

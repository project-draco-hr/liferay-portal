{
  List<AnalyticsEvent> list=findByGtCD_C_C_R_R_E(createDate,className,classPK,referrerClassName,referrerClassPK,type,0,1,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  int count=countByC_GtCD(companyId,createDate);
  if (count == 0) {
    return null;
  }
  List<AnalyticsEvent> list=findByC_GtCD(companyId,createDate,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

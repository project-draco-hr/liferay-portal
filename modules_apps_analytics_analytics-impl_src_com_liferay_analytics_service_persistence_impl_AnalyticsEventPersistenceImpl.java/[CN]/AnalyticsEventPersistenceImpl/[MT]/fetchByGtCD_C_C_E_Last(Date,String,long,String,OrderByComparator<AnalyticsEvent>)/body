{
  int count=countByGtCD_C_C_E(createDate,className,classPK,type);
  if (count == 0) {
    return null;
  }
  List<AnalyticsEvent> list=findByGtCD_C_C_E(createDate,className,classPK,type,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

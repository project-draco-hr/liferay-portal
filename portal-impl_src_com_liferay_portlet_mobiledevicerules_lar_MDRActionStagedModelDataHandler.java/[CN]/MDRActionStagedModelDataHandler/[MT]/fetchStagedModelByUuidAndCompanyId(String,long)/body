{
  List<MDRAction> actions=MDRActionLocalServiceUtil.getMDRActionsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<MDRAction>());
  if (ListUtil.isEmpty(actions)) {
    return null;
  }
  return actions.get(0);
}

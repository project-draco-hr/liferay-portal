{
  JSONSerializer jsonSerializer=JSONFactoryUtil.createJSONSerializer();
  String contextString=jsonSerializer.serializeDeep(context);
  String modulesString=jsonSerializer.serialize(modules);
  return StringUtil.replace(_javaScriptTPL,new String[]{"$CONTEXT","$ID","$MODULES"},new String[]{contextString,id,modulesString});
}

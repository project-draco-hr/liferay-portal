{
  ResourceLocalServiceUtil.addModelResources(item.getCompanyId(),category.getGroupId(),item.getUserId(),ShoppingItem.class.getName(),item.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

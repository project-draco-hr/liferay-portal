{
  ShoppingItemFieldUtil.removeByItemId(item.getItemId());
  ShoppingItemPriceUtil.removeByItemId(item.getItemId());
  ImageLocalUtil.deleteImage(item.getSmallImageId());
  ImageLocalUtil.deleteImage(item.getMediumImageId());
  ImageLocalUtil.deleteImage(item.getLargeImageId());
  ResourceLocalServiceUtil.deleteResource(item.getCompanyId(),ShoppingItem.class.getName(),ResourceImpl.SCOPE_INDIVIDUAL,item.getItemId());
  ShoppingItemUtil.remove(item.getItemId());
}

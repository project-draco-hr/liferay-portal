{
  try (PreparedStatement ps=connection.prepareStatement("update KaleoDefinition set content = ? where " + "kaleoDefinitionId = ?")){
    ps.setString(1,content);
    ps.setLong(2,kaleoDefinitionId);
    ps.executeUpdate();
  }
 }

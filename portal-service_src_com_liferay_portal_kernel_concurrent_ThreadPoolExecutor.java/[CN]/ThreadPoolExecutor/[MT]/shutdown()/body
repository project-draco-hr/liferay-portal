{
  _mainLock.lock();
  try {
    int state=_runState;
    if (state < _SHUTDOWN) {
      _runState=_SHUTDOWN;
    }
    for (    WorkerTask worker : _workerTaskSet) {
      worker._interruptIfWaiting();
    }
    _tryTerminate();
  }
  finally {
    _mainLock.unlock();
  }
}

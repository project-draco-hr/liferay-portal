{
  _mainLock.lock();
  try {
    int count=0;
    for (    WorkerTask worker : _workerTaskSet) {
      if (worker._isLocked()) {
        count++;
      }
    }
    return count;
  }
  finally {
    _mainLock.unlock();
  }
}

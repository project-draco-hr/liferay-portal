{
  if (obj == null) {
    return -1;
  }
  ImageImpl image=(ImageImpl)obj;
  int value=0;
  if (getImageId() < image.getImageId()) {
    value=-1;
  }
 else   if (getImageId() > image.getImageId()) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  return 0;
}

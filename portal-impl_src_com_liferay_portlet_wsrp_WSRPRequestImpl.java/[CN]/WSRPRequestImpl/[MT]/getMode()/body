{
  if (_currentMode == null) {
    PortletMode portletMode=_portletRequest.getPortletMode();
    _currentMode=Modes.getWsrpModeFromJsrPortletMode(portletMode).toString();
  }
  return _currentMode;
}

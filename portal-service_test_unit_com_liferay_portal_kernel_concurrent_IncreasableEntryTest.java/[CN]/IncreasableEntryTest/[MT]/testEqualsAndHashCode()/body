{
  IncreasableEntry<String,Integer> increasableEntry=new IntegerIncreasableEntry("test",0);
  Assert.assertTrue(increasableEntry.equals(increasableEntry));
  Assert.assertFalse(increasableEntry.equals(new Object()));
  Assert.assertFalse(increasableEntry.equals(new IntegerIncreasableEntry("test1",0)));
  Assert.assertFalse(increasableEntry.equals(new IntegerIncreasableEntry("test",1)));
  Assert.assertTrue(increasableEntry.equals(new IntegerIncreasableEntry("test",0)));
  int hash=HashUtil.hash(0,increasableEntry.getKey());
  Assert.assertEquals(HashUtil.hash(hash,increasableEntry.getValue()),increasableEntry.hashCode());
}

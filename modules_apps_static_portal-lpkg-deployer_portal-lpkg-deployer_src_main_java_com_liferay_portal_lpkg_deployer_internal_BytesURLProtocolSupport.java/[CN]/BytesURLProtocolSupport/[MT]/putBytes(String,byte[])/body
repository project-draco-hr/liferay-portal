{
  try {
    URL url=new URL("bytes://localhost/".concat(URLCodec.encodeURL(id)));
    _bytesMap.put(url,bytes);
    return url;
  }
 catch (  MalformedURLException murle) {
    return ReflectionUtil.throwException(murle);
  }
}

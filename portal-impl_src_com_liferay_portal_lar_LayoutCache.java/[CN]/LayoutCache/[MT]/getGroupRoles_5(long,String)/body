{
  List<Role> roles=groupRolesMap.get(groupId);
  if (roles == null) {
    Group group=GroupLocalServiceUtil.getGroup(groupId);
    roles=ResourceActionsUtil.getRoles(group.getCompanyId(),group,resourceName);
    groupRolesMap.put(groupId,roles);
  }
  return roles;
}

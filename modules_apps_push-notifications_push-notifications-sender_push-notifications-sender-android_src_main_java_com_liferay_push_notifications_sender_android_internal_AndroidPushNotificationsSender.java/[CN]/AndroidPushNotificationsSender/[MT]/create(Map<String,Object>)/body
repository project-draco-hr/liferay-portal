{
  String apiKey=MapUtil.getString(configuration,PortletPropsKeys.ANDROID_API_KEY,null);
  Integer retries=(Integer)configuration.get(PortletPropsKeys.ANDROID_RETRIES);
  return new AndroidPushNotificationsSender(apiKey,retries);
}

{
  List<DLFolder> folders=DLFolderLocalServiceUtil.getDLFoldersByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<DLFolder>());
  if (ListUtil.isEmpty(folders)) {
    return null;
  }
  return new LiferayFolder(folders.get(0));
}

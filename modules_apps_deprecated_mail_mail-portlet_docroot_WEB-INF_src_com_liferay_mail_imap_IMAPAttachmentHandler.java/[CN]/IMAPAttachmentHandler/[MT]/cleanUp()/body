{
  try {
    Folder folder=getFolder();
    if ((folder == null) || !folder.isOpen()) {
      return;
    }
    folder.close(false);
  }
 catch (  MessagingException me) {
    _log.error(me,me);
  }
}

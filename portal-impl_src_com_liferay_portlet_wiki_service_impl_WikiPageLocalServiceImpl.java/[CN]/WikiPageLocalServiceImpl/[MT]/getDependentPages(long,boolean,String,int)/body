{
  List<WikiPage> dependentPages=new ArrayList<>();
  List<WikiPage> childPages=getChildren(nodeId,head,redirectTitle,status);
  dependentPages.addAll(childPages);
  List<WikiPage> redirectPages=getRedirectPages(nodeId,head,redirectTitle,status);
  dependentPages.addAll(redirectPages);
  return dependentPages;
}

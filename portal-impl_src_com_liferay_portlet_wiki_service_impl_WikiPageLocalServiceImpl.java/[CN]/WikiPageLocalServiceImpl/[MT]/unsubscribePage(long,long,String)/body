{
  WikiPage page=getPage(nodeId,title);
  subscriptionLocalService.deleteSubscription(userId,WikiPage.class.getName(),page.getResourcePrimKey());
  socialEquityLogLocalService.deactivateEquityLogs(userId,WikiPage.class.getName(),page.getResourcePrimKey(),ActionKeys.SUBSCRIBE);
}

{
  double version=WikiPageImpl.DEFAULT_VERSION;
  String content=null;
  String format=WikiPageImpl.DEFAULT_FORMAT;
  boolean head=true;
  String[] tagsEntries=null;
  return addPage(null,userId,nodeId,title,version,content,format,head,tagsEntries,prefs,themeDisplay);
}

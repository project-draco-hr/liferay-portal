{
  AssetPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_VOCABULARY);
  return assetCategoryVocabularyLocalService.addVocabulary(getUserId(),name,serviceContext);
}

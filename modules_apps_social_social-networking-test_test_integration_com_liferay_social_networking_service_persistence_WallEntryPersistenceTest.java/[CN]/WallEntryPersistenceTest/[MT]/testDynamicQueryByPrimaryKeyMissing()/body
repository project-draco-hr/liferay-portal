{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(WallEntry.class,WallEntry.class.getClassLoader());
  dynamicQuery.add(RestrictionsFactoryUtil.eq("wallEntryId",RandomTestUtil.nextLong()));
  List<WallEntry> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.WIKI,ActionKeys.ADD_NODE);
  return wikiNodeLocalService.addNode(getUserId(),plid,name,description,addCommunityPermissions,addGuestPermissions);
}

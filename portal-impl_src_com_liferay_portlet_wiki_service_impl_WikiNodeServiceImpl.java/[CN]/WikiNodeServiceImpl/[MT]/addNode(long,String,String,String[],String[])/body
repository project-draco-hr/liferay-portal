{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.WIKI,ActionKeys.ADD_NODE);
  return WikiNodeLocalServiceUtil.addNode(getUserId(),plid,name,description,communityPermissions,guestPermissions);
}

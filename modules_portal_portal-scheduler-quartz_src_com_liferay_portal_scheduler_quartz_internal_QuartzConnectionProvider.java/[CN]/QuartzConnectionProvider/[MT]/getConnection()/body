{
  Connection con=null;
  try {
    DataSource dataSource=InfrastructureUtil.getDataSource();
    con=dataSource.getConnection();
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return con;
}

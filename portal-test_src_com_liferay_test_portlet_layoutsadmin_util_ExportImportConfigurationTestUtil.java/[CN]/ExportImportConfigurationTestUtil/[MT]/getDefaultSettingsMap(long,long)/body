{
  User user=UserLocalServiceUtil.getUser(userId);
  Date startDate=new Date();
  Date endDate=startDate;
  return ExportImportConfigurationSettingsMapFactory.buildSettingsMap(userId,groupId,groupId,false,null,null,startDate,endDate,user.getLocale(),user.getTimeZone());
}

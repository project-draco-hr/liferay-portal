{
  setHeaders(request,response,fileName,contentType);
  if (range != null) {
    response.setHeader(HttpHeaders.CONTENT_RANGE,range.getContentRange());
    response.setHeader(HttpHeaders.CONTENT_LENGTH,String.valueOf(range.getLength()));
  }
}

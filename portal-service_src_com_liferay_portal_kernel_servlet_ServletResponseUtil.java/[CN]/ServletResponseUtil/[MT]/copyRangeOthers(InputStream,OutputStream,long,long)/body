{
  RandomAccessInputStream fileCacheSeekableStream=new RandomAccessInputStream(inputStream);
  fileCacheSeekableStream.seek(start);
  StreamUtil.transfer(fileCacheSeekableStream,outputStream,length);
}

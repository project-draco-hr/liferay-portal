{
  try {
    if (_ddmServiceConfiguration == null) {
      _ddmServiceConfiguration=getConfiguration(DDMServiceConfiguration.class);
    }
    return _ddmServiceConfiguration;
  }
 catch (  PortalException pe) {
    throw new SystemException(pe);
  }
}

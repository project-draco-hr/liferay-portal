{
  Map<Long,Fields> restuls=new HashMap<Long,Fields>();
  List<Fields> fields=_doQuery(structureId,classPKs,fieldNames,null,null);
  for (int i=0; i < fields.size(); i++) {
    restuls.put(classPKs[i],fields.get(i));
  }
  return restuls;
}

{
  XPath xPathSelector=SAXReaderUtil.createXPath("//dynamic-element[@name=".concat(HtmlUtil.escapeXPathAttribute(name)).concat("]"));
  List<Node> nodes=xPathSelector.selectNodes(document);
  if (nodes.size() == 1) {
    return (Element)nodes.get(0);
  }
 else {
    return null;
  }
}

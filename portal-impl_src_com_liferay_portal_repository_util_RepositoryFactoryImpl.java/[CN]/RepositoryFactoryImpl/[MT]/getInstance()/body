{
  if (_classLoader == null) {
    return (BaseRepository)InstanceFactory.newInstance(_className);
  }
 else {
    return (BaseRepository)ProxyFactory.newInstance(_classLoader,BaseRepository.class,_className);
  }
}

{
  if (documentRepository instanceof LocalRepository) {
    return new DLFileVersionServiceAdapter(DLFileVersionLocalServiceUtil.getService());
  }
  return new DLFileVersionServiceAdapter(DLFileVersionLocalServiceUtil.getService(),DLFileVersionServiceUtil.getService());
}

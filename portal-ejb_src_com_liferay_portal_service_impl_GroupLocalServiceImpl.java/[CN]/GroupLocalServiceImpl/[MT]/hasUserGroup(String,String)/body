{
  if (UserUtil.containsGroup(userId,groupId)) {
    return true;
  }
 else {
    if (GroupFinder.countByGroupsOrgs(groupId,userId) > 0) {
      return true;
    }
 else {
      return false;
    }
  }
}

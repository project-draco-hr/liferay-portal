{
  if (UserUtil.containsGroup(userId,groupId)) {
    return true;
  }
 else {
    if (GroupFinder.countByOrgsGroups(groupId,userId) > 0) {
      return true;
    }
 else {
      return false;
    }
  }
}

{
  String fileName=getFileName(image.getImageId(),image.getType());
  Date now=new Date();
  InputStream is=new UnsyncByteArrayInputStream(bytes);
  if (DLStoreUtil.hasFile(_COMPANY_ID,_REPOSITORY_ID,fileName,_VERSION_NUMBER)) {
    DLStoreUtil.deleteFile(_COMPANY_ID,_PORTLET_ID,_REPOSITORY_ID,fileName);
  }
  DLStoreUtil.addFile(_COMPANY_ID,_PORTLET_ID,_GROUP_ID,_REPOSITORY_ID,fileName,true,_FILE_ENTRY_ID,_PROPERTIES,now,new ServiceContext(),is);
}

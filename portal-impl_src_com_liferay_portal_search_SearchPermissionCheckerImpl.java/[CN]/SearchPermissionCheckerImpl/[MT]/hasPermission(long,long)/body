{
  if (resourceId == 0) {
    return false;
  }
  List<Permission> permissions=PermissionLocalServiceUtil.getRolePermissions(roleId,resourceId);
  List<String> actions=ResourceActionsUtil.getActions(permissions);
  if (actions.contains(ActionKeys.VIEW)) {
    return true;
  }
  return false;
}

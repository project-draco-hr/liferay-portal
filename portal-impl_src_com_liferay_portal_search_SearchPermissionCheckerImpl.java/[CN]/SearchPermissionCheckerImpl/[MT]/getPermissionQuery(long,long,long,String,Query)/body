{
  try {
    if (PropsValues.PERMISSIONS_USER_CHECK_ALGORITHM == 5) {
      return doGetPermissionQuery(companyId,groupId,userId,className,query);
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return query;
}

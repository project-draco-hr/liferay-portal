{
  ExpandoValue expandoValue=(ExpandoValue)EntityCacheUtil.getResult(ExpandoValueModelImpl.ENTITY_CACHE_ENABLED,ExpandoValueImpl.class,valueId,this);
  if (expandoValue == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (expandoValue == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      expandoValue=(ExpandoValue)session.get(ExpandoValueImpl.class,Long.valueOf(valueId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (expandoValue == null)) {
        EntityCacheUtil.putResult(ExpandoValueModelImpl.ENTITY_CACHE_ENABLED,ExpandoValueImpl.class,valueId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(expandoValue);
      }
      closeSession(session);
    }
  }
  return expandoValue;
}

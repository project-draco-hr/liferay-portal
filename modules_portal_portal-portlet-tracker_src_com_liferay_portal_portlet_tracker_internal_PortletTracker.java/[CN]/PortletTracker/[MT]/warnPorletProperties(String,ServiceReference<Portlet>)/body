{
  if (!_log.isWarnEnabled()) {
    return;
  }
  List<String> invalidKeys=_portletPropertyValidator.validate(serviceReference.getPropertyKeys());
  for (  String key : invalidKeys) {
    _log.warn("Invalid property " + key + " for portlet "+ portletName);
  }
}

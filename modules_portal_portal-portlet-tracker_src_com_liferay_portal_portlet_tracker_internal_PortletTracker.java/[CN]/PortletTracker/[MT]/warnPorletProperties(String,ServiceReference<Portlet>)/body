{
  String[] unsupportedKeys=PortletPropertyValidator.validatePropertiesKeys(serviceReference.getPropertyKeys());
  for (  String key : unsupportedKeys) {
    _log.info("Portlet:" + portletName + " - Incorrect property key:"+ key);
  }
}

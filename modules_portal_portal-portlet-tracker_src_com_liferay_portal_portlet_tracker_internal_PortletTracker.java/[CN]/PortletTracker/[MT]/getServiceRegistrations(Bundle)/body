{
  ServiceRegistrations serviceRegistrations=_serviceRegistrations.get(bundle);
  if (serviceRegistrations == null) {
    serviceRegistrations=new ServiceRegistrations();
    ServiceRegistrations original=_serviceRegistrations.putIfAbsent(bundle,serviceRegistrations);
    if (original != null) {
      serviceRegistrations=original;
    }
  }
  return serviceRegistrations;
}

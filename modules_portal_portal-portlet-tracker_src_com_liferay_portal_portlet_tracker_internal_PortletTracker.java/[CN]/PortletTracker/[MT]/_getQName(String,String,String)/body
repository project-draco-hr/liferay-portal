{
  if (Validator.isNull(name) && Validator.isNull(qname)) {
    return null;
  }
  if (Validator.isNull(qname)) {
    return SAXReaderUtil.createQName(name,SAXReaderUtil.createNamespace(defaultNamespace));
  }
  return SAXReaderUtil.createQName(name,SAXReaderUtil.createNamespace(qname));
}

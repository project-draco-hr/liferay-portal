{
  if (Validator.isNull(name) && Validator.isNull(uri)) {
    return null;
  }
  if (Validator.isNull(uri)) {
    return SAXReaderUtil.createQName(name,SAXReaderUtil.createNamespace(defaultNamespace));
  }
  return SAXReaderUtil.createQName(name,SAXReaderUtil.createNamespace(uri));
}

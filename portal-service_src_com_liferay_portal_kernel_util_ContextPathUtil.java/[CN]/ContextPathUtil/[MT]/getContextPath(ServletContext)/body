{
  String contextPath=GetterUtil.getString(servletContext.getContextPath(),StringPool.BLANK);
  if (contextPath.equals(StringPool.SLASH)) {
    contextPath=StringPool.BLANK;
  }
  return contextPath;
}

{
  String contextPath=GetterUtil.getString(servletRequest.getContextPath(),StringPool.BLANK);
  if (contextPath.equals(StringPool.SLASH)) {
    contextPath=StringPool.BLANK;
  }
  return contextPath;
}

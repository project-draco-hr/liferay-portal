{
  String contextPath=GetterUtil.getString(portletRequest.getContextPath(),StringPool.BLANK);
  if (contextPath.equals(StringPool.SLASH)) {
    contextPath=StringPool.BLANK;
  }
  return contextPath;
}

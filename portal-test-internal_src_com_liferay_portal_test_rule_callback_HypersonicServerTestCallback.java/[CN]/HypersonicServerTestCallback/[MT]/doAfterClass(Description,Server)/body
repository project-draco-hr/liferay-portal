{
  if (server != null) {
    try (Connection con=DriverManager.getConnection(_databaseURL,"sa","")){
      try (Statement statement=con.createStatement()){
        statement.execute("SHUTDOWN COMPACT");
      }
     }
     server.stop();
  }
}

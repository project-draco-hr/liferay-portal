{
  sacpEntry=toUnwrappedModel(sacpEntry);
  Session session=null;
  try {
    session=openSession();
    if (!session.contains(sacpEntry)) {
      sacpEntry=(SACPEntry)session.get(SACPEntryImpl.class,sacpEntry.getPrimaryKeyObj());
    }
    if (sacpEntry != null) {
      session.delete(sacpEntry);
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  if (sacpEntry != null) {
    clearCache(sacpEntry);
  }
  return sacpEntry;
}

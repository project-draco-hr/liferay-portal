{
  SACPEntry sacpEntry=(SACPEntry)EntityCacheUtil.getResult(SACPEntryModelImpl.ENTITY_CACHE_ENABLED,SACPEntryImpl.class,primaryKey);
  if (sacpEntry == _nullSACPEntry) {
    return null;
  }
  if (sacpEntry == null) {
    Session session=null;
    try {
      session=openSession();
      sacpEntry=(SACPEntry)session.get(SACPEntryImpl.class,primaryKey);
      if (sacpEntry != null) {
        cacheResult(sacpEntry);
      }
 else {
        EntityCacheUtil.putResult(SACPEntryModelImpl.ENTITY_CACHE_ENABLED,SACPEntryImpl.class,primaryKey,_nullSACPEntry);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(SACPEntryModelImpl.ENTITY_CACHE_ENABLED,SACPEntryImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return sacpEntry;
}

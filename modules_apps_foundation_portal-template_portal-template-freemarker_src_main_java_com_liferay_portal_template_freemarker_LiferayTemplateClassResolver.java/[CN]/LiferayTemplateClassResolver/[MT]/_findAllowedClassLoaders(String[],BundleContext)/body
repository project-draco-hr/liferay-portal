{
  Set<ClassLoader> classLoaders=new HashSet<>();
  for (  String allowedClass : allowedClasses) {
    classLoaders.addAll(_findAllowedClassLoaders(allowedClass,bundleContext));
  }
  return classLoaders;
}

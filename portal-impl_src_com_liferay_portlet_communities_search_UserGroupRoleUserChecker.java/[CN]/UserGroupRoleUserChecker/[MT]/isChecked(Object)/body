{
  User user=(User)obj;
  try {
    return UserGroupRoleLocalServiceUtil.hasUserGroupRole(user.getUserId(),_group.getGroupId(),_role.getRoleId(),false);
  }
 catch (  Exception e) {
    _log.error(e);
    return false;
  }
}

{
  Integer type=DDLRecordModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DDLRecordModelImpl.TABLE_NAME);
  }
  return type;
}

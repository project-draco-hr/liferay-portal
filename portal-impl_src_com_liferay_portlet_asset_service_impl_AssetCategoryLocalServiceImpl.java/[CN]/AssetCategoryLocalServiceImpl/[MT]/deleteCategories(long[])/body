{
  List<AssetCategory> categories=new ArrayList<AssetCategory>();
  for (  long categoryId : categoryIds) {
    categories.add(assetCategoryPersistence.findByPrimaryKey(categoryId));
  }
  deleteCategories(categories);
}

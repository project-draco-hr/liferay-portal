{
  PortletPreferences preferences=getPreferences();
  String[] questions=StringUtil.split(preferences.getValue("reminder-queries",null),StringPool.NEW_LINE);
  return SetUtil.fromArray(questions);
}

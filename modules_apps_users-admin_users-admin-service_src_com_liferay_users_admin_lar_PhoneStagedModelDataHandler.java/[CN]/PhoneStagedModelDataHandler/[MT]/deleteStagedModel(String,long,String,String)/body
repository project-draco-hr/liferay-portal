{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  Phone phone=PhoneLocalServiceUtil.fetchPhoneByUuidAndCompanyId(uuid,group.getCompanyId());
  if (phone != null) {
    deleteStagedModel(phone);
  }
}

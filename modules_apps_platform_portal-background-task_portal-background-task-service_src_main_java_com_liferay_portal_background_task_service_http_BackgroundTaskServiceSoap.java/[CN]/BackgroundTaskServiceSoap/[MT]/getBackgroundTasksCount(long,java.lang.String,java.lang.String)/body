{
  try {
    int returnValue=BackgroundTaskServiceUtil.getBackgroundTasksCount(groupId,taskExecutorClassName,completed);
    return returnValue;
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

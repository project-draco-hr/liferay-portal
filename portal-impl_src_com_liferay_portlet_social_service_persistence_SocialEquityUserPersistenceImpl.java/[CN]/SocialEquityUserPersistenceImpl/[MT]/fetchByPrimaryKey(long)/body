{
  SocialEquityUser socialEquityUser=(SocialEquityUser)EntityCacheUtil.getResult(SocialEquityUserModelImpl.ENTITY_CACHE_ENABLED,SocialEquityUserImpl.class,equityUserId,this);
  if (socialEquityUser == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (socialEquityUser == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      socialEquityUser=(SocialEquityUser)session.get(SocialEquityUserImpl.class,Long.valueOf(equityUserId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (socialEquityUser == null)) {
        EntityCacheUtil.putResult(SocialEquityUserModelImpl.ENTITY_CACHE_ENABLED,SocialEquityUserImpl.class,equityUserId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(socialEquityUser);
      }
      closeSession(session);
    }
  }
  return socialEquityUser;
}

{
  Session session=null;
  try {
    session=openSession();
    SocialEquityUser socialEquityUser=(SocialEquityUser)session.get(SocialEquityUserImpl.class,Long.valueOf(equityUserId));
    if (socialEquityUser == null) {
      if (_log.isWarnEnabled()) {
        _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + equityUserId);
      }
      throw new NoSuchEquityUserException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + equityUserId);
    }
    return socialEquityUserPersistence.remove(socialEquityUser);
  }
 catch (  NoSuchEquityUserException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

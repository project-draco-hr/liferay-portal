{
  Configuration toolConfiguration=GradleUtil.getConfiguration(project,TOOL_CONFIGURATION_NAME);
  Configuration configuration=GradleUtil.getConfiguration(project,CONFIGURATION_NAME);
  return toolConfiguration.plus(configuration);
}

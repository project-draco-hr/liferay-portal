{
  Collection<Document> documents=getArticleVersions(article);
  if (documents.isEmpty() && !PropsValues.JOURNAL_ARTICLE_INDEX_ALL_VERSIONS) {
    doDelete(article);
  }
  SearchEngineUtil.updateDocuments(getSearchEngineId(),article.getCompanyId(),documents,isCommitImmediately());
}

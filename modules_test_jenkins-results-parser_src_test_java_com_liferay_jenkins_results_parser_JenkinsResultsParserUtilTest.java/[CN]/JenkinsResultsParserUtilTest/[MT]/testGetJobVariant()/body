{
  Assert.assertEquals("integration-db2",JenkinsResultsParserUtil.getJobVariant(new JSONObject(read(new File(dependenciesDir,"/axis-integration-db2-1/api/json")))));
  Assert.assertEquals("plugins",JenkinsResultsParserUtil.getJobVariant(new JSONObject(read(new File(dependenciesDir,"/axis-plugin-1/api/json")))));
  Assert.assertEquals("",JenkinsResultsParserUtil.getJobVariant(new JSONObject(read(new File(dependenciesDir,"/job-1/api/json")))));
}

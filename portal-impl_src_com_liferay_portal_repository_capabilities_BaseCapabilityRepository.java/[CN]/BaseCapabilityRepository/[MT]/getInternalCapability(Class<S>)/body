{
  Capability capability=_supportedCapabilitiesMap.get(capabilityClass);
  if (capability == null) {
    throw new IllegalArgumentException(String.format("Capability %s not supported by repository %d",capabilityClass.getName(),getRepositoryId()));
  }
  return (S)capability;
}

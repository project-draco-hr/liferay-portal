{
  String changePasswordCmd=PropsUtil.get(PropsUtil.MAIL_HOOK_SENDMAIL_CHANGE_PASSWORD);
  changePasswordCmd=StringUtil.replace(changePasswordCmd,"%1%",userId);
  changePasswordCmd=StringUtil.replace(changePasswordCmd,"%2%",password);
  Runtime rt=Runtime.getRuntime();
  try {
    Process p=rt.exec(changePasswordCmd);
    ProcessUtil.close(p);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

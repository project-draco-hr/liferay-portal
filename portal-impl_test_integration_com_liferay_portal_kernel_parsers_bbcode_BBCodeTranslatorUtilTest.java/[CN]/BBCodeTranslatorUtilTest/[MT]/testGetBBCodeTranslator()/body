{
  BBCodeTranslator bbCodeTranslator=BBCodeTranslatorUtil.getBBCodeTranslator();
  Class<?> clazz=bbCodeTranslator.getClass();
  Assert.assertEquals(TestBBCodeTranslator.class.getName(),clazz.getName());
}

{
  schedulerEntry.setTimeUnit(TimeUnit.MINUTE);
  schedulerEntry.setTriggerType(TriggerType.SIMPLE);
  _messageBoardsConfiguration=Configurable.createConfigurable(MBConfiguration.class,properties);
  schedulerEntry.setTriggerValue(_messageBoardsConfiguration.expireBanJobInterval());
}

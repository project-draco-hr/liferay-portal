{
  schedulerEntry.setTimeUnit(TimeUnit.MINUTE);
  schedulerEntry.setTriggerType(TriggerType.SIMPLE);
  _messageBoardsSystemConfiguration=Configurable.createConfigurable(MessageBoardsSystemConfiguration.class,properties);
  schedulerEntry.setTriggerValue(_messageBoardsSystemConfiguration.expireBanJobInterval());
}

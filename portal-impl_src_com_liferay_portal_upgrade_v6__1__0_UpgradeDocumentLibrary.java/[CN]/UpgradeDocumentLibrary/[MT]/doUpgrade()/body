{
  updateFileEntries();
  updateFileRanks();
  updateFileShortcuts();
  updateFileVersions();
  updateLocks();
  if (PropsValues.DL_FILE_ENTRY_PREVIEW_AUTO_CREATE_ON_UPGRADE) {
    updateThumbnails();
  }
}

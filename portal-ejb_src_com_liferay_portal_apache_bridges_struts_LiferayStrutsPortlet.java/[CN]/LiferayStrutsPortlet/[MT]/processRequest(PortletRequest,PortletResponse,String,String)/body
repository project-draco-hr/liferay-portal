{
  Map strutsAttributes=StrutsUtil.removeStrutsAttributes(getPortletContext(),req);
  try {
    super.processRequest(req,res,defaultPage,reqType);
  }
  finally {
    StrutsUtil.setStrutsAttributes(req,strutsAttributes);
  }
}

{
  runSQL("alter table Lock_ add mvccVersion LONG default 0");
  try {
    runSQL("alter_column_type Lock_ owner VARCHAR(1024) null");
  }
 catch (  SQLException sqle) {
    upgradeTable(LockTable.TABLE_NAME,LockTable.TABLE_COLUMNS,LockTable.TABLE_SQL_CREATE,LockTable.TABLE_SQL_ADD_INDEXES);
  }
}

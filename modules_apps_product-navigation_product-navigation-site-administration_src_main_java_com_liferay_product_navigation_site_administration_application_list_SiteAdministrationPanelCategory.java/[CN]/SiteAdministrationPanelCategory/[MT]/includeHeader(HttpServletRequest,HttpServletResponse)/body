{
  request.setAttribute(ApplicationListWebKeys.PANEL_CATEGORY,this);
  request.setAttribute(ApplicationListWebKeys.GROUP_PROVIDER,_groupProvider);
  request.setAttribute(SiteAdministrationWebKeys.GROUP_URL_PROVIDER,_groupURLProvider);
  request.setAttribute(SiteAdministrationWebKeys.ITEM_SELECTOR,_itemSelector);
  request.setAttribute(SiteAdministrationWebKeys.RECENT_GROUP_MANAGER,_recentGroupManager);
  return super.includeHeader(request,response);
}

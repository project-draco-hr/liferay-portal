{
  request.setAttribute(SiteAdministrationWebKeys.GROUP_PROVIDER,_groupProvider);
  request.setAttribute(ApplicationListWebKeys.PANEL_CATEGORY,this);
  return super.includeHeader(request,response);
}

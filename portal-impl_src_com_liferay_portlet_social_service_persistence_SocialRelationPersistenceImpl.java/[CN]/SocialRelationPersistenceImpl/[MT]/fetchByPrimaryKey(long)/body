{
  Session session=null;
  try {
    session=openSession();
    return (SocialRelation)session.get(SocialRelationImpl.class,new Long(relationId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

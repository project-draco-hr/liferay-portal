{
  SocialRelation socialRelation=(SocialRelation)EntityCacheUtil.getResult(SocialRelationModelImpl.ENTITY_CACHE_ENABLED,SocialRelationImpl.class,relationId);
  if (socialRelation == _nullSocialRelation) {
    return null;
  }
  if (socialRelation == null) {
    Session session=null;
    try {
      session=openSession();
      socialRelation=(SocialRelation)session.get(SocialRelationImpl.class,Long.valueOf(relationId));
      if (socialRelation != null) {
        cacheResult(socialRelation);
      }
 else {
        EntityCacheUtil.putResult(SocialRelationModelImpl.ENTITY_CACHE_ENABLED,SocialRelationImpl.class,relationId,_nullSocialRelation);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(SocialRelationModelImpl.ENTITY_CACHE_ENABLED,SocialRelationImpl.class,relationId);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return socialRelation;
}

{
  Session session=null;
  try {
    session=openSession();
    SocialRelation socialRelation=(SocialRelation)session.get(SocialRelationImpl.class,new Long(relationId));
    if (socialRelation == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No SocialRelation exists with the primary key " + relationId);
      }
      throw new NoSuchRelationException("No SocialRelation exists with the primary key " + relationId);
    }
    return remove(socialRelation);
  }
 catch (  NoSuchRelationException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

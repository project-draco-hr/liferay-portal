{
  PollerProcessor pollerProcessor=PollerProcessorUtil.getPollerProcessor("PollerProcessorUtilTest");
  Assert.assertNotNull(pollerProcessor);
  Class<?> clazz=pollerProcessor.getClass();
  Assert.assertEquals(TestPollerProcessor.class.getName(),clazz.getName());
}

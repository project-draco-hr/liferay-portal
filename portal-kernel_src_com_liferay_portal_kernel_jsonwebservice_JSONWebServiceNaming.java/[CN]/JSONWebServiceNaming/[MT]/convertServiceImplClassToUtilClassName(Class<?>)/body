{
  String className=clazz.getName();
  if (className.endsWith("Impl")) {
    className=className.substring(0,className.length() - 4);
  }
  return StringUtil.replace(className + "Util",".impl.",StringPool.PERIOD);
}

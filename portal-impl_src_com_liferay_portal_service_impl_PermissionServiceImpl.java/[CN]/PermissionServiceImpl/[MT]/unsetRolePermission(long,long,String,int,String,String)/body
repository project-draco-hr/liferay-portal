{
  checkPermission(getPermissionChecker(),groupId,Role.class.getName(),roleId);
  User user=getUser();
  permissionLocalService.unsetRolePermission(roleId,user.getCompanyId(),name,scope,primKey,actionId);
}

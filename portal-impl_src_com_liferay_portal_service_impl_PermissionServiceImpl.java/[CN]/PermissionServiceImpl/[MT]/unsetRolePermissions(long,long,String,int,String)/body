{
  checkPermission(getPermissionChecker(),groupId,Role.class.getName(),roleId);
  User user=getUser();
  permissionLocalService.unsetRolePermissions(roleId,user.getCompanyId(),name,scope,actionId);
}

{
  if (((sku == null) && (_sku != null)) || ((sku != null) && (_sku == null)) || ((sku != null) && (_sku != null) && !sku.equals(_sku))) {
    if (!XSS_ALLOW_SKU) {
      sku=XSSUtil.strip(sku);
    }
    _sku=sku;
  }
}

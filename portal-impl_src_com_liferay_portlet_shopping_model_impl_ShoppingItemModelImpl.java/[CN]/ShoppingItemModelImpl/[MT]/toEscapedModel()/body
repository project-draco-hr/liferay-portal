{
  if (isEscapedModel()) {
    return (ShoppingItem)this;
  }
 else {
    return (ShoppingItem)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  Map localeNameMap=new HashMap();
  Locale defaultLocale=LocaleUtil.getDefault();
  localeNameMap.put(defaultLocale,name);
  return addLayout(groupId,privateLayout,parentLayoutId,localeNameMap,new HashMap(),description,type,hidden,friendlyURL);
}

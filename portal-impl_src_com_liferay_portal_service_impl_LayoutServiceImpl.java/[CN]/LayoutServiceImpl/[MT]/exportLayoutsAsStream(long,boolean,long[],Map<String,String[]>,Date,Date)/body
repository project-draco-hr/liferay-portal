{
  GroupPermissionUtil.check(getPermissionChecker(),groupId,ActionKeys.MANAGE_LAYOUTS);
  return layoutLocalService.exportLayoutsAsStream(groupId,privateLayout,layoutIds,parameterMap,startDate,endDate);
}

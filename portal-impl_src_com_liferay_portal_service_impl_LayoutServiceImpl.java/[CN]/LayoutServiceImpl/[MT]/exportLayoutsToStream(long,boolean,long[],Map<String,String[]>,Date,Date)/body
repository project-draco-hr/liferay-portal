{
  GroupPermissionUtil.check(getPermissionChecker(),groupId,ActionKeys.MANAGE_LAYOUTS);
  return layoutLocalService.exportLayoutsToStream(groupId,privateLayout,layoutIds,parameterMap,startDate,endDate);
}

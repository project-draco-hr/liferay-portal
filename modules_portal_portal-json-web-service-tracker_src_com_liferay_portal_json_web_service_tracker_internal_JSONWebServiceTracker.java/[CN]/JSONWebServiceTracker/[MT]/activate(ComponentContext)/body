{
  _componentContext=componentContext;
  BundleContext bundleContext=componentContext.getBundleContext();
  try {
    _serviceTracker=new ServiceTracker<Object,Object>(bundleContext,bundleContext.createFilter("(&(json.web.service.path=*)(json.web.service.context=*))"),this);
    _serviceTracker.open();
  }
 catch (  InvalidSyntaxException ise) {
    throw new RuntimeException("Unable to activate Liferay Portal JSON Web Service Tracker",ise);
  }
}

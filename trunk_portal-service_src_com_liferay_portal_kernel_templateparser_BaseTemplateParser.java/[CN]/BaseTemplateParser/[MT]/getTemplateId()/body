{
  long companyGroupId=GetterUtil.getLong(_tokens.get("company_group_id"));
  String templateId=_tokens.get("template_id");
  if (companyGroupId > 0) {
    return getCompanyId() + companyGroupId + templateId;
  }
 else {
    return getCompanyId() + getGroupId() + templateId;
  }
}

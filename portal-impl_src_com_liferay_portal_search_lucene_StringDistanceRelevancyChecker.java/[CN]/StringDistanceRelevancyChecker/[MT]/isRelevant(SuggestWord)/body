{
  String word=suggestWord.string;
  if (word.equals(_word)) {
    return false;
  }
  suggestWord.score=_stringDistance.getDistance(_word,suggestWord.string);
  if (suggestWord.score <= _scoresThreshold) {
    return false;
  }
  return true;
}

{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof OrgGroupRolePK)) {
    return false;
  }
  OrgGroupRolePK pk=(OrgGroupRolePK)obj;
  if ((organizationId == pk.organizationId) && (groupId == pk.groupId) && (roleId == pk.roleId)) {
    return true;
  }
 else {
    return false;
  }
}

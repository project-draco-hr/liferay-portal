{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBCategory mbCategory) method. To improve performance, call update(MBCategory mbCategory, boolean merge).");
  }
  return update(mbCategory,false);
}

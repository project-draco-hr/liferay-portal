{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBCategory mbCategory) method. Use update(MBCategory mbCategory, boolean merge) instead.");
  }
  return update(mbCategory,false);
}

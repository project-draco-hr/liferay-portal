{
  Session session=null;
  try {
    session=openSession();
    return (MBCategory)session.get(MBCategoryImpl.class,new Long(categoryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

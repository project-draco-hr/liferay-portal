{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ConfiguredProducer configuredProducer) method. Use update(ConfiguredProducer configuredProducer, boolean merge) instead.");
  }
  return update(configuredProducer,false);
}

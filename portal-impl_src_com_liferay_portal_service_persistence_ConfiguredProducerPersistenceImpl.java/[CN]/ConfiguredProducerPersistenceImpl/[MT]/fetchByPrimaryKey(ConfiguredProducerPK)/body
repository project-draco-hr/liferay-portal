{
  Session session=null;
  try {
    session=openSession();
    return (ConfiguredProducer)session.get(ConfiguredProducerImpl.class,configuredProducerPK);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

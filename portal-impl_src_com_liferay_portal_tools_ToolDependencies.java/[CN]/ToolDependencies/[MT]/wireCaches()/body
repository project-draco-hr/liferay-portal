{
  RegistryUtil.setRegistry(new BasicRegistryImpl());
  Registry registry=RegistryUtil.getRegistry();
  registry.registerService(SingleVMPool.class,new TestSingleVMPool(new DummyPortalCacheManager<>(PortalCacheManagerNames.SINGLE_VM)));
  registry.registerService(MultiVMPool.class,new TestMultiVMPool(new DummyPortalCacheManager<>(PortalCacheManagerNames.MULTI_VM)));
}

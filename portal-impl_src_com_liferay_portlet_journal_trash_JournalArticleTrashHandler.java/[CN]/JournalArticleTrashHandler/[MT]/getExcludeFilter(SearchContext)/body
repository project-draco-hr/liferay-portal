{
  BooleanFilter excludeFilter=new BooleanFilter();
  excludeFilter.addRequiredTerm(Field.ENTRY_CLASS_NAME,JournalArticle.class.getName());
  excludeFilter.addRequiredTerm("head",false);
  return excludeFilter;
}

{
  BooleanQuery excludeQuery=BooleanQueryFactoryUtil.create(searchContext);
  excludeQuery.addRequiredTerm(Field.ENTRY_CLASS_NAME,JournalArticle.class.getName());
  excludeQuery.addRequiredTerm("head",false);
  return excludeQuery;
}

{
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Page Comments Test Page")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.click(RuntimeVariables.replace("link=Page Comments Test Page"));
  selenium.waitForPageToLoad("30000");
  selenium.click("//tr[8]/td[2]/table[1]/tbody/tr/td[4]/span/a[2]");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isVisible("_107_editReplyBody3")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  assertTrue(selenium.isVisible("_107_editReplyBody3"));
  selenium.type("_107_editReplyBody3",RuntimeVariables.replace("This is a edtied test reply comment."));
  selenium.click(RuntimeVariables.replace("_107_editReplyButton3"));
  selenium.waitForPageToLoad("30000");
  assertTrue(selenium.isTextPresent("Your request processed successfully."));
  assertFalse(selenium.isVisible("_107_editReplyBody3"));
  assertEquals(RuntimeVariables.replace("This is a edtied test reply comment."),selenium.getText("//tr[8]/td[2]/div[1]"));
}

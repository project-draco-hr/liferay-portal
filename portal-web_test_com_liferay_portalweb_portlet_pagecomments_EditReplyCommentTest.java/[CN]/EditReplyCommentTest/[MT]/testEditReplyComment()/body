{
  selenium.open("/web/guest/home/");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Page Comments Test Page")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.clickAt("link=Page Comments Test Page",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  selenium.clickAt("//tr[8]/td[2]/table[1]/tbody/tr/td[4]/span/a[2]",RuntimeVariables.replace(""));
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isVisible("_107_editReplyBody3")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  assertTrue(selenium.isVisible("_107_editReplyBody3"));
  selenium.type("_107_editReplyBody3",RuntimeVariables.replace("This is a edited test reply comment."));
  selenium.keyPress("_107_editReplyBody3",RuntimeVariables.replace("\\48"));
  selenium.keyPress("_107_editReplyBody3",RuntimeVariables.replace("\\8"));
  selenium.clickAt("_107_editReplyButton3",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  assertTrue(selenium.isTextPresent("Your request processed successfully."));
  assertFalse(selenium.isVisible("_107_editReplyBody3"));
  assertEquals(RuntimeVariables.replace("This is a edited test reply comment."),selenium.getText("//tr[8]/td[2]/div[1]"));
}

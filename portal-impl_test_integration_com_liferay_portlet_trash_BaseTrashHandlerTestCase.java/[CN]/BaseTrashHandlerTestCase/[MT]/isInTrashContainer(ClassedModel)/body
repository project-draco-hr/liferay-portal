{
  Class<?> clazz=classedModel.getModelClass();
  TrashHandler trashHandler=TrashHandlerRegistryUtil.getTrashHandler(clazz.getName());
  return trashHandler.isInTrashContainer(getAssetClassPK(baseModel));
}

{
  DDLRecordSet ddlRecordSet=DDLRecordSetLocalServiceUtil.fetchDDLRecordSetByUuidAndGroupId(uuid,groupId);
  if (ddlRecordSet != null) {
    DDLRecordSetLocalServiceUtil.deleteRecordSet(ddlRecordSet);
  }
}

{
  runSQL("ALTER TABLE SyncAccount ADD COLUMN batchFileMaxSize " + "VARCHAR(16777216) BEFORE filePathName;");
  runSQL("ALTER TABLE SyncFile ADD COLUMN previousModifiedTime LONG " + "BEFORE repositoryId;");
}

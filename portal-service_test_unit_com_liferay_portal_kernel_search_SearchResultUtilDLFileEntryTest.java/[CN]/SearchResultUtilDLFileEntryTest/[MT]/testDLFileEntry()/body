{
  SearchResult searchResult=assertOneSearchResult(createDLFileEntryDocument());
  Assert.assertEquals(_DL_FILEENTRY_CLASS_NAME,searchResult.getClassName());
  Assert.assertEquals(SearchTestUtil.ENTRY_CLASS_PK,searchResult.getClassPK());
  assertEmptyFileEntryTuples(searchResult);
  Assert.assertNull(searchResult.getSummary());
  verifyZeroInteractions(_dlAppLocalService);
  assertEmptyMBMessages(searchResult);
  assertEmptyVersions(searchResult);
}

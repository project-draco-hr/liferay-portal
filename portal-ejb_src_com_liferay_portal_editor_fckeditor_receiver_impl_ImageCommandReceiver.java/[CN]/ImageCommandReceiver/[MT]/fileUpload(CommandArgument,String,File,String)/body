{
  try {
    IGFolder folder=_getFolder(arg.getGroupId(),arg.getCurrentFolder());
    IGImageServiceUtil.addImage(folder.getFolderId(),fileName,file,extension,true,true);
  }
 catch (  Exception e) {
    throw new FCKException(e);
  }
  return "0";
}

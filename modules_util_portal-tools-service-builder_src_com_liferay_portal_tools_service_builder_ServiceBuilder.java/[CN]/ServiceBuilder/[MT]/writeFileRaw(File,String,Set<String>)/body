{
  if (!file.exists() || !content.equals(FileUtils.readFileToString(file))) {
    FileUtils.write(file,content);
    modifiedFiles.add(file.getAbsolutePath());
    System.out.println("Writing " + file);
  }
}

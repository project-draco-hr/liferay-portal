{
  for (  EntityColumn entityColumn : entity.getFinderColumnsList()) {
    if (columnDBName.equals(entityColumn.getDBName())) {
      return entityColumn;
    }
  }
  throw new IllegalArgumentException("No entity column exist with db name :" + columnDBName + " on entity :"+ entity.getName());
}

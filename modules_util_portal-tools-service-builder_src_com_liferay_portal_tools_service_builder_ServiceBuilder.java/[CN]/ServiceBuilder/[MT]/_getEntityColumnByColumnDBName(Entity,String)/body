{
  for (  EntityColumn entityColumn : entity.getFinderColumnsList()) {
    if (columnDBName.equals(entityColumn.getDBName())) {
      return entityColumn;
    }
  }
  throw new IllegalArgumentException("No entity column exist with column database name " + columnDBName + " for entity "+ entity.getName());
}

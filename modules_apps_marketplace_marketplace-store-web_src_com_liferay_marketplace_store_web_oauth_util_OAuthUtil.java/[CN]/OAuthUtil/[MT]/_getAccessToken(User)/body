{
  ExpandoValue secretExpandoValue=_instance._expandoValueLocalService.getValue(user.getCompanyId(),User.class.getName(),"MP","secret",user.getUserId());
  ExpandoValue tokenExpandoValue=_instance._expandoValueLocalService.getValue(user.getCompanyId(),User.class.getName(),"MP","token",user.getUserId());
  if ((secretExpandoValue == null) || (tokenExpandoValue == null)) {
    return null;
  }
  return new Token(tokenExpandoValue.getString(),secretExpandoValue.getString());
}

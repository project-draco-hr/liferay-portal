{
  _instance._expandoValueLocalService.addValue(user.getCompanyId(),User.class.getName(),"MP","secret",user.getUserId(),token.getSecret());
  _instance._expandoValueLocalService.addValue(user.getCompanyId(),User.class.getName(),"MP","token",user.getUserId(),token.getToken());
}

{
  if (!contains(permissionChecker,entry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),BlogsEntry.class.getName(),entry.getEntryId(),actionId);
  }
}

{
  String className=ClusterRequestReceiver.class.getName();
  _parallelExecutorService=PortalExecutorManagerUtil.getPortalExecutor(className + "_parallel");
  _serialExecutorService=Executors.newSingleThreadExecutor(new NamedThreadFactory(className + "_serial",Thread.NORM_PRIORITY,PortalClassLoaderUtil.getClassLoader()));
}

{
  String address=null;
  User user=null;
  UserService userService=getUserService();
  address="test@" + TestConstants.COMPANY_ID;
  user=userService.getUserByEmailAddress(TestConstants.COMPANY_ID,address);
  assertEquals(address,user.getEmailAddress());
}

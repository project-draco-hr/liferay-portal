{
  for (  Website website : websites) {
    if (entityCache.getResult(WebsiteModelImpl.ENTITY_CACHE_ENABLED,WebsiteImpl.class,website.getPrimaryKey()) == null) {
      cacheResult(website);
    }
 else {
      website.resetOriginalValues();
    }
  }
}

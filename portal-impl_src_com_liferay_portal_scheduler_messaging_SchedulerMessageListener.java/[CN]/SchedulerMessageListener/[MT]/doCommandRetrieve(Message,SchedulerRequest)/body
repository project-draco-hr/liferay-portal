{
  List<SchedulerRequest> schedulerRequests=_schedulerEngine.getScheduledJobs(schedulerRequest.getGroupName());
  message.setPayload(schedulerRequests);
  _messageSender.send(message.getResponseDestination(),message);
}

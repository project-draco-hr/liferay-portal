{
  List<SchedulerRequest> schedulerRequests=_schedulerEngine.getScheduledJobs(schedulerRequest.getTrigger().getGroupName());
  Message responseMessage=MessageBusUtil.createResponseMessage(message,schedulerRequests);
  _messageSender.send(responseMessage.getDestinationName(),responseMessage);
}

{
  if (message.equals(_EMTPY_MESSAGE)) {
    message="{\"responseId\":\"" + _responseId + "\"}";
  }
 else {
    message=message.substring(0,message.length() - 1) + ",\"responseId\":\"" + _responseId+ "\"}";
  }
  _destination.send(message);
  wait(_timeout);
  return _responseMessage;
}

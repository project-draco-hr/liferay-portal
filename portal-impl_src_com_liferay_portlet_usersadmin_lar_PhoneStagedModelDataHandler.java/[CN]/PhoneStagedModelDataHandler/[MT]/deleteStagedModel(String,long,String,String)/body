{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  Phone phone=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  if (phone != null) {
    PhoneLocalServiceUtil.deletePhone(phone);
  }
}

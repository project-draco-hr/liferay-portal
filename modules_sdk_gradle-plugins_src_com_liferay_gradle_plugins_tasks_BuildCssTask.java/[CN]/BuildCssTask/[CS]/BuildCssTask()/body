{
  _portalWebConfiguration=GradleUtil.addConfiguration(project,_PORTAL_WEB_CONFIGURATION_NAME);
  _portalWebConfiguration.setDescription("The portal-web configuration used for compiling CSS files.");
  _portalWebConfiguration.setVisible(false);
  GradleUtil.executeIfEmpty(_portalWebConfiguration,new Action<Configuration>(){
    @Override public void execute(    Configuration configuration){
      addPortalWebDependencies();
    }
  }
);
}

{
  if ((type == RoleConstants.TYPE_SITE) || (type == RoleConstants.TYPE_ORGANIZATION) || (type == RoleConstants.TYPE_REGULAR)) {
    return String.valueOf(type);
  }
 else {
    return StringPool.BLANK;
  }
}

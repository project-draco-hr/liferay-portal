{
  SearchEngine searchEngine=SearchEngineUtil.getSearchEngine(SearchEngineUtil.getDefaultSearchEngineId());
  String vendor=searchEngine.getVendor();
  if ("SOLR".equals(vendor) || "Lucene".equals(vendor)) {
    return true;
  }
  return false;
}

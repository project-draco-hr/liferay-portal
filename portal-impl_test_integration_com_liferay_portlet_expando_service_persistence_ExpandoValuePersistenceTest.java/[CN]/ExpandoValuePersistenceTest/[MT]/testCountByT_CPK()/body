{
  try {
    _persistence.countByT_CPK(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
    _persistence.countByT_CPK(0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

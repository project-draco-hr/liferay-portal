{
  try {
    return _roleLocalService.getRole(companyId,RoleConstants.POWER_USER);
  }
 catch (  PortalException e) {
    _log.error("Unable to obtain power user role in company " + companyId,e);
  }
  return null;
}

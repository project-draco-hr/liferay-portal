{
  try {
    return _groupLocalService.getUserPersonalSiteGroup(companyId);
  }
 catch (  PortalException e) {
    _log.error("Unable to obtain personal site in company " + companyId,e);
  }
  return null;
}

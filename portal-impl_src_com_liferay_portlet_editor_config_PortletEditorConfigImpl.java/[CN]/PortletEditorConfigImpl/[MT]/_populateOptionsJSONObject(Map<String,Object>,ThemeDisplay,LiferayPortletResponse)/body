{
  Iterator<PortletEditorConfigContributor> iterator=ListUtil.reverseIterator(_portletEditorConfigContributors);
  while (iterator.hasNext()) {
    PortletEditorConfigContributor portletEditorConfigContributor=iterator.next();
    JSONObject optionsJSONObject=portletEditorConfigContributor.getOptionsJSONObject(inputEditorTaglibAttributes,themeDisplay,liferayPortletResponse);
    _optionsJSONObject.merge(optionsJSONObject);
  }
  return _optionsJSONObject;
}

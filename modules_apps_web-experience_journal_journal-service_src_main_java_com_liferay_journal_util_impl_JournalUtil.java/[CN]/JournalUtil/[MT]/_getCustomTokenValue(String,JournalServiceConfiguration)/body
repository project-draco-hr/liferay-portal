{
  for (  String keyValue : journalServiceConfiguration.customTokenValues()) {
    String[] tokenArray=keyValue.split("\\=");
    if (tokenArray.length != 2) {
      continue;
    }
    if (tokenArray[0].equals(token)) {
      return tokenArray[1];
    }
  }
  return null;
}

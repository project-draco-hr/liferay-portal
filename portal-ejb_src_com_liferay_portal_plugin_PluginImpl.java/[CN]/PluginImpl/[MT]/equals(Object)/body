{
  if (!(o instanceof Plugin)) {
    return false;
  }
  Plugin p=(Plugin)o;
  return new EqualsBuilder().append(getModuleId(),p.getModuleId()).append(getRepositoryURL(),p.getRepositoryURL()).isEquals();
}

{
  TasksReview tasksReview=findByPrimaryKey(reviewId);
  Session session=null;
  try {
    session=openSession();
    TasksReview[] array=new TasksReviewImpl[3];
    array[0]=getByP_S_C_PrevAndNext(session,tasksReview,proposalId,stage,completed,orderByComparator,true);
    array[1]=tasksReview;
    array[2]=getByP_S_C_PrevAndNext(session,tasksReview,proposalId,stage,completed,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

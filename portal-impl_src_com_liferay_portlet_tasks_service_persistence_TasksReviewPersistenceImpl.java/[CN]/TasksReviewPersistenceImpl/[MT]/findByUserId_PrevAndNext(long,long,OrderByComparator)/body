{
  TasksReview tasksReview=findByPrimaryKey(reviewId);
  Session session=null;
  try {
    session=openSession();
    TasksReview[] array=new TasksReviewImpl[3];
    array[0]=getByUserId_PrevAndNext(session,tasksReview,userId,orderByComparator,true);
    array[1]=tasksReview;
    array[2]=getByUserId_PrevAndNext(session,tasksReview,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (TasksReview)session.get(TasksReviewImpl.class,new Long(reviewId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    TasksReview tasksReview=(TasksReview)session.get(TasksReviewImpl.class,Long.valueOf(reviewId));
    if (tasksReview == null) {
      if (_log.isWarnEnabled()) {
        _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + reviewId);
      }
      throw new NoSuchReviewException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + reviewId);
    }
    return tasksReviewPersistence.remove(tasksReview);
  }
 catch (  NoSuchReviewException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

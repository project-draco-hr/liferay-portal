{
  List<WikiPage> pages=WikiPageLocalServiceUtil.getWikiPagesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<WikiPage>());
  if (ListUtil.isEmpty(pages)) {
    return null;
  }
  return pages.get(0);
}

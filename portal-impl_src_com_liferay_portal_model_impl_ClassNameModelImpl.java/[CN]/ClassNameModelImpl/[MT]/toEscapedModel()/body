{
  if (isEscapedModel()) {
    return (ClassName)this;
  }
 else {
    return (ClassName)Proxy.newProxyInstance(ClassName.class.getClassLoader(),new Class[]{ClassName.class},new AutoEscapeBeanHandler(this));
  }
}

{
  int beforeInboxSize=MailServiceTestUtil.getInboxSize();
  boolean isPasswordReset=UserServiceUtil.sendPasswordByUserId(_user.getUserId());
  Assert.assertTrue(isPasswordReset);
  Assert.assertEquals(beforeInboxSize + 1,MailServiceTestUtil.getInboxSize());
}

{
  User otherGroupAdminUser=UserTestUtil.addGroupAdminUser(_group);
  UserServiceTest.unsetGroupUsers(_group.getGroupId(),_groupAdminUser,otherGroupAdminUser);
  Assert.assertTrue(UserLocalServiceUtil.hasGroupUser(_group.getGroupId(),otherGroupAdminUser.getUserId()));
}

{
  UserServiceTest.unsetGroupUsers(_group.getGroupId(),_organizationAdminUser,_groupOwnerUser);
  Assert.assertTrue(UserLocalServiceUtil.hasGroupUser(_group.getGroupId(),_groupOwnerUser.getUserId()));
}

{
  Group group=GroupTestUtil.addGroup();
  LayoutSet publicLayoutSet=group.getPublicLayoutSet();
  Theme theme=ThemeLocalServiceUtil.getTheme(group.getCompanyId(),publicLayoutSet.getThemeId(),false);
  Assert.notNull(theme);
}

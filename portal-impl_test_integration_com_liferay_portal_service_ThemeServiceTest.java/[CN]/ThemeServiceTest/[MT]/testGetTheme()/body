{
  Group group=GroupTestUtil.addGroup();
  LayoutSet layoutSet=group.getPublicLayoutSet();
  Theme theme=ThemeLocalServiceUtil.getTheme(group.getCompanyId(),layoutSet.getThemeId(),false);
  Assert.notNull(theme);
}

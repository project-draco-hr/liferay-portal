{
  long layoutPrototypeId=ParamUtil.getLong(actionRequest,"layoutPrototypeId");
  Map<Locale,String> localeTitlesMap=LocalizationUtil.getLocalizedParameter(actionRequest,"title");
  String description=ParamUtil.getString(actionRequest,"description");
  boolean active=ParamUtil.getBoolean(actionRequest,"active");
  Locale defaultLocale=LocaleUtil.getDefault();
  String name=localeTitlesMap.get(defaultLocale);
  if (layoutPrototypeId <= 0) {
    LayoutPrototypeServiceUtil.addLayoutPrototype(name,localeTitlesMap,description,active);
  }
 else {
    LayoutPrototypeServiceUtil.updateLayoutPrototype(layoutPrototypeId,name,localeTitlesMap,description,active);
  }
}

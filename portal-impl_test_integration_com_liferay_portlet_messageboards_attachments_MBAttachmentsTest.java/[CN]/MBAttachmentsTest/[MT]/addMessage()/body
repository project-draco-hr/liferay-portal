{
  if (_category == null) {
    addCategory();
  }
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(_group.getGroupId());
  _message=MBTestUtil.addMessage(_category.getGroupId(),_category.getCategoryId(),serviceContext);
}

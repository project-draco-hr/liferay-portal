{
  if (_category == null) {
    addCategory();
  }
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext(_group.getGroupId());
  _message=MBTestUtil.addMessage(_category.getCategoryId(),serviceContext);
}

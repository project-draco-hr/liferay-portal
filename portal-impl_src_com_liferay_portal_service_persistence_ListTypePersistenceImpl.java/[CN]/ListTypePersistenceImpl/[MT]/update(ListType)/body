{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ListType listType) method. To improve performance, call update(ListType listType, boolean merge).");
  }
  return update(listType,false);
}

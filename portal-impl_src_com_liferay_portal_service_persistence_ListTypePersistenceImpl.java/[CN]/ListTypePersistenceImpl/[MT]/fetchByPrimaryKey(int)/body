{
  ListType listType=(ListType)EntityCacheUtil.getResult(ListTypeModelImpl.ENTITY_CACHE_ENABLED,ListTypeImpl.class,listTypeId,this);
  if (listType == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (listType == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      listType=(ListType)session.get(ListTypeImpl.class,Integer.valueOf(listTypeId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (listType == null)) {
        EntityCacheUtil.putResult(ListTypeModelImpl.ENTITY_CACHE_ENABLED,ListTypeImpl.class,listTypeId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(listType);
      }
      closeSession(session);
    }
  }
  return listType;
}

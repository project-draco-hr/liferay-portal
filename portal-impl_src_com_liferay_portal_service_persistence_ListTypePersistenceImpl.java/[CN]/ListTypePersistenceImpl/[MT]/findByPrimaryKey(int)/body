{
  ListType listType=fetchByPrimaryKey(listTypeId);
  if (listType == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + listTypeId);
    }
    throw new NoSuchListTypeException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + listTypeId);
  }
  return listType;
}

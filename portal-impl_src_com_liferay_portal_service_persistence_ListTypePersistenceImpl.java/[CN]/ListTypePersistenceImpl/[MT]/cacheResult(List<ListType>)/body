{
  for (  ListType listType : listTypes) {
    if (EntityCacheUtil.getResult(ListTypeModelImpl.ENTITY_CACHE_ENABLED,ListType.class,listType.getPrimaryKey(),this) == null) {
      cacheResult(listType);
    }
  }
}

{
  try {
    _persistence.countByH_P(RandomTestUtil.randomBoolean(),RandomTestUtil.nextLong());
    _persistence.countByH_P(RandomTestUtil.randomBoolean(),0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

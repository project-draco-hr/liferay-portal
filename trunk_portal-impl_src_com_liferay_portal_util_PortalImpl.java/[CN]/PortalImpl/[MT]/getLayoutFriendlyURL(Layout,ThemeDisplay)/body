{
  if (!isLayoutFriendliable(layout)) {
    return null;
  }
  String groupFriendlyURL=getGroupFriendlyURL(layout.getGroup(),layout.isPrivateLayout(),themeDisplay);
  return groupFriendlyURL.concat(layout.getFriendlyURL());
}

{
  try {
    DLLocalService dlLocalService=DLLocalServiceFactory.getService();
    return dlLocalService.search(companyId,portletId,groupId,repositoryIds,keywords);
  }
 catch (  SystemException se) {
    throw se;
  }
catch (  Exception e) {
    throw new SystemException(e);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (UserTrackerPath)session.get(UserTrackerPathImpl.class,userTrackerPathId);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

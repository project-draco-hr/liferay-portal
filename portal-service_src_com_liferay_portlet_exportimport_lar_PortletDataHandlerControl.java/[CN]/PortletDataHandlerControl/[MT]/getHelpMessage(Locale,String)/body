{
  String helpMessage=LanguageUtil.get(locale,action + "-" + _controlLabel+ "-help",StringPool.BLANK);
  if (Validator.isNull(helpMessage)) {
    helpMessage=LanguageUtil.get(locale,"export-import-publish-" + _controlLabel + "-help",StringPool.BLANK);
  }
  return helpMessage;
}

{
  if (name == null) {
    return null;
  }
  String companyId=(String)userInfo.get(UserAttributes.LIFERAY_COMPANY_ID);
  String userId=(String)userInfo.get(UserAttributes.LIFERAY_USER_ID);
  if (name.equals("user.name.random")) {
    String[] names=new String[]{"Aaa","Bbb","Ccc"};
    return names[Randomizer.getInstance().nextInt(3)];
  }
 else {
    return null;
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (DLFileVersion)session.get(DLFileVersion.class,dlFileVersionPK);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

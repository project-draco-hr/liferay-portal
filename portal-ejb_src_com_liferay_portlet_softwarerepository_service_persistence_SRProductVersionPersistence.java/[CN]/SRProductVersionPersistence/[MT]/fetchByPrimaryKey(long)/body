{
  Session session=null;
  try {
    session=openSession();
    return (SRProductVersion)session.get(SRProductVersionImpl.class,new Long(productVersionId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

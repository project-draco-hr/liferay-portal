{
  try {
    clearSRFrameworkVersions.clear(pk);
    for (int i=0; i < srFrameworkVersions.size(); i++) {
      com.liferay.portlet.softwarerepository.model.SRFrameworkVersion srFrameworkVersion=(com.liferay.portlet.softwarerepository.model.SRFrameworkVersion)srFrameworkVersions.get(i);
      addSRFrameworkVersion.add(pk,srFrameworkVersion.getPrimaryKey());
    }
  }
 catch (  DataAccessException dae) {
    throw new SystemException(dae);
  }
}

{
  SRProductVersion srProductVersion=fetchByPrimaryKey(productVersionId);
  if (srProductVersion == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No SRProductVersion exists with the primary key " + productVersionId);
    }
    throw new NoSuchProductVersionException("No SRProductVersion exists with the primary key " + productVersionId);
  }
  return srProductVersion;
}

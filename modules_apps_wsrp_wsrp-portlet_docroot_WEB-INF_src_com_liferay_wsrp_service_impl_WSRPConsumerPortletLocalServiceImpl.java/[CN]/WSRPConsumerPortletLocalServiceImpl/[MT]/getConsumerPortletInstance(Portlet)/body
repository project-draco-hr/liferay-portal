{
  if (_consumerPortletClass == null) {
    ClassLoader classLoader=getClassLoader();
    _consumerPortletClass=(Class<ConsumerPortlet>)classLoader.loadClass(portlet.getPortletClass());
  }
  return _consumerPortletClass.newInstance();
}

{
  boolean exists=_resourceExists(servletContext,theme,portletId,path);
  if (!exists && Validator.isNotNull(portletId)) {
    exists=_resourceExists(servletContext,theme,null,path);
  }
  return exists;
}

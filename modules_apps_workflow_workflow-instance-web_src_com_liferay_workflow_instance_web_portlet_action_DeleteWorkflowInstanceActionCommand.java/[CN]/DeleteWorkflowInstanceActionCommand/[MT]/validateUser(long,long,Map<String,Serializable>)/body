{
  User user=UserLocalServiceUtil.fetchUser(userId);
  if (user == null) {
    long defaultUserId=UserLocalServiceUtil.getDefaultUserId(companyId);
    workflowContext.put(WorkflowConstants.CONTEXT_USER_ID,String.valueOf(defaultUserId));
  }
}

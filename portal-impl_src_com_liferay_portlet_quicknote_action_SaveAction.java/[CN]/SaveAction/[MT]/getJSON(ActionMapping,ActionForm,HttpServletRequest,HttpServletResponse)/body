{
  ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
  String portletId=ParamUtil.getString(request,"portletId");
  PortletPermissionUtil.check(themeDisplay.getPermissionChecker(),themeDisplay.getPlid(),portletId,ActionKeys.CONFIGURATION);
  PortletPreferences prefs=PortletPreferencesFactoryUtil.getPortletSetup(request,portletId);
  String color=ParamUtil.getString(request,"color");
  String data=ParamUtil.getString(request,"data");
  if (Validator.isNotNull(color)) {
    prefs.setValue("color",color);
  }
  if (Validator.isNotNull(data)) {
    prefs.setValue("data",data);
  }
  prefs.store();
  return null;
}

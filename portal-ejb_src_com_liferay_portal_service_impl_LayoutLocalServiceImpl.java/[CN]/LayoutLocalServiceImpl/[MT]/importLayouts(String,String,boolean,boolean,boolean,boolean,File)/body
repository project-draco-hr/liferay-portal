{
  try {
    importLayouts(userId,ownerId,importPortletPreferences,importPortletData,importPermissions,importTheme,new FileInputStream(file));
  }
 catch (  FileNotFoundException fnfe) {
    throw new SystemException(fnfe);
  }
}

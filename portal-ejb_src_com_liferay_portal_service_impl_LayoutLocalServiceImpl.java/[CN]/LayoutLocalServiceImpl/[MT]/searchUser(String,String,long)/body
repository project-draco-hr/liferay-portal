{
  LinkedHashMap params=new LinkedHashMap();
  params.put("usersGroups",new Long(groupId));
  List users=UserLocalServiceUtil.search(companyId,null,null,null,emailAddress,true,params,true,0,1,null);
  if (users.size() == 0) {
    return null;
  }
 else {
    return (User)users.get(0);
  }
}

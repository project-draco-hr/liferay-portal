{
  String pk=_instance._getByC_N(companyId,name);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_N" + " " + companyId.toString() + " "+ name.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

{
  String pk=_instance._getByC_C_C(companyId,className,classPK);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_C_C" + " " + companyId.toString() + " "+ className.toString()+ " "+ classPK.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

{
  Iterator itr=DLFileShortcutUtil.findByTF_TN(toFolderId,toName).iterator();
  while (itr.hasNext()) {
    DLFileShortcut fileShortcut=(DLFileShortcut)itr.next();
    deleteFileShortcut(fileShortcut);
  }
}

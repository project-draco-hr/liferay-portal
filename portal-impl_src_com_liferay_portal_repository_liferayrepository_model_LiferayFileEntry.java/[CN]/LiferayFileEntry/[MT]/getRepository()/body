{
  try {
    return RepositoryProviderUtil.getRepository(getRepositoryId());
  }
 catch (  PortalException pe) {
    throw new SystemException("Cannot get repository for file entry " + getFileEntryId(),pe);
  }
}

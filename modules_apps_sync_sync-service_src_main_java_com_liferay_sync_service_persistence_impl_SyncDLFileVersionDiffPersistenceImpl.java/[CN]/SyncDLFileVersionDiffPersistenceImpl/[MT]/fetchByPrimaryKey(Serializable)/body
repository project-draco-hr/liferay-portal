{
  Serializable serializable=entityCache.getResult(SyncDLFileVersionDiffModelImpl.ENTITY_CACHE_ENABLED,SyncDLFileVersionDiffImpl.class,primaryKey);
  if (serializable == nullModel) {
    return null;
  }
  SyncDLFileVersionDiff syncDLFileVersionDiff=(SyncDLFileVersionDiff)serializable;
  if (syncDLFileVersionDiff == null) {
    Session session=null;
    try {
      session=openSession();
      syncDLFileVersionDiff=(SyncDLFileVersionDiff)session.get(SyncDLFileVersionDiffImpl.class,primaryKey);
      if (syncDLFileVersionDiff != null) {
        cacheResult(syncDLFileVersionDiff);
      }
 else {
        entityCache.putResult(SyncDLFileVersionDiffModelImpl.ENTITY_CACHE_ENABLED,SyncDLFileVersionDiffImpl.class,primaryKey,nullModel);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(SyncDLFileVersionDiffModelImpl.ENTITY_CACHE_ENABLED,SyncDLFileVersionDiffImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return syncDLFileVersionDiff;
}

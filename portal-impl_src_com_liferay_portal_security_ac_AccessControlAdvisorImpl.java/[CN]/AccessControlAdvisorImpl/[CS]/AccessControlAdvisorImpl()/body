{
  _policies=new CopyOnWriteArrayList<>();
  Registry registry=RegistryUtil.getRegistry();
  if (registry == null) {
    _serviceTracker=null;
    return;
  }
  _serviceTracker=registry.trackServices(AccessControlPolicy.class,new AccessControlPolicyTrackerCustomizer());
  _serviceTracker.open();
}

{
  Registry registry=RegistryUtil.getRegistry();
  AccessControlPolicy accessControlPolicy=registry.getService(serviceReference);
  _policies.add(accessControlPolicy);
  return accessControlPolicy;
}

{
  if (!_indexingFixture.isSearchEngineAvailable()) {
    return;
  }
  _documentFixture.tearDown();
  _indexWriter.deleteEntityDocuments(createSearchContext(),_entryClassName);
  _indexingFixture.tearDown();
}

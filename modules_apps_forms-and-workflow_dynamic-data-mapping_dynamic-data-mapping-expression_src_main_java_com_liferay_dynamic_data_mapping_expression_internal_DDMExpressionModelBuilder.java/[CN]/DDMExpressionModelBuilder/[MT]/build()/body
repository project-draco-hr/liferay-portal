{
  Stack<Expression> expressions=_ddmExpressionListener.getExpressionTokens();
  Stack<Expression> operands=new Stack<>();
  while (!expressions.isEmpty()) {
    processExpressionToken(operands,expressions.pop());
  }
  return operands.pop();
}

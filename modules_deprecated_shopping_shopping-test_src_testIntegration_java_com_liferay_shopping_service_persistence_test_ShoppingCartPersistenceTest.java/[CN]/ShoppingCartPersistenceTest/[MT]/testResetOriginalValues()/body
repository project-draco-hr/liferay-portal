{
  ShoppingCart newShoppingCart=addShoppingCart();
  _persistence.clearCache();
  ShoppingCart existingShoppingCart=_persistence.findByPrimaryKey(newShoppingCart.getPrimaryKey());
  Assert.assertEquals(Long.valueOf(existingShoppingCart.getGroupId()),ReflectionTestUtil.<Long>invoke(existingShoppingCart,"getOriginalGroupId",new Class<?>[0]));
  Assert.assertEquals(Long.valueOf(existingShoppingCart.getUserId()),ReflectionTestUtil.<Long>invoke(existingShoppingCart,"getOriginalUserId",new Class<?>[0]));
}

{
  try {
    SchedulingRequest sr=new SchedulingRequest(getSchedulerGroupName(liveGroupId),jobName,SchedulingRequest.UNREGISTER_TYPE);
    MessageBusUtil.sendMessage(DestinationNames.SCHEDULER,JSONUtil.serialize(sr));
  }
 catch (  Exception e) {
    throw new SystemException(e);
  }
}

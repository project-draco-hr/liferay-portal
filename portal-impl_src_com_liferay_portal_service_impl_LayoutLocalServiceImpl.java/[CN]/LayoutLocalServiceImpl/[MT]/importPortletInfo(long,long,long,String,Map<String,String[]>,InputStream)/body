{
  BatchSessionUtil.setEnabled(true);
  try {
    PortletImporter portletImporter=new PortletImporter();
    portletImporter.importPortletInfo(userId,plid,groupId,portletId,parameterMap,is);
  }
  finally {
    BatchSessionUtil.setEnabled(false);
  }
}

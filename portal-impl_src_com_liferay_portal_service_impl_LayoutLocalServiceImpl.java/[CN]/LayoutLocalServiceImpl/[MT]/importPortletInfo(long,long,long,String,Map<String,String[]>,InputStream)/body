{
  try {
    File file=FileUtil.createTempFile("lar");
    FileUtil.write(file,is);
    importPortletInfo(userId,plid,groupId,portletId,parameterMap,file);
  }
 catch (  IOException e) {
    throw new SystemException(e);
  }
}

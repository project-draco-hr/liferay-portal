{
  Map localeNamesMap=new HashMap();
  Locale defaultLocale=LocaleUtil.getDefault();
  localeNamesMap.put(defaultLocale,name);
  return addLayout(userId,groupId,privateLayout,parentLayoutId,localeNamesMap,new HashMap(),description,type,hidden,friendlyURL,dlFolderId);
}

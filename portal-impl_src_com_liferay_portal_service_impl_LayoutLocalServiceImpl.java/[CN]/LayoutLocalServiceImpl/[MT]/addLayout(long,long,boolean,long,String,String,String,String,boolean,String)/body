{
  Map localeNameMap=new HashMap();
  Locale defaultLocale=LocaleUtil.getDefault();
  localeNameMap.put(defaultLocale,name);
  return addLayout(userId,groupId,privateLayout,parentLayoutId,localeNameMap,new HashMap(),description,type,hidden,friendlyURL);
}

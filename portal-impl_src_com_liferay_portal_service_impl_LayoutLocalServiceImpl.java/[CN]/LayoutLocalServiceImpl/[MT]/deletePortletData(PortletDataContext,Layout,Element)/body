{
  List<Element> elements=parentEl.elements("portlet-data");
  for (  Element el : elements) {
    String portletId=el.attributeValue("portlet-id");
    deletePortletData(context,portletId,layout.getPlid());
  }
}

{
  if (Validator.isNull(friendlyURL)) {
    throw new NoSuchLayoutException();
  }
  friendlyURL=friendlyURL.toLowerCase();
  return layoutPersistence.findByG_P_F(groupId,privateLayout,friendlyURL);
}

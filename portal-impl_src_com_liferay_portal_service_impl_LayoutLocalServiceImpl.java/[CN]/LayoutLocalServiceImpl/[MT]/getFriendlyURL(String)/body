{
  friendlyURL=GetterUtil.getString(friendlyURL);
  friendlyURL=friendlyURL.replaceAll(StringPool.SPACE,StringPool.BLANK);
  friendlyURL=friendlyURL.toLowerCase();
  return Normalizer.normalizeToAscii(friendlyURL);
}

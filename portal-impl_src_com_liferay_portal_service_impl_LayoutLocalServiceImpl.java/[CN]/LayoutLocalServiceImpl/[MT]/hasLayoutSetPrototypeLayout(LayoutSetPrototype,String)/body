{
  Group layoutSetPrototypeGroup=layoutSetPrototype.getGroup();
  Layout layout=layoutPersistence.fetchByUUID_G(layoutUuid,layoutSetPrototypeGroup.getGroupId());
  if (layout != null) {
    return true;
  }
  return false;
}

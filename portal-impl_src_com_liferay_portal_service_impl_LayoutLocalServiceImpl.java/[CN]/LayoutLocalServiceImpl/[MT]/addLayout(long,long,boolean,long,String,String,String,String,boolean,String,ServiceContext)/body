{
  Map<Locale,String> localeDescriptionsMap=new HashMap<Locale,String>();
  Map<Locale,String> localeNamesMap=new HashMap<Locale,String>();
  Map<Locale,String> localeTitlesMap=new HashMap<Locale,String>();
  Locale defaultLocale=LocaleUtil.getDefault();
  localeDescriptionsMap.put(defaultLocale,description);
  localeNamesMap.put(defaultLocale,name);
  localeTitlesMap.put(defaultLocale,title);
  return addLayout(userId,groupId,privateLayout,parentLayoutId,localeNamesMap,localeTitlesMap,localeDescriptionsMap,new HashMap<Locale,String>(),new HashMap<Locale,String>(),type,hidden,friendlyURL,serviceContext);
}

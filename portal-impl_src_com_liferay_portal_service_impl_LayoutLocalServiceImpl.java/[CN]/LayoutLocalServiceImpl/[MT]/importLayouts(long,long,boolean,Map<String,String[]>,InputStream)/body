{
  try {
    File tempFile=FileUtil.createTempFile("lar");
    FileUtil.write(tempFile,is);
    importLayouts(userId,groupId,privateLayout,parameterMap,tempFile);
  }
 catch (  IOException e) {
    throw new SystemException(e);
  }
}

{
  BatchSessionUtil.setEnabled(true);
  try {
    LayoutImporter layoutImporter=new LayoutImporter();
    layoutImporter.importLayouts(userId,groupId,privateLayout,parameterMap,is);
  }
  finally {
    BatchSessionUtil.setEnabled(false);
  }
}

{
  for (  String portletId : layoutTypePortlet.getPortletIds()) {
    if (layoutTypePortlet.hasPortletId(portletId)) {
      exportPortletPermissions(layoutCache,companyId,groupId,guestGroup,layout.getPlid(),portletId,permissionsEl,exportUserPermissions);
    }
  }
}

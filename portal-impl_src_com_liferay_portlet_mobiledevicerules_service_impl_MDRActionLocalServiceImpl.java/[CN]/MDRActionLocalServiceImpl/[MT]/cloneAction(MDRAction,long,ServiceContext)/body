{
  MDRRule rule=mdrRuleLocalService.getMDRRule(targetRuleId);
  return addAction(rule.getGroupId(),rule.getRuleGroupId(),rule.getRuleId(),action.getNameMap(),action.getDescriptionMap(),action.getType(),action.getTypeSettings(),serviceContext);
}

{
  mdrActionPersistence.remove(action);
  MDRRuleGroupInstance ruleGroupInstance=mdrRuleGroupInstancePersistence.fetchByPrimaryKey(action.getRuleGroupInstanceId());
  if (ruleGroupInstance != null) {
    ruleGroupInstance.setModifiedDate(new Date());
    mdrRuleGroupInstancePersistence.update(ruleGroupInstance);
  }
  systemEventLocalService.addSystemEvent(action.getGroupId(),MDRAction.class.getName(),action.getActionId(),action.getUuid(),SystemEventConstants.TYPE_DELETE);
}

{
  long classNameId=PortalUtil.getClassNameId(className);
  List<MDRAction> actions=mdrActionPersistence.findByC_C(classNameId,classPK);
  for (  MDRAction action : actions) {
    deleteMDRAction(action);
  }
}

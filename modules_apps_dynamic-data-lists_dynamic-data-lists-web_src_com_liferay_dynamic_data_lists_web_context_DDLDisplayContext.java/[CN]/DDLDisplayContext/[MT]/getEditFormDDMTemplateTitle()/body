{
  DDLRecordSet recordSet=getRecordSet();
  if (recordSet == null) {
    return LanguageUtil.get(_renderRequest.getLocale(),"add-list");
  }
  return getDDMDisplay().getEditTemplateTitle(recordSet.getDDMStructure(),fetchFormDDMTemplate(),getLocale());
}

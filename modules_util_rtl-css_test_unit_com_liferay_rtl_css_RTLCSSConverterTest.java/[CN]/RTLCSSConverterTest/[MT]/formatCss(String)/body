{
  CascadingStyleSheet cascadingStyleSheet=CSSReader.readFromString(css,CCharset.CHARSET_UTF_8_OBJ,ECSSVersion.CSS30);
  CSSWriter writer=new CSSWriter(new CSSWriterSettings(ECSSVersion.CSS30,true));
  return writer.getCSSAsString(cascadingStyleSheet);
}

{
  RTLCSSConverter rtlCssConverter=new RTLCSSConverter();
  Assert.assertNotNull(rtlCssConverter);
  Assert.assertEquals("p{text-align:left}",rtlCssConverter.process("p{text-align:right}"));
  Assert.assertEquals("p{text-align:right}",rtlCssConverter.process("p{text-align:left}"));
}

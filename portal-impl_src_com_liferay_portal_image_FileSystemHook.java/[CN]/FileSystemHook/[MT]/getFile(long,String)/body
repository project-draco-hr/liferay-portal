{
  StringBuilder sb=new StringBuilder();
  buildPath(sb,String.valueOf(imageId));
  return new File(_rootDir + StringPool.SLASH + sb.toString()+ StringPool.SLASH+ imageId+ StringPool.PERIOD+ type);
}

{
  String languageId=LocaleUtil.toLanguageId(locale);
  XPath dynamicContentXPath=SAXReaderUtil.createXPath("//dynamic-content(@language-id=" + languageId + ")");
  List<Element> elements=(List<Element>)dynamicContentXPath.evaluate(dynamicElement);
  return elements.get(index);
}

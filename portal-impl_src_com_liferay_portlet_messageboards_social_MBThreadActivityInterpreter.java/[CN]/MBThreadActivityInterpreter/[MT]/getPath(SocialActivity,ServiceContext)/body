{
  MBThread thread=MBThreadLocalServiceUtil.getThread(activity.getClassPK());
  long rootMessageId=thread.getRootMessageId();
  return addNoSuchEntryRedirect("/message_boards/find_message?messageId=" + rootMessageId,activity.getClassName(),rootMessageId,serviceContext);
}

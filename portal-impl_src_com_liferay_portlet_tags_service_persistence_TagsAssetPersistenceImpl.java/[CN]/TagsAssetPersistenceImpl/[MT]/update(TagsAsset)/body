{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TagsAsset tagsAsset) method. To improve performance, call update(TagsAsset tagsAsset, boolean merge).");
  }
  return update(tagsAsset,false);
}

{
  TagsAsset tagsAsset=(TagsAsset)EntityCacheUtil.getResult(TagsAssetModelImpl.ENTITY_CACHE_ENABLED,TagsAssetImpl.class,assetId,this);
  if (tagsAsset == null) {
    Session session=null;
    try {
      session=openSession();
      tagsAsset=(TagsAsset)session.get(TagsAssetImpl.class,new Long(assetId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (tagsAsset != null) {
        cacheResult(tagsAsset);
      }
      closeSession(session);
    }
  }
  return tagsAsset;
}

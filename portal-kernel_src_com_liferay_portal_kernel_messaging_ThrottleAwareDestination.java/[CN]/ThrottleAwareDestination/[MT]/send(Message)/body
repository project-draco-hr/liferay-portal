{
  if ((_maximumQueueSize > 0) && (getPendingRequestCount() > _maximumQueueSize)) {
    throw new IllegalStateException(getName() + " has exceeded the maximum number of pending " + "requests.  Current size: "+ getPendingRequestCount()+ " maximum allowed: "+ _maximumQueueSize);
  }
  _delegateDestination.send(message);
}

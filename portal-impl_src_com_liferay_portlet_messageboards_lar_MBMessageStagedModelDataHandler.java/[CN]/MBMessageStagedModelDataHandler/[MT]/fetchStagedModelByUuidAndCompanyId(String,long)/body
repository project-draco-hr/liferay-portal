{
  List<MBMessage> messages=MBMessageLocalServiceUtil.getMBMessagesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<MBMessage>());
  if (ListUtil.isEmpty(messages)) {
    return null;
  }
  return messages.get(0);
}

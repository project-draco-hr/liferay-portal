{
  _policy=policy;
  try {
    _init();
  }
 catch (  PrivilegedActionException pae) {
    throw new IllegalStateException("Liferay needs to be able to change the accessibility of the " + "'key' field in " + ProtectionDomain.class.getName() + " as well as get the protection domains of classes.",pae.getException());
  }
}

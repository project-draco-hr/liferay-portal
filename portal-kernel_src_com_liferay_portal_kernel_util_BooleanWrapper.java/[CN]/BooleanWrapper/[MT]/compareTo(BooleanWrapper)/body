{
  if (booleanWrapper == null) {
    return 1;
  }
  if (getValue() && !booleanWrapper.getValue()) {
    return 1;
  }
 else   if (!getValue() && booleanWrapper.getValue()) {
    return -1;
  }
 else {
    return 0;
  }
}

{
  String backURL=ParamUtil.getString(liferayPortletRequest,"backURL");
  if (Validator.isNull(backURL)) {
    backURL=getViewTemplatesURL(liferayPortletRequest,liferayPortletResponse,classNameId,classPK);
  }
  return backURL;
}

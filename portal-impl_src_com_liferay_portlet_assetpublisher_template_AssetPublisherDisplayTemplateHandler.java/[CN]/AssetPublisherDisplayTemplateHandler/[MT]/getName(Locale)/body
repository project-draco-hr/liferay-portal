{
  StringBundler sb=new StringBundler();
  sb.append(PortalUtil.getPortletTitle(PortletKeys.ASSET_PUBLISHER,locale));
  sb.append(StringPool.SPACE);
  sb.append(LanguageUtil.get(locale,"template"));
  return sb.toString();
}

{
  Document doc=getEventDocument(companyId,groupId,userId,userName,eventId,title,description,displayDate,assetTagNames,expandoBridge);
  SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
}

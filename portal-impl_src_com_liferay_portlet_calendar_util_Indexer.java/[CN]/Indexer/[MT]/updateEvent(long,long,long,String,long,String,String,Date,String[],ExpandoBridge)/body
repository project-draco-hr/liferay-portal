{
  Document doc=getEventDocument(companyId,groupId,userId,userName,eventId,title,description,startDate,tagsEntries,expandoBridge);
  SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
}

{
  Session session=null;
  try {
    session=openSession();
    return (JournalSyndicatedFeed)session.get(JournalSyndicatedFeedImpl.class,new Long(id));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  TrashEntry entry1=(TrashEntry)obj1;
  TrashEntry entry2=(TrashEntry)obj2;
  int value=entry1.getUserName().toLowerCase().compareTo(entry2.getUserName().toLowerCase());
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

{
  CommandReceiver receiver=_instances.get(typeStr);
  if (receiver == null) {
    receiver=(CommandReceiver)InstancePool.get("com.liferay.portal.editor.fckeditor.receiver.impl." + typeStr + "CommandReceiver");
  }
  return receiver;
}

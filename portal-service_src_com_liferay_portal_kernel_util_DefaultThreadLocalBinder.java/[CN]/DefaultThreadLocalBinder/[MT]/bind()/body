{
  Map<ThreadLocal<?>,?> threadLocalValueMap=_threadLocalValueMap.get();
  for (  Map.Entry<ThreadLocal<?>,?> threadLocalValueEntry : threadLocalValueMap.entrySet()) {
    ThreadLocal<Object> threadLocal=(ThreadLocal<Object>)threadLocalValueEntry.getKey();
    Object value=threadLocalValueEntry.getValue();
    threadLocal.set(value);
  }
}

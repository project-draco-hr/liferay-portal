{
  Matcher matcher=_hookPluginPattern.matcher(file.getName());
  if (matcher.find() && isMatchingFile(file,"WEB-INF/liferay-hook.xml") && !isMatchingFile(file,"WEB-INF/liferay-portlet.xml")) {
    return true;
  }
  return false;
}

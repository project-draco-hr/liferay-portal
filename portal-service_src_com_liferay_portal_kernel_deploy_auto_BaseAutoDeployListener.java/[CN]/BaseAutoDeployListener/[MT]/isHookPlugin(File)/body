{
  Matcher matcher=_hookPluginPattern.matcher(file.getName());
  if (matcher.find() && isMatchingFile(file,"WEB-INF/liferay-hook.xml",false) && !isMatchingFile(file,"WEB-INF/liferay-portlet.xml",false)) {
    return true;
  }
  return false;
}

{
  String fileName=file.getName();
  if (isMatchingFile(file,"WEB-INF/liferay-plugin-package.properties") && fileName.contains("-hook") && !fileName.contains("-portlet")) {
    return true;
  }
  return false;
}

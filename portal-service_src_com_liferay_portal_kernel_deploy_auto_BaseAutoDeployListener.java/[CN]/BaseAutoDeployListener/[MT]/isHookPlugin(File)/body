{
  String fileName=file.getName();
  if (isMatchingFile(file,"WEB-INF/liferay-hook.xml") && !isMatchingFile(file,"WEB-INF/liferay-portlet.xml") && !fileName.contains("-theme")&& !fileName.contains("-web")&& !isJarFile(file)) {
    return true;
  }
  return false;
}

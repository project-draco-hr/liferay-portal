{
  if (isMatchingFile(file,"WEB-INF/liferay-look-and-feel.xml")) {
    return true;
  }
  Matcher matcher=_themePluginPattern.matcher(file.getName());
  if (matcher.find() && isMatchingFile(file,"WEB-INF/liferay-plugin-package.properties",false)) {
    return true;
  }
  return false;
}

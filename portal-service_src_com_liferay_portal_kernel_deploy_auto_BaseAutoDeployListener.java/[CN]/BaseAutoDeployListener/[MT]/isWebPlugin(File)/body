{
  String fileName=file.getName();
  Matcher matcher=_webPluginPattern.matcher(fileName);
  if (matcher.find() && isMatchingFile(file,"WEB-INF/liferay-plugin-package.properties")) {
    return true;
  }
  return false;
}

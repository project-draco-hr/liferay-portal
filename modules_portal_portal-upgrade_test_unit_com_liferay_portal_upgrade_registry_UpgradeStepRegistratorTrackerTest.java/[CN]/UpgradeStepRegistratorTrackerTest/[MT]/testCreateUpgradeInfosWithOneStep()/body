{
  TestUpgradeStep testUpgradeStep=new TestUpgradeStep();
  List<UpgradeInfo> upgradeInfos=UpgradeStepRegistratorTracker.createUpgradeInfos("0.0.0","1.0.0",testUpgradeStep);
  Assert.assertEquals(1,upgradeInfos.size());
  Assert.assertEquals(new UpgradeInfo("0.0.0","1.0.0",testUpgradeStep),upgradeInfos.get(0));
}

{
  if (super.canSerialize(javaClass,jsonClass)) {
    return true;
  }
 else   if (Message.class.isAssignableFrom(javaClass) && (jsonClass == null || jsonClass == JSONObject.class)) {
    return true;
  }
  return false;
}

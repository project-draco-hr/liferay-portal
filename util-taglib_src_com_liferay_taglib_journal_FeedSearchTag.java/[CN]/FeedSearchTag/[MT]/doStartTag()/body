{
  List<JournalFeed> feeds;
  try {
    feeds=JournalFeedLocalServiceUtil.search(_companyId,_groupId,_feedId,_name,_description,_andOperator,_start,_end,_obc);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,feeds);
  return SKIP_BODY;
}

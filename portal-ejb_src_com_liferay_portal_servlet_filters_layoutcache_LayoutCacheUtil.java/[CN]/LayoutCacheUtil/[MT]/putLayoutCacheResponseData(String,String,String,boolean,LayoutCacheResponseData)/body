{
  if (data != null) {
    plid=plid.trim().toUpperCase();
    String key=_encodeKey(companyId,plid + LANGUAGE + languageId+ IS_IE+ Boolean.toString(isIe));
    _cache.putInCache(key,data,GROUP_NAME_ARRAY);
  }
}

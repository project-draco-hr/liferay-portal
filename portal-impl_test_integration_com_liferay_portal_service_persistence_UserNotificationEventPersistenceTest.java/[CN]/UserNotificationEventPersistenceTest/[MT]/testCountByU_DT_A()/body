{
  try {
    _persistence.countByU_DT_A(RandomTestUtil.nextLong(),RandomTestUtil.nextInt(),RandomTestUtil.randomBoolean());
    _persistence.countByU_DT_A(0L,0,RandomTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

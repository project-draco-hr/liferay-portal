{
  try {
    return _atomCollectionAdapter.postEntry(title,summary,updated,content.getText(),new AtomRequestContextImpl(requestContext));
  }
 catch (  AtomException e) {
    throw new ResponseContextException(e.getErrorCode(),e.getCause());
  }
}

{
  try {
    _atomCollectionAdapter.putEntry(entry,title,updated,summary,content.getText(),new AtomRequestContextImpl(requestContext));
  }
 catch (  AtomException e) {
    throw new ResponseContextException(e.getErrorCode(),e.getCause());
  }
}

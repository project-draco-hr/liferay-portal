{
  DLFileEntryMetadata newDLFileEntryMetadata=addDLFileEntryMetadata();
  _persistence.clearCache();
  DLFileEntryMetadata existingDLFileEntryMetadata=_persistence.findByPrimaryKey(newDLFileEntryMetadata.getPrimaryKey());
  Assert.assertEquals(Long.valueOf(existingDLFileEntryMetadata.getDDMStructureId()),ReflectionTestUtil.<Long>invoke(existingDLFileEntryMetadata,"getOriginalDDMStructureId",new Class<?>[0]));
  Assert.assertEquals(Long.valueOf(existingDLFileEntryMetadata.getFileVersionId()),ReflectionTestUtil.<Long>invoke(existingDLFileEntryMetadata,"getOriginalFileVersionId",new Class<?>[0]));
}

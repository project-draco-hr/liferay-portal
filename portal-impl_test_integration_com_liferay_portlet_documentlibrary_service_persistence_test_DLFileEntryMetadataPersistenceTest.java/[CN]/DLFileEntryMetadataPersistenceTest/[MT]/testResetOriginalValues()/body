{
  DLFileEntryMetadata newDLFileEntryMetadata=addDLFileEntryMetadata();
  _persistence.clearCache();
  DLFileEntryMetadata existingDLFileEntryMetadata=_persistence.findByPrimaryKey(newDLFileEntryMetadata.getPrimaryKey());
  Assert.assertEquals(existingDLFileEntryMetadata.getDDMStructureId(),ReflectionTestUtil.invoke(existingDLFileEntryMetadata,"getOriginalDDMStructureId",new Class<?>[0]));
  Assert.assertEquals(existingDLFileEntryMetadata.getFileVersionId(),ReflectionTestUtil.invoke(existingDLFileEntryMetadata,"getOriginalFileVersionId",new Class<?>[0]));
}

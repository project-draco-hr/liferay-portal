{
  try {
    _persistence.countByG_C_A(RandomTestUtil.nextLong(),RandomTestUtil.nextLong(),RandomTestUtil.nextInt());
    _persistence.countByG_C_A(0L,0L,0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

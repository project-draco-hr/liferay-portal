{
  MBMessage mbMessage=MBMessageLocalServiceUtil.getMBMessage(primaryKey);
  if (!mbMessage.isRoot()) {
    MBMessageLocalServiceUtil.deleteMBMessage(primaryKey);
  }
 else {
    MBThreadLocalServiceUtil.deleteThread(mbMessage.getThreadId());
  }
}

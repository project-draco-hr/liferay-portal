{
  MBCategory category=(MBCategory)parentBaseModel;
  return MBTestUtil.addMessageWithWorkflow(serviceContext.getUserId(),category.getGroupId(),category.getCategoryId(),RandomTestUtil.randomString(),keywords,approved,serviceContext);
}

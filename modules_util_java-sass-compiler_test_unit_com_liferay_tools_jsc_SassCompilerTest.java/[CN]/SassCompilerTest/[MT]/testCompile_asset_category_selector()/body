{
  SassCompiler compiler=new SassCompiler();
  assumeNotNull(compiler);
  String output=compiler.compileFile(_BASE_RESOURCES + "_asset_category_selector.scss","","");
  assertNotNull(output);
  assertEquals(stripNewLines(readFileContents(_BASE_RESOURCES + "_asset_category_selector.css")),stripNewLines(output));
}

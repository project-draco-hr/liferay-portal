{
  if (type == BARE) {
    return "-bare";
  }
 else   if (type == MINIMAL) {
    return "-minimal";
  }
 else   if (type == SHARDED) {
    return "-sharded";
  }
 else {
    return StringPool.BLANK;
  }
}

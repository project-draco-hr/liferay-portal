{
  AuthFailuresContainer authFailuresContainer=new AuthFailuresContainer();
  _authFailuresContainerMap.put(servletContextName,authFailuresContainer);
  initAuthFailures(portletClassLoader,portalProperties,AUTH_FAILURE,authFailuresContainer);
  initAuthFailures(portletClassLoader,portalProperties,AUTH_MAX_FAILURES,authFailuresContainer);
}

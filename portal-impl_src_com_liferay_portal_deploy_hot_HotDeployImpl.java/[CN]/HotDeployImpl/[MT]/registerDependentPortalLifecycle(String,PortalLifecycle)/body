{
  for (  HotDeployEvent hotDeployEvent : _dependentHotDeployEvents) {
    if (Validator.equals(servletContextName,hotDeployEvent.getServletContextName())) {
      hotDeployEvent.addPortalLifecycle(portalLifecycle);
      return true;
    }
  }
  return false;
}

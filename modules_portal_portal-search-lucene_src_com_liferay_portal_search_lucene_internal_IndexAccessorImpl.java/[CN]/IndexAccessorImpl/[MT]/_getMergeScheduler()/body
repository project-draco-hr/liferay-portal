{
  if (Validator.equals(luceneConfiguration.mergeScheduler(),NoMergeScheduler.class.getName())) {
    return NoMergeScheduler.INSTANCE;
  }
  return (MergeScheduler)InstanceFactory.newInstance(getClass().getClassLoader(),luceneConfiguration.mergeScheduler());
}

{
  _analyzer=analyzer;
  _version=version;
  _companyId=companyId;
  _path=luceneConfiguration.dir() + _companyId + StringPool.SLASH;
  IndexSearcherManager indexSearcherManager=null;
  try {
    _checkLuceneDir();
    _initIndexWriter();
    _initCommitScheduler();
    indexSearcherManager=new IndexSearcherManager(_indexWriter);
  }
 catch (  IOException ioe) {
    _log.error("Unable to initialize index searcher manager for company " + _companyId,ioe);
  }
  _indexSearcherManager=indexSearcherManager;
}

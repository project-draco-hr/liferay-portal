{
  if (clazz == null || clazz.equals(Object.class)) {
    return null;
  }
  if (isAnnotationDeclaredInClass(annotationType,clazz)) {
    return clazz;
  }
 else {
    return findDeclaringClassAnnotation(annotationType,clazz.getSuperclass());
  }
}

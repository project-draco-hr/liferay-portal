{
  List recursiveCategoryIds=new ArrayList();
  _getRecursiveCategoryIds(recursiveCategoryIds,companyId,categoryIds);
  return ShoppingItemFinder.countByKeywords(companyId,(String[])recursiveCategoryIds.toArray(new String[0]),keywords);
}

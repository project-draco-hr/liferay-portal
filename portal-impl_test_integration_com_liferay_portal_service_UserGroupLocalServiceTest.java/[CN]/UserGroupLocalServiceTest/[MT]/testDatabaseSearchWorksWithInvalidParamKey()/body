{
  String keywords=null;
  LinkedHashMap<String,Object> userGroupParams=new LinkedHashMap<>();
  userGroupParams.put(UserGroupFinderConstants.PARAM_USER_GROUPS_ROLES,Long.valueOf(_role.getRoleId()));
  userGroupParams.put("invalidParamKey","invalidParamValue");
  List<UserGroup> userGroups=_doSearchUserGroups(keywords,userGroupParams);
  Assert.assertEquals(1,userGroups.size());
}

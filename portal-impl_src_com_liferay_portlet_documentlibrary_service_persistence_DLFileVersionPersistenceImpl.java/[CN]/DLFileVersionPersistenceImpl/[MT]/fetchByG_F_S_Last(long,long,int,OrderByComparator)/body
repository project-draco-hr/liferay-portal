{
  int count=countByG_F_S(groupId,folderId,status);
  List<DLFileVersion> list=findByG_F_S(groupId,folderId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

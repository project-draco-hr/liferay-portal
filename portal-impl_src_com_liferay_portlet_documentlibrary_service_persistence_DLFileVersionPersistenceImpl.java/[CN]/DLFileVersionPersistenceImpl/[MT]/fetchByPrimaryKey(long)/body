{
  Session session=null;
  try {
    session=openSession();
    return (DLFileVersion)session.get(DLFileVersionImpl.class,new Long(fileVersionId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

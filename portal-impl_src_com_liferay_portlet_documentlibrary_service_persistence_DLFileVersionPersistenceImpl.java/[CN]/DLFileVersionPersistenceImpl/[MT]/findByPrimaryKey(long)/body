{
  DLFileVersion dlFileVersion=fetchByPrimaryKey(fileVersionId);
  if (dlFileVersion == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No DLFileVersion exists with the primary key " + fileVersionId);
    }
    throw new NoSuchFileVersionException("No DLFileVersion exists with the primary key " + fileVersionId);
  }
  return dlFileVersion;
}

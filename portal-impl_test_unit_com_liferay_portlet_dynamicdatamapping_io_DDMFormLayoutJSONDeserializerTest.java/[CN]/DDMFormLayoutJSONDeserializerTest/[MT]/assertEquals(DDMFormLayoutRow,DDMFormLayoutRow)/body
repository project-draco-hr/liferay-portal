{
  int i=0;
  for (  DDMFormLayoutColumn expectedDDMFormLayoutColumn : expectedDDMFormLayoutRow.getDDMFormLayoutColumns()) {
    assertEquals(expectedDDMFormLayoutColumn,actualDDMFormLayoutRow.getDDMFormLayoutColumn(i++));
  }
}

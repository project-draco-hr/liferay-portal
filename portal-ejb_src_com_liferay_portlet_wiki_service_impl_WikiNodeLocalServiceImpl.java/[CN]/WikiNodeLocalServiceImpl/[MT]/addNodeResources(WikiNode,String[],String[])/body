{
  ResourceLocalServiceUtil.addModelResources(node.getCompanyId(),node.getGroupId(),node.getUserId(),WikiNode.class.getName(),node.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

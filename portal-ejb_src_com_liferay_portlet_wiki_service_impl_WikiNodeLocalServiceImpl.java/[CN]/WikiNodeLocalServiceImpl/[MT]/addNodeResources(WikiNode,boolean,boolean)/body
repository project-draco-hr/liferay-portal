{
  ResourceLocalServiceUtil.addResources(node.getCompanyId(),node.getGroupId(),node.getUserId(),WikiNode.class.getName(),node.getPrimaryKey().toString(),false,addCommunityPermissions,addGuestPermissions);
}

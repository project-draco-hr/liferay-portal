{
  DDMTemplate template1=(DDMTemplate)obj1;
  DDMTemplate template2=(DDMTemplate)obj2;
  int value=template1.getName().toLowerCase().compareTo(template2.getName().toLowerCase());
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

{
  try {
    if (DLFolderPermission.contains(themeDisplay.getPermissionChecker(),themeDisplay.getScopeGroupId(),_folder.getFolderId(),ActionKeys.ADD_FOLDER) && !_folder.isMountPoint()) {
      return true;
    }
  }
 catch (  PortalException e) {
  }
  return false;
}

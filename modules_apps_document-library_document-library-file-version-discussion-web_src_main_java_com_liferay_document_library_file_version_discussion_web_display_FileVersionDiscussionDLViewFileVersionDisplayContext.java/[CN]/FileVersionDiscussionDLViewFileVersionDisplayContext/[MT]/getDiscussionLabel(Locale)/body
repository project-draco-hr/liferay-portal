{
  ResourceBundle resourceBundle=_resourceBundleLoader.loadResourceBundle(LanguageUtil.getLanguageId(locale));
  return LanguageUtil.format(resourceBundle,"comments-for-version-x",fileVersion.getVersion());
}

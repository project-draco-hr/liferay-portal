{
  TagsEntry result=(TagsEntry)EntityCacheUtil.getResult(TagsEntryModelImpl.ENTITY_CACHE_ENABLED,TagsEntry.class,entryId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      TagsEntry tagsEntry=(TagsEntry)session.get(TagsEntryImpl.class,new Long(entryId));
      cacheResult(tagsEntry);
      return tagsEntry;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (TagsEntry)result;
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TagsEntry tagsEntry) method. To improve performance, call update(TagsEntry tagsEntry, boolean merge).");
  }
  return update(tagsEntry,false);
}

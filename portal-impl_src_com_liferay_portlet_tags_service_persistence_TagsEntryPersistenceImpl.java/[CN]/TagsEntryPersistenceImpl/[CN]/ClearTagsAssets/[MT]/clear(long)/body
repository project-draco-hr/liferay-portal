{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(entryId,com.liferay.portlet.tags.model.TagsAsset.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(entryId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(entryId,com.liferay.portlet.tags.model.TagsAsset.class.getName());
    }
  }
}

{
  MBMessageFlag mbMessageFlag=findByPrimaryKey(messageFlagId);
  Session session=null;
  try {
    session=openSession();
    MBMessageFlag[] array=new MBMessageFlagImpl[3];
    array[0]=getByUserId_PrevAndNext(session,mbMessageFlag,userId,orderByComparator,true);
    array[1]=mbMessageFlag;
    array[2]=getByUserId_PrevAndNext(session,mbMessageFlag,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

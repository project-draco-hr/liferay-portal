{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBMessageFlag mbMessageFlag) method. To improve performance, call update(MBMessageFlag mbMessageFlag, boolean merge).");
  }
  return update(mbMessageFlag,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBMessageFlag mbMessageFlag) method. Use update(MBMessageFlag mbMessageFlag, boolean merge) instead.");
  }
  return update(mbMessageFlag,false);
}

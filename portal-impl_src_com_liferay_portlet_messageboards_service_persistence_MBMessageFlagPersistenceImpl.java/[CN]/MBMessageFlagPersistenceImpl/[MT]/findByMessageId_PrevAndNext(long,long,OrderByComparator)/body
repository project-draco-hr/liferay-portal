{
  MBMessageFlag mbMessageFlag=findByPrimaryKey(messageFlagId);
  Session session=null;
  try {
    session=openSession();
    MBMessageFlag[] array=new MBMessageFlagImpl[3];
    array[0]=getByMessageId_PrevAndNext(session,mbMessageFlag,messageId,orderByComparator,true);
    array[1]=mbMessageFlag;
    array[2]=getByMessageId_PrevAndNext(session,mbMessageFlag,messageId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

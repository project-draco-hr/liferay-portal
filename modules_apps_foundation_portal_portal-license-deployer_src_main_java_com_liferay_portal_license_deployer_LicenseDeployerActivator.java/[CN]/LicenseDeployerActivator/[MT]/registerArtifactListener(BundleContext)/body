{
  Dictionary<String,Object> properties=new Hashtable<>();
  properties.put("lpkg.deployer.artifact.installer.type","license");
  return bundleContext.registerService(new String[]{ArtifactInstaller.class.getName(),ArtifactListener.class.getName()},new LicenseInstaller(),properties);
}

{
  StringMaker sm=new StringMaker();
  for (int i=0; i < groupIds.length; i++) {
    sm.append(table);
    sm.append(".groupId = ?");
    if ((i + 1) < groupIds.length) {
      sm.append(" OR ");
    }
  }
  return sm.toString();
}

{
  addUsers();
  addStandardRoles();
  Group group=getGroup();
  UserGroupRoleServiceUtil.addUserGroupRoles(getUserIds(),group.getGroupId(),getStandardRoleIds()[0]);
  Assert.assertTrue(getPropagateRolesMethodFlag());
}

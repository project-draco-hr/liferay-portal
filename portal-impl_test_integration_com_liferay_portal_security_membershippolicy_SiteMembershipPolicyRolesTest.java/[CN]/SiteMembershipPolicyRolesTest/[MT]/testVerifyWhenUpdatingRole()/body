{
  Role role=MembershipPolicyTestUtil.addRole(getClass(),RoleConstants.TYPE_SITE);
  RoleServiceUtil.updateRole(role.getRoleId(),ServiceTestUtil.randomString(),role.getTitleMap(),role.getDescriptionMap(),role.getSubtype(),new ServiceContext());
  Assert.assertTrue(isVerify());
}

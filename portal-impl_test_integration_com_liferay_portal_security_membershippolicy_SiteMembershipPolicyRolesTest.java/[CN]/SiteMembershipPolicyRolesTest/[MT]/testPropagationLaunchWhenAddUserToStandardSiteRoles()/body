{
  addUsers();
  addStandardRoles();
  Group group=getGroup();
  UserGroupRoleServiceUtil.addUserGroupRoles(getUserIds()[0],group.getGroupId(),getStandardRoleIds());
  Assert.assertTrue(getPropagateRolesMethodFlag());
}

{
  addUsers();
  addStandardRoles();
  Group group=getGroup();
  UserGroupRoleServiceUtil.deleteUserGroupRoles(getUserIds(),group.getGroupId(),getStandardRoleIds()[0]);
  Assert.assertTrue(getPropagateRolesMethodFlag());
}

{
  addUsers();
  addStandardRoles();
  Group group=getGroup();
  UserGroupRoleServiceUtil.addUserGroupRoles(getUser().getUserId(),group.getGroupId(),getStandardRoleIds());
  MembershipPolicyTestUtil.updateUser(getUser(),null,null,null,null,Collections.<UserGroupRole>emptyList());
  Assert.assertTrue(getPropagateRolesMethodFlag());
}

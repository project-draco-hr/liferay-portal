{
  addUsers();
  addStandardRoles();
  Group group=getGroup();
  UserGroupRoleServiceUtil.deleteUserGroupRoles(getUserIds()[0],group.getGroupId(),getStandardRoleIds());
  Assert.assertTrue(getPropagateRolesMethodFlag());
}

{
  if (!filePath.equals(getBaseFilePath())) {
    return;
  }
  WatchableFile watchableFile=new WatchableFile(filePath.toFile());
  watchableFile.register(_watchService,ExtendedWatchEventKind.ENTRY_RENAME_FROM,ExtendedWatchEventKind.ENTRY_RENAME_TO,StandardWatchEventKind.ENTRY_CREATE,StandardWatchEventKind.ENTRY_DELETE,StandardWatchEventKind.ENTRY_MODIFY);
  if (_logger.isTraceEnabled()) {
    _logger.trace("Registered file path {}",filePath);
  }
}

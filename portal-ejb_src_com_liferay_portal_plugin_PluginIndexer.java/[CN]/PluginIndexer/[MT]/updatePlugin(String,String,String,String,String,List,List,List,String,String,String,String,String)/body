{
  try {
    deletePlugin(moduleId,repositoryURL);
  }
 catch (  IOException ioe) {
  }
  addPlugin(moduleId,name,version,author,type,tags,licenses,liferayVersions,shortDescription,longDescription,pageURL,repositoryURL,status);
}

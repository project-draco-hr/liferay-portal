{
  if (!contains(permissionChecker,productEntry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),SCProductEntry.class.getName(),productEntry.getProductEntryId(),actionId);
  }
}

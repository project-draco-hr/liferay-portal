{
  if (!contains(permissionChecker,productEntry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,SCProductEntry.class.getName(),productEntry.getProductEntryId(),actionId);
  }
}

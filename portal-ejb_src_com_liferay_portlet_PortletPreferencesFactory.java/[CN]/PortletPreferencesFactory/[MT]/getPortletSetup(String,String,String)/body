{
  Layout layout=LayoutLocalServiceUtil.getLayout(layoutId,ownerId);
  PortletPreferencesPK pk=new PortletPreferencesPK(portletId,layoutId,ownerId);
  return PortletPreferencesLocalServiceUtil.getPreferences(layout.getCompanyId(),pk);
}

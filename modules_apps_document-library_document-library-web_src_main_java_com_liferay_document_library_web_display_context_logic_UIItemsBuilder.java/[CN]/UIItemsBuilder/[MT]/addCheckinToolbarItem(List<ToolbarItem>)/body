{
  if (!_fileEntryDisplayContextHelper.isCheckinActionAvailable()) {
    return;
  }
  _addJavaScriptUIItem(new JavaScriptToolbarItem(),toolbarItems,DLUIItemKeys.CHECKIN,LanguageUtil.get(_request,"checkin"),getSubmitFormJavaScript(Constants.CHECKIN,null));
}

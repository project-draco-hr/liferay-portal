{
  User user=permissionChecker.getUser();
  List<Group> siteGroups=user.getMySiteGroups(new String[]{Group.class.getName(),Organization.class.getName()},false,1);
  if (siteGroups.isEmpty()) {
    return false;
  }
  return super.hasAccessPermission(permissionChecker,group);
}

{
  Reference<org.osgi.util.tracker.ServiceTracker<?,?>> reference=new WeakReference<org.osgi.util.tracker.ServiceTracker<?,?>>(serviceTracker,_referenceQueue);
  _serviceTrackerReferences.add(reference);
  while ((reference=(Reference<org.osgi.util.tracker.ServiceTracker<?,?>>)_referenceQueue.poll()) != null) {
    _serviceTrackerReferences.remove(reference);
  }
}

{
  RuleHandler oldRuleHandler=_ruleHandlers.put(ruleHandler.getType(),ruleHandler);
  if (oldRuleHandler != null) {
    if (_log.isWarnEnabled()) {
      _log.warn("RuleHandler with key: " + ruleHandler.getType() + " has already been registered. Replacing with "+ ruleHandler);
    }
  }
}

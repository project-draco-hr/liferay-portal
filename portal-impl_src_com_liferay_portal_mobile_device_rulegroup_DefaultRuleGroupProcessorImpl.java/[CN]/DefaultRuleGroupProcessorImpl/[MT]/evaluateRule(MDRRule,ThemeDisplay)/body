{
  RuleHandler ruleHandler=_ruleHandlers.get(rule.getType());
  if (ruleHandler != null) {
    return ruleHandler.evaluateRule(rule,themeDisplay);
  }
 else {
    if (_log.isWarnEnabled()) {
      _log.warn("No RuleHandler registered for type:" + rule.getType());
    }
  }
  return false;
}

{
  List<ExpandoColumn> columns=expandoColumnLocalService.getColumns(classNameId);
  for (  ExpandoColumn column : columns) {
    expandoValuePersistence.removeByC_C(classPK,column.getColumnId());
  }
}

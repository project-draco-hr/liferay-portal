{
  if (isEscapedModel()) {
    return (FormStructure)this;
  }
 else {
    return (FormStructure)Proxy.newProxyInstance(FormStructure.class.getClassLoader(),new Class[]{FormStructure.class},new AutoEscapeBeanHandler(this));
  }
}

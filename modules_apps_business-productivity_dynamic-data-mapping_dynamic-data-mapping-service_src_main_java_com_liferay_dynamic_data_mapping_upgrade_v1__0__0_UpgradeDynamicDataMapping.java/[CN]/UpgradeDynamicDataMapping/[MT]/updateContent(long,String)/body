{
  try (PreparedStatement ps=connection.prepareStatement("update DDMContent set data_= ? where contentId = ?")){
    ps.setString(1,data_);
    ps.setLong(2,contentId);
    ps.executeUpdate();
  }
 }

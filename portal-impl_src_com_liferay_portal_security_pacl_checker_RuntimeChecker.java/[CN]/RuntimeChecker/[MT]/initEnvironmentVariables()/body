{
  _environmentVariables=getPropertySet("security-manager-get-environment-variable");
  if (_log.isDebugEnabled()) {
    Set<String> environmentVariables=new TreeSet<String>(_environmentVariables);
    for (    String environmentVariable : environmentVariables) {
      _log.debug("Allowing access to environment variable " + environmentVariable);
    }
  }
}

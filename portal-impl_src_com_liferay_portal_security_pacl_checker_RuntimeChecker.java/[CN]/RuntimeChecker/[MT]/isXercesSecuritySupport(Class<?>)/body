{
  String className=clazz.getName();
  if (className.contains(".SecuritySupport$") && ((clazz.getPackage() == AbstractDOMParser.class.getPackage()) || (clazz.getPackage() == DatatypeException.class.getPackage()))) {
    return true;
  }
  return false;
}

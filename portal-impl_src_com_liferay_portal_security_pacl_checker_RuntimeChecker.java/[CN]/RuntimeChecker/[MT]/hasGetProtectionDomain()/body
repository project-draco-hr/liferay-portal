{
  Class<?> callerClass8=Reflection.getCallerClass(8);
  if (((callerClass8 == AccessController.class) && CheckerUtil.isAccessControllerDoPrivileged(8))) {
    logGetProtectionDomain(callerClass8,8);
    return true;
  }
  return false;
}

{
  Class<?> callerClass7=Reflection.getCallerClass(7);
  if (callerClass7.getName().startsWith("javax.crypto") && CheckerUtil.isAccessControllerDoPrivileged(10)) {
    logCreateSecurityManager(callerClass7,7);
    return true;
  }
  return false;
}

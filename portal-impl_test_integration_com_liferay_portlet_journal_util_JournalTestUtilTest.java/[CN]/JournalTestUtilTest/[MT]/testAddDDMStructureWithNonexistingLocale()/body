{
  try {
    resetCompanyLanguages("en_US");
    DDMStructureTestUtil.addDDMStructure(JournalArticle.class.getName(),Locale.CANADA);
    Assert.fail();
  }
 catch (  StructureNameException sne) {
  }
}

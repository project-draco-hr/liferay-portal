{
  try {
    resetCompanyLanguages("en_US");
    DDMStructureTestUtil.addStructure(JournalArticle.class.getName(),Locale.CANADA);
    Assert.fail();
  }
 catch (  StructureNameException sne) {
  }
}

{
  WikiPage copyPage=addPage(page.getUserId(),page.getNodeId(),ServiceTestUtil.randomString(),page.getContent(),true,approved,serviceContext);
  WikiPageLocalServiceUtil.copyPageAttachments(page.getUserId(),page.getNodeId(),page.getTitle(),copyPage.getNodeId(),copyPage.getTitle());
  return copyPage;
}

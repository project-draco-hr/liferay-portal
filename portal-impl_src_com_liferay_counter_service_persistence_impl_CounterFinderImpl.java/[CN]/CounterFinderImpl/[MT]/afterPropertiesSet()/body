{
  CacheRegistryUtil.register(this);
  if (PropsValues.COUNTER_DATA_CENTER_COUNT > 1) {
    _multiDataCenterCounterIncrementer.initialize(PropsValues.COUNTER_DATA_CENTER_COUNT,PropsValues.COUNTER_DATA_CENTER_DEPLOYMENT_ID);
  }
 else {
    _multiDataCenterCounterIncrementer=null;
  }
}

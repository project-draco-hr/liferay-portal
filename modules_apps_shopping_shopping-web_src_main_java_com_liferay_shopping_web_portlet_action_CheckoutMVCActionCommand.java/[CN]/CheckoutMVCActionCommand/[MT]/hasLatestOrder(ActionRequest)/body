{
  try {
    ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
    _shoppingOrderLocalService.getLatestOrder(themeDisplay.getUserId(),themeDisplay.getScopeGroupId());
    return true;
  }
 catch (  NoSuchOrderException nsoe) {
    return false;
  }
}

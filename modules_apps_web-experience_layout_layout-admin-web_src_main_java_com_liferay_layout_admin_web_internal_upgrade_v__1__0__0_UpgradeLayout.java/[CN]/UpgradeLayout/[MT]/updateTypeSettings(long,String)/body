{
  try (PreparedStatement ps=connection.prepareStatement("update Layout set typeSettings = ? where plid = ?")){
    ps.setString(1,typeSettings);
    ps.setLong(2,plid);
    ps.executeUpdate();
  }
 }

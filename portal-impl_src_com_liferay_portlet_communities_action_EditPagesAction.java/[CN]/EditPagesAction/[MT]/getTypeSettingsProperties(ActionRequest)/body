{
  UnicodeProperties typeSettingsProperties=new UnicodeProperties(true);
  String prefix="TypeSettingsProperties(";
  for (  String paramName : actionRequest.getParameterMap().keySet()) {
    if (paramName.startsWith(prefix)) {
      String key=paramName.substring(prefix.length(),paramName.length() - 1);
      typeSettingsProperties.setProperty(key,actionRequest.getParameter(paramName));
    }
  }
  return typeSettingsProperties;
}

{
  Properties typeSettingsProperties=new SafeProperties();
  String prefix="TypeSettingsProperties(";
  for (  String paramName : req.getParameterMap().keySet()) {
    if (paramName.startsWith(prefix)) {
      String key=paramName.substring(prefix.length(),paramName.length() - 1);
      typeSettingsProperties.setProperty(key,req.getParameter(paramName));
    }
  }
  return typeSettingsProperties;
}

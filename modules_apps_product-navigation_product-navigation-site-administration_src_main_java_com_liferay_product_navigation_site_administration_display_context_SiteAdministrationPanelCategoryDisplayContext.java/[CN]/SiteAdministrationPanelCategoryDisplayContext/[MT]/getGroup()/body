{
  if (_group != null) {
    return _group;
  }
  _group=_themeDisplay.getScopeGroup();
  if (!_group.isControlPanel()) {
    updateLatentGroup(_group.getGroupId());
    return _group;
  }
  _group=_groupProvider.getGroup(PortalUtil.getHttpServletRequest(_portletRequest));
  return _group;
}

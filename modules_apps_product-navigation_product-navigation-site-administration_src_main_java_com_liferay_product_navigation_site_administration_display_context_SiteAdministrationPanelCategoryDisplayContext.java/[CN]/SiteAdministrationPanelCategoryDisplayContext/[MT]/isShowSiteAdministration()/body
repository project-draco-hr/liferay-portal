{
  Group group=getGroup();
  if ((group != null) && GroupPermissionUtil.contains(_themeDisplay.getPermissionChecker(),group,ActionKeys.VIEW_SITE_ADMINISTRATION)) {
    return true;
  }
  return false;
}

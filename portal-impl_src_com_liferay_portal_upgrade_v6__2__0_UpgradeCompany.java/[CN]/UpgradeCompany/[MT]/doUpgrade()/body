{
  if (Encryptor.KEY_ALGORITHM.equals("DES")) {
    _log.warn("Unable to upgrade company key, DES is chosen as default");
    return;
  }
  List<Company> companies=CompanyLocalServiceUtil.getCompanies();
  for (  Company company : companies) {
    upgradeKey(company);
  }
}

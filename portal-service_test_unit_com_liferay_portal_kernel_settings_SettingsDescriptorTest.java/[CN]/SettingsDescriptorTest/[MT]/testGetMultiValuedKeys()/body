{
  Set<String> multiValuedKeys=_settingsDescriptor.getMultiValuedKeys();
  Collection<String> expectedMultiValuedKeys=Arrays.asList("stringArray","stringArray2");
  Assert.assertEquals(multiValuedKeys.size(),expectedMultiValuedKeys.size());
  Assert.assertTrue(multiValuedKeys.containsAll(expectedMultiValuedKeys));
}

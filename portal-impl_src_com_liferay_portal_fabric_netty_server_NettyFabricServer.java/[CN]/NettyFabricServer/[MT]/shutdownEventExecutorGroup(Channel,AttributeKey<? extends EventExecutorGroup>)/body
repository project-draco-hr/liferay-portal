{
  Attribute<? extends EventExecutorGroup> attribute=channel.attr(attributeKey);
  EventExecutorGroup eventExecutorGroup=attribute.getAndRemove();
  if (eventExecutorGroup != null) {
    eventExecutorGroup.shutdownGracefully();
  }
}

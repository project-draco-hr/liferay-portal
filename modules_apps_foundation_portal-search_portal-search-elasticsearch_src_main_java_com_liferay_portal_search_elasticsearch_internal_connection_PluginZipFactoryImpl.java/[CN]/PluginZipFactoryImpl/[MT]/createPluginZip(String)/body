{
  try {
    return new PluginZipImpl(ResourceUtil.getResourceAsTempFile(getClass(),resourceName));
  }
 catch (  IOException ioe) {
    throw new RuntimeException("Unable to write temporary plugin zip file from resource " + resourceName,ioe);
  }
}

{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByGroupId_PrevAndNext(documentTypeId,groupId,orderByComparator);
  }
  DLDocumentType dlDocumentType=findByPrimaryKey(documentTypeId);
  Session session=null;
  try {
    session=openSession();
    DLDocumentType[] array=new DLDocumentTypeImpl[3];
    array[0]=filterGetByGroupId_PrevAndNext(session,dlDocumentType,groupId,orderByComparator,true);
    array[1]=dlDocumentType;
    array[2]=filterGetByGroupId_PrevAndNext(session,dlDocumentType,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

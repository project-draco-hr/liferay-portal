{
  BlockingQueue<Thread> threadQueue=ReturnWithoutExitProcessCallable._threadQueue;
  try {
    Thread thread=threadQueue.take();
    thread.interrupt();
  }
 catch (  InterruptedException ie) {
    throw new ProcessException(ie);
  }
  return null;
}

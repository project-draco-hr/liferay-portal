{
  AnnouncementsDelivery announcementsDelivery=fetchByPrimaryKey(deliveryId);
  if (announcementsDelivery == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No AnnouncementsDelivery exists with the primary key " + deliveryId);
    }
    throw new NoSuchDeliveryException("No AnnouncementsDelivery exists with the primary key " + deliveryId);
  }
  return announcementsDelivery;
}

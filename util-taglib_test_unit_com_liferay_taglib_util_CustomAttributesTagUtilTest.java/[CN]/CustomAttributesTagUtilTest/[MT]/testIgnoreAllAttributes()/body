{
  Assert.assertEquals(StringPool.BLANK,StringUtil.merge(filterAttributes(_attributes,"aaa,bbb,ccc")));
  Assert.assertEquals(StringPool.BLANK,StringUtil.merge(filterAttributes(_attributes,"bbb,ccc,aaa")));
  Assert.assertEquals(StringPool.BLANK,StringUtil.merge(filterAttributes(_attributes,"ccc,bbb,aaa")));
}

{
  Assert.assertEquals("bbb,ccc",StringUtil.merge(filterAttributes(_attributes,"aaa")));
  Assert.assertEquals("aaa,ccc",StringUtil.merge(filterAttributes(_attributes,"bbb")));
  Assert.assertEquals("aaa,bbb",StringUtil.merge(filterAttributes(_attributes,"ccc")));
}

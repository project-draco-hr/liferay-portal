{
  DLPermission.check(getPermissionChecker(),groupId,ActionKeys.ADD_DOCUMENT_TYPE);
  return dlDocumentTypeLocalService.addDocumentType(getUserId(),groupId,name,description,ddmStructureIds,serviceContext);
}

{
  long articleId=addBaseModel(0);
  JournalArticleLocalServiceUtil.subscribeStructure(group.getGroupId(),TestPropsValues.getUserId(),0);
  updateEntry(articleId);
  Assert.assertEquals(1,MailServiceTestUtil.getInboxSize());
}

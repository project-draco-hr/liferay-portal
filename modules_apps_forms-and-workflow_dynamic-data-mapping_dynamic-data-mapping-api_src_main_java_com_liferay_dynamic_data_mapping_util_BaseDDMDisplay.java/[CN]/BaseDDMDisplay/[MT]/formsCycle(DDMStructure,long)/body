{
  while ((structure != null) && (structure.getParentStructureId() != 0)) {
    if (structure.getParentStructureId() == classPK) {
      return true;
    }
 else {
      structure=DDMStructureLocalServiceUtil.fetchStructure(structure.getParentStructureId());
    }
  }
  return false;
}

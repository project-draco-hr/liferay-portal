{
  @SuppressWarnings("rawtypes") MVCCPortalCache mvccPortalCache=new MVCCPortalCache(new MemoryPortalCache(new MockPortalCacheManager(_PORTAL_CACHE_MANAGER_NAME),_PORTAL_CACHE_NAME,16));
  Serializable key=_KEY_1;
  MVCCModel value=new MockMVCCModel(_VERSION_1);
  mvccPortalCache.put(key,value);
  mvccPortalCache.put(key,value,10);
}

{
  validate(fileName);
  if ((PropsValues.WEBDAV_LITMUS || PropsValues.DL_FILE_MAX_SIZE > 0) && ((bytes == null) || (bytes.length > PropsValues.DL_FILE_MAX_SIZE))) {
    throw new FileSizeException(fileName);
  }
}

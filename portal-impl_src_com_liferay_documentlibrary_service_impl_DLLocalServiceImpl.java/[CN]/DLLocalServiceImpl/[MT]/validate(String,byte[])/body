{
  validate(fileName);
  if (((PropsValues.WEBDAV_LITMUS) || (PrefsPropsUtil.getLong(PropsKeys.DL_FILE_MAX_SIZE) > 0)) && ((bytes == null) || (bytes.length > PrefsPropsUtil.getLong(PropsKeys.DL_FILE_MAX_SIZE)))) {
    throw new FileSizeException(fileName);
  }
}

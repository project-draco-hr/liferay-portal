{
  String titleWithExtension=title;
  if (FileUtil.getExtension(titleWithExtension) == null) {
    titleWithExtension+=StringPool.PERIOD + FileUtil.getExtension(name);
  }
  return titleWithExtension;
}

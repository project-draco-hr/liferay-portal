{
  String emailMessageSubject=preferences.getValue("email-message-subject",StringPool.BLANK);
  if (Validator.isNotNull(emailMessageSubject)) {
    return emailMessageSubject;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsKeys.INVITATION_EMAIL_MESSAGE_SUBJECT));
  }
}

{
  String emailMessageBody=prefs.getValue("email-message-body",StringPool.BLANK);
  if (Validator.isNotNull(emailMessageBody)) {
    return emailMessageBody;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsUtil.INVITATION_EMAIL_MESSAGE_BODY));
  }
}

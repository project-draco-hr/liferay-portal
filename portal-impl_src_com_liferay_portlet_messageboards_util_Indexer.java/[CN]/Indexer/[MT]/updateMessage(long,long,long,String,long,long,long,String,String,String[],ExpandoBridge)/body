{
  Document doc=getMessageDocument(companyId,groupId,userId,userName,categoryId,threadId,messageId,title,content,tagsEntries,expandoBridge);
  SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
}

{
  Document doc=getMessageDocument(companyId,groupId,userId,userName,categoryId,threadId,messageId,title,content,anonymous,tagsEntries,expandoBridge);
  SearchEngineUtil.addDocument(companyId,doc);
}

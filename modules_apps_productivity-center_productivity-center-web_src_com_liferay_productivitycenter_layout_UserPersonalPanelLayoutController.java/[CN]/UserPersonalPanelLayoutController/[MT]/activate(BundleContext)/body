{
  _servletContextName=bundleContext.getBundle().getSymbolicName();
  _servletServiceRegistration=createJspServlet(bundleContext);
  _servletContextHelperServiceRegistration=createContext(bundleContext.getBundle());
}

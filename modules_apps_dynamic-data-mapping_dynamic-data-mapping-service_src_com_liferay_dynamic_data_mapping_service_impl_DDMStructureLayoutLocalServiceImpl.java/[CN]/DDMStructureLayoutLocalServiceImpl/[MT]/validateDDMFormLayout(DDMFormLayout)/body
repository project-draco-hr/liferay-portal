{
  try {
    DDMFormLayoutValidator ddmFormLayoutValidator=getDDMFormLayoutValidator();
    ddmFormLayoutValidator.validate(ddmFormLayout);
  }
 catch (  DDMFormLayoutValidationException ddmflve) {
    throw ddmflve;
  }
}

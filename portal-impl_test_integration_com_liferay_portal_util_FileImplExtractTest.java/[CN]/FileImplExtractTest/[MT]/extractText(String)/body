{
  InputStream fileInputStream=null;
  Class<?> clazz=getClass();
  fileInputStream=clazz.getResourceAsStream("dependencies/" + fileName);
  String text=FileUtil.extractText(fileInputStream,fileName);
  return text.trim();
}

{
  PortletPreferences companyDefaultSettings=new PortletPreferencesImpl();
  companyDefaultSettings.setValue(name,value);
  GroupPortletSettings sitePortletSettings=(GroupPortletSettings)portletSettings;
  sitePortletSettings.setCompanyPortletPreferences(companyDefaultSettings);
}

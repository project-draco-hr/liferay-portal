{
  Release release=fetchByPrimaryKey(releaseId);
  if (release == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + releaseId);
    }
    throw new NoSuchReleaseException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + releaseId);
  }
  return release;
}

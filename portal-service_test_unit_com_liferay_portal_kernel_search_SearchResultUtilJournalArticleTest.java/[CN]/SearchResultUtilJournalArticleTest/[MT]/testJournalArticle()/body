{
  SearchResult searchResult=searchSingleDocument(createJournalArticleDocumentWithVersion());
  assertThatSearchResultHasVersion(searchResult);
  Assert.assertNull("Summary should be null with no Indexer or AssetRenderer defined",searchResult.getSummary());
}

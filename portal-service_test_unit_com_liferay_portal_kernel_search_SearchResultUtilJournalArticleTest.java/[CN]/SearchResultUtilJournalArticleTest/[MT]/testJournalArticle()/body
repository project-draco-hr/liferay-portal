{
  searchSingleDocument(newDocumentJournalArticleWithVersion());
  assertThatSearchResultHasVersion();
  Assert.assertNull("Summary should be null with no Indexer or AssetRenderer defined",result.getSummary());
}

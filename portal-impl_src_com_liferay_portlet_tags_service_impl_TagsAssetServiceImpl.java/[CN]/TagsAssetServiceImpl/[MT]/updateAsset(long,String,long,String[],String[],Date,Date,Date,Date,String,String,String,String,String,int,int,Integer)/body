{
  return tagsAssetLocalService.updateAsset(getUserId(),groupId,className,classPK,categoryNames,entryNames,startDate,endDate,publishDate,expirationDate,mimeType,title,description,summary,url,height,width,priority);
}

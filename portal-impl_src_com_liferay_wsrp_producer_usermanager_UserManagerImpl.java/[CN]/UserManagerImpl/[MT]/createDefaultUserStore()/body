{
  try {
    User user=_getAuthlessUser();
    Company company=_getWSRPCompany();
    _getOrganization(company.getCompanyId(),_producer.getProducerKey(),user);
  }
 catch (  Exception ex) {
    _log.error(ex,ex);
    throw new ProducerException(ex);
  }
}

{
  if (isEscapedModel()) {
    return (Portlet)this;
  }
 else {
    return (Portlet)Proxy.newProxyInstance(Portlet.class.getClassLoader(),new Class[]{Portlet.class},new AutoEscapeBeanHandler(this));
  }
}

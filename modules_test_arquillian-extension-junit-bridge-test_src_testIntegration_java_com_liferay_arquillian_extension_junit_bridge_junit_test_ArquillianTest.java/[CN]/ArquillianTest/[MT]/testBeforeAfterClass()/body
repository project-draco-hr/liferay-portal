{
  try {
    Result result=BridgeJUnitTestRunner.runBridgeTests(new BridgeRunListener(ArquillianTest.class),BeforeAfterClassTestItem.class);
    assertResult(result,BeforeAfterClassTestItem.class);
  }
  finally {
    BeforeAfterClassTestItem.assertAndTearDown();
  }
}

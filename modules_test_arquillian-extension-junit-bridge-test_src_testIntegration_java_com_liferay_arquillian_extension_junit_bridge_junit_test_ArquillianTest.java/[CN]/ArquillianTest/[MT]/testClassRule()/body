{
  try {
    Result result=BridgeJUnitTestRunner.runBridgeTests(new BridgeRunListener(ArquillianTest.class),ClassRuleTestItem.class);
    assertResult(result,ClassRuleTestItem.class);
  }
  finally {
    ClassRuleTestItem.assertAndTearDown();
  }
}

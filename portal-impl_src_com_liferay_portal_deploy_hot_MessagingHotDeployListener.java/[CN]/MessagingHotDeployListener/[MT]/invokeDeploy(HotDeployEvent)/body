{
  try {
    doInvokeDeploy(hotDeployEvent);
  }
 catch (  Throwable t) {
    throwHotDeployException(hotDeployEvent,"Error sending deploy message for " + hotDeployEvent.getServletContextName(),t);
  }
}

{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByC_LtD(companyId,new Date(),start,end);
  }
 else {
    return blogsEntryPersistence.findByC_LtD_S(companyId,new Date(),status,start,end);
  }
}

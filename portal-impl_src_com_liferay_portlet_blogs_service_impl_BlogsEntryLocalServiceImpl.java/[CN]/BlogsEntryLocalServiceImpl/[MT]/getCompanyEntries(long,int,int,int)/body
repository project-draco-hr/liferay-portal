{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByC_D(companyId,new Date(),start,end);
  }
 else {
    return blogsEntryPersistence.findByC_D_S(companyId,new Date(),status,start,end);
  }
}

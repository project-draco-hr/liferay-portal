{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByG_U_D(groupId,userId,new Date(),start,end);
  }
 else {
    return blogsEntryPersistence.findByG_U_D_S(groupId,userId,new Date(),status,start,end);
  }
}

{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByG_U_LtD(groupId,userId,new Date(),start,end);
  }
 else {
    return blogsEntryPersistence.findByG_U_LtD_S(groupId,userId,new Date(),status,start,end);
  }
}

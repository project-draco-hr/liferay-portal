{
  if (status == StatusConstants.ANY) {
    return blogsEntryPersistence.countByC_D(companyId,new Date());
  }
 else {
    return blogsEntryPersistence.countByC_D_S(companyId,new Date(),status);
  }
}

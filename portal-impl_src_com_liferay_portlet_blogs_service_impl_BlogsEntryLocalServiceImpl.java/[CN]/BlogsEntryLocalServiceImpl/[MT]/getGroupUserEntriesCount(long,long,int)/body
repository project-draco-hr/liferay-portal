{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.countByG_U_LtD(groupId,userId,new Date());
  }
 else {
    return blogsEntryPersistence.countByG_U_LtD_S(groupId,userId,new Date(),status);
  }
}

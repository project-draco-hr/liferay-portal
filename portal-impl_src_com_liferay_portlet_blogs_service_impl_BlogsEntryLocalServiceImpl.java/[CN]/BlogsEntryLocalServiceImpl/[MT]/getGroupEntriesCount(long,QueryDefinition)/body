{
  if (queryDefinition.isExcludeStatus()) {
    return blogsEntryPersistence.countByG_NeS(groupId,queryDefinition.getStatus());
  }
 else {
    return blogsEntryPersistence.countByG_S(groupId,queryDefinition.getStatus());
  }
}

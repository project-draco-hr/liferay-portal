{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByGroupId(groupId,start,end,obc);
  }
 else {
    return blogsEntryPersistence.findByG_S(groupId,status,start,end,obc);
  }
}

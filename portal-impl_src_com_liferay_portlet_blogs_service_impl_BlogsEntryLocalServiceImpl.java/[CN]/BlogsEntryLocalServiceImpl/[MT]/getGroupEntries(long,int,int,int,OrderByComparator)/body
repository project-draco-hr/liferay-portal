{
  if (status == StatusConstants.ANY) {
    return blogsEntryPersistence.findByG_D(groupId,new Date(),start,end,obc);
  }
 else {
    return blogsEntryPersistence.findByG_D_S(groupId,new Date(),status,start,end,obc);
  }
}

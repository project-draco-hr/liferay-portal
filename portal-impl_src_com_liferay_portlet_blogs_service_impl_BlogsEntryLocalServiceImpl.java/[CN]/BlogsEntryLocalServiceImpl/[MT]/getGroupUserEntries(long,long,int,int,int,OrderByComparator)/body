{
  if (status == StatusConstants.ANY) {
    return blogsEntryPersistence.findByG_U_D(groupId,userId,new Date(),start,end,obc);
  }
 else {
    return blogsEntryPersistence.findByG_U_D_S(groupId,userId,new Date(),status,start,end,obc);
  }
}

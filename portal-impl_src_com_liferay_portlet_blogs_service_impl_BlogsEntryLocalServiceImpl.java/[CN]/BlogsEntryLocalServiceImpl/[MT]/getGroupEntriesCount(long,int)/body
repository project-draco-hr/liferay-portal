{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.countByG_LtD(groupId,new Date());
  }
 else {
    return blogsEntryPersistence.countByG_LtD_S(groupId,new Date(),status);
  }
}

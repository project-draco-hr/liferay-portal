{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.countByG_D(groupId,new Date());
  }
 else {
    return blogsEntryPersistence.countByG_D_S(groupId,new Date(),status);
  }
}

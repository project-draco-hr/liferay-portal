{
  BlogsEntry entry=blogsEntryPersistence.findByPrimaryKey(entryId);
  return blogsEntryPersistence.findByGroupId_PrevAndNext(entry.getEntryId(),entry.getGroupId(),new EntryDisplayDateComparator(true));
}

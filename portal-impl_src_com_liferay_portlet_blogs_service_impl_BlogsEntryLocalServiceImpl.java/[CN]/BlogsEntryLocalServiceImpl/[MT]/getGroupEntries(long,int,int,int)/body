{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByG_LtD(groupId,new Date(),start,end);
  }
 else {
    return blogsEntryPersistence.findByG_LtD_S(groupId,new Date(),status,start,end);
  }
}

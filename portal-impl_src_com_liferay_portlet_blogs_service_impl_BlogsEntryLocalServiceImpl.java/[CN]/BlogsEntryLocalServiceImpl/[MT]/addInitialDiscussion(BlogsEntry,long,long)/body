{
  if (PropsValues.BLOGS_ENTRY_COMMENTS_ENABLED) {
    CommentManager commentManager=getCommentManager();
    commentManager.addInitialDiscussion(userId,groupId,BlogsEntry.class.getName(),entry.getEntryId(),entry.getUserName());
  }
}

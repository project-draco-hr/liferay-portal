{
  if (status == WorkflowConstants.STATUS_ANY) {
    return blogsEntryPersistence.findByG_LtD(groupId,displayDate,start,end);
  }
 else {
    return blogsEntryPersistence.findByG_LtD_S(groupId,displayDate,status,start,end);
  }
}

{
  try (Connection con=DriverManager.getConnection("jdbc:hsqldb:hsql://localhost/lportal;","sa","")){
    try (Statement statement=con.createStatement()){
      statement.execute("SHUTDOWN COMPACT");
    }
   }
   _server.stop();
}

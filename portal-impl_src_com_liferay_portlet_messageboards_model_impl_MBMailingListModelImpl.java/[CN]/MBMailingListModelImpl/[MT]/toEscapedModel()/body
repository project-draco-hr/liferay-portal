{
  if (isEscapedModel()) {
    return (MBMailingList)this;
  }
 else {
    return (MBMailingList)Proxy.newProxyInstance(MBMailingList.class.getClassLoader(),new Class[]{MBMailingList.class},new AutoEscapeBeanHandler(this));
  }
}

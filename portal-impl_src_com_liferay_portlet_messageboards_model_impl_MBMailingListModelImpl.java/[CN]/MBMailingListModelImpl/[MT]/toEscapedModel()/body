{
  if (isEscapedModel()) {
    return (MBMailingList)this;
  }
 else {
    return (MBMailingList)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (Layout)this;
  }
 else {
    return (Layout)Proxy.newProxyInstance(Layout.class.getClassLoader(),new Class[]{Layout.class},new AutoEscapeBeanHandler(this));
  }
}

{
  selenium.click("link=Add Application");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("//div[@id=\"Religion-Christianity-GlobalPrayerDigest\"]")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.mouseDown("//div[@id=\"Religion-Christianity-GlobalPrayerDigest\"]/p/a");
  selenium.mouseUp("//div[@id=\"Religion-Christianity-GlobalPrayerDigest\"]/p/a");
  verifyTrue(selenium.isTextPresent("Global Prayer Digest"));
}

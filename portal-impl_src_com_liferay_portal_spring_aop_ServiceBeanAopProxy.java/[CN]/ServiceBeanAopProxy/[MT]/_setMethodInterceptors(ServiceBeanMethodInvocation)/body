{
  List<MethodInterceptor> methodInterceptors=_methodInterceptors.get(serviceBeanMethodInvocation);
  if (methodInterceptors == null) {
    methodInterceptors=_getMethodInterceptors(serviceBeanMethodInvocation);
    _methodInterceptors.put(serviceBeanMethodInvocation.toCacheKeyModel(),methodInterceptors);
  }
  serviceBeanMethodInvocation.setMethodInterceptors(methodInterceptors);
}

{
  User user=(User)obj;
  try {
    return UserGroupRoleLocalServiceUtil.hasUserGroupRole(user.getUserId(),_role.getRoleId(),_group.getGroupId());
  }
 catch (  Exception e) {
    _log.error(e);
    return false;
  }
}

{
  if (isEscapedModel()) {
    return (DDLRecordSet)this;
  }
 else {
    return (DDLRecordSet)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

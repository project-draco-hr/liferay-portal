{
  Group group=(Group)obj;
  try {
    return PermissionLocalServiceUtil.hasRolePermission(_role.getRoleId(),group.getCompanyId(),_resourceName,Resource.TYPE_CLASS,Resource.SCOPE_GROUP,group.getGroupId(),_actionId);
  }
 catch (  Exception e) {
    _log.error(e);
    return false;
  }
}

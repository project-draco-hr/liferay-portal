{
  Group group=(Group)obj;
  try {
    return PermissionLocalServiceUtil.hasRolePermission(_role.getRoleId(),group.getCompanyId(),_resourceName,ResourceImpl.TYPE_CLASS,ResourceImpl.SCOPE_GROUP,group.getGroupId(),_actionId);
  }
 catch (  Exception e) {
    _log.error(e);
    return false;
  }
}

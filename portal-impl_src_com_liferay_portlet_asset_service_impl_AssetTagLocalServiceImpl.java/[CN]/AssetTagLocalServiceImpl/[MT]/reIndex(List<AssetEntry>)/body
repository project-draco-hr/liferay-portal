{
  for (  AssetEntry assetEntry : entries) {
    ClassName className=ClassNameLocalServiceUtil.getClassName(assetEntry.getClassNameId());
    Indexer indexer=IndexerRegistryUtil.getIndexer(className.getValue());
    indexer.reIndex(className.getValue(),assetEntry.getClassPK());
  }
}

{
  for (  AssetEntry entry : entries) {
    ClassName className=ClassNameLocalServiceUtil.getClassName(entry.getClassNameId());
    Indexer indexer=IndexerRegistryUtil.getIndexer(className.getValue());
    indexer.reIndex(className.getValue(),entry.getClassPK());
  }
}

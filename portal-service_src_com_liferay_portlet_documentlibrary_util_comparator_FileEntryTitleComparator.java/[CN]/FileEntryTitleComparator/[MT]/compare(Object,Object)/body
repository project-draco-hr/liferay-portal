{
  DLFileEntry fileEntry1=(DLFileEntry)obj1;
  DLFileEntry fileEntry2=(DLFileEntry)obj2;
  int value=fileEntry1.getTitle().toLowerCase().compareTo(fileEntry2.getTitle().toLowerCase());
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

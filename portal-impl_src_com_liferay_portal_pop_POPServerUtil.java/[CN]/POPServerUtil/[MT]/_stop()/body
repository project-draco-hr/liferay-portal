{
  if (_log.isDebugEnabled()) {
    _log.debug("Stop");
  }
  try {
    if (_schedulerEntry != null) {
      SchedulerEngineUtil.unschedule(_schedulerEntry,StorageType.PERSISTED);
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

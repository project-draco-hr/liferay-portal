{
  groupName=getRealGroupName(groupName);
  SchedulerContext schedulerContext=scheduler.getContext();
  JobState jobState=(JobState)schedulerContext.get(getFullName(jobName,groupName));
  return jobState;
}

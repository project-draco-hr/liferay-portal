{
  if (!_fileEntries.contains(fileVersion.getFileEntryId()) && _isSupportedAudio(fileVersion)) {
    _fileEntries.add(fileVersion.getFileEntryId());
    MessageBusUtil.sendMessage(DestinationNames.DOCUMENT_LIBRARY_AUDIO_PROCESSOR,fileVersion);
  }
}

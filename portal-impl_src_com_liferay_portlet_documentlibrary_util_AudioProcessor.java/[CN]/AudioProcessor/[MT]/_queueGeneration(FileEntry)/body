{
  if (!_fileEntries.contains(fileEntry.getFileEntryId()) && _isSupportedAudio(fileEntry)) {
    _fileEntries.add(fileEntry.getFileEntryId());
    MessageBusUtil.sendMessage(DestinationNames.DOCUMENT_LIBRARY_AUDIO_PROCESSOR,fileEntry);
  }
}

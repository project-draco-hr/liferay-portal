{
  float[] scores=hits.getScores();
  if ((scores != null) && (scores.length != 0)) {
    Arrays.sort(scores);
    if (scores[0] >= _scoringThreshold) {
      indexKeywordQuery(searchContext.getCompanyId(),searchContext.getKeywords(),searchContext.getLocale());
    }
  }
  return true;
}

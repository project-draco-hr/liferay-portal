{
  List<MBMessage> messages=mbMessagePersistence.findByThreadId(threadId);
  for (  MBMessage message : messages) {
    if (message.isRoot()) {
      mbMessageFlagPersistence.removeByM_F(message.getMessageId(),MBMessageFlagImpl.QUESTION_FLAG);
    }
    mbMessageFlagPersistence.removeByM_F(message.getMessageId(),MBMessageFlagImpl.ANSWER_FLAG);
  }
}

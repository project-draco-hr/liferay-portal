{
  User user=UserLocalServiceUtil.getUserById(userId);
  if (user.isDefaultUser()) {
    return true;
  }
  MBMessageFlag messageFlag=MBMessageFlagUtil.fetchByU_M(userId,messageId);
  if (messageFlag != null) {
    return true;
  }
 else {
    return false;
  }
}

{
  for (  Layout layout : layouts) {
    if (EntityCacheUtil.getResult(LayoutModelImpl.ENTITY_CACHE_ENABLED,Layout.class,layout.getPrimaryKey(),this) == null) {
      cacheResult(layout);
    }
  }
}

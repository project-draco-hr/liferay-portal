{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Layout layout) method. To improve performance, call update(Layout layout, boolean merge).");
  }
  return update(layout,false);
}

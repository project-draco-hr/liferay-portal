{
  int count=countBySourcePrototypeLayoutUuid(sourcePrototypeLayoutUuid);
  List<Layout> list=findBySourcePrototypeLayoutUuid(sourcePrototypeLayoutUuid,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

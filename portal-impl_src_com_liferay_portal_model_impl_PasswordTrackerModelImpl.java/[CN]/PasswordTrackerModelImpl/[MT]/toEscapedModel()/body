{
  if (isEscapedModel()) {
    return (PasswordTracker)this;
  }
 else {
    return (PasswordTracker)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (PasswordTracker)this;
  }
 else {
    return (PasswordTracker)Proxy.newProxyInstance(PasswordTracker.class.getClassLoader(),new Class[]{PasswordTracker.class},new AutoEscapeBeanHandler(this));
  }
}

{
  PasswordTracker model=new PasswordTrackerImpl();
  model.setPasswordTrackerId(getPasswordTrackerId());
  model.setUserId(getUserId());
  model.setCreateDate(getCreateDate());
  model.setPassword(Html.escape(getPassword()));
  if (true) {
    model=(PasswordTracker)Proxy.newProxyInstance(PasswordTracker.class.getClassLoader(),new Class[]{PasswordTracker.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

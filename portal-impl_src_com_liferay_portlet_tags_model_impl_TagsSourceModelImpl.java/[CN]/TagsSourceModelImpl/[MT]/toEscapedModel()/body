{
  TagsSource model=new TagsSourceImpl();
  model.setSourceId(getSourceId());
  model.setParentSourceId(getParentSourceId());
  model.setName(Html.escape(getName()));
  model.setAcronym(Html.escape(getAcronym()));
  if (true) {
    model=(TagsSource)Proxy.newProxyInstance(TagsSource.class.getClassLoader(),new Class[]{TagsSource.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

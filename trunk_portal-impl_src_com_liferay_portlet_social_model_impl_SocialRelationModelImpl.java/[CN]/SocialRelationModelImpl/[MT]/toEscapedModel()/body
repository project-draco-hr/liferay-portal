{
  if (isEscapedModel()) {
    return (SocialRelation)this;
  }
 else {
    return (SocialRelation)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

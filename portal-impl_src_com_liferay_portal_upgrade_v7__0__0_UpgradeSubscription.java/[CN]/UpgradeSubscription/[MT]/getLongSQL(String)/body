{
  final long[] value=new long[1];
  visitSQL(sql,new SQLVisitor(){
    @Override public void visit(    ResultSet rs) throws Exception {
      value[0]=rs.getLong(1);
    }
  }
);
  return value[0];
}

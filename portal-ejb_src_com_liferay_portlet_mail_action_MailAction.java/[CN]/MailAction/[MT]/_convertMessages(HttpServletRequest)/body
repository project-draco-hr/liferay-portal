{
  String[] messagesArray=StringUtil.split(ParamUtil.getString(req,"messages"),",");
  MultiHashMap messages=new MultiHashMap();
  for (int i=0; i < messagesArray.length; i+=2) {
    messages.put(messagesArray[i],messagesArray[i + 1]);
  }
  return messages;
}

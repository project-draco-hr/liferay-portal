{
  ExpectedMultipleLogs expectedMultipleLogs=description.getAnnotation(ExpectedMultipleLogs.class);
  List<ExpectedLogs> expectedLogses=new ArrayList<>();
  if (expectedMultipleLogs == null) {
    ExpectedLogs expectedLogs=description.getAnnotation(ExpectedLogs.class);
    if (expectedLogs != null) {
      expectedLogses.add(expectedLogs);
    }
  }
 else {
    expectedLogses.addAll(Arrays.asList(expectedMultipleLogs.expectedMultipleLogs()));
  }
  return startAssert(expectedLogses);
}

{
  InterfaceOne service=RegistryUtil.getRegistry().getService(serviceReference);
  TrackedOne testWrapper=new TrackedOne();
  if (service == _a) {
    _referenceA.set(testWrapper);
  }
 else   if (service == _b) {
    _referenceB.set(testWrapper);
  }
  return testWrapper;
}

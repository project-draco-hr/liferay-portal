{
  if (isEscapedModel()) {
    return (AnnouncementsEntry)this;
  }
 else {
    return (AnnouncementsEntry)Proxy.newProxyInstance(AnnouncementsEntry.class.getClassLoader(),new Class[]{AnnouncementsEntry.class},new AutoEscapeBeanHandler(this));
  }
}

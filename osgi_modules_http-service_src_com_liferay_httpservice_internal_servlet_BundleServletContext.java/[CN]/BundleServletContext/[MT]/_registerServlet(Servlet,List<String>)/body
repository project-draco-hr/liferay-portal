{
  for (  String urlPattern : urlPatterns) {
    _servletsMap.put(urlPattern,servlet);
    if (_log.isInfoEnabled()) {
      _log.info("Registered servlet at " + getContextPath() + urlPattern);
    }
  }
}

{
  Address address=null;
  try {
    List addresses=getAddresses();
    if (addresses.size() > 0) {
      address=(Address)addresses.get(0);
    }
  }
 catch (  Exception e) {
    _log.error(e);
  }
  if (address == null) {
    address=new Address();
  }
  return address;
}

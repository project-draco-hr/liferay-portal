{
  Group group=null;
  try {
    group=GroupLocalServiceUtil.getOrganizationGroup(getCompanyId(),getOrganizationId());
  }
 catch (  Exception e) {
    group=new Group();
    _log.error(e);
  }
  return group;
}

{
  Map<String,TemplateManager> templateManagers=_getTemplateManagers();
  TemplateManager templateManager=templateManagers.remove(templateManagerName);
  if (templateManager != null) {
    templateManager.destroy();
  }
}

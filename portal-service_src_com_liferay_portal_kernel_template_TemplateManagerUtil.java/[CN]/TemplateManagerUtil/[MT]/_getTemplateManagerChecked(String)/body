{
  Map<String,TemplateManager> templateManagers=_templateManagers;
  TemplateManager templateManager=templateManagers.get(templateManagerName);
  if (templateManager == null) {
    throw new TemplateException("Unsupported template manager " + templateManagerName);
  }
  return templateManager;
}

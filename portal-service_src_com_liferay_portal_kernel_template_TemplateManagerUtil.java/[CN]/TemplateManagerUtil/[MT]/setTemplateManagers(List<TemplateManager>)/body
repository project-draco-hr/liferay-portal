{
  PortalRuntimePermission.checkSetBeanProperty(getClass());
  Map<String,TemplateManager> templateManagersMap=_getTemplateManagers();
  for (  TemplateManager templateManager : templateManagers) {
    templateManagersMap.put(templateManager.getTemplateManagerName(),templateManager);
  }
}

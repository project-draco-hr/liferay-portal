{
  Iterator<TemplateManager> iterator=_templateManagers.values().iterator();
  while (iterator.hasNext()) {
    TemplateManager templateManager=iterator.next();
    iterator.remove();
    templateManager.destroy();
  }
}

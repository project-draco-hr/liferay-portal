{
  if (isSearchWithIndex()) {
    return AssetUtil.searchAssetEntries(_request,assetEntryQuery,start,end);
  }
  int total=AssetEntryServiceUtil.getEntriesCount(assetEntryQuery);
  assetEntryQuery.setEnd(end);
  assetEntryQuery.setStart(start);
  List<AssetEntry> results=AssetEntryServiceUtil.getEntries(assetEntryQuery);
  return new BaseModelSearchResult<>(results,total);
}

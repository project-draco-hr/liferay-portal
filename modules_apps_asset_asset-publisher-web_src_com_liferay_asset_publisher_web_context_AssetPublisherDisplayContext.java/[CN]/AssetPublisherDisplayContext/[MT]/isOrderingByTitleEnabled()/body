{
  String rootPortletId=getRootPortletId();
  if (!PropsValues.ASSET_PUBLISHER_SEARCH_WITH_INDEX || rootPortletId.equals(AssetPublisherPortletKeys.RELATED_ASSETS)) {
    return false;
  }
  return true;
}

{
  if (!contains(permissionChecker,groupId,template,portletId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,DDMTemplate.class.getName(),template.getTemplateId(),actionId);
  }
}

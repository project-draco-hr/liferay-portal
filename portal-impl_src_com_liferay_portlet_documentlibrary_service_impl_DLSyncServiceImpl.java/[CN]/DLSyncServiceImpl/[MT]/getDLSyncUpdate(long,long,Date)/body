{
  Date now=new Date();
  Collection<DLSync> dlSyncs=dlSyncPersistence.findByC_M_R(companyId,lastAccessDate,repositoryId);
  DLSyncUpdate dlSyncUpdate=new DLSyncUpdate(dlSyncs,now);
  return dlSyncUpdate;
}

{
  Team team=fetchByPrimaryKey(pk);
  if (team == null) {
    teamToUserTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,userPK);
  }
 else {
    teamToUserTableMapper.addTableMapping(team.getCompanyId(),pk,userPK);
  }
}

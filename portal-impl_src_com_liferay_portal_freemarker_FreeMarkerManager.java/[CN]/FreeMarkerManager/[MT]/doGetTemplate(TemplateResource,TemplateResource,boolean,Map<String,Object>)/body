{
  Template template=new FreeMarkerTemplate(templateResource,errorTemplateResource,helperUtilities,_configuration,_templateContextHelper);
  if (restricted) {
    template=new RestrictedTemplate(template,_templateContextHelper.getRestrictedVariables());
  }
  return template;
}

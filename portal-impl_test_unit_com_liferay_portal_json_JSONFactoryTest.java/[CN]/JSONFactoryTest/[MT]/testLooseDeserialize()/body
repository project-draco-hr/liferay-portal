{
  Object object=JSONFactoryUtil.looseDeserialize("{\"class\":\"" + EntityCacheUtil.class.getName() + "\"}");
  Assert.assertTrue(object instanceof Map);
  object=JSONFactoryUtil.looseDeserialize("{\"class\":\"java.lang.Thread\"}");
  Assert.assertTrue(object instanceof Map);
}

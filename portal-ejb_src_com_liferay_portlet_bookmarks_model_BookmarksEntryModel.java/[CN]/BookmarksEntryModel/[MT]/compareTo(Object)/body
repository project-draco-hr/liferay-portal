{
  if (obj == null) {
    return -1;
  }
  BookmarksEntry bookmarksEntry=(BookmarksEntry)obj;
  int value=0;
  value=getFolderId().compareTo(bookmarksEntry.getFolderId());
  if (value != 0) {
    return value;
  }
  value=getName().toLowerCase().compareTo(bookmarksEntry.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  if (isEscapedModel()) {
    return (SocialRelation)this;
  }
 else {
    return (SocialRelation)Proxy.newProxyInstance(SocialRelation.class.getClassLoader(),new Class[]{SocialRelation.class},new AutoEscapeBeanHandler(this));
  }
}

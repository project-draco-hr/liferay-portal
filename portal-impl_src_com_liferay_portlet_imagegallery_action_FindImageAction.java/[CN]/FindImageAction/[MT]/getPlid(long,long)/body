{
  if (plid != LayoutConstants.DEFAULT_PLID) {
    try {
      Layout layout=LayoutLocalServiceUtil.getLayout(plid);
      LayoutTypePortlet layoutTypePortlet=(LayoutTypePortlet)layout.getLayoutType();
      if (layoutTypePortlet.hasPortletId(PortletKeys.IMAGE_GALLERY)) {
        return plid;
      }
    }
 catch (    NoSuchLayoutException nsle) {
    }
  }
  IGImage image=IGImageLocalServiceUtil.getImage(imageId);
  plid=PortalUtil.getPlidFromPortletId(image.getGroupId(),PortletKeys.IMAGE_GALLERY);
  if (plid != LayoutConstants.DEFAULT_PLID) {
    return plid;
  }
 else {
    throw new NoSuchLayoutException("No page was found with the Image Gallery portlet.");
  }
}

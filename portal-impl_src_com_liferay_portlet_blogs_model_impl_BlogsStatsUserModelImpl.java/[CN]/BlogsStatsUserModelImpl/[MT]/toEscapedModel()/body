{
  if (isEscapedModel()) {
    return (BlogsStatsUser)this;
  }
 else {
    return (BlogsStatsUser)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

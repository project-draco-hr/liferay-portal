{
  if (isEscapedModel()) {
    return (BlogsStatsUser)this;
  }
 else {
    return (BlogsStatsUser)Proxy.newProxyInstance(BlogsStatsUser.class.getClassLoader(),new Class[]{BlogsStatsUser.class},new AutoEscapeBeanHandler(this));
  }
}

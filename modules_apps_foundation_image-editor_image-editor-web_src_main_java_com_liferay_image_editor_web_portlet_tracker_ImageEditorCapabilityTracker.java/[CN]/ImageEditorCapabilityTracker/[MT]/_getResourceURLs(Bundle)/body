{
  List<URL> allURs=new ArrayList<>();
  Enumeration<URL> urls=bundle.findEntries("META-INF/resources",_ES_JS_FILE_EXTENSION,true);
  if (urls != null) {
    allURs.addAll(Collections.list(urls));
  }
  return allURs;
}

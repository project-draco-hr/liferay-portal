{
  for (  ShoppingOrder shoppingOrder : shoppingOrders) {
    if (EntityCacheUtil.getResult(ShoppingOrderModelImpl.ENTITY_CACHE_ENABLED,ShoppingOrder.class,shoppingOrder.getPrimaryKey(),this) == null) {
      cacheResult(shoppingOrder);
    }
  }
}

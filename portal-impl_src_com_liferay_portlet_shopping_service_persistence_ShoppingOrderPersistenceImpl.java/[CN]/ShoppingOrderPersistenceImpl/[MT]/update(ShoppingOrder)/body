{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingOrder shoppingOrder) method. To improve performance, call update(ShoppingOrder shoppingOrder, boolean merge).");
  }
  return update(shoppingOrder,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingOrder shoppingOrder) method. Use update(ShoppingOrder shoppingOrder, boolean merge) instead.");
  }
  return update(shoppingOrder,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PollsChoice pollsChoice) method. To improve performance, call update(PollsChoice pollsChoice, boolean merge).");
  }
  return update(pollsChoice,false);
}

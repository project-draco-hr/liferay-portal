{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PollsChoice pollsChoice) method. Use update(PollsChoice pollsChoice, boolean merge) instead.");
  }
  return update(pollsChoice,false);
}

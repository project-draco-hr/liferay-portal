{
  if (event1 == null || event2 == null) {
    return 1;
  }
  if (event1.getCacheName().equals(event2.getCacheName()) && event1.getElementKey().equals(event2.getElementKey())) {
    return 0;
  }
 else {
    return -1;
  }
}

{
  CommonPermission.checkPermission(getPermissionChecker(),className,classPK,ActionKeys.UPDATE);
  return PhoneLocalServiceUtil.addPhone(getUserId(),className,classPK,number,extension,typeId,primary);
}

{
  Phone phone=PhoneUtil.findByPrimaryKey(phoneId);
  CommonPermission.checkPermission(getPermissionChecker(),phone.getClassName(),phone.getClassPK(),ActionKeys.UPDATE);
  return PhoneLocalServiceUtil.updatePhone(phoneId,number,extension,typeId,primary);
}

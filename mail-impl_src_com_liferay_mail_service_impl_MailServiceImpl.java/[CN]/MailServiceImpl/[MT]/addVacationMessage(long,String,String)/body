{
  String hookImpl=PropsUtil.get(PropsUtil.MAIL_HOOK_IMPL);
  MethodWrapper methodWrapper=new MethodWrapper(hookImpl,"addVacationMessage",new Object[]{new LongWrapper(userId),emailAddress,vacationMessage});
  MailProducer.produce(methodWrapper);
}

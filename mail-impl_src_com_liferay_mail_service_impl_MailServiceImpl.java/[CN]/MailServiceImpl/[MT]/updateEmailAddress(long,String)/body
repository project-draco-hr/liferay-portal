{
  if (_log.isDebugEnabled()) {
    _log.debug("updateEmailAddress");
  }
  String hookImpl=PropsUtil.get(PropsUtil.MAIL_HOOK_IMPL);
  MethodWrapper methodWrapper=new MethodWrapper(hookImpl,"updateEmailAddress",new Object[]{new LongWrapper(userId),emailAddress});
  MailProducer.produce(methodWrapper);
}

{
  String hookImpl=PropsUtil.get(PropsUtil.MAIL_HOOK_IMPL);
  MethodWrapper methodWrapper=new MethodWrapper(hookImpl,"addForward",new Object[]{new LongWrapper(userId),filters,emailAddresses,new BooleanWrapper(leaveCopy)});
  MailProducer.produce(methodWrapper);
}

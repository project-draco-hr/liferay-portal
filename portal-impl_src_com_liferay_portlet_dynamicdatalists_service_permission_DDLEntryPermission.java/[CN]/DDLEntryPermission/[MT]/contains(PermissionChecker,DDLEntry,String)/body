{
  if (permissionChecker.hasOwnerPermission(entry.getCompanyId(),DDLEntry.class.getName(),entry.getEntryId(),entry.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(entry.getGroupId(),DDLEntry.class.getName(),entry.getEntryId(),actionId);
}

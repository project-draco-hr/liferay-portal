{
  if ((_resourceBundle == null) || (key == null)) {
    return key;
  }
  String value=ResourceBundleUtil.getString(_resourceBundle,key);
  if (value == null) {
    return key;
  }
  return value;
}

{
  BundleExtension bundleExtension=GradleUtil.getExtension(project,BundleExtension.class);
  bundleExtension.setJarBuilderFactory(new Factory<JarBuilder>(){
    @Override public JarBuilder create(){
      return new LiferayJarBuilder();
    }
  }
);
}

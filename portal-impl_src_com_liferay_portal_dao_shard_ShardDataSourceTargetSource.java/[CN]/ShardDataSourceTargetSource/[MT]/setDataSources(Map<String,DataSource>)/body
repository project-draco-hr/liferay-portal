{
  _dataSources=dataSources;
  _availableShardNames=_dataSources.keySet().toArray(new String[0]);
  if (_log.isInfoEnabled()) {
    _log.info("Sharding configured with " + _availableShardNames.length + " data sources");
  }
}

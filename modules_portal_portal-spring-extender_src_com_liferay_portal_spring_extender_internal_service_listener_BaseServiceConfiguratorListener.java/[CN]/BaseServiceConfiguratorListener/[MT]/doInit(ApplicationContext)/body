{
  try {
    _serviceConfigurator.init();
  }
 catch (  Exception e) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to setup services in application context " + applicationContext.getDisplayName(),e);
    }
  }
}

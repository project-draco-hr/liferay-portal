{
  String key=getKey(locale,timeZone);
  Format format=_dateFormats.get(key);
  if (format == null) {
    format=FastDateFormat.getDateInstance(FastDateFormat.SHORT,timeZone,locale);
    _dateFormats.put(key,format);
  }
  return format;
}

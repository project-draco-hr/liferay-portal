{
  String key=getKey(locale,timeZone);
  Format format=_timeFormats.get(key);
  if (format == null) {
    format=FastDateFormat.getTimeInstance(FastDateFormat.SHORT,timeZone,locale);
    _timeFormats.put(key,format);
  }
  return format;
}

{
  String themeDir="frontend-themes-";
  if (theme.equals("_styled") || theme.equals("_unstyled")) {
    themeDir+=theme.substring(1);
  }
 else {
    themeDir+=theme + "-web";
  }
  return new File(getFrontendThemesDir(),themeDir + "/src/META-INF/resources");
}

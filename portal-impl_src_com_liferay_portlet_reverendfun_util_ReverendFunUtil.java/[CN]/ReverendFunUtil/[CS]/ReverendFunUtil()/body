{
  _dates=new ArrayList<String>();
  String[] dates=StringUtil.split(ContentUtil.get("com/liferay/portlet/reverendfun/dependencies/dates.txt"),"\n");
  for (int i=0; i < dates.length; i++) {
    _dates.add(dates[i]);
  }
}

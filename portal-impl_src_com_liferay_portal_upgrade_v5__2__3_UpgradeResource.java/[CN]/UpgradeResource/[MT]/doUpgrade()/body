{
  if (isSupportsAlterColumnType()) {
    runSQL("alter_column_type Resource_ primKey VARCHAR(255) null");
  }
 else {
    upgradeTable(ResourceTable.TABLE_NAME,ResourceTable.TABLE_COLUMNS,ResourceTable.TABLE_SQL_CREATE,ResourceTable.TABLE_SQL_ADD_INDEXES);
  }
}

{
  User user=null;
  try {
    user=UserLocalServiceUtil.getUserByScreenName(node.getCompanyId(),author);
  }
 catch (  NoSuchUserException e) {
    user=UserLocalServiceUtil.getUserById(userId);
  }
  return user.getUserId();
}

{
  Settings settings=_settingsFactory.getSettings(new GroupServiceSettingsLocator(groupId,_getSettingsId()));
  try {
    return _getSettings(settings);
  }
 catch (  Exception e) {
    throw new SettingsException("Unable to create proxy for group service settings",e);
  }
}

{
  TrashEntry entry1=(TrashEntry)obj1;
  TrashEntry entry2=(TrashEntry)obj2;
  int value=StringUtil.toLowerCase(entry1.getUserName()).compareTo(StringUtil.toLowerCase(entry2.getUserName()));
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

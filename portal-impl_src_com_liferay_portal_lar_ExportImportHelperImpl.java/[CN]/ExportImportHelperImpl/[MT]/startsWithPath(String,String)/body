{
  if ((url == null) || (path == null)) {
    return false;
  }
  if (url.startsWith(path) && (((url.length() > path.length()) && (url.charAt(url.indexOf(path)) == CharPool.SLASH)) || (url.length() == path.length()))) {
    return true;
  }
  return false;
}

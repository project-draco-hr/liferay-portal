{
  if ((url == null) || (path == null)) {
    return false;
  }
  if (url.startsWith(path) && ((url.length() == path.length()) || (url.charAt(path.length()) == CharPool.SLASH))) {
    return true;
  }
  return false;
}

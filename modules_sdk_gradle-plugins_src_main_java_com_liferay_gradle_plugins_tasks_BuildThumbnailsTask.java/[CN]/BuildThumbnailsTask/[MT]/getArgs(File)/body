{
  List<String> args=getArgs();
  args.add("thumbnail.original.file=" + FileUtil.getAbsolutePath(screenshotFile));
  File thumbnailFile=new File(screenshotFile.getParentFile(),"thumbnail.png");
  args.add("thumbnail.thumbnail.file=" + FileUtil.getAbsolutePath(thumbnailFile));
  return args;
}

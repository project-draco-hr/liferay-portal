{
  ActionHandler oldActionHandler=_deviceActionHandlers.put(actionHandler.getType(),actionHandler);
  if (oldActionHandler != null) {
    if (_log.isWarnEnabled()) {
      _log.warn("ActionHandler with key: " + actionHandler.getType() + " has already been registered. Replacing with "+ actionHandler);
    }
  }
}

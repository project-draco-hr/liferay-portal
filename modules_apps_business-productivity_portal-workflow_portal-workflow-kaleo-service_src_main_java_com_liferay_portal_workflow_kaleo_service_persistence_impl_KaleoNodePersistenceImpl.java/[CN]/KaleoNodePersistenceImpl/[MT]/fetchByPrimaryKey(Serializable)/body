{
  KaleoNode kaleoNode=(KaleoNode)entityCache.getResult(KaleoNodeModelImpl.ENTITY_CACHE_ENABLED,KaleoNodeImpl.class,primaryKey);
  if (kaleoNode == _nullKaleoNode) {
    return null;
  }
  if (kaleoNode == null) {
    Session session=null;
    try {
      session=openSession();
      kaleoNode=(KaleoNode)session.get(KaleoNodeImpl.class,primaryKey);
      if (kaleoNode != null) {
        cacheResult(kaleoNode);
      }
 else {
        entityCache.putResult(KaleoNodeModelImpl.ENTITY_CACHE_ENABLED,KaleoNodeImpl.class,primaryKey,_nullKaleoNode);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(KaleoNodeModelImpl.ENTITY_CACHE_ENABLED,KaleoNodeImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return kaleoNode;
}

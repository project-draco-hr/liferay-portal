{
  Object value=null;
  if (name.startsWith(StrutsUtil.STRUTS_PACKAGE) && _strutsAttributes.containsKey(name)) {
    value=_strutsAttributes.get(name);
  }
 else {
    value=super.getAttribute(name);
  }
  return value;
}

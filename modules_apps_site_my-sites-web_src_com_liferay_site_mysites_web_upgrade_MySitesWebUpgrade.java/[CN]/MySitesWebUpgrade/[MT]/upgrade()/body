{
  UpgradePortletId upgradePortletId=new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"29",MySitesPortletKeys.MY_SITES}};
    }
  }
;
  _releaseLocalService.updateRelease("com.liferay.site.mysites",Collections.<UpgradeProcess>singletonList(upgradePortletId),1,1,false);
}

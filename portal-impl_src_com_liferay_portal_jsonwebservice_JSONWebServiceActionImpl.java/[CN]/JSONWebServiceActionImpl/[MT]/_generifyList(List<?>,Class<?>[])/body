{
  if (types == null) {
    return list;
  }
  if (types.length != 1) {
    return list;
  }
  List newList=new ArrayList(list.size());
  for (  Object entry : list) {
    if (entry != null) {
      entry=ReflectUtil.castType(entry,types[0]);
    }
    newList.add(entry);
  }
  return newList;
}

{
  if (permissionChecker.isGroupAdmin(groupId) || permissionChecker.isGroupOwner(groupId)) {
    return true;
  }
  if (permissionChecker.hasPermission(groupId,getPortletId(),0,actionId)) {
    return true;
  }
  return false;
}

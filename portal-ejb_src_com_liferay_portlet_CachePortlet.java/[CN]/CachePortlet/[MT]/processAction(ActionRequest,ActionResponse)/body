{
  long start=0;
  if (_log.isDebugEnabled()) {
    start=System.currentTimeMillis();
  }
  _invoke(req,res,true);
  if (_log.isDebugEnabled()) {
    long end=System.currentTimeMillis();
    _log.debug("processAction for " + _portletId + " takes "+ (end - start)+ " ms");
  }
}

{
  List<JournalStructure> structures=journalStructurePersistence.findByGroupId(groupId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StructurePKComparator());
  for (  JournalStructure structure : structures) {
    deleteStructure(structure);
  }
}

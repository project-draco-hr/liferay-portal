{
  if (user != null) {
    String userID=user.getUserID();
    if (userID != null) {
      if (!users.containsKey(userID)) {
        users.put(userID,user);
        return null;
      }
    }
  }
  return user;
}

{
  String[] requiredFieldNames=StringUtil.split(LanguageUtil.get(locale,"lang.user.name.required.field.names"));
  if (!ArrayUtil.contains(requiredFieldNames,"first-name")) {
    requiredFieldNames=ArrayUtil.append(new String[]{"first-name"},requiredFieldNames);
  }
  return requiredFieldNames;
}

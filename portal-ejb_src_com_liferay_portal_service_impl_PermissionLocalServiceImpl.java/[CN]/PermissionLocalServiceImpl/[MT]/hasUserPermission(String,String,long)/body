{
  Permission permission=null;
  try {
    permission=PermissionUtil.findByA_R(actionId,resourceId);
  }
 catch (  NoSuchPermissionException nspe) {
    return false;
  }
  return UserUtil.containsPermission(userId,permission.getPermissionId());
}

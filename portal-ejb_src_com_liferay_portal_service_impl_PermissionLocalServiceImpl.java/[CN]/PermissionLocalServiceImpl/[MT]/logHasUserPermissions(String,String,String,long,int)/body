{
  if (!_log.isDebugEnabled()) {
    return 0;
  }
  long end=System.currentTimeMillis();
  _log.debug("Checking user permissions block " + block + " for "+ userId+ " "+ actionId+ " "+ resourceId+ " takes "+ (end - start)+ " ms");
  return end;
}

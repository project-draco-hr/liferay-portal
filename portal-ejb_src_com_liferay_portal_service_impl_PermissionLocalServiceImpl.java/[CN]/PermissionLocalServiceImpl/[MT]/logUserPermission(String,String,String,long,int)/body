{
  if (!_log.isDebugEnabled()) {
    return;
  }
  long end=System.currentTimeMillis();
  _log.debug("Checking user permission block " + block + " for "+ userId+ " "+ actionId+ " "+ resourceId+ " takes "+ (end - start)+ " ms");
}

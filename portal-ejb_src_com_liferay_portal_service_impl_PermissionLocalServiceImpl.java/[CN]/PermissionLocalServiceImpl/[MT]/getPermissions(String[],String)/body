{
  List permissions=new ArrayList();
  for (int i=0; i < actionIds.length; i++) {
    Permission permission=PermissionUtil.findByA_R(actionIds[i],resourceId);
    permissions.add(permission);
  }
  return permissions;
}

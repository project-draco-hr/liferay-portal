{
  Assert.assertTrue(json.contains("\"doubleArray\":" + DOUBLE_ARRAY_STRING));
  Assert.assertTrue(json.contains("\"longArray\":" + LONG_ARRAY_STRING));
  Assert.assertTrue(json.contains("\"integerArray\":" + INTEGER_ARRAY_STRING));
}

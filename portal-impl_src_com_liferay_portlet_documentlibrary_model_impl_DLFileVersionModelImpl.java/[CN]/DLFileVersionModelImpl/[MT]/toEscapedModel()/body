{
  if (isEscapedModel()) {
    return (DLFileVersion)this;
  }
 else {
    return (DLFileVersion)Proxy.newProxyInstance(DLFileVersion.class.getClassLoader(),new Class[]{DLFileVersion.class},new AutoEscapeBeanHandler(this));
  }
}

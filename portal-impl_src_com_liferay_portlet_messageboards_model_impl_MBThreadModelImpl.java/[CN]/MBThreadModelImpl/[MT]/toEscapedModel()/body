{
  if (isEscapedModel()) {
    return (MBThread)this;
  }
 else {
    return (MBThread)Proxy.newProxyInstance(MBThread.class.getClassLoader(),new Class[]{MBThread.class},new AutoEscapeBeanHandler(this));
  }
}

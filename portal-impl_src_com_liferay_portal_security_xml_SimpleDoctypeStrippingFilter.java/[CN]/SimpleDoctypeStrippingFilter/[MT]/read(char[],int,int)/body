{
  int read;
  for (read=0; read < len; read++) {
    int c=read();
    if (c == -1) {
      if (read == 0) {
        return -1;
      }
      return read;
    }
    cbuf[off + read]=(char)c;
  }
  return read;
}

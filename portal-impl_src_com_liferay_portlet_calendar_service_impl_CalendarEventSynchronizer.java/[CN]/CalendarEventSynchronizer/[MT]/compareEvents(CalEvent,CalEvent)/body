{
  boolean status=true;
  if (remoteEvent == null) {
    status=false;
  }
 else   if (!(localEvent.getStartDate().compareTo(remoteEvent.getStartDate()) == 0)) {
    status=false;
  }
  return status;
}

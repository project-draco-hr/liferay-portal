{
  if (isEscapedModel()) {
    return (IGFolder)this;
  }
 else {
    return (IGFolder)Proxy.newProxyInstance(IGFolder.class.getClassLoader(),new Class[]{IGFolder.class},new AutoEscapeBeanHandler(this));
  }
}

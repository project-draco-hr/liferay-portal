{
  if (_initables != null) {
    Iterator itr=_initables.iterator();
    while (itr.hasNext()) {
      PortalInitable initable=(PortalInitable)itr.next();
      initable.portalInit();
      itr.remove();
    }
    _initables=null;
  }
}

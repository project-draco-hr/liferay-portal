{
  AssetEntryValidator assetEntryValidator=_serviceTrackerMap.getService(className);
  if (assetEntryValidator == null) {
    super.validate(groupId,className,classTypePK,categoryIds,entryNames);
  }
 else {
    assetEntryValidator.validate(groupId,className,classTypePK,categoryIds,entryNames);
  }
}

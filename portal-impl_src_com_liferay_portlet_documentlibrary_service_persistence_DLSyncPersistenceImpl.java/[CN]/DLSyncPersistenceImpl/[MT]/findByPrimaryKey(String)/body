{
  DLSync dlSync=fetchByPrimaryKey(fileId);
  if (dlSync == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + fileId);
    }
    throw new NoSuchSyncException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + fileId);
  }
  return dlSync;
}

{
  DLSync dlSync=(DLSync)EntityCacheUtil.getResult(DLSyncModelImpl.ENTITY_CACHE_ENABLED,DLSyncImpl.class,primaryKey);
  if (dlSync == _nullDLSync) {
    return null;
  }
  if (dlSync == null) {
    Session session=null;
    try {
      session=openSession();
      dlSync=(DLSync)session.get(DLSyncImpl.class,primaryKey);
      if (dlSync != null) {
        cacheResult(dlSync);
      }
 else {
        EntityCacheUtil.putResult(DLSyncModelImpl.ENTITY_CACHE_ENABLED,DLSyncImpl.class,primaryKey,_nullDLSync);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(DLSyncModelImpl.ENTITY_CACHE_ENABLED,DLSyncImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return dlSync;
}

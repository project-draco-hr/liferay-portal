{
  try (ClassLoaderSetter classLoaderSetter=new ClassLoaderSetter(BaseCMISRepositoryFactory.class.getClassLoader())){
    return new RepositoryProxyBean(createBaseRepository(repositoryId),BaseCMISRepositoryFactory.class.getClassLoader());
  }
 }

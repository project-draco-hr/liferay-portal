{
  PortletURL portletURL=PortletProviderUtil.getPortletURL(liferayPortletRequest,DDLRecord.class.getName(),PortletProvider.Action.VIEW);
  portletURL.setParameter("recordId",String.valueOf(_record.getRecordId()));
  return portletURL.toString();
}

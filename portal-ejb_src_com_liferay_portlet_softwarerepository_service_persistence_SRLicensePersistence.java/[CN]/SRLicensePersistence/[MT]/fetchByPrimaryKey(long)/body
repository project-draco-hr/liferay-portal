{
  Session session=null;
  try {
    session=openSession();
    return (SRLicense)session.get(SRLicenseImpl.class,new Long(licenseId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

{
  File propsFile=new File(srcFile + "/WEB-INF/liferay-plugin-package.properties");
  if (!propsFile.exists()) {
    return null;
  }
  String propsString=FileUtil.read(propsFile);
  return PropertiesUtil.load(propsString);
}

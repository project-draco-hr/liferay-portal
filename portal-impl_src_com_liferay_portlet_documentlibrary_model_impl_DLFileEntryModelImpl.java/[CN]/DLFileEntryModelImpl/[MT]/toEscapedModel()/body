{
  if (isEscapedModel()) {
    return (DLFileEntry)this;
  }
 else {
    return (DLFileEntry)Proxy.newProxyInstance(DLFileEntry.class.getClassLoader(),new Class[]{DLFileEntry.class},new AutoEscapeBeanHandler(this));
  }
}

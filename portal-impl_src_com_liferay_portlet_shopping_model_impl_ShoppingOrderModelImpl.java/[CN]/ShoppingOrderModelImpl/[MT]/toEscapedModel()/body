{
  if (isEscapedModel()) {
    return (ShoppingOrder)this;
  }
 else {
    return (ShoppingOrder)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

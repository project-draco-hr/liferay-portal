{
  if (isEscapedModel()) {
    return (ShoppingOrder)this;
  }
 else {
    return (ShoppingOrder)Proxy.newProxyInstance(ShoppingOrder.class.getClassLoader(),new Class[]{ShoppingOrder.class},new AutoEscapeBeanHandler(this));
  }
}

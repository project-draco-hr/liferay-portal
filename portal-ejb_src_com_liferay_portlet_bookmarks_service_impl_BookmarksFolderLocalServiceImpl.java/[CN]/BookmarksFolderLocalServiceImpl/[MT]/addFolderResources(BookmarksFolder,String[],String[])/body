{
  ResourceLocalServiceUtil.addModelResources(folder.getCompanyId(),folder.getGroupId(),folder.getUserId(),BookmarksFolder.class.getName(),String.valueOf(folder.getPrimaryKey()),communityPermissions,guestPermissions);
}

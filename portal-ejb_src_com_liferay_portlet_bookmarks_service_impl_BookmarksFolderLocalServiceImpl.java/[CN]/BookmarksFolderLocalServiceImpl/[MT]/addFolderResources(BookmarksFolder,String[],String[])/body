{
  ResourceLocalServiceUtil.addModelResources(folder.getCompanyId(),folder.getGroupId(),folder.getUserId(),BookmarksFolder.class.getName(),folder.getPrimaryKey(),communityPermissions,guestPermissions);
}

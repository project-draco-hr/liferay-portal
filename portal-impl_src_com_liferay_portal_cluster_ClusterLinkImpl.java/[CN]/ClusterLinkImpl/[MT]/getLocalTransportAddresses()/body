{
  if (!isEnabled()) {
    return Collections.emptyList();
  }
  List<Address> addresses=new ArrayList<>(_localTransportAddresses.size());
  for (  org.jgroups.Address address : _localTransportAddresses) {
    addresses.add(new AddressImpl(address));
  }
  return addresses;
}

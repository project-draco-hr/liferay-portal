{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof SPIKey)) {
    return false;
  }
  SPIKey clusterNode=(SPIKey)obj;
  if (Validator.equals(_spiProviderName,clusterNode._spiProviderName) && Validator.equals(_spiId,clusterNode._spiId)) {
    return true;
  }
  return false;
}

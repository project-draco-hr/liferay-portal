{
  Document document=new DocumentImpl();
  document.addUID(CLASS_NAME,journalFolder.getFolderId());
  SearchEngineUtil.deleteDocument(getSearchEngineId(),journalFolder.getCompanyId(),document.get(Field.UID),isCommitImmediately());
}

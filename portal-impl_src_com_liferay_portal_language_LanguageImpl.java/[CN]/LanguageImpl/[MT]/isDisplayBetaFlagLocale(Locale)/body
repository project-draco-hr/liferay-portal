{
  if (!isBetaLocale(locale)) {
    return false;
  }
  String languageId=LocaleUtil.toLanguageId(locale);
  for (  String curLanguageId : PropsValues.LOCALES_BETA_HIDE_FLAG) {
    if (languageId.equals(curLanguageId)) {
      return false;
    }
  }
  return true;
}

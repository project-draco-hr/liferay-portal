{
  try {
    return _get(null,null,resourceBundle,resourceBundle.getLocale(),key,defaultValue);
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn(e,e);
    }
    return defaultValue;
  }
}

{
  try {
    String value=_get(resourceBundle,key,defaultValue);
    if (value != null) {
      return value;
    }
 else {
      return defaultValue;
    }
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn(e,e);
    }
    return defaultValue;
  }
}

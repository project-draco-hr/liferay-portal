{
  try {
    String value=_get(resourceBundle,key);
    if (value != null) {
      return value;
    }
  }
 catch (  MissingResourceException mre) {
    if (_log.isWarnEnabled()) {
      _log.warn(mre,mre);
    }
  }
  return defaultValue;
}

{
  try {
    ResourceBundle resourceBundle=_getResourceBundleFromPageContext(pageContext);
    return _get(resourceBundle,key,defaultValue);
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn(e,e);
    }
    return defaultValue;
  }
}

{
  SingleDestinationMessageSender messageSender=baseServiceProxy.getSingleDestinationMessageSender();
  if (messageSender == null) {
    throw new IllegalStateException("Asynchronous message sender was not configured " + "properly in: " + baseServiceProxy.getClass().getName());
  }
  messageSender.send(serviceRequest);
}

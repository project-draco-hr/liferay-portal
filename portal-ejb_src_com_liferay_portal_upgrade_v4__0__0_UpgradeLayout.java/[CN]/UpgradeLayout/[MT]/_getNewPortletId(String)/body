{
  for (int i=0; i < _PORTLET_IDS.length; i++) {
    if (oldPortletId.startsWith(_PORTLET_IDS[i][0])) {
      oldPortletId=StringUtil.replace(oldPortletId,_PORTLET_IDS[i][0],_PORTLET_IDS[i][1]);
      break;
    }
  }
  return oldPortletId;
}

{
  ObjectValuePair<StringBundler,Integer> ovp=new ObjectValuePair<>(sb,sb.index());
  int index=_sbIndexList.indexOf(ovp);
  if (index == -1) {
    _sbIndexList.add(ovp);
  }
 else {
    ovp=_sbIndexList.get(index);
    ovp.setValue(sb.index());
  }
}

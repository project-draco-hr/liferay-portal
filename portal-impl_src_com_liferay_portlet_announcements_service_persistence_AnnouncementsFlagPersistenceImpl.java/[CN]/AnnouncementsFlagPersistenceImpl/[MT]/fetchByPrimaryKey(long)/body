{
  Session session=null;
  try {
    session=openSession();
    return (AnnouncementsFlag)session.get(AnnouncementsFlagImpl.class,new Long(flagId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

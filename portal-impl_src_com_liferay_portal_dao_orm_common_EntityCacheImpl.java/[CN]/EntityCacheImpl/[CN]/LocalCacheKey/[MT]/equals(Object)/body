{
  LocalCacheKey localCacheKey=(LocalCacheKey)obj;
  if (Validator.equals(_className,localCacheKey._className) && Validator.equals(_primaryKey,localCacheKey._primaryKey)) {
    return true;
  }
  return false;
}

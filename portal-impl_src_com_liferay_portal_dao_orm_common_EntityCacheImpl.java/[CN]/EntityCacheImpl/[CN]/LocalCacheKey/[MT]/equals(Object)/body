{
  LocalCacheKey localCacheKey=(LocalCacheKey)obj;
  if (localCacheKey._shardName.equals(_shardName) && localCacheKey._className.equals(_className) && localCacheKey._primaryKey.equals(_primaryKey)) {
    return true;
  }
 else {
    return false;
  }
}

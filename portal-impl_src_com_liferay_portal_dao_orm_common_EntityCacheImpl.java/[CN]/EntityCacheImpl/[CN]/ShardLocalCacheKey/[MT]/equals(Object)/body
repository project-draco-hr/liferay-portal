{
  ShardLocalCacheKey shardLocalCacheKey=(ShardLocalCacheKey)obj;
  if (shardLocalCacheKey._shardName.equals(_shardName) && shardLocalCacheKey._className.equals(_className) && shardLocalCacheKey._primaryKey.equals(_primaryKey)) {
    return true;
  }
  return false;
}

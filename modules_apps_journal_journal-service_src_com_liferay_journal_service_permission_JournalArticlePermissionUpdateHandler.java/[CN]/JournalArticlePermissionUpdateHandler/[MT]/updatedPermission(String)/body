{
  JournalArticle article=_journalArticleLocalService.fetchLatestArticle(GetterUtil.getLong(primKey));
  if (article == null) {
    return;
  }
  article.setModifiedDate(new Date());
  _journalArticleLocalService.updateJournalArticle(article);
}

{
  JournalArticle article=_journalArticleLocalService.fetchLatestArticle(Long.valueOf(primKey));
  article.setModifiedDate(new Date());
  _journalArticleLocalService.updateJournalArticle(article);
}

{
  if (fileSize < threshold) {
    initData();
    if (index < data.length) {
      return data[index++] & 0xff;
    }
 else {
      return -1;
    }
  }
 else {
    initFileInputStream();
    return fileInputStream.read();
  }
}

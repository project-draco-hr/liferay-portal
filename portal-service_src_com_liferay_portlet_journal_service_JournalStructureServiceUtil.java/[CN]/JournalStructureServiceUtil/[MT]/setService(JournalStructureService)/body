{
  MethodCache.remove(JournalStructureService.class);
  _service=service;
  ReferenceRegistry.registerReference(JournalStructureServiceUtil.class,"_service");
  MethodCache.remove(JournalStructureService.class);
}

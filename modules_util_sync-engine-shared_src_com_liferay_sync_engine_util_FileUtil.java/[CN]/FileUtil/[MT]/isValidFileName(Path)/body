{
  String fileName=String.valueOf(filePath.getFileName());
  if ((Files.isDirectory(filePath) && (fileName.length() > 100)) || (!Files.isDirectory(filePath) && (fileName.length() > 255))) {
    return false;
  }
  return isValidFileName(fileName);
}

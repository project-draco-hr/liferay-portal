{
  List contentSearches=new ArrayList();
  for (int i=0; i < articleIds.length; i++) {
    JournalContentSearch contentSearch=updateContentSearch(companyId,groupId,privateLayout,layoutId,portletId,articleIds[i]);
    contentSearches.add(contentSearch);
  }
  return contentSearches;
}

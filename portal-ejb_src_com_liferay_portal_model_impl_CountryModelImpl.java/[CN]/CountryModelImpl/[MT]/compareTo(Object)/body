{
  if (obj == null) {
    return -1;
  }
  CountryImpl country=(CountryImpl)obj;
  int value=0;
  value=getName().compareTo(country.getName());
  if (value != 0) {
    return value;
  }
  return 0;
}

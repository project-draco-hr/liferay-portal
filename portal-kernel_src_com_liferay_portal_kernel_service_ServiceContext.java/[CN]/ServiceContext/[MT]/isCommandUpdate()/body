{
  if (Validator.equals(_command,Constants.UPDATE) || Validator.equals(_command,Constants.UPDATE_AND_CHECKIN) || Validator.equals(_command,Constants.UPDATE_WEBDAV)) {
    return true;
  }
 else {
    return false;
  }
}

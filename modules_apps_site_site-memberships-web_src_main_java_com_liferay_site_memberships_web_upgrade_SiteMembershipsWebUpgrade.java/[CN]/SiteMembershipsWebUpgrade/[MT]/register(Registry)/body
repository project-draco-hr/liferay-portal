{
  registry.register("com.liferay.site.memberships.web","0.0.1","1.0.0",new UpgradePortletId(){
    @Override protected String[][] getRenamePortletIdsArray(){
      return new String[][]{new String[]{"174",SiteMembershipsPortletKeys.SITE_MEMBERSHIPS_ADMIN}};
    }
  }
);
}

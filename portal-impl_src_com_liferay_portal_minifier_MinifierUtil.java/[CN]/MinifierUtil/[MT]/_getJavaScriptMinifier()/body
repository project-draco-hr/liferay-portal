{
  try {
    return (JavaScriptMinifier)InstanceFactory.newInstance(PropsValues.MINIFIER_JAVASCRIPT_IMPL);
  }
 catch (  Exception e) {
    _log.error("Unable to instantiate " + PropsValues.MINIFIER_JAVASCRIPT_IMPL);
    return new GoogleJavaScriptMinifier();
  }
}

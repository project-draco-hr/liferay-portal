{
  try {
    return (JavaScriptMinifier)InstanceFactory.newInstance(PropsValues.MINIFIER_JAVASCRIPT);
  }
 catch (  Exception e) {
    _log.error("Unable to instantiate " + PropsValues.MINIFIER_JAVASCRIPT);
    return new GoogleJavaScriptMinifier();
  }
}

{
  Session session=null;
  try {
    session=openSession();
    return (ExpandoTableRow)session.get(ExpandoTableRowImpl.class,new Long(rowId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

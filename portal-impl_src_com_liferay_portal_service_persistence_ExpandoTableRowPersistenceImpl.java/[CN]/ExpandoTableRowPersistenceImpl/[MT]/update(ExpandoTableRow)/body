{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoTableRow expandoTableRow) method. Use update(ExpandoTableRow expandoTableRow, boolean merge) instead.");
  }
  return update(expandoTableRow,false);
}

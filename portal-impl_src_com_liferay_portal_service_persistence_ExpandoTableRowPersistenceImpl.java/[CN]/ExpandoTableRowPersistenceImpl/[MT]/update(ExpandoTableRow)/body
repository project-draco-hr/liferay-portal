{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoTableRow expandoTableRow) method. To improve performance, call update(ExpandoTableRow expandoTableRow, boolean merge).");
  }
  return update(expandoTableRow,false);
}

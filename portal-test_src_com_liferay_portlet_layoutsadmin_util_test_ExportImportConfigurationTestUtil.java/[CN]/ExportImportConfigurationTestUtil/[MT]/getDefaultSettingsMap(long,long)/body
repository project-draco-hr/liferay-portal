{
  User user=UserLocalServiceUtil.getUser(userId);
  return ExportImportConfigurationSettingsMapFactory.buildSettingsMap(userId,groupId,groupId,false,null,null,user.getLocale(),user.getTimeZone());
}

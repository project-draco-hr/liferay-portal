{
  String path=PropsUtil.get(PropsUtil.DEFAULT_LANDING_PAGE_PATH);
  LastPath lastPath=new LastPath(StringPool.BLANK,path,new HashMap());
  HttpSession ses=req.getSession();
  ses.setAttribute(WebKeys.LAST_PATH,lastPath);
}

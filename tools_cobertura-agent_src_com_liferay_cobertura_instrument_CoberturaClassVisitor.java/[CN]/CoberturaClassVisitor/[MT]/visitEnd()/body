{
  if (_instrument && (_classData.getNumberOfValidLines() == 0)) {
    System.err.println("No line number information found for class " + _classData.getName() + ". Please recompile with debug info.");
  }
}

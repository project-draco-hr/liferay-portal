{
  AWSCredentials credentials=getAWSCredentials();
  Jets3tProperties jets3tProperties=getJets3tProperties();
  return new RestS3Service(credentials,ReleaseInfo.getServerInfo(),null,jets3tProperties);
}

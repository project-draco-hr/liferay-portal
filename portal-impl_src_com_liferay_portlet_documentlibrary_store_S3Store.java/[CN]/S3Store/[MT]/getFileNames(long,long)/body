{
  try {
    S3Object[] s3Objects=_s3Service.listObjects(_s3Bucket.getName(),getKey(companyId,repositoryId),null);
    return getFileNames(s3Objects);
  }
 catch (  S3ServiceException s3se) {
    throw new SystemException(s3se);
  }
}

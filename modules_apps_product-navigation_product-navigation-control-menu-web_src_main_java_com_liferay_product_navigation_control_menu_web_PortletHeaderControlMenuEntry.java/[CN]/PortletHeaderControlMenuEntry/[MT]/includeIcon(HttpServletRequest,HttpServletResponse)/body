{
  ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
  PortletDisplay portletDisplay=themeDisplay.getPortletDisplay();
  request.setAttribute(ControlMenuWebKeys.PORTLET_DESCRIPTION,portletDisplay.getDescription());
  request.setAttribute(ControlMenuWebKeys.PORTLET_TITLE,portletDisplay.getTitle());
  return super.includeIcon(request,response);
}

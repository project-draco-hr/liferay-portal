{
  if (notificationEvent1.equals(notificationEvent2)) {
    return 0;
  }
  long deliverBy1=notificationEvent1.getDeliverBy();
  long deliverBy2=notificationEvent2.getDeliverBy();
  int value=0;
  value=(int)(deliverBy1 - deliverBy2);
  if (value == 0) {
    value=(int)(notificationEvent1.getTimestamp() - notificationEvent2.getTimestamp());
    if (value == 0) {
      value=notificationEvent1.hashCode() - notificationEvent2.hashCode();
    }
  }
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

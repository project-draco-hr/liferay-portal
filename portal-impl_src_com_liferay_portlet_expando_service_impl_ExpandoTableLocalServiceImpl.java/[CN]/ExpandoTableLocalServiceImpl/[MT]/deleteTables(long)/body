{
  long companyId=CompanyThreadLocal.getCompanyId();
  List<ExpandoTable> tables=expandoTablePersistence.findByC_C(companyId,classNameId);
  for (  ExpandoTable table : tables) {
    deleteTable(table.getTableId());
  }
}

{
  String fileName=RandomTestUtil.randomString();
  store.addFile(companyId,repositoryId,fileName,_DATA_VERSION_1);
  String versionLabel="1.";
  for (int i=1; i <= newVersionCount; i++) {
    store.updateFile(companyId,repositoryId,fileName,versionLabel + i,_DATA_VERSION_1);
  }
  Assert.assertTrue(store.hasFile(companyId,repositoryId,fileName,Store.VERSION_DEFAULT));
  for (int i=1; i <= newVersionCount; i++) {
    Assert.assertTrue(store.hasFile(companyId,repositoryId,fileName,versionLabel + i));
  }
  return fileName;
}

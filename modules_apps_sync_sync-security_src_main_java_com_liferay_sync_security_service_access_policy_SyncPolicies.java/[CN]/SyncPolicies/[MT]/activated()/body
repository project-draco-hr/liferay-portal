{
  for (  Company company : _companyLocalService.getCompanies()) {
    try {
      create(company.getCompanyId());
    }
 catch (    PortalException pe) {
      throw new Exception("Unable to add default SAP entry for company " + company.getCompanyId(),pe);
    }
  }
}

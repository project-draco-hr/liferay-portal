{
  if (isEscapedModel()) {
    return (Region)this;
  }
 else {
    return (Region)Proxy.newProxyInstance(Region.class.getClassLoader(),new Class[]{Region.class},new AutoEscapeBeanHandler(this));
  }
}

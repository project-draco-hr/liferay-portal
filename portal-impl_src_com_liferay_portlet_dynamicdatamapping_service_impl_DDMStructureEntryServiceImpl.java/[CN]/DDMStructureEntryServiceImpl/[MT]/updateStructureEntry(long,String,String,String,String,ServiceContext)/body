{
  DDMStructureEntryPermission.check(getPermissionChecker(),groupId,structureKey,ActionKeys.UPDATE);
  return ddmStructureEntryLocalService.updateStructureEntry(groupId,structureKey,name,description,xsd,serviceContext);
}

{
  Phone phone=(Phone)entityCache.getResult(PhoneModelImpl.ENTITY_CACHE_ENABLED,PhoneImpl.class,primaryKey);
  if (phone == _nullPhone) {
    return null;
  }
  if (phone == null) {
    Session session=null;
    try {
      session=openSession();
      phone=(Phone)session.get(PhoneImpl.class,primaryKey);
      if (phone != null) {
        cacheResult(phone);
      }
 else {
        entityCache.putResult(PhoneModelImpl.ENTITY_CACHE_ENABLED,PhoneImpl.class,primaryKey,_nullPhone);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(PhoneModelImpl.ENTITY_CACHE_ENABLED,PhoneImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return phone;
}

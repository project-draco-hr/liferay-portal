{
  Phone phone=findByPrimaryKey(phoneId);
  Session session=null;
  try {
    session=openSession();
    Phone[] array=new PhoneImpl[3];
    array[0]=getByUuid_PrevAndNext(session,phone,uuid,orderByComparator,true);
    array[1]=phone;
    array[2]=getByUuid_PrevAndNext(session,phone,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

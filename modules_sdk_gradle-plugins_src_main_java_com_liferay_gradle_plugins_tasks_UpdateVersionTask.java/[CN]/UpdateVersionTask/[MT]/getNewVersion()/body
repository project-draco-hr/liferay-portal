{
  VersionNumber versionNumber=VersionNumber.parse(getVersion());
  VersionNumber newVersionNumber=new VersionNumber(versionNumber.getMajor() + getMacroIncrement(),versionNumber.getMinor() + getMinorIncrement(),versionNumber.getMicro() + getMicroIncrement(),versionNumber.getQualifier());
  return newVersionNumber.toString();
}

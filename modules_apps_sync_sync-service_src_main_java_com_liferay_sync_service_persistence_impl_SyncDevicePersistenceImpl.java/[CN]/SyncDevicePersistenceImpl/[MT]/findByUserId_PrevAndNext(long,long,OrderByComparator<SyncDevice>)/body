{
  SyncDevice syncDevice=findByPrimaryKey(syncDeviceId);
  Session session=null;
  try {
    session=openSession();
    SyncDevice[] array=new SyncDeviceImpl[3];
    array[0]=getByUserId_PrevAndNext(session,syncDevice,userId,orderByComparator,true);
    array[1]=syncDevice;
    array[2]=getByUserId_PrevAndNext(session,syncDevice,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

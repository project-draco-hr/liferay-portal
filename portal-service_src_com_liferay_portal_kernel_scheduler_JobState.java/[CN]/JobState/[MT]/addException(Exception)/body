{
  if (_exceptions == null) {
    _exceptions=new LinkedList<ObjectValuePair<Exception,Date>>();
  }
  _exceptions.add(new ObjectValuePair<Exception,Date>(exception,new Date()));
  while (_exceptions.size() > _exceptionsMaxSize) {
    _exceptions.poll();
  }
}

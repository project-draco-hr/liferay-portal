{
  if (_portlets == null) {
    _portlets=PortletLocalServiceUtil.getPortlets();
  }
  try {
    _portletDescriptionHolder=PortletDescriptorHolderFactory.getPortletDescriptorHolder();
  }
 catch (  Exception ex) {
    throw new ProducerException(ex);
  }
}

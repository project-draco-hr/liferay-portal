{
  try {
    _storageEngine.update(_dlFileEntryMetadata.getDDMStorageId(),_fields,new ServiceContext());
  }
 catch (  StorageException se) {
    throw new SystemException("Unable to update DDM fields for file version " + _dlFileVersion.getFileVersionId(),se);
  }
}

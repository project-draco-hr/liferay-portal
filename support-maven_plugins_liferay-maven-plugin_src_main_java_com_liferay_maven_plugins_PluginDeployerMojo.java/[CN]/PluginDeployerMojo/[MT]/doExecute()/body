{
  if (warFile.exists()) {
    getLog().info("Deploying " + warFileName + " to "+ autoDeployDir.getAbsolutePath());
    FileUtils.copyFile(warFile,new File(autoDeployDir,warFileName));
  }
 else {
    getLog().warn(warFileName + " does not exist and won't be deployed.");
  }
}

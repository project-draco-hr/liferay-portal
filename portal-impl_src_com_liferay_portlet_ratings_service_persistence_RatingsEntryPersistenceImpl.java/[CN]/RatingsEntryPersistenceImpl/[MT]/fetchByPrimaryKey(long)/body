{
  Session session=null;
  try {
    session=openSession();
    return (RatingsEntry)session.get(RatingsEntryImpl.class,new Long(entryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

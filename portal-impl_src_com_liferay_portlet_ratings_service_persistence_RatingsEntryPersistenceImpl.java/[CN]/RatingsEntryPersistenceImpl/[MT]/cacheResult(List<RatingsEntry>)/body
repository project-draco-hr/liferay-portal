{
  for (  RatingsEntry ratingsEntry : ratingsEntries) {
    if (EntityCacheUtil.getResult(RatingsEntryModelImpl.ENTITY_CACHE_ENABLED,RatingsEntry.class,ratingsEntry.getPrimaryKey(),this) == null) {
      cacheResult(ratingsEntry);
    }
  }
}

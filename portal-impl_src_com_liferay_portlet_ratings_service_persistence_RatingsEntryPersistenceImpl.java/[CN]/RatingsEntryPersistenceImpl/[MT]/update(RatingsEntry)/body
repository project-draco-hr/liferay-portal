{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(RatingsEntry ratingsEntry) method. To improve performance, call update(RatingsEntry ratingsEntry, boolean merge).");
  }
  return update(ratingsEntry,false);
}

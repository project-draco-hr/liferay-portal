{
  if ((typeId > 0) && !DLFileEntryTypePermission.contains(permissionChecker,typeId,ActionKeys.VIEW)) {
    return false;
  }
  return DLPermission.contains(permissionChecker,groupId,ActionKeys.ADD_DOCUMENT);
}

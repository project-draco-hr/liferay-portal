{
  BookmarksFolderPermission.check(getPermissionChecker(),folderId,ActionKeys.ADD_ENTRY);
  return bookmarksEntryLocalService.addEntry(getUserId(),folderId,name,url,comments,tagsEntries,communityPermissions,guestPermissions);
}

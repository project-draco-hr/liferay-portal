{
  SCProductEntry productEntry1=(SCProductEntry)obj1;
  SCProductEntry productEntry2=(SCProductEntry)obj2;
  int value=productEntry1.getName().toLowerCase().compareTo(productEntry2.getName().toLowerCase());
  if (_asc) {
    return value;
  }
 else {
    return -value;
  }
}

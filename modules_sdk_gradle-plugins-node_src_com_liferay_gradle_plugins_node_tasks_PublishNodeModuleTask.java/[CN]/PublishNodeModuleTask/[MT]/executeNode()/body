{
  setArgs(getCompleteArgs());
  File npmrcFile=null;
  try {
    if (isSyncVersion()) {
      syncVersion();
    }
    npmrcFile=createNpmrcFile();
    super.executeNode();
  }
 catch (  IOException ioe) {
  }
 finally {
    if (npmrcFile != null) {
      npmrcFile.delete();
    }
  }
}

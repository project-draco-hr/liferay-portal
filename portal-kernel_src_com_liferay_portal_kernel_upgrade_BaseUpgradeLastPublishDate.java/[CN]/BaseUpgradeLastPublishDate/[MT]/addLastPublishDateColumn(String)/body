{
  try {
    runSQL("alter table " + tableName + " add lastPublishDate DATE null");
  }
 catch (  SQLException sqle) {
    if (_log.isDebugEnabled()) {
      _log.debug("Table " + tableName + " has been previously recreated, "+ "there is not need to add the column");
    }
  }
}

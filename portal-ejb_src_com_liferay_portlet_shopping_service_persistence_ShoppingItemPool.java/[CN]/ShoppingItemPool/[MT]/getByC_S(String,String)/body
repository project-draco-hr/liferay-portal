{
  String pk=_instance._getByC_S(companyId,sku);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_S" + " " + companyId.toString() + " "+ sku.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

{
  if (_log.isDebugEnabled()) {
    _log.debug("Delete control panel menu preferences");
  }
  DB db=DBManagerUtil.getDB();
  db.runSQL("delete from PortletPreferences where portletId = " + _CONTROL_PANEL_MENU_PORTLET);
}

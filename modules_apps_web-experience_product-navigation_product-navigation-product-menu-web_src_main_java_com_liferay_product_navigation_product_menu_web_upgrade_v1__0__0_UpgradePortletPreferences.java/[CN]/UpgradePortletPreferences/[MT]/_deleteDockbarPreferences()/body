{
  if (_log.isDebugEnabled()) {
    _log.debug("Delete dockbar preferences");
  }
  DB db=DBManagerUtil.getDB();
  db.runSQL("delete from PortletPreferences where portletId = " + _DOCKBAR_PORTLET);
}

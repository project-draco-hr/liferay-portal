{
  String s=StringUtil.merge(values,StringPool.PERIOD);
  String[] arguments=new String[]{propertyName + "-IN-" + s};
  return new CriterionImpl(org.hibernate.criterion.Restrictions.in(propertyName,values),arguments);
}

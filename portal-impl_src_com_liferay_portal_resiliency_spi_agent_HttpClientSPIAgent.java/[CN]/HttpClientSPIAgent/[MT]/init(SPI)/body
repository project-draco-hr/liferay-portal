{
  try {
    SPIConfiguration spiConfiguration=spi.getSPIConfiguration();
    spi.addServlet(AGENT_CONTEXT_PATH,spiConfiguration.getBaseDir(),MAPPING_PATTERN,AcceptorServlet.class.getName());
  }
 catch (  Exception e) {
    throw new PortalResiliencyException(e);
  }
}

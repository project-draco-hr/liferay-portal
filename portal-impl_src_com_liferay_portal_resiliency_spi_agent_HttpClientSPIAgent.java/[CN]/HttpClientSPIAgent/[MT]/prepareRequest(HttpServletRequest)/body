{
  AgentRequest agentRequest=AgentRequest.readFrom(request.getInputStream());
  HttpServletRequest agentServletRequest=agentRequest.populateRequest(request);
  agentServletRequest.setAttribute(WebKeys.SPI_AGENT_REQUEST,agentRequest);
  return agentServletRequest;
}

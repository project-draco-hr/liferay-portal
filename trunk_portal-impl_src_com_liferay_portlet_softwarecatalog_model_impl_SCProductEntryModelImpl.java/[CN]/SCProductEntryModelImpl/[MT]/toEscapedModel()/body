{
  if (isEscapedModel()) {
    return (SCProductEntry)this;
  }
 else {
    return (SCProductEntry)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

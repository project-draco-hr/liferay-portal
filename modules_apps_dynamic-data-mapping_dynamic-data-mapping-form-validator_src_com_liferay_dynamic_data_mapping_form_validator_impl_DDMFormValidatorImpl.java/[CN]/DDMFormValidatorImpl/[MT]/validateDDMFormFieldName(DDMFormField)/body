{
  Matcher matcher=_ddmFormFieldNamePattern.matcher(ddmFormField.getName());
  if (!matcher.matches()) {
    throw new DDMFormValidationException("Nonalphanumeric characters were defined for field name " + ddmFormField.getName());
  }
}

{
  if (isEscapedModel()) {
    return (SocialActivity)this;
  }
 else {
    return (SocialActivity)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

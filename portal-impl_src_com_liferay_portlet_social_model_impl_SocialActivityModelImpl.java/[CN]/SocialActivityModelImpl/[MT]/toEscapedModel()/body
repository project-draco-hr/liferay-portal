{
  if (isEscapedModel()) {
    return (SocialActivity)this;
  }
 else {
    return (SocialActivity)Proxy.newProxyInstance(SocialActivity.class.getClassLoader(),new Class[]{SocialActivity.class},new AutoEscapeBeanHandler(this));
  }
}

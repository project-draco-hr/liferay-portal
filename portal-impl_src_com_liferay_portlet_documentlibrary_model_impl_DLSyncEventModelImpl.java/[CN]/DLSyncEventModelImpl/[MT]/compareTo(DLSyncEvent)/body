{
  int value=0;
  if (getModifiedDate() < dlSyncEvent.getModifiedDate()) {
    value=-1;
  }
 else   if (getModifiedDate() > dlSyncEvent.getModifiedDate()) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  return 0;
}

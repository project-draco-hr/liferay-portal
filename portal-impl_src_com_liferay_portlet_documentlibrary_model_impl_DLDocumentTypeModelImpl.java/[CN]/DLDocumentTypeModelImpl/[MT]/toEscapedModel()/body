{
  if (isEscapedModel()) {
    return (DLDocumentType)this;
  }
 else {
    return (DLDocumentType)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

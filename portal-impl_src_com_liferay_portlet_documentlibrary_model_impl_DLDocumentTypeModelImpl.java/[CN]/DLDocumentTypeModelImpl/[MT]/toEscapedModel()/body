{
  if (isEscapedModel()) {
    return (DLDocumentType)this;
  }
 else {
    return (DLDocumentType)Proxy.newProxyInstance(DLDocumentType.class.getClassLoader(),new Class[]{DLDocumentType.class},new AutoEscapeBeanHandler(this));
  }
}

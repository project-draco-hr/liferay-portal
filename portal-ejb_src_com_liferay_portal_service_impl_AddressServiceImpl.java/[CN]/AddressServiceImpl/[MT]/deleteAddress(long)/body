{
  Address address=AddressUtil.findByPrimaryKey(addressId);
  CommonPermission.checkPermission(getPermissionChecker(),address.getClassName(),address.getClassPK(),ActionKeys.UPDATE);
  AddressLocalServiceUtil.deleteAddress(addressId);
}

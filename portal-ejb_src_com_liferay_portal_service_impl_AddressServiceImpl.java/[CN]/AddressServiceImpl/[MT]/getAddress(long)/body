{
  Address address=AddressUtil.findByPrimaryKey(addressId);
  CommonPermission.checkPermission(getPermissionChecker(),address.getClassName(),address.getClassPK(),ActionKeys.VIEW);
  return address;
}

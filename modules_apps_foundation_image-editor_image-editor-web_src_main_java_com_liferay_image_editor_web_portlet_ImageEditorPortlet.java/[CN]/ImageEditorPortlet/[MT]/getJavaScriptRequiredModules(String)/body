{
  Set<String> requiredModules=new HashSet<>();
  requiredModules.addAll(super.getJavaScriptRequiredModules(path));
  requiredModules.addAll(_capabilityTracker.getCapabilitiesRequirements());
  return requiredModules;
}

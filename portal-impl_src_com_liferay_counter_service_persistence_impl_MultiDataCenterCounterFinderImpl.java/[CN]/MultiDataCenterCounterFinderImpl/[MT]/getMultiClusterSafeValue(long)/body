{
  byte[] bytes=new byte[8];
  BigEndianCodec.putLong(bytes,0,value);
  bytes[0]=(byte)((bytes[0] >>> _multiDataCenterBits) + (byte)(_multiDataCenterDeploymentId << (_MAXIMUM_BYTE_SHIFTS - _multiDataCenterBits)));
  return BigEndianCodec.getLong(bytes,0);
}

{
  DDMStructure structure=addStructure();
  Assert.assertNotNull(structure);
  DDMStructureManagerUtil.deleteStructure(structure.getStructureId());
  structure=DDMStructureManagerUtil.fetchStructure(structure.getGroupId(),structure.getClassNameId(),structure.getStructureKey());
  Assert.assertNull(structure);
}

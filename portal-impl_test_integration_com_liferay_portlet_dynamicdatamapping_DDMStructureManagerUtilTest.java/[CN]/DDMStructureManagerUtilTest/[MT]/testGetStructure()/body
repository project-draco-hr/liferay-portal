{
  DDMStructure expectedStructure=addStructure();
  DDMStructure actualStructure=DDMStructureManagerUtil.getStructure(_group.getGroupId(),expectedStructure.getClassNameId(),expectedStructure.getStructureKey());
  assertEquals(expectedStructure,actualStructure);
}

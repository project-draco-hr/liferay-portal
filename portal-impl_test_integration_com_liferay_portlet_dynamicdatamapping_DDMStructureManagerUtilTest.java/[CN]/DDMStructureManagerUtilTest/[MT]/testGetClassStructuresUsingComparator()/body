{
  List<DDMStructure> structures=DDMStructureManagerUtil.getClassStructures(_group.getCompanyId(),_classNameId,DDMStructureManager.STRUCTURE_COMPARATOR_STRUCTURE_KEY);
  int initialSize=structures.size();
  addStructure();
  structures=DDMStructureManagerUtil.getClassStructures(_group.getCompanyId(),_classNameId,DDMStructureManager.STRUCTURE_COMPARATOR_STRUCTURE_KEY);
  Assert.assertEquals(initialSize + 1,structures.size());
}

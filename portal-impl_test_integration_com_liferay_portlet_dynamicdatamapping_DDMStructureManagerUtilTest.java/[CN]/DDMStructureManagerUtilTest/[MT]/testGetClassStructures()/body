{
  DDMStructure structure=addStructure();
  List<DDMStructure> structures=DDMStructureManagerUtil.getClassStructures(_group.getCompanyId(),structure.getClassNameId(),QueryUtil.ALL_POS,QueryUtil.ALL_POS);
  Assert.assertEquals(1,structures.size());
}

{
  Map<Locale,String> nameMap=new HashMap<>();
  nameMap.put(LocaleUtil.US,"test");
  Map<Locale,String> descriptionMap=new HashMap<>();
  descriptionMap.put(LocaleUtil.US,"description");
  _structure=DDMStructureManagerUtil.addStructure(TestPropsValues.getUserId(),_group.getGroupId(),null,PortalUtil.getClassNameId("com.liferay.portlet.dynamicdatamapping.model.DDMStructure"),"KEY",nameMap,descriptionMap,createForm(),createFormLayout(),"json",1,_serviceContext);
  return _structure;
}

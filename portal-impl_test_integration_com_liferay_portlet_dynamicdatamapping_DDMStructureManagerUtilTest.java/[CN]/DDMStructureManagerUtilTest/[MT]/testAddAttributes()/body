{
  DDMStructure structure=addStructure();
  Document document=new DocumentImpl();
  DDMStructureManagerUtil.addAttributes(structure.getStructureId(),document,createDDMFormValues());
  String fieldProperty=structure.getFieldProperty("fieldName","indexType");
  Assert.assertNotNull(fieldProperty);
}

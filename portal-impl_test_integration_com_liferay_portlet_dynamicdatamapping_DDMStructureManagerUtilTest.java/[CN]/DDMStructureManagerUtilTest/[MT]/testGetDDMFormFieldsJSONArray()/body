{
  DDMStructure structure=addStructure();
  JSONArray jsonArray=DDMStructureManagerUtil.getDDMFormFieldsJSONArray(structure.getStructureId(),structure.getDefinition());
  Assert.assertEquals(1,jsonArray.length());
}

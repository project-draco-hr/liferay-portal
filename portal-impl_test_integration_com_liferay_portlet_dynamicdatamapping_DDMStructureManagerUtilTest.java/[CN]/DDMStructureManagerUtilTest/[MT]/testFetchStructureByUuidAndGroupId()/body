{
  DDMStructure expectedStructure=addStructure();
  DDMStructure actualStructure=DDMStructureManagerUtil.fetchStructureByUuidAndGroupId(expectedStructure.getUuid(),expectedStructure.getGroupId());
  Assert.assertNotNull(actualStructure);
  assertEquals(expectedStructure,actualStructure);
}

{
  if ((_connection == null) || (_converter == null)) {
    String host=PrefsPropsUtil.getString(PropsUtil.OPENOFFICE_SERVER_HOST);
    int port=PrefsPropsUtil.getInteger(PropsUtil.OPENOFFICE_SERVER_PORT);
    _connection=new SocketOpenOfficeConnection(host,port);
    _converter=new StreamOpenOfficeDocumentConverter(_connection);
  }
  return _converter;
}

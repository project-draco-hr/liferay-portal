{
  try {
    deletePage(companyId,nodeId,title);
  }
 catch (  SearchException se) {
  }
  Document doc=getPageDocument(companyId,groupId,resourcePrimKey,nodeId,title,content,tagsEntries,expandoBridge);
  SearchEngineUtil.addDocument(companyId,doc);
}

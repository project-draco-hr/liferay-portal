{
  try {
    deletePage(companyId,nodeId,title);
  }
 catch (  SearchException se) {
  }
  Document doc=getPageDocument(companyId,groupId,nodeId,title,content,tagsEntries);
  SearchEngineUtil.addDocument(companyId,doc);
}

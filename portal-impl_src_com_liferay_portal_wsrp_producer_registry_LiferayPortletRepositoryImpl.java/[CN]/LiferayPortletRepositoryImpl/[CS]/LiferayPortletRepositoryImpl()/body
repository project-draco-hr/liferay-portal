{
  if (portletsList == null) {
    portletsList=PortletLocalServiceUtil.getPortlets();
  }
  try {
    portletDescHolder=PortletDescriptorHolderFactory.getPortletDescriptorHolder();
  }
 catch (  Exception ex) {
    throw new ProducerException(ex);
  }
}

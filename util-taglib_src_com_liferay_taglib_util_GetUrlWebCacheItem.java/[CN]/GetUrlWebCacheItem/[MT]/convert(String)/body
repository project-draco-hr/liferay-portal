{
  String url=_url;
  String content=null;
  try {
    content=HttpUtil.URLtoString(_url);
  }
 catch (  Exception e) {
    throw new WebCacheException(url + " " + e.toString());
  }
  return content;
}

{
  int count=countByC_C_A(classNameId,classPK,alert);
  if (count == 0) {
    return null;
  }
  List<AnnouncementsEntry> list=findByC_C_A(classNameId,classPK,alert,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  Integer type=AnnouncementsEntryModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ AnnouncementsEntryModelImpl.TABLE_NAME);
  }
  return type;
}

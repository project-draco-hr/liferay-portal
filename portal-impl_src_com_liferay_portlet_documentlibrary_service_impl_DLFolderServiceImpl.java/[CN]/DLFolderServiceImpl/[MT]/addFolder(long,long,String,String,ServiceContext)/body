{
  DLFolderPermission.check(getPermissionChecker(),groupId,parentFolderId,ActionKeys.ADD_FOLDER);
  return dlFolderLocalService.addFolder(null,getUserId(),groupId,parentFolderId,name,description,serviceContext);
}

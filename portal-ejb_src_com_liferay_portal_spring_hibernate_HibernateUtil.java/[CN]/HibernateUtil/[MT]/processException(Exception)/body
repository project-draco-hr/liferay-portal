{
  if (e instanceof HibernateException) {
    _log.error("Caught HibernateException. " + e.getMessage());
  }
 else {
    _log.error("Caught unexpected Exception " + e.getClass().getName() + ". "+ e.getMessage());
  }
  return new SystemException(e);
}

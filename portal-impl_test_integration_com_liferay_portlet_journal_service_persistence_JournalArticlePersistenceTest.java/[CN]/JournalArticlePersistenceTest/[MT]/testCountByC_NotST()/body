{
  try {
    _persistence.countByC_NotST(RandomTestUtil.nextLong(),RandomTestUtil.nextInt());
    _persistence.countByC_NotST(0L,0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

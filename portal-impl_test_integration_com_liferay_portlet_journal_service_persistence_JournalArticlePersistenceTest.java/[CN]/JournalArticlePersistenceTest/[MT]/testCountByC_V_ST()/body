{
  try {
    _persistence.countByC_V_ST(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDouble(),ServiceTestUtil.nextInt());
    _persistence.countByC_V_ST(0L,0D,0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

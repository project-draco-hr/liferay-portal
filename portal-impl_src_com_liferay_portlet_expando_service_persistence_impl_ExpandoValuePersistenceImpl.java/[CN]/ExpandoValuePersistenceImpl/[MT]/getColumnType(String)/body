{
  Integer type=ExpandoValueModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ExpandoValueModelImpl.TABLE_NAME);
  }
  return type;
}

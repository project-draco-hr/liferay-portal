{
  String sql="update CalEvent set endDate = null where endDate is not null " + "and (recurrence like '%\"until\":null%' or " + "CAST_TEXT(recurrence) = 'null')";
  sql=SQLTransformer.transform(sql);
  runSQL(sql);
}

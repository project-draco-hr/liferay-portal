{
  List results=execute(new Object[]{userId,roleId});
  if (results.size() > 0) {
    Integer count=(Integer)results.get(0);
    if (count.intValue() > 0) {
      return true;
    }
  }
  return false;
}

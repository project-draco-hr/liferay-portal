{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WSRPProducer wsrpProducer) method. Use update(WSRPProducer wsrpProducer, boolean merge) instead.");
  }
  return update(wsrpProducer,false);
}

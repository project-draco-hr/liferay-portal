{
  Session session=null;
  try {
    session=openSession();
    return (WSRPProducer)session.get(WSRPProducerImpl.class,new Long(producerId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  String portletId=_id;
  if (Validator.isNotNull(_portletResource)) {
    portletId=_portletResource;
  }
  return SettingsFactoryUtil.getPortletInstanceSettings(_themeDisplay.getLayout(),portletId);
}

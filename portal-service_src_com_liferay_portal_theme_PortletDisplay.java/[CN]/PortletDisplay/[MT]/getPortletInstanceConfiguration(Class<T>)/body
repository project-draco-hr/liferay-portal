{
  String portletId=Validator.isNull(_portletResource) ? _id : _portletResource;
  return ModuleConfigurationFactoryUtil.getConfiguration(clazz,new PortletInstanceSettingsLocator(_themeDisplay.getLayout(),portletId));
}

{
  SettingsFactory settingsFactory=SettingsFactoryUtil.getSettingsFactory();
  String portletId=Validator.isNull(_portletResource) ? _id : _portletResource;
  return settingsFactory.getSettings(clazz,new PortletInstanceSettingsLocator(_themeDisplay.getLayout(),portletId));
}

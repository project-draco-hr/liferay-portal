{
  String portletId=Validator.isNull(_portletResource) ? _id : _portletResource;
  return ModuleConfigurationFactoryUtil.getModuleConfiguration(clazz,new PortletInstanceSettingsLocator(_themeDisplay.getLayout(),portletId));
}

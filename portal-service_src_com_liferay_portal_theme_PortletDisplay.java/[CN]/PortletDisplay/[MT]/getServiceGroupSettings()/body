{
  String portletId=_id;
  if (Validator.isNotNull(_portletResource)) {
    portletId=_portletResource;
  }
  return SettingsFactoryUtil.getServiceGroupSettings(_themeDisplay.getSiteGroupId(),portletId);
}

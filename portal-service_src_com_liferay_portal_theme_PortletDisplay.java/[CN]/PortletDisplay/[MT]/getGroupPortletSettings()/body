{
  String portletId=_id;
  if (Validator.isNotNull(_portletResource)) {
    portletId=_portletResource;
  }
  return PortletSettingsFactoryUtil.getGroupPortletSettings(_themeDisplay.getSiteGroupId(),portletId);
}

{
  JournalStructure structure=JournalStructureUtil.findByPrimaryKey(new JournalStructurePK(companyId,groupId,structureId));
  addStructureResources(structure,communityPermissions,guestPermissions);
}

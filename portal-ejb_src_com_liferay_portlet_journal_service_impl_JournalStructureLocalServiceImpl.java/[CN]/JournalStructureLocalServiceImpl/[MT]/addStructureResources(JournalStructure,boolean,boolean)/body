{
  ResourceLocalServiceUtil.addResources(structure.getCompanyId(),structure.getGroupId(),structure.getUserId(),JournalStructure.class.getName(),structure.getPrimaryKey().toString(),false,addCommunityPermissions,addGuestPermissions);
}

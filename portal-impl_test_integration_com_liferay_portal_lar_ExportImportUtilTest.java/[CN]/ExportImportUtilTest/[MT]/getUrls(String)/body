{
  Pattern urlPatter=Pattern.compile("(?:href=|\\{|\\[)(.*?)" + "(?:>|\\}|\\]|Link\\]\\])");
  Matcher m=urlPatter.matcher(content);
  List<String> urls=new ArrayList<String>();
  while (m.find()) {
    urls.add(m.group(1));
  }
  return urls;
}

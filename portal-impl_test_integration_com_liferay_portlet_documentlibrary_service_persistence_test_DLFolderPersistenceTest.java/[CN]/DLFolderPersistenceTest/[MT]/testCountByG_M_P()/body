{
  try {
    _persistence.countByG_M_P(RandomTestUtil.nextLong(),RandomTestUtil.randomBoolean(),RandomTestUtil.nextLong());
    _persistence.countByG_M_P(0L,RandomTestUtil.randomBoolean(),0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

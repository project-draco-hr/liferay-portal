{
  if (_persistence == null) {
    _persistence=(FormStructurePersistence)PortalBeanLocatorUtil.locate(FormStructurePersistence.class.getName());
    ReferenceRegistry.registerReference(FormStructureUtil.class,"_persistence");
  }
  return _persistence;
}

{
  ThemeDisplay themeDisplay=initThemeDisplay(company,group,privateLayout,LOCALHOST);
  Layout layout=LayoutTestUtil.addLayout(group);
  addParameterToThemeDisplayRequest(themeDisplay,"refererPlid",String.valueOf(layout.getPlid()));
  Assert.assertEquals(privateLayoutRelativeURL,PortalUtil.getLayoutRelativeURL(privateLayout,themeDisplay));
}

{
  ThemeDisplay themeDisplay=initThemeDisplay(company,group,privateLayout,LOCALHOST,VIRTUAL_HOSTNAME);
  String relativeURL=PortalUtil.getLayoutRelativeURL(privateLayout,themeDisplay);
  Assert.assertEquals(privateLayoutRelativeURL,relativeURL);
}

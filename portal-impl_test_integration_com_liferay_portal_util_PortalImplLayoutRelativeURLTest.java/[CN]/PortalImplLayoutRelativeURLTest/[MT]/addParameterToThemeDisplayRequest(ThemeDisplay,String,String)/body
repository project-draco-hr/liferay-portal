{
  if (themeDisplay.getRequest() == null) {
    themeDisplay.setRequest(new MockHttpServletRequest());
  }
  MockHttpServletRequest request=(MockHttpServletRequest)themeDisplay.getRequest();
  request.addParameter(paramName,paramValue);
}

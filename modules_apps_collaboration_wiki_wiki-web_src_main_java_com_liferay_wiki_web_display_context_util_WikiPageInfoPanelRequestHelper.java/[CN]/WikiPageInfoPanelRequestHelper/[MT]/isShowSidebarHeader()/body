{
  HttpServletRequest request=getRequest();
  boolean showSidebarHeader=GetterUtil.getBoolean(request.getAttribute(WikiWebKeys.SHOW_SIDEBAR_HEADER));
  return ParamUtil.getBoolean(request,"showSidebarHeader",showSidebarHeader);
}

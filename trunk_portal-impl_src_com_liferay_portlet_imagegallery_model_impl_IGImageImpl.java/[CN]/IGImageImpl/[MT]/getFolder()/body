{
  IGFolder folder=null;
  if (getFolderId() > 0) {
    try {
      folder=IGFolderLocalServiceUtil.getFolder(getFolderId());
    }
 catch (    Exception e) {
      folder=new IGFolderImpl();
      _log.error(e);
    }
  }
 else {
    folder=new IGFolderImpl();
  }
  return folder;
}

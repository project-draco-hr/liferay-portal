{
  return cacheExtension.task(LiferayThemePlugin.GULP_BUILD_TASK_NAME,new Closure<Void>(project){
    @SuppressWarnings("unused") public void doCall(    TaskCache taskCache){
      taskCache.setBaseDir("dist");
      taskCache.setCacheDir(".task-cache");
      taskCache.skipTaskDependency(NodePlugin.DOWNLOAD_NODE_TASK_NAME,NodePlugin.NPM_INSTALL_TASK_NAME);
      taskCache.testFile("gulpfile.js","package.json","src");
    }
  }
);
}

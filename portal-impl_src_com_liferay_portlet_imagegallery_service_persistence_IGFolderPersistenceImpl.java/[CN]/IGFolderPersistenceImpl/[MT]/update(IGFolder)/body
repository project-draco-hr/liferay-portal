{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(IGFolder igFolder) method. To improve performance, call update(IGFolder igFolder, boolean merge).");
  }
  return update(igFolder,false);
}

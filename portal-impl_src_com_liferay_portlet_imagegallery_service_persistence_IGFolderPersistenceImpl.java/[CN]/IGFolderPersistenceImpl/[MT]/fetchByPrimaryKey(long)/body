{
  IGFolder result=(IGFolder)EntityCacheUtil.getResult(IGFolderModelImpl.ENTITY_CACHE_ENABLED,IGFolder.class,folderId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      IGFolder igFolder=(IGFolder)session.get(IGFolderImpl.class,new Long(folderId));
      cacheResult(igFolder);
      return igFolder;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (IGFolder)result;
  }
}

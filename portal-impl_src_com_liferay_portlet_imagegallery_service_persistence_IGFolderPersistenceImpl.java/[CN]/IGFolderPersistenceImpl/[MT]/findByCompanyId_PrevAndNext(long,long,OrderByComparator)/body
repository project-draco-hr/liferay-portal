{
  IGFolder igFolder=findByPrimaryKey(folderId);
  Session session=null;
  try {
    session=openSession();
    IGFolder[] array=new IGFolderImpl[3];
    array[0]=getByCompanyId_PrevAndNext(session,igFolder,companyId,orderByComparator,true);
    array[1]=igFolder;
    array[2]=getByCompanyId_PrevAndNext(session,igFolder,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

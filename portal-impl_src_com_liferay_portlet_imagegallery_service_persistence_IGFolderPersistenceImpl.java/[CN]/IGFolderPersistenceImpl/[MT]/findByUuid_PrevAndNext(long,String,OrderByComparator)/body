{
  IGFolder igFolder=findByPrimaryKey(folderId);
  Session session=null;
  try {
    session=openSession();
    IGFolder[] array=new IGFolderImpl[3];
    array[0]=getByUuid_PrevAndNext(session,igFolder,uuid,orderByComparator,true);
    array[1]=igFolder;
    array[2]=getByUuid_PrevAndNext(session,igFolder,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isDebugEnabled()) {
    _log.debug("Invoking pageExists(" + title + ")");
  }
  try {
    if (WikiPageLocalServiceUtil.getPagesCount(_nodeId,title,true) > 0) {
      return true;
    }
 else {
      return false;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return false;
}

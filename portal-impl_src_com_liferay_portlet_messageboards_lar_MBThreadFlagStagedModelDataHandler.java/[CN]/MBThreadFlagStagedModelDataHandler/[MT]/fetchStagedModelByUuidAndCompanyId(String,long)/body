{
  List<MBThreadFlag> threadFlags=MBThreadFlagLocalServiceUtil.getMBThreadFlagsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<MBThreadFlag>());
  if (ListUtil.isEmpty(threadFlags)) {
    return null;
  }
  return threadFlags.get(0);
}

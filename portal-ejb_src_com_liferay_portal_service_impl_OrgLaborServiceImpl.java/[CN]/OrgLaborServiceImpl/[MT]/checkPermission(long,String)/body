{
  Organization organization=OrganizationUtil.findByPrimaryKey(organizationId);
  if (organization.isRoot()) {
    OrganizationPermission.check(getPermissionChecker(),organizationId,actionId);
  }
 else {
    LocationPermission.check(getPermissionChecker(),organizationId,actionId);
  }
}

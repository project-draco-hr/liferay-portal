{
  if (obj == null) {
    return -1;
  }
  UserIdMapperPK pk=(UserIdMapperPK)obj;
  int value=0;
  if (userId < pk.userId) {
    value=-1;
  }
 else   if (userId > pk.userId) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  value=type.compareTo(pk.type);
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.SHOPPING,ActionKeys.MANAGE_COUPONS);
  return shoppingCouponLocalService.search(plid,companyId,code,active,discountType,andOperator,start,end);
}

{
  if (!contains(permissionChecker,record,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DDLRecord.class.getName(),record.getRecordId(),actionId);
  }
}

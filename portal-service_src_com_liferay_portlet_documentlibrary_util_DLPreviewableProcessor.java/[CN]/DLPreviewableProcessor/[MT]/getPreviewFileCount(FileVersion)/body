{
  try {
    String[] fileNames=DLStoreUtil.getFileNames(fileVersion.getCompanyId(),REPOSITORY_ID,PREVIEW_PATH.concat(getPathSegment(fileVersion)));
    return fileNames.length;
  }
 catch (  Exception e) {
  }
  return 0;
}

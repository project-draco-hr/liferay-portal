{
  try {
    String[] files=DLStoreUtil.getFileNames(fileVersion.getCompanyId(),REPOSITORY_ID,PREVIEW_PATH + getPathSegment(fileVersion));
    return files.length;
  }
 catch (  Exception e) {
  }
  return 0;
}

{
  try {
    DLStoreUtil.deleteDirectory(companyId,REPOSITORY_ID,_getPathSegment(groupId,fileEntryId,fileVersionId,true));
  }
 catch (  Exception e) {
  }
  try {
    DLStoreUtil.deleteDirectory(companyId,REPOSITORY_ID,_getPathSegment(groupId,fileEntryId,fileVersionId,false));
  }
 catch (  Exception e) {
  }
}

{
  try {
    String dirName=getThumbnailFilePath(groupId,fileEntryId,fileVersionId,index,thumbnailType);
    DLStoreUtil.deleteFile(companyId,REPOSITORY_ID,dirName);
  }
 catch (  Exception e) {
  }
}

{
  String previewFilePath=getPreviewFilePath(groupId,fileEntryId,fileVersionId,null);
  try {
    DLStoreUtil.deleteDirectory(companyId,REPOSITORY_ID,previewFilePath);
  }
 catch (  Exception e) {
  }
}

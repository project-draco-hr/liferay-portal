{
  for (  String type : getPreviewTypes()) {
    String path=getPreviewFilePath(groupId,fileEntryId,fileVersionId,type);
    try {
      DLStoreUtil.deleteFile(companyId,REPOSITORY_ID,path);
    }
 catch (    Exception e) {
    }
  }
}

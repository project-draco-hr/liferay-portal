{
  DLFolder dlFolder=DLTestUtil.addFolder(_group.getGroupId());
  DLFileEntry dlFileEntry=DLTestUtil.addFileEntry(dlFolder.getFolderId());
  RepositoryLocalServiceUtil.getRepositoryImpl(0,dlFileEntry.getFileEntryId(),0);
}

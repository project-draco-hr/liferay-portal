{
  DLFolder folder=DLTestUtil.addFolder(_group.getGroupId());
  DLFileEntry fileEntry=DLTestUtil.addFileEntry(folder);
  RepositoryLocalServiceUtil.getRepositoryImpl(0,fileEntry.getFileEntryId(),0);
}

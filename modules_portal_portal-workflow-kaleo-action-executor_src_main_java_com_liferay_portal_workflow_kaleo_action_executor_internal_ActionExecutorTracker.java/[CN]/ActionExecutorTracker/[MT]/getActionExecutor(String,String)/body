{
  String key=_actionExecutorServiceReferenceMapper.getKey(scriptLanguage,StringUtil.trim(script));
  ActionExecutor actionExecutor=_serviceTrackerMap.getService(key);
  if (actionExecutor == null) {
    throw new WorkflowException("No Action executor registered with key " + key);
  }
  return actionExecutor;
}

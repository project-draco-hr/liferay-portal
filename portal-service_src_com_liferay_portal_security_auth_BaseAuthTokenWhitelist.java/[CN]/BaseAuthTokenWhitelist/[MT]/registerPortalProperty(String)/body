{
  Registry registry=RegistryUtil.getRegistry();
  String[] propertyValue=PropsUtil.getArray(propertyName);
  Map<String,Object> properties=new HashMap<>();
  properties.put(propertyName,propertyValue);
  ServiceRegistration<Object> serviceRegistration=registry.registerService(Object.class,new Object(),properties);
  serviceRegistrations.put(StringUtil.merge(propertyValue),serviceRegistration);
}

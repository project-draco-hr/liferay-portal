{
  try {
    FileEntry fileEntry=getTemporaryFileEntries(callerUuid,folderPath,fileName);
    _localRepository.deleteFileEntry(fileEntry.getFileEntryId());
  }
 catch (  NoSuchModelException nsme) {
  }
}

{
  ServiceContext serviceContext=new ServiceContext();
  serviceContext.setAddGroupPermissions(true);
  serviceContext.setAddGuestPermissions(true);
  Folder tempFolder=_getFolder(DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,FOLDER_NAME_TEMP);
  Folder callerFolder=_getFolder(tempFolder.getFolderId(),callerUuid.toString());
  return _getFolders(callerFolder.getFolderId(),folderPath);
}

{
  ServiceContext serviceContext=new ServiceContext();
  serviceContext.setAddGroupPermissions(true);
  serviceContext.setAddGuestPermissions(true);
  Folder tempFolder=_addFolder(userId,DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,_FOLDER_NAME_TEMP,serviceContext);
  Folder callerFolder=_addFolder(userId,tempFolder.getFolderId(),callerUuid.toString(),serviceContext);
  return _addFolders(userId,callerFolder.getFolderId(),folderPath,serviceContext);
}

{
  Set<String> availableLanguageIdsSet=SetUtil.fromArray(getAvailableLanguageIds());
  String[] languageIds=getLanguageIds();
  Arrays.sort(languageIds);
  List<KeyValuePair> availableLanguageList=new ArrayList<>();
  for (  String languageId : availableLanguageIdsSet) {
    if (Arrays.binarySearch(languageIds,languageId) < 0) {
      availableLanguageList.add(new KeyValuePair(languageId,LocaleUtil.fromLanguageId(languageId).getDisplayName(_themeDisplay.getLocale())));
    }
  }
  availableLanguageList=ListUtil.sort(availableLanguageList,new KeyValuePairComparator(false,true));
  return availableLanguageList;
}

{
  WikiPage wikiPage=(WikiPage)EntityCacheUtil.getResult(WikiPageModelImpl.ENTITY_CACHE_ENABLED,WikiPageImpl.class,primaryKey);
  if (wikiPage == _nullWikiPage) {
    return null;
  }
  if (wikiPage == null) {
    Session session=null;
    try {
      session=openSession();
      wikiPage=(WikiPage)session.get(WikiPageImpl.class,primaryKey);
      if (wikiPage != null) {
        cacheResult(wikiPage);
      }
 else {
        EntityCacheUtil.putResult(WikiPageModelImpl.ENTITY_CACHE_ENABLED,WikiPageImpl.class,primaryKey,_nullWikiPage);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(WikiPageModelImpl.ENTITY_CACHE_ENABLED,WikiPageImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return wikiPage;
}

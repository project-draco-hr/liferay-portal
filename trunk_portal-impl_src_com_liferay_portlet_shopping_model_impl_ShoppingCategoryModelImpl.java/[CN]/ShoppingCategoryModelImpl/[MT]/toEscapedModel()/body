{
  if (isEscapedModel()) {
    return (ShoppingCategory)this;
  }
 else {
    return (ShoppingCategory)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

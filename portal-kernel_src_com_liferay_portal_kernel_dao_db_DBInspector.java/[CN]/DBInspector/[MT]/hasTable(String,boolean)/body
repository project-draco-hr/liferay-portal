{
  if (caseSensitive) {
    if (_hasTable(tableName)) {
      return true;
    }
    return false;
  }
  if (_hasTable(StringUtil.toLowerCase(tableName)) || _hasTable(StringUtil.toUpperCase(tableName)) || _hasTable(tableName)) {
    return true;
  }
  return false;
}

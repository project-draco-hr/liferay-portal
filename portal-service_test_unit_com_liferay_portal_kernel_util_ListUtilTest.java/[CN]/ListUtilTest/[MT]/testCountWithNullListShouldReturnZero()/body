{
  List<String> list=null;
  PredicateFilter predicate=new PredicateFilter(){
    @Override public boolean filter(    Object o){
      return true;
    }
  }
;
  Assert.assertEquals(0,ListUtil.count(list,predicate));
}

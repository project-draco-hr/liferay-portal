{
  if (_alternativeShipping != null) {
    try {
      for (int i=0; i < 10; i++) {
        if (Validator.isNotNull(_alternativeShipping[0][i]) && Validator.isNotNull(_alternativeShipping[1][i])) {
          return true;
        }
      }
    }
 catch (    Exception e) {
    }
  }
  return false;
}

{
  WikiPageNode root=parseFile(HORIZONTAL_FILES_PREFIX + "horizontal-1.creole");
  Assert.assertEquals(1,root.getChildASTNodesCount());
  Assert.assertTrue(root.getChildASTNode(0) instanceof HorizontalNode);
}

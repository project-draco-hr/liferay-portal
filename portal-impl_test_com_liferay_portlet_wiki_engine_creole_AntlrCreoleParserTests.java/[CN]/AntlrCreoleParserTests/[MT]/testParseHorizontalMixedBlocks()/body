{
  WikiPageNode root=parseFile(HORIZONTAL_FILES_PREFIX + "horizontal-3.creole");
  Assert.assertEquals(3,root.getChildASTNodesCount());
  Assert.assertTrue(root.getChildASTNode(1) instanceof HorizontalNode);
}

{
  WikiPageNode root=parseFile(NOWIKI_FILES_PREFIX + "nowikiblock-4.creole");
  NoWikiSectionNode child=(NoWikiSectionNode)root.getChildASTNode(0);
  Assert.assertEquals("This is a non \\empty\\ block",child.getContent());
}

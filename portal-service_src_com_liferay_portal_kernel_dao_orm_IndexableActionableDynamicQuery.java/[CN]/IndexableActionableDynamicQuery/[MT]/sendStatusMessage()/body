{
  if (!BackgroundTaskThreadLocal.hasBackgroundTask()) {
    return;
  }
  Indexer<?> indexer=IndexerRegistryUtil.getIndexer(getModelClass());
  ReindexStatusMessageSenderUtil.sendStatusMessage(indexer.getClassName(),_progress,_count);
}

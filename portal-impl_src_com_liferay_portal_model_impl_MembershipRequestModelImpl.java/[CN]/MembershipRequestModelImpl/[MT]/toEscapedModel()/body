{
  if (isEscapedModel()) {
    return (MembershipRequest)this;
  }
 else {
    return (MembershipRequest)Proxy.newProxyInstance(MembershipRequest.class.getClassLoader(),new Class[]{MembershipRequest.class},new AutoEscapeBeanHandler(this));
  }
}

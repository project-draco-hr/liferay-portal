{
  if (!contains(permissionChecker,entry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),PortalUtil.getClassName(entry.getClassNameId()),entry.getClassPK(),actionId);
  }
}

{
  if (!contains(permissionChecker,layout,checkViewableGroup,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),Layout.class.getName(),layout.getLayoutId(),actionId);
  }
}

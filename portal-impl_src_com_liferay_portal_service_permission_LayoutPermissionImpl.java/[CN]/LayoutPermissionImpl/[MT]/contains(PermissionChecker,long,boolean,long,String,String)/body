{
  Layout layout=LayoutLocalServiceUtil.getLayout(groupId,privateLayout,layoutId);
  if (isAttemptToModifyLockedLayout(layout,actionId)) {
    return false;
  }
  return contains(permissionChecker,layout,controlPanelCategory,actionId);
}

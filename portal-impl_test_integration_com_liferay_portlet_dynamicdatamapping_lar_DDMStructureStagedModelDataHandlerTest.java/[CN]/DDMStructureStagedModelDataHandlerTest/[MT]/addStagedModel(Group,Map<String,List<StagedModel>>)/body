{
  List<StagedModel> dependentStagedModels=dependentStagedModelsMap.get(DDMStructure.class.getSimpleName());
  DDMStructure parentStructure=(DDMStructure)dependentStagedModels.get(0);
  return DDMStructureTestUtil.addStructure(group.getGroupId(),DDLRecordSet.class.getName(),parentStructure.getStructureId());
}

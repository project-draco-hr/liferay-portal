{
  if ((path != null) && !path.equals(_PATH_C) && !path.startsWith(_PATH_COMMON)&& !path.contains(_PATH_J_SECURITY_CHECK)&& !path.startsWith(_PATH_PORTAL)&& !path.startsWith(_PATH_TAGLIB)) {
    return true;
  }
 else {
    return false;
  }
}

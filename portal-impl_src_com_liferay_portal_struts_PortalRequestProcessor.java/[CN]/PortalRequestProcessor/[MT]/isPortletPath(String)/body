{
  if ((path != null) && !path.equals(_PATH_C) && !path.startsWith(_PATH_COMMON)&& (path.indexOf(_PATH_J_SECURITY_CHECK) == -1)&& !path.startsWith(_PATH_PORTAL)) {
    return true;
  }
 else {
    return false;
  }
}

{
  try {
    PermissionServiceUtil.setRolePermission(roleId,groupId,name,typeId,scope,primKey,actionId);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

{
  try {
    PermissionServiceUtil.setOrgGroupPermissions(organizationId,groupId,actionIds,resourceId);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

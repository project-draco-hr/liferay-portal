{
  try {
    PermissionServiceUtil.setGroupPermissions(organizationId,groupId,actionIds,resourceId);
  }
 catch (  Exception e) {
    String stackTrace=StackTraceUtil.getStackTrace(e);
    _log.error(stackTrace);
    throw new RemoteException(stackTrace);
  }
}

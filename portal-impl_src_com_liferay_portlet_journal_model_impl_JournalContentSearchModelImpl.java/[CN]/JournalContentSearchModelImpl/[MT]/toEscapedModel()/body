{
  if (isEscapedModel()) {
    return (JournalContentSearch)this;
  }
 else {
    return (JournalContentSearch)Proxy.newProxyInstance(JournalContentSearch.class.getClassLoader(),new Class[]{JournalContentSearch.class},new AutoEscapeBeanHandler(this));
  }
}

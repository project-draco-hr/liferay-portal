{
  if (cache instanceof BlockingCache) {
    return cache;
  }
  Ehcache decoratedCache=new BlockingCache(cache);
  _cacheManager.replaceCacheWithDecoratedCache(cache,decoratedCache);
  return decoratedCache;
}

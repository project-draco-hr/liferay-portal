{
  BasicServiceReference<?> otherServiceReference=(BasicServiceReference<?>)serviceReference;
  int thisRanking=(Integer)_properties.get("service.ranking");
  int otherRanking=(Integer)otherServiceReference._properties.get("service.ranking");
  if (thisRanking != otherRanking) {
    if (thisRanking < otherRanking) {
      return -1;
    }
    return 1;
  }
  long thisId=(Long)_properties.get("service.id");
  long otherId=(Long)otherServiceReference._properties.get("service.id");
  if (thisId == otherId) {
    return 0;
  }
  if (thisId < otherId) {
    return 1;
  }
  return -1;
}

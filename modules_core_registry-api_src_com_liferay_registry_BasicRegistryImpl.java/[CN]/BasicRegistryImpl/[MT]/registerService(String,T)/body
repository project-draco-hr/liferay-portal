{
  BasicServiceReference<T> dummyServiceReference=new BasicServiceReference<T>(className,_serviceIdCounter.incrementAndGet(),0,new HashMap<String,Object>());
  doTrackers_addingService(dummyServiceReference,service);
  return new BasicServiceRegistration<T>(dummyServiceReference);
}

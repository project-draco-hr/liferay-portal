{
  BasicServiceReference<T> dummyServiceReference=new BasicServiceReference<T>(clazz.getName(),_serviceIdCounter.incrementAndGet(),0,new HashMap<String,Object>());
  doTrackers_addingService(dummyServiceReference,service);
  return new BasicServiceRegistration<T>(dummyServiceReference);
}

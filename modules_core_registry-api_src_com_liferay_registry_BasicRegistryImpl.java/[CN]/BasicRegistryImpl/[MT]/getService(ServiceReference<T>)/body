{
  BasicServiceReference<?> dummyServiceReference=(BasicServiceReference<?>)serviceReference;
  for (  Entry<ServiceReference<?>,Object> entry : getServices().entrySet()) {
    if (dummyServiceReference.matches(entry.getKey())) {
      return (T)entry.getValue();
    }
  }
  return null;
}

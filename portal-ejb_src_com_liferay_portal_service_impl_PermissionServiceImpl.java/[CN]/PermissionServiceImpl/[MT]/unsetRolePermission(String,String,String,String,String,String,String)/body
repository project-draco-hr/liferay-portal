{
  checkPermission(getPermissionChecker(),groupId,Role.class.getName(),roleId);
  PermissionLocalServiceUtil.unsetRolePermission(roleId,getUser().getCompanyId(),name,typeId,scope,primKey,actionId);
}

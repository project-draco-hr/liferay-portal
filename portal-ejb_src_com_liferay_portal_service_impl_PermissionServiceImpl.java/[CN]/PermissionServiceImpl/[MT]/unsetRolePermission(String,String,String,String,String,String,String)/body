{
  checkPermission(getPermissionChecker(),groupId,Role.class.getName(),roleId);
  return PermissionLocalServiceUtil.unsetRolePermission(roleId,getUser().getCompanyId(),name,typeId,scope,primKey,actionId);
}

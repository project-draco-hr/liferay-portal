{
  checkPermission(getPermissionChecker(),groupId,Role.class.getName(),roleId);
  return PermissionLocalServiceUtil.unsetRolePermissions(roleId,getUser().getCompanyId(),name,typeId,scope,actionId);
}

{
  Socket socket=null;
  try {
    socket=new Socket(testHost,testPort);
    return socket.getLocalAddress().getHostAddress();
  }
  finally {
    if (socket != null) {
      try {
        socket.close();
      }
 catch (      IOException ex) {
      }
    }
  }
}

{
  if (Validator.isNotNull(_actionTemplate)) {
    try {
      mergeTemplate(getTemplate(_actionTemplate),actionRequest,actionResponse);
    }
 catch (    Exception e) {
      throw new PortletException(e);
    }
  }
}

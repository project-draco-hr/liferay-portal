{
  if (Validator.isNotNull(_resourceTemplate)) {
    try {
      mergeTemplate(getTemplate(_actionTemplate),resourceRequest,resourceResponse);
    }
 catch (    Exception e) {
      throw new PortletException(e);
    }
  }
 else {
    super.serveResource(resourceRequest,resourceResponse);
  }
}

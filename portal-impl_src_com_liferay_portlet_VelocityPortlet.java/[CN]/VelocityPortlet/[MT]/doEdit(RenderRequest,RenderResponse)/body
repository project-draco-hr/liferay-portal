{
  if (renderRequest.getPreferences() == null) {
    super.doEdit(renderRequest,renderResponse);
  }
 else {
    try {
      mergeTemplate(getTemplate(_editTemplate),renderRequest,renderResponse);
    }
 catch (    Exception e) {
      throw new PortletException(e);
    }
  }
}

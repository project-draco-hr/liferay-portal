{
  Context context=new VelocityContext();
  context.put(REQUEST,portletRequest);
  context.put(RESPONSE,portletResponse);
  VelocityVariables.insertVariables((VelocityContext)context,PortalUtil.getHttpServletRequest(portletRequest));
  return context;
}

{
  UnsyncBufferedReader ubr=new UnsyncBufferedReader(new StringReader(""));
  assertTrue(ubr.ready());
  ubr=new UnsyncBufferedReader(new InputStreamReader(new ByteArrayInputStream(new byte[0])));
  assertFalse(ubr.ready());
}

{
  UnsyncBufferedReader ubr=new UnsyncBufferedReader(new StringReader(""));
  assertEquals(8192,ubr.buffer.length);
  ubr=new UnsyncBufferedReader(new StringReader(""),10);
  assertEquals(10,ubr.buffer.length);
}

{
  Class<?> clazz=getClass();
  Properties properties=new Properties();
  String content=JenkinsResultsParserUtil.toString(JenkinsResultsParserUtil.getLocalURL("${dependencies.url}" + clazz.getSimpleName() + "/"+ sampleName+ "/sample.properties"));
  properties.load(new StringReader(content));
  return properties;
}

{
  List<Role> allRoles=RoleLocalServiceUtil.getRoles(group.getCompanyId());
  int[] types=new int[]{RoleConstants.TYPE_REGULAR,RoleConstants.TYPE_COMMUNITY};
  if (isPortalModelResource(modelResource)) {
    types=new int[]{RoleConstants.TYPE_REGULAR};
  }
 else {
    if (group.isOrganization()) {
      types=new int[]{RoleConstants.TYPE_REGULAR,RoleConstants.TYPE_ORGANIZATION};
    }
 else     if (group.isUser()) {
      types=new int[]{RoleConstants.TYPE_REGULAR};
    }
  }
  List<Role> roles=new ArrayList();
  for (  int type : types) {
    for (    Role role : allRoles) {
      if (role.getType() == type) {
        roles.add(role);
      }
    }
  }
  return roles;
}

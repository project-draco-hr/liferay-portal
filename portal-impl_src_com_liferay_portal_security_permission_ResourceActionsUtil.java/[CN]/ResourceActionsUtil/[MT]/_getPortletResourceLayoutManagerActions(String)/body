{
  name=PortletImpl.getRootPortletId(name);
  List<String> actions=_getActions(_portletResourceLayoutManagerActions,name);
  if (actions.size() < 1) {
    actions.add("CONFIGURATION");
    actions.add("PREFERENCES");
    actions.add("VIEW");
  }
  return actions;
}

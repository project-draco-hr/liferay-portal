{
  if (!actions.contains("CONFIGURATION")) {
    actions.add("CONFIGURATION");
  }
  if (!actions.contains("VIEW")) {
    actions.add("VIEW");
  }
  if (!actions.contains("ADD_TO_PAGE") && !actions.contains("ACCESS_IN_CONTROL_PANEL")) {
    actions.add("ADD_TO_PAGE");
  }
}

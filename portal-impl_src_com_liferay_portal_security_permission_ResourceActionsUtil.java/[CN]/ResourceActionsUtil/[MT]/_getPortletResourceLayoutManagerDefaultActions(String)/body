{
  name=PortletImpl.getRootPortletId(name);
  List actions=_getActions(_portletResourceLayoutManagerDefaultActions,name);
  if (actions.size() == 0) {
    _checkPortletLayoutManagerDefaultActions(actions);
  }
  return actions;
}

{
  List<DLContent> dlContents=dlContentPersistence.findByC_R_P(companyId,repositoryId,path);
  if (dlContents == null || dlContents.isEmpty()) {
    throw new NoSuchContentException(path);
  }
  return dlContents.get(0);
}

{
  if (!dirName.endsWith(StringPool.SLASH)) {
    dirName=dirName.concat(StringPool.SLASH);
  }
  dirName.concat(StringPool.PERCENT);
  dlContentPersistence.removeByC_P_R_LikeP(companyId,portletId,repositoryId,dirName);
}

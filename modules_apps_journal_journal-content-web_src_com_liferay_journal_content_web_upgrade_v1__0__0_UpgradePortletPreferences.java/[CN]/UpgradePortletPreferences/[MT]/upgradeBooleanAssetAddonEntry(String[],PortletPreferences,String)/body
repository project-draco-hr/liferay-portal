{
  boolean preferenceValue=GetterUtil.getBoolean(portletPreferences.getValue(preferenceKey,null));
  if (preferenceValue) {
    selectableEntries=ArrayUtil.append(selectableEntries,preferenceKey);
  }
  portletPreferences.reset(preferenceKey);
  return selectableEntries;
}

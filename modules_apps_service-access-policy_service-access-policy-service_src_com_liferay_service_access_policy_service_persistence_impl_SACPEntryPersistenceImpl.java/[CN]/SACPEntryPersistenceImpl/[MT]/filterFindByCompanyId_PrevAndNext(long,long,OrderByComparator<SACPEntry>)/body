{
  if (!InlineSQLHelperUtil.isEnabled()) {
    return findByCompanyId_PrevAndNext(sacpEntryId,companyId,orderByComparator);
  }
  SACPEntry sacpEntry=findByPrimaryKey(sacpEntryId);
  Session session=null;
  try {
    session=openSession();
    SACPEntry[] array=new SACPEntryImpl[3];
    array[0]=filterGetByCompanyId_PrevAndNext(session,sacpEntry,companyId,orderByComparator,true);
    array[1]=sacpEntry;
    array[2]=filterGetByCompanyId_PrevAndNext(session,sacpEntry,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

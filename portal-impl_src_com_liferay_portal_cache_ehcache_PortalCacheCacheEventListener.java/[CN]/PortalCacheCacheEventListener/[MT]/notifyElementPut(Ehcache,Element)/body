{
  Serializable key=element.getKey();
  _cacheListener.notifyEntryPut(_portalCache,String.valueOf(key),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug("Inserted " + key + " into "+ ehcache.getName());
  }
}

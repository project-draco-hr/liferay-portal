{
  Serializable key=element.getKey();
  _cacheListener.notifyEntryRemoved(_portalCache,String.valueOf(key),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug("Removed " + key + " from "+ ehcache.getName());
  }
}

{
  @SuppressWarnings("unchecked") K key=(K)element.getKey();
  @SuppressWarnings("unchecked") V value=(V)element.getObjectValue();
  _cacheListener.notifyEntryUpdated(_portalCache,key,value);
  if (_log.isDebugEnabled()) {
    _log.debug("Updated " + key + " in "+ ehcache.getName());
  }
}

{
  Serializable key=element.getKey();
  _cacheListener.notifyEntryEvicted(_portalCache,String.valueOf(key),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug("Evicted " + key + " from "+ ehcache.getName());
  }
}

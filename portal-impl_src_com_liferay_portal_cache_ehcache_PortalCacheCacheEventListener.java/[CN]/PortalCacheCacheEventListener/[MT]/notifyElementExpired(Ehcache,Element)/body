{
  Serializable key=element.getKey();
  _cacheListener.notifyEntryExpired(_portalCache,String.valueOf(key),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug("Expired " + key + " from "+ ehcache.getName());
  }
}

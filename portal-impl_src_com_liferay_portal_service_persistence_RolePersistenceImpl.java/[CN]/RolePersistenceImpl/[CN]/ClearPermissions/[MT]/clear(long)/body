{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(roleId,com.liferay.portal.model.Permission.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(roleId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(roleId,com.liferay.portal.model.Permission.class.getName());
    }
  }
}

{
  Object[] finderArgs=new Object[]{new Long(pk),new Long(permissionPK)};
  Object result=FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_PERMISSION,finderArgs,this);
  if (result == null) {
    try {
      Boolean value=Boolean.valueOf(containsPermission.contains(pk,permissionPK));
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_PERMISSION,finderArgs,value);
      return value.booleanValue();
    }
 catch (    Exception e) {
      throw processException(e);
    }
  }
 else {
    return ((Boolean)result).booleanValue();
  }
}

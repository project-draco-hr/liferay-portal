{
  Role role=(Role)EntityCacheUtil.getResult(RoleModelImpl.ENTITY_CACHE_ENABLED,RoleImpl.class,roleId,this);
  if (role == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (role == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      role=(Role)session.get(RoleImpl.class,Long.valueOf(roleId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (role == null)) {
        EntityCacheUtil.putResult(RoleModelImpl.ENTITY_CACHE_ENABLED,RoleImpl.class,roleId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(role);
      }
      closeSession(session);
    }
  }
  return role;
}

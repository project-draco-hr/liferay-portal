{
  Role role=(Role)EntityCacheUtil.getResult(RoleModelImpl.ENTITY_CACHE_ENABLED,RoleImpl.class,roleId);
  if (role == _nullRole) {
    return null;
  }
  if (role == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      role=(Role)session.get(RoleImpl.class,Long.valueOf(roleId));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (role != null) {
        cacheResult(role);
      }
 else       if (!hasException) {
        EntityCacheUtil.putResult(RoleModelImpl.ENTITY_CACHE_ENABLED,RoleImpl.class,roleId,_nullRole);
      }
      closeSession(session);
    }
  }
  return role;
}

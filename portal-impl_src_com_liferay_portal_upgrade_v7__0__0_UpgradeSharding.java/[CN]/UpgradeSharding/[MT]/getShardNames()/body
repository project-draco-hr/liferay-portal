{
  List<String> shardNames=new ArrayList<>();
  try (PreparedStatement ps=connection.prepareStatement("select name from Shard");ResultSet rs=ps.executeQuery()){
    while (rs.next()) {
      shardNames.add(rs.getString("name"));
    }
  }
   return shardNames;
}

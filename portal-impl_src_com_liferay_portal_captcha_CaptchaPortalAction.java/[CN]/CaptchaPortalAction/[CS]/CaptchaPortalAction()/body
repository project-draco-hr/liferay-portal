{
  Properties props=ExtPropertiesLoader.getInstance(getClass().getClassLoader(),PropsFiles.CAPTCHA).getProperties();
  _producer=(CaptchaProducer)Helper.ThingFactory.loadImpl(Helper.ThingFactory.CPROD,props);
}

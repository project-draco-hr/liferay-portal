{
  request.setAttribute("liferay-portlet:preview:portletName",portletName);
  request.setAttribute("liferay-portlet:preview:queryString",queryString);
  request.setAttribute("liferay-portlet:preview:width",width);
  RequestDispatcher rd=servletContext.getRequestDispatcher(page);
  rd.include(request,response);
}

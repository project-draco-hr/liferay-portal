{
  SourceFormatter sourceFormatter=SourceFormatterUtil.create(false,true,false,false);
  try {
    sourceFormatter.format();
  }
 catch (  MismatchException m) {
    Assert.assertEquals(m.getFile().toString(),m.getFormatted(),m.getOriginal());
  }
}

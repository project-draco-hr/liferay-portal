{
  Session session=null;
  try {
    session=openSession();
    return (BlogsCategory)session.get(BlogsCategoryImpl.class,categoryId);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

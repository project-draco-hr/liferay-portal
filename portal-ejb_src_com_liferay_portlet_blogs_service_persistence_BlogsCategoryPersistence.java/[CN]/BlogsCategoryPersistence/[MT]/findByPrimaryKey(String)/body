{
  BlogsCategory blogsCategory=fetchByPrimaryKey(categoryId);
  if (blogsCategory == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No BlogsCategory exists with the primary key " + categoryId.toString());
    }
    throw new NoSuchCategoryException("No BlogsCategory exists with the primary key " + categoryId.toString());
  }
  return blogsCategory;
}

{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof SearchResult)) {
    return false;
  }
  SearchResult searchResult=(SearchResult)obj;
  return ((_classPK == searchResult.getClassPK()) && _className.equals(searchResult.getClassName()));
}

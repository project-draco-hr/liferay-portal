{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof SearchResult)) {
    return false;
  }
  SearchResult searchResult=(SearchResult)obj;
  if ((_classPK == searchResult.getClassPK()) && _className.equals(searchResult.getClassName())) {
    return true;
  }
  return false;
}

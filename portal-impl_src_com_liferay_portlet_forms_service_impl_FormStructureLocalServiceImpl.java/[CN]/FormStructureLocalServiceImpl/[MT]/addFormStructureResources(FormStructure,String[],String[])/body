{
  resourceLocalService.addModelResources(formStructure.getCompanyId(),formStructure.getGroupId(),formStructure.getUserId(),FormStructure.class.getName(),formStructure.getFormStructureId(),communityPermissions,guestPermissions);
}

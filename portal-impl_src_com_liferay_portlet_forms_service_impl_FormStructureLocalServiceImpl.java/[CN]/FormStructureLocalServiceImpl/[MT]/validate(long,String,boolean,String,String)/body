{
  if (!autoStructureId) {
    validateFormStructureId(formStructureId);
    FormStructure structure=formStructurePersistence.fetchByG_F(groupId,formStructureId);
    if (structure != null) {
      throw new FormDuplicateStructureIdException();
    }
  }
  validate(groupId,name,xsd);
}

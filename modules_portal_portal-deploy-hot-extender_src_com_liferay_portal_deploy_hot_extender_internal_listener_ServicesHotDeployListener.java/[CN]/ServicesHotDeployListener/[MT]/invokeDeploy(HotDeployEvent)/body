{
  ModuleHotDeployEvent moduleHotDeployEvent=(ModuleHotDeployEvent)hotDeployEvent;
  if (!moduleHotDeployEvent.hasBundleHeader("Liferay-Service")) {
    return;
  }
  super.invokeDeploy(hotDeployEvent);
  try {
    _processServiceResourceActions(hotDeployEvent);
  }
 catch (  SystemException se) {
    throw new HotDeployException("Error processing service resource actions for " + hotDeployEvent.getServletContextName(),se);
  }
}

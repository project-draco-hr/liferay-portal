{
  Assert.assertSame(Collections.emptySet(),CollectionUtil.intersect(new ArrayList<String>(),Arrays.asList("a")));
  Assert.assertSame(Collections.emptySet(),CollectionUtil.intersect(Arrays.asList("a"),new ArrayList<String>()));
}

{
  Integer type=ServiceComponentModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ServiceComponentModelImpl.TABLE_NAME);
  }
  return type;
}

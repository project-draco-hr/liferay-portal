{
  if (isEscapedModel()) {
    return (LayoutSetPrototype)this;
  }
 else {
    return (LayoutSetPrototype)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

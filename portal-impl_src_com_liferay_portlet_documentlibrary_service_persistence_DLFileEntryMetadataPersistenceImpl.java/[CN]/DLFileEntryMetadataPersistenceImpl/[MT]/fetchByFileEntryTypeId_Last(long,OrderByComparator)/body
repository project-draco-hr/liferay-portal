{
  int count=countByFileEntryTypeId(fileEntryTypeId);
  List<DLFileEntryMetadata> list=findByFileEntryTypeId(fileEntryTypeId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

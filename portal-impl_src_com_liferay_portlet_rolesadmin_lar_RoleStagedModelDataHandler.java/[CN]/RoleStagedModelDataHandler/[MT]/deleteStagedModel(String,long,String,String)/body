{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  Role role=RoleLocalServiceUtil.fetchRoleByUuidAndCompanyId(uuid,group.getCompanyId());
  if (role != null) {
    RoleLocalServiceUtil.deleteRole(role);
  }
}

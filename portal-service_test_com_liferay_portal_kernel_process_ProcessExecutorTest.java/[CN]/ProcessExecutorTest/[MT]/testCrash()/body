{
  KillJVMProcessCallable killJVMProcessCallable=new KillJVMProcessCallable(-1);
  try {
    ProcessExecutor.execute(killJVMProcessCallable,_classPath);
    fail();
  }
 catch (  ProcessException pe) {
  }
  killJVMProcessCallable=new KillJVMProcessCallable(0);
  Serializable result=ProcessExecutor.execute(killJVMProcessCallable,_classPath);
  assertNull(result);
}

{
  Field field=ReflectionUtil.getDeclaredField(ProcessContext.class,"_attachThreadReference");
  AtomicReference<? extends Thread> threadReference=(AtomicReference<? extends Thread>)field.get(null);
  if (remove) {
    return threadReference.getAndSet(null);
  }
 else {
    return threadReference.get();
  }
}

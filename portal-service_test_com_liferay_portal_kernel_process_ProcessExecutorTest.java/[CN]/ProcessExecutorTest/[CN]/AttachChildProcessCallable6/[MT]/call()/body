{
  try {
    boolean result=ProcessContext.attach("Child Process",100,new NPEOOSShutdownHook());
    if (!result || !ProcessContext.isAttached()) {
      throw new ProcessException("Attach failed!");
    }
    super.call();
  }
 catch (  Exception e) {
    throw new ProcessException(e);
  }
  return null;
}

{
  try (LoggingTimer loggingTimer=new LoggingTimer()){
    runSQL("alter table BlogsEntry drop column draft");
    alter(BlogsEntryTable.class,new AlterColumnType("smallImageURL","STRING null"));
  }
 catch (  Exception e) {
  }
}

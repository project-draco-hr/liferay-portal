{
  List<MBMessageModel> mbMessageModels=new ArrayList<MBMessageModel>(maxMessageCount);
  for (int i=1; i <= maxMessageCount; i++) {
    mbMessageModels.add(newMBMessageModel(mbThreadModel,classNameId,classPK,i));
  }
  return mbMessageModels;
}

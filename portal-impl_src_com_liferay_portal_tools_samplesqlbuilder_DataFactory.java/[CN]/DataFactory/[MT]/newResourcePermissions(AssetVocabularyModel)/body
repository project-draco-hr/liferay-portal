{
  if (assetVocabulary.getUserId() == _defaultUserId) {
    List<ResourcePermissionModel> resourcePermissions=new ArrayList<ResourcePermissionModel>(1);
    resourcePermissions.add(newResourcePermission(AssetVocabulary.class.getName(),String.valueOf(assetVocabulary.getVocabularyId()),_ownerRole.getRoleId(),_defaultUserId));
    return resourcePermissions;
  }
  return newResourcePermissions(AssetVocabulary.class.getName(),String.valueOf(assetVocabulary.getVocabularyId()),_sampleUserId);
}

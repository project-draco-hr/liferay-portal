{
  List<BlogsEntryModel> blogEntryModels=new ArrayList<BlogsEntryModel>(_maxBlogsEntryCount);
  for (int i=1; i <= _maxBlogsEntryCount; i++) {
    blogEntryModels.add(newBlogsEntryModel(groupId,i));
  }
  return blogEntryModels;
}

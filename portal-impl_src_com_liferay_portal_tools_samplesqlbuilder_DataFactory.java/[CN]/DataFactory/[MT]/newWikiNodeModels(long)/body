{
  List<WikiNodeModel> wikiNodeModels=new ArrayList<WikiNodeModel>(_maxWikiNodeCount);
  for (int i=1; i <= _maxWikiNodeCount; i++) {
    wikiNodeModels.add(newWikiNodeModel(groupId,i));
  }
  return wikiNodeModels;
}

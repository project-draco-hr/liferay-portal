{
  List<DLFolderModel> dlFolderModels=new ArrayList<DLFolderModel>(_maxDLFolderCount);
  for (int i=1; i <= _maxDLFolderCount; i++) {
    dlFolderModels.add(newDLFolderModel(groupId,parentFolderId,i));
  }
  return dlFolderModels;
}

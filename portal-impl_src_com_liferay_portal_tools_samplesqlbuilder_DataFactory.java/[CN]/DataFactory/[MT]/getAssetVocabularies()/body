{
  List<AssetVocabulary> mergedAssetVocabularies=new ArrayList<AssetVocabulary>();
  mergedAssetVocabularies.add(_defaultAssetVocabulary);
  for (  List<AssetVocabulary> assetVocabularies : _assetVocabulariesArray) {
    mergedAssetVocabularies.addAll(assetVocabularies);
  }
  return mergedAssetVocabularies;
}

{
  GroupModel group=new GroupModelImpl();
  group.setUuid(SequentialUUID.generate());
  group.setGroupId(groupId);
  group.setCompanyId(_companyId);
  group.setCreatorUserId(_sampleUserId);
  group.setClassNameId(classNameId);
  group.setClassPK(classPK);
  group.setTreePath(StringPool.SLASH + group.getGroupId() + StringPool.SLASH);
  group.setName(name);
  group.setFriendlyURL(StringPool.FORWARD_SLASH + FriendlyURLNormalizerUtil.normalize(name));
  group.setSite(site);
  group.setActive(true);
  return group;
}

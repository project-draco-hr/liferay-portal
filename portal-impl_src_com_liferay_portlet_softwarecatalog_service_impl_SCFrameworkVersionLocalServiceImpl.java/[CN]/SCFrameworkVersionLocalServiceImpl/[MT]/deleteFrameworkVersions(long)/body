{
  Iterator itr=scFrameworkVersionPersistence.findByGroupId(groupId).iterator();
  while (itr.hasNext()) {
    SCFrameworkVersion frameworkVersion=(SCFrameworkVersion)itr.next();
    deleteFrameworkVersion(frameworkVersion);
  }
}

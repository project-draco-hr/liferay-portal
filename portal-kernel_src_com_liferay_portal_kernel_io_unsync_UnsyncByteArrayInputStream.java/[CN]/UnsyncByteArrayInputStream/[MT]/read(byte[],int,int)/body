{
  if (len <= 0) {
    return 0;
  }
  int length=len;
  if (index >= capability) {
    return -1;
  }
  if (index + length > capability) {
    length=capability - index;
  }
  System.arraycopy(buffer,index,b,off,length);
  index+=length;
  return length;
}

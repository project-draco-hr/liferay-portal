{
  this.buffer=buffer;
  this.index=offset;
  int expectedCapability=offset + length;
  int physicalCapability=buffer.length;
  this.capability=expectedCapability > physicalCapability ? physicalCapability : expectedCapability;
  this.markIndex=offset;
}

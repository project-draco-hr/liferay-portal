{
  if (_ids == null) {
    _ids=new HashSet<>();
    Settings.Config settingsConfig=_settingsClass.getAnnotation(Settings.Config.class);
    for (    String id : settingsConfig.ids()) {
      _ids.add(id);
    }
  }
  return _ids;
}

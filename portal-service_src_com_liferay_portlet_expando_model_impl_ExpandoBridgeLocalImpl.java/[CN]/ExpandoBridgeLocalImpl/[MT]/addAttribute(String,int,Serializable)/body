{
  try {
    ExpandoTable table=null;
    try {
      table=ExpandoTableLocalServiceUtil.getDefaultTable(super.getClassName());
    }
 catch (    NoSuchTableException nste) {
      table=ExpandoTableLocalServiceUtil.addDefaultTable(super.getClassName());
    }
    ExpandoColumnLocalServiceUtil.addColumn(table.getTableId(),name,type,defaultValue);
  }
 catch (  Exception e) {
    if (e instanceof PortalException) {
      throw (PortalException)e;
    }
 else {
      _log.error(e,e);
    }
  }
}

{
  Query query=null;
  if (like) {
    query=new WildCardQueryImpl(new QueryTerm(field,String.valueOf(value)));
  }
 else {
    query=new TermQueryImpl(new QueryTerm(field,String.valueOf(value)));
  }
  add(query,BooleanClauseOccur.SHOULD);
}

{
  for (  Map.Entry<String,String> entry : _typeMappings.entrySet()) {
    String mappingDefinition=ResourceUtil.getResourceAsString(getClass(),entry.getValue());
    createIndexRequestBuilder.addMapping(entry.getKey(),mappingDefinition);
  }
}

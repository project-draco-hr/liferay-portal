{
  if (!contains(permissionChecker,layoutPrototypeId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),LayoutPrototype.class.getName(),layoutPrototypeId,actionId);
  }
}

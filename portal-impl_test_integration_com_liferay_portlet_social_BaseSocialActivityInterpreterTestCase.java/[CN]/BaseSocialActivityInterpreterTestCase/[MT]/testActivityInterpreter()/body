{
  addActivities();
  long renameTime=System.currentTimeMillis();
  renameEntities();
  if (supportsTrash()) {
    moveEntitiesToTrash();
    checkLinks();
    restoreEntitiesFromTrash();
  }
  checkInterpret(renameTime);
}

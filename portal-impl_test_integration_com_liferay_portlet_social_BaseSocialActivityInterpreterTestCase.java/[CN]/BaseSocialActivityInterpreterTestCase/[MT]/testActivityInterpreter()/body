{
  addActivities();
  long time=System.currentTimeMillis();
  renameEntities();
  if (isSupportsTrash()) {
    moveEntitiesToTrash();
    checkLinks();
    restoreEntitiesFromTrash();
  }
  checkInterpret(time);
}

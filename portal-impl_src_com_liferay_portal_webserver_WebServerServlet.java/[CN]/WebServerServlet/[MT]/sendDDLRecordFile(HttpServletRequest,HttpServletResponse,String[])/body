{
  if (pathArray.length == 3) {
    long recordId=GetterUtil.getLong(pathArray[1]);
    String fieldName=GetterUtil.getString(pathArray[2]);
    DDLUtil.sendRecordFileUpload(request,response,recordId,fieldName);
  }
}

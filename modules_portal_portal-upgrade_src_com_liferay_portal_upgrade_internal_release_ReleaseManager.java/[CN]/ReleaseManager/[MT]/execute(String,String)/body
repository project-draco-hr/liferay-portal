{
  String version=getVersion(componentName);
  ReleaseGraphManager releaseGraphManager=new ReleaseGraphManager(_serviceTrackerMap.getService(componentName));
  executeUpgradeInfos(componentName,releaseGraphManager.getUpgradeInfos(version,to));
}

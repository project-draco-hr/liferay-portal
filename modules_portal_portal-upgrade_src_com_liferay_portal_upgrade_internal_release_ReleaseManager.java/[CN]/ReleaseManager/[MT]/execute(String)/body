{
  ReleaseGraphManager releaseGraphManager=new ReleaseGraphManager(_serviceTrackerMap.getService(bundleSymbolicName));
  String version=getVersion(bundleSymbolicName);
  executeUpgradeInfos(bundleSymbolicName,releaseGraphManager.getUpgradeInfos(version));
}

{
  ReleaseGraphManager releaseGraphManager=new ReleaseGraphManager(_serviceTrackerMap.getService(bundleSymbolicName));
  String versionString=getVersionString(bundleSymbolicName);
  executeUpgradeInfos(bundleSymbolicName,releaseGraphManager.getUpgradeInfos(versionString));
}

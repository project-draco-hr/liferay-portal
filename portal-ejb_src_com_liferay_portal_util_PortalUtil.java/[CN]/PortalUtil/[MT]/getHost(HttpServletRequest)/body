{
  String host=req.getHeader("Host");
  if (host != null) {
    int pos=host.indexOf(':');
    if (pos >= 0) {
      host=host.substring(0,pos);
    }
  }
 else {
    host=null;
  }
  return host;
}

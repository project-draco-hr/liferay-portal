{
  Long companyIdObj=(Long)req.getSession().getAttribute(WebKeys.COMPANY_ID);
  if (companyIdObj == null) {
    companyIdObj=(Long)req.getAttribute(WebKeys.COMPANY_ID);
  }
  if (companyIdObj != null) {
    return companyIdObj.longValue();
  }
 else {
    return 0;
  }
}

{
  for (  DLFileVersionPolicy dlFileVersionPolicy : _serviceTrackerList) {
    if (!dlFileVersionPolicy.isKeepFileVersionLabel(lastDLFileVersion,latestDLFileVersion,serviceContext)) {
      return false;
    }
  }
  return isKeepFileVersionLabel(lastDLFileVersion.getFileEntry(),lastDLFileVersion,latestDLFileVersion,serviceContext);
}

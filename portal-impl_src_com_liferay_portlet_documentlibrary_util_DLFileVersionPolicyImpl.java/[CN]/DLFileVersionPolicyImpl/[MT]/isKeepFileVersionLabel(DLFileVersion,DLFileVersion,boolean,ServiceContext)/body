{
  for (  DLFileVersionPolicy dlFileVersionPolicy : _serviceTrackerList) {
    if ((dlFileVersionPolicy != this) && !dlFileVersionPolicy.isKeepFileVersionLabel(lastDLFileVersion,latestDLFileVersion,majorVersion,serviceContext)) {
      return false;
    }
  }
  return isKeepFileVersionLabel(lastDLFileVersion.getFileEntry(),lastDLFileVersion,latestDLFileVersion,majorVersion,serviceContext);
}

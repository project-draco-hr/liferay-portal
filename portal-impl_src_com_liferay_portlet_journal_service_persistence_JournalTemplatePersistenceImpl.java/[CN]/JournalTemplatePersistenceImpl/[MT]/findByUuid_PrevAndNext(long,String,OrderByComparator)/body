{
  JournalTemplate journalTemplate=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalTemplate[] array=new JournalTemplateImpl[3];
    array[0]=getByUuid_PrevAndNext(session,journalTemplate,uuid,orderByComparator,true);
    array[1]=journalTemplate;
    array[2]=getByUuid_PrevAndNext(session,journalTemplate,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

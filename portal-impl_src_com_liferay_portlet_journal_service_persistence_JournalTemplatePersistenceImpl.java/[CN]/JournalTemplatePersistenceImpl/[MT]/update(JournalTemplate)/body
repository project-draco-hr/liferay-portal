{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalTemplate journalTemplate) method. To improve performance, call update(JournalTemplate journalTemplate, boolean merge).");
  }
  return update(journalTemplate,false);
}

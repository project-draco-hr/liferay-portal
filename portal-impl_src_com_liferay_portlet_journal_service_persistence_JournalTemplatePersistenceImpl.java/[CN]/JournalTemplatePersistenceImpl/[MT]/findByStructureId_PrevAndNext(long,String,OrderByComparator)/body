{
  JournalTemplate journalTemplate=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalTemplate[] array=new JournalTemplateImpl[3];
    array[0]=getByStructureId_PrevAndNext(session,journalTemplate,structureId,orderByComparator,true);
    array[1]=journalTemplate;
    array[2]=getByStructureId_PrevAndNext(session,journalTemplate,structureId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

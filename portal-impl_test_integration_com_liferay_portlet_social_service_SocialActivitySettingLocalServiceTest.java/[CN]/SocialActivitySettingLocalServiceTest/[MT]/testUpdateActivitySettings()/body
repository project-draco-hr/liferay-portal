{
  long classNameId=PortalUtil.getClassNameId(TEST_MODEL);
  boolean enabled=SocialActivitySettingLocalServiceUtil.isEnabled(guestGroup.getGroupId(),classNameId);
  Assert.assertFalse(enabled);
  SocialActivitySettingLocalServiceUtil.updateActivitySetting(guestGroup.getGroupId(),TEST_MODEL,true);
  enabled=SocialActivitySettingLocalServiceUtil.isEnabled(guestGroup.getGroupId(),classNameId);
  Assert.assertTrue(enabled);
}

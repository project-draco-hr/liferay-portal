{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(assetId));
  }
  com.liferay.portlet.tags.model.TagsAsset tagsAsset=getPersistence().remove(assetId);
  if (listener != null) {
    listener.onAfterRemove(tagsAsset);
  }
  return tagsAsset;
}

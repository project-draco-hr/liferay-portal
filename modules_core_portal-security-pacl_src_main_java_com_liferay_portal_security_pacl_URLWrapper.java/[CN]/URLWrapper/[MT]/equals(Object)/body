{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof URLWrapper)) {
    return false;
  }
  URLWrapper urlWrapper=(URLWrapper)obj;
  if ((_url == urlWrapper._url) || Validator.equals(_urlString,urlWrapper._urlString)) {
    return true;
  }
  return false;
}

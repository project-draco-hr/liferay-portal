{
  if (standard) {
    VelocityEngineUtil.flushTemplate("null_STANDARD_" + layoutTemplateId);
    _warStandard.remove(layoutTemplateId);
  }
 else {
    VelocityEngineUtil.flushTemplate("null_CUSTOM_" + layoutTemplateId);
    _warCustom.remove(layoutTemplateId);
  }
}

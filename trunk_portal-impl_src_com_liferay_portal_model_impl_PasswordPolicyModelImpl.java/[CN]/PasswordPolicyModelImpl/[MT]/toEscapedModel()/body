{
  if (isEscapedModel()) {
    return (PasswordPolicy)this;
  }
 else {
    return (PasswordPolicy)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

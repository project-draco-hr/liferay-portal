{
  if (_log.isInfoEnabled()) {
    _log.info("Update C_U_C_C" + " " + companyId.toString() + " "+ userId.toString()+ " "+ className.toString()+ " "+ classPK.toString());
  }
  return _instance._putByC_U_C_C(companyId,userId,className,classPK,pk,true);
}

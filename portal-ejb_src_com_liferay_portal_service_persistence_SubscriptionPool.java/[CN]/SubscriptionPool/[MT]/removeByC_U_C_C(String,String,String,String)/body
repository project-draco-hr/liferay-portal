{
  if (_log.isInfoEnabled()) {
    _log.info("Remove C_U_C_C" + " " + companyId.toString() + " "+ userId.toString()+ " "+ className.toString()+ " "+ classPK.toString());
  }
  return _instance._removeByC_U_C_C(companyId,userId,className,classPK);
}

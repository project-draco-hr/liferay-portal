{
  MethodCache.remove(FormStructureService.class);
  _service=service;
  ReferenceRegistry.registerReference(FormStructureServiceUtil.class,"_service");
  MethodCache.remove(FormStructureService.class);
}

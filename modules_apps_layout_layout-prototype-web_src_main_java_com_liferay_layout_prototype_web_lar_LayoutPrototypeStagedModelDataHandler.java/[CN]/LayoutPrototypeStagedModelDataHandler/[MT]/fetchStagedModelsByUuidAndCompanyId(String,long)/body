{
  List<LayoutPrototype> layoutPrototypes=new ArrayList<>();
  layoutPrototypes.add(_layoutPrototypeLocalService.fetchLayoutPrototypeByUuidAndCompanyId(uuid,companyId));
  return layoutPrototypes;
}

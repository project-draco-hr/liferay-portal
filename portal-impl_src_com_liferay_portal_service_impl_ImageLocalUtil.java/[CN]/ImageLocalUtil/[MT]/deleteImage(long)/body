{
  try {
    ImageLocalServiceUtil.deleteImage(imageId);
  }
 catch (  PortalException pe) {
    if (_log.isWarnEnabled()) {
      _log.warn("Unable to delete image " + imageId + ": "+ pe.getMessage());
    }
  }
}

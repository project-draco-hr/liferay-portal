{
  SettingsFactory settingsFactory=SettingsFactoryUtil.getSettingsFactory();
  for (  String settingsId : settingsIds) {
    settingsFactory.registerFallbackKeys(settingsId,fallbackKeys);
    SettingsConfigurationAction.registerMultiValuedKeys(settingsId,multiValuedKeys);
  }
}

{
  if (!ClusterReplicationThreadLocal.isReplicate()) {
    return;
  }
  _clusterLinkCacheReplicator.notifyEntryUpdated(_getPortalCache(ehcache),(K)element.getObjectKey(),(V)element.getObjectValue(),element.getTimeToLive());
}

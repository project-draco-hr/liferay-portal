{
  try {
    Resource resource=getResource(companyId,name,typeId,scope,primKey);
    deleteResource(resource.getResourceId());
  }
 catch (  NoSuchResourceException nsre) {
    _log.error(nsre);
  }
}

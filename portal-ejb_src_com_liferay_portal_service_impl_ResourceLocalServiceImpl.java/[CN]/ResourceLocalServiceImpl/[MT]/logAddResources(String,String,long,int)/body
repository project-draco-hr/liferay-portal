{
  if (!_log.isDebugEnabled()) {
    return 0;
  }
  long end=System.currentTimeMillis();
  _log.debug("Adding resources block " + block + " for "+ name+ " "+ primKey+ " takes "+ (end - start)+ " ms");
  return end;
}

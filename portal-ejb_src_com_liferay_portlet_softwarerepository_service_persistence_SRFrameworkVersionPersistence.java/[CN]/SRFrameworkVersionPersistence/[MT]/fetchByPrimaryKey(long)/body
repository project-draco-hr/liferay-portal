{
  Session session=null;
  try {
    session=openSession();
    return (SRFrameworkVersion)session.get(SRFrameworkVersionImpl.class,new Long(frameworkVersionId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

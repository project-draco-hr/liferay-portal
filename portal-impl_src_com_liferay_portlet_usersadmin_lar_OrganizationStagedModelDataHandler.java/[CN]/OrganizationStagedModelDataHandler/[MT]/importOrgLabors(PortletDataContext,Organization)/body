{
  String path=getDependentModelPath(organization,OrgLabor.class);
  List<OrgLabor> rawEntries=(List<OrgLabor>)portletDataContext.getZipEntryAsObject(path);
  for (  OrgLabor orgLabor : rawEntries) {
    orgLabor.setOrgLaborId(0);
  }
  return rawEntries;
}

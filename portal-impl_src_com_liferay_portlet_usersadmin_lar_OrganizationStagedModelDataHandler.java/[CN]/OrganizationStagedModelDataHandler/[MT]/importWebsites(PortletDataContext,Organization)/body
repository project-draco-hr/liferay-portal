{
  String path=getDependentModelPath(organization,Website.class);
  List<Website> websites=(List<Website>)portletDataContext.getZipEntryAsObject(path);
  for (  Website website : websites) {
    website.setWebsiteId(0);
  }
  return websites;
}

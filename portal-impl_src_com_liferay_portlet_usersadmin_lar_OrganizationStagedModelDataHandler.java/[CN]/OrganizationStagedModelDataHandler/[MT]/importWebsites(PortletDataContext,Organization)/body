{
  String path=getDependentModelPath(organization,Website.class);
  List<Website> rawEntries=(List<Website>)portletDataContext.getZipEntryAsObject(path);
  for (  Website website : rawEntries) {
    website.setWebsiteId(0);
  }
  return rawEntries;
}

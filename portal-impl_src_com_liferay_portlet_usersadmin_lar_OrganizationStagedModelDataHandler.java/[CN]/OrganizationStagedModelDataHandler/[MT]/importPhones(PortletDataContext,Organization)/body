{
  String path=getDependentModelPath(organization,Phone.class);
  List<Phone> phones=(List<Phone>)portletDataContext.getZipEntryAsObject(path);
  for (  Phone phone : phones) {
    phone.setPhoneId(0);
  }
  return phones;
}

{
  String path=getDependentModelPath(organization,Phone.class);
  List<Phone> rawEntries=(List<Phone>)portletDataContext.getZipEntryAsObject(path);
  for (  Phone phone : rawEntries) {
    phone.setPhoneId(0);
  }
  return rawEntries;
}

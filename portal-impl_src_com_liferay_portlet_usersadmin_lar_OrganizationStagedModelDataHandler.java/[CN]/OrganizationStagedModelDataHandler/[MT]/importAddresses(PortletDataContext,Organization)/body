{
  String path=getDependentModelPath(organization,Address.class);
  List<Address> rawEntries=(List<Address>)portletDataContext.getZipEntryAsObject(path);
  for (  Address address : rawEntries) {
    address.setAddressId(0);
  }
  return rawEntries;
}

{
  super.setContentType(contentType);
  if (contentType != null) {
    if (contentType.equals(ContentTypes.APPLICATION_GZIP) || contentType.equals(ContentTypes.APPLICATION_X_GZIP)) {
      _gZipContentType=true;
    }
  }
}

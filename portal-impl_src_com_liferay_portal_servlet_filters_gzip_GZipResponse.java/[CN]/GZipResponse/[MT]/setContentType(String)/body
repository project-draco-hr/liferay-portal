{
  super.setContentType(contentType);
  if (Validator.equals(contentType,ContentTypes.APPLICATION_GZIP) || Validator.equals(contentType,ContentTypes.APPLICATION_X_GZIP)) {
    _contentTypeGzip=true;
  }
}

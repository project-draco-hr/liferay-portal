{
  if (_cmisFolder.isRootFolder()) {
    try {
      DLFolder dlFolder=DLFolderLocalServiceUtil.getFolderByRepositoryId(getRepositoryId());
      return dlFolder.getName();
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
  return _cmisFolder.getName();
}

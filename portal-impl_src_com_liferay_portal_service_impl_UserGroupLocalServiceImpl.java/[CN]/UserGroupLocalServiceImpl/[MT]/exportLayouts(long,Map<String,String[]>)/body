{
  File[] files=new File[2];
  UserGroup userGroup=userGroupPersistence.findByPrimaryKey(userGroupId);
  Group group=userGroup.getGroup();
  if (userGroup.hasPrivateLayouts()) {
    files[0]=layoutLocalService.exportLayoutsAsFile(group.getGroupId(),true,null,parameterMap,null,null);
  }
  if (userGroup.hasPublicLayouts()) {
    files[1]=layoutLocalService.exportLayoutsAsFile(group.getGroupId(),false,null,parameterMap,null,null);
  }
  return files;
}

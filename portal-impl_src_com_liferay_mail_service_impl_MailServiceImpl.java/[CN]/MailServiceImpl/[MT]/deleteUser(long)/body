{
  if (_log.isDebugEnabled()) {
    _log.debug("deleteUser");
  }
  MethodWrapper methodWrapper=new MethodWrapper(PropsValues.MAIL_HOOK_IMPL,"deleteUser",new Object[]{new LongWrapper(userId)});
  MessageBusUtil.sendMessage(DestinationNames.MAIL,methodWrapper);
}

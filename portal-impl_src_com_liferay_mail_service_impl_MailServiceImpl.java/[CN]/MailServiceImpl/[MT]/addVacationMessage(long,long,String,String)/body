{
  if (_log.isDebugEnabled()) {
    _log.debug("addVacationMessage");
  }
  MethodWrapper methodWrapper=new MethodWrapper(PropsValues.MAIL_HOOK_IMPL,"addVacationMessage",new Object[]{new LongWrapper(companyId),new LongWrapper(userId),emailAddress,vacationMessage});
  MessageBusUtil.sendMessage(DestinationNames.MAIL,methodWrapper);
}

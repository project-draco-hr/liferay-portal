{
  if (_log.isDebugEnabled()) {
    _log.debug("updatePassword");
  }
  MethodWrapper methodWrapper=new MethodWrapper(PropsValues.MAIL_HOOK_IMPL,"updatePassword",new Object[]{new LongWrapper(userId),password});
  Message message=new Message(MessageTypes.MAIL_MESSAGE);
  message.setPayload(methodWrapper);
  MessageBusUtil.sendMessage(DestinationNames.MAIL,message);
}

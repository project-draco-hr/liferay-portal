{
  if (_log.isDebugEnabled()) {
    _log.debug("addForward");
  }
  MethodWrapper methodWrapper=new MethodWrapper(PropsValues.MAIL_HOOK_IMPL,"addForward",new Object[]{new LongWrapper(companyId),new LongWrapper(userId),filters,emailAddresses,new BooleanWrapper(leaveCopy)});
  MessageBusUtil.sendMessage(DestinationNames.MAIL,methodWrapper);
}

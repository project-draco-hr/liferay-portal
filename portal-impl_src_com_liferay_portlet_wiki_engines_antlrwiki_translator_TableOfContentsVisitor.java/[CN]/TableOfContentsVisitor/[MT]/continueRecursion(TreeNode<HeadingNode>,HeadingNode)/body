{
  List<TreeNode<HeadingNode>> children=treeNode.getChildNodes();
  if ((headingNode.getLevel() > treeNode.getValue().getLevel()) && (children != null) && (children.size() > 0)) {
    return true;
  }
  return false;
}

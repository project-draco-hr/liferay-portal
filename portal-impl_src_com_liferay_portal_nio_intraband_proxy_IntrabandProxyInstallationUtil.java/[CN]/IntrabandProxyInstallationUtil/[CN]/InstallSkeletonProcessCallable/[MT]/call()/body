{
  ClassLoader classLoader=ClassLoaderPool.getClassLoader(_servletContextName);
  try {
    return installSkeleton(classLoader,classLoader.loadClass(_skeletonId),_targetLocator);
  }
 catch (  Exception e) {
    throw new ProcessException(e);
  }
}

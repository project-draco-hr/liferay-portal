{
  Document document=getDocument(wikiNode);
  if (!wikiNode.isInTrash()) {
    SearchEngineUtil.deleteDocument(getSearchEngineId(),wikiNode.getCompanyId(),document.get(Field.UID),isCommitImmediately());
    return;
  }
  SearchEngineUtil.updateDocument(getSearchEngineId(),wikiNode.getCompanyId(),document,isCommitImmediately());
}

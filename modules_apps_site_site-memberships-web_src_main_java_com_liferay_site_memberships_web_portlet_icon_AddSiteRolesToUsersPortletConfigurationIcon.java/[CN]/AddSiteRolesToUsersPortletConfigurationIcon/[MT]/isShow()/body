{
  try {
    return GroupPermissionUtil.contains(themeDisplay.getPermissionChecker(),themeDisplay.getScopeGroup(),ActionKeys.ASSIGN_USER_ROLES);
  }
 catch (  PortalException e) {
  }
  return false;
}

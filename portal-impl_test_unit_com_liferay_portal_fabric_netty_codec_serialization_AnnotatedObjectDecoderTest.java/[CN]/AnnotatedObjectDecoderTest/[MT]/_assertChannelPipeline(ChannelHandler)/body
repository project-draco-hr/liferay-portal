{
  Map<String,ChannelHandler> map=_innerChannelPipeline.toMap();
  Assert.assertEquals(channelHandlers.length,map.size());
  Assert.assertEquals(Arrays.asList(channelHandlers),new ArrayList<ChannelHandler>(map.values()));
}

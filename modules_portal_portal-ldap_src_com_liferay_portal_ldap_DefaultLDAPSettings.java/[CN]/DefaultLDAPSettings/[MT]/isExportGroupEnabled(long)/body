{
  boolean defaultLDAPExportGroupEnabled=_ldapConfiguration.exportGroupEnabled();
  if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.LDAP_EXPORT_GROUP_ENABLED,defaultLDAPExportGroupEnabled)) {
    return true;
  }
 else {
    return false;
  }
}

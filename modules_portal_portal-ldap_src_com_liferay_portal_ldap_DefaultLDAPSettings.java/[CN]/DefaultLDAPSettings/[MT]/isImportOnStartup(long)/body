{
  LDAPConfiguration ldapCompanyServiceSettings=LDAPConfigurationUtil.getLDAPConfiguration(companyId);
  if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.LDAP_IMPORT_ON_STARTUP,ldapCompanyServiceSettings.importOnStartup())) {
    return true;
  }
 else {
    return false;
  }
}

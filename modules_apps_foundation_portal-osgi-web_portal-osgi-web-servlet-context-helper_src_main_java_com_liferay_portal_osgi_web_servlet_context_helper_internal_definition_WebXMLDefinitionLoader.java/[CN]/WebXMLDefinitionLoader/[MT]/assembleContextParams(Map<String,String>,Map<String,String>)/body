{
  for (  Entry<String,String> entry : fragmentContextParameters.entrySet()) {
    String paramName=entry.getKey();
    if (!assembledContextParameters.containsKey(paramName)) {
      assembledContextParameters.put(paramName,entry.getValue());
    }
  }
}

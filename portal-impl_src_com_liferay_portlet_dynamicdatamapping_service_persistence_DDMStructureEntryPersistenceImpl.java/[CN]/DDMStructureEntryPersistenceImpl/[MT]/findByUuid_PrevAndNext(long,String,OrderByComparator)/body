{
  DDMStructureEntry ddmStructureEntry=findByPrimaryKey(structureEntryId);
  Session session=null;
  try {
    session=openSession();
    DDMStructureEntry[] array=new DDMStructureEntryImpl[3];
    array[0]=getByUuid_PrevAndNext(session,ddmStructureEntry,uuid,orderByComparator,true);
    array[1]=ddmStructureEntry;
    array[2]=getByUuid_PrevAndNext(session,ddmStructureEntry,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

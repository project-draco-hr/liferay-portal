{
  Map<String,DDMFormField> fieldsMap=new HashMap<String,DDMFormField>();
  for (  DDMFormField nestedField : _nestedFields) {
    fieldsMap.put(nestedField.getName(),nestedField);
    fieldsMap.putAll(nestedField.getNestedFieldsMap());
  }
  return fieldsMap;
}

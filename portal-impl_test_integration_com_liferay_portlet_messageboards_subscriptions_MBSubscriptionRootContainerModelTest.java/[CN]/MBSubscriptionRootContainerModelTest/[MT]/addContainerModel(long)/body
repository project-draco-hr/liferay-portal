{
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(group.getGroupId(),TestPropsValues.getUserId());
  MBCategory category=MBCategoryServiceUtil.addCategory(TestPropsValues.getUserId(),containerModelId,RandomTestUtil.randomString(),StringPool.BLANK,serviceContext);
  return category.getCategoryId();
}

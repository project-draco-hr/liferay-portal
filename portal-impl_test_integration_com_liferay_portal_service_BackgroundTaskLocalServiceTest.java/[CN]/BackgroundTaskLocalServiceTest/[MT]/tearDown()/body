{
  MessageBus messageBus=MessageBusUtil.getMessageBus();
  Destination destination=messageBus.getDestination(DestinationNames.BACKGROUND_TASK);
  _destination.copyMessageListeners(destination);
  BackgroundTaskLocalServiceUtil.deleteGroupBackgroundTasks(_group.getGroupId());
  UserLocalServiceUtil.deleteUser(_user);
  GroupLocalServiceUtil.deleteGroup(_group);
}

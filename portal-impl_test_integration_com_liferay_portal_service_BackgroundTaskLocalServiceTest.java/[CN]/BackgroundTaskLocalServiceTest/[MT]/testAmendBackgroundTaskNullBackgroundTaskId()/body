{
  ServiceContext serviceContext=new ServiceContext();
  BackgroundTask amendBackgroundTask=BackgroundTaskLocalServiceUtil.amendBackgroundTask(Long.MIN_VALUE,null,BackgroundTaskConstants.STATUS_NEW,serviceContext);
  Assert.assertNull(amendBackgroundTask);
}

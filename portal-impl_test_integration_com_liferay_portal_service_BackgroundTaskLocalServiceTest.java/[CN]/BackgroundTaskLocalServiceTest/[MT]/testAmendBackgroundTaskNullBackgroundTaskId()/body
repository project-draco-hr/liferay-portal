{
  ServiceContext serviceContext=new ServiceContext();
  BackgroundTask amendBackgroundTask=BackgroundTaskLocalServiceUtil.amendBackgroundTask(33L,null,0,serviceContext);
  Assert.assertNull(amendBackgroundTask);
}

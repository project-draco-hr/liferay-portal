{
  try {
    doInvokeDeploy(hotDeployEvent);
  }
 catch (  Throwable t) {
    throwHotDeployException(hotDeployEvent,"Error registering theme loader for " + hotDeployEvent.getServletContextName(),t);
  }
}

{
  PortletBag portletBag=PortletBagPool.get(getRootPortletId());
  List<Method> xmlRpcMethodInstances=portletBag.getXmlRpcMethodInstances();
  if (xmlRpcMethodInstances.isEmpty()) {
    return null;
  }
  return xmlRpcMethodInstances.get(0);
}

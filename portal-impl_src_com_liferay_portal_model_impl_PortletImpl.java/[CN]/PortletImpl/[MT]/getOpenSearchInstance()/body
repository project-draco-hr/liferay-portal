{
  PortletBag portletBag=PortletBagPool.get(getRootPortletId());
  List<OpenSearch> openSearchInstances=portletBag.getOpenSearchInstances();
  if (openSearchInstances.isEmpty()) {
    return null;
  }
  return openSearchInstances.get(0);
}

{
  if (_timestamp == null) {
    ServletContext servletContext=ServletContextPool.get(getContextName());
    _timestamp=ServletContextUtil.getLastModified(servletContext,StringPool.SLASH,true);
  }
  return _timestamp;
}

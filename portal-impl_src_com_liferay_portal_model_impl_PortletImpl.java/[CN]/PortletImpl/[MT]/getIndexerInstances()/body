{
  if (_indexerClasses.isEmpty() && !_portletClass.equals(AlloyPortlet.class.getName())) {
    return Collections.emptyList();
  }
  PortletBag portletBag=PortletBagPool.get(getRootPortletId());
  return portletBag.getIndexerInstances();
}

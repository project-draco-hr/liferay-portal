{
  PortletBag portletBag=PortletBagPool.get(getRootPortletId());
  List<URLEncoder> urlEncoderInstances=portletBag.getURLEncoderInstances();
  if (urlEncoderInstances.isEmpty()) {
    return null;
  }
  return urlEncoderInstances.get(0);
}

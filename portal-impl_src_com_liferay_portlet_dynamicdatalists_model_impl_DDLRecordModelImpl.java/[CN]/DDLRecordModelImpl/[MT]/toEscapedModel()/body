{
  if (isEscapedModel()) {
    return (DDLRecord)this;
  }
 else {
    return (DDLRecord)Proxy.newProxyInstance(DDLRecord.class.getClassLoader(),new Class[]{DDLRecord.class},new AutoEscapeBeanHandler(this));
  }
}

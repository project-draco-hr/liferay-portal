{
  if (ArrayUtil.contains(ORDER_BY_COLUMNS,orderByCol) || ((orderByCol != null) && orderByCol.startsWith(DDMIndexer.DDM_FIELD_NAMESPACE + StringPool.DOUBLE_UNDERLINE))) {
    return orderByCol;
  }
  return ORDER_BY_COLUMNS[2];
}

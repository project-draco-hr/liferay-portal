{
  if (ArrayUtil.contains(ORDER_BY_COLUMNS,orderByCol) || orderByCol.startsWith(DDMIndexer.DDM_FIELD_NAMESPACE + StringPool.FORWARD_SLASH)) {
    return orderByCol;
  }
  return ORDER_BY_COLUMNS[2];
}

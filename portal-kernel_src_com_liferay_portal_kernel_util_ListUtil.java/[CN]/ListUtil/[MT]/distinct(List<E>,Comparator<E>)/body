{
  if ((list == null) || list.isEmpty()) {
    return;
  }
  Set<E> set=null;
  if (comparator == null) {
    set=new TreeSet<E>();
  }
 else {
    set=new TreeSet<E>(comparator);
  }
  Iterator<E> itr=list.iterator();
  while (itr.hasNext()) {
    E obj=itr.next();
    if (set.contains(obj)) {
      itr.remove();
    }
 else {
      set.add(obj);
    }
  }
}

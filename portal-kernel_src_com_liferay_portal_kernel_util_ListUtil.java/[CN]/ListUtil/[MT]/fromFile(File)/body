{
  List<String> list=new ArrayList<String>();
  UnsyncBufferedReader br=new UnsyncBufferedReader(new FileReader(file));
  String s=StringPool.BLANK;
  while ((s=br.readLine()) != null) {
    list.add(s);
  }
  br.close();
  return list;
}

{
  if (isEmpty(list)) {
    return;
  }
  Set<E> set=new HashSet<>();
  Iterator<? extends E> itr=list.iterator();
  while (itr.hasNext()) {
    E obj=itr.next();
    if (!set.add(obj)) {
      itr.remove();
    }
  }
  if (comparator != null) {
    Collections.sort(list,comparator);
  }
}

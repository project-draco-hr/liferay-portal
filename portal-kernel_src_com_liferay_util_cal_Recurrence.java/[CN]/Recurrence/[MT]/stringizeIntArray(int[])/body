{
  if (a == null) {
    return "null";
  }
  StringMaker sm=new StringMaker();
  sm.append("[");
  for (int i=0; i < a.length; i++) {
    if (i != 0) {
      sm.append(",");
    }
    sm.append(a[i]);
  }
  sm.append("]");
  return sm.toString();
}

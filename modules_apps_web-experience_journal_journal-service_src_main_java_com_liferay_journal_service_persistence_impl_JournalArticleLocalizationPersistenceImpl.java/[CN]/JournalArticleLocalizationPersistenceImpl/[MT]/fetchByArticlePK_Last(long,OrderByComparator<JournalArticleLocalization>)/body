{
  int count=countByArticlePK(articlePK);
  if (count == 0) {
    return null;
  }
  List<JournalArticleLocalization> list=findByArticlePK(articlePK,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

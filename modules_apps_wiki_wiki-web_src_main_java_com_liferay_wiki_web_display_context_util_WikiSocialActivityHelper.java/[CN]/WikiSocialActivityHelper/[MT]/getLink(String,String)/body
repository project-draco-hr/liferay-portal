{
  if (Validator.isNull(url)) {
    return title;
  }
  StringBundler sb=new StringBundler(5);
  sb.append("<a href='");
  sb.append(url);
  sb.append("'>");
  sb.append(title);
  sb.append("</a>");
  return sb.toString();
}

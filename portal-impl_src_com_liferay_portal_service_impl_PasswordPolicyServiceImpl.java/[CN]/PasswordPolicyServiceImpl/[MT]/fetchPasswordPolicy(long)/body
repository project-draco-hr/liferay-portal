{
  PasswordPolicy passwordPolicy=passwordPolicyLocalService.fetchPasswordPolicy(passwordPolicyId);
  if (passwordPolicy != null) {
    PasswordPolicyPermissionUtil.check(getPermissionChecker(),passwordPolicyId,ActionKeys.VIEW);
  }
  return passwordPolicy;
}

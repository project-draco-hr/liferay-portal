{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  ExpandoTable newExpandoTable=addExpandoTable();
  _persistence.clearCache();
  ExpandoTableModelImpl existingExpandoTableModelImpl=(ExpandoTableModelImpl)_persistence.findByPrimaryKey(newExpandoTable.getPrimaryKey());
  Assert.assertEquals(existingExpandoTableModelImpl.getCompanyId(),existingExpandoTableModelImpl.getOriginalCompanyId());
  Assert.assertEquals(existingExpandoTableModelImpl.getClassNameId(),existingExpandoTableModelImpl.getOriginalClassNameId());
  Assert.assertTrue(Validator.equals(existingExpandoTableModelImpl.getName(),existingExpandoTableModelImpl.getOriginalName()));
}

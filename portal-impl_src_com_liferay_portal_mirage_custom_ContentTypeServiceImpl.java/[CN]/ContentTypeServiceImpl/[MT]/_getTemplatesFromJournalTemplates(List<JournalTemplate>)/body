{
  if (journalTemplates == null) {
    return null;
  }
  List<Template> templates=new ArrayList<Template>(journalTemplates.size());
  for (  JournalTemplate template : journalTemplates) {
    templates.add(new JournalMirageTemplate(template));
  }
  return templates;
}

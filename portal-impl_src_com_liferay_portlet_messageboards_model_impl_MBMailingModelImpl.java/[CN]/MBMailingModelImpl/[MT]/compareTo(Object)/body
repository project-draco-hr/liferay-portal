{
  if (obj == null) {
    return -1;
  }
  MBMailingImpl mbMailing=(MBMailingImpl)obj;
  int value=0;
  if (getCategoryId() < mbMailing.getCategoryId()) {
    value=-1;
  }
 else   if (getCategoryId() > mbMailing.getCategoryId()) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  value=getMailingListAddress().toLowerCase().compareTo(mbMailing.getMailingListAddress().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

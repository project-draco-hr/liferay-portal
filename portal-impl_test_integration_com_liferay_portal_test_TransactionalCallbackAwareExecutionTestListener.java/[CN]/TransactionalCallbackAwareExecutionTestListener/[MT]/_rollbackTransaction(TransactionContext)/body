{
  TransactionalPortalCacheHelper.commit();
  List<Callable<?>> callables=TransactionCommitCallbackTestUtil.popCallbackList();
  for (  Callable<?> callable : callables) {
    try {
      callable.call();
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
  super._rollbackTransaction(transactionContext);
}

{
  UserGroupPermission.check(getPermissionChecker(),groupId,userGroupId,ActionKeys.UPDATE);
  return UserGroupLocalServiceUtil.updateUserGroup(getUser().getCompanyId(),userGroupId,name,description);
}

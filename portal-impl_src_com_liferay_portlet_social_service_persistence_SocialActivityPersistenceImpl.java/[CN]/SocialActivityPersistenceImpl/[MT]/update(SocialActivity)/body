{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SocialActivity socialActivity) method. Use update(SocialActivity socialActivity, boolean merge) instead.");
  }
  return update(socialActivity,false);
}

{
  int count=countByG_P_S(groupId,parentFolderId,status);
  if (count == 0) {
    return null;
  }
  List<JournalFolder> list=findByG_P_S(groupId,parentFolderId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

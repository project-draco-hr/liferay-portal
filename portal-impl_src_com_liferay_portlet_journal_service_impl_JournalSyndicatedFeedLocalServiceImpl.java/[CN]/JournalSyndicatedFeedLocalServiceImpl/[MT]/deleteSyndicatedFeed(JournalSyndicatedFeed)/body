{
  resourceLocalService.deleteResource(synFeed.getCompanyId(),JournalSyndicatedFeed.class.getName(),ResourceImpl.SCOPE_INDIVIDUAL,synFeed.getId());
  journalSyndicatedFeedPersistence.remove(synFeed.getPrimaryKey());
}

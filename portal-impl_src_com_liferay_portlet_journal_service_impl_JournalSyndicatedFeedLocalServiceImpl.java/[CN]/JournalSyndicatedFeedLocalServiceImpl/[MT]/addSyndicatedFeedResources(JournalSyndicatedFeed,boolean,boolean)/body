{
  resourceLocalService.addResources(synFeed.getCompanyId(),synFeed.getGroupId(),synFeed.getUserId(),JournalSyndicatedFeed.class.getName(),synFeed.getId(),false,addCommunityPermissions,addGuestPermissions);
}

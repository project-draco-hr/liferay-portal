{
  resourceLocalService.addModelResources(synFeed.getCompanyId(),synFeed.getGroupId(),synFeed.getUserId(),JournalSyndicatedFeed.class.getName(),synFeed.getId(),communityPermissions,guestPermissions);
}

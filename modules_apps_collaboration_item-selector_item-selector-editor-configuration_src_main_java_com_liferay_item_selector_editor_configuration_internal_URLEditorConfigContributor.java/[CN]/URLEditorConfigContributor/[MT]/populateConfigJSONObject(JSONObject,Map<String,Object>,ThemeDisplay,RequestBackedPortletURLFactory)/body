{
  PortletURL itemSelectorURL=getItemSelectorPortletURL(inputEditorTaglibAttributes,requestBackedPortletURLFactory,new URLItemSelectorCriterion());
  if (itemSelectorURL != null) {
    jsonObject.put("filebrowserBrowseUrl",itemSelectorURL.toString());
  }
}

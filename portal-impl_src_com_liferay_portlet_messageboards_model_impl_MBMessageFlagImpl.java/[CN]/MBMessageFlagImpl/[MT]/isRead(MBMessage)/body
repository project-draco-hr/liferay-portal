{
  Calendar lastRead=Calendar.getInstance();
  lastRead.setTime(getModifiedDate());
  lastRead.add(Calendar.SECOND,_MARGIN_SECONDS);
  if ((DateUtil.compareTo(lastRead.getTime(),message.getModifiedDate()) == -1)) {
    return false;
  }
  return true;
}

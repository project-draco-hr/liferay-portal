{
  if (longWrapper == null) {
    return 1;
  }
  if (getValue() > longWrapper.getValue()) {
    return 1;
  }
 else   if (getValue() < longWrapper.getValue()) {
    return -1;
  }
 else {
    return 0;
  }
}

{
  ShoppingItemField shoppingItemField=_instance._get(itemFieldId);
  _log.info("Get " + itemFieldId + " is "+ ((shoppingItemField == null) ? "NOT " : "")+ "in cache");
  return shoppingItemField;
}

{
  Document doc=getUserDocument(companyId,userId,firstName,middleName,lastName,screenName,emailAddress,jobTitle,active,organizationIds,expandoBridge);
  SearchEngineUtil.addDocument(companyId,doc);
}

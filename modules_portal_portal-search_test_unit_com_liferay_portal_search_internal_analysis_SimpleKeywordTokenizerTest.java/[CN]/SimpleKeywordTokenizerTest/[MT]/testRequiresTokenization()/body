{
  SimpleKeywordTokenizer simpleKeywordTokenizer=new SimpleKeywordTokenizer();
  Assert.assertFalse(simpleKeywordTokenizer.requiresTokenization("This is a simple test"));
  Assert.assertFalse(simpleKeywordTokenizer.requiresTokenization("This is a simple test"));
}

{
  ensureOpen();
  DatagramReceiveHandler[] datagramReceiveHandlers=datagramReceiveHandlersReference.get();
  DatagramReceiveHandler[] copyDatagramReceiveHandlers=new DatagramReceiveHandler[256];
  System.arraycopy(datagramReceiveHandlers,0,copyDatagramReceiveHandlers,0,256);
  return copyDatagramReceiveHandlers;
}

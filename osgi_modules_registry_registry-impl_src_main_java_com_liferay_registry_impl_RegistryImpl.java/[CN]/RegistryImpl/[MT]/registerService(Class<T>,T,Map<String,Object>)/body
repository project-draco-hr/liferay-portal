{
  org.osgi.framework.ServiceRegistration<T> serviceRegistration=_bundleContext.registerService(clazz,service,new MapWrapper(map));
  return new ServiceRegistrationWrapper<T>(serviceRegistration);
}

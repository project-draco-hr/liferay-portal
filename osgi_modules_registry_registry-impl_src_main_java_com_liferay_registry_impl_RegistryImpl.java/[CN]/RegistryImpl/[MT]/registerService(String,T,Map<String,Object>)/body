{
  org.osgi.framework.ServiceRegistration<?> serviceRegistration=_bundleContext.registerService(className,service,new MapWrapper(map));
  return new ServiceRegistrationWrapper(serviceRegistration);
}

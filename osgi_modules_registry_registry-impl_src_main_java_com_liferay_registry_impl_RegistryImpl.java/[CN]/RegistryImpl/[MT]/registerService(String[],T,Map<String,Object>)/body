{
  org.osgi.framework.ServiceRegistration<?> serviceRegistration=_bundleContext.registerService(classNames,service,new MapWrapper(map));
  return new ServiceRegistrationWrapper(serviceRegistration);
}

{
  try {
    if (!context.addPrimaryKey(JournalPortletDataHandlerImpl.class,"deleteData")) {
      JournalArticleLocalServiceUtil.deleteArticles(context.getScopeGroupId());
      JournalTemplateLocalServiceUtil.deleteTemplates(context.getScopeGroupId());
      JournalStructureLocalServiceUtil.deleteStructures(context.getScopeGroupId());
    }
    return preferences;
  }
 catch (  Exception e) {
    throw new PortletDataException(e);
  }
}

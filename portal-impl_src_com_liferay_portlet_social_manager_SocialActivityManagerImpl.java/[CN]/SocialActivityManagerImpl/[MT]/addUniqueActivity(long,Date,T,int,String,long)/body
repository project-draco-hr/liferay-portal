{
  SocialActivityManager<T> socialActivityManager=getSocialActivityHandler(model.getModelClassName());
  socialActivityManager.addUniqueActivity(userId,createDate,model,type,extraData,receiverUserId);
}

{
  Integer type=SCProductScreenshotModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SCProductScreenshotModelImpl.TABLE_NAME);
  }
  return type;
}

{
  Object[] args=new Object[]{scProductScreenshotModelImpl.getThumbnailId()};
  finderCache.removeResult(FINDER_PATH_COUNT_BY_THUMBNAILID,args);
  finderCache.removeResult(FINDER_PATH_FETCH_BY_THUMBNAILID,args);
  if ((scProductScreenshotModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_THUMBNAILID.getColumnBitmask()) != 0) {
    args=new Object[]{scProductScreenshotModelImpl.getOriginalThumbnailId()};
    finderCache.removeResult(FINDER_PATH_COUNT_BY_THUMBNAILID,args);
    finderCache.removeResult(FINDER_PATH_FETCH_BY_THUMBNAILID,args);
  }
  args=new Object[]{scProductScreenshotModelImpl.getFullImageId()};
  finderCache.removeResult(FINDER_PATH_COUNT_BY_FULLIMAGEID,args);
  finderCache.removeResult(FINDER_PATH_FETCH_BY_FULLIMAGEID,args);
  if ((scProductScreenshotModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_FULLIMAGEID.getColumnBitmask()) != 0) {
    args=new Object[]{scProductScreenshotModelImpl.getOriginalFullImageId()};
    finderCache.removeResult(FINDER_PATH_COUNT_BY_FULLIMAGEID,args);
    finderCache.removeResult(FINDER_PATH_FETCH_BY_FULLIMAGEID,args);
  }
  args=new Object[]{scProductScreenshotModelImpl.getProductEntryId(),scProductScreenshotModelImpl.getPriority()};
  finderCache.removeResult(FINDER_PATH_COUNT_BY_P_P,args);
  finderCache.removeResult(FINDER_PATH_FETCH_BY_P_P,args);
  if ((scProductScreenshotModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_P_P.getColumnBitmask()) != 0) {
    args=new Object[]{scProductScreenshotModelImpl.getOriginalProductEntryId(),scProductScreenshotModelImpl.getOriginalPriority()};
    finderCache.removeResult(FINDER_PATH_COUNT_BY_P_P,args);
    finderCache.removeResult(FINDER_PATH_FETCH_BY_P_P,args);
  }
}

{
  long rawInterval=GetterUtil.getLong(PropsUtil.get(PropsUtil.JOURNAL_ARTICLES_CHECK_INTERVAL));
  if (_log.isDebugEnabled()) {
    _log.debug("Interval " + rawInterval + " minutes");
  }
  _interval=rawInterval * Time.MINUTE;
}

{
  if (isEscapedModel()) {
    return (VirtualHost)this;
  }
 else {
    return (VirtualHost)Proxy.newProxyInstance(VirtualHost.class.getClassLoader(),new Class[]{VirtualHost.class},new AutoEscapeBeanHandler(this));
  }
}

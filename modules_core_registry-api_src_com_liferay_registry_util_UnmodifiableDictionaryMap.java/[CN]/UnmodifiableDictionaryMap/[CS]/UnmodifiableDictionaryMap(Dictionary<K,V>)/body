{
  Map<K,V> map=new HashMap<K,V>();
  if (dictionary != null) {
    for (Enumeration<K> enumeration=dictionary.keys(); enumeration.hasMoreElements(); ) {
      K key=enumeration.nextElement();
      map.put(key,dictionary.get(key));
    }
  }
  _map=Collections.unmodifiableMap(map);
}

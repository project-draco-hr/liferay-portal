{
  if (!_fileEntryDisplayContextHelper.isCheckoutDocumentActionAvailable()) {
    return;
  }
  _addURLUIItem(new URLMenuItem(),menuItems,"icon-unlock",DLUIItemKeys.CHECKOUT,"checkout[document]",_getActionURL("/document_library/edit_file_entry",Constants.CHECKOUT));
}

{
  if (!_fileEntryDisplayContextHelper.isCheckinActionAvailable()) {
    return;
  }
  _addURLUIItem(new URLMenuItem(),menuItems,"icon-lock",DLUIItemKeys.CHECKIN,"checkin",_getActionURL("/document_library/edit_file_entry",Constants.CHECKIN));
}

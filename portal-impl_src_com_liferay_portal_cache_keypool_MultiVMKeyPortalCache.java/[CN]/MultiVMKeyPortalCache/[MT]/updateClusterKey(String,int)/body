{
  if (!(_DUMMY_VALUE == _localCache.get(key)) || !key.equals(_clusterCache.get(key))) {
    _clusterCache.put(key,key,timeToLive);
    if (_log.isDebugEnabled()) {
      _log.debug("Invalidating cluster key [" + key + "]");
    }
  }
}

{
  String useDownloadURL=getRepository().getSettings().getProperty(PluginPackageRepository.SETTING_USE_DOWNLOAD_URL);
  if (!GetterUtil.getBoolean(useDownloadURL,false)) {
    return getArtifactURL();
  }
  return _downloadURL;
}

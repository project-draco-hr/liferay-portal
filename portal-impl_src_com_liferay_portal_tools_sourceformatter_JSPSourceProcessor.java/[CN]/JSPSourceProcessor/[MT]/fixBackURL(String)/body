{
  Pattern pattern=Pattern.compile("String redirect = ParamUtil\\.getString\\(request, " + "\"redirect\".*\\);\nString backURL = ParamUtil\\.getString" + "\\(request, \"backURL\", redirect\\);");
  Matcher matcher=pattern.matcher(content);
  if (matcher.find()) {
    content=StringUtil.replace(content,"String backURL = ParamUtil.getString(request, \"backURL\", " + "redirect);","\nString backURL = ParamUtil.getString(request, " + "\"backURL\", redirect);");
  }
  return content;
}

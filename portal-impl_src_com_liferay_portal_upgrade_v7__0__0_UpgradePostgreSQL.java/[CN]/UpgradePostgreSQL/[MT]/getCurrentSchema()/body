{
  try (PreparedStatement ps=connection.prepareStatement("select current_schema();");ResultSet rs=ps.executeQuery()){
    if (rs.next()) {
      return (String)rs.getObject("current_schema");
    }
    return null;
  }
 }

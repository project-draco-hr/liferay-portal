{
  try (PreparedStatement ps=connection.prepareStatement("update KaleoAction set script = ? where " + "kaleoActionId = ?")){
    ps.setString(1,script);
    ps.setLong(2,kaleoActionId);
    ps.executeUpdate();
  }
 }

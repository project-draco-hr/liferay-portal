{
  Object obj=null;
  if (cache) {
    obj=_cache.get(location);
    if (obj == null) {
      obj=_lookup(ctx,location);
      _cache.put(location,obj);
    }
  }
 else {
    obj=_lookup(ctx,location);
  }
  return obj;
}

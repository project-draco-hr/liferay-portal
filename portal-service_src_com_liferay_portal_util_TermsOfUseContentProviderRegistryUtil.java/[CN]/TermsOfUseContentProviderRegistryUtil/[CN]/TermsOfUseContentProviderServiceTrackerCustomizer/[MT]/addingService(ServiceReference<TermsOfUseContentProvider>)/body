{
  Registry registry=RegistryUtil.getRegistry();
  TermsOfUseContentProvider termsOfUseContentProvider=registry.getService(serviceReference);
  _termsOfUseContentProviders.put(termsOfUseContentProvider.getClassName(),termsOfUseContentProvider);
  return termsOfUseContentProvider;
}

{
  Session session=null;
  try {
    session=openSession();
    UserIdMapperHBM userIdMapperHBM=(UserIdMapperHBM)session.get(UserIdMapperHBM.class,userIdMapperPK);
    if (userIdMapperHBM == null) {
      _log.warn("No UserIdMapper exists with the primary key " + userIdMapperPK.toString());
      throw new NoSuchUserIdMapperException("No UserIdMapper exists with the primary key " + userIdMapperPK.toString());
    }
    session.delete(userIdMapperHBM);
    session.flush();
    return UserIdMapperHBMUtil.model(userIdMapperHBM);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

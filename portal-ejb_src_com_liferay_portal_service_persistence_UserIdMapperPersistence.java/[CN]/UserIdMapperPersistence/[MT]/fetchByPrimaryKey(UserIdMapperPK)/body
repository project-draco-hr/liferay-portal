{
  Session session=null;
  try {
    session=openSession();
    return (UserIdMapper)session.get(UserIdMapperImpl.class,userIdMapperPK);
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  ShoppingCategory newShoppingCategory=addShoppingCategory();
  _persistence.clearCache();
  ShoppingCategoryModelImpl existingShoppingCategoryModelImpl=(ShoppingCategoryModelImpl)_persistence.findByPrimaryKey(newShoppingCategory.getPrimaryKey());
  Assert.assertEquals(existingShoppingCategoryModelImpl.getGroupId(),existingShoppingCategoryModelImpl.getOriginalGroupId());
  Assert.assertTrue(Validator.equals(existingShoppingCategoryModelImpl.getName(),existingShoppingCategoryModelImpl.getOriginalName()));
}

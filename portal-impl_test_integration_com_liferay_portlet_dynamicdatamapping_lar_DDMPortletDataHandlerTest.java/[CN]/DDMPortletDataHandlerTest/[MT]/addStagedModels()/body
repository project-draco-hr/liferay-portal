{
  DDMStructure structure=DDMStructureTestUtil.addStructure(stagingGroup.getGroupId(),DDLRecordSet.class.getName());
  DDMTemplateTestUtil.addTemplate(stagingGroup.getGroupId(),structure.getStructureId());
}

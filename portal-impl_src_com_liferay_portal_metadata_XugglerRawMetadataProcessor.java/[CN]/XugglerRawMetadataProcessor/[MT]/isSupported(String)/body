{
  if (PrefsPropsUtil.getBoolean(PropsKeys.XUGGLER_ENABLED,PropsValues.XUGGLER_ENABLED) && (AudioProcessor.getInstance().isSupported(mimeType) || VideoProcessor.getInstance().isSupported(mimeType))) {
    return true;
  }
 else {
    return false;
  }
}

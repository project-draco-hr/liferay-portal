{
  if (PrefsPropsUtil.getBoolean(PropsKeys.XUGGLER_ENABLED,PropsValues.XUGGLER_ENABLED) && (AudioProcessor.isSupportedAudio(mimeType) || VideoProcessor.isSupportedVideo(mimeType))) {
    return true;
  }
 else {
    return false;
  }
}

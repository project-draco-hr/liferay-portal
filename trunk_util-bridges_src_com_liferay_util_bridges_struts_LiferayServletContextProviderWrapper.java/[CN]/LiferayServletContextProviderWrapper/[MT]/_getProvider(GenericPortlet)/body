{
  PortletContext portletContext=portlet.getPortletContext();
  if (_provider == null) {
    _provider=(ServletContextProvider)portletContext.getAttribute(ServletContextProvider.STRUTS_BRIDGES_CONTEXT_PROVIDER);
  }
  return _provider;
}

{
  if (_liveGroup != null) {
    return _liveGroup;
  }
  Group selGroup=getSelGroup();
  if (selGroup.isStagingGroup()) {
    _liveGroup=selGroup.getLiveGroup();
  }
 else {
    _liveGroup=selGroup;
  }
  return _liveGroup;
}

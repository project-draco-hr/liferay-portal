{
  Registry registry=RegistryUtil.getRegistry();
  DisplayContextFactory service=registry.getService(DLDisplayContextFactory.class);
  Assert.assertEquals(TestDLDisplayContextFactoryImpl.class.getName(),service.getClass().getName());
}

{
  String extension=FilenameUtils.getExtension(filePath.toString());
  if (extension == null) {
    return false;
  }
  if (_excelExtensions.contains(extension.toLowerCase()) && !Files.isDirectory(filePath)) {
    return true;
  }
  return false;
}

{
  Folder folder=DLAppTestUtil.addFolder(TestPropsValues.getGroupId(),DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,ServiceTestUtil.randomString());
  FileEntry initialFileEntry=DLAppTestUtil.addFileEntry(TestPropsValues.getGroupId(),folder.getFolderId(),ServiceTestUtil.randomString() + ".txt");
  _convertProcess.convert();
  FileEntry fileEntry=DLAppLocalServiceUtil.getFileEntry(initialFileEntry.getFileEntryId());
  String fileEntryTitle=fileEntry.getTitle();
  Assert.assertTrue(fileEntryTitle.endsWith(".txt"));
}

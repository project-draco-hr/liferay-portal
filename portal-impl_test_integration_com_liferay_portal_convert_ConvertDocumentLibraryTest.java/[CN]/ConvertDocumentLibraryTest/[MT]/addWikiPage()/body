{
  WikiNode wikiNode=WikiTestUtil.addNode(TestPropsValues.getUserId(),TestPropsValues.getGroupId(),ServiceTestUtil.randomString(),ServiceTestUtil.randomString(50));
  return WikiTestUtil.addPage(wikiNode.getUserId(),_group.getGroupId(),wikiNode.getNodeId(),ServiceTestUtil.randomString(),true);
}

{
  Image image=addImage();
  try {
    _convertProcess.convert();
    DLContentLocalServiceUtil.getContent(0,0,image.getImageId() + ".jpg");
  }
  finally {
    ImageLocalServiceUtil.deleteImage(image);
  }
}

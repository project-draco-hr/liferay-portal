{
  BookmarksFolderPermission.check(getPermissionChecker(),folderId,ActionKeys.UPDATE);
  return BookmarksFolderLocalServiceUtil.updateFolder(getUser().getCompanyId(),folderId,parentFolderId,name,description);
}

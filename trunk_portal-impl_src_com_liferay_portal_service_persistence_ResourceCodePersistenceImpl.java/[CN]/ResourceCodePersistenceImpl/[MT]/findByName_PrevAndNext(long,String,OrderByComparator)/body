{
  ResourceCode resourceCode=findByPrimaryKey(codeId);
  Session session=null;
  try {
    session=openSession();
    ResourceCode[] array=new ResourceCodeImpl[3];
    array[0]=getByName_PrevAndNext(session,resourceCode,name,orderByComparator,true);
    array[1]=resourceCode;
    array[2]=getByName_PrevAndNext(session,resourceCode,name,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  ResourceBundle bundle=bundles.get(languageId);
  if (bundle == null) {
    try {
      bundle=new PropertyResourceBundle(new UnsyncByteArrayInputStream(new byte[0]));
      bundles.put(languageId,bundle);
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
  return bundle;
}

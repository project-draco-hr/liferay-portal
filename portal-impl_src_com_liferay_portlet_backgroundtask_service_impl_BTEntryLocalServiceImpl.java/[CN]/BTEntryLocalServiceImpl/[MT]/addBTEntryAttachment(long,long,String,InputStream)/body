{
  BTEntry btEntry=getBTEntry(btEntryId);
  Folder folder=btEntry.addAttachmentsFolder();
  PortletFileRepositoryUtil.addPortletFileEntry(btEntry.getGroupId(),userId,BTEntry.class.getName(),btEntry.getPrimaryKey(),PortletKeys.BACKGROUND_TASK,folder.getFolderId(),inputStream,fileName,null);
}

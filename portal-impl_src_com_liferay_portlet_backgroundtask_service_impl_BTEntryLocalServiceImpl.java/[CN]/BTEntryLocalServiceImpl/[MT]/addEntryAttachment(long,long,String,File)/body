{
  BTEntry entry=getBTEntry(entryId);
  Folder folder=entry.addAttachmentsFolder();
  PortletFileRepositoryUtil.addPortletFileEntry(entry.getGroupId(),userId,BTEntry.class.getName(),entry.getPrimaryKey(),PortletKeys.BACKGROUND_TASK,folder.getFolderId(),file,fileName,null);
}

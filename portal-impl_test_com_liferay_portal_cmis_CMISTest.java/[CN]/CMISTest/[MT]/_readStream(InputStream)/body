{
  String str=null;
  try {
    byte[] bytes=new byte[is.available()];
    is.read(bytes);
    str=new String(bytes,StringPool.UTF8);
  }
  finally {
    if (is != null) {
      is.close();
    }
  }
  return str;
}

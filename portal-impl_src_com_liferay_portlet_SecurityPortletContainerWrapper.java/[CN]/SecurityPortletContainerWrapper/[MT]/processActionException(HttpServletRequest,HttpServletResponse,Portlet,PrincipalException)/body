{
  if (_log.isDebugEnabled()) {
    _log.debug(e);
  }
  String url=getOriginalURL(request);
  _log.warn("Reject process action for " + url + " on "+ portlet.getPortletId());
  return ActionResult.EMPTY_ACTION_RESULT;
}

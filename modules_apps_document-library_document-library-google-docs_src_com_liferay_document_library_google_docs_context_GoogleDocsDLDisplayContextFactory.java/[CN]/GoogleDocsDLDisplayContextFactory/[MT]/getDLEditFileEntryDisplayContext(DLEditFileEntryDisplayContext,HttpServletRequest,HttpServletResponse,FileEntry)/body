{
  GoogleDocsMetadataHelper googleDocsMetadataHelper=new GoogleDocsMetadataHelper((DLFileEntry)fileEntry.getModel(),_dlFileEntryMetadataLocalService,_storageEngine);
  if (googleDocsMetadataHelper.isGoogleDocs()) {
    return new GoogleDocsDLEditFileEntryDisplayContext(parentDLEditFileEntryDisplayContext,request,response,fileEntry);
  }
  return parentDLEditFileEntryDisplayContext;
}

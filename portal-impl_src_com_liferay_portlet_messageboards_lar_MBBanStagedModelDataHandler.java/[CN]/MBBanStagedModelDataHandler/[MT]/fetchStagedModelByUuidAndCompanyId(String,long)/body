{
  List<MBBan> bans=MBBanLocalServiceUtil.getMBBansByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<MBBan>());
  if (ListUtil.isEmpty(bans)) {
    return null;
  }
  return bans.get(0);
}

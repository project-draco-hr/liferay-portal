{
  Registry registry=RegistryUtil.getRegistry();
  Filter filter=registry.getFilter("(objectClass=" + DLDisplayContextFactory.class.getName() + ")");
  _serviceTracker=registry.trackServices(filter,new DLDisplayContextFactoryServiceTrackerCustomizer());
  _serviceTracker.open();
}

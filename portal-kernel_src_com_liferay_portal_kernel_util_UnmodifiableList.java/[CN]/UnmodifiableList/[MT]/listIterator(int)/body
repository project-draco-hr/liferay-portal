{
  return new ListIterator<E>(){
    @Override public void add(    E element){
      throw new UnsupportedOperationException(_MESSAGE);
    }
    @Override public boolean hasNext(){
      return _itr.hasNext();
    }
    @Override public boolean hasPrevious(){
      return _itr.hasPrevious();
    }
    @Override public E next(){
      return _itr.next();
    }
    @Override public int nextIndex(){
      return _itr.nextIndex();
    }
    @Override public E previous(){
      return _itr.previous();
    }
    @Override public int previousIndex(){
      return _itr.previousIndex();
    }
    @Override public void remove(){
      throw new UnsupportedOperationException(_MESSAGE);
    }
    @Override public void set(    E element){
      throw new UnsupportedOperationException(_MESSAGE);
    }
    private final ListIterator<? extends E> _itr=_list.listIterator(index);
  }
;
}

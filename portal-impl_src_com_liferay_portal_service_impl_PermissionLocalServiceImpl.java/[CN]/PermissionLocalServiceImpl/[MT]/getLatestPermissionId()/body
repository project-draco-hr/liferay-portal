{
  List<Permission> permissions=permissionPersistence.findAll(0,1,new PermissionComparator());
  if (permissions.size() == 0) {
    return 0;
  }
 else {
    Permission permission=permissions.get(0);
    return permission.getPermissionId();
  }
}

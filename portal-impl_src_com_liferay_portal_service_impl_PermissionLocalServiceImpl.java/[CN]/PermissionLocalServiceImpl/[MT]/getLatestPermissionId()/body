{
  List list=permissionPersistence.findAll(0,1,new PermissionComparator());
  if (list.size() == 0) {
    return 0;
  }
 else {
    Permission permission=(Permission)list.get(0);
    return permission.getPermissionId();
  }
}

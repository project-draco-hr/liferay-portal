{
  Permission permission=permissionPersistence.fetchByPrimaryKey(permissionId);
  if (permission != null) {
    rolePersistence.removePermission(roleId,permission);
  }
  PermissionCacheUtil.clearCache();
}

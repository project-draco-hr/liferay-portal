{
  ResourceCode resourceCode=resourceCodeLocalService.getResourceCode(companyId,name,scope);
  if (permissionFinder.countByR_A_C(roleId,actionId,resourceCode.getCodeId()) > 0) {
    return true;
  }
 else {
    return false;
  }
}

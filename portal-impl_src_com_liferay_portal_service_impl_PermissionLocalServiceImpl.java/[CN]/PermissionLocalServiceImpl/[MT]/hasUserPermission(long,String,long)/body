{
  Permission permission=permissionPersistence.fetchByA_R(actionId,resourceId);
  if (permission == null) {
    return false;
  }
  return userPersistence.containsPermission(userId,permission.getPermissionId());
}

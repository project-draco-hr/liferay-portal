{
  List<Permission> permissions=new ArrayList<Permission>();
  for (  String actionId : actionIds) {
    Permission permission=addPermission(companyId,actionId,resourceId);
    permissions.add(permission);
  }
  return permissions;
}

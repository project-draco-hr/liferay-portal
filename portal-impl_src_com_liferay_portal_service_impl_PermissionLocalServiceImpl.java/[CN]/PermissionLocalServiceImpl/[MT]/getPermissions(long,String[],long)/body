{
  List<Permission> permissions=new ArrayList<Permission>();
  for (int i=0; i < actionIds.length; i++) {
    Permission permission=addPermission(companyId,actionIds[i],resourceId);
    permissions.add(permission);
  }
  return permissions;
}

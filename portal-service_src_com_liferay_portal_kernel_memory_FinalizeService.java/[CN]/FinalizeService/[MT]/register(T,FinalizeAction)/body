{
  Reference<T> reference=new WeakReference<T>(realReference,_referenceQueue);
  _referenceActionMap.put(reference,finalizeAction);
  if (!THREAD_ENABLE) {
    pollingCleanup();
  }
  return reference;
}

{
  if (isEscapedModel()) {
    return (Release)this;
  }
 else {
    return (Release)Proxy.newProxyInstance(Release.class.getClassLoader(),new Class[]{Release.class},new AutoEscapeBeanHandler(this));
  }
}

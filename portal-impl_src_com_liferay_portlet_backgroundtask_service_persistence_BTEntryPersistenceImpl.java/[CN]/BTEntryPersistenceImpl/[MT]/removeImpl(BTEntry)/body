{
  btEntry=toUnwrappedModel(btEntry);
  Session session=null;
  try {
    session=openSession();
    if (!session.contains(btEntry)) {
      btEntry=(BTEntry)session.get(BTEntryImpl.class,btEntry.getPrimaryKeyObj());
    }
    if (btEntry != null) {
      session.delete(btEntry);
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  if (btEntry != null) {
    clearCache(btEntry);
  }
  return btEntry;
}

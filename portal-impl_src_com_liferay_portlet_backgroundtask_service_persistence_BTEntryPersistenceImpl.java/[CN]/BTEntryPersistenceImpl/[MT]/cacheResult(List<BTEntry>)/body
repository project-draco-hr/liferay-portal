{
  for (  BTEntry btEntry : btEntries) {
    if (EntityCacheUtil.getResult(BTEntryModelImpl.ENTITY_CACHE_ENABLED,BTEntryImpl.class,btEntry.getPrimaryKey()) == null) {
      cacheResult(btEntry);
    }
 else {
      btEntry.resetOriginalValues();
    }
  }
}

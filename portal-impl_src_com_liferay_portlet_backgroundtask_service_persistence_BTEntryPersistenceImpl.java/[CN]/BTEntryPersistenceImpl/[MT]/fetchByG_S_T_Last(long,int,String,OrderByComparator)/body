{
  int count=countByG_S_T(groupId,status,taskExecutorClassName);
  List<BTEntry> list=findByG_S_T(groupId,status,taskExecutorClassName,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

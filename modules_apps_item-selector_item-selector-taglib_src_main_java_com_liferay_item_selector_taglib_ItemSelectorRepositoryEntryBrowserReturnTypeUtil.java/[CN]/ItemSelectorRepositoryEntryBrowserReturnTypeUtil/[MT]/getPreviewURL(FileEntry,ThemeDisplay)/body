{
  String previewURL=DLUtil.getPreviewURL(fileEntry,fileEntry.getFileVersion(),themeDisplay,StringPool.BLANK,false,false);
  if (Validator.isNull(previewURL)) {
    return DLUtil.getImagePreviewURL(fileEntry,themeDisplay);
  }
  return previewURL;
}

{
  if (isEscapedModel()) {
    return (DDMContent)this;
  }
 else {
    return (DDMContent)Proxy.newProxyInstance(DDMContent.class.getClassLoader(),new Class[]{DDMContent.class},new AutoEscapeBeanHandler(this));
  }
}

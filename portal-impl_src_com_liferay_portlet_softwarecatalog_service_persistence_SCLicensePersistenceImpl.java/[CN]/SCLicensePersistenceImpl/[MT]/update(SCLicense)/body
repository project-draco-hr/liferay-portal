{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCLicense scLicense) method. To improve performance, call update(SCLicense scLicense, boolean merge).");
  }
  return update(scLicense,false);
}

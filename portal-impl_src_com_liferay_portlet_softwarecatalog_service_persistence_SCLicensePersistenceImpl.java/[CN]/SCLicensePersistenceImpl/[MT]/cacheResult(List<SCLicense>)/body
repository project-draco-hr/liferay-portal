{
  for (  SCLicense scLicense : scLicenses) {
    if (EntityCacheUtil.getResult(SCLicenseModelImpl.ENTITY_CACHE_ENABLED,SCLicense.class,scLicense.getPrimaryKey(),this) == null) {
      cacheResult(scLicense);
    }
  }
}

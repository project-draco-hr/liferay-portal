{
  for (  SCLicense scLicense : scLicenses) {
    if (EntityCacheUtil.getResult(SCLicenseModelImpl.ENTITY_CACHE_ENABLED,SCLicenseImpl.class,scLicense.getPrimaryKey()) == null) {
      cacheResult(scLicense);
    }
 else {
      scLicense.resetOriginalValues();
    }
  }
}

{
  try {
    long[] scProductEntryPKs=new long[scProductEntries.size()];
    for (int i=0; i < scProductEntries.size(); i++) {
      com.liferay.portlet.softwarecatalog.model.SCProductEntry scProductEntry=scProductEntries.get(i);
      scProductEntryPKs[i]=scProductEntry.getPrimaryKey();
    }
    setSCProductEntries(pk,scProductEntryPKs);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    FinderCacheUtil.clearCache(SCLicenseModelImpl.TABLE_SCLICENSES_SCPRODUCTENTRIES);
  }
}

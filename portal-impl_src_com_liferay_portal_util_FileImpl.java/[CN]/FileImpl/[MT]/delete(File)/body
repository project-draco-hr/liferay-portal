{
  if (file != null) {
    boolean exists=true;
    try {
      exists=file.exists();
    }
 catch (    SecurityException se) {
    }
    if (exists) {
      return file.delete();
    }
  }
  return false;
}

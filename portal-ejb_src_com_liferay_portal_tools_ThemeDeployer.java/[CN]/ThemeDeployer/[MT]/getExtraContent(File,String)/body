{
  String extraContent=super.getExtraContent(srcFile,displayName);
  extraContent+="<listener>" + "<listener-class>" + "com.liferay.portal.kernel.servlet.ThemeContextListener"+ "</listener-class>"+ "</listener>";
  return extraContent;
}

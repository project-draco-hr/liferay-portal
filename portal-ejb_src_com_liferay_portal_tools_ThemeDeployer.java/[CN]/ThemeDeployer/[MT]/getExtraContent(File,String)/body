{
  String extraContent=super.getExtraContent(srcFile,displayName);
  extraContent+="<listener>" + "<listener-class>" + "com.liferay.portal.shared.servlet.ThemeContextListener"+ "</listener-class>"+ "</listener>";
  return extraContent;
}

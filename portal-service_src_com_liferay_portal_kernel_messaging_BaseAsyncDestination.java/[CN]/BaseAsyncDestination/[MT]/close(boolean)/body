{
  ThreadPoolExecutor threadPoolExecutor=PortalExecutorManagerUtil.getPortalExecutor(getName());
  if (force) {
    threadPoolExecutor.shutdownNow();
  }
 else {
    threadPoolExecutor.shutdown();
  }
}

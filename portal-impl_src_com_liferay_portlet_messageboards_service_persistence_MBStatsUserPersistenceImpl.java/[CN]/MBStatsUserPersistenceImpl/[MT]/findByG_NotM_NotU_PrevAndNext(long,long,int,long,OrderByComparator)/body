{
  MBStatsUser mbStatsUser=findByPrimaryKey(statsUserId);
  Session session=null;
  try {
    session=openSession();
    MBStatsUser[] array=new MBStatsUserImpl[3];
    array[0]=getByG_NotM_NotU_PrevAndNext(session,mbStatsUser,groupId,messageCount,userId,orderByComparator,true);
    array[1]=mbStatsUser;
    array[2]=getByG_NotM_NotU_PrevAndNext(session,mbStatsUser,groupId,messageCount,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

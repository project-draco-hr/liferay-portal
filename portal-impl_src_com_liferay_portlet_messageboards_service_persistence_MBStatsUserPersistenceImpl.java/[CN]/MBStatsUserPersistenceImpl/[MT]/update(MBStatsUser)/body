{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBStatsUser mbStatsUser) method. To improve performance, call update(MBStatsUser mbStatsUser, boolean merge).");
  }
  return update(mbStatsUser,false);
}

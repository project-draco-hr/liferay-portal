{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBStatsUser mbStatsUser) method. Use update(MBStatsUser mbStatsUser, boolean merge) instead.");
  }
  return update(mbStatsUser,false);
}

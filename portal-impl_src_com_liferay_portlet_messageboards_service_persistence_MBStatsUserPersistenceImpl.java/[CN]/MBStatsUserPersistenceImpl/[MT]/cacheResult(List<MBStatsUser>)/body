{
  for (  MBStatsUser mbStatsUser : mbStatsUsers) {
    if (EntityCacheUtil.getResult(MBStatsUserModelImpl.ENTITY_CACHE_ENABLED,MBStatsUser.class,mbStatsUser.getPrimaryKey(),this) == null) {
      cacheResult(mbStatsUser);
    }
  }
}

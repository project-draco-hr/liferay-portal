{
  int count=countByG_NotM_NotU(groupId,messageCount,userId);
  List<MBStatsUser> list=findByG_NotM_NotU(groupId,messageCount,userId,count - 1,count,orderByComparator);
  if (list.isEmpty()) {
    StringBundler msg=new StringBundler(8);
    msg.append(_NO_SUCH_ENTITY_WITH_KEY);
    msg.append("groupId=");
    msg.append(groupId);
    msg.append(", messageCount=");
    msg.append(messageCount);
    msg.append(", userId=");
    msg.append(userId);
    msg.append(StringPool.CLOSE_CURLY_BRACE);
    throw new NoSuchStatsUserException(msg.toString());
  }
 else {
    return list.get(0);
  }
}

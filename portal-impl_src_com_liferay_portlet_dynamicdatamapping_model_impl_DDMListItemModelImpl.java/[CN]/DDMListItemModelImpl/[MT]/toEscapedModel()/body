{
  if (isEscapedModel()) {
    return (DDMListItem)this;
  }
 else {
    return (DDMListItem)Proxy.newProxyInstance(DDMListItem.class.getClassLoader(),new Class[]{DDMListItem.class},new AutoEscapeBeanHandler(this));
  }
}

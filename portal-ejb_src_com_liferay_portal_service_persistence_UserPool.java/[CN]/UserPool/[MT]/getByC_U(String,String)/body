{
  String pk=_instance._getByC_U(companyId,userId);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_U" + " " + companyId.toString() + " "+ userId.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

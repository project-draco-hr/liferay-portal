{
  String pk=_instance._getByC_EA(companyId,emailAddress);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_EA" + " " + companyId.toString() + " "+ emailAddress.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

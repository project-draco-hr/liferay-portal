{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  try {
    WikiPage page=ActionUtil.getPage(portletRequest);
    boolean subscribed=_subscriptionLocalService.isSubscribed(themeDisplay.getCompanyId(),themeDisplay.getUserId(),WikiNode.class.getName(),page.getNodeId());
    return new PageSubscriptionPortletConfigurationIcon(portletRequest,page,subscribed);
  }
 catch (  Exception e) {
  }
  return null;
}

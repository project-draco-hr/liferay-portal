{
  LayoutLocalService layoutLocalService=LayoutLocalServiceFactory.getService();
  layoutLocalService.schedulePublishToLiveEvent(userId,stagingGroupId,liveGroupId,privateLayout,parameterMap,cronText,scope,layoutIdMap,startDate,endDate,description);
}

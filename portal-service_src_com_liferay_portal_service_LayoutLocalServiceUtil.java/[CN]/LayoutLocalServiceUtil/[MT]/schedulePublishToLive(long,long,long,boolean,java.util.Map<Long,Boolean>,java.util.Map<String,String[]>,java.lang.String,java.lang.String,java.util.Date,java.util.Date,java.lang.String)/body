{
  LayoutLocalService layoutLocalService=LayoutLocalServiceFactory.getService();
  layoutLocalService.schedulePublishToLive(userId,stagingGroupId,liveGroupId,privateLayout,layoutIdMap,parameterMap,scope,cronText,startDate,endDate,description);
}

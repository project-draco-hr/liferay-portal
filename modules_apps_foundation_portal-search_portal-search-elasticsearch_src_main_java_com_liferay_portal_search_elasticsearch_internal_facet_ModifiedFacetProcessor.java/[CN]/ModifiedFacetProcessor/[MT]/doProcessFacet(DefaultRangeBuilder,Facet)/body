{
  super.doProcessFacet(defaultRangeBuilder,facet);
  SearchContext searchContext=facet.getSearchContext();
  String rangeParam=GetterUtil.getString(searchContext.getAttribute(facet.getFieldId()));
  if (Validator.isNotNull(rangeParam)) {
    addRange(defaultRangeBuilder,rangeParam);
  }
}

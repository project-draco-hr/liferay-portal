{
  Project project=getProject();
  String failureMessage=FailureMessageUtil.getFailureMessage(project,JenkinsResultsParserUtil.getLocalURL(urlString));
  return formatXML("<div>" + failureMessage + "</div>");
}

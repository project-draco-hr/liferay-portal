{
  if (file.exists()) {
    if (file.isFile()) {
      file.delete();
    }
 else {
      File[] fileArray=file.listFiles();
      for (      File subFile : fileArray) {
        _deleteFile(subFile);
      }
      file.delete();
    }
  }
}

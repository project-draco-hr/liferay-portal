{
  String name=dir.getName();
  File[] files=dir.listFiles();
  for (  File file : files) {
    if (file.isDirectory()) {
      if (!validateCase(project,name,file)) {
        return false;
      }
    }
  }
  return true;
}

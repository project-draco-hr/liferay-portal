{
  String name=groupRoot.getName();
  File[] files=groupRoot.listFiles();
  for (  File file : files) {
    if (file.isDirectory()) {
      if (!validateCase(project,name,file)) {
        return false;
      }
    }
  }
  return true;
}

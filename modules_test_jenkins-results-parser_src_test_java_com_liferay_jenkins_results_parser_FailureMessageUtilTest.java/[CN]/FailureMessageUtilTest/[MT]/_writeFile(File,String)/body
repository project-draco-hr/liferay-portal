{
  if (file.exists()) {
    file.delete();
  }
  file.createNewFile();
  FileWriter writer=null;
  try {
    writer=new FileWriter(file);
    writer.write(content);
    writer.flush();
  }
  finally {
    writer.close();
  }
}

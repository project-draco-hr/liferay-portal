{
  WikiPageResource wikiPageResource=(WikiPageResource)EntityCacheUtil.getResult(WikiPageResourceModelImpl.ENTITY_CACHE_ENABLED,WikiPageResourceImpl.class,resourcePrimKey);
  if (wikiPageResource == _nullWikiPageResource) {
    return null;
  }
  if (wikiPageResource == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      wikiPageResource=(WikiPageResource)session.get(WikiPageResourceImpl.class,Long.valueOf(resourcePrimKey));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (wikiPageResource != null) {
        cacheResult(wikiPageResource);
      }
 else       if (!hasException) {
        EntityCacheUtil.putResult(WikiPageResourceModelImpl.ENTITY_CACHE_ENABLED,WikiPageResourceImpl.class,resourcePrimKey,_nullWikiPageResource);
      }
      closeSession(session);
    }
  }
  return wikiPageResource;
}

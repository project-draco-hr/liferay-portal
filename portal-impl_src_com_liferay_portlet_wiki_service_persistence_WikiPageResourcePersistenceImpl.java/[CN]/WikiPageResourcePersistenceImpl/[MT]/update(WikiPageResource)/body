{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WikiPageResource wikiPageResource) method. Use update(WikiPageResource wikiPageResource, boolean merge) instead.");
  }
  return update(wikiPageResource,false);
}

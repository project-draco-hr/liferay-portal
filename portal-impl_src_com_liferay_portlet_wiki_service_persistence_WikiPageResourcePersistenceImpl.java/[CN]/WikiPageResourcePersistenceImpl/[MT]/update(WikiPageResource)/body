{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WikiPageResource wikiPageResource) method. To improve performance, call update(WikiPageResource wikiPageResource, boolean merge).");
  }
  return update(wikiPageResource,false);
}

{
  Class<?> clazz=getClass();
  String className=clazz.getName();
  if (className.contains(".sampledata523.")) {
    return;
  }
  if (className.contains(".sampledata525.")) {
    return;
  }
  if (className.contains(".sampledata527.")) {
    return;
  }
  if (className.contains(".sampledata528.")) {
    return;
  }
  if (className.contains(".sampledata529.")) {
    return;
  }
  String location=selenium.getLocation();
  if (location.contains("/documents/")) {
    return;
  }
  if (location.contains("/journal/rss/")) {
    return;
  }
  if (location.contains(TestPropsValues.PORTAL_URL) || location.contains("www.able.com") || location.contains("www.baker.com")|| location.contains("www.charlie.com")|| location.contains("www.dog.com")|| location.contains("www.easy.com")|| location.contains("www.fox.com")) {
    selenium.getEval("window.Liferay.fire(\'initDockbar\');");
  }
}

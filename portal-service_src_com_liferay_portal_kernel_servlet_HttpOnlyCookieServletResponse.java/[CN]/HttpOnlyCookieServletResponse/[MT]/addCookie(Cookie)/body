{
  Set<String> httpOnlyCookieNamesExcludes=getCookieHttpOnlyCookieNamesExcludes();
  if (!httpOnlyCookieNamesExcludes.contains(cookie.getName())) {
    cookie.setHttpOnly(true);
  }
  super.addCookie(cookie);
}

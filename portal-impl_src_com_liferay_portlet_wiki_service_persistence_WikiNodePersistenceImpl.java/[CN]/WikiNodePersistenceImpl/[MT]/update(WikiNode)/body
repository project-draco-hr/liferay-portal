{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WikiNode wikiNode) method. To improve performance, call update(WikiNode wikiNode, boolean merge).");
  }
  return update(wikiNode,false);
}

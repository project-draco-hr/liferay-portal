{
  if (solrServerWrapper1.getInvocationCount() > solrServerWrapper2.getInvocationCount()) {
    return 1;
  }
 else   if (solrServerWrapper1.getInvocationCount() < solrServerWrapper2.getInvocationCount()) {
    return -1;
  }
  return 0;
}

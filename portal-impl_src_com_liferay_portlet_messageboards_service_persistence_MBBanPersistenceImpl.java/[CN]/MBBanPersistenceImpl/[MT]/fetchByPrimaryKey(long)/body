{
  MBBan mbBan=(MBBan)EntityCacheUtil.getResult(MBBanModelImpl.ENTITY_CACHE_ENABLED,MBBanImpl.class,banId);
  if (mbBan == _nullMBBan) {
    return null;
  }
  if (mbBan == null) {
    Session session=null;
    try {
      session=openSession();
      mbBan=(MBBan)session.get(MBBanImpl.class,Long.valueOf(banId));
      if (mbBan != null) {
        cacheResult(mbBan);
      }
 else {
        EntityCacheUtil.putResult(MBBanModelImpl.ENTITY_CACHE_ENABLED,MBBanImpl.class,banId,_nullMBBan);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(MBBanModelImpl.ENTITY_CACHE_ENABLED,MBBanImpl.class,banId);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return mbBan;
}

{
  mbBan=toUnwrappedModel(mbBan);
  Session session=null;
  try {
    session=openSession();
    BatchSessionUtil.delete(session,mbBan);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST_WITH_PAGINATION);
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST_WITHOUT_PAGINATION);
  MBBanModelImpl mbBanModelImpl=(MBBanModelImpl)mbBan;
  FinderCacheUtil.removeResult(FINDER_PATH_FETCH_BY_G_B,new Object[]{Long.valueOf(mbBanModelImpl.getGroupId()),Long.valueOf(mbBanModelImpl.getBanUserId())});
  EntityCacheUtil.removeResult(MBBanModelImpl.ENTITY_CACHE_ENABLED,MBBanImpl.class,mbBan.getPrimaryKey());
  return mbBan;
}

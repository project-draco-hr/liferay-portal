{
  try {
    doInvokeUndeploy(hotDeployEvent);
  }
 catch (  Throwable t) {
    throwHotDeployException(hotDeployEvent,"Error unregistering social for " + hotDeployEvent.getServletContextName(),t);
  }
}

{
  int count=countByC_P(companyId,primKey);
  List<ResourcePermission> list=findByC_P(companyId,primKey,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  int count=countByC_N_P_O(companyId,name,primKey,ownerId);
  List<ResourcePermission> list=findByC_N_P_O(companyId,name,primKey,ownerId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  ResourcePermission resourcePermission=findByPrimaryKey(resourcePermissionId);
  Session session=null;
  try {
    session=openSession();
    ResourcePermission[] array=new ResourcePermissionImpl[3];
    array[0]=getByR_S_PrevAndNext(session,resourcePermission,roleId,scope,orderByComparator,true);
    array[1]=resourcePermission;
    array[2]=getByR_S_PrevAndNext(session,resourcePermission,roleId,scope,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

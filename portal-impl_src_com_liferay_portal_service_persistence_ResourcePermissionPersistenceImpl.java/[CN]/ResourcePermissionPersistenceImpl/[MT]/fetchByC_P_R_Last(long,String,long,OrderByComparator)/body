{
  int count=countByC_P_R(companyId,primKey,roleId);
  List<ResourcePermission> list=findByC_P_R(companyId,primKey,roleId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

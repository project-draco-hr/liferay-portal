{
  int count=countByC_N_S_P_R_A(companyId,name,scope,primKey,roleId,actionIds);
  List<ResourcePermission> list=findByC_N_S_P_R_A(companyId,name,scope,primKey,roleId,actionIds,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ResourcePermission resourcePermission) method. Use update(ResourcePermission resourcePermission, boolean merge) instead.");
  }
  return update(resourcePermission,false);
}

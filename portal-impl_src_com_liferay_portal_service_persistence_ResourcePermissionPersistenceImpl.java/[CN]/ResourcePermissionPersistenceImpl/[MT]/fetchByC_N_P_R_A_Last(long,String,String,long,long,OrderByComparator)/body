{
  int count=countByC_N_P_R_A(companyId,name,primKey,roleId,actionIds);
  List<ResourcePermission> list=findByC_N_P_R_A(companyId,name,primKey,roleId,actionIds,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

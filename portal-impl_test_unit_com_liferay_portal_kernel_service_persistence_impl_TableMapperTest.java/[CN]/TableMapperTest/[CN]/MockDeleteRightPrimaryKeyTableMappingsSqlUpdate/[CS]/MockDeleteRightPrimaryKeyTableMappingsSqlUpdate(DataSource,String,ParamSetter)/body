{
  Assert.assertSame(_dataSource,dataSource);
  Assert.assertEquals("DELETE FROM " + _TABLE_NAME + " WHERE "+ _RIGHT_COLUMN_NAME+ " = ?",sql);
  Assert.assertArrayEquals(new ParamSetter[]{ParamSetter.BIGINT},paramSetters);
}

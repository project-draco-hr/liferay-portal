{
  int count=_count++;
  if (count == 0) {
    return new MockAddMappingSqlUpdate(dataSource,sql,paramSetters);
  }
  if (count == 1) {
    return new MockDeleteLeftPrimaryKeyTableMappingsSqlUpdate(dataSource,sql,paramSetters);
  }
  if (count == 2) {
    return new MockDeleteRightPrimaryKeyTableMappingsSqlUpdate(dataSource,sql,paramSetters);
  }
  if (count == 3) {
    return new MockDeleteMappingSqlUpdate(dataSource,sql,paramSetters);
  }
  return null;
}

{
  PortletURL editLayoutURL=PortalUtil.getControlPanelPortletURL(liferayPortletRequest,LayoutAdminPortletKeys.LAYOUT_PROTOTYPE_PAGE,PortletRequest.RENDER_PHASE);
  editLayoutURL.setParameter("groupId",String.valueOf(themeDisplay.getScopeGroupId()));
  Layout layout=getLayout();
  editLayoutURL.setParameter("selPlid",String.valueOf(layout.getPlid()));
  editLayoutURL.setParameter("privateLayout",Boolean.TRUE.toString());
  editLayoutURL.setParameter("viewLayout",Boolean.TRUE.toString());
  return editLayoutURL.toString();
}

{
  String name=Organization.class.getName();
  if (!organization.isRoot()) {
    name="com.liferay.portal.model.Location";
  }
  ResourceLocalServiceUtil.addResources(organization.getCompanyId(),null,userId,name,organization.getPrimaryKey().toString(),false,false,false);
}

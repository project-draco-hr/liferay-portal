{
  String name=Organization.class.getName();
  if (!organization.isRoot()) {
    name=Location.class.getName();
  }
  ResourceLocalServiceUtil.addResources(organization.getCompanyId(),0,userId,name,organization.getOrganizationId(),false,false,false);
}

{
  try {
    _persistence.countByA_R(RandomTestUtil.randomBoolean(),RandomTestUtil.randomBoolean());
    _persistence.countByA_R(RandomTestUtil.randomBoolean(),RandomTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

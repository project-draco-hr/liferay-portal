{
  ServletRequest req=pageContext.getRequest();
  req.setAttribute("liferay-ui:breadcrumb:selLayout",_selLayout);
  req.setAttribute("liferay-ui:breadcrumb:selLayoutParam",_selLayoutParam);
  req.setAttribute("liferay-ui:breadcrumb:portletURL",_portletURL);
  req.setAttribute("liferay-ui:breadcrumb:displayStyle",String.valueOf(_displayStyle));
  return EVAL_BODY_BUFFERED;
}

{
  Fields fields=StorageEngineUtil.getFields(recordVersion.getDDMStorageId());
  if (fields == null) {
    return StringPool.BLANK;
  }
  DDLRecordSet recordSet=recordVersion.getRecordSet();
  return DDMIndexerUtil.extractAttributes(recordSet.getDDMStructure(),fields,locale);
}

{
  String action=_functionToActionMap.get(functionCallExpression.getFunctionName());
  List<Expression> parameters=functionCallExpression.getParameters();
  String target=doVisit(parameters.get(0));
  return new DDLFormRuleAction(action,target);
}

{
  CalEvent calEvent=findByPrimaryKey(eventId);
  Session session=null;
  try {
    session=openSession();
    CalEvent[] array=new CalEventImpl[3];
    array[0]=getByG_R_T_PrevAndNext(session,calEvent,groupId,repeating,type,orderByComparator,true);
    array[1]=calEvent;
    array[2]=getByG_R_T_PrevAndNext(session,calEvent,groupId,repeating,type,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

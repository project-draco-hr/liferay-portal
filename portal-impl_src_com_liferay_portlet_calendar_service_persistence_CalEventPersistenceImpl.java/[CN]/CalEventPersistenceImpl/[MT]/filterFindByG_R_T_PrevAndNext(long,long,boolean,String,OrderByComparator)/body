{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByG_R_T_PrevAndNext(eventId,groupId,repeating,type,orderByComparator);
  }
  CalEvent calEvent=findByPrimaryKey(eventId);
  Session session=null;
  try {
    session=openSession();
    CalEvent[] array=new CalEventImpl[3];
    array[0]=filterGetByG_R_T_PrevAndNext(session,calEvent,groupId,repeating,type,orderByComparator,true);
    array[1]=calEvent;
    array[2]=filterGetByG_R_T_PrevAndNext(session,calEvent,groupId,repeating,type,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

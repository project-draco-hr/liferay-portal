{
  calEvent=toUnwrappedModel(calEvent);
  Session session=null;
  try {
    session=openSession();
    if (calEvent.isCachedModel()) {
      calEvent=(CalEvent)session.get(CalEventImpl.class,calEvent.getPrimaryKeyObj());
    }
    session.delete(calEvent);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(calEvent);
  return calEvent;
}

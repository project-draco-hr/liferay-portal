{
  for (  CalEvent calEvent : calEvents) {
    if (EntityCacheUtil.getResult(CalEventModelImpl.ENTITY_CACHE_ENABLED,CalEvent.class,calEvent.getPrimaryKey(),this) == null) {
      cacheResult(calEvent);
    }
  }
}

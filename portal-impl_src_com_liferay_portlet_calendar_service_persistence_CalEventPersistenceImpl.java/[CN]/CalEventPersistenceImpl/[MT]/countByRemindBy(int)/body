{
  Object[] finderArgs=new Object[]{new Integer(remindBy)};
  Long count=(Long)FinderCacheUtil.getResult(FINDER_PATH_COUNT_BY_REMINDBY,finderArgs,this);
  if (count == null) {
    Session session=null;
    try {
      session=openSession();
      StringBundler query=new StringBundler(4);
      query.append("SELECT COUNT(calEvent) ");
      query.append("FROM CalEvent calEvent WHERE ");
      query.append("calEvent.remindBy != ?");
      query.append(" ");
      Query q=session.createQuery(query.toString());
      QueryPos qPos=QueryPos.getInstance(q);
      qPos.add(remindBy);
      count=(Long)q.uniqueResult();
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (count == null) {
        count=Long.valueOf(0);
      }
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_REMINDBY,finderArgs,count);
      closeSession(session);
    }
  }
  return count.intValue();
}

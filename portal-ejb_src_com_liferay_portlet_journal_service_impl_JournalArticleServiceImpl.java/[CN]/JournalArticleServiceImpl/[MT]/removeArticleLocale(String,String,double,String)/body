{
  JournalArticlePermission.check(getPermissionChecker(),companyId,articleId,ActionKeys.UPDATE);
  return JournalArticleLocalServiceUtil.removeArticleLocale(companyId,articleId,version,languageId);
}

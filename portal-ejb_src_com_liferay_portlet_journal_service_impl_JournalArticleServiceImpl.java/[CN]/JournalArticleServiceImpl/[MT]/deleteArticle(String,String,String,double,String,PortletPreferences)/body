{
  JournalArticlePermission.check(getPermissionChecker(),companyId,groupId,articleId,ActionKeys.DELETE);
  JournalArticleLocalServiceUtil.deleteArticle(companyId,groupId,articleId,version,articleURL,prefs);
}

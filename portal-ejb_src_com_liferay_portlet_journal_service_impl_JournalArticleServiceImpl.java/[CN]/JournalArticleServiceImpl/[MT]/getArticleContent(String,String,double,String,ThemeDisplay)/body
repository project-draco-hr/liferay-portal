{
  JournalArticlePermission.check(getPermissionChecker(),companyId,articleId,ActionKeys.VIEW);
  return JournalArticleLocalServiceUtil.getArticleContent(companyId,articleId,version,languageId,themeDisplay);
}

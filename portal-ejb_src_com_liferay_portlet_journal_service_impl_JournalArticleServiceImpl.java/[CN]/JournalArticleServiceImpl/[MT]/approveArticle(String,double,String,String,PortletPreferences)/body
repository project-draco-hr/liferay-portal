{
  User user=getUser();
  PortletPermission.check(getPermissionChecker(),plid,PortletKeys.JOURNAL,ActionKeys.APPROVE_ARTICLE);
  return JournalArticleLocalServiceUtil.approveArticle(user.getUserId(),articleId,version,articleURL,prefs);
}

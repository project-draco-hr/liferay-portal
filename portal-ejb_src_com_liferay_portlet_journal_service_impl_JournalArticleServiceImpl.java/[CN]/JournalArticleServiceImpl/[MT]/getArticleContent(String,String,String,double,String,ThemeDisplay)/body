{
  JournalArticlePermission.check(getPermissionChecker(),companyId,groupId,articleId,ActionKeys.VIEW);
  return JournalArticleLocalServiceUtil.getArticleContent(companyId,groupId,articleId,version,languageId,themeDisplay);
}

{
  Ticket ticket=(Ticket)entityCache.getResult(TicketModelImpl.ENTITY_CACHE_ENABLED,TicketImpl.class,primaryKey);
  if (ticket == _nullTicket) {
    return null;
  }
  if (ticket == null) {
    Session session=null;
    try {
      session=openSession();
      ticket=(Ticket)session.get(TicketImpl.class,primaryKey);
      if (ticket != null) {
        cacheResult(ticket);
      }
 else {
        entityCache.putResult(TicketModelImpl.ENTITY_CACHE_ENABLED,TicketImpl.class,primaryKey,_nullTicket);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(TicketModelImpl.ENTITY_CACHE_ENABLED,TicketImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return ticket;
}

{
  entityCache.putResult(TicketModelImpl.ENTITY_CACHE_ENABLED,TicketImpl.class,ticket.getPrimaryKey(),ticket);
  finderCache.putResult(FINDER_PATH_FETCH_BY_KEY,new Object[]{ticket.getKey()},ticket);
  ticket.resetOriginalValues();
}

{
  if (groups == null) {
    return false;
  }
 else {
    Iterator itr=groups.iterator();
    while (itr.hasNext()) {
      Group group=(Group)itr.next();
      if (group.getGroupId() == getGroupId()) {
        return true;
      }
    }
  }
  return false;
}

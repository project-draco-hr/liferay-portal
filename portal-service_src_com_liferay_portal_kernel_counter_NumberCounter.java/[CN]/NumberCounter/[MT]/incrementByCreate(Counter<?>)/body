{
  if (delta.getValue() instanceof Number) {
    return incrementByCreate((Number)delta.getValue());
  }
  throw new IllegalArgumentException("Invalid type " + delta.getValue().getClass().getName() + ". Expected type is java.lang.Number.");
}

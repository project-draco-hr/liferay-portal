{
  UnicodeProperties settingsProperties=layoutSetPrototypeLayoutSet.getSettingsProperties();
  if (newMergeFailCount == 0) {
    settingsProperties.remove(MERGE_FAIL_COUNT);
  }
 else {
    settingsProperties.setProperty(MERGE_FAIL_COUNT,String.valueOf(newMergeFailCount));
  }
}

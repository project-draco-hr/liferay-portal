{
  StopWatch stopWatch=null;
  if (_log.isDebugEnabled()) {
    stopWatch=new StopWatch();
    stopWatch.start();
  }
  try {
    doCopyPortletPermissions(targetLayout,sourceLayout);
  }
  finally {
    if (stopWatch != null) {
      stopWatch.stop();
      _log.debug("doCopyPortletPermissions() took " + stopWatch.getTime() + " ms");
    }
  }
}

{
  try {
    return _syncFilePersistence.containsChildState(parentFilePathName,state);
  }
 catch (  SQLException sqle) {
    if (_logger.isDebugEnabled()) {
      _logger.debug(sqle.getMessage(),sqle);
    }
    return false;
  }
}

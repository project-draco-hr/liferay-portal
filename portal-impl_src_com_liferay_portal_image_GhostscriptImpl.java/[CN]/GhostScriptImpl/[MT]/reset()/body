{
  if (isEnabled()) {
    try {
      _globalSearchPath=ImageMagickImpl.getInstance().getGlobalSearchPath();
      _commandPath=getCommandPath();
    }
 catch (    Exception e) {
      _log.warn(e,e);
    }
  }
}

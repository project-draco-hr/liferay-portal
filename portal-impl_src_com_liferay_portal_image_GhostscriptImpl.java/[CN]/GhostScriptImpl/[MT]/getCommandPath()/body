{
  String commandPath;
  if (OSDetector.isWindows()) {
    commandPath=getCommandPathWindows();
  }
 else {
    commandPath=getCommandPathUnix();
  }
  if (commandPath == null) {
    throw new FileNotFoundException("The Ghostscript command cannot be found.");
  }
  return commandPath;
}

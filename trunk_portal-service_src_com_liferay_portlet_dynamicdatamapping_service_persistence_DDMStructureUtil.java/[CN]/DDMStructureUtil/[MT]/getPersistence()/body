{
  if (_persistence == null) {
    _persistence=(DDMStructurePersistence)PortalBeanLocatorUtil.locate(DDMStructurePersistence.class.getName());
    ReferenceRegistry.registerReference(DDMStructureUtil.class,"_persistence");
  }
  return _persistence;
}

{
  String extraContent=super.getExtraContent(webXmlVersion,srcFile,displayName);
  extraContent+="<listener>" + "<listener-class>" + "com.liferay.portal.kernel.servlet.LayoutTemplateContextListener"+ "</listener-class>"+ "</listener>";
  return extraContent;
}

{
  String extraContent=super.getExtraContent(srcFile,displayName);
  extraContent+="<listener>" + "<listener-class>" + "com.liferay.portal.shared.servlet.LayoutTemplateContextListener"+ "</listener-class>"+ "</listener>";
  return extraContent;
}

{
  if ((array == null) || (array.length == 0) || (value == null)) {
    return false;
  }
  for (int i=0; i < array.length; i++) {
    if (value.equals(array[i])) {
      return true;
    }
  }
  return false;
}

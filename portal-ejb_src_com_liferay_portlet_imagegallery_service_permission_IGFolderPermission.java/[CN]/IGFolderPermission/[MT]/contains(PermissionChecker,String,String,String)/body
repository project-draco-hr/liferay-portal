{
  if (Validator.equals(folderId,IGFolder.DEFAULT_PARENT_FOLDER_ID)) {
    return PortletPermission.contains(permissionChecker,plid,PortletKeys.IMAGE_GALLERY,actionId);
  }
 else {
    return contains(permissionChecker,folderId,actionId);
  }
}

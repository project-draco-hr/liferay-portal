{
  if (isEscapedModel()) {
    return (BookmarksFolder)this;
  }
 else {
    BookmarksFolder model=new BookmarksFolderImpl();
    model.setEscapedModel(true);
    model.setUuid(Html.escape(getUuid()));
    model.setFolderId(getFolderId());
    model.setGroupId(getGroupId());
    model.setCompanyId(getCompanyId());
    model.setUserId(getUserId());
    model.setCreateDate(getCreateDate());
    model.setModifiedDate(getModifiedDate());
    model.setParentFolderId(getParentFolderId());
    model.setName(Html.escape(getName()));
    model.setDescription(Html.escape(getDescription()));
    model=(BookmarksFolder)Proxy.newProxyInstance(BookmarksFolder.class.getClassLoader(),new Class[]{BookmarksFolder.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

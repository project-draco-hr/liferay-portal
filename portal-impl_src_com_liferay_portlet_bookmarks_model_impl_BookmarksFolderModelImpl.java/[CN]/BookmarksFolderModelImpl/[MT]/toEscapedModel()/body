{
  if (isEscapedModel()) {
    return (BookmarksFolder)this;
  }
 else {
    return (BookmarksFolder)Proxy.newProxyInstance(BookmarksFolder.class.getClassLoader(),new Class[]{BookmarksFolder.class},new AutoEscapeBeanHandler(this));
  }
}

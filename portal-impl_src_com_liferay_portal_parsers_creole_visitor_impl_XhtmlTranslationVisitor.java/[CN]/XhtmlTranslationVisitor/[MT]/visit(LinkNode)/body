{
  write("<a href=\"");
  write(node.getLink());
  write("\">");
  if (node.hasAlternative()) {
    traverse(node.getAlternative().getNodes());
  }
 else {
    write(node.getLink());
  }
  write("</a>");
}

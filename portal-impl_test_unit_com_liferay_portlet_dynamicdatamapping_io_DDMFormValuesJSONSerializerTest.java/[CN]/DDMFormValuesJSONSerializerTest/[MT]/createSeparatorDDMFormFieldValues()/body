{
  List<DDMFormFieldValue> separatorDDMFormFieldValues=new ArrayList<DDMFormFieldValue>();
  for (int i=0; i < 2; i++) {
    separatorDDMFormFieldValues.add(createSeparatorDDMFormFieldValue(i));
  }
  return separatorDDMFormFieldValues;
}

{
  if (isEscapedModel()) {
    return (DLDocumentMetadataSet)this;
  }
 else {
    return (DLDocumentMetadataSet)Proxy.newProxyInstance(DLDocumentMetadataSet.class.getClassLoader(),new Class[]{DLDocumentMetadataSet.class},new AutoEscapeBeanHandler(this));
  }
}

{
  AssetSearcher assetSearcher=getAssetSearcher(searchContext,assetEntryQuery,start,end);
  Hits hits=assetSearcher.search(searchContext,SELECTED_FIELD_NAMES);
  List<AssetEntry> assetEntries=getAssetEntries(hits);
  return new BaseModelSearchResult<AssetEntry>(assetEntries,hits.getLength());
}

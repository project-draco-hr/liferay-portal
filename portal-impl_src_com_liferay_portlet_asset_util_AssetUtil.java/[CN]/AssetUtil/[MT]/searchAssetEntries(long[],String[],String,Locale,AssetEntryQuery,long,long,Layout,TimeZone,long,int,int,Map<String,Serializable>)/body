{
  SearchContext searchContext=SearchContextFactory.getInstance(assetCategoryIds,assetTagNames,keywords,locale,companyId,scopeGroupId,layout,timeZone,userId,attributes);
  return searchAssetEntries(searchContext,assetEntryQuery,start,end);
}

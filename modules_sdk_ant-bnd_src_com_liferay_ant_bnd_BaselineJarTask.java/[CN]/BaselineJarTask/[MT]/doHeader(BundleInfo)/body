{
  if (!bundleInfo.mismatch) {
    return;
  }
  project.log("[Baseline Report] Mode: " + _reportLevel,Project.MSG_WARN);
  String output="[Baseline Warning] Bundle Version Change Recommended: " + bundleInfo.suggestedVersion;
  project.log(output,Project.MSG_WARN);
  persistLog(output);
}

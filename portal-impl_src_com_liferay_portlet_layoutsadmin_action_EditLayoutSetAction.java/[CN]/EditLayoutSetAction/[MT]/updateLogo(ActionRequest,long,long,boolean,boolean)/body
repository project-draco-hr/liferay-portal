{
  UploadPortletRequest uploadPortletRequest=PortalUtil.getUploadPortletRequest(actionRequest);
  boolean useLogo=ParamUtil.getBoolean(actionRequest,"useLogo");
  InputStream inputStream=uploadPortletRequest.getFileAsStream("logoFileName");
  if (useLogo && (inputStream == null)) {
    if (hasLogo) {
      return;
    }
    throw new UploadException("No logo uploaded for use");
  }
  LayoutSetServiceUtil.updateLogo(liveGroupId,privateLayout,useLogo,inputStream);
  if (stagingGroupId > 0) {
    LayoutSetServiceUtil.updateLogo(stagingGroupId,privateLayout,useLogo,inputStream);
  }
}

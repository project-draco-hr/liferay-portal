{
  List<Group> groups=doFindByC_C_N_D(_bookmarkFolderResourceAction.getActionId(),_resourceTypePermission.getName(),_resourceTypePermission.getRoleId());
  for (  Group group : groups) {
    if (group.getGroupId() == _group.getGroupId()) {
      return;
    }
  }
  Assert.fail("The method findByC_C_N_D should have returned the group " + _group.getGroupId());
}

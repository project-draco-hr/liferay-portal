{
  List themeIds=new ArrayList();
  try {
    for (int i=0; i < xmls.length; i++) {
      Iterator itr=_readThemes(servletContextName,ctx,xmls[i]).iterator();
      while (itr.hasNext()) {
        String themeId=(String)itr.next();
        if (!themeIds.contains(themeId)) {
          themeIds.add(themeId);
        }
      }
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  _themesPool.clear();
  return themeIds;
}

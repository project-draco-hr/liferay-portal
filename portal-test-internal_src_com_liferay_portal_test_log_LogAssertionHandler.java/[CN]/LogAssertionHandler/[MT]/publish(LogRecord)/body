{
  Level level=logRecord.getLevel();
  if (level.equals(Level.SEVERE)) {
    StringBundler sb=new StringBundler(6);
    sb.append("{level=");
    sb.append(logRecord.getLevel());
    sb.append(", loggerName=");
    sb.append(logRecord.getLoggerName());
    sb.append(", message=");
    sb.append(logRecord.getMessage());
    LogAssertionExecutor logAssertionExecutor=LogAssertionExecutorUtil.getInstance();
    logAssertionExecutor.caughtFailure(Thread.currentThread(),new AssertionError(new AssertionError(sb.toString(),logRecord.getThrown())));
  }
}

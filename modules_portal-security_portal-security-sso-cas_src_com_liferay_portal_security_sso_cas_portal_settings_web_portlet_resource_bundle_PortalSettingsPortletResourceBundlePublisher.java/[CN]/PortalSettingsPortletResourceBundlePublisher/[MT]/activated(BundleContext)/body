{
  Bundle bundle=bundleContext.getBundle();
  Enumeration<URL> propertiesFiles=bundle.findEntries("/content","Language*.properties",false);
  while (propertiesFiles.hasMoreElements()) {
    registerResourceBundle(bundleContext,propertiesFiles.nextElement());
  }
}

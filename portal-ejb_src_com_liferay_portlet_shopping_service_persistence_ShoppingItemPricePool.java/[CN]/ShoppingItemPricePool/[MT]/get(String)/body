{
  ShoppingItemPrice shoppingItemPrice=_instance._get(itemPriceId);
  _log.info("Get " + itemPriceId + " is "+ ((shoppingItemPrice == null) ? "NOT " : "")+ "in cache");
  return shoppingItemPrice;
}

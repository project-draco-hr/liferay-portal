{
  DBMetadata dbMetadata=new DBMetadata(connection);
  if (!dbMetadata.hasColumnType(UserNotificationEventTable.class,"type_","VARCHAR(200) null")) {
    alter(UserNotificationEventTable.class,new AlterColumnType("type_","VARCHAR(200) null"));
  }
}

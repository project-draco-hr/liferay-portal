{
  try {
    PortalPreferences prefs=PortletPreferencesFactory.getPortalPreferences(req);
    return prefs.getValue(CLASS_NAME,key,defaultValue);
  }
 catch (  Exception e) {
    _log.error(StackTraceUtil.getStackTrace(e));
    return null;
  }
}

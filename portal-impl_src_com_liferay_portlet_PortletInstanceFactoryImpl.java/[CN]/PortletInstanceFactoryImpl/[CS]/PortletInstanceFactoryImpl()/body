{
  _pool=new ConcurrentHashMap<>();
  Registry registry=RegistryUtil.getRegistry();
  _serviceTracker=registry.trackServices(InvokerPortletFactory.class,new InvokerPortletFactoryTrackerCustomizer());
  _serviceTracker.open();
}

{
  _pool=new ConcurrentHashMap<>();
  Registry registry=RegistryUtil.getRegistry();
  Filter filter=registry.getFilter("(&(javax.portlet.name=" + "com_liferay_monitoring_web_filter_" + "MonitoringInvokerPortletFactoryImpl)"+ "(objectClass=" + InvokerPortletFactory.class.getName() + "))");
  _serviceTracker=registry.trackServices(filter,new InvokerPortletFactoryTrackerCustomizer());
  _serviceTracker.open();
}

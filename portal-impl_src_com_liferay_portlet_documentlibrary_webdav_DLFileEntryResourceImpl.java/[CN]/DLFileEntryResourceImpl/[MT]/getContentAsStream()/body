{
  try {
    return DLFileEntryLocalServiceUtil.getFileAsStream(_webDavRequest.getCompanyId(),_webDavRequest.getUserId(),_fileEntry.getFolderId(),_fileEntry.getName());
  }
 catch (  Exception e) {
    throw new WebDAVException(e);
  }
}

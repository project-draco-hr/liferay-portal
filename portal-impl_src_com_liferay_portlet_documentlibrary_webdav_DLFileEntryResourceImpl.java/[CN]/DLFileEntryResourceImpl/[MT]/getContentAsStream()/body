{
  try {
    return DLFileEntryLocalServiceUtil.getFileAsStream(_webDavReq.getCompanyId(),_webDavReq.getUserId(),_fileEntry.getFolderId(),_fileEntry.getName());
  }
 catch (  Exception e) {
    throw new WebDAVException(e);
  }
}

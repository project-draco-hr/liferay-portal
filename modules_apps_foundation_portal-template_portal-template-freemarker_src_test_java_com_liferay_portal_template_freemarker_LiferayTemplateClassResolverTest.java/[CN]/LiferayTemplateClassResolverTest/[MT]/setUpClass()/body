{
  Registry registry=RegistryUtil.getRegistry();
  Filter filter=registry.getFilter("(&(objectClass=" + TemplateClassResolver.class.getName() + "))");
  _serviceTracker=registry.trackServices(filter);
  _serviceTracker.open();
  _liferayTemplateClassResolver=(TemplateClassResolver)_serviceTracker.getService();
}

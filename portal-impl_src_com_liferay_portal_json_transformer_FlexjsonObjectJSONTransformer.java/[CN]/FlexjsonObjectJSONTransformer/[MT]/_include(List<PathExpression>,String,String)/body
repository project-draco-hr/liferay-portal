{
  for (  String name : names) {
    PathExpression pathExpression=new PathExpression(path + name,true);
    if (!pathExpressions.contains(pathExpression)) {
      pathExpressions.add(0,pathExpression);
    }
  }
}

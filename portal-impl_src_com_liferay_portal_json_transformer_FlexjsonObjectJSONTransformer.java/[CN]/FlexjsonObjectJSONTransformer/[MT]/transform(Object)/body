{
  Class<?> type=resolveClass(object);
  List<PathExpression> pathExpressions=(List<PathExpression>)BeanUtil.getDeclaredProperty(getContext(),"pathExpressions");
  String path=getPath();
  addIncludesAndExcludesForType(type,pathExpressions,path);
  super.transform(object);
}

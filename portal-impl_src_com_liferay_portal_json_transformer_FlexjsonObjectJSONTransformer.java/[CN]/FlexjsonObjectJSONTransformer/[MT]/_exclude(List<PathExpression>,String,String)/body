{
  for (  String name : names) {
    PathExpression pathExpression=new PathExpression(path + name,false);
    if (!pathExpressions.contains(pathExpression)) {
      pathExpressions.add(pathExpression);
    }
  }
}

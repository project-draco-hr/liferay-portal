{
  if (_repositoryService != null) {
    throw new PrincipalException("Update is forbidden when using the Repository interface");
  }
  return _repositoryLocalService.updateRepository(repository);
}

{
  if (variable == null) {
    throw new Exception("Invalid variable: " + variable);
  }
  variable.trim();
  if (!isVariable(variable)) {
    throw new Exception("Property not found in 'test.case.available.property.names': " + variable);
  }
}

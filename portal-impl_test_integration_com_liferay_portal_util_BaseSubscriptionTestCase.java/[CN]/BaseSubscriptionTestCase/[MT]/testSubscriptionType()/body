{
  long typeId=addType();
  long baseModelId=addBaseModelWithType(_PARENT_CONTAINER_MODEL_ID_DEFAULT,typeId);
  addSubscriptionType(typeId);
  updateEntry(baseModelId);
  Assert.assertEquals(1,MailServiceTestUtil.getInboxSize());
}

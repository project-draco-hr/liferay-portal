{
  return new Enumeration<String>(){
    @Override public boolean hasMoreElements(){
      return _keys.hasNext();
    }
    @Override public String nextElement(){
      return _keys.next();
    }
    private Iterator<String> _keys=_initParameterMap.keySet().iterator();
  }
;
}

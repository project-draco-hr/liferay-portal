{
  WikiPage copyPage=addPage(page.getUserId(),page.getNodeId(),RandomTestUtil.randomString(),page.getContent(),approved,serviceContext);
  WikiPageLocalServiceUtil.copyPageAttachments(page.getUserId(),page.getNodeId(),page.getTitle(),copyPage.getNodeId(),copyPage.getTitle());
  return copyPage;
}

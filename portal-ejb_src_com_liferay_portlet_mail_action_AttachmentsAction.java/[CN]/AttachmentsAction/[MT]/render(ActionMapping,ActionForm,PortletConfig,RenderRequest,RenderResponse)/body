{
  try {
    ActionUtil.getMessageModel(req);
    ActionUtil.getAttachments(req);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return mapping.findForward("portlet.mail.attachments");
}

{
  return ((group.isCommunity() && (roleName.equals(RoleConstants.COMMUNITY_MEMBER))) || (group.isOrganization() && (roleName.equals(RoleConstants.ORGANIZATION_MEMBER)))) && GroupLocalServiceUtil.hasUserGroup(userId,group.getGroupId());
}

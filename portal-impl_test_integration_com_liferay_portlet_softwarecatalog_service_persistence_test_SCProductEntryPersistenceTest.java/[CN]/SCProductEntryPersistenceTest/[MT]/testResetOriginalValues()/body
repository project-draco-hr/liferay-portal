{
  SCProductEntry newSCProductEntry=addSCProductEntry();
  _persistence.clearCache();
  SCProductEntry existingSCProductEntry=_persistence.findByPrimaryKey(newSCProductEntry.getPrimaryKey());
  Assert.assertTrue(Validator.equals(existingSCProductEntry.getRepoGroupId(),ReflectionTestUtil.invoke(existingSCProductEntry,"getOriginalRepoGroupId",new Class<?>[0])));
  Assert.assertTrue(Validator.equals(existingSCProductEntry.getRepoArtifactId(),ReflectionTestUtil.invoke(existingSCProductEntry,"getOriginalRepoArtifactId",new Class<?>[0])));
}

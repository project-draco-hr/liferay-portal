{
  User user=UserLocalServiceUtil.getUser(TestPropsValues.getUserId());
  return PollsVoteLocalServiceUtil.addVote(user.getUserId(),questionId,choiceId,ServiceTestUtil.getServiceContext(groupId));
}

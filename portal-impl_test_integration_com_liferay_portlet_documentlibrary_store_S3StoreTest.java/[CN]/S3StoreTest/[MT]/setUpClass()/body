{
  Assume.assumeTrue("Property \"" + PropsKeys.DL_STORE_S3_ACCESS_KEY + "\" must be set",Validator.isNotNull(PropsUtil.get(PropsKeys.DL_STORE_S3_ACCESS_KEY)));
  Assume.assumeTrue("Property \"" + PropsKeys.DL_STORE_S3_SECRET_KEY + "\" must be set",Validator.isNotNull(PropsUtil.get(PropsKeys.DL_STORE_S3_SECRET_KEY)));
  Assume.assumeTrue("Property \"" + PropsKeys.DL_STORE_S3_BUCKET_NAME + "\" must be set",Validator.isNotNull(PropsUtil.get(PropsKeys.DL_STORE_S3_BUCKET_NAME)));
}

{
  Object value=properties.get(ActionExecutorConstants.ACTION_EXECUTOR_LANGUAGE_KEY);
  String[] languages=GetterUtil.getStringValues(value,new String[]{String.valueOf(value)});
  for (  String language : languages) {
    String actionExecutorKey=getActionExecutorKey(language,ClassUtil.getClassName(actionExecutor));
    _actionExecutors.put(actionExecutorKey,actionExecutor);
  }
}

{
  List<FormStructureSoap> soapModels=new ArrayList<FormStructureSoap>(models.size());
  for (  FormStructure model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new FormStructureSoap[soapModels.size()]);
}

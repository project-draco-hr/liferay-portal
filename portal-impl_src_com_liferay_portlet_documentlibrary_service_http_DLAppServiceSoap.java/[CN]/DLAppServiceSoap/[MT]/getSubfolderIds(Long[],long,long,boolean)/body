{
  try {
    DLAppServiceUtil.getSubfolderIds(ListUtil.toList(folderIds),repositoryId,folderId,recurse);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

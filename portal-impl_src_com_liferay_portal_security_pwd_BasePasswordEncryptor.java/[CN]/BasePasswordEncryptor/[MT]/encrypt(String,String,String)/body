{
  if (Validator.isNull(clearTextPassword)) {
    throw new PwdEncryptorException("Unable to encrypt empty passwords.");
  }
  return doEncrypt(algorithm,clearTextPassword,currentEncryptedPassword);
}

{
  if (_extraSettingsProperties == null) {
    _extraSettingsProperties=new NullSafeProperties();
    try {
      PropertiesUtil.load(_extraSettingsProperties,super.getExtraSettings());
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
  return _extraSettingsProperties;
}

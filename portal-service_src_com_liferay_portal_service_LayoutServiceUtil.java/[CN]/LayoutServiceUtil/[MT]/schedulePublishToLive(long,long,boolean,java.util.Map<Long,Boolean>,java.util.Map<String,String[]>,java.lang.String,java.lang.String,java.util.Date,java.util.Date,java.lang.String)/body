{
  LayoutService layoutService=LayoutServiceFactory.getService();
  layoutService.schedulePublishToLive(stagingGroupId,liveGroupId,privateLayout,layoutIdMap,parameterMap,scope,cronText,startDate,endDate,description);
}

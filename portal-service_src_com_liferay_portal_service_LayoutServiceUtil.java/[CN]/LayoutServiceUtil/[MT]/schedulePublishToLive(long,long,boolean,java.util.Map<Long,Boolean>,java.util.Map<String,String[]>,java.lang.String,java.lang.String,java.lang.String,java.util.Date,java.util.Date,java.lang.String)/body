{
  LayoutService layoutService=LayoutServiceFactory.getService();
  layoutService.schedulePublishToLive(stagingGroupId,liveGroupId,privateLayout,layoutIdMap,parameterMap,scope,groupName,cronText,startDate,endDate,description);
}

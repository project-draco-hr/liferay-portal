{
  LayoutService layoutService=LayoutServiceFactory.getService();
  layoutService.schedulePublishToLiveEvent(stagingGroupId,liveGroupId,privateLayout,parameterMap,cronText,scope,layoutIdMap,startDate,endDate,description);
}

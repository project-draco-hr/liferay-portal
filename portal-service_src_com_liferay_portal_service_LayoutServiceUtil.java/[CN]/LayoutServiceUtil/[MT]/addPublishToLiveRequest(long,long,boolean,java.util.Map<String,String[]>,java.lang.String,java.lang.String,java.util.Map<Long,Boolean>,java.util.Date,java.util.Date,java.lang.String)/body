{
  LayoutService layoutService=LayoutServiceFactory.getService();
  layoutService.addPublishToLiveRequest(stagingGroupId,liveGroupId,privateLayout,parameterMap,cronText,scope,layoutIdMap,startDate,endDate,description);
}

{
  if (!isEditButtonVisible()) {
    return;
  }
  PortletURL portletURL=getRenderURL("/document_library/edit_file_entry");
  portletURL.setParameter("backURL",_getCurrentURL());
  addURLMenuItem(menuItems,"icon-edit",DLMenuItemKeys.EDIT,"edit",portletURL.toString());
}

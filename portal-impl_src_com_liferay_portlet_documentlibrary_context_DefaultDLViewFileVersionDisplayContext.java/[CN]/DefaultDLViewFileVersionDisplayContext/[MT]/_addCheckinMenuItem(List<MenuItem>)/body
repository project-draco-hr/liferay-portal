{
  if (!_fileEntryDisplayContextHelper.isCheckinButtonVisible()) {
    return;
  }
  _addURLUIItem(new URLMenuItem(),menuItems,DLUIItemKeys.CHECKIN,"icon-lock","checkin",_getActionURL("/document_library/edit_file_entry",Constants.CHECKIN));
}

{
  if (!isDownloadButtonVisible()) {
    return;
  }
  _addURLToolbarItem(toolbarItems,DLToolbarItemKeys.DOWNLOAD,"icon-download",UnicodeLanguageUtil.get(_request,"download"),DLUtil.getDownloadURL(_fileEntry,_fileVersion,_themeDisplay,StringPool.BLANK));
}

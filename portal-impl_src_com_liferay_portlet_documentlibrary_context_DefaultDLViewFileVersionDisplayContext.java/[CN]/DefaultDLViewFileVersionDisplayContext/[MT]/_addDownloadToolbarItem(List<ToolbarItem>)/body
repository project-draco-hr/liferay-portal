{
  if (!isDownloadButtonVisible()) {
    return;
  }
  _addURLUIItem(new URLToolbarItem(),toolbarItems,DLUIItemKeys.DOWNLOAD,"icon-download",UnicodeLanguageUtil.get(_request,"download"),DLUtil.getDownloadURL(_fileEntry,_fileVersion,_themeDisplay,StringPool.BLANK));
}

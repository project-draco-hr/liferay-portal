{
  if (!_fileEntryDisplayContextHelper.isCheckoutDocumentButtonVisible()) {
    return;
  }
  _addURLUIItem(new URLMenuItem(),menuItems,DLUIItemKeys.CHECKOUT,"icon-unlock","checkout[document]",_getActionURL("/document_library/edit_file_entry",Constants.CHECKOUT));
}

{
  if ((_defaultDLFileEntryActionsDisplayContextHelper.isCheckedOut() || _defaultDLFileEntryActionsDisplayContextHelper.isPending()) && !_isDLFileEntryDraftsEnabled()) {
    return true;
  }
  return false;
}

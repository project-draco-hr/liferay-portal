{
  if (!isEditButtonVisible()) {
    return;
  }
  PortletURL portletURL=_getRenderURL("/document_library/edit_file_entry");
  _addURLUIItem(new URLToolbarItem(),toolbarItems,DLToolbarItemKeys.EDIT,"icon-edit",UnicodeLanguageUtil.get(_request,"edit"),portletURL.toString());
}

{
  if (!isCheckoutDocumentButtonVisible()) {
    return;
  }
  _addJavascriptUIItem(new JavascriptToolbarItem(),toolbarItems,DLToolbarItemKeys.CHECKOUT,"icon-unlock",UnicodeLanguageUtil.get(_request,"checkout[document]"),getSubmitFormJavascript(Constants.CHECKOUT,null));
}

{
  if (!isMoveButtonVisible()) {
    return;
  }
  PortletURL portletURL=_getRenderURL("/document_library/move_entry");
  _addURLToolbarItem(toolbarItems,DLToolbarItemKeys.MOVE,"icon-move",UnicodeLanguageUtil.get(_request,"move"),portletURL.toString());
}

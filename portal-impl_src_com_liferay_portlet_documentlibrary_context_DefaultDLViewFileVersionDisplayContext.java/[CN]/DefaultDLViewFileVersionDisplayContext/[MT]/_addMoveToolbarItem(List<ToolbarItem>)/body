{
  if (!isMoveButtonVisible()) {
    return;
  }
  PortletURL portletURL=_getRenderURL("/document_library/move_entry");
  _addURLUIItem(new URLToolbarItem(),toolbarItems,DLToolbarItemKeys.MOVE,"icon-move",UnicodeLanguageUtil.get(_request,"move"),portletURL.toString());
}

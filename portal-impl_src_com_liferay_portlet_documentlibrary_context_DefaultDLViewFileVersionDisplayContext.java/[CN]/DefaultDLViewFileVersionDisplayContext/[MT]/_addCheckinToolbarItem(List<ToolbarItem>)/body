{
  if (!isCheckinButtonVisible()) {
    return;
  }
  _addJavascriptUIItem(new JavascriptToolbarItem(),toolbarItems,DLToolbarItemKeys.CHECKIN,"icon-lock",UnicodeLanguageUtil.get(_request,"checkin"),getSubmitFormJavascript(Constants.CHECKIN,null));
}

{
  if (portalExecutorManager == null) {
    return;
  }
  ThreadPoolExecutor threadPoolExecutor=portalExecutorManager.getPortalExecutor(getName());
  if (force) {
    threadPoolExecutor.shutdownNow();
  }
 else {
    threadPoolExecutor.shutdown();
  }
}

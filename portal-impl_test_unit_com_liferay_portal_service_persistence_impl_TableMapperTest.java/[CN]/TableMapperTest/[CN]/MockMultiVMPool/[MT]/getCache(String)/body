{
  PortalCache<?,?> portalCache=_portalCaches.get(name);
  if (portalCache == null) {
    portalCache=new TestPortalCache<>(name);
    _portalCaches.put(name,portalCache);
  }
  return (PortalCache<? extends Serializable,? extends Serializable>)portalCache;
}

{
  for (  Class<?> clazz : value) {
    try {
      return parse(clazz);
    }
 catch (    IllegalArgumentException iae) {
    }
  }
  throw new IllegalArgumentException("No valid value found in the set");
}

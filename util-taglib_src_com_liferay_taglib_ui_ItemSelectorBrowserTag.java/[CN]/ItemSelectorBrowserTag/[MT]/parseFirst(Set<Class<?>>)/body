{
  for (  Class<?> clazz : value) {
    try {
      return parse(clazz);
    }
 catch (    IllegalArgumentException iae) {
    }
  }
  throw new IllegalArgumentException("No return type found for value " + value);
}

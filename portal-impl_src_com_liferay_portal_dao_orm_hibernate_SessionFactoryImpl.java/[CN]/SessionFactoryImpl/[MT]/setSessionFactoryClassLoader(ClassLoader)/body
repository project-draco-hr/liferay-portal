{
  ClassLoader portalClassLoader=ClassLoaderUtil.getPortalClassLoader();
  if (sessionFactoryClassLoader == portalClassLoader) {
    _sessionFactoryClassLoader=sessionFactoryClassLoader;
  }
 else {
    _sessionFactoryClassLoader=new PreloadClassLoader(sessionFactoryClassLoader,getPreloadClassLoaderClasses());
  }
}

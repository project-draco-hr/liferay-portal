{
  DLFileRank dlFileRank=fetchByPrimaryKey(dlFileRankPK);
  if (dlFileRank == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No DLFileRank exists with the primary key " + dlFileRankPK);
    }
    throw new NoSuchFileRankException("No DLFileRank exists with the primary key " + dlFileRankPK);
  }
  return dlFileRank;
}

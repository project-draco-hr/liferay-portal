{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingItemField shoppingItemField) method. To improve performance, call update(ShoppingItemField shoppingItemField, boolean merge).");
  }
  return update(shoppingItemField,false);
}

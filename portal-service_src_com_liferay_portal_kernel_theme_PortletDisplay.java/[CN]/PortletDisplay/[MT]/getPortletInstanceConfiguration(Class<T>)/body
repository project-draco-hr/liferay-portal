{
  String portletId=Validator.isNull(_portletResource) ? _id : _portletResource;
  PortletInstance portletInstance=PortletInstance.fromPortletInstanceKey(portletId);
  return ConfigurationProviderUtil.getPortletInstanceConfiguration(clazz,_themeDisplay.getLayout(),portletInstance);
}

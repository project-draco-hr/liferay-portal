{
  BlogsCategoryPermission.check(getPermissionChecker(),parentCategoryId,ActionKeys.ADD_CATEGORY);
  return BlogsCategoryLocalServiceUtil.addCategory(getUserId(),parentCategoryId,name,description,addCommunityPermissions,addGuestPermissions);
}

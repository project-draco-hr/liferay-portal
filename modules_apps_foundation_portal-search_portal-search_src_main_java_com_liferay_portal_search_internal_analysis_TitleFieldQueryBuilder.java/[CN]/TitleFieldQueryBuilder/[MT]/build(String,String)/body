{
  FullTextQueryBuilder builder=new FullTextQueryBuilder(keywordTokenizer);
  builder.setAutocomplete(true);
  builder.setExactMatchBoost(_exactMatchBoost);
  return builder.build(field,keywords);
}

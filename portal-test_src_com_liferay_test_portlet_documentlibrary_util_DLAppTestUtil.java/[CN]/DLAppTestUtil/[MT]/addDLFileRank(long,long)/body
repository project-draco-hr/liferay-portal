{
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(groupId);
  return DLAppLocalServiceUtil.addFileRank(groupId,TestPropsValues.getCompanyId(),TestPropsValues.getUserId(),fileEntryId,serviceContext);
}

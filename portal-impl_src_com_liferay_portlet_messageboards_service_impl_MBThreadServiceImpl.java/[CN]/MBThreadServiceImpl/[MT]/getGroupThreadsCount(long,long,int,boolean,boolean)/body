{
  long[] categoryIds=mbCategoryService.getCategoryIds(groupId,MBCategoryConstants.DEFAULT_PARENT_CATEGORY_ID);
  if (categoryIds.length == 0) {
    return 0;
  }
  if (userId <= 0) {
    if (status == WorkflowConstants.STATUS_ANY) {
      return mbThreadPersistence.countByG_C(groupId,categoryIds);
    }
 else {
      return mbThreadPersistence.countByG_C_S(groupId,categoryIds,status);
    }
  }
 else {
    if (subscribed) {
      return mbThreadFinder.filterCountByS_G_C_U_S(groupId,categoryIds,userId,status);
    }
 else {
      if (includeAnonymous) {
        return mbMessageFinder.filterCountByG_C_U_S(groupId,categoryIds,userId,status);
      }
 else {
        return mbMessageFinder.filterCountByG_C_U_A_S(groupId,categoryIds,userId,false,status);
      }
    }
  }
}

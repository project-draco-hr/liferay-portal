{
  if (!_hasNextCalled) {
    hasNext();
  }
  _hasNextCalled=false;
  if (StringPool.UTF8.equals(_encoding) && (_line.charAt(0) == _UNICODE_BYTE_ORDER_MARK)) {
    _line=_line.substring(1);
  }
  return new DictionaryEntry(_line);
}

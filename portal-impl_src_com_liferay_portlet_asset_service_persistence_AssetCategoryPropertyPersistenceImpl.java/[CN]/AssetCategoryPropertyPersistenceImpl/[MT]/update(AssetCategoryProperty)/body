{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetCategoryProperty assetCategoryProperty) method. Use update(AssetCategoryProperty assetCategoryProperty, boolean merge) instead.");
  }
  return update(assetCategoryProperty,false);
}

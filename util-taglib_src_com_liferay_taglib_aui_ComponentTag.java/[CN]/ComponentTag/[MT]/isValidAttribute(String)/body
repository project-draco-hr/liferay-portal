{
  String excludeAttributes=getExcludeAttributes();
  if (excludeAttributes == null) {
    return true;
  }
  List<String> excludeAttributesList=Arrays.asList(StringUtil.split(excludeAttributes));
  if (key.equals("dynamicAttributes") || excludeAttributesList.contains(key)) {
    return false;
  }
  return true;
}

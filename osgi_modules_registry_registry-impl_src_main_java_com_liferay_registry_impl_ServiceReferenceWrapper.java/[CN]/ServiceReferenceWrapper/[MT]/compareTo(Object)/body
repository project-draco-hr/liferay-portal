{
  if (!(object instanceof ServiceReferenceWrapper)) {
    throw new IllegalArgumentException();
  }
  ServiceReferenceWrapper<T> serviceReferenceWrapper=(ServiceReferenceWrapper<T>)object;
  return _serviceReference.compareTo(serviceReferenceWrapper.getServiceReference());
}

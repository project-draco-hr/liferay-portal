{
  prototypeLayout=layoutPrototypeLayout;
  journalArticle=globalJournalArticle;
  journalContentPortletId=addJournalContentPortletToLayout(TestPropsValues.getUserId(),layoutPrototypeLayout,journalArticle,"column-1");
  layout=LayoutTestUtil.addLayout(group.getGroupId(),RandomTestUtil.randomString(),true,layoutPrototype,true);
  layout=propagateChanges(layout);
}

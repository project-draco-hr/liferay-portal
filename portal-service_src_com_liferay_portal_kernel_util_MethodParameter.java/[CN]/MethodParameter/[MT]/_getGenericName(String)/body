{
  if (typeName.equals(StringPool.STAR)) {
    return null;
  }
  if (typeName.startsWith(StringPool.PLUS) || typeName.startsWith(StringPool.MINUS)) {
    typeName=typeName.substring(1,typeName.length());
  }
  return typeName;
}

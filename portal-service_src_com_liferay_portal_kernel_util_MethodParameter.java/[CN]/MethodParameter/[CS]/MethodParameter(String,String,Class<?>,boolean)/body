{
  _name=name;
  _signatures=signatures;
  _type=type;
  if (initialize) {
    try {
      getGenericTypes();
    }
 catch (    ClassNotFoundException cnfe) {
      throw new IllegalArgumentException(cnfe);
    }
  }
}

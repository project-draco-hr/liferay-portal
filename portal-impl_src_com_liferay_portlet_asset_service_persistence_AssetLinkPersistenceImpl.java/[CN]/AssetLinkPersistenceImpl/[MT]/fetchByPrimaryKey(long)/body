{
  AssetLink assetLink=(AssetLink)EntityCacheUtil.getResult(AssetLinkModelImpl.ENTITY_CACHE_ENABLED,AssetLinkImpl.class,linkId,this);
  if (assetLink == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (assetLink == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      assetLink=(AssetLink)session.get(AssetLinkImpl.class,Long.valueOf(linkId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (assetLink == null)) {
        EntityCacheUtil.putResult(AssetLinkModelImpl.ENTITY_CACHE_ENABLED,AssetLinkImpl.class,linkId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(assetLink);
      }
      closeSession(session);
    }
  }
  return assetLink;
}

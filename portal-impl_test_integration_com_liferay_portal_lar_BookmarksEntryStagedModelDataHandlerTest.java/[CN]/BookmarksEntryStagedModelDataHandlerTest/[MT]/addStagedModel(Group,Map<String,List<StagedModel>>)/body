{
  List<StagedModel> dependentStagedModels=dependentStagedModelsMap.get(BookmarksFolder.class.getName());
  BookmarksFolder folder=(BookmarksFolder)dependentStagedModels.get(0);
  return BookmarksTestUtil.addEntry(group.getGroupId(),folder.getFolderId(),true);
}

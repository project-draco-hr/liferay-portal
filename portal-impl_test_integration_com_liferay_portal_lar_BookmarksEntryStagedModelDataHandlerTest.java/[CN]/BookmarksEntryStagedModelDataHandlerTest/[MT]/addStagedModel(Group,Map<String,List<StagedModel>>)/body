{
  List<StagedModel> folderList=relatedStagedModels.get(BookmarksFolder.class.getName());
  BookmarksFolder folder=(BookmarksFolder)folderList.get(0);
  return BookmarksTestUtil.addEntry(group.getGroupId(),folder.getFolderId(),true);
}

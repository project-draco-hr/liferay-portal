{
  List<StagedModel> folders=dependentStagedModels.get(BookmarksFolder.class.getName());
  Assert.assertEquals(1,folders.size());
  BookmarksFolder folder=(BookmarksFolder)folders.get(0);
  BookmarksFolderLocalServiceUtil.getBookmarksFolderByUuidAndGroupId(folder.getUuid(),group.getGroupId());
}

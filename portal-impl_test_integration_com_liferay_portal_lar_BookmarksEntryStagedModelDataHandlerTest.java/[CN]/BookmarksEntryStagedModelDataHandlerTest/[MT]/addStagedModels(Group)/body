{
  Map<String,List<StagedModel>> stagedModelsMap=new HashMap<String,List<StagedModel>>();
  List<StagedModel> stagedModels=new ArrayList<StagedModel>();
  stagedModels.add(BookmarksTestUtil.addFolder(group.getGroupId(),ServiceTestUtil.randomString()));
  stagedModelsMap.put(BookmarksFolder.class.getName(),stagedModels);
  return stagedModelsMap;
}

{
  _availableLocales=LanguageUtil.getAvailableLocales();
  _oldLocale=LocaleUtil.getDefault();
  CompanyTestUtil.resetCompanyLocales(PortalUtil.getDefaultCompanyId(),Arrays.asList(LocaleUtil.CANADA_FRENCH,LocaleUtil.SPAIN,LocaleUtil.US),LocaleUtil.US);
}

{
  BlogsEntry entry=_blogsEntryLocalService.fetchBlogsEntry(GetterUtil.getLong(primKey));
  if (entry == null) {
    return;
  }
  entry.setModifiedDate(new Date());
  _blogsEntryLocalService.updateBlogsEntry(entry);
}

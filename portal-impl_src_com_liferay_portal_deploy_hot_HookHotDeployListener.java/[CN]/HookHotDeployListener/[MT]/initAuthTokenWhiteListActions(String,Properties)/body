{
  String[] tokenIgnoreActions=StringUtil.split(portalProperties.getProperty(AUTH_TOKEN_IGNORE_ACTIONS));
  for (  String tokenIgnoreAction : tokenIgnoreActions) {
    registerService(servletContextName,AUTH_TOKEN_IGNORE_ACTIONS + tokenIgnoreAction,Object.class,new Object());
  }
}

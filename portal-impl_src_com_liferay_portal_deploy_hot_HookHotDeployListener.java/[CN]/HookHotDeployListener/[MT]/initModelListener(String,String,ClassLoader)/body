{
  ModelListener<BaseModel<?>> modelListener=(ModelListener<BaseModel<?>>)portletClassLoader.loadClass(modelListenerClass).newInstance();
  modelListener=new InvokerModelListener<BaseModel<?>>(modelListener,portletClassLoader);
  BasePersistence persistence=getPersistence(modelName);
  persistence.registerListener(modelListener);
  return modelListener;
}

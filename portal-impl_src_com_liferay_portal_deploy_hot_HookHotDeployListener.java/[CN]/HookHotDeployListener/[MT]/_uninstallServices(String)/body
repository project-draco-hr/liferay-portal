{
  List<ServiceBag> serviceBags=_servicesContainer.findByServletContextName(servletContextName);
  for (  ServiceBag serviceBag : serviceBags) {
    Object serviceProxy=PortalBeanLocatorUtil.locate(serviceBag.getServiceType());
    _setUnwrappedService(serviceProxy,serviceBag.getOldService());
  }
}

{
  Map<Object,ServiceRegistration<?>> serviceRegistrations=getServiceRegistrations(servletContextName);
  initAuthFailures(portletClassLoader,portalProperties,AUTH_FAILURE,serviceRegistrations);
  initAuthFailures(portletClassLoader,portalProperties,AUTH_MAX_FAILURES,serviceRegistrations);
}

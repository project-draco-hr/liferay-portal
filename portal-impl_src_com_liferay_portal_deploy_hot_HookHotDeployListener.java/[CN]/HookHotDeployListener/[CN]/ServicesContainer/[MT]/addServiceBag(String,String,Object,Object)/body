{
  if (findByServiceType(serviceType) != null) {
    throw new IllegalStateException("An old ServiceBag with type:" + serviceType + " already exist!");
  }
  _serviceBags.add(new ServiceBag(servletContextName,serviceType,oldService,newService));
}

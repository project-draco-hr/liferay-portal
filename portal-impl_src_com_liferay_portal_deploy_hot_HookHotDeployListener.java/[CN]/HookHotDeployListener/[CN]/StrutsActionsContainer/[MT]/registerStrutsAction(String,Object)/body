{
  if (strutsAction instanceof StrutsAction) {
    StrutsActionRegistry.register(path,(StrutsAction)strutsAction);
  }
 else {
    StrutsActionRegistry.register(path,(StrutsPortletAction)strutsAction);
  }
  _paths.add(path);
}

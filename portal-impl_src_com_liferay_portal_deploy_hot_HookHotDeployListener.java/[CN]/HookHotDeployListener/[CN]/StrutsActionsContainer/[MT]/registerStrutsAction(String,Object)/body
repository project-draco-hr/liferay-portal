{
  if (strutsAction instanceof StrutsAction) {
    StrutsActionRegistryUtil.register(path,(StrutsAction)strutsAction);
  }
 else {
    StrutsActionRegistryUtil.register(path,(StrutsPortletAction)strutsAction);
  }
  _paths.add(path);
}

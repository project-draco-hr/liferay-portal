{
  InvokerFilter invokerFilter=getInvokerFilter();
  Filter originalFilter=invokerFilter.registerFilter(filterName,filter);
  _filterConfigs.put(filterName,filterConfig);
  _filters.put(filterName,originalFilter);
}

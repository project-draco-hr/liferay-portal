{
  InvokerFilter invokerFilter=getInvokerFilter();
  Filter previousFilter=invokerFilter.registerFilter(filterName,filter);
  _filterConfigs.put(filterName,filterConfig);
  _filters.put(filterName,previousFilter);
}

{
  MBCategory category=(MBCategory)parentBaseModel;
  MBMessage message=MBTestUtil.addMBMessage(category.getCategoryId(),getSearchKeywords(),approved,serviceContext);
  return message.getThread();
}

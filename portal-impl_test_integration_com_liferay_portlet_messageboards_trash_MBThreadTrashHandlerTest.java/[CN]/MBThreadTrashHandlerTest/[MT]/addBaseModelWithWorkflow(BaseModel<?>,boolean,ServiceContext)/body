{
  MBCategory category=(MBCategory)parentBaseModel;
  String keywords=getSearchKeywords();
  MBMessage message=MBTestUtil.addMessageWithWorkflow(serviceContext.getUserId(),category.getGroupId(),category.getCategoryId(),keywords,keywords,approved,serviceContext);
  return message.getThread();
}

{
  List<TrashEntry> trashEntries=_trashEntryServiceAdapter.getEntries(repositoryId,className);
  for (  TrashEntry trashEntry : trashEntries) {
    _trashEntryServiceAdapter.deleteTrashEntry(trashEntry);
  }
}

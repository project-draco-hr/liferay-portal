{
  List<TrashEntry> trashEntries=TrashEntryLocalServiceUtil.getEntries(repositoryId,className);
  long[] entryIds=StringUtil.split(ListUtil.toString(trashEntries,_TRASH_ENTRY_ID_ACCESSOR),0L);
  TrashEntryServiceUtil.deleteEntries(entryIds);
}

{
  long newFolderId=DLFolderConstants.DEFAULT_PARENT_FOLDER_ID;
  if (newFolder != null) {
    newFolderId=newFolder.getFolderId();
  }
  return DLAppHelperLocalServiceUtil.moveFileShortcutFromTrash(userId,fileShortcut,newFolderId,serviceContext);
}

{
  if (name.startsWith("/")) {
    name=name.substring(1);
  }
  URL url=super.getResource("META-INF/resources/" + name);
  if (url != null) {
    return url;
  }
  return super.getResource(name);
}

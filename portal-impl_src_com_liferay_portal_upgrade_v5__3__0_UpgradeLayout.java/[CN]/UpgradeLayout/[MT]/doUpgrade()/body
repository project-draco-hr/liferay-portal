{
  if (isSupportsAlterColumnType()) {
    runSQL("alter_column_type Layout friendlyURL VARCHAR(255) null");
  }
 else {
    UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(LayoutTable.TABLE_NAME,LayoutModelImpl.TABLE_COLUMNS);
    upgradeTable.setCreateSQL(LayoutModelImpl.TABLE_SQL_CREATE);
    upgradeTable.updateTable();
  }
}

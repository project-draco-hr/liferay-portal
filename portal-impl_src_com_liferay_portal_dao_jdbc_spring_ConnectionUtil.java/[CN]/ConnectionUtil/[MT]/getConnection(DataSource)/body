{
  Connection connection=CurrentConnectionUtil.getConnection(dataSource);
  if (connection != null) {
    return (Connection)ProxyUtil.newProxyInstance(ClassLoader.getSystemClassLoader(),_interfaces,new UncloseableInvocationHandler(connection));
  }
  return dataSource.getConnection();
}

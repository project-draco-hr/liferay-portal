{
  Field field=ReflectionUtil.getDeclaredField(WriterOutputStream.class,"_outputCharBuffer");
  CharBuffer outputBuffer=(CharBuffer)field.get(writerOutputStream);
  return outputBuffer.capacity();
}

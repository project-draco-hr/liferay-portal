{
  runSQL("delete from Permission_ where permissionId = " + permissionId);
  runSQL("delete from Groups_Permissions where permissionId = " + permissionId);
  runSQL("delete from Roles_Permissions where permissionId = " + permissionId);
  runSQL("delete from Users_Permissions where permissionId = " + permissionId);
}

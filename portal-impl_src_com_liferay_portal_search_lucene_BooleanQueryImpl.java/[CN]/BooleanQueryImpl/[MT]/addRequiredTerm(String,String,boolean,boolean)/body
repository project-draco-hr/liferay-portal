{
  if (Validator.isNull(value)) {
    return;
  }
  String[] values=null;
  if (parseKeywords) {
    values=parseKeywords(value);
  }
 else {
    values=new String[]{value};
  }
  LuceneHelperUtil.addRequiredTerm(_booleanQuery,field,values,like);
}

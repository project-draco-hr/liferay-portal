{
  Integer type=WorkflowInstanceLinkModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ WorkflowInstanceLinkModelImpl.TABLE_NAME);
  }
  return type;
}

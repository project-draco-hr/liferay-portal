{
  Integer type=MDRRuleGroupModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ MDRRuleGroupModelImpl.TABLE_NAME);
  }
  return type;
}

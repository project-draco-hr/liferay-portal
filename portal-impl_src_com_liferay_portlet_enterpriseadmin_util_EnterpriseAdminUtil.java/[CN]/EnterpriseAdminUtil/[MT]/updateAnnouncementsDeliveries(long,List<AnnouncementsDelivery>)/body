{
  for (  AnnouncementsDelivery delivery : deliveries) {
    AnnouncementsDeliveryServiceUtil.updateDelivery(userId,delivery.getType(),delivery.getEmail(),delivery.getSms(),delivery.getWebsite());
  }
}

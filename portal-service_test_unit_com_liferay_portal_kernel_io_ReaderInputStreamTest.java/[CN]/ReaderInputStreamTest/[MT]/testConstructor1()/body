{
  Reader reader=new StringReader(_TEST_STRING_ENGLISH);
  try {
    ReaderInputStream readerInputStream=new ReaderInputStream(reader,StringPool.UTF8,0,0);
    fail();
  }
 catch (  Exception e) {
    assertTrue(e instanceof IllegalArgumentException);
  }
}

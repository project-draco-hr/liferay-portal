{
  PortletPreferences preferences=getPreferences();
  String[] questions=StringUtil.split(LocalizationUtil.getPreferencesValue(preferences,"reminderQueries",languageId,false),StringPool.NEW_LINE);
  return SetUtil.fromArray(questions);
}

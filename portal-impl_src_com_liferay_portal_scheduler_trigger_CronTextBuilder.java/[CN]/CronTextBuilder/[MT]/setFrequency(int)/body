{
  if ((frequency != CronTextBuilder.DAILY_FREQUENCY) && (frequency != CronTextBuilder.MINUTELY_FREQUENCY) && (frequency != CronTextBuilder.MONTHLY_FREQUENCY)&& (frequency != CronTextBuilder.NO_FREQUENCY)&& (frequency != CronTextBuilder.WEEKLY_FREQUENCY)&& (frequency != CronTextBuilder.YEARLY_FREQUENCY)) {
    throw new IllegalArgumentException(String.valueOf(frequency));
  }
  _frequency=frequency;
}

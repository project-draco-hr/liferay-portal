{
  Group group=null;
  if (isStagingGroup()) {
    group=getLiveGroup();
  }
 else {
    group=this;
  }
  String treePath=group.getTreePath();
  if (treePath.contains(StringPool.SLASH + groupId + StringPool.SLASH)) {
    return true;
  }
 else {
    return false;
  }
}

{
  try {
    LayoutSet layoutSet=getPrivateLayoutSet();
    int pageCount=layoutSet.getPageCount();
    if (isUser()) {
      List<UserGroup> userGroups=UserGroupLocalServiceUtil.getUserUserGroups(getClassPK());
      for (      UserGroup userGroup : userGroups) {
        pageCount+=userGroup.getPrivateLayoutsPageCount();
      }
    }
    return pageCount;
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return 0;
}

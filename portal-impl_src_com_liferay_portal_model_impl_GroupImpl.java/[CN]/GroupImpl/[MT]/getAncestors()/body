{
  List<Group> groups=new ArrayList<Group>();
  Group group=this;
  while (true) {
    if (!group.isRoot()) {
      group=group.getParentGroup();
      groups.add(group);
    }
 else {
      break;
    }
  }
  return groups;
}

{
  if (stagingInclude) {
    if (isOrganization() || (isStagingGroup() && getLiveGroup().isOrganization())) {
      return true;
    }
 else {
      return false;
    }
  }
 else {
    return hasClassName(Organization.class);
  }
}

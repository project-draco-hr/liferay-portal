{
  if ((typeId > 0) && !DDMStructurePermission.contains(permissionChecker,typeId,ActionKeys.VIEW)) {
    return false;
  }
  return JournalPermission.contains(permissionChecker,groupId,ActionKeys.ADD_ARTICLE);
}

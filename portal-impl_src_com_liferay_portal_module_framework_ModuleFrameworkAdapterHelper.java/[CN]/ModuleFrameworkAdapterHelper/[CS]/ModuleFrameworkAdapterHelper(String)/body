{
  try {
    _adaptedInstance=InstanceFactory.newInstance(getClassLoader(),classname);
  }
 catch (  Exception e) {
    _log.error("Unexpected error has ocurred while loading module framework" + ". It will be unusable");
    throw new RuntimeException(e);
  }
}

{
  String treeId=ParamUtil.getString(request,"treeId");
  String paginationJSON=SessionClicks.get(request,treeId + "PaginationMap",StringPool.BLANK);
  try {
    JSONObject paginationJSONObject=JSONFactoryUtil.createJSONObject(paginationJSON);
    String key=String.valueOf(layoutId);
    if (paginationJSONObject.has(key)) {
      int paginationEnd=paginationJSONObject.getInt(key);
      if (paginationEnd > end) {
        end=paginationEnd;
      }
    }
  }
 catch (  JSONException e) {
  }
  return end;
}

{
  if (getType().equals("link_to_layout")) {
    return PropsValues.LAYOUT_FRIENDLY_URL_PRIVATE_GROUP_SERVLET_MAPPING + StringPool.SLASH + "@group_id@"+ StringPool.SLASH+ getData();
  }
 else {
    return StringPool.BLANK;
  }
}

{
  if (getType().equals("link_to_layout")) {
    StringBuilder sb=new StringBuilder();
    sb.append(PropsValues.LAYOUT_FRIENDLY_URL_PRIVATE_GROUP_SERVLET_MAPPING);
    sb.append(StringPool.SLASH);
    sb.append("@group_id@");
    sb.append(StringPool.SLASH);
    sb.append(getData());
    return sb.toString();
  }
 else {
    return StringPool.BLANK;
  }
}

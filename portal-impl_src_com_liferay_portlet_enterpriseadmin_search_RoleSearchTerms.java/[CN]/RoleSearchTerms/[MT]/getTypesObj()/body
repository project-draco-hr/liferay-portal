{
  if ((type == RoleConstants.TYPE_COMMUNITY) || (type == RoleConstants.TYPE_ORGANIZATION) || (type == RoleConstants.TYPE_REGULAR)) {
    return new Integer[]{type};
  }
 else {
    return new Integer[0];
  }
}

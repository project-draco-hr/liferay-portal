{
  Folder newFolder=addFolder();
  _persistence.clearCache();
  Folder existingFolder=_persistence.findByPrimaryKey(newFolder.getPrimaryKey());
  Assert.assertEquals(Long.valueOf(existingFolder.getAccountId()),ReflectionTestUtil.<Long>invoke(existingFolder,"getOriginalAccountId",new Class<?>[0]));
  Assert.assertTrue(Objects.equals(existingFolder.getFullName(),ReflectionTestUtil.invoke(existingFolder,"getOriginalFullName",new Class<?>[0])));
}

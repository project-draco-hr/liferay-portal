{
  try {
    Expression<Boolean> expression=_expressionFactory.createBooleanExpression(booleanExpression);
    return expression.evaluate();
  }
 catch (  ExpressionEvaluationException eee) {
    _log.error("Unable to evaluate expression " + booleanExpression,eee);
    return false;
  }
}

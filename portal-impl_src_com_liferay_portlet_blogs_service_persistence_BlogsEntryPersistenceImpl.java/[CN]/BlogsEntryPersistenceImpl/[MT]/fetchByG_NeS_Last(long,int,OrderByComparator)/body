{
  int count=countByG_NeS(groupId,status);
  List<BlogsEntry> list=findByG_NeS(groupId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

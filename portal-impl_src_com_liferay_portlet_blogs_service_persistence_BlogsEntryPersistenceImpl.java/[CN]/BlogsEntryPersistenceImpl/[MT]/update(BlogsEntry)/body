{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BlogsEntry blogsEntry) method. To improve performance, call update(BlogsEntry blogsEntry, boolean merge).");
  }
  return update(blogsEntry,false);
}

{
  for (  BlogsEntry blogsEntry : blogsEntries) {
    if (EntityCacheUtil.getResult(BlogsEntryModelImpl.ENTITY_CACHE_ENABLED,BlogsEntry.class,blogsEntry.getPrimaryKey(),this) == null) {
      cacheResult(blogsEntry);
    }
  }
}

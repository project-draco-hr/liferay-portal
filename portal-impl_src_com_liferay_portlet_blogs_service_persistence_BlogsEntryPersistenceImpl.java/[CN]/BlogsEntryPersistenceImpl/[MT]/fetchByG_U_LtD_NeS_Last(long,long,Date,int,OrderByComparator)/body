{
  int count=countByG_U_LtD_NeS(groupId,userId,displayDate,status);
  List<BlogsEntry> list=findByG_U_LtD_NeS(groupId,userId,displayDate,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

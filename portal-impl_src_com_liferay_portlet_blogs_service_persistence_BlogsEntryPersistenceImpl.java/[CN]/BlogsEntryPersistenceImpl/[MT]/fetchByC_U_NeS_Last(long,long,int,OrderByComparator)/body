{
  int count=countByC_U_NeS(companyId,userId,status);
  List<BlogsEntry> list=findByC_U_NeS(companyId,userId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  int count=countByG_U_NeS(groupId,userId,status);
  List<BlogsEntry> list=findByG_U_NeS(groupId,userId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  int count=countByC_LtD_NeS(companyId,displayDate,status);
  List<BlogsEntry> list=findByC_LtD_NeS(companyId,displayDate,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

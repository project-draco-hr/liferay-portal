{
  BlogsEntry result=(BlogsEntry)EntityCacheUtil.getResult(BlogsEntryModelImpl.ENTITY_CACHE_ENABLED,BlogsEntry.class,entryId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      BlogsEntry blogsEntry=(BlogsEntry)session.get(BlogsEntryImpl.class,new Long(entryId));
      cacheResult(blogsEntry);
      return blogsEntry;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (BlogsEntry)result;
  }
}

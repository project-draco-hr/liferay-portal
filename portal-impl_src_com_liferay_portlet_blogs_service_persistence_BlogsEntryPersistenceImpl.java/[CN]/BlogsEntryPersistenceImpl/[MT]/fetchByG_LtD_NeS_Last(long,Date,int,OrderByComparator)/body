{
  int count=countByG_LtD_NeS(groupId,displayDate,status);
  List<BlogsEntry> list=findByG_LtD_NeS(groupId,displayDate,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

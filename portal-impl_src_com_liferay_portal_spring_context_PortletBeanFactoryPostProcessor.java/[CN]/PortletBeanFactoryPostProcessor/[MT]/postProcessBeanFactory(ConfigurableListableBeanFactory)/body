{
  ClassLoader beanClassLoader=new AggregateClassLoader(PortletClassLoaderUtil.getClassLoader(),PortalClassLoaderUtil.getClassLoader());
  if (ServerDetector.isJBoss()) {
    beanClassLoader=new JBossClassLoader(beanClassLoader);
  }
  beanFactory.setBeanClassLoader(beanClassLoader);
}

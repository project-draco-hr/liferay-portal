{
  if (type == SocialEquitySettingConstants.TYPE_INFORMATION) {
    return equals(equityActionMapping.getInformationDailyLimit(),equityActionMapping.getInformationLifespan(),type,equityActionMapping.isUnique(),equityActionMapping.getInformationValue());
  }
 else {
    return equals(equityActionMapping.getParticipationDailyLimit(),equityActionMapping.getParticipationLifespan(),type,equityActionMapping.isUnique(),equityActionMapping.getParticipationValue());
  }
}

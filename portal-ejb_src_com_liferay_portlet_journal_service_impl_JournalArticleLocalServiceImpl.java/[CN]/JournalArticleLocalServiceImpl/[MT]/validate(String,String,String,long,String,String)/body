{
  if (Validator.isNull(title)) {
    throw new ArticleTitleException();
  }
 else   if (Validator.isNull(content)) {
    throw new ArticleContentException();
  }
  if (Validator.isNotNull(structureId)) {
    JournalStructureUtil.findByPrimaryKey(new JournalStructurePK(companyId,groupId,structureId));
    JournalTemplate template=JournalTemplateUtil.findByPrimaryKey(new JournalTemplatePK(companyId,groupId,templateId));
    if (!template.getStructureId().equals(structureId)) {
      throw new NoSuchTemplateException();
    }
  }
}

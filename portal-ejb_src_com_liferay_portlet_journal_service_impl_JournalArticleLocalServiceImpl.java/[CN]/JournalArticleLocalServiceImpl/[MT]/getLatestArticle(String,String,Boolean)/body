{
  List articles=null;
  if (approved == null) {
    articles=JournalArticleUtil.findByC_A(companyId,articleId,0,1);
  }
 else {
    articles=JournalArticleUtil.findByC_A_A(companyId,articleId,approved.booleanValue(),0,1);
  }
  if (articles.size() == 0) {
    throw new NoSuchArticleException();
  }
  return (JournalArticle)articles.get(0);
}

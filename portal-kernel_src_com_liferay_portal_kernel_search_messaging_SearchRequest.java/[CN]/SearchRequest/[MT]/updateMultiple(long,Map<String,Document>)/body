{
  SearchRequest searchRequest=new SearchRequest(SearchEngineCommand.UPDATE_MULTIPLE);
  searchRequest.setCompanyId(companyId);
  searchRequest.setDocumentsMap(documentsMap);
  return searchRequest;
}

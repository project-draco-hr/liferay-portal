{
  List<Layout> layouts=LayoutLocalServiceUtil.getLayoutsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<Layout>());
  if (ListUtil.isEmpty(layouts)) {
    return null;
  }
  return layouts.get(0);
}

{
  for (int i=0; i < _remoteAttachments.size(); i++) {
    RemoteMailAttachment rma=(RemoteMailAttachment)_remoteAttachments.get(i);
    if (rma.isDirty()) {
      _remoteAttachments.remove(i);
      i--;
    }
  }
}

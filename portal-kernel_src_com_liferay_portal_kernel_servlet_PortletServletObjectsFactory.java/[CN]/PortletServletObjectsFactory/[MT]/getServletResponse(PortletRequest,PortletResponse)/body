{
  Object response=portletRequest.getAttribute("javax.portlet.portletc.httpServletResponse");
  if (response == null) {
    response=portletRequest.getAttribute(PortletServlet.PORTLET_SERVLET_RESPONSE);
  }
  return (HttpServletResponse)response;
}

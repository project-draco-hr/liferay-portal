{
  _checkForSpecExceptions(configList);
  configList=_preSort(configList);
  WebXMLDefinition[] configs=configList.toArray(new WebXMLDefinition[configList.size()]);
  _innerSort(configs);
  _postSort(configs);
  return new ArrayList<>(Arrays.asList(configs));
}

{
  String targetVersion=syncFile.getVersion();
  if (patch && (Double.valueOf(targetVersion) > Double.valueOf(sourceVersion))) {
    FileEventUtil.downloadPatch(sourceVersion,getSyncAccountId(),syncFile,targetVersion);
  }
 else {
    FileEventUtil.downloadFile(getSyncAccountId(),syncFile);
  }
}

{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  Subscription newSubscription=addSubscription();
  _persistence.clearCache();
  SubscriptionModelImpl existingSubscriptionModelImpl=(SubscriptionModelImpl)_persistence.findByPrimaryKey(newSubscription.getPrimaryKey());
  assertEquals(existingSubscriptionModelImpl.getCompanyId(),existingSubscriptionModelImpl.getOriginalCompanyId());
  assertEquals(existingSubscriptionModelImpl.getUserId(),existingSubscriptionModelImpl.getOriginalUserId());
  assertEquals(existingSubscriptionModelImpl.getClassNameId(),existingSubscriptionModelImpl.getOriginalClassNameId());
  assertEquals(existingSubscriptionModelImpl.getClassPK(),existingSubscriptionModelImpl.getOriginalClassPK());
}

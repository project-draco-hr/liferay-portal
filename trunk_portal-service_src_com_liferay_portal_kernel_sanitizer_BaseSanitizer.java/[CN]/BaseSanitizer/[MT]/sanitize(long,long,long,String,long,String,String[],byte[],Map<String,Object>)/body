{
  ByteArrayOutputStream byteArrayOutputStream=new ByteArrayOutputStream();
  sanitize(companyId,groupId,userId,className,classPK,contentType,modes,new ByteArrayInputStream(bytes),byteArrayOutputStream,options);
  return byteArrayOutputStream.toByteArray();
}

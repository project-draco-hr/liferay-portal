{
  for (  EntityColumn column : columns) {
    if (Validator.equals(StringUtil.lowerCase(column.getName()),StringUtil.lowerCase(scopeComparator))) {
      return column;
    }
  }
  throw new RuntimeException("Cannot find scope-comparator " + scopeComparator + " in "+ ListUtil.toString(columns,"name"));
}

{
  Type[] typeArguments=javaParameter.getType().getActualTypeArguments();
  if ((typeArguments.length == 2) && typeArguments[1].getValue().equals(String.class.getName()) && typeArguments[0].getValue().equals(Locale.class.getName())) {
    return true;
  }
  return false;
}

{
  List<Entity> referenceList=entity.getReferenceList();
  List<Entity> list=new UniqueList<Entity>(_ejbList.size() + referenceList.size());
  if (_autoImportDefaultReferences) {
    list.addAll(_ejbList);
  }
 else {
    list.add(entity);
  }
  list.addAll(referenceList);
  return list;
}

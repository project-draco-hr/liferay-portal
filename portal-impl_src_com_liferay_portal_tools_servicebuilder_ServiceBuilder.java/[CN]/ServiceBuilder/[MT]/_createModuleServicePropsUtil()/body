{
  if (Validator.isNull(_osgiModule)) {
    return;
  }
  File file=new File(_implDir + "/" + StringUtil.replace(_propsUtil,".","/")+ ".java");
  if (file.exists()) {
    return;
  }
  Map<String,Object> context=_getContext();
  int indexOf=_propsUtil.lastIndexOf(".");
  context.put("moduleServicePropsUtilClass",_propsUtil.substring(indexOf + 1));
  context.put("moduleServicePropsUtilPackage",_propsUtil.substring(0,indexOf));
  String content=_processTemplate(_tplModuleServiceProps,context);
  writeFile(file,content);
}

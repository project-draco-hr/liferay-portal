{
  for (  EntityColumn column : columns) {
    if (column.isScopePrimary()) {
      return column;
    }
  }
  throw new RuntimeException("Cannot find scope-primary column in " + ListUtil.toString(columns,"name"));
}

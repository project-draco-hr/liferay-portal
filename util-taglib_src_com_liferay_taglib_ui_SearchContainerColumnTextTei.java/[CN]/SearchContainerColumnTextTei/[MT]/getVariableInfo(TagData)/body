{
  String buffer=data.getAttributeString("buffer");
  if (Validator.isNotNull(buffer)) {
    return new VariableInfo[]{new VariableInfo(buffer,StringBuilder.class.getName(),true,VariableInfo.NESTED)};
  }
 else {
    return null;
  }
}

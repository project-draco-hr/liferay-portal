{
  Object buffer=data.getAttribute("buffer");
  if (Validator.isNotNull(buffer)) {
    return new VariableInfo[]{new VariableInfo(String.valueOf(buffer),StringBuilder.class.getName(),true,VariableInfo.NESTED)};
  }
 else {
    return null;
  }
}

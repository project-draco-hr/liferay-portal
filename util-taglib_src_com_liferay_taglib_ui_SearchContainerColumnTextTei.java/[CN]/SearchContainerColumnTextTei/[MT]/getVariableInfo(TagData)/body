{
  String buffer=tagData.getAttributeString("buffer");
  if (Validator.isNotNull(buffer)) {
    return new VariableInfo[]{new VariableInfo(buffer,StringBundler.class.getName(),true,VariableInfo.NESTED)};
  }
 else {
    return null;
  }
}

{
  String auth=MapUtil.getString(properties,"type");
  if (Validator.isNull(auth)) {
    throw new IllegalArgumentException("Invalid authentication type " + auth);
  }
  _httpClientFactories.put(auth,httpClientFactory);
}

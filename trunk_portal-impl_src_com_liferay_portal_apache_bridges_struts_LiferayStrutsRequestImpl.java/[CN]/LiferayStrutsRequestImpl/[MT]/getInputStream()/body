{
  if (_bytes == null) {
    InputStream is=super.getInputStream();
    _bytes=FileUtil.getBytes(is);
    is.close();
  }
  return new UnsyncByteArrayInputStreamWrapper(new UnsyncByteArrayInputStream(_bytes));
}

{
  if (groups == null) {
    return false;
  }
 else {
    Iterator itr=groups.iterator();
    while (itr.hasNext()) {
      Group group=(Group)itr.next();
      if (group.getGroupId().equals(getGroupId())) {
        return true;
      }
    }
  }
  return false;
}

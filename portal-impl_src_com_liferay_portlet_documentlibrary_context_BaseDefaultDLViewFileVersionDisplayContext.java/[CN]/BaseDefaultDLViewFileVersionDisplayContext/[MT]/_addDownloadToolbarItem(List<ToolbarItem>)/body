{
  if (!_fileEntryDisplayContextHelper.isDownloadActionAvailable()) {
    return;
  }
  _addURLUIItem(new URLToolbarItem(),toolbarItems,"icon-download",DLUIItemKeys.DOWNLOAD,UnicodeLanguageUtil.get(request,"download"),DLUtil.getDownloadURL(fileEntry,fileVersion,_themeDisplay,StringPool.BLANK));
}

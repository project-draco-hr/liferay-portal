{
  DLFileEntry dlFileEntry=DLFileEntryLocalServiceUtil.fetchDLFileEntryByUuidAndGroupId(uuid,groupId);
  if (dlFileEntry != null) {
    DLFileEntryLocalServiceUtil.deleteFileEntry(dlFileEntry);
  }
}

{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update DLFileEntry set fileName = ? where fileEntryId = ?");
    ps.setString(1,fileName);
    ps.setLong(2,fileEntryId);
    ps.executeUpdate();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

{
  try (PreparedStatement ps=connection.prepareStatement("update DLFileVersion set fileName = ? where " + "fileVersionId = ?")){
    ps.setString(1,fileName);
    ps.setLong(2,fileVersionId);
    ps.executeUpdate();
  }
 }

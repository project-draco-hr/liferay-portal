{
  if (isEscapedModel()) {
    return (ShoppingCart)this;
  }
 else {
    return (ShoppingCart)Proxy.newProxyInstance(ShoppingCart.class.getClassLoader(),new Class[]{ShoppingCart.class},new AutoEscapeBeanHandler(this));
  }
}

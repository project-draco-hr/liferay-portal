{
  if (isEscapedModel()) {
    return (ShoppingCart)this;
  }
 else {
    return (ShoppingCart)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

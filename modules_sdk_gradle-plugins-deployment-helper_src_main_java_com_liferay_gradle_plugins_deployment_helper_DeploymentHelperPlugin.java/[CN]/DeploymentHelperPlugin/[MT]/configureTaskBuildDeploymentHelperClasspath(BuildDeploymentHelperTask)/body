{
  FileCollection fileCollection=buildDeploymentHelperTask.getClasspath();
  if ((fileCollection != null) && !fileCollection.isEmpty()) {
    return;
  }
  Configuration configuration=GradleUtil.getConfiguration(buildDeploymentHelperTask.getProject(),CONFIGURATION_NAME);
  buildDeploymentHelperTask.setClasspath(configuration);
}

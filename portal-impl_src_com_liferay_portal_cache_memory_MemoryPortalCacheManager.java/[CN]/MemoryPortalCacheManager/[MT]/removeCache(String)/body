{
  MemoryPortalCache<K,V> memoryPortalCache=_memoryPortalCaches.remove(name);
  memoryPortalCache.destroy();
  for (  CacheManagerListener cacheManagerListener : _cacheManagerListeners) {
    cacheManagerListener.notifyCacheRemoved(name);
  }
}

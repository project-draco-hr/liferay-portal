{
  getService().addConfiguredProducer(name,portalId,namespace,producerURL,producerVersion,producerMarkupURL,status,registrationData,registrationContext,serviceDescription,userCategoryMapping,customUserProfile,identityPropagationType,lifetimeTerminationTime,sdLastModified,entityVersion);
}

{
  getService().addWSRPConfiguredProducer(name,portalId,namespace,producerURL,producerVersion,markupURL,status,registrationData,registrationContext,serviceDescription,userCategoryMapping,customUserProfile,identityPropagationType,sdLastModified,entityVersion);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WebDAVProps webDAVProps) method. To improve performance, call update(WebDAVProps webDAVProps, boolean merge).");
  }
  return update(webDAVProps,false);
}

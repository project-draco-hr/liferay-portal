{
  WebDAVProps result=(WebDAVProps)EntityCacheUtil.getResult(WebDAVPropsModelImpl.ENTITY_CACHE_ENABLED,WebDAVProps.class,webDavPropsId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      WebDAVProps webDAVProps=(WebDAVProps)session.get(WebDAVPropsImpl.class,new Long(webDavPropsId));
      if (webDAVProps != null) {
        cacheResult(webDAVProps);
      }
      return webDAVProps;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (WebDAVProps)result;
  }
}

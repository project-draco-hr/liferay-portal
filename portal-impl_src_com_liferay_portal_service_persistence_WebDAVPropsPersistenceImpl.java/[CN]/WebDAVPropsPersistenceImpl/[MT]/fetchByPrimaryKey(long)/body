{
  Session session=null;
  try {
    session=openSession();
    return (WebDAVProps)session.get(WebDAVPropsImpl.class,new Long(webDavPropsId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (Validator.isNotNull(_name)) {
    return _name;
  }
  String layoutTemplateId=StringUtil.replace(_layoutTemplateId,CharPool.UNDERLINE,CharPool.DASH);
  return LanguageUtil.get(locale,"layout-template-" + layoutTemplateId);
}

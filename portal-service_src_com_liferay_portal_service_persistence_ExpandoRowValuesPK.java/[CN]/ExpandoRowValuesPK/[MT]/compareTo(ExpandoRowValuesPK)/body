{
  if (pk == null) {
    return -1;
  }
  int value=0;
  if (rowId < pk.rowId) {
    value=-1;
  }
 else   if (rowId > pk.rowId) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  if (valueId < pk.valueId) {
    value=-1;
  }
 else   if (valueId > pk.valueId) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  return 0;
}

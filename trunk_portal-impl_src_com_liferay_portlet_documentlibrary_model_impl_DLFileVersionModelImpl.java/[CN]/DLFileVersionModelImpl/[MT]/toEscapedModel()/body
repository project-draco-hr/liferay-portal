{
  if (isEscapedModel()) {
    return (DLFileVersion)this;
  }
 else {
    return (DLFileVersion)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

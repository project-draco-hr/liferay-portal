{
  for (  SAPEntry sapEntry : sapEntries) {
    if (EntityCacheUtil.getResult(SAPEntryModelImpl.ENTITY_CACHE_ENABLED,SAPEntryImpl.class,sapEntry.getPrimaryKey()) == null) {
      cacheResult(sapEntry);
    }
 else {
      sapEntry.resetOriginalValues();
    }
  }
}

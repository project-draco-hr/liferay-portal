{
  SAPEntry sapEntry=(SAPEntry)entityCache.getResult(SAPEntryModelImpl.ENTITY_CACHE_ENABLED,SAPEntryImpl.class,primaryKey);
  if (sapEntry == _nullSAPEntry) {
    return null;
  }
  if (sapEntry == null) {
    Session session=null;
    try {
      session=openSession();
      sapEntry=(SAPEntry)session.get(SAPEntryImpl.class,primaryKey);
      if (sapEntry != null) {
        cacheResult(sapEntry);
      }
 else {
        entityCache.putResult(SAPEntryModelImpl.ENTITY_CACHE_ENABLED,SAPEntryImpl.class,primaryKey,_nullSAPEntry);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(SAPEntryModelImpl.ENTITY_CACHE_ENABLED,SAPEntryImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return sapEntry;
}

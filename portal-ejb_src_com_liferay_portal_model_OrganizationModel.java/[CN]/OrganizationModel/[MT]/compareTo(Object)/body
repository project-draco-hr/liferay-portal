{
  if (obj == null) {
    return -1;
  }
  Organization organization=(Organization)obj;
  int value=0;
  value=getName().compareTo(organization.getName());
  if (value != 0) {
    return value;
  }
  return 0;
}

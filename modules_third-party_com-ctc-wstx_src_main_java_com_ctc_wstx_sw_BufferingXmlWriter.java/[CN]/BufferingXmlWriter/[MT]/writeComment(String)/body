{
  if (mCheckContent) {
    int ix=verifyCommentContent(data);
    if (ix >= 0) {
      if (!mFixContent) {
        return ix;
      }
      writeSegmentedComment(data,ix);
      return -1;
    }
  }
  fastWriteRaw("<!--");
  writeRaw(data);
  fastWriteRaw("-->");
  return -1;
}

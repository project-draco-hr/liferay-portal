{
  if (c != null) {
    start+=2;
    while (start < end) {
      char ch=c[start];
      if (ch == ']') {
        ++start;
        continue;
      }
      if (ch == '>') {
        if (c[start - 1] == ']' && c[start - 2] == ']') {
          return start - 2;
        }
      }
      start+=2;
    }
  }
  return -1;
}

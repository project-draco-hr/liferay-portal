{
  Object object=message.getObject();
  if (object == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("Message content is null");
    }
    return;
  }
  _clusterReceiver.receive(object,_wrapJGroupsAddress(message.getSrc()),_wrapJGroupsAddress(message.getDest()));
}

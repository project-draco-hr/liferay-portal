{
  Object object=message.getObject();
  if (object == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("Message content is null");
    }
    return;
  }
  _clusterReceiver.receive(object,new AddressImpl(message.getSrc()),new AddressImpl(message.getDest()));
}

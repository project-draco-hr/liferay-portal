{
  String[] upgradeProcessClassNames=getUpgradeProcessClassNames(PropsKeys.UPGRADE_PROCESSES + StringPool.PERIOD + buildNumber);
  if (upgradeProcessClassNames.length == 0) {
    System.out.println("Upgrading from " + buildNumber + " to "+ ReleaseInfo.getBuildNumber()+ " is not supported");
    System.exit(0);
  }
  _upgraded=UpgradeProcessUtil.upgradeProcess(buildNumber,upgradeProcessClassNames,PortalClassLoaderUtil.getClassLoader());
}

{
  if (isEscapedModel()) {
    return (OrgGroupPermission)this;
  }
 else {
    return (OrgGroupPermission)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

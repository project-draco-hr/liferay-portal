{
  Project project=buildUpgradeTableTask.getProject();
  File file=GradleUtil.getProperty(project,"upgrade.table.dir",(File)null);
  if (file != null) {
    buildUpgradeTableTask.setUpgradeTableDirName(FileUtil.getAbsolutePath(file));
  }
}

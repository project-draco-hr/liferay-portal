{
  File classesDir=project.file("test-classes/unit");
  File srcDir=null;
  if (legacyDirLayout) {
    srcDir=project.file("test/unit");
  }
  configureSourceSet(project,SourceSet.TEST_SOURCE_SET_NAME,classesDir,srcDir);
}

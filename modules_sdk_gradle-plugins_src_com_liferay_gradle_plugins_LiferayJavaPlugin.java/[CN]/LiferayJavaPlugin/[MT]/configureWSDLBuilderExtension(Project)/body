{
  WSDLBuilderExtension wsdlBuilderExtension=GradleUtil.getExtension(project,WSDLBuilderExtension.class);
  configureWSDLBuilderExtensionDestinationDir(project,wsdlBuilderExtension);
  configureWSDLBuilderExtensionWSDLDir(project,wsdlBuilderExtension);
}

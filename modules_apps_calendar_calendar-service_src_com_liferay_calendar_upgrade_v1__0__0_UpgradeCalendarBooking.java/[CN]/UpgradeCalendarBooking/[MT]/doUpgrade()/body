{
  if (!tableHasColumn("CalendarBooking","vEventUid")) {
    runSQL("alter table CalendarBooking add vEventUid VARCHAR(255) null");
  }
  runSQL("update CalendarBooking set vEventUid = uuid_ where vEventUid is " + "NULL or vEventUid = ''");
}

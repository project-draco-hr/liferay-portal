{
  Document doc=getFileDocument(companyId,portletId,groupId,repositoryId,fileName,fileEntryId,properties,modifiedDate,assetCategoryIds,assetTagNames);
  if (doc != null) {
    SearchEngineUtil.addDocument(companyId,doc);
  }
}

{
  if (SearchEngineUtil.isIndexReadOnly()) {
    return;
  }
  JournalArticle article=null;
  try {
    article=getLatestArticle(resourcePrimKey,StatusConstants.APPROVED);
  }
 catch (  Exception e) {
    if (e instanceof NoSuchArticleException) {
      return;
    }
  }
  reindex(article);
}

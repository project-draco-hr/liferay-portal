{
  if (status == StatusConstants.ANY) {
    return journalArticlePersistence.findByCompanyId(companyId,start,end);
  }
 else {
    return journalArticlePersistence.findByC_S(companyId,status,start,end);
  }
}

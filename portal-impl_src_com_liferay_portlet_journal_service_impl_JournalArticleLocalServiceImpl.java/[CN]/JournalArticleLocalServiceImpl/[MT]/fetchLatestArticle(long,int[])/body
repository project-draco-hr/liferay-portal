{
  OrderByComparator orderByComparator=new ArticleVersionComparator();
  List<JournalArticle> list=journalArticlePersistence.findByR_ST(resourcePrimKey,statuses,0,1,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  JournalArticleDisplay articleDisplay=getArticleDisplay(article,templateId,articleMode,languageId,1,null,themeDisplay);
  if (articleDisplay == null) {
    return StringPool.BLANK;
  }
 else {
    return articleDisplay.getContent();
  }
}

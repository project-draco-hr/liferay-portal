{
  if (!autoArticleId) {
    validate(groupId,articleId);
  }
  validate(companyId,groupId,classNameId,expirationDate,titleMap,content,type,structureId,templateId,smallImage,smallImageURL,smallImageFile,smallImageBytes);
}

{
  if (!autoArticleId) {
    validate(articleId);
    try {
      journalArticlePersistence.findByG_A_V(groupId,articleId,version);
      throw new DuplicateArticleIdException();
    }
 catch (    NoSuchArticleException nste) {
    }
  }
  validate(groupId,title,content,type,structureId,templateId,smallImage,smallImageURL,smallFile,smallBytes);
}

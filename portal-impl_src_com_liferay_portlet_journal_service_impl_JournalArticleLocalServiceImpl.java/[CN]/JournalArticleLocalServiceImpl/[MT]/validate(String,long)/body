{
  if ((Validator.isNull(articleId)) || (articleId.indexOf(CharPool.SPACE) != -1)) {
    throw new ArticleIdException();
  }
  if (journalArticlePersistence.countByG_A(groupId,articleId) > 0) {
    throw new DuplicateArticleIdException();
  }
}

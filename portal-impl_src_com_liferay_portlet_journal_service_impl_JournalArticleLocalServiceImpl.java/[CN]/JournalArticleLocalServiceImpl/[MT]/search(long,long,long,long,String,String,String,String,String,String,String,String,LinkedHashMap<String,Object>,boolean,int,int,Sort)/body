{
  List<Long> folderIds=new ArrayList<Long>();
  folderIds.add(folderId);
  return search(companyId,groupId,folderIds,classNameId,articleId,title,description,content,type,status,structureId,templateId,params,andSearch,start,end,sort);
}

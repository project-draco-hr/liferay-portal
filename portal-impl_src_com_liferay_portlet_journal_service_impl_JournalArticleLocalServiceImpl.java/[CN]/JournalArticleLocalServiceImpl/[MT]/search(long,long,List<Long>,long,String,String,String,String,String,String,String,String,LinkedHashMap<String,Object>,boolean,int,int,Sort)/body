{
  int statusCode=GetterUtil.getInteger(status);
  return search(companyId,groupId,folderIds,classNameId,articleId,title,description,content,type,statusCode,ddmStructureKey,ddmTemplateKey,params,andSearch,start,end,sort);
}

{
  StringBundler className=new StringBundler(128);
  long classPK=JournalUtil.getSubscriptionToStructureClass(groupId,ddmStructureId,className);
  subscriptionLocalService.deleteSubscription(userId,className.toString(),classPK);
}

{
  if (!autoArticleId) {
    validate(groupId,articleId);
  }
  validate(companyId,groupId,classNameId,titleMap,content,expirationDate,type,structureId,templateId,smallImage,smallImageURL,smallImageFile,smallImageBytes);
}

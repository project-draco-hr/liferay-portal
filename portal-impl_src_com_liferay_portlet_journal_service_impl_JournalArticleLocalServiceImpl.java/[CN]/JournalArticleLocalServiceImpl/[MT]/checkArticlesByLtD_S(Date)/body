{
  List<JournalArticle> articles=journalArticlePersistence.findByLtD_S(sinceDate,WorkflowConstants.STATUS_SCHEDULED);
  for (  JournalArticle article : articles) {
    ServiceContext serviceContext=new ServiceContext();
    serviceContext.setCommand(Constants.UPDATE);
    String layoutFullURL=PortalUtil.getLayoutFullURL(article.getGroupId(),PortletKeys.JOURNAL);
    serviceContext.setLayoutFullURL(layoutFullURL);
    serviceContext.setScopeGroupId(article.getGroupId());
    updateStatus(article.getUserId(),article,WorkflowConstants.STATUS_APPROVED,null,new HashMap<String,Serializable>(),serviceContext);
  }
}

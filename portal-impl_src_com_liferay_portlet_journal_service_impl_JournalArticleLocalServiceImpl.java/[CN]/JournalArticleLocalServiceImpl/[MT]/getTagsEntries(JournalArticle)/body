{
  List<TagsEntry> entries=tagsEntryLocalService.getEntries(JournalArticle.class.getName(),article.getPrimaryKey(),true);
  String[] tagsEntries=new String[entries.size()];
  for (int i=0; i < entries.size(); i++) {
    tagsEntries[i]=entries.get(i).getName();
  }
  return tagsEntries;
}

{
  JournalArticleDisplay articleDisplay=getArticleDisplay(article,templateId,languageId,1,"<request/>",themeDisplay);
  if (articleDisplay == null) {
    return StringPool.BLANK;
  }
 else {
    return articleDisplay.getContent();
  }
}

{
  JournalArticleDisplay articleDisplay=getArticleDisplay(article,templateId,languageId,1,null,themeDisplay);
  if (articleDisplay == null) {
    return StringPool.BLANK;
  }
 else {
    return articleDisplay.getContent();
  }
}

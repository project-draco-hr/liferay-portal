{
  CacheEventListener cacheEventListener=_cacheListenersMap.get(cacheListener);
  if (cacheEventListener != null) {
    RegisteredEventListeners registeredEventListeners=_cache.getCacheEventNotificationService();
    registeredEventListeners.unregisterListener(cacheEventListener);
  }
  _cacheListenersMap.remove(cacheListener);
}

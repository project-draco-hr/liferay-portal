{
  if (!contains(permissionChecker,question,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),PollsQuestion.class.getName(),question.getQuestionId(),actionId);
  }
}

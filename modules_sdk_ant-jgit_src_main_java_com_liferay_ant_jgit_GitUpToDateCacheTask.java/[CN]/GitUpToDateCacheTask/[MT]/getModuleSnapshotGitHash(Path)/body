{
  try (InputStream inputStream=Files.newInputStream(path.resolve(_snapshotFileName))){
    Properties properties=new Properties();
    properties.load(inputStream);
    return properties.getProperty("module.snapshot.git.hash");
  }
 }

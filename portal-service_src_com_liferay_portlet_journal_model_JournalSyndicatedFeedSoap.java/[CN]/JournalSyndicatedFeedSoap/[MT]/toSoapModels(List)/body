{
  List soapModels=new ArrayList(models.size());
  for (int i=0; i < models.size(); i++) {
    JournalSyndicatedFeed model=(JournalSyndicatedFeed)models.get(i);
    soapModels.add(toSoapModel(model));
  }
  return (JournalSyndicatedFeedSoap[])soapModels.toArray(new JournalSyndicatedFeedSoap[0]);
}

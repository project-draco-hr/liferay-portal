{
  String cmd=ParamUtil.getString(req,Constants.CMD);
  try {
    if (cmd.equals("addProperty")) {
      addProperty(req);
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return null;
}

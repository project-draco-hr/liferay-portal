{
  List<String> tableNamesWithoutCompanyId=_dbProvider.getTableNamesWithoutCompanyId(exportContext.getSchema());
  List<Long> companyIds=exportContext.getCompanyIds();
  for (  Long companyId : companyIds) {
    _exportCompany(companyId,tableNamesWithoutCompanyId,exportContext);
  }
}

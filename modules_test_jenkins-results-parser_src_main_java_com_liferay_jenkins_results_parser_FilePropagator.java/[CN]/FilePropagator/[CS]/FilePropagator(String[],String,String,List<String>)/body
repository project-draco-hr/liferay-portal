{
  for (  String fileName : fileNames) {
    _filePropagatorTasks.add(new FilePropagatorTask(dirPath + "/" + fileName,originDirPath + "/" + fileName));
  }
  _targetSlaves.addAll(targetSlaves);
  _copyFromOrigin();
}

{
  for (  String fileName : fileNames) {
    _filePropagatorTasks.add(new FilePropagatorTask(filePath + "/" + fileName,originFilePath + "/" + fileName));
  }
  _targetSlaves.addAll(targetSlaves);
  _copyFromOrigin();
}

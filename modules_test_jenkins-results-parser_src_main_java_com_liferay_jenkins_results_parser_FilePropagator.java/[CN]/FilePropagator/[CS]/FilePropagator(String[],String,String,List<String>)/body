{
  for (  String fileName : fileNames) {
    _filePropagatorTasks.add(new FilePropagatorTask(filePath + "/" + fileName,originPath + "/" + fileName));
  }
  _targetSlaves.addAll(targetSlaves);
  _copyFromOrigin();
}

{
  _filePropagatorTasks=new ArrayList<>(fileNames.length);
  for (  String fileName : fileNames) {
    _filePropagatorTasks.add(new FilePropagatorTask(slavePath + "/" + fileName,originPath + "/" + fileName));
  }
  _targetSlaves=targetSlaves;
  _copyFromOrigin();
}

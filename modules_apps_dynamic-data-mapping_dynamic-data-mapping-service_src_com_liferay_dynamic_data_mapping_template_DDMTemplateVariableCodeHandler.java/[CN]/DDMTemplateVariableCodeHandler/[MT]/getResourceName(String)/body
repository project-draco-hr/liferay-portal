{
  if (isCommonResource(dataType)) {
    dataType="common";
  }
  StringBundler sb=new StringBundler(3);
  sb.append(getTemplatePath());
  sb.append(dataType);
  sb.append(".ftl");
  return sb.toString();
}

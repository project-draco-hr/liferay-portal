{
  FinderCacheUtil.clearCache();
  group=ServiceTestUtil.addGroup();
  User user=ServiceTestUtil.addUser(ServiceTestUtil.randomString(),group.getGroupId());
  entry=BlogsTestUtil.addEntry(user.getUserId(),group,false);
}

{
  FinderCacheUtil.clearCache();
  group=GroupTestUtil.addGroup();
  user=UserTestUtil.addUser(ServiceTestUtil.randomString(),group.getGroupId());
  entry=BlogsTestUtil.addEntry(user.getUserId(),group,false);
}

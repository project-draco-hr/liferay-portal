{
  Set<String> queryTerms=new HashSet<>();
  try {
    queryTerms=_luceneHelper.getQueryTerms((org.apache.lucene.search.Query)_queryTranslator.translate(query));
  }
 catch (  ParseException pe) {
    _log.error("Query " + query,pe);
  }
  return queryTerms;
}

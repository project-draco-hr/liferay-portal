{
  if (isEmpty(array)) {
    return false;
  }
  for (int i=0; i < array.length; i++) {
    if (value == null) {
      if (array[i] == null) {
        return true;
      }
    }
 else     if (value.equals(array[i])) {
      return true;
    }
  }
  return false;
}

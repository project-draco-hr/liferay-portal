{
  if (isEscapedModel()) {
    return (Revision)this;
  }
 else {
    return (Revision)Proxy.newProxyInstance(Revision.class.getClassLoader(),new Class[]{Revision.class},new AutoEscapeBeanHandler(this));
  }
}

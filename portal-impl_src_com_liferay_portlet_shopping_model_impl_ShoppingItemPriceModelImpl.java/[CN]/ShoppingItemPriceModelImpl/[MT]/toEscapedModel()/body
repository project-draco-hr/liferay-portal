{
  if (isEscapedModel()) {
    return (ShoppingItemPrice)this;
  }
 else {
    return (ShoppingItemPrice)Proxy.newProxyInstance(ShoppingItemPrice.class.getClassLoader(),new Class[]{ShoppingItemPrice.class},new AutoEscapeBeanHandler(this));
  }
}

{
  Registry registry=RegistryUtil.getRegistry();
  ScriptingExecutor scriptingExecutor=registry.getService(serviceReference);
  _scriptingExecutors.put(scriptingExecutor.getLanguage(),scriptingExecutor);
  return scriptingExecutor;
}

{
  StringBundler sb=new StringBundler(args.length * 2 + 1);
  sb.append(methodName);
  for (  Object arg : args) {
    sb.append(StringPool.PERIOD);
    sb.append(StringUtil.toHexString(arg));
  }
  CacheKeyGenerator cacheKeyGenerator=CacheKeyGeneratorUtil.getCacheKeyGenerator(SocialActivityCounterFinder.class.getName());
  return cacheKeyGenerator.getCacheKey(sb);
}

{
  DateFormatFactory dateFormatFactory=Mockito.mock(DateFormatFactory.class);
  setUpPattern(dateFormatFactory,pattern);
  setUpPattern(dateFormatFactory,"yyyy-MM-dd'T'HH:mm:ss.000'Z'");
  return dateFormatFactory;
}

{
  String[] scriptingLanguages=getScriptingLanguages(taskAssignmentSelector,properties);
  for (  String scriptingLanguage : scriptingLanguages) {
    String taskAssignmentSelectorKey=getTaskAssignmentSelectKey(scriptingLanguage,ClassUtil.getClassName(taskAssignmentSelector));
    _taskAssignmentSelectors.remove(taskAssignmentSelectorKey);
  }
}

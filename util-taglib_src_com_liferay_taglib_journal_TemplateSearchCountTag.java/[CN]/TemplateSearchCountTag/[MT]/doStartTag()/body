{
  int count;
  try {
    count=JournalTemplateLocalServiceUtil.searchCount(_companyId,_groupId,_templateId,_structureId,_structureIdComparator,_name,_description,_andOperator);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,count);
  return SKIP_BODY;
}

{
  BufferedImage image=new BufferedImage(6,1,BufferedImage.TYPE_INT_ARGB);
  Graphics g=image.getGraphics();
  int[] rgb=ColorUtil.getRGB(hex);
  g.setColor(new Color(rgb[0],rgb[1],rgb[2]));
  g.fillRect(0,0,3,1);
  ImageUtil.encodeGIF(image,out);
}

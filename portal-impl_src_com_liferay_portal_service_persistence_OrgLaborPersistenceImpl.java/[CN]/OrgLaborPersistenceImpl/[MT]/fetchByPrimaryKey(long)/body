{
  OrgLabor orgLabor=(OrgLabor)EntityCacheUtil.getResult(OrgLaborModelImpl.ENTITY_CACHE_ENABLED,OrgLaborImpl.class,orgLaborId,this);
  if (orgLabor == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (orgLabor == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      orgLabor=(OrgLabor)session.get(OrgLaborImpl.class,Long.valueOf(orgLaborId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (orgLabor == null)) {
        EntityCacheUtil.putResult(OrgLaborModelImpl.ENTITY_CACHE_ENABLED,OrgLaborImpl.class,orgLaborId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(orgLabor);
      }
      closeSession(session);
    }
  }
  return orgLabor;
}

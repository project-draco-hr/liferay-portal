{
  OrgLabor orgLabor=(OrgLabor)EntityCacheUtil.getResult(OrgLaborModelImpl.ENTITY_CACHE_ENABLED,OrgLaborImpl.class,orgLaborId);
  if (orgLabor == _nullOrgLabor) {
    return null;
  }
  if (orgLabor == null) {
    Session session=null;
    try {
      session=openSession();
      orgLabor=(OrgLabor)session.get(OrgLaborImpl.class,Long.valueOf(orgLaborId));
      if (orgLabor != null) {
        cacheResult(orgLabor);
      }
 else {
        EntityCacheUtil.putResult(OrgLaborModelImpl.ENTITY_CACHE_ENABLED,OrgLaborImpl.class,orgLaborId,_nullOrgLabor);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(OrgLaborModelImpl.ENTITY_CACHE_ENABLED,OrgLaborImpl.class,orgLaborId);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return orgLabor;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(OrgLabor orgLabor) method. To improve performance, call update(OrgLabor orgLabor, boolean merge).");
  }
  return update(orgLabor,false);
}

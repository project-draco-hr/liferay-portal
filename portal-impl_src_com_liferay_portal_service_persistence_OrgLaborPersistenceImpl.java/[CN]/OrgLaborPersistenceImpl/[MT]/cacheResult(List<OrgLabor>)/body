{
  for (  OrgLabor orgLabor : orgLabors) {
    if (EntityCacheUtil.getResult(OrgLaborModelImpl.ENTITY_CACHE_ENABLED,OrgLabor.class,orgLabor.getPrimaryKey(),this) == null) {
      cacheResult(orgLabor);
    }
  }
}

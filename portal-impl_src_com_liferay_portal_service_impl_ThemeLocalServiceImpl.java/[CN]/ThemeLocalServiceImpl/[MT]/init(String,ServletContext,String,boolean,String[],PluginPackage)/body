{
  List<String> themeIdsList=new ArrayList<String>();
  try {
    for (    String xml : xmls) {
      Set<String> themeIds=_readThemes(servletContextName,servletContext,themesPath,loadFromServletContext,xml,pluginPackage);
      for (      String themeId : themeIds) {
        if (!themeIdsList.contains(themeId)) {
          themeIdsList.add(themeId);
        }
      }
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  _themesPool.clear();
  return themeIdsList;
}

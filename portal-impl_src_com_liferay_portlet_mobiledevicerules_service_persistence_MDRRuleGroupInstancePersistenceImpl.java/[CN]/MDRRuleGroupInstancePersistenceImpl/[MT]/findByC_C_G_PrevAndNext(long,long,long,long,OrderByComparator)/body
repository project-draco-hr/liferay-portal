{
  MDRRuleGroupInstance mdrRuleGroupInstance=findByPrimaryKey(ruleGroupInstanceId);
  Session session=null;
  try {
    session=openSession();
    MDRRuleGroupInstance[] array=new MDRRuleGroupInstanceImpl[3];
    array[0]=getByC_C_G_PrevAndNext(session,mdrRuleGroupInstance,classNameId,classPK,groupId,orderByComparator,true);
    array[1]=mdrRuleGroupInstance;
    array[2]=getByC_C_G_PrevAndNext(session,mdrRuleGroupInstance,classNameId,classPK,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

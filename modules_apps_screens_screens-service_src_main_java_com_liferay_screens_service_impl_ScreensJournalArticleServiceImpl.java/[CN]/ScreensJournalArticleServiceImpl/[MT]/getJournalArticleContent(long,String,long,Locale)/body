{
  JournalArticlePermission.check(getPermissionChecker(),groupId,ActionKeys.VIEW);
  return journalArticleLocalService.getArticleContent(groupId,articleId,null,getDDMTemplateKey(ddmTemplateId),getLanguageId(locale),null,null);
}

{
  String outputFormat=outputIContainer.getContainerFormat().getOutputFormatShortName();
  if (outputFormat.equals("ogg")) {
    return 2;
  }
  return super.getAudioEncodingChannels(outputIContainer,channels);
}

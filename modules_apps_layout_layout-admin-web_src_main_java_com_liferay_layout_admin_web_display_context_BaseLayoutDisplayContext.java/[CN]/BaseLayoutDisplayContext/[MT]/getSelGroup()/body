{
  if (_selGroup != null) {
    return _selGroup;
  }
  _selGroup=themeDisplay.getScopeGroup();
  if (_selGroup.isControlPanel()) {
    _selGroup=groupProvider.getGroup(PortalUtil.getHttpServletRequest(liferayPortletRequest));
  }
  return _selGroup;
}

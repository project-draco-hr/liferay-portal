{
  Annotation[] annotations=entity.getAnnotations();
  if (annotations != null) {
    for (int i=0; i < annotations.length; i++) {
      JavaClass annotation=annotations[i].getType().getJavaClass();
      if (annotation.getName().equals(annotationName)) {
        return true;
      }
    }
  }
  return false;
}

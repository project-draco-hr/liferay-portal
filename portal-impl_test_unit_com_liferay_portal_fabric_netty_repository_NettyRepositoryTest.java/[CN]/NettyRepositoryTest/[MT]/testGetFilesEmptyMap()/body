{
  NoticeableFuture<Map<Path,Path>> noticeableFuture=_nettyRepository.getFiles(Collections.<Path,Path>emptyMap(),true);
  Assert.assertSame(Collections.<Path,Path>emptyMap(),noticeableFuture.get());
}

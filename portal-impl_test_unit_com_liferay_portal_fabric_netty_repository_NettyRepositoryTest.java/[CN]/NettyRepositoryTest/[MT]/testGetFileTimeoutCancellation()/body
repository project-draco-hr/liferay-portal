{
  NettyRepository repository=new NettyRepository(_repositoryPath,_embeddedChannel,_embeddedChannel.eventLoop(),0);
  NoticeableFuture<Path> noticeableFuture=repository.getFile(Paths.get("remoteFile"),Paths.get("localFile"),false,false);
  Assert.assertTrue(noticeableFuture.isDone());
  Assert.assertTrue(noticeableFuture.isCancelled());
}

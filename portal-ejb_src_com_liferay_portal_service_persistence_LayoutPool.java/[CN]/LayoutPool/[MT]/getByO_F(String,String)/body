{
  LayoutPK pk=_instance._getByO_F(ownerId,friendlyURL);
  if (_log.isInfoEnabled()) {
    _log.info("Get O_F" + " " + ownerId.toString() + " "+ friendlyURL.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

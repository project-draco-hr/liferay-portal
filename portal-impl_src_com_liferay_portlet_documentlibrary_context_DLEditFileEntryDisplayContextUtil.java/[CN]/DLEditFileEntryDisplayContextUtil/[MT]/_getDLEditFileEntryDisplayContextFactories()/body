{
  try {
    Registry registry=RegistryUtil.getRegistry();
    return registry.getServices(DLEditFileEntryDisplayContextFactory.class,null);
  }
 catch (  Exception e) {
    throw new SystemException(e);
  }
}

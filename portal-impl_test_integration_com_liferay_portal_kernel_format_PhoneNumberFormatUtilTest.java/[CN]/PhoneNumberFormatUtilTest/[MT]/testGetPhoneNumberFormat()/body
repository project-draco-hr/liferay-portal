{
  PhoneNumberFormat phoneNumberFormat=PhoneNumberFormatUtil.getPhoneNumberFormat();
  Class<?> clazz=phoneNumberFormat.getClass();
  Assert.assertEquals(TestPhoneNumberFormat.class.getName(),clazz.getName());
}

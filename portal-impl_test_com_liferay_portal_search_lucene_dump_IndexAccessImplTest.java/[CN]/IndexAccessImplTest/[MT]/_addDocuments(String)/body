{
  for (int i=0; i < _documentNumber; i++) {
    Document document=new Document();
    document.add(new Field("name",key + i,Field.Store.YES,Field.Index.ANALYZED));
    _indexAccessorImpl.addDocument(document);
  }
}

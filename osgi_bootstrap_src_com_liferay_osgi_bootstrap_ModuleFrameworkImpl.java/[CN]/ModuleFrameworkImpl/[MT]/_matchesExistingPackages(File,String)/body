{
  try {
    String storedHashcode=FileUtil.read(hashcodeFile);
    if (storedHashcode.equals(hashcode)) {
      return true;
    }
  }
 catch (  IOException e) {
    _log.error(e,e);
  }
  return false;
}

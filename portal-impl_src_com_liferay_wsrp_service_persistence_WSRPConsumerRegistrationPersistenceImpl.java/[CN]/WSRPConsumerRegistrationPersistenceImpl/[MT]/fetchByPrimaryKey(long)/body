{
  Session session=null;
  try {
    session=openSession();
    return (WSRPConsumerRegistration)session.get(WSRPConsumerRegistrationImpl.class,new Long(consumerRegistrationId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

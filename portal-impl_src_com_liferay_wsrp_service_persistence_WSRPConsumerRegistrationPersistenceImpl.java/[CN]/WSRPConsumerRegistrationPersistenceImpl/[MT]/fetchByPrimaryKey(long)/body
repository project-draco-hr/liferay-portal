{
  Session session=null;
  try {
    session=openSession();
    return (WSRPConsumerRegistration)session.get(WSRPConsumerRegistrationImpl.class,new Long(registrationId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    WSRPConsumerRegistration wsrpConsumerRegistration=(WSRPConsumerRegistration)session.get(WSRPConsumerRegistrationImpl.class,new Long(registrationId));
    if (wsrpConsumerRegistration == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No WSRPConsumerRegistration exists with the primary key " + registrationId);
      }
      throw new NoSuchConsumerRegistrationException("No WSRPConsumerRegistration exists with the primary key " + registrationId);
    }
    return remove(wsrpConsumerRegistration);
  }
 catch (  NoSuchConsumerRegistrationException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

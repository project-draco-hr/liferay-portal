{
  WSRPConsumerRegistration wsrpConsumerRegistration=fetchByPrimaryKey(registrationId);
  if (wsrpConsumerRegistration == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No WSRPConsumerRegistration exists with the primary key " + registrationId);
    }
    throw new NoSuchConsumerRegistrationException("No WSRPConsumerRegistration exists with the primary key " + registrationId);
  }
  return wsrpConsumerRegistration;
}

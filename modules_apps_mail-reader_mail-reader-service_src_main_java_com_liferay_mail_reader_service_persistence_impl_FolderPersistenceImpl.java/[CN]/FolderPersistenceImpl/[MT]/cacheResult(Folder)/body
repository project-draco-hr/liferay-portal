{
  entityCache.putResult(FolderModelImpl.ENTITY_CACHE_ENABLED,FolderImpl.class,folder.getPrimaryKey(),folder);
  finderCache.putResult(FINDER_PATH_FETCH_BY_A_F,new Object[]{folder.getAccountId(),folder.getFullName()},folder);
  folder.resetOriginalValues();
}

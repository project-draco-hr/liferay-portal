{
  if (template.equals(TEMPLATE_FULL_CONTENT)) {
    portletRequest.setAttribute(WebKeys.LAYOUT_REVISION,_layoutRevision);
    return "/asset/" + template + ".jsp";
  }
 else {
    return null;
  }
}

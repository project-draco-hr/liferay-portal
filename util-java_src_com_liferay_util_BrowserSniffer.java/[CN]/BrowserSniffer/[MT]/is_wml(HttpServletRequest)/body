{
  if (req == null) {
    return false;
  }
  String accept=req.getHeader(ACCEPT);
  if (accept == null) {
    return false;
  }
  accept=accept.toLowerCase();
  if (accept.indexOf("wap.wml") != -1) {
    return true;
  }
 else {
    return false;
  }
}

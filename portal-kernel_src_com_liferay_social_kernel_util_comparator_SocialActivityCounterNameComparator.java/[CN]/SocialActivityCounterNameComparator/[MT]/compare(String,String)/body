{
  String name1=LanguageUtil.get(_locale,"social.counter." + activityCounterName1);
  String name2=LanguageUtil.get(_locale,"social.counter." + activityCounterName2);
  Collator collator=Collator.getInstance(_locale);
  return collator.compare(name1,name2);
}

{
  int count=countByMBThreadId(mbThreadId);
  if (count == 0) {
    return null;
  }
  List<UserThread> list=findByMBThreadId(mbThreadId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  int count=countByU_D(userId,deleted);
  if (count == 0) {
    return null;
  }
  List<UserThread> list=findByU_D(userId,deleted,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

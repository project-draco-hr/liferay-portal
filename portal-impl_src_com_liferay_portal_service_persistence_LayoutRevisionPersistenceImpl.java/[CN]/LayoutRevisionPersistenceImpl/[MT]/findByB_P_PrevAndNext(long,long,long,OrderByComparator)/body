{
  LayoutRevision layoutRevision=findByPrimaryKey(revisionId);
  Session session=null;
  try {
    session=openSession();
    LayoutRevision[] array=new LayoutRevisionImpl[3];
    array[0]=getByB_P_PrevAndNext(session,layoutRevision,branchId,plid,orderByComparator,true);
    array[1]=layoutRevision;
    array[2]=getByB_P_PrevAndNext(session,layoutRevision,branchId,plid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

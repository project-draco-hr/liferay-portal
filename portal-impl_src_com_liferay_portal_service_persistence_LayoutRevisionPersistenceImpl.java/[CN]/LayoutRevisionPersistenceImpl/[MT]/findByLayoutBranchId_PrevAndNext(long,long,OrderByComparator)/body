{
  LayoutRevision layoutRevision=findByPrimaryKey(layoutRevisionId);
  Session session=null;
  try {
    session=openSession();
    LayoutRevision[] array=new LayoutRevisionImpl[3];
    array[0]=getByLayoutBranchId_PrevAndNext(session,layoutRevision,layoutBranchId,orderByComparator,true);
    array[1]=layoutRevision;
    array[2]=getByLayoutBranchId_PrevAndNext(session,layoutRevision,layoutBranchId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

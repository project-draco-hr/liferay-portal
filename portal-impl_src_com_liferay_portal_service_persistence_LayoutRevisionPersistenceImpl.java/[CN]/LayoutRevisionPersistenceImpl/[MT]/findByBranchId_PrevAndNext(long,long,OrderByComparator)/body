{
  LayoutRevision layoutRevision=findByPrimaryKey(revisionId);
  Session session=null;
  try {
    session=openSession();
    LayoutRevision[] array=new LayoutRevisionImpl[3];
    array[0]=getByBranchId_PrevAndNext(session,layoutRevision,branchId,orderByComparator,true);
    array[1]=layoutRevision;
    array[2]=getByBranchId_PrevAndNext(session,layoutRevision,branchId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

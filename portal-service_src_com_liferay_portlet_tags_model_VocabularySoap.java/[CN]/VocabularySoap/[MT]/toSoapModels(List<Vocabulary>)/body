{
  List<VocabularySoap> soapModels=new ArrayList<VocabularySoap>(models.size());
  for (  Vocabulary model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new VocabularySoap[soapModels.size()]);
}

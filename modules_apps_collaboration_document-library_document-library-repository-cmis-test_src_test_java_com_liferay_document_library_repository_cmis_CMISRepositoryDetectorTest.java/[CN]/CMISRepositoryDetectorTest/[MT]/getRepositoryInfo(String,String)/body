{
  RepositoryInfo repositoryInfo=mock(RepositoryInfo.class);
  when(repositoryInfo.getProductName()).thenReturn(productName);
  when(repositoryInfo.getProductVersion()).thenReturn(productVersion);
  return repositoryInfo;
}

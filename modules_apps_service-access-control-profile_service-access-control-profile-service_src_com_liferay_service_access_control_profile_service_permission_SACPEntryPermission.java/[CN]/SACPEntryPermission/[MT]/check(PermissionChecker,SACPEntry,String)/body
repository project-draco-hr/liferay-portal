{
  if (!contains(permissionChecker,sacpEntry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),SACPEntry.class.getName(),sacpEntry.getSacpEntryId(),actionId);
  }
}

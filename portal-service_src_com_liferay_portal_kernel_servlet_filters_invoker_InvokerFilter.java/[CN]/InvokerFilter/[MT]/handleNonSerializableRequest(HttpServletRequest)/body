{
  if (ServerDetector.isWebLogic()) {
    if (!NonSerializableObjectRequestWrapper.isRequestWrapped(request)) {
      request=new NonSerializableObjectRequestWrapper(request);
    }
  }
  return request;
}

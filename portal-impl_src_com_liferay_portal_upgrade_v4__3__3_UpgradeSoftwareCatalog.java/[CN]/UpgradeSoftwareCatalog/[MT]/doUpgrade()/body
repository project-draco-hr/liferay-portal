{
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(SCFrameworkVersionImpl.TABLE_NAME,SCFrameworkVersionImpl.TABLE_COLUMNS);
  upgradeTable.updateTable();
  upgradeTable=new DefaultUpgradeTableImpl(SCLicenseImpl.TABLE_NAME,SCLicenseImpl.TABLE_COLUMNS);
  upgradeTable.updateTable();
  upgradeTable=new DefaultUpgradeTableImpl(SCProductEntryImpl.TABLE_NAME,SCProductEntryImpl.TABLE_COLUMNS);
  upgradeTable.updateTable();
  upgradeTable=new DefaultUpgradeTableImpl(SCProductVersionImpl.TABLE_NAME,SCProductVersionImpl.TABLE_COLUMNS);
  upgradeTable.updateTable();
}

{
  if (documents.isEmpty()) {
    return;
  }
  SearchRequest searchRequest=SearchRequest.addMultiple(companyId,documents);
  MessageBusUtil.sendMessage(DestinationNames.SEARCH_WRITER,searchRequest);
}

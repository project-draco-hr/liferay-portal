{
  validate(fileName,validateFileExtension);
  if ((bytes == null) || ((PrefsPropsUtil.getLong(PropsKeys.DL_FILE_MAX_SIZE) > 0) && (bytes.length > PrefsPropsUtil.getLong(PropsKeys.DL_FILE_MAX_SIZE)))) {
    throw new FileSizeException(fileName);
  }
}

{
  if (!isValidName(fileName)) {
    throw new FileNameException(fileName);
  }
  if (fileName.endsWith(TempFileUtil.SUFFIX_TEMP_FILE_NAME)) {
    fileName=StringUtil.replaceLast(fileName,TempFileUtil.SUFFIX_TEMP_FILE_NAME,StringPool.BLANK);
  }
  if (validateFileExtension) {
    String fileNameLowerCase=fileName.toLowerCase();
    boolean validFileExtension=false;
    String[] fileExtensions=PrefsPropsUtil.getStringArray(PropsKeys.DL_FILE_EXTENSIONS,StringPool.COMMA);
    for (    String fileExtension : fileExtensions) {
      if (StringPool.STAR.equals(fileExtension) || fileNameLowerCase.endsWith(fileExtension.toLowerCase())) {
        validFileExtension=true;
        break;
      }
    }
    if (!validFileExtension) {
      throw new FileExtensionException(fileName);
    }
  }
}

{
  validate(fileName,fileExtension,sourceFileName,validateFileExtension,file);
  AntivirusScannerUtil.scan(file);
  store.updateFile(companyId,repositoryId,fileName,versionNumber,sourceFileName,file);
}

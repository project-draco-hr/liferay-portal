{
  validate(fileName,fileExtension,sourceFileName,validateFileExtension);
  if ((file != null) && (PrefsPropsUtil.getLong(PropsKeys.DL_FILE_MAX_SIZE) > 0) && (file.length() > PrefsPropsUtil.getLong(PropsKeys.DL_FILE_MAX_SIZE))) {
    throw new FileSizeException(fileName);
  }
}

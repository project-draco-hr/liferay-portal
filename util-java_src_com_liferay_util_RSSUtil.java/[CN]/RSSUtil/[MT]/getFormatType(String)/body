{
  if (format == null) {
    return DEFAULT_TYPE;
  }
  int x=format.indexOf(ATOM);
  if (x >= 0) {
    return ATOM;
  }
  int y=format.indexOf(RSS);
  if (y >= 0) {
    return RSS;
  }
  return DEFAULT_TYPE;
}

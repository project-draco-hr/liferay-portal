{
  ScheduledFuture<?> scheduledFuture=_scheduledExecutorService.schedule(_threadDumper,PropsValues.THREAD_DUMP_SPEED_THRESHOLD,TimeUnit.SECONDS);
  try {
    processFilter(ThreadDumpFilter.class,request,response,filterChain);
  }
  finally {
    scheduledFuture.cancel(true);
  }
}

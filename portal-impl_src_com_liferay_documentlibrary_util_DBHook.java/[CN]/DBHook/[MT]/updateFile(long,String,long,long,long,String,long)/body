{
  ContentLocalServiceUtil.updateContent(companyId,portletId,groupId,repositoryId,newRepositoryId,fileName);
  reindex(new String[]{String.valueOf(companyId),portletId,fileName,String.valueOf(newRepositoryId)});
}

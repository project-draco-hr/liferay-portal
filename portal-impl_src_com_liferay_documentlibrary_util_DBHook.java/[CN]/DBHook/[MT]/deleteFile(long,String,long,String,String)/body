{
  if (ContentLocalServiceUtil.removeByC_R_P_V(companyId,repositoryId,fileName,versionNumber)) {
    deleteIndex(companyId,fileName,portletId,repositoryId);
  }
 else {
    throw new NoSuchFileException(fileName);
  }
}

{
  InputStream inputStream=getFileAsStream(companyId,repositoryId,fileName,versionNumber);
  try {
    return FileUtil.getBytes(inputStream);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

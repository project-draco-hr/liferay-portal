{
  ContentLocalServiceUtil.updateContent(companyId,portletId,groupId,repositoryId,fileName,newFileName);
  if (reindex) {
    reindex(new String[]{String.valueOf(companyId),portletId,newFileName,String.valueOf(repositoryId)});
  }
}

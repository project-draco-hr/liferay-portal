{
  Content fileContent=ContentLocalServiceUtil.getContent(companyId,repositoryId,fileName);
  try {
    return fileContent.getData().getBinaryStream();
  }
 catch (  SQLException sqle) {
    throw new SystemException(sqle);
  }
}

{
  InputStream inputStream=getFileAsStream(companyId,repositoryId,fileName);
  try {
    return FileUtil.getBytes(inputStream);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

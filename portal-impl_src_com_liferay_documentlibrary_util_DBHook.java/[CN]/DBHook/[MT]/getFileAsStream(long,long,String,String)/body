{
  Content fileContent=ContentLocalServiceUtil.getContent(companyId,repositoryId,fileName,versionNumber);
  try {
    return fileContent.getData().getBinaryStream();
  }
 catch (  SQLException sqle) {
    throw new SystemException(sqle);
  }
}

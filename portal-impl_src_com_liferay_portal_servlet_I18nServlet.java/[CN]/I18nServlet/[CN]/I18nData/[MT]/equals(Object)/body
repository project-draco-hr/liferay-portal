{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof I18nData)) {
    return false;
  }
  I18nData i18nData=(I18nData)obj;
  if (Validator.equals(getI18nPath(),i18nData.getI18nPath()) && Validator.equals(getLanguageCode(),i18nData.getLanguageCode()) && Validator.equals(getLanguageId(),i18nData.getLanguageId())&& Validator.equals(getPath(),i18nData.getPath())) {
    return true;
  }
  return false;
}

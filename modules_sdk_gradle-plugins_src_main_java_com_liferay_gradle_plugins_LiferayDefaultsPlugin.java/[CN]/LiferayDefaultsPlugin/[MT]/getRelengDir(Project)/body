{
  File relengDir=new File(project.getRootDir(),".releng");
  if (!relengDir.exists()) {
    return null;
  }
  return new File(relengDir,FileUtil.relativize(project.getProjectDir(),project.getRootDir()));
}

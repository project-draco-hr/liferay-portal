{
  if (isEscapedModel()) {
    return (DDMTemplate)this;
  }
 else {
    return (DDMTemplate)Proxy.newProxyInstance(DDMTemplate.class.getClassLoader(),new Class[]{DDMTemplate.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (obj == null) {
    return false;
  }
  ObjectValuePair<K,V> ovp=(ObjectValuePair<K,V>)obj;
  K key=ovp.getKey();
  if (_key == key || (_key != null && _key.equals(key))) {
    return true;
  }
 else {
    return false;
  }
}

{
  String strutsAction=_wikiRequestHelper.getStrutsAction();
  if (strutsAction.endsWith("view_page_activities") || strutsAction.endsWith("view_page_attachments")) {
    return false;
  }
  return true;
}

{
  String strutsAction=_wikiRequestHelper.getStrutsAction();
  String frontPageName=_wikiServiceConfiguration.frontPageName();
  if (Validator.isNull(strutsAction) || (wikiPage != null) && frontPageName.equals(wikiPage.getTitle())) {
    return true;
  }
  return false;
}

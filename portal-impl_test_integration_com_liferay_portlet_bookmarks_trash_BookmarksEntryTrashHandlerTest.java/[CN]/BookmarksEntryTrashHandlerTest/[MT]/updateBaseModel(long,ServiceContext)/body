{
  BookmarksEntry entry=BookmarksEntryLocalServiceUtil.getEntry(primaryKey);
  String description="Content: Enterprise. Open Source. For Life.";
  return BookmarksEntryLocalServiceUtil.updateEntry(entry.getUserId(),primaryKey,entry.getGroupId(),entry.getFolderId(),entry.getName(),entry.getUrl(),description,serviceContext);
}

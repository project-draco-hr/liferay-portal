{
  for (  DDMList ddmList : ddmLists) {
    if (EntityCacheUtil.getResult(DDMListModelImpl.ENTITY_CACHE_ENABLED,DDMListImpl.class,ddmList.getPrimaryKey(),this) == null) {
      cacheResult(ddmList);
    }
  }
}

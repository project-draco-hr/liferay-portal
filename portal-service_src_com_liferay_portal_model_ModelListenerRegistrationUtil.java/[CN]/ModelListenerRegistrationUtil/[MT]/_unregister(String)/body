{
  Registry registry=RegistryUtil.getRegistry();
  ServiceReference<ModelListener<?>> serviceReference=registry.getServiceReference(key);
  if (serviceReference != null) {
    registry.ungetService(serviceReference);
  }
}

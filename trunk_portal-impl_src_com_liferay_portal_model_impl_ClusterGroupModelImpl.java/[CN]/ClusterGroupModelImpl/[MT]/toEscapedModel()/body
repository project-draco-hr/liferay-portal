{
  if (isEscapedModel()) {
    return (ClusterGroup)this;
  }
 else {
    return (ClusterGroup)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

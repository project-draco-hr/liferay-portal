{
  if (isEscapedModel()) {
    return (JournalArticle)this;
  }
 else {
    return (JournalArticle)Proxy.newProxyInstance(JournalArticle.class.getClassLoader(),new Class[]{JournalArticle.class},new AutoEscapeBeanHandler(this));
  }
}

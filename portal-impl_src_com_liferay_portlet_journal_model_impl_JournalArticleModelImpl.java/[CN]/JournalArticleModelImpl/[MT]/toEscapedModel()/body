{
  if (isEscapedModel()) {
    return (JournalArticle)this;
  }
 else {
    return (JournalArticle)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

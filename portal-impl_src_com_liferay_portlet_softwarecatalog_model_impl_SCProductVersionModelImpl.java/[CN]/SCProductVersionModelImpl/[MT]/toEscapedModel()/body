{
  if (isEscapedModel()) {
    return (SCProductVersion)this;
  }
 else {
    return (SCProductVersion)Proxy.newProxyInstance(SCProductVersion.class.getClassLoader(),new Class[]{SCProductVersion.class},new AutoEscapeBeanHandler(this));
  }
}

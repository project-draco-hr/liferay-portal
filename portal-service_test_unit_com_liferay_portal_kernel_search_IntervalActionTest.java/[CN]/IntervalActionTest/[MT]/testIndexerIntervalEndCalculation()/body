{
  IntervalAction intervalActionInstance=new IntervalAction();
  intervalActionInstance.setPerformActionMethod(new IntervalAction.PerformIntervalActionMethod(){
    @Override public void performIntervalAction(    int start,    int end) throws PortalException {
      for (int i=0; i < end; i++) {
        incrementCounter();
      }
    }
  }
);
  intervalActionInstance.setCount(125);
  intervalActionInstance.performInterval();
  Assert.assertEquals(200,_count);
}

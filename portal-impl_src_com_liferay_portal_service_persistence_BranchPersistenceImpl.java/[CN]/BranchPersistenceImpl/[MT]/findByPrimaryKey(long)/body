{
  Branch branch=fetchByPrimaryKey(branchId);
  if (branch == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + branchId);
    }
    throw new NoSuchBranchException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + branchId);
  }
  return branch;
}

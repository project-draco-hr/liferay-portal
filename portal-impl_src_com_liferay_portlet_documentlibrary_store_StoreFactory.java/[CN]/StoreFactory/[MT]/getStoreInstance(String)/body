{
  Store store=_storeServiceTrackerMap.getService(key);
  StoreWrapper storeWrapper=_storeWrapperServiceTrackerMap.getService(key);
  if (storeWrapper != null) {
    return storeWrapper.wrap(store);
  }
  return store;
}

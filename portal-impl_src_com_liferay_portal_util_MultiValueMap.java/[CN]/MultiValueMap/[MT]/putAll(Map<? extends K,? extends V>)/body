{
  MultiValueMap<? extends K,? extends V> mvp=null;
  if (map instanceof MultiValueMap) {
    mvp=(MultiValueMap<? extends K,? extends V>)map;
  }
  for (  K key : map.keySet()) {
    if (mvp != null) {
      putAll(key,mvp.getAll(key));
    }
 else {
      put(key,map.get(key));
    }
  }
}

{
  _atomicState.reset();
  try {
    PortalLDAPUtil.getMultivaluedAttribute(1,null,"baseDN","filter",null);
  }
 catch (  Exception e) {
    Assert.fail();
  }
  Assert.assertTrue(_atomicState.isSet());
}

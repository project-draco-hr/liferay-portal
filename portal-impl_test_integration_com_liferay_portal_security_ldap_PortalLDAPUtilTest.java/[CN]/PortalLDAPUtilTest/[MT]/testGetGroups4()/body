{
  try {
    byte[] byteArray=PortalLDAPUtil.getGroups(4,null,new byte[4],4,"baseDN","groupFilter",new String[4],null);
    Assert.assertEquals(byteArray.length,4);
  }
 catch (  Exception e) {
    Assert.fail();
  }
}

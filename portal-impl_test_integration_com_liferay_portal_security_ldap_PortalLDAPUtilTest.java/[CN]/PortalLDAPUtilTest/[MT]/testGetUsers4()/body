{
  try {
    byte[] byteArray=PortalLDAPUtil.getUsers(4,null,new byte[4],4,"baseDN","userFilter",new String[4],null);
    Assert.assertEquals(byteArray.length,4);
  }
 catch (  Exception e) {
    Assert.fail();
  }
}

{
  try {
    Assert.assertTrue(PortalLDAPUtil.isUserGroupMember(1,1,"testGroup","testUser"));
    Assert.assertFalse(PortalLDAPUtil.isUserGroupMember(2,1,"testGroup","testUser"));
  }
 catch (  Exception e) {
    Assert.fail();
  }
}

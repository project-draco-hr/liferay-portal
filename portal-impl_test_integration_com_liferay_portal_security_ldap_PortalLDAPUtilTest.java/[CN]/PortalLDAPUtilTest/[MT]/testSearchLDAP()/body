{
  try {
    byte[] byteArray=PortalLDAPUtil.searchLDAP(1,null,new byte[1],1,"baseDN","filter",new String[1],null);
    Assert.assertEquals(byteArray.length,1);
  }
 catch (  Exception e) {
    Assert.fail();
  }
}

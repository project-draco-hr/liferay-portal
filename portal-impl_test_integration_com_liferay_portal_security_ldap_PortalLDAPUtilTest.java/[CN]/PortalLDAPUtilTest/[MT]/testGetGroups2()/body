{
  try {
    byte[] byteArray=PortalLDAPUtil.getGroups(2,null,new byte[2],2,"baseDN","groupFilter",null);
    Assert.assertEquals(byteArray.length,2);
  }
 catch (  Exception e) {
    Assert.fail();
  }
}

{
  ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
  BlogsGroupServiceSettings rssBlogsGroupServiceSettings=ModuleConfigurationFactoryUtil.getConfiguration(BlogsGroupServiceSettings.class,new GroupServiceSettingsLocator(themeDisplay.getSiteGroupId(),BlogsConstants.SERVICE_NAME));
  return rssBlogsGroupServiceSettings.enableRss();
}

{
  String contextPath=HttpUtil.fixPath(PortalUtil.getPathContext(request),false,true);
  String servletPath=HttpUtil.fixPath(request.getServletPath(),false,true);
  return contextPath.concat(servletPath);
}

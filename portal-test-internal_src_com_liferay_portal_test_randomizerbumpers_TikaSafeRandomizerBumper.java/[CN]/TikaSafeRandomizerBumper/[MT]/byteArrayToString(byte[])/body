{
  StringBundler sb=new StringBundler((byteArray.length * 3) + 2);
  sb.append("{");
  for (  byte val : byteArray) {
    sb.append("(byte)");
    sb.append(val);
    sb.append(",");
  }
  sb.setIndex(sb.index() - 1);
  sb.append("}");
  return sb.toString();
}

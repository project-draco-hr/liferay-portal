{
  Recurrence recurrence=new Recurrence(dtStart,duration,Recurrence.YEARLY);
  int[] months={month};
  recurrence.setByMonth(months);
  int[] monthDays={monthDay};
  recurrence.setByMonthDay(monthDays);
  recurrence.setInterval(interval);
  return recurrence;
}

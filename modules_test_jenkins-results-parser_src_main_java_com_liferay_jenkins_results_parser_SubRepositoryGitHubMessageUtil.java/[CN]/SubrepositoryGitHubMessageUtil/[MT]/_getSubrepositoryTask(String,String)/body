{
  if (console.contains("A report with all the test results can be found at " + "test-results/html/index.html")) {
    return new SubrepositoryTaskReport(buildURL);
  }
 else {
    return new SubrepositoryTaskNoReport(console);
  }
}

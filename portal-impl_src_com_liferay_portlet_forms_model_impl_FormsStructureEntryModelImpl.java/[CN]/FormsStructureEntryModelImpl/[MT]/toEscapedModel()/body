{
  if (isEscapedModel()) {
    return (FormsStructureEntry)this;
  }
 else {
    return (FormsStructureEntry)Proxy.newProxyInstance(FormsStructureEntry.class.getClassLoader(),new Class[]{FormsStructureEntry.class},new AutoEscapeBeanHandler(this));
  }
}

{
  _serviceReferences.remove(tuple);
  ServiceReferenceServiceTuple<S> headTuple=_serviceReferences.peek();
  if (headTuple != null) {
    _service=headTuple.getService();
  }
 else {
    _service=null;
  }
}

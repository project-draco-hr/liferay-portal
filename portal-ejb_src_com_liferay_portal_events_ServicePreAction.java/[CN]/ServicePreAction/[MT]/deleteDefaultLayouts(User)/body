{
  if (user.hasPrivateLayouts()) {
    Group userGroup=user.getGroup();
    LayoutLocalServiceUtil.deleteLayouts(LayoutImpl.PRIVATE + userGroup.getGroupId());
  }
  if (user.hasPublicLayouts()) {
    Group userGroup=user.getGroup();
    LayoutLocalServiceUtil.deleteLayouts(LayoutImpl.PUBLIC + userGroup.getGroupId());
  }
}

{
  if (getCompanyId() > 0) {
    try {
      return GroupLocalServiceUtil.getCompanyGroup(getCompanyId());
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
  return new GroupImpl();
}

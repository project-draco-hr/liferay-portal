{
  try {
    VirtualHost virtualHost=VirtualHostLocalServiceUtil.getVirtualHost(getCompanyId(),0);
    return virtualHost.getVirtualHostName();
  }
 catch (  Exception e) {
    return StringPool.BLANK;
  }
}

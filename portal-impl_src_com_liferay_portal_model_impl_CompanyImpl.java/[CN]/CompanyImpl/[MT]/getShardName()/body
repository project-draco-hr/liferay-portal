{
  String shardName=PropsValues.SHARD_DEFAULT_NAME;
  try {
    Shard shard=ShardLocalServiceUtil.getShard(Company.class.getName(),getCompanyId());
    shardName=shard.getName();
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return shardName;
}

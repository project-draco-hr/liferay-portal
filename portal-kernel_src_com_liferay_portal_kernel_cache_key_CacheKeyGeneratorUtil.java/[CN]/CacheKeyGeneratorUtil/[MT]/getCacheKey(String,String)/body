{
  CacheKeyGenerator cacheKeyGenerator=_cacheKeyGenerators.get(cacheName);
  if (cacheKeyGenerator == null) {
    cacheKeyGenerator=_defaultKeyGenerator;
  }
  if (cacheKeyGenerator != null) {
    return cacheKeyGenerator.getCacheKey(proposedKey);
  }
  return proposedKey;
}

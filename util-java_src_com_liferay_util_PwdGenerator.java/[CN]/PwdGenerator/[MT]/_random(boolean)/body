{
  try {
    if (secure) {
      if (_secureRandom == null) {
        _secureRandom=new SecureRandom();
      }
      return _secureRandom.nextDouble();
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return Math.random();
}

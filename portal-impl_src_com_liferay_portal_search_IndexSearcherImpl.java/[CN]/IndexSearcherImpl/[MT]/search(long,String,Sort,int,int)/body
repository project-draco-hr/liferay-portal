{
  try {
    SearchRequest searchRequest=new SearchRequest(SearchRequest.COMMAND_SEARCH,companyId,query,sort,start,end);
    String message=MessageBusUtil.sendSynchronizedMessage(DestinationNames.SEARCH,JSONUtil.serialize(searchRequest));
    JSONObject jsonObj=new JSONObject(message);
    return (Hits)JSONUtil.deserialize(jsonObj.getString("hits"));
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}

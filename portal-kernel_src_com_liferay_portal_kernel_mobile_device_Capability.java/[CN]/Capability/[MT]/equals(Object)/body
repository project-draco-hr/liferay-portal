{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof Capability)) {
    return false;
  }
  Capability capability=(Capability)obj;
  if (Validator.equals(_name,capability._name) && Validator.equals(_value,capability._value)) {
    return true;
  }
  return false;
}

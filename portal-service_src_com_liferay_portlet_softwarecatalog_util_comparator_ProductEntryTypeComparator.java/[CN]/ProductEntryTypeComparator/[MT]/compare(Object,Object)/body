{
  SCProductEntry productEntry1=(SCProductEntry)obj1;
  SCProductEntry productEntry2=(SCProductEntry)obj2;
  int value=productEntry1.getType().toLowerCase().compareTo(productEntry2.getType().toLowerCase());
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

{
  SCProductEntry productEntry1=(SCProductEntry)obj1;
  SCProductEntry productEntry2=(SCProductEntry)obj2;
  int value=StringUtil.toLowerCase(productEntry1.getType()).compareTo(StringUtil.toLowerCase(productEntry2.getType()));
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

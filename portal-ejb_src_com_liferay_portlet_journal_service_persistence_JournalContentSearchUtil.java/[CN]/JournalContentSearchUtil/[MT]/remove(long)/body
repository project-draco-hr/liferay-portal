{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(contentSearchId));
  }
  com.liferay.portlet.journal.model.JournalContentSearch journalContentSearch=getPersistence().remove(contentSearchId);
  if (listener != null) {
    listener.onAfterRemove(journalContentSearch);
  }
  return journalContentSearch;
}

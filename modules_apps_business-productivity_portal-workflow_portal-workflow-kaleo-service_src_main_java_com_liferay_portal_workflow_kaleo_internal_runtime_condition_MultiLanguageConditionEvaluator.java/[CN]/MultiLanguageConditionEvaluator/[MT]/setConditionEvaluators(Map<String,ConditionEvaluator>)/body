{
  for (  Map.Entry<String,ConditionEvaluator> entry : conditionEvaluators.entrySet()) {
    ScriptLanguage scriptLanguage=ScriptLanguage.parse(entry.getKey());
    _conditionEvaluators.put(scriptLanguage,entry.getValue());
  }
}

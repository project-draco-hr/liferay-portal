{
  DLFolderPermission.check(getPermissionChecker(),folderId,ActionKeys.ADD_SHORTCUT);
  try {
    DLFileEntryPermission.check(getPermissionChecker(),toFolderId,toName,ActionKeys.VIEW);
  }
 catch (  PrincipalException pe) {
    throw new FileShortcutPermissionException();
  }
  return dlFileShortcutLocalService.addFileShortcut(getUserId(),folderId,toFolderId,toName,addCommunityPermissions,addGuestPermissions);
}

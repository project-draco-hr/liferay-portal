{
  if (isEscapedModel()) {
    return (DDMStructureEntryLink)this;
  }
 else {
    return (DDMStructureEntryLink)Proxy.newProxyInstance(DDMStructureEntryLink.class.getClassLoader(),new Class[]{DDMStructureEntryLink.class},new AutoEscapeBeanHandler(this));
  }
}

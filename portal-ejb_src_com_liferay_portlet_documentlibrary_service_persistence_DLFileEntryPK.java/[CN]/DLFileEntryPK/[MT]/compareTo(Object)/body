{
  if (obj == null) {
    return -1;
  }
  DLFileEntryPK pk=(DLFileEntryPK)obj;
  int value=0;
  value=folderId.compareTo(pk.folderId);
  if (value != 0) {
    return value;
  }
  value=name.compareTo(pk.name);
  if (value != 0) {
    return value;
  }
  return 0;
}

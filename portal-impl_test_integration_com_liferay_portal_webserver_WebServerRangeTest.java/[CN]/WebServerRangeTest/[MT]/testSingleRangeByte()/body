{
  MockHttpServletResponse response=testRange("bytes=10-10");
  Assert.assertEquals("1",response.getHeader(HttpHeaders.CONTENT_LENGTH));
  Assert.assertEquals("bytes 10-10/80",response.getHeader(HttpHeaders.CONTENT_RANGE));
  Assert.assertEquals("B",response.getContentAsString());
}

{
  MockHttpServletResponse response=testRange("bytes=0-9");
  Assert.assertEquals("10",response.getHeader(HttpHeaders.CONTENT_LENGTH));
  Assert.assertEquals("bytes 0-9/80",response.getHeader(HttpHeaders.CONTENT_RANGE));
  Assert.assertEquals("A123456789",response.getContentAsString());
}

{
  String propertyValue=_properties.getProperty(_KEY_REQUIRED_CLASS_NAME_IDS_AND_CLASS_TYPE_PKS);
  if (Validator.isNull(propertyValue)) {
    return new String[0];
  }
  return StringUtil.split(propertyValue);
}

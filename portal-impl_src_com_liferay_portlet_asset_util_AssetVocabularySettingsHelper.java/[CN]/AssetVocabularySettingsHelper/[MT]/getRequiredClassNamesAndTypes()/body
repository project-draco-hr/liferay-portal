{
  String propertyValue=_properties.getProperty(_KEY_REQUIRED_CLASS_NAME_AND_TYPE_IDS);
  if (Validator.isNull(propertyValue)) {
    return new String[0];
  }
  return StringUtil.split(propertyValue);
}

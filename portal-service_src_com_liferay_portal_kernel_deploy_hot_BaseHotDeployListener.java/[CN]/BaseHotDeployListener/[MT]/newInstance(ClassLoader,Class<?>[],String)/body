{
  Object instance=portletClassLoader.loadClass(implClassName).newInstance();
  return Proxy.newProxyInstance(portletClassLoader,interfaceClasses,new ContextClassLoaderBeanHandler(instance,portletClassLoader));
}

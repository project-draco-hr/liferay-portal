{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PasswordPolicy passwordPolicy) method. To improve performance, call update(PasswordPolicy passwordPolicy, boolean merge).");
  }
  return update(passwordPolicy,false);
}

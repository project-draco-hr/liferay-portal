{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Portlet portlet) method. To improve performance, call update(Portlet portlet, boolean merge).");
  }
  return update(portlet,false);
}

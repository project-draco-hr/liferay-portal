{
  for (  Portlet portlet : portlets) {
    if (EntityCacheUtil.getResult(PortletModelImpl.ENTITY_CACHE_ENABLED,Portlet.class,portlet.getPrimaryKey(),this) == null) {
      cacheResult(portlet);
    }
  }
}

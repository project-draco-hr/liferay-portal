{
  Portlet portlet=(Portlet)EntityCacheUtil.getResult(PortletModelImpl.ENTITY_CACHE_ENABLED,PortletImpl.class,primaryKey);
  if (portlet == _nullPortlet) {
    return null;
  }
  if (portlet == null) {
    Session session=null;
    try {
      session=openSession();
      portlet=(Portlet)session.get(PortletImpl.class,primaryKey);
      if (portlet != null) {
        cacheResult(portlet);
      }
 else {
        EntityCacheUtil.putResult(PortletModelImpl.ENTITY_CACHE_ENABLED,PortletImpl.class,primaryKey,_nullPortlet);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(PortletModelImpl.ENTITY_CACHE_ENABLED,PortletImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return portlet;
}

{
  FreeMarkerThemeHelper freeMarkerThemeHelper=new FreeMarkerThemeHelperImpl(request.getServletContext(),request,response,contextObjects);
  contextObjects.put(themeName,freeMarkerThemeHelper);
  contextObjects.put("theme",freeMarkerThemeHelper);
}

{
  Iterator itr=shoppingCategoryPersistence.findByGroupId(groupId).iterator();
  while (itr.hasNext()) {
    ShoppingCategory category=(ShoppingCategory)itr.next();
    deleteCategory(category);
  }
}

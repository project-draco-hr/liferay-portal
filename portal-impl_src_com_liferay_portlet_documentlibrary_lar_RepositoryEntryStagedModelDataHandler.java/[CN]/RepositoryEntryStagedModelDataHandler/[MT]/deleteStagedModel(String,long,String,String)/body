{
  RepositoryEntry repositoryEntry=fetchExistingStagedModel(uuid,groupId);
  if (repositoryEntry != null) {
    RepositoryEntryLocalServiceUtil.deleteRepositoryEntry(repositoryEntry.getRepositoryId());
  }
}

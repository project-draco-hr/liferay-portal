{
  List<RepositoryEntry> entries=RepositoryEntryLocalServiceUtil.getRepositoryEntriesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<RepositoryEntry>());
  if (ListUtil.isEmpty(entries)) {
    return null;
  }
  return entries.get(0);
}

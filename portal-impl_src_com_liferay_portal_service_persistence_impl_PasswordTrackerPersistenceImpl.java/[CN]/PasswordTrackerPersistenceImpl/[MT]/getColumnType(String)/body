{
  Integer type=PasswordTrackerModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ PasswordTrackerModelImpl.TABLE_NAME);
  }
  return type;
}

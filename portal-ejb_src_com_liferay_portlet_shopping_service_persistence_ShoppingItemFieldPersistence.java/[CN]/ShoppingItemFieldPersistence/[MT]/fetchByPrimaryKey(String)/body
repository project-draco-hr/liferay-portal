{
  Session session=null;
  try {
    session=openSession();
    return (ShoppingItemField)session.get(ShoppingItemField.class,itemFieldId);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

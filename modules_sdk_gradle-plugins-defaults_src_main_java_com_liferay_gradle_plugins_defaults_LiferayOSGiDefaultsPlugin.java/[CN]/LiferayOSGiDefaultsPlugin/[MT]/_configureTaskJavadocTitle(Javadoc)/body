{
  Project project=javadoc.getProject();
  String bundleName=getBundleInstruction(project,Constants.BUNDLE_NAME);
  if (Validator.isNull(bundleName)) {
    return;
  }
  String title=String.format("%s %s API",bundleName,project.getVersion());
  javadoc.setTitle(title);
}

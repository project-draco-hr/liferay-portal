{
  ResourceRetriever resourceRetriever=_resourceManager.getResourceRetriever("resource-not-found.properties");
  Assert.assertNotNull(resourceRetriever);
  Assert.assertNull(resourceRetriever.getInputStream());
}

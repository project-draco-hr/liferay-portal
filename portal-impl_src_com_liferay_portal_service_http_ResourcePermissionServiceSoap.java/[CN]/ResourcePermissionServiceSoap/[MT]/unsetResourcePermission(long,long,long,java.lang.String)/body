{
  try {
    ResourcePermissionServiceUtil.unsetResourcePermission(roleId,groupId,resourceId,actionId);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

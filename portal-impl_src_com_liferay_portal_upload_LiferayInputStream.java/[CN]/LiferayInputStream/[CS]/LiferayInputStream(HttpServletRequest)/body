{
  super(request.getInputStream());
  _session=request.getSession();
  _totalSize=request.getContentLength();
  if (_totalSize < 0) {
    _totalSize=GetterUtil.getLong(request.getHeader(HttpHeaders.CONTENT_LENGTH),_totalSize);
  }
}

{
  if (_totalSize < THRESHOLD_SIZE) {
    return this;
  }
 else {
    return new UnsyncByteArrayInputStreamWrapper(new UnsyncByteArrayInputStream(_cachedBytes.unsafeGetByteArray(),0,_cachedBytes.size()));
  }
}

{
  _configuration=configuration;
  try {
    _normalizeNameMethod=ReflectionUtil.getDeclaredMethod(TemplateCache.class,"normalizeName",String.class);
  }
 catch (  Exception e) {
    throw new TemplateException(e);
  }
  String loaderName=TemplateResourceLoaderUtil.getTemplateResourceLoader(TemplateManager.FREEMARKER).getName();
  _portalCache=SingleVMPoolUtil.getCache(TemplateResource.class.getName().concat(StringPool.POUND).concat(loaderName));
}

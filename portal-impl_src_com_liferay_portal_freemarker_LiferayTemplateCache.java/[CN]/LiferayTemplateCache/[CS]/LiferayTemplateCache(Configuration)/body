{
  _configuration=configuration;
  try {
    _normalizeNameMethod=ReflectionUtil.getDeclaredMethod(TemplateCache.class,"normalizeName",String.class);
  }
 catch (  Exception e) {
    throw new TemplateException(e);
  }
  String cacheName=TemplateResource.class.getName();
  cacheName=cacheName.concat(StringPool.POUND).concat(TemplateManager.FREEMARKER);
  _portalCache=SingleVMPoolUtil.getCache(cacheName);
}

{
  for (int second=0; ; second++) {
    if (second >= 240) {
      fail("timeout");
    }
    try {
      if (selenium.isTextPresent("The system is shutdown. Please try again later.")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SocialRequest socialRequest) method. Use update(SocialRequest socialRequest, boolean merge) instead.");
  }
  return update(socialRequest,false);
}

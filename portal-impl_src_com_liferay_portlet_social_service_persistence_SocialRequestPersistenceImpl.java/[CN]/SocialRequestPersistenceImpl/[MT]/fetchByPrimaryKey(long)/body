{
  SocialRequest socialRequest=(SocialRequest)EntityCacheUtil.getResult(SocialRequestModelImpl.ENTITY_CACHE_ENABLED,SocialRequestImpl.class,requestId,this);
  if (socialRequest == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (socialRequest == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      socialRequest=(SocialRequest)session.get(SocialRequestImpl.class,Long.valueOf(requestId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (socialRequest == null)) {
        EntityCacheUtil.putResult(SocialRequestModelImpl.ENTITY_CACHE_ENABLED,SocialRequestImpl.class,requestId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(socialRequest);
      }
      closeSession(session);
    }
  }
  return socialRequest;
}

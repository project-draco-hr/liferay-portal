{
  User user=fetchByPrimaryKey(pk);
  if (user == null) {
    userToRoleTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,rolePK);
  }
 else {
    userToRoleTableMapper.addTableMapping(user.getCompanyId(),pk,rolePK);
  }
}

{
  User user=fetchByPrimaryKey(pk);
  if (user == null) {
    userToUserGroupTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,userGroupPK);
  }
 else {
    userToUserGroupTableMapper.addTableMapping(user.getCompanyId(),pk,userGroupPK);
  }
}

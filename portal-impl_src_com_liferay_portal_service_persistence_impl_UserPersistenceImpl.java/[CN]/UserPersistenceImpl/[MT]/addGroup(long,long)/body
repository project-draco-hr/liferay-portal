{
  User user=fetchByPrimaryKey(pk);
  if (user == null) {
    userToGroupTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,groupPK);
  }
 else {
    userToGroupTableMapper.addTableMapping(user.getCompanyId(),pk,groupPK);
  }
}

{
  WikiPageLocalService wikiPageLocalService=WikiPageLocalServiceFactory.getService();
  return wikiPageLocalService.addPage(uuid,userId,nodeId,title,version,content,summary,minorEdit,format,head,parentTitle,redirectTitle,tagsEntries,prefs,themeDisplay);
}

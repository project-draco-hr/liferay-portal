{
  WikiPageLocalService wikiPageLocalService=WikiPageLocalServiceFactory.getService();
  return wikiPageLocalService.addPage(uuid,userId,nodeId,title,version,content,format,head,parentTitle,redirectTitle,tagsEntries,prefs,themeDisplay);
}

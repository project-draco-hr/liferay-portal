{
  WikiPageLocalService wikiPageLocalService=WikiPageLocalServiceFactory.getService();
  return wikiPageLocalService.updatePage(userId,nodeId,title,version,content,format,redirectTo,tagsEntries,prefs,themeDisplay);
}

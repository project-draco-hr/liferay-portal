{
  WikiPageLocalService wikiPageLocalService=WikiPageLocalServiceFactory.getService();
  return wikiPageLocalService.addPage(uuid,userId,nodeId,title,version,content,format,head,redirectTo,tagsEntries,prefs,themeDisplay);
}

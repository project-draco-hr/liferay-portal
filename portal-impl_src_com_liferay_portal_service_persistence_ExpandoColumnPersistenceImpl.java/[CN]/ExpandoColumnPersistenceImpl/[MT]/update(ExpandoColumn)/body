{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoColumn expandoColumn) method. To improve performance, call update(ExpandoColumn expandoColumn, boolean merge).");
  }
  return update(expandoColumn,false);
}

{
  _resourceURLs=new ArrayList<>();
  Enumeration<URL> urls=getBundle().findEntries("META-INF/resources",_ES_JS_FILE_EXTENSION,true);
  if (urls != null) {
    _resourceURLs.addAll(Collections.list(urls));
  }
}

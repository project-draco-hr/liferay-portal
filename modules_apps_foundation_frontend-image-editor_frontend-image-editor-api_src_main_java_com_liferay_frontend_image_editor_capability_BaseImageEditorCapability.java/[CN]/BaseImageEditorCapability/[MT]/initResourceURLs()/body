{
  _resourceURLs=new ArrayList<>();
  Enumeration<URL> enumeration=getBundle().findEntries("META-INF/resources","*.es.js",true);
  if (enumeration != null) {
    _resourceURLs.addAll(Collections.list(enumeration));
  }
}

{
  _resourceURLs=new ArrayList<>();
  Enumeration<URL> enumeration=getBundle().findEntries("META-INF/resources",_ES_JS_FILE_EXTENSION,true);
  if (enumeration != null) {
    _resourceURLs.addAll(Collections.list(enumeration));
  }
}

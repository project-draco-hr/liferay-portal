{
  try (LoggingTimer loggingTimer=new LoggingTimer()){
    for (int i=0; i < _OLD_PORTLET_PREFERENCES.length; i++) {
      updatePreferences("PortletPreferences","portletPreferencesId",_OLD_PORTLET_PREFERENCES[i],_NEW_PORTLET_PREFERENCES[i]);
    }
  }
 }

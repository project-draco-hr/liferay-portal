{
  if (isEscapedModel()) {
    return (JournalStructure)this;
  }
 else {
    return (JournalStructure)Proxy.newProxyInstance(JournalStructure.class.getClassLoader(),new Class[]{JournalStructure.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Subscription subscription) method. To improve performance, call update(Subscription subscription, boolean merge).");
  }
  return update(subscription,false);
}

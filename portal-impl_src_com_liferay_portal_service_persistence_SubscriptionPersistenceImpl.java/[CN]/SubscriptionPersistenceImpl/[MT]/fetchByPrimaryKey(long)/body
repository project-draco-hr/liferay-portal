{
  Subscription result=(Subscription)EntityCacheUtil.getResult(SubscriptionModelImpl.ENTITY_CACHE_ENABLED,Subscription.class,subscriptionId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      Subscription subscription=(Subscription)session.get(SubscriptionImpl.class,new Long(subscriptionId));
      cacheResult(subscription);
      return subscription;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (Subscription)result;
  }
}

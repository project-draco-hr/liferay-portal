{
  for (  Subscription subscription : subscriptions) {
    if (EntityCacheUtil.getResult(SubscriptionModelImpl.ENTITY_CACHE_ENABLED,Subscription.class,subscription.getPrimaryKey(),this) == null) {
      cacheResult(subscription);
    }
  }
}

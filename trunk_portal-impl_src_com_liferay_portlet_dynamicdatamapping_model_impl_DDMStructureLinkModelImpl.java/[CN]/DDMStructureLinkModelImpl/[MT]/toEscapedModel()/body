{
  if (isEscapedModel()) {
    return (DDMStructureLink)this;
  }
 else {
    return (DDMStructureLink)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  String groupId=Layout.getGroupId(ownerId);
  GroupPermission.check(getPermissionChecker(),groupId,ActionKeys.MANAGE_LAYOUTS);
  return LayoutSetLocalServiceUtil.updateLookAndFeel(ownerId,themeId,colorSchemeId);
}

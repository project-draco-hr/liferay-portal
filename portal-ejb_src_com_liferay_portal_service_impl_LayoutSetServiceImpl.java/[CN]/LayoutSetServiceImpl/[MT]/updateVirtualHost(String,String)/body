{
  long groupId=LayoutImpl.getGroupId(ownerId);
  GroupPermission.check(getPermissionChecker(),groupId,ActionKeys.UPDATE);
  return LayoutSetLocalServiceUtil.updateVirtualHost(ownerId,virtualHost);
}

{
  LocalRepository localRepository=_repositoryFactory.createLocalRepository(repositoryId);
  CapabilityLocalRepository capabilityLocalRepository=new CapabilityLocalRepository(localRepository,getRepositoryEventTrigger());
  _repositoryDefiner.registerCapabilities(capabilityLocalRepository);
  setupCommonCapabilities(capabilityLocalRepository);
  return capabilityLocalRepository;
}

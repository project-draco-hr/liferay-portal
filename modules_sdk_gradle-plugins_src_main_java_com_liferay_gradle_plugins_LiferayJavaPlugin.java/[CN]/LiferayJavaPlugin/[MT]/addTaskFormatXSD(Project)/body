{
  FormatXMLTask formatXMLTask=GradleUtil.addTask(project,FORMAT_XSD_TASK_NAME,FormatXMLTask.class);
  formatXMLTask.setDescription("Runs Liferay XML Formatter to format XSD files.");
  return formatXMLTask;
}

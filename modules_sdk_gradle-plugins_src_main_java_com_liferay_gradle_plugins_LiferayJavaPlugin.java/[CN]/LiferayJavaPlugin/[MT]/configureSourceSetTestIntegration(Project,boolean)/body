{
  File classesDir=project.file("test-classes/integration");
  File srcDir=null;
  if (legacyDirLayout) {
    srcDir=project.file("test/integration");
  }
  configureSourceSet(project,TEST_INTEGRATION_SOURCE_SET_NAME,classesDir,srcDir);
}

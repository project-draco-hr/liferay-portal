{
  try {
    return _storageEngine.create(companyId,ddmStructureId,DDMBeanCopyUtil.copyDDMFormValues(ddmFormValues),serviceContext);
  }
 catch (  PortalException pe) {
    throw translate(pe);
  }
}

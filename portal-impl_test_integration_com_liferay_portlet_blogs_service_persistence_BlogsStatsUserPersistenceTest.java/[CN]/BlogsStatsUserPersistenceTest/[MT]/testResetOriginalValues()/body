{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  BlogsStatsUser newBlogsStatsUser=addBlogsStatsUser();
  _persistence.clearCache();
  BlogsStatsUserModelImpl existingBlogsStatsUserModelImpl=(BlogsStatsUserModelImpl)_persistence.findByPrimaryKey(newBlogsStatsUser.getPrimaryKey());
  Assert.assertEquals(existingBlogsStatsUserModelImpl.getGroupId(),existingBlogsStatsUserModelImpl.getOriginalGroupId());
  Assert.assertEquals(existingBlogsStatsUserModelImpl.getUserId(),existingBlogsStatsUserModelImpl.getOriginalUserId());
}

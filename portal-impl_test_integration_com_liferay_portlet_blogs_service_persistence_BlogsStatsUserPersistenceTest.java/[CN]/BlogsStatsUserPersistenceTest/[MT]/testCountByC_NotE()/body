{
  try {
    _persistence.countByC_NotE(RandomTestUtil.nextLong(),RandomTestUtil.nextInt());
    _persistence.countByC_NotE(0L,0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

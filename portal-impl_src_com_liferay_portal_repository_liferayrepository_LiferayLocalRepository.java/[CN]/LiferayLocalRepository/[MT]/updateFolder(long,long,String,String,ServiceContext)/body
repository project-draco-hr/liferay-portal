{
  initByFolder(folderId);
  DLFolder dlFolder=DLRepositoryLocalServiceUtil.updateFolder(getFolderId(folderId),getFolderId(parentFolderId),title,description,serviceContext);
  return new LiferayFolder(dlFolder);
}

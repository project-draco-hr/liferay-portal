{
  DLFileEntry dlFileEntry=DLRepositoryLocalServiceUtil.addFileEntry(userId,getGroupId(),getFolderId(folderId),title,description,changeLog,extraSettings,is,size,serviceContext);
  return new LiferayFileEntry(dlFileEntry);
}

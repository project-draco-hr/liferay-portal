{
  DLFileEntry dlFileEntry=DLRepositoryLocalServiceUtil.addFileEntry(userId,getGroupId(),toFolderId(folderId),title,description,changeLog,extraSettings,is,size,serviceContext);
  return new LiferayFileEntry(dlFileEntry);
}

{
  Integer type=RatingsEntryModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ RatingsEntryModelImpl.TABLE_NAME);
  }
  return type;
}

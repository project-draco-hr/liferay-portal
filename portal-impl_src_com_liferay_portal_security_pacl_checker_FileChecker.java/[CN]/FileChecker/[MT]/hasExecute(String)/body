{
  Permission permission=new FilePermission(fileName,_PERMISSION_EXECUTE);
  for (  Permission executeFilePermission : _executePermissions) {
    if (executeFilePermission.implies(permission)) {
      return true;
    }
  }
  return false;
}

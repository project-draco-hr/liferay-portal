{
  if (!ServerDetector.isJOnAS() && ServerDetector.isJetty()) {
    return new JettySharedSessionWrapper(session,_sharedSessionAttributes);
  }
 else {
    return new SharedSessionWrapper(session,_sharedSessionAttributes);
  }
}

{
  User user=userPersistence.findByPrimaryKey(userId);
  SocialActivityCounter activityCounter=getActivityCounter(groupId,user,new SocialActivityImpl(),SocialActivityCounterDefinition.USER_ACHIEVEMENTS);
  incrementActivityCounter(activityCounter,SocialActivityCounterDefinition.USER_ACHIEVEMENTS);
}

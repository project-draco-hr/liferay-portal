{
  SocialActivityCounter activityCounter=null;
  try {
    activityCounter=getSocialActivityCounterLocalService().addActivityCounter(companyId,groupId,classNameId,classPK,name,ownerType,0,overallValue);
  }
 catch (  SystemException se) {
    activityCounter=fetchLastCounter(groupId,classNameId,classPK,name,ownerType);
  }
  return activityCounter;
}

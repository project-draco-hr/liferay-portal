{
  String var=tagData.getAttributeString("var");
  if (Validator.isNull(var)) {
    var=SearchContainer.DEFAULT_VAR;
  }
  return new VariableInfo[]{new VariableInfo(var,SearchContainer.class.getName(),true,VariableInfo.NESTED)};
}

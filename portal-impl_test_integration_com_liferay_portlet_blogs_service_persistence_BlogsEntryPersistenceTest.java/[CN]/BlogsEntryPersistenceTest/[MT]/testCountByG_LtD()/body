{
  try {
    _persistence.countByG_LtD(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDate());
    _persistence.countByG_LtD(0L,ServiceTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  try {
    _persistence.countByC_LtD_NotS(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDate(),ServiceTestUtil.nextInt());
    _persistence.countByC_LtD_NotS(0L,ServiceTestUtil.nextDate(),0);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

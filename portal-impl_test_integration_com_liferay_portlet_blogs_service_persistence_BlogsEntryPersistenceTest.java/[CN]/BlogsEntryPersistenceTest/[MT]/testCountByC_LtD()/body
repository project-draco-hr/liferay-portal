{
  try {
    _persistence.countByC_LtD(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDate());
    _persistence.countByC_LtD(0L,ServiceTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

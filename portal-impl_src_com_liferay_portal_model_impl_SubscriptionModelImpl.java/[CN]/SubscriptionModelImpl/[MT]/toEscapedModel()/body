{
  if (isEscapedModel()) {
    return (Subscription)this;
  }
 else {
    return (Subscription)Proxy.newProxyInstance(Subscription.class.getClassLoader(),new Class[]{Subscription.class},new AutoEscapeBeanHandler(this));
  }
}

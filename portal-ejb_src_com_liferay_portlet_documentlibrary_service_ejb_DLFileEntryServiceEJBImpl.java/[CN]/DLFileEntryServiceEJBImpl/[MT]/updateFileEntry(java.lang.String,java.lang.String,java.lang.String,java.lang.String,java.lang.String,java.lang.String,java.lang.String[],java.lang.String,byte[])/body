{
  PrincipalSessionBean.setThreadValues(_sc);
  return DLFileEntryServiceFactory.getTxImpl().updateFileEntry(folderId,newFolderId,name,sourceFileName,title,description,tagsEntries,extraSettings,byteArray);
}

{
  if (folderId == DLFolderImpl.DEFAULT_PARENT_FOLDER_ID) {
    return PortletPermission.contains(permissionChecker,plid,PortletKeys.DOCUMENT_LIBRARY,actionId);
  }
 else {
    return contains(permissionChecker,folderId,actionId);
  }
}

{
  if (isEscapedModel()) {
    return (ExpandoTable)this;
  }
 else {
    return (ExpandoTable)Proxy.newProxyInstance(ExpandoTable.class.getClassLoader(),new Class[]{ExpandoTable.class},new AutoEscapeBeanHandler(this));
  }
}

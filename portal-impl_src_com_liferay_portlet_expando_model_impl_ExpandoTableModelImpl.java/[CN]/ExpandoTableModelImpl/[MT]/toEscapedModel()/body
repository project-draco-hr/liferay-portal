{
  if (isEscapedModel()) {
    return (ExpandoTable)this;
  }
 else {
    ExpandoTable model=new ExpandoTableImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setTableId(getTableId());
    model.setClassNameId(getClassNameId());
    model.setName(HtmlUtil.escape(getName()));
    model=(ExpandoTable)Proxy.newProxyInstance(ExpandoTable.class.getClassLoader(),new Class[]{ExpandoTable.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

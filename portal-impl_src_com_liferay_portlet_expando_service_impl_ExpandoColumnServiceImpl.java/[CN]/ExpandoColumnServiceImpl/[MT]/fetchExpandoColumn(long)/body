{
  ExpandoColumn column=expandoColumnLocalService.fetchExpandoColumn(columnId);
  if (column != null) {
    ExpandoColumnPermissionUtil.check(getPermissionChecker(),column,ActionKeys.VIEW);
  }
  return column;
}

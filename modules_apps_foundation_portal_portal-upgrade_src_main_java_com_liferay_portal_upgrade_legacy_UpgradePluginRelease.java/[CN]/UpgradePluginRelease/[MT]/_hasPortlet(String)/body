{
  try (PreparedStatement ps=_connection.prepareStatement("select portletId from Portlet where portletId = ?")){
    ps.setString(1,portletId);
    try (ResultSet rs=ps.executeQuery()){
      if (rs.next()) {
        return true;
      }
      return false;
    }
   }
 }

{
  if (obj == null) {
    return false;
  }
  GroupRelPK pk=null;
  try {
    pk=(GroupRelPK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((groupId.equals(pk.groupId)) && (className.equals(pk.className)) && (classPK.equals(pk.classPK))) {
    return true;
  }
 else {
    return false;
  }
}

{
  if (!contains(permissionChecker,layout,name,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),AnnouncementsEntry.class.getName(),name,actionId);
  }
}

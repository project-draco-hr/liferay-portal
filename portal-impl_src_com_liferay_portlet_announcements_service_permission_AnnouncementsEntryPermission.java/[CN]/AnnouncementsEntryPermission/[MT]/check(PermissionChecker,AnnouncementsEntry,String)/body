{
  if (!contains(permissionChecker,entry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),AnnouncementsEntry.class.getName(),entry.getEntryId(),actionId);
  }
}

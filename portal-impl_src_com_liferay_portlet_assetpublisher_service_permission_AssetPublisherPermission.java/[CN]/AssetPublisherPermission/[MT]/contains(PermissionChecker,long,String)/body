{
  Boolean hasPermission=StagingPermissionUtil.hasPermission(permissionChecker,groupId,_CLASS_NAME,groupId,PortletKeys.ASSET_PUBLISHER,actionId);
  if (hasPermission != null) {
    return hasPermission.booleanValue();
  }
  return permissionChecker.hasPermission(groupId,_CLASS_NAME,groupId,actionId);
}

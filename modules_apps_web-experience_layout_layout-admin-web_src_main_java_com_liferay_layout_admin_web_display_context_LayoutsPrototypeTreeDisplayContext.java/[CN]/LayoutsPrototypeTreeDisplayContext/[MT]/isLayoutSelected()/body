{
  Layout layout=getLayout();
  if ((layout.getPlid() == themeDisplay.getPlid()) || Validator.equals(LayoutAdminPortletKeys.LAYOUT_PROTOTYPE_PAGE,themeDisplay.getPpid())) {
    return true;
  }
  return false;
}

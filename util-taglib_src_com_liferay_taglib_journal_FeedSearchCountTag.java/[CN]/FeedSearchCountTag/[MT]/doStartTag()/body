{
  try {
    int count=JournalFeedLocalServiceUtil.searchCount(_companyId,_groupId,_feedId,_name,_description,_andOperator);
    pageContext.setAttribute(_var,count);
    return SKIP_BODY;
  }
 catch (  Exception e) {
    throw new JspException(e);
  }
}

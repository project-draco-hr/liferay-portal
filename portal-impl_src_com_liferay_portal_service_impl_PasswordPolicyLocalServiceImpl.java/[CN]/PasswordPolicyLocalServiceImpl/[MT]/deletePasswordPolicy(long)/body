{
  PasswordPolicy passwordPolicy=passwordPolicyPersistence.findByPrimaryKey(passwordPolicyId);
  if (passwordPolicy.isDefaultPolicy()) {
    throw new RequiredPasswordPolicyException();
  }
  resourceLocalService.deleteResource(passwordPolicy.getCompanyId(),PasswordPolicy.class.getName(),ResourceImpl.SCOPE_INDIVIDUAL,passwordPolicy.getPasswordPolicyId());
  passwordPolicyPersistence.remove(passwordPolicyId);
}

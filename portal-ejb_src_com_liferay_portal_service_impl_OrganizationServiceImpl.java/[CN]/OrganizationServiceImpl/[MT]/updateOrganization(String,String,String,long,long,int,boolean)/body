{
  checkPermission(organizationId,ActionKeys.UPDATE);
  return OrganizationLocalServiceUtil.updateOrganization(getUser().getCompanyId(),organizationId,parentOrganizationId,name,regionId,countryId,statusId,location);
}

{
  DDMListEntry ddmListEntry=findByPrimaryKey(listEntryId);
  Session session=null;
  try {
    session=openSession();
    DDMListEntry[] array=new DDMListEntryImpl[3];
    array[0]=getByUuid_PrevAndNext(session,ddmListEntry,uuid,orderByComparator,true);
    array[1]=ddmListEntry;
    array[2]=getByUuid_PrevAndNext(session,ddmListEntry,uuid,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

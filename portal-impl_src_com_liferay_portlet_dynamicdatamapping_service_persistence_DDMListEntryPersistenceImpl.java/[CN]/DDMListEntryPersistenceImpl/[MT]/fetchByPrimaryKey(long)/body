{
  DDMListEntry ddmListEntry=(DDMListEntry)EntityCacheUtil.getResult(DDMListEntryModelImpl.ENTITY_CACHE_ENABLED,DDMListEntryImpl.class,listEntryId,this);
  if (ddmListEntry == null) {
    Session session=null;
    try {
      session=openSession();
      ddmListEntry=(DDMListEntry)session.get(DDMListEntryImpl.class,Long.valueOf(listEntryId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (ddmListEntry != null) {
        cacheResult(ddmListEntry);
      }
      closeSession(session);
    }
  }
  return ddmListEntry;
}

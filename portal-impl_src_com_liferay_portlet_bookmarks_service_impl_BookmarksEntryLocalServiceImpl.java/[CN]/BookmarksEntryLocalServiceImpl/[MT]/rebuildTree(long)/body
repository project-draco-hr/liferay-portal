{
  List<BookmarksEntry> entries=bookmarksEntryPersistence.findByCompanyId(companyId);
  for (  BookmarksEntry entry : entries) {
    entry.setTreePath(entry.buildTreePath());
    bookmarksEntryPersistence.update(entry);
  }
}

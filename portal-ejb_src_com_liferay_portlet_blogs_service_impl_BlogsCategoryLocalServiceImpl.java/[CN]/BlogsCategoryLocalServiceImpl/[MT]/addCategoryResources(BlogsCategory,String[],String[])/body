{
  ResourceLocalServiceUtil.addModelResources(category.getCompanyId(),0,category.getUserId(),BlogsCategory.class.getName(),category.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

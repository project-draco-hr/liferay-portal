{
  try {
    int count=SyncDLObjectLocalServiceUtil.getSyncDLObjectsCount();
    if (count == 0) {
      return;
    }
    super.upgrade();
  }
 catch (  Exception e) {
    throw new UpgradeException(e);
  }
}

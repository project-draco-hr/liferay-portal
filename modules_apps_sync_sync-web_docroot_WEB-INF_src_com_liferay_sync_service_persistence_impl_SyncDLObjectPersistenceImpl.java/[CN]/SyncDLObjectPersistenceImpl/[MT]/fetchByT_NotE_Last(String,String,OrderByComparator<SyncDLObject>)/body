{
  int count=countByT_NotE(treePath,event);
  if (count == 0) {
    return null;
  }
  List<SyncDLObject> list=findByT_NotE(treePath,event,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

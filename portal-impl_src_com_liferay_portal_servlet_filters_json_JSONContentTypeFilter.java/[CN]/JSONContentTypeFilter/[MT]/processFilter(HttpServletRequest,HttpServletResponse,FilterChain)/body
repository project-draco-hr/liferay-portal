{
  if (!BrowserSnifferUtil.isIe(request)) {
    processFilter(JSONContentTypeFilter.class,request,response,filterChain);
  }
 else {
    processFilter(JSONContentTypeFilter.class,request,new JSONContentTypeServletResponse(response),filterChain);
  }
}

{
  try {
    DLFileVersion dlFileVersion=DLRepositoryLocalServiceUtil.getFileVersion(fileVersionId);
    initByRepositoryId(dlFileVersion.getGroupId());
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

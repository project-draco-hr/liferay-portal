{
  try {
    DLFileEntry dlFileEntry=DLRepositoryLocalServiceUtil.getFileEntry(fileEntryId);
    initByRepositoryId(dlFileEntry.getGroupId());
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

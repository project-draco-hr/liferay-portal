{
  if (cause instanceof WorkflowDefinitionFileException || cause instanceof WorkflowException || cause instanceof RequiredWorkflowDefinitionException|| super.isSessionErrorException(cause)) {
    return true;
  }
  return false;
}

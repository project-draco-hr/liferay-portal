{
  StringPageContext stringPageContext=new StringPageContext(_pageContext);
  BreadcrumbTag breadcrumbTag=new BreadcrumbTag();
  breadcrumbTag.setPageContext(stringPageContext);
  return breadcrumbTag;
}

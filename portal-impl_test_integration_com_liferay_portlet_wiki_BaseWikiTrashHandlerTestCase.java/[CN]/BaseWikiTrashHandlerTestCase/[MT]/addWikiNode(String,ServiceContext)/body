{
  serviceContext.setWorkflowAction(WorkflowConstants.ACTION_SAVE_DRAFT);
  WikiNode wikiNode=WikiNodeLocalServiceUtil.addNode(getUserId(),name,ServiceTestUtil.randomString(),serviceContext);
  return wikiNode;
}

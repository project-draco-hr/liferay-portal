{
  Website website=fetchByPrimaryKey(websiteId);
  if (website == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + websiteId);
    }
    throw new NoSuchWebsiteException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + websiteId);
  }
  return website;
}

{
  BookmarksFolder folder=getBookmarksFolder(classPK);
  if ((folder.getParentFolderId() > 0) && (BookmarksFolderLocalServiceUtil.fetchBookmarksFolder(folder.getParentFolderId()) == null)) {
    return false;
  }
  return !folder.isInTrashContainer();
}

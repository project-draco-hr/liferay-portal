{
  List<String> labels=new ArrayList<>();
  List<FormNavigatorEntry> formNavigatorEntries=getFormNavigatorEntries(formNavigatorId,categoryKey,user,formModelBean);
  for (  FormNavigatorEntry formNavigatorEntry : formNavigatorEntries) {
    String label=formNavigatorEntry.getLabel();
    if (Validator.isNotNull(label)) {
      labels.add(label);
    }
  }
  return labels.toArray(new String[labels.size()]);
}

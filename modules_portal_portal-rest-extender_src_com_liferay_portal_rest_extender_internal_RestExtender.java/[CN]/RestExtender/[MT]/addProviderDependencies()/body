{
  RestExtenderConfiguration soapExtenderConfiguration=getRestExtenderConfiguration();
  String[] providerFilters=soapExtenderConfiguration.jaxRsProviderFilterStrings();
  if (providerFilters != null) {
    for (    String providerFilter : providerFilters) {
      addTCCLDependency(false,null,providerFilter,"addProvider","removeProvider");
    }
  }
}

{
  Group group=fetchByPrimaryKey(pk);
  if (group == null) {
    groupToUserGroupTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,userGroupPK);
  }
 else {
    groupToUserGroupTableMapper.addTableMapping(group.getCompanyId(),pk,userGroupPK);
  }
}

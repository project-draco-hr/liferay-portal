{
  int count=countByG_C_C_P(groupId,companyId,classNameId,parentGroupId);
  if (count == 0) {
    return null;
  }
  List<Group> list=findByG_C_C_P(groupId,companyId,classNameId,parentGroupId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

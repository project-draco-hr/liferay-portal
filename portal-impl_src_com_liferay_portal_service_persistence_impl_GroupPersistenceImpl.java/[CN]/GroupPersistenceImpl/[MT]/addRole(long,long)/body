{
  Group group=fetchByPrimaryKey(pk);
  if (group == null) {
    groupToRoleTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,rolePK);
  }
 else {
    groupToRoleTableMapper.addTableMapping(group.getCompanyId(),pk,rolePK);
  }
}

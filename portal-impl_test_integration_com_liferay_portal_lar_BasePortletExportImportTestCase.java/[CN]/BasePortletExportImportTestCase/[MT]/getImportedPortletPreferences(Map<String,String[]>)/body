{
  String portletId=LayoutTestUtil.addPortletToLayout(TestPropsValues.getUserId(),this.layout,getPortletId(),"column-1",preferenceMap);
  doExportImportPortlet(portletId);
  return LayoutTestUtil.getPortletPreferences(importedLayout.getCompanyId(),importedLayout.getPlid(),portletId);
}

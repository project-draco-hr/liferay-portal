{
  URL url=_classLoader.getResource(fileName);
  Configuration conf=addPropertiesSource(fileName,url,_baseConfiguration);
  if ((conf != null) && (!conf.isEmpty())) {
    _baseConfigurationLoaded=true;
  }
}

{
  BlogsStatsUser newBlogsStatsUser=addBlogsStatsUser();
  _persistence.clearCache();
  BlogsStatsUser existingBlogsStatsUser=_persistence.findByPrimaryKey(newBlogsStatsUser.getPrimaryKey());
  Assert.assertEquals(existingBlogsStatsUser.getGroupId(),ReflectionTestUtil.invoke(existingBlogsStatsUser,"getOriginalGroupId",new Class<?>[0]));
  Assert.assertEquals(existingBlogsStatsUser.getUserId(),ReflectionTestUtil.invoke(existingBlogsStatsUser,"getOriginalUserId",new Class<?>[0]));
}

{
  if (!_enabled) {
    return null;
  }
  TestClusterChannel.clearAllMessages();
  MockClusterExecutor newMockClusterExecutor=new MockClusterExecutor(true);
  List<Serializable> messages=TestClusterChannel.getMulticastMessages();
  handleReceivedClusterRequest((ClusterRequest)messages.get(0));
  return newMockClusterExecutor.getLocalClusterNodeId();
}

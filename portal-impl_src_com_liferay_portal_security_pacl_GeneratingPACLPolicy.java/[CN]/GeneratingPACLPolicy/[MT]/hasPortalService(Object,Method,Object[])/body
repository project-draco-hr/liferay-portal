{
  PortalServiceChecker portalServiceChecker=getPortalServiceChecker();
  if (!portalServiceChecker.hasService(object,method,arguments)) {
    AuthorizationProperty authorizationProperty=portalServiceChecker.generateAuthorizationProperty(object,method,arguments);
    trackAuthorizationProperty(authorizationProperty);
  }
  return true;
}

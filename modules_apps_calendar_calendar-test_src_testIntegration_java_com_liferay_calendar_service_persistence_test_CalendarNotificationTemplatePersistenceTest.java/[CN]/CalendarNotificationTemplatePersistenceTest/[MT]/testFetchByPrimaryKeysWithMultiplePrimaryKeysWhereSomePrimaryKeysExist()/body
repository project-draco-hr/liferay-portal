{
  CalendarNotificationTemplate newCalendarNotificationTemplate=addCalendarNotificationTemplate();
  long pk=RandomTestUtil.nextLong();
  Set<Serializable> primaryKeys=new HashSet<Serializable>();
  primaryKeys.add(newCalendarNotificationTemplate.getPrimaryKey());
  primaryKeys.add(pk);
  Map<Serializable,CalendarNotificationTemplate> calendarNotificationTemplates=_persistence.fetchByPrimaryKeys(primaryKeys);
  Assert.assertEquals(1,calendarNotificationTemplates.size());
  Assert.assertEquals(newCalendarNotificationTemplate,calendarNotificationTemplates.get(newCalendarNotificationTemplate.getPrimaryKey()));
}

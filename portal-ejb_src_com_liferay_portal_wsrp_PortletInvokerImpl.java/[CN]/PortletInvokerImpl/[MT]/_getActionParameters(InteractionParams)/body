{
  Map actionParams=_getParameters(interactionParams.getInteractionState());
  Map formParams=_getFormParameters(interactionParams.getFormParameters());
  actionParams.putAll(formParams);
  return actionParams;
}

{
  Document document=new DocumentImpl();
  document.addUID(CLASS_NAME,dlFolder.getFolderId());
  SearchEngineUtil.deleteDocument(getSearchEngineId(),dlFolder.getCompanyId(),document.get(Field.UID),isCommitImmediately());
}

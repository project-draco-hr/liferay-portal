{
  Company newCompany=addCompany();
  _persistence.clearCache();
  Company existingCompany=_persistence.findByPrimaryKey(newCompany.getPrimaryKey());
  Assert.assertTrue(Objects.equals(existingCompany.getWebId(),ReflectionTestUtil.invoke(existingCompany,"getOriginalWebId",new Class<?>[0])));
  Assert.assertTrue(Objects.equals(existingCompany.getMx(),ReflectionTestUtil.invoke(existingCompany,"getOriginalMx",new Class<?>[0])));
  Assert.assertEquals(Long.valueOf(existingCompany.getLogoId()),ReflectionTestUtil.<Long>invoke(existingCompany,"getOriginalLogoId",new Class<?>[0]));
}

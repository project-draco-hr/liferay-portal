{
  if (obj == null) {
    return false;
  }
  PortalPreferencesImpl portalPreferences=(PortalPreferencesImpl)obj;
  if (this == portalPreferences) {
    return true;
  }
  if ((getCompanyId() == portalPreferences.getCompanyId()) && (getOwnerId() == portalPreferences.getOwnerId()) && (getOwnerType() == portalPreferences.getOwnerType())&& (getMap().equals(portalPreferences.getMap()))) {
    return true;
  }
 else {
    return false;
  }
}

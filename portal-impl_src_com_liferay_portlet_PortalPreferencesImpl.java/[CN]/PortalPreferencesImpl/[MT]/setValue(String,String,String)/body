{
  if (Validator.isNull(key) || key.equals(_RANDOM_KEY)) {
    return;
  }
  key=_encodeKey(namespace,key);
  try {
    if (value != null) {
      super.setValue(key,value);
    }
 else {
      reset(key);
    }
    if (_signedIn) {
      store();
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

{
  if (isReadOnly(key)) {
    throw new ReadOnlyException(key);
  }
  Map<String,Preference> modifiedPreferences=getModifiedPreferences(false);
  if (modifiedPreferences != null) {
    modifiedPreferences.remove(key);
  }
}

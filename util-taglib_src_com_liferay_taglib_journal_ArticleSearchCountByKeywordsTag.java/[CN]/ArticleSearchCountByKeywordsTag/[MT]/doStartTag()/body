{
  int count;
  try {
    count=JournalArticleLocalServiceUtil.searchCount(_companyId,_groupId,_keywords,_version,_type,_structureId,_templateId,_displayDateGT,_displayDateLT,_approved,_expired,_reviewDate);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,count);
  return SKIP_BODY;
}

{
  Organization organization=OrganizationUtil.findByPrimaryKey(organizationId);
  if (!organization.isLocation()) {
    OrganizationPermissionUtil.check(getPermissionChecker(),organizationId,actionId);
  }
 else {
    LocationPermissionUtil.check(getPermissionChecker(),organizationId,actionId);
  }
}

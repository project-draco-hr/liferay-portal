{
  int count=countByResourceBlockId(resourceBlockId);
  List<BookmarksEntry> list=findByResourceBlockId(resourceBlockId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

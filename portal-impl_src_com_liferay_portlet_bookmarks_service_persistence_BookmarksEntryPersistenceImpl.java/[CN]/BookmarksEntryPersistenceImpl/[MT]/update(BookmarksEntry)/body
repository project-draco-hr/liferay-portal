{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BookmarksEntry bookmarksEntry) method. To improve performance, call update(BookmarksEntry bookmarksEntry, boolean merge).");
  }
  return update(bookmarksEntry,false);
}

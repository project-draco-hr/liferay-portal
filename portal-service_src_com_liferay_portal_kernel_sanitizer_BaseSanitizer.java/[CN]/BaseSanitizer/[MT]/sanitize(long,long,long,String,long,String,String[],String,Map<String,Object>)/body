{
  ByteArrayOutputStream byteArrayOutputStream=new ByteArrayOutputStream();
  sanitize(companyId,groupId,userId,className,classPK,contentType,modes,new ByteArrayInputStream(s.getBytes()),byteArrayOutputStream,options);
  return byteArrayOutputStream.toString();
}

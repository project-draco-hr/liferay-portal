{
  if ((isIE(request) && majorVersion(request) >= 5.5) || (isMozilla(request) && majorVersion(request) >= 1.3) || (isSafari(request) && majorVersion(request) >= 3.0 && !isMobile(request))) {
    return true;
  }
 else {
    return false;
  }
}

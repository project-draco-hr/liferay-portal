{
  String userAgent=getUserAgent(request);
  if ((userAgent.indexOf("windows") != -1) || (userAgent.indexOf("win32") != -1) || (userAgent.indexOf("16bit") != -1)) {
    return true;
  }
 else {
    return false;
  }
}

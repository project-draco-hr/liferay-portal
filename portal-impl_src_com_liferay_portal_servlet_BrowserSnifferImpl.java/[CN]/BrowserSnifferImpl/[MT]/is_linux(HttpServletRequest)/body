{
  String agent=req.getHeader(HttpHeaders.USER_AGENT);
  if (agent == null) {
    return false;
  }
  agent=agent.toLowerCase();
  if (agent.matches(".*linux.*")) {
    return true;
  }
 else {
    return false;
  }
}

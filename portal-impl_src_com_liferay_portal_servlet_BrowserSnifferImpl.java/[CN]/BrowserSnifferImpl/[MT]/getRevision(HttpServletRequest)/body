{
  String revision=(String)request.getAttribute(_BROWSER_SNIFFER_REVISION);
  if (revision != null) {
    return revision;
  }
  revision=parseVersion(getUserAgent(request),_revisionLeadings,_revisionSeparators);
  request.setAttribute(_BROWSER_SNIFFER_REVISION,revision);
  return revision;
}

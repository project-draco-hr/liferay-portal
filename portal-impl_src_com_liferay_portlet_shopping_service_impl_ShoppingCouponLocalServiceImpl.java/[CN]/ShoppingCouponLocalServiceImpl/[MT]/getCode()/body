{
  String code=PwdGenerator.getPassword(PwdGenerator.KEY1 + PwdGenerator.KEY2,8);
  try {
    ShoppingCoupon shoppingCoupon=shoppingCouponPersistence.findByCode(code);
    return shoppingCoupon.getCode();
  }
 catch (  NoSuchCouponException nsce) {
    return code;
  }
}

{
  String code=PwdGenerator.getPassword(PwdGenerator.KEY1 + PwdGenerator.KEY2,8);
  ShoppingCoupon coupon=shoppingCouponPersistence.fetchByCode(code);
  if (coupon != null) {
    return coupon.getCode();
  }
  return code;
}

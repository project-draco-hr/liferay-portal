{
  ComponentProperties properties=getAvailableProperties();
  if (!properties.hasBaseConfiguration()) {
    throw new ConfigurationNotFoundException(_componentName,"The base properties file was not found");
  }
  return properties;
}

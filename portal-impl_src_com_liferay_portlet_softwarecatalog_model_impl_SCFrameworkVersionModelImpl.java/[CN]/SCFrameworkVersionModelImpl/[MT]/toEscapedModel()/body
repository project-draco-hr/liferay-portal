{
  if (isEscapedModel()) {
    return (SCFrameworkVersion)this;
  }
 else {
    return (SCFrameworkVersion)Proxy.newProxyInstance(SCFrameworkVersion.class.getClassLoader(),new Class[]{SCFrameworkVersion.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (SCFrameworkVersion)this;
  }
 else {
    return (SCFrameworkVersion)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

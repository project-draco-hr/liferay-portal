{
  PortletPermissionUtil.check(getPermissionChecker(),SACPPortletKeys.PORTLET_ID,SACPActionKeys.ACTION_ADD_SACP_ENTRY);
  return sacpEntryLocalService.addSACPEntry(getUserId(),allowedServices,name,titleMap,serviceContext);
}

{
  Session session=null;
  try {
    session=openSession();
    return (Subscription)session.get(SubscriptionImpl.class,new Long(subscriptionId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Properties userMappings=getUserMappings(companyId);
  UserLocalServiceUtil.clearUserGroupUsers(userGroupId);
  for (int i=0; i < attr.size(); i++) {
    String userDN=(String)attr.get(i);
    Attributes userAttrs=ctx.getAttributes(userDN);
    String emailAddress=LDAPUtil.getAttributeValue(userAttrs,userMappings.getProperty("emailAddress"));
    User user=null;
    try {
      user=UserLocalServiceUtil.getUserByEmailAddress(companyId,emailAddress);
    }
 catch (    NoSuchUserException nsue) {
      user=importLDAPUser(companyId,ctx,userAttrs,false);
    }
    if (user != null) {
      if (_log.isDebugEnabled()) {
        _log.debug("Adding " + user.getUserId() + " to group "+ userGroupId);
      }
      UserLocalServiceUtil.addUserGroupUsers(userGroupId,new long[]{user.getUserId()});
    }
  }
}

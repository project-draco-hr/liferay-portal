{
  Properties groupMappings=getGroupMappings(companyId);
  String[] mappedGroupAttributeIds={groupMappings.getProperty("groupName"),groupMappings.getProperty("description")};
  return getAttributes(ctx,fullDistinguishedName,mappedGroupAttributeIds);
}

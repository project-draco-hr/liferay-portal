{
  WorkflowDefinitionLink workflowDefinitionLink=fetchByPrimaryKey(workflowDefinitionLinkId);
  if (workflowDefinitionLink == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No WorkflowDefinitionLink exists with the primary key " + workflowDefinitionLinkId);
    }
    throw new NoSuchWorkflowDefinitionLinkException("No WorkflowDefinitionLink exists with the primary key " + workflowDefinitionLinkId);
  }
  return workflowDefinitionLink;
}

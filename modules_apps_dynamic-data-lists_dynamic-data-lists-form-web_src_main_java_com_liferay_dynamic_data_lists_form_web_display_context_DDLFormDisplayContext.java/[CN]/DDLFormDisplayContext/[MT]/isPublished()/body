{
  DDLRecordSet ddlRecordSet=getRecordSet();
  if (ddlRecordSet == null) {
    return false;
  }
  String published=ddlRecordSet.getSettingsProperty("published",Boolean.FALSE.toString());
  return GetterUtil.getBoolean(published);
}

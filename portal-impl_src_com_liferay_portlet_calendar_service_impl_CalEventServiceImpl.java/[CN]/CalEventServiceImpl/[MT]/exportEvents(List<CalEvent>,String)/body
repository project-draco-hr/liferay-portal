{
  for (  CalEvent event : events) {
    CalEventPermission.check(getPermissionChecker(),event.getEventId(),ActionKeys.VIEW);
  }
  return calEventLocalService.exportEvents(getGuestOrUserId(),events,fileName);
}

{
  if (_allPermittedNodes == null) {
    _allPermittedNodes=WikiUtil.getNodes(getAllNodes(),_wikiPortletInstanceSettings.hiddenNodes(),_wikiRequestHelper.getPermissionChecker());
  }
  return _allPermittedNodes;
}

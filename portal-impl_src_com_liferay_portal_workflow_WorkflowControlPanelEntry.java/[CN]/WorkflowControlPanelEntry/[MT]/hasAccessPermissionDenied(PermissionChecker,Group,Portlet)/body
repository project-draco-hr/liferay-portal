{
  if (group.isLayoutPrototype() || group.isLayoutSetPrototype() || !WorkflowEngineManagerUtil.isDeployed()) {
    return true;
  }
  return super.hasAccessPermissionDenied(permissionChecker,group,portlet);
}

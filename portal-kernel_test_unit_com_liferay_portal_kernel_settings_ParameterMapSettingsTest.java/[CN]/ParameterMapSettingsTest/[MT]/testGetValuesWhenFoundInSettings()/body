{
  String[] values={"settingsValue1","settingsValue2"};
  _parentSettings.setValues("key",values);
  Assert.assertArrayEquals(values,_parameterMapSettings.getValues("key",new String[]{"defaultValue"}));
}

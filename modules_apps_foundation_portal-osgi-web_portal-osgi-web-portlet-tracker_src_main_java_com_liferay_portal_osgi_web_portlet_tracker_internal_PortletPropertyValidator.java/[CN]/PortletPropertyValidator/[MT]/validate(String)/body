{
  if (!StringUtil.startsWith(key,"com.liferay.portlet.") && !StringUtil.startsWith(key,"javax.portlet.")) {
    return true;
  }
  if (_validKeys.contains(key)) {
    return true;
  }
  return false;
}

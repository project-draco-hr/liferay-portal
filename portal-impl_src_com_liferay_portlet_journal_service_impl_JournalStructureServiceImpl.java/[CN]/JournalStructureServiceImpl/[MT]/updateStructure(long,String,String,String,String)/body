{
  JournalStructurePermission.check(getPermissionChecker(),groupId,structureId,ActionKeys.UPDATE);
  return JournalStructureLocalServiceUtil.updateStructure(groupId,structureId,name,description,xsd);
}

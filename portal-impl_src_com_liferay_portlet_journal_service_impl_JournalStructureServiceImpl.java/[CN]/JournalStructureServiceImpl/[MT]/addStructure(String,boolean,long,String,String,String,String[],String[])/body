{
  PortletPermission.check(getPermissionChecker(),plid,PortletKeys.JOURNAL,ActionKeys.ADD_STRUCTURE);
  return JournalStructureLocalServiceUtil.addStructure(getUserId(),structureId,autoStructureId,plid,name,description,xsd,communityPermissions,guestPermissions);
}

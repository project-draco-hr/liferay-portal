{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.JOURNAL,ActionKeys.ADD_STRUCTURE);
  return journalStructureLocalService.addStructure(getUserId(),structureId,autoStructureId,plid,name,description,xsd,parentStructureId,communityPermissions,guestPermissions);
}

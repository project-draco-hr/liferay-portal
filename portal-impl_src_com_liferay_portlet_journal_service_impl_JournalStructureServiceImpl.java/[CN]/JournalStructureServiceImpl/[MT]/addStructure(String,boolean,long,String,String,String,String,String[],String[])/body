{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.JOURNAL,ActionKeys.ADD_STRUCTURE);
  return journalStructureLocalService.addStructure(getUserId(),structureId,autoStructureId,plid,parentStructureId,name,description,xsd,communityPermissions,guestPermissions);
}

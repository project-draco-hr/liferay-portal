{
  JournalStructurePermission.check(getPermissionChecker(),groupId,oldStructureId,ActionKeys.ADD_STRUCTURE);
  return journalStructureLocalService.copyStructure(getUserId(),groupId,oldStructureId,newStructureId,autoStructureId);
}

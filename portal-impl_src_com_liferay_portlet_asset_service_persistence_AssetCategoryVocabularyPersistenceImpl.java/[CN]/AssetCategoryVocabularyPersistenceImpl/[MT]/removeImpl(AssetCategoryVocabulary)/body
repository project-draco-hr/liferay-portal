{
  Session session=null;
  try {
    session=openSession();
    if (assetCategoryVocabulary.isCachedModel() || BatchSessionUtil.isEnabled()) {
      Object staleObject=session.get(AssetCategoryVocabularyImpl.class,assetCategoryVocabulary.getPrimaryKeyObj());
      if (staleObject != null) {
        session.evict(staleObject);
      }
    }
    session.delete(assetCategoryVocabulary);
    session.flush();
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST);
  AssetCategoryVocabularyModelImpl assetCategoryVocabularyModelImpl=(AssetCategoryVocabularyModelImpl)assetCategoryVocabulary;
  FinderCacheUtil.removeResult(FINDER_PATH_FETCH_BY_G_N,new Object[]{new Long(assetCategoryVocabularyModelImpl.getOriginalGroupId()),assetCategoryVocabularyModelImpl.getOriginalName()});
  EntityCacheUtil.removeResult(AssetCategoryVocabularyModelImpl.ENTITY_CACHE_ENABLED,AssetCategoryVocabularyImpl.class,assetCategoryVocabulary.getPrimaryKey());
  return assetCategoryVocabulary;
}

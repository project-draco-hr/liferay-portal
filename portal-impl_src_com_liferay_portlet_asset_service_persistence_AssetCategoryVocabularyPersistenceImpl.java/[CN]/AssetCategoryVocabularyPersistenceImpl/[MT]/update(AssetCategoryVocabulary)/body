{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetCategoryVocabulary assetCategoryVocabulary) method. Use update(AssetCategoryVocabulary assetCategoryVocabulary, boolean merge) instead.");
  }
  return update(assetCategoryVocabulary,false);
}

{
  Object[] actualRedirectArray=_friendlyURLServlet.getRedirect(request,path,mainPath,Collections.<String,String[]>emptyMap());
  Assert.assertArrayEquals(expectedRedirectArray,actualRedirectArray);
}

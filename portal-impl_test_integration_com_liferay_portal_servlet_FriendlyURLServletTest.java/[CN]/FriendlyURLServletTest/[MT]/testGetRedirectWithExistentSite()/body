{
  Group group=GroupTestUtil.addGroup();
  Layout layout=LayoutTestUtil.addLayout(group.getGroupId(),ServiceTestUtil.randomString());
  String[] pathAndURL=getPathAndURL(group,layout);
  doTestGetRedirect(pathAndURL[0],Portal.PATH_MAIN,new Object[]{pathAndURL[1],false});
}

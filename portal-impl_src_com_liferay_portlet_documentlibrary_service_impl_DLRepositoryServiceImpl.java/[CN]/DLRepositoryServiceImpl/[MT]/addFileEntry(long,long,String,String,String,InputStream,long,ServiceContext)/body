{
  DLFolderPermission.check(getPermissionChecker(),groupId,folderId,ActionKeys.ADD_DOCUMENT);
  return dlRepositoryLocalService.addFileEntry(getUserId(),groupId,folderId,title,description,changeLog,is,size,serviceContext);
}

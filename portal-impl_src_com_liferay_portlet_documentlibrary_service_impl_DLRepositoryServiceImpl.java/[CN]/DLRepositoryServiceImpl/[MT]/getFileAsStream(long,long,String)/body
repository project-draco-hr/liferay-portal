{
  DLFileEntryPermission.check(getPermissionChecker(),groupId,folderId,name,ActionKeys.VIEW);
  User user=getUser();
  return dlRepositoryLocalService.getFileAsStream(user.getCompanyId(),user.getUserId(),groupId,folderId,name);
}

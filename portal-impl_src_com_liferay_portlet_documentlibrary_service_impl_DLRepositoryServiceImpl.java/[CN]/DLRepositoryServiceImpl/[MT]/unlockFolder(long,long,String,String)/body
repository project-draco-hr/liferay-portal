{
  DLFolder dlFolder=getFolder(groupId,parentFolderId,name);
  DLFolderPermission.check(getPermissionChecker(),dlFolder,ActionKeys.UPDATE);
  unlockFolder(groupId,dlFolder.getFolderId(),lockUuid);
}

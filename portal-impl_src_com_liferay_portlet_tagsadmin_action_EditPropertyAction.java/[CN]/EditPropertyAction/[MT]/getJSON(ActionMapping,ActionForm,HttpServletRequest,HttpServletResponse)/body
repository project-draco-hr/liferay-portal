{
  String cmd=ParamUtil.getString(request,Constants.CMD);
  try {
    if (cmd.equals("addProperty")) {
      addProperty(request);
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return null;
}

{
  if (obj == null) {
    return -1;
  }
  CompanyImpl company=(CompanyImpl)obj;
  long pk=company.getPrimaryKey();
  if (getPrimaryKey() < pk) {
    return -1;
  }
 else   if (getPrimaryKey() > pk) {
    return 1;
  }
 else {
    return 0;
  }
}

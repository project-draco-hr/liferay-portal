{
  DDLRecord ddlRecord=fetchByPrimaryKey(recordId);
  if (ddlRecord == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + recordId);
    }
    throw new NoSuchRecordException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + recordId);
  }
  return ddlRecord;
}

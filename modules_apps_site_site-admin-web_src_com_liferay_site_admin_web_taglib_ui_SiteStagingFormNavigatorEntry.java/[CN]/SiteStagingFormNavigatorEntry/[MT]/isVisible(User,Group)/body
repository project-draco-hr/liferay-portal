{
  if (group == null) {
    return false;
  }
  if (group.hasStagingGroup() || (group.hasRemoteStagingGroup() && !PropsValues.STAGING_LIVE_GROUP_REMOTE_STAGING_ENABLED)) {
    return false;
  }
  return true;
}

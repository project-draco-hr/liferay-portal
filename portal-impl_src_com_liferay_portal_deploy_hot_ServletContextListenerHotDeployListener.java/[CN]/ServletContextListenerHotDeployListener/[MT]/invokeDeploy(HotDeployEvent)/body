{
  try {
    doInvokeDeploy(hotDeployEvent);
  }
 catch (  Throwable t) {
    throwHotDeployException(hotDeployEvent,"Error registering servlet context listeners for " + hotDeployEvent.getServletContextName(),t);
  }
}

{
  List<String> configLocations=ListUtil.fromArray(PropsUtil.getArray(PropsKeys.SPRING_CONFIGS));
  if (extraConfigLocations != null) {
    configLocations.addAll(extraConfigLocations);
  }
  loadContext(configLocations.toArray(new String[configLocations.size()]));
}

{
  if (_applicationContext == null) {
    System.out.println("Manually loading Spring context");
    _applicationContext=new ArrayApplicationContext(PropsUtil.getArray(PropsKeys.SPRING_CONFIGS));
  }
  return _applicationContext;
}

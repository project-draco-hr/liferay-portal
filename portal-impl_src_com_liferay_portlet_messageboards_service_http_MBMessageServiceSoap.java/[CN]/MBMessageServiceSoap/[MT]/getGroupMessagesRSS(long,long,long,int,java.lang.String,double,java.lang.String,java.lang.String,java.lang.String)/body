{
  try {
    java.lang.String returnValue=MBMessageServiceUtil.getGroupMessagesRSS(companyId,groupId,userId,max,type,version,displayStyle,feedURL,entryURL);
    return returnValue;
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

{
  super.init(req,res,portletName,layout.getCompanyId(),layout.getPlid());
  _req=req;
  _portletName=portletName;
  _user=user;
  _layout=layout;
  setWindowState(windowState);
  setPortletMode(portletMode);
  _calledSetRenderParameter=false;
}

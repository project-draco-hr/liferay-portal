{
  if (isEscapedModel()) {
    return (JournalArticleResource)this;
  }
 else {
    return (JournalArticleResource)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

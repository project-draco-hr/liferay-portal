{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  MBCategory newMBCategory=addMBCategory();
  _persistence.clearCache();
  MBCategoryModelImpl existingMBCategoryModelImpl=(MBCategoryModelImpl)_persistence.findByPrimaryKey(newMBCategory.getPrimaryKey());
  assertTrue(Validator.equals(existingMBCategoryModelImpl.getUuid(),existingMBCategoryModelImpl.getOriginalUuid()));
  assertEquals(existingMBCategoryModelImpl.getGroupId(),existingMBCategoryModelImpl.getOriginalGroupId());
}

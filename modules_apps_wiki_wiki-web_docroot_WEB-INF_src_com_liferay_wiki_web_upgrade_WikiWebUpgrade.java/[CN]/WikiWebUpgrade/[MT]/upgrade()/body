{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradePortletSettings(_settingsFactory));
  _releaseLocalService.updateRelease("com.liferay.wiki.web",upgradeProcesses,1,1,false);
}

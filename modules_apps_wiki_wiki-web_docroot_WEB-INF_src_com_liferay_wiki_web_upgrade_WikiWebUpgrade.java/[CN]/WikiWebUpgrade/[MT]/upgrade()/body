{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradePortletSettings());
  _releaseLocalService.updateRelease("com.liferay.wiki.web",upgradeProcesses,1,0,false);
}

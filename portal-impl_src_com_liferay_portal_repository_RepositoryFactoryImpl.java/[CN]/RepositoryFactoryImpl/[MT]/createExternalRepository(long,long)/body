{
  RepositoryConfiguration repositoryConfiguration=getRepositoryConfiguration(classNameId);
  RepositoryCreator repositoryCreator=repositoryConfiguration.getRepositoryCreator();
  return new CapabilityRepository(repositoryCreator.createRepository(repositoryId),repositoryConfiguration.getSupportedCapabilities(),repositoryConfiguration.getPublicCapabilities());
}

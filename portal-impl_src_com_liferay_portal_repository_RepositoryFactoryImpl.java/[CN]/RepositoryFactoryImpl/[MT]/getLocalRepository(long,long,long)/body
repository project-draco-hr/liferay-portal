{
  LocalRepository localRepository=new LiferayLocalRepository(folderId,fileEntryId,fileVersionId);
  long repositoryId=localRepository.getRepositoryId();
  checkRepository(repositoryId);
  return localRepository;
}

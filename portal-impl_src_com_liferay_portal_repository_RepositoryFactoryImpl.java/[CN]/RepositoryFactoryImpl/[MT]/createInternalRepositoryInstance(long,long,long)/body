{
  Repository repository=createLiferayInternalRepository(groupId,repositoryId,dlFolderId);
  return new CapabilityRepository(repository,getInternalSupportedCapabilities(),getInternalExportedCapabilityClasses());
}

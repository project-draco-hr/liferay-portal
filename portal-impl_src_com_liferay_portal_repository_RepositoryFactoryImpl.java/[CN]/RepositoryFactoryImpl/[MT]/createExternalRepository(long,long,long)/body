{
  long repositoryId=getRepositoryId(folderId,fileEntryId,fileVersionId);
  Repository repository=create(repositoryId);
  return new CapabilityRepository(repository,getExternalSupportedCapabilities(),getExternalExportedCapabilityClasses());
}

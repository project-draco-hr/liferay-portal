{
  if (hasCompoundPK()) {
    return false;
  }
 else {
    EntityColumn col=(EntityColumn)_pkList.get(0);
    if (col.isPrimitiveType()) {
      return true;
    }
 else {
      return false;
    }
  }
}

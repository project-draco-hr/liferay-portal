{
  if (hasPrimitivePK() && hasColumn("userId") && finder.hasColumn("groupId")&& ResourceActionsUtil.hasModelResourceActions(_packagePath + ".model." + _name)) {
    return true;
  }
 else {
    return false;
  }
}

{
  if (!finder.getName().equals("UUID_G") && hasPrimitivePK() && hasColumn("userId")&& finder.hasColumn("groupId")&& ResourceActionsUtil.hasModelResourceActions(_packagePath + ".model." + _name)) {
    return true;
  }
 else {
    return false;
  }
}

{
  if ((_blobList == null) || _blobList.isEmpty()) {
    return false;
  }
  for (  EntityColumn entityColumn : _blobList) {
    if (entityColumn.isLazy()) {
      return true;
    }
  }
  return false;
}

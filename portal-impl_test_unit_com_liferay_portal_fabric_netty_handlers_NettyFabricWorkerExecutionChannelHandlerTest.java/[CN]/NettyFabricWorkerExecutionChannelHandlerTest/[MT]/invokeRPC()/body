{
  RPCSerializable rpcSerializable=(RPCSerializable)_embeddedChannel.readOutbound();
  rpcSerializable.execute(_embeddedChannel);
  Queue<Object> messages=_embeddedChannel.outboundMessages();
  messages.clear();
}

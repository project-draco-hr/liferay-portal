{
  String[] urlsIncludes=StringUtil.split(authVerifierConfiguration.getProperties().getProperty("urls.includes"));
  if ((urlsIncludes.length == 0) && _log.isWarnEnabled()) {
    _log.warn("Auth verifier " + authVerifierConfiguration.getAuthVerifierClassName() + " does not have any URLs configured");
    return false;
  }
  return true;
}

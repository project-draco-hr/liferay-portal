{
  Registry registry=RegistryUtil.getRegistry();
  Filter authVerifierPipelineFilter=registry.getFilter("(&(objectClass=" + AuthVerifier.class.getName() + ")(urls.includes=*))");
  _serviceTracker=registry.trackServices(authVerifierPipelineFilter,new AuthVerifierTrackerCustomizer());
  _serviceTracker.open();
}

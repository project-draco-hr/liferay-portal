{
  Registry registry=RegistryUtil.getRegistry();
  _initAuthVerifierConfigurations(registry);
  _serviceTracker=registry.trackServices(AuthVerifier.class,new AuthVerifierTrackerCustomizer());
  _serviceTracker.open();
}

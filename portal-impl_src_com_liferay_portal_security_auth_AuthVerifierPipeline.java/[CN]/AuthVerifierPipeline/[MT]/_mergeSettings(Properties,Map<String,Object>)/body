{
  Map<String,Object> mergedSettings=new HashMap<String,Object>(settings);
  if (properties != null) {
    for (    String key : properties.stringPropertyNames()) {
      mergedSettings.put(key,properties.getProperty(key));
    }
  }
  return mergedSettings;
}

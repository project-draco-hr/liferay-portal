{
  Matcher matcher=validatorEqualsPattern.matcher(content);
  while (matcher.find()) {
    processErrorMessage(fileName,"Use Objects.equals(Object, Object) instead of " + "Validator.equals(Object, Object): " + fileName + " "+ getLineCount(content,matcher.start()));
  }
}

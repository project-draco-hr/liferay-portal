{
  Matcher matcher=_subnamePattern.matcher(content);
  while (matcher.find()) {
    processMessage(fileName,"'sub' should be followed by a lowercase character for '" + matcher.group(1) + "'");
  }
}

{
  Properties databaseProperties=new Properties();
  try {
    ShardExporterFactory.getShardExporter(databaseProperties);
  }
 catch (  IllegalArgumentException iae) {
    Assert.assertEquals("Database exporter not found",iae.getMessage());
  }
}

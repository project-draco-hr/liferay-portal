{
  if (_portletFilters.size() > _pos) {
    RenderFilter portletFilter=(RenderFilter)_portletFilters.get(_pos++);
    portletFilter.doFilter(renderRequest,renderResponse,this);
  }
 else {
    _portlet.render(renderRequest,renderResponse);
  }
}

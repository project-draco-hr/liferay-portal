{
  if (_portletFilters.size() > _pos) {
    ActionFilter portletFilter=(ActionFilter)_portletFilters.get(_pos++);
    portletFilter.doFilter(actionRequest,actionResponse,this);
  }
 else {
    _portlet.processAction(actionRequest,actionResponse);
  }
}

{
  String groupName=_buildGroupName(mailingList);
  List<SchedulerRequest> list=SchedulerEngineUtil.getScheduledJobs(groupName);
  SchedulerRequest request=null;
  if (list.size() == 1) {
    request=list.get(0);
  }
  return request;
}

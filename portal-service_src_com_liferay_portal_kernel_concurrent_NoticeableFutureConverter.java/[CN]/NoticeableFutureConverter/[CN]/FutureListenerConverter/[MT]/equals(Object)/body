{
  FutureListener<?> futureListener=(FutureListener<?>)obj;
  if (futureListener instanceof NoticeableFutureConverter.FutureListenerConverter) {
    futureListener=((FutureListenerConverter)futureListener)._futureListener;
  }
  return _futureListener.equals(futureListener);
}

{
  String key=_encodeKey(groupId);
  Map<String,List<CalEvent>> eventsPool=(Map<String,List<CalEvent>>)_cache.get(key);
  if (eventsPool == null) {
    eventsPool=new ConcurrentHashMap<String,List<CalEvent>>();
    _cache.put(key,eventsPool);
  }
  return eventsPool;
}

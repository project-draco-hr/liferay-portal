{
  if (smallImage) {
    if ((smallFile != null) && (smallBytes != null)) {
      _imageLocalService.updateImage(smallImageId,smallBytes);
    }
  }
 else {
    _imageLocalService.deleteImage(smallImageId);
  }
}

{
  IGFolder srcFolder=getFolder(sourceFolderId);
  IGFolder destFolder=addFolder(parentFolderId,name,description,serviceContext);
  copyFolder(srcFolder,destFolder,serviceContext);
  return destFolder;
}

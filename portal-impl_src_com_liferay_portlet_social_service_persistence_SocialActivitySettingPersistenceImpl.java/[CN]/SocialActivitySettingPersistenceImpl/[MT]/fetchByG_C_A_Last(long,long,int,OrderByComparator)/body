{
  int count=countByG_C_A(groupId,classNameId,activityType);
  List<SocialActivitySetting> list=findByG_C_A(groupId,classNameId,activityType,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

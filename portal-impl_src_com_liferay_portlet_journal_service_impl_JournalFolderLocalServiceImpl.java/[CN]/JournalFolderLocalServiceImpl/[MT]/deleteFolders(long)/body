{
  List<JournalFolder> folders=journalFolderPersistence.findByG_P(groupId,JournalFolderConstants.DEFAULT_PARENT_FOLDER_ID);
  for (  JournalFolder folder : folders) {
    deleteFolder(folder);
  }
}

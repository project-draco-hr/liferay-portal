{
  List<JournalFolder> folders=journalFolderPersistence.findByCompanyId(companyId);
  for (  JournalFolder folder : folders) {
    folder.setTreePath(folder.buildTreePath());
    journalFolderPersistence.update(folder);
  }
}

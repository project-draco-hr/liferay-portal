{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof DDMStorageLinkWrapper)) {
    return false;
  }
  DDMStorageLinkWrapper ddmStorageLinkWrapper=(DDMStorageLinkWrapper)obj;
  if (Validator.equals(_ddmStorageLink,ddmStorageLinkWrapper._ddmStorageLink)) {
    return true;
  }
  return false;
}

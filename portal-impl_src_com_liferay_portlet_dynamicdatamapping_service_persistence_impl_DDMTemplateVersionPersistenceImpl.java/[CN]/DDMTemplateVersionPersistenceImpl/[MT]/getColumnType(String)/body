{
  Integer type=DDMTemplateVersionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DDMTemplateVersionModelImpl.TABLE_NAME);
  }
  return type;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBDiscussion mbDiscussion) method. To improve performance, call update(MBDiscussion mbDiscussion, boolean merge).");
  }
  return update(mbDiscussion,false);
}

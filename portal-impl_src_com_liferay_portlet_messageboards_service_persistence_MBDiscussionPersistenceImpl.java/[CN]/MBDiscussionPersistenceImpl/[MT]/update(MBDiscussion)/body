{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBDiscussion mbDiscussion) method. Use update(MBDiscussion mbDiscussion, boolean merge) instead.");
  }
  return update(mbDiscussion,false);
}

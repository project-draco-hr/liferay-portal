{
  Locale[] contentLocales=LocaleUtil.fromLanguageIds(LocalizationUtil.getAvailableLanguageIds(content));
  Fields fields=DDMXMLUtil.getFields(ddmStructure,content);
  validateDDMStructureFields(ddmStructure,classNameId,fields,contentLocales);
}

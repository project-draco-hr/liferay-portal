{
  UpgradeStep upgradeStep=new TestUpgradeStep();
  UpgradeInfo upgradeInfo1=new UpgradeInfo("1.0.0","2.0.0",upgradeStep);
  UpgradeInfo upgradeInfo2=new UpgradeInfo("1.0.0","2.0.0",upgradeStep);
  Assert.assertTrue(upgradeInfo1.equals(upgradeInfo2));
}

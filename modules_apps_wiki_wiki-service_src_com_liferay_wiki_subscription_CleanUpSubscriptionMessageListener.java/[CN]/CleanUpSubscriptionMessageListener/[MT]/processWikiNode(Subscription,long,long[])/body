{
  WikiNode wikiNode=_wiWikiNodeLocalService.fetchWikiNode(subscription.getClassPK());
  if ((wikiNode != null) && ((wikiNode.getGroupId() == groupId) || !ArrayUtil.contains(groupIds,wikiNode.getGroupId()))) {
    SubscriptionLocalServiceUtil.deleteSubscription(subscription.getSubscriptionId());
  }
}

{
  _sessionId=sessionId;
  _className=className;
  _isMaintaining=true;
  _status=new StringBuffer();
  Collection<HttpSession> sessionPool=PortalSessionContext.getSessionPool();
  for (  HttpSession session : sessionPool) {
    session.invalidate();
  }
}

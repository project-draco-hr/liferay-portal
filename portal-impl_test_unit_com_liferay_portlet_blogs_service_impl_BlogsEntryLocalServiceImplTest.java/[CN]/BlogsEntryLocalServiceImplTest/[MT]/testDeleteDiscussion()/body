{
  long blogsEntryId=RandomTestUtil.randomLong();
  Mockito.when(_blogsEntry.getEntryId()).thenReturn(blogsEntryId);
  _blogsEntryLocalServiceImpl.deleteDiscussion(_blogsEntry);
  Mockito.verify(_commentManager).deleteDiscussion(BlogsEntry.class.getName(),blogsEntryId);
}

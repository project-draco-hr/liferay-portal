{
  long entryId=RandomTestUtil.randomLong();
  Mockito.when(_blogsEntry.getEntryId()).thenReturn(entryId);
  _blogsEntryLocalServiceImpl.deleteDiscussion(_blogsEntry);
  Mockito.verify(_mbMessageLocalService).deleteDiscussionMessages(BlogsEntry.class.getName(),entryId);
}

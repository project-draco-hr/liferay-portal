{
  Document document=new DocumentImpl();
  document.addUID(getClassName(),field1,field2);
  SearchEngineHelperUtil.deleteDocument(getSearchEngineId(),companyId,document.get(Field.UID),_commitImmediately);
}

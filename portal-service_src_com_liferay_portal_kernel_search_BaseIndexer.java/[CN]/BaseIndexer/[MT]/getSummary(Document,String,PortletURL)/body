{
  Summary summary=doGetSummary(document,snippet,portletURL);
  for (  IndexerPostProcessor indexerPostProcessor : _indexerPostProcessors) {
    indexerPostProcessor.postProcessSummary(summary,document,snippet,portletURL);
  }
  return summary;
}

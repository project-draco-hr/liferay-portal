{
  String emailBlogsAddedSubject=preferences.getValue("email-blogs-added-subject",StringPool.BLANK);
  if (Validator.isNotNull(emailBlogsAddedSubject)) {
    return emailBlogsAddedSubject;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsKeys.BLOGS_EMAIL_ENTRY_ADDED_SUBJECT));
  }
}

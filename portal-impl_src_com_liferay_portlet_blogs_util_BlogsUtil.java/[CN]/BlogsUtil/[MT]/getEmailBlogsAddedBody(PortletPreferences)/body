{
  String emailBlogsAddedBody=preferences.getValue("email-blogs-added-body",StringPool.BLANK);
  if (Validator.isNotNull(emailBlogsAddedBody)) {
    return emailBlogsAddedBody;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsKeys.BLOGS_EMAIL_ENTRY_ADDED_BODY));
  }
}

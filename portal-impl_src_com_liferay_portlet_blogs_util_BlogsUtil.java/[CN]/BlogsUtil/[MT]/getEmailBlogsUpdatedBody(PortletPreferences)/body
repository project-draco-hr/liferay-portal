{
  String emailBlogsUpdatedBody=preferences.getValue("email-blogs-updated-body",StringPool.BLANK);
  if (Validator.isNotNull(emailBlogsUpdatedBody)) {
    return emailBlogsUpdatedBody;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsKeys.BLOGS_EMAIL_ENTRY_UPDATED_BODY));
  }
}

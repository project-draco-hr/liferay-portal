{
  Settings settings=SettingsFactoryUtil.getPortletInstanceSettings(layout,portletId);
  Settings parameterMapSettings=new ParameterMapSettings(request.getParameterMap(),settings);
  return new BlogsPortletInstanceSettings(parameterMapSettings);
}

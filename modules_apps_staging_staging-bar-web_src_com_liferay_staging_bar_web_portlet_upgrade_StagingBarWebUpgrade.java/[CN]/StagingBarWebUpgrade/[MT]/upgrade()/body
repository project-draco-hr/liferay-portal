{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradeStagingBarPortletId());
  _releaseLocalService.updateRelease("com.liferay.staging.bar.web",upgradeProcesses,1,1,false);
}

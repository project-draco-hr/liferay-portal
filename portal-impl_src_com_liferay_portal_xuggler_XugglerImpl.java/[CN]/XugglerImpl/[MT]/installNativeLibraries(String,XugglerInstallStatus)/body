{
  try {
    String url=PropsValues.XUGGLER_JAR_URL + name;
    JarUtil.downloadAndInstallJar(false,url,name,xugglerInstallStatus);
  }
 catch (  Exception e) {
    _log.error("Installation of jar " + name + " failed",e);
    throw e;
  }
}

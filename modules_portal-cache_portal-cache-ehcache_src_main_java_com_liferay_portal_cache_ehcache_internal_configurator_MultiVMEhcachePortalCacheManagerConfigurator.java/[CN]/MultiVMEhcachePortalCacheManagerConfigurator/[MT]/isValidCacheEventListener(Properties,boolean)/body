{
  if (Boolean.valueOf(properties.getProperty(PortalCacheReplicator.REPLICATOR))) {
    if (_clusterEnabled) {
      return true;
    }
    return false;
  }
  return super.isValidCacheEventListener(properties,usingDefault);
}

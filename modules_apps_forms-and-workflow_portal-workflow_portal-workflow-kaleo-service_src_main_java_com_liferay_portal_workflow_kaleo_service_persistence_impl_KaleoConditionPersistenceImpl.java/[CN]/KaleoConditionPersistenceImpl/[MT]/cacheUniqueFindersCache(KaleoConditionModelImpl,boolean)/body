{
  if (isNew) {
    Object[] args=new Object[]{kaleoConditionModelImpl.getKaleoNodeId()};
    finderCache.putResult(FINDER_PATH_COUNT_BY_KALEONODEID,args,Long.valueOf(1));
    finderCache.putResult(FINDER_PATH_FETCH_BY_KALEONODEID,args,kaleoConditionModelImpl);
  }
 else {
    if ((kaleoConditionModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_KALEONODEID.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{kaleoConditionModelImpl.getKaleoNodeId()};
      finderCache.putResult(FINDER_PATH_COUNT_BY_KALEONODEID,args,Long.valueOf(1));
      finderCache.putResult(FINDER_PATH_FETCH_BY_KALEONODEID,args,kaleoConditionModelImpl);
    }
  }
}

{
  List<Callable<?>> callbackList=TransactionCommitCallbackUtil.popCallbackList();
  for (  Callable<?> callable : callbackList) {
    try {
      callable.call();
    }
 catch (    Exception e) {
      _log.error("Failed to execute transaction commit callback",e);
    }
  }
}

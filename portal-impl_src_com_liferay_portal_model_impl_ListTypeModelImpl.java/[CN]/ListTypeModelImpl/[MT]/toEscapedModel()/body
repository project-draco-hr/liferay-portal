{
  if (isEscapedModel()) {
    return (ListType)this;
  }
 else {
    return (ListType)Proxy.newProxyInstance(ListType.class.getClassLoader(),new Class[]{ListType.class},new AutoEscapeBeanHandler(this));
  }
}

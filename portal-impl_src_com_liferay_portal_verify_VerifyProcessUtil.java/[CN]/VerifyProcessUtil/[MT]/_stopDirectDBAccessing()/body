{
  PropsValues.SPRING_HIBERNATE_SESSION_DELEGATED=GetterUtil.getBoolean(PropsUtil.get(PropsKeys.SPRING_HIBERNATE_SESSION_DELEGATED));
  try {
    Field annotationsField=ReflectionUtil.getDeclaredField(ServiceBeanAopCacheManager.class,"_annotations");
    annotationsField.set(null,new ConcurrentHashMap<MethodInvocation,Annotation[]>());
  }
 catch (  Exception e) {
    throw new VerifyException(e);
  }
}

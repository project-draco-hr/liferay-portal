{
  IndexValidator indexValidator=new IndexValidator();
  indexValidator.setIncludeTargetPlatform(true);
  indexValidator.setModuleFrameworkBaseDirName(PropsValues.MODULE_FRAMEWORK_BASE_DIR);
  return indexValidator;
}

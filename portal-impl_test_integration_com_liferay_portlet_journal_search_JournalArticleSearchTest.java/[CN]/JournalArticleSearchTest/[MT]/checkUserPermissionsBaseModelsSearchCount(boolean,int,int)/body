{
  if (addBaseModelPermission && !PropsValues.JOURNAL_ARTICLE_VIEW_PERMISSION_CHECK_ENABLED) {
    initialBaseModelsSearchCount++;
  }
  Assert.assertEquals(initialBaseModelsSearchCount,searchBaseModelsCount);
}

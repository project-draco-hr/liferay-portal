{
  for (  Counter counter : counters) {
    if (EntityCacheUtil.getResult(CounterModelImpl.ENTITY_CACHE_ENABLED,CounterModelImpl.class,counter.getPrimaryKey(),this) == null) {
      cacheResult(counter);
    }
  }
}

{
  Counter counter=fetchByPrimaryKey(name);
  if (counter == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + name);
    }
    throw new NoSuchCounterException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + name);
  }
  return counter;
}

{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  DDMList newDDMList=addDDMList();
  _persistence.clearCache();
  DDMListModelImpl existingDDMListModelImpl=(DDMListModelImpl)_persistence.findByPrimaryKey(newDDMList.getPrimaryKey());
  assertTrue(Validator.equals(existingDDMListModelImpl.getUuid(),existingDDMListModelImpl.getOriginalUuid()));
  assertEquals(existingDDMListModelImpl.getGroupId(),existingDDMListModelImpl.getOriginalGroupId());
}

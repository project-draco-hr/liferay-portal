{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof JournalXSLURIResolver)) {
    return false;
  }
  JournalXSLURIResolver uriResolver=(JournalXSLURIResolver)obj;
  if (Validator.equals(_languageId,uriResolver._languageId) && _tokens.equals(uriResolver._tokens)) {
    return true;
  }
  return false;
}

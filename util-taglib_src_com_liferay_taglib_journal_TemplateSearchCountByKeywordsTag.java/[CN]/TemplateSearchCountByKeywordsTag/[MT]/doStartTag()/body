{
  int count;
  try {
    count=JournalTemplateLocalServiceUtil.searchCount(_companyId,_groupId,_keywords,_structureId,_structureIdComparator);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,count);
  return SKIP_BODY;
}

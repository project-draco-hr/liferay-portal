{
class B implements EventListener, Serializable {
  }
class C extends B {
  }
  Set<Class<?>> interfaces=OSGiBeanProperties.Service.interfaces(new C());
  Assert.assertEquals(2,interfaces.size());
}

{
@OSGiBeanProperties(service=Serializable.class) class C implements EventListener, Serializable {
  }
  Set<Class<?>> interfaceClasses=OSGiBeanProperties.Service.interfaces(new C());
  Assert.assertEquals(1,interfaceClasses.size());
}

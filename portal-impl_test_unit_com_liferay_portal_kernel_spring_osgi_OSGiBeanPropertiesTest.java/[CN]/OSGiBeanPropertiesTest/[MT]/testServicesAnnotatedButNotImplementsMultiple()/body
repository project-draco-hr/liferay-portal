{
@OSGiBeanProperties(service={EventListener.class,Serializable.class}) class C implements Serializable {
  }
  Set<Class<?>> interfaces=OSGiBeanProperties.Service.interfaces(new C());
  Assert.assertEquals(1,interfaces.size());
}

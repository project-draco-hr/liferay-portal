{
  FileEntry fileEntry=addFileEntry(false,_NAME);
  fileEntry=DLAppServiceUtil.moveFileEntryToTrash(fileEntry.getFileEntryId());
  Assert.assertFalse(fileEntry.getTitle().contains(StringPool.SLASH));
}

{
  List<PollsQuestion> questions=PollsQuestionLocalServiceUtil.getPollsQuestionsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<PollsQuestion>());
  if (ListUtil.isEmpty(questions)) {
    return null;
  }
  return questions.get(0);
}

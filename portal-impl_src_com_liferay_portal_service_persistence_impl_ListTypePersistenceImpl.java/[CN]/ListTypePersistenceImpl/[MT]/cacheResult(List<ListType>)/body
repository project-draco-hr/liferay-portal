{
  for (  ListType listType : listTypes) {
    if (entityCache.getResult(ListTypeModelImpl.ENTITY_CACHE_ENABLED,ListTypeImpl.class,listType.getPrimaryKey()) == null) {
      cacheResult(listType);
    }
 else {
      listType.resetOriginalValues();
    }
  }
}

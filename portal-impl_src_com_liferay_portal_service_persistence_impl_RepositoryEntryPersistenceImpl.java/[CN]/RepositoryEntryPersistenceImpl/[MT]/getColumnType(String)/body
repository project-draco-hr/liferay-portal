{
  Integer type=RepositoryEntryModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ RepositoryEntryModelImpl.TABLE_NAME);
  }
  return type;
}

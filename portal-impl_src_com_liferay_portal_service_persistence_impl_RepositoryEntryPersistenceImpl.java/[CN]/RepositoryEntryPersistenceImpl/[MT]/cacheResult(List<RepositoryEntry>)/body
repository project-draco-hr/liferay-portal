{
  for (  RepositoryEntry repositoryEntry : repositoryEntries) {
    if (entityCache.getResult(RepositoryEntryModelImpl.ENTITY_CACHE_ENABLED,RepositoryEntryImpl.class,repositoryEntry.getPrimaryKey()) == null) {
      cacheResult(repositoryEntry);
    }
 else {
      repositoryEntry.resetOriginalValues();
    }
  }
}

{
  try {
    VirtualHost virtualHost=VirtualHostLocalServiceUtil.getVirtualHost(getCompanyId(),getLayoutSetId());
    return virtualHost.getHostname();
  }
 catch (  Exception e) {
    return StringPool.BLANK;
  }
}

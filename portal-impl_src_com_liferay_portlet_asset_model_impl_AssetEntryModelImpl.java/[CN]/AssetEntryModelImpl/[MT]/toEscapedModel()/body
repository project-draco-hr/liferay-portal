{
  if (isEscapedModel()) {
    return (AssetEntry)this;
  }
 else {
    return (AssetEntry)Proxy.newProxyInstance(AssetEntry.class.getClassLoader(),new Class[]{AssetEntry.class},new AutoEscapeBeanHandler(this));
  }
}

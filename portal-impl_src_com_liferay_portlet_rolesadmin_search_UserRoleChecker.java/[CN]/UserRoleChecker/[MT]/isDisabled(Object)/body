{
  User user=(User)obj;
  try {
    if (isChecked(user)) {
      if (RoleMembershipPolicyUtil.isRoleRequired(user.getUserId(),_role.getRoleId())) {
        return true;
      }
    }
 else {
      if (!RoleMembershipPolicyUtil.isRoleAllowed(user.getUserId(),_role.getRoleId())) {
        return true;
      }
    }
  }
  finally {
    return super.isDisabled(obj);
  }
}

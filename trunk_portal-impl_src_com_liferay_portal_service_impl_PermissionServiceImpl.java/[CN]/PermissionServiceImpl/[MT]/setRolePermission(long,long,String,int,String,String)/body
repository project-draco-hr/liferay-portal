{
  checkPermission(getPermissionChecker(),groupId,Role.class.getName(),roleId);
  User user=getUser();
  permissionLocalService.setRolePermission(roleId,user.getCompanyId(),name,scope,primKey,actionId);
}

{
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("//a[@id='my-community-private-pages']")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.click(RuntimeVariables.replace("//a[@id='my-community-private-pages']"));
  selenium.waitForPageToLoad("30000");
  selenium.click("//div[@id='add-page']/a/span");
  selenium.typeKeys("new_page",RuntimeVariables.replace("Wiki Navigation Test Page"));
  selenium.type("new_page",RuntimeVariables.replace("Wiki Navigation Test Page"));
  selenium.click("link=Save");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Wiki Navigation Test Page")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.click(RuntimeVariables.replace("link=Wiki Navigation Test Page"));
  selenium.waitForPageToLoad("30000");
}

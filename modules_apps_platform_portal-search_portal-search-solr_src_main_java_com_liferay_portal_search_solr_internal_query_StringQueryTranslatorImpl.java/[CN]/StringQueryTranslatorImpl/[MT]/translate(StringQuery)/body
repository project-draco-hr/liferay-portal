{
  try {
    QueryParser queryParser=new QueryParser("uuid",new KeywordAnalyzer());
    return queryParser.parse(stringQuery.getQuery());
  }
 catch (  ParseException pe) {
    throw new IllegalArgumentException("Invalid query",pe);
  }
}

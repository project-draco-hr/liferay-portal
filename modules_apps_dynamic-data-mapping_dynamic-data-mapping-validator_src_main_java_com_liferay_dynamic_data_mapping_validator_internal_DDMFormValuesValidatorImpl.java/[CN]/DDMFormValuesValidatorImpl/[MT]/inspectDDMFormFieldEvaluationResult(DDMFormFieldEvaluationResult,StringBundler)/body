{
  if (ddmFormFieldEvaluationResult.isValid()) {
    return;
  }
  String errorMessage=String.format("%s : %s \n",ddmFormFieldEvaluationResult.getName(),ddmFormFieldEvaluationResult.getErrorMessage());
  sb.append(errorMessage);
}

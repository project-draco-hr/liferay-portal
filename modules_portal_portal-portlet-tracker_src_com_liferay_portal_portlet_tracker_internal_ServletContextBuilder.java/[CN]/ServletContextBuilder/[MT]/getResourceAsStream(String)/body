{
  try {
    URL resource=getResource(path);
    if (resource == null) {
      return null;
    }
    return resource.openStream();
  }
 catch (  MalformedURLException e) {
    _log.error(e,e);
  }
catch (  IOException e) {
    _log.error(e,e);
  }
  return null;
}

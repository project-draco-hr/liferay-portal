{
  try {
    URL url=getResource(path);
    if (url == null) {
      return null;
    }
    return url.openStream();
  }
 catch (  MalformedURLException e) {
    _log.error(e,e);
  }
catch (  IOException e) {
    _log.error(e,e);
  }
  return null;
}

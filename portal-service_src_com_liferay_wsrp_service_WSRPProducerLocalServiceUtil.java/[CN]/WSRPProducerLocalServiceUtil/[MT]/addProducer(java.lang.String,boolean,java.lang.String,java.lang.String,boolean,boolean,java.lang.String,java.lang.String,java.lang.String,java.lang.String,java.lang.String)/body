{
  getService().addProducer(portalId,status,namespace,instanceName,requiresRegistration,supportsInbandRegistration,version,offeredPortlets,producerProfileMap,registrationProperties,registrationValidatorClass);
}

{
  if (parentElement != null) {
    throw new IllegalStateException("Generate xml string from non-root element.");
  }
  if (xmlString == null) {
    flushPendingOpenElements();
    xmlString=stringBundler.toString();
  }
  return xmlString;
}

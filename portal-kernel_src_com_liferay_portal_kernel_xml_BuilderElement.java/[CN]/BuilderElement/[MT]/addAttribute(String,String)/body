{
  if (openTagClosed) {
    throw new IllegalStateException("Adding attribute to a closed element");
  }
  stringBundler.append(StringPool.SPACE).append(name).append(StringPool.EQUAL).append(StringPool.QUOTE).append(value).append(StringPool.QUOTE);
}

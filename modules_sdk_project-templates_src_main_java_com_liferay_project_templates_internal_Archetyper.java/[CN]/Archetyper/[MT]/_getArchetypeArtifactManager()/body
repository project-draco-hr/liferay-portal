{
  if (_archetypeArtifactManager == null) {
    _archetypeArtifactManager=new ArchetyperArchetypeArtifactManager();
    ReflectionUtil.setFieldValue(_loggerField,_archetypeArtifactManager,_logger);
  }
  return _archetypeArtifactManager;
}

{
  super.generateArchetype(archetypeGenerationRequest,archetypeFile,archetypeGenerationResult);
  String fileName=archetypeFile.getName();
  if (fileName.startsWith(_TEMP_ARCHETYPE_PREFIX)) {
    archetypeFile.delete();
  }
}

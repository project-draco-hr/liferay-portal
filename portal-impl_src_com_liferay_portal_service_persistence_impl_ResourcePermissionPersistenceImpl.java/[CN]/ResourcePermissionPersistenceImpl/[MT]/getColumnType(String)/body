{
  Integer type=ResourcePermissionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ResourcePermissionModelImpl.TABLE_NAME);
  }
  return type;
}

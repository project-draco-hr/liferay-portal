{
  DDMForm ddmForm=DDMFormTestUtil.createDDMForm();
  Map<String,Object> templateContext=_ddmFormTemplateContextFactory.create(ddmForm,new DDMFormRenderingContext());
  Assert.assertNotNull(templateContext.get("containerId"));
}

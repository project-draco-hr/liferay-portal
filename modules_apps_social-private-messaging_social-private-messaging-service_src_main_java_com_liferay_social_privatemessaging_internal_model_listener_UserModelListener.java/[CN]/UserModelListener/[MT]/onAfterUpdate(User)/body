{
  try {
    if (_log.isDebugEnabled()) {
      _log.debug("Updating private messages user name for user " + user.getUserId());
    }
    _userThreadLocalService.updateUserName(user);
  }
 catch (  Exception e) {
    _log.error("Unable to update private messages user name for user " + user.getUserId());
  }
}

{
  try {
    if (_log.isDebugEnabled()) {
      _log.debug("Removing private messages for user " + user.getUserId());
    }
    _userThreadLocalService.deleteUser(user.getUserId());
  }
 catch (  Exception e) {
    _log.error("Unable to remove private messages for user " + user.getUserId());
  }
}

{
  RepositoryEntry repositoryEntry=repositoryEntryLocalService.getRepositoryEntry(PrincipalThreadLocal.getUserId(),getGroupId(),getRepositoryId(),objectId);
  return new Object[]{repositoryEntry.getRepositoryEntryId(),repositoryEntry.getUuid(),false};
}

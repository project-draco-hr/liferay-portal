{
  if (permissionChecker.hasOwnerPermission(entry.getCompanyId(),CategoriesEntry.class.getName(),entry.getEntryId(),entry.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(entry.getGroupId(),CategoriesEntry.class.getName(),entry.getEntryId(),actionId);
}

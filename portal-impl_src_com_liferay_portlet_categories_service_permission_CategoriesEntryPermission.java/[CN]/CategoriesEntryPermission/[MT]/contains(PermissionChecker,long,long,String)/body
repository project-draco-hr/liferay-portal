{
  if (entryId == CategoriesEntryConstants.DEFAULT_PARENT_ENTRY_ID) {
    return CategoriesPermission.contains(permissionChecker,groupId,actionId);
  }
 else {
    return contains(permissionChecker,entryId,actionId);
  }
}

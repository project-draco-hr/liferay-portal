{
  UnsyncByteArrayOutputStream unsyncBaos=new UnsyncByteArrayOutputStream();
  for (int i=0; i < _BUFFER_SIZE; i++) {
    unsyncBaos.write(i);
    assertEquals(i + 1,unsyncBaos.size());
  }
  assertTrue(Arrays.equals(_BUFFER,unsyncBaos.toByteArray()));
}

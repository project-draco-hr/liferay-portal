{
  Calendar calendar=_calendarLocalService.fetchCalendar(GetterUtil.getLong(primKey));
  if (calendar == null) {
    return;
  }
  calendar.setModifiedDate(new Date());
  _calendarLocalService.updateCalendar(calendar);
}

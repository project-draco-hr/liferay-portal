{
  HttpServletRequest request=PortalUtil.getHttpServletRequest(portletRequest);
  if (SessionMessages.contains(portletRequest,key) || SessionMessages.contains(request,key)) {
    return true;
  }
  return false;
}

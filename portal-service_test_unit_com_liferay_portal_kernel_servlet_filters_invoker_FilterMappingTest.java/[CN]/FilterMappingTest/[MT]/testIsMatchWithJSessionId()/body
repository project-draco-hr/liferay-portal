{
  List<String> urlPatterns=new ArrayList<String>();
  urlPatterns.add("/c/portal/login");
  FilterMapping filterMapping=new FilterMapping(_filter,_filterConfig,urlPatterns,_dispatchers);
  String uri="/c/portal/login;jsessionid=ACD311312312323BF.worker1";
  MockHttpServletRequest request=new MockHttpServletRequest(HttpMethods.GET,uri);
  boolean isMatch=filterMapping.isMatch(request,Dispatcher.REQUEST,uri);
  Assert.assertEquals(true,isMatch);
}

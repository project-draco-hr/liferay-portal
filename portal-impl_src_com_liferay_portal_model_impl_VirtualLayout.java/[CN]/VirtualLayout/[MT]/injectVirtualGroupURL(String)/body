{
  try {
    Group group=super.getGroup();
    int pos=layoutURL.indexOf(group.getFriendlyURL());
    return layoutURL.substring(0,pos).concat(_virtualGroup.getFriendlyURL()).concat(getFriendlyURL());
  }
 catch (  Exception e) {
    throw new IllegalStateException(e);
  }
}

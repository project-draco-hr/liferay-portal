{
  JournalArticleDisplay articleDisplay=getDisplay(groupId,articleId,templateId,languageId,themeDisplay,disableCaching);
  if (articleDisplay != null) {
    return articleDisplay.getContent();
  }
 else {
    return null;
  }
}

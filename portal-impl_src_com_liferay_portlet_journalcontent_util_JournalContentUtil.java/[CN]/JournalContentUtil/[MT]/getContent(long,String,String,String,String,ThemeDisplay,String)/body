{
  JournalArticleDisplay articleDisplay=getDisplay(groupId,articleId,templateId,articleMode,languageId,themeDisplay,1,xmlRequest);
  if (articleDisplay != null) {
    return articleDisplay.getContent();
  }
 else {
    return null;
  }
}

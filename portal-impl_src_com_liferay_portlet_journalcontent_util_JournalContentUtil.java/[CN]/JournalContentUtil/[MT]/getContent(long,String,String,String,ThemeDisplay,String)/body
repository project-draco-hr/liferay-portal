{
  JournalArticleDisplay articleDisplay=getDisplay(groupId,articleId,templateId,languageId,themeDisplay);
  if (articleDisplay != null) {
    return articleDisplay.getContent();
  }
 else {
    return null;
  }
}

{
  SCProductVersion productVersion=SCProductVersionLocalServiceUtil.getProductVersion(productVersionId);
  SCProductEntryPermission.check(getPermissionChecker(),productVersion.getProductEntryId(),ActionKeys.UPDATE);
  SCProductVersionLocalServiceUtil.deleteProductVersion(productVersionId);
}

{
  SCProductVersionPermission.check(getPermissionChecker(),productVersionId,ActionKeys.UPDATE);
  return SCProductVersionLocalServiceUtil.updateProductVersion(productVersionId,version,changeLog,downloadPageURL,directDownloadURL,repoStoreArtifact,frameworkVersionIds);
}

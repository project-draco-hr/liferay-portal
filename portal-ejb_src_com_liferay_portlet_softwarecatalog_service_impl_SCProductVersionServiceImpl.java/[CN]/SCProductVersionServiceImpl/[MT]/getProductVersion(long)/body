{
  SCProductVersion productVersion=SCProductVersionLocalServiceUtil.getProductVersion(productVersionId);
  SCProductEntryPermission.check(getPermissionChecker(),productVersion.getProductEntryId(),ActionKeys.VIEW);
  return productVersion;
}

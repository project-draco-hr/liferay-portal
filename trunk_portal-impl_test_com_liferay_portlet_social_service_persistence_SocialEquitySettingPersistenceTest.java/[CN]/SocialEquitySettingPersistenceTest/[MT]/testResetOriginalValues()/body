{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  SocialEquitySetting newSocialEquitySetting=addSocialEquitySetting();
  _persistence.clearCache();
  SocialEquitySettingModelImpl existingSocialEquitySettingModelImpl=(SocialEquitySettingModelImpl)_persistence.findByPrimaryKey(newSocialEquitySetting.getPrimaryKey());
  assertEquals(existingSocialEquitySettingModelImpl.getGroupId(),existingSocialEquitySettingModelImpl.getOriginalGroupId());
  assertEquals(existingSocialEquitySettingModelImpl.getClassNameId(),existingSocialEquitySettingModelImpl.getOriginalClassNameId());
  assertTrue(Validator.equals(existingSocialEquitySettingModelImpl.getActionId(),existingSocialEquitySettingModelImpl.getOriginalActionId()));
  assertEquals(existingSocialEquitySettingModelImpl.getType(),existingSocialEquitySettingModelImpl.getOriginalType());
}

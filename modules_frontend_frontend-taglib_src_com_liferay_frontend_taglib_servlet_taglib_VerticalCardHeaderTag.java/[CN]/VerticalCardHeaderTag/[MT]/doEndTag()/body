{
  VerticalCardTag verticalCardTag=(VerticalCardTag)findAncestorWithClass(this,VerticalCardTag.class);
  BodyContent bodyContent=getBodyContent();
  if (bodyContent != null) {
    verticalCardTag.setHeader(bodyContent.getString());
  }
  return EVAL_PAGE;
}

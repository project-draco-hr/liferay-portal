{
  try {
    if (_urlPageSettings == null) {
      _urlPageSettings=PortletProviderUtil.getPortletURL(getRequest(),Layout.class.getName(),PortletProvider.Action.EDIT);
    }
    return _urlPageSettings;
  }
 catch (  PortalException pe) {
    throw new SystemException(pe);
  }
}

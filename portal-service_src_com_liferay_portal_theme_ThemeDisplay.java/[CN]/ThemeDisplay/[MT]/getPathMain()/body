{
  if (isI18n()) {
    if (Validator.isNotNull(_pathContext)) {
      return StringUtil.replaceFirst(_pathMain,_pathContext,_pathContext + StringPool.SLASH + _i18nLanguageId);
    }
 else {
      return StringPool.SLASH + _i18nLanguageId + _pathMain;
    }
  }
  return _pathMain;
}

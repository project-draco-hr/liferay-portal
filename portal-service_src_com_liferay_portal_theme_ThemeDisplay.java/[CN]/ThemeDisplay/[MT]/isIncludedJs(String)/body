{
  String path=getPathJavaScript();
  if (isIncludeCalendarJs() && js.equals(path + "/calendar/calendar_stripped.js")) {
    return true;
  }
 else   if (isIncludePortletCssJs() && js.equals(path + "/liferay/portlet_css_packed.js")) {
    return true;
  }
 else   if (isIncludeServiceJs() && js.equals(path + "/liferay/service_packed.js")) {
    return true;
  }
 else {
    return false;
  }
}

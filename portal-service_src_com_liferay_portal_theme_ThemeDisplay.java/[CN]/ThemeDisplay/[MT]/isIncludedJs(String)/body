{
  String path=getPathJavaScript();
  if (isIncludePortletCssJs() && js.startsWith(path + "/liferay/portlet_css.js")) {
    return true;
  }
 else   if (isIncludeServiceJs() && js.startsWith(path + "/liferay/service.js")) {
    return true;
  }
 else {
    return false;
  }
}

{
  PortletDisplay portletDisplay=getPortletDisplay();
  if ((portletDisplay != null) && portletDisplay.getId().equals(getFocusedPortletId()) && Validator.isNotNull(getFocusedPortletId())) {
    return true;
  }
 else {
    return false;
  }
}

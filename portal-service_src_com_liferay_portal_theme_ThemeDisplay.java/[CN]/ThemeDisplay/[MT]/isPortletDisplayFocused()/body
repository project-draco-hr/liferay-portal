{
  if (Validator.isNotNull(getPortletDisplay()) && Validator.isNotNull(getFocusedPortletId()) && getPortletDisplay().getId().equals(getFocusedPortletId())) {
    return true;
  }
 else {
    return false;
  }
}

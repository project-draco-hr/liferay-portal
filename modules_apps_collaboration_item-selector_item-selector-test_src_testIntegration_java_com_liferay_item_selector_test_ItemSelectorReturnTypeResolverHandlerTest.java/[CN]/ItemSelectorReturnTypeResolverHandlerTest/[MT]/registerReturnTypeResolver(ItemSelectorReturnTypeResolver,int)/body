{
  Dictionary<String,Object> properties=new Hashtable<>();
  properties.put("service.ranking",serviceRanking);
  return _bundleContext.registerService(ItemSelectorReturnTypeResolver.class,itemSelectorReturnTypeResolver,properties);
}

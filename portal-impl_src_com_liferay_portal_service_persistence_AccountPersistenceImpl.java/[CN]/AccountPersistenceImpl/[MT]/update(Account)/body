{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Account account) method. To improve performance, call update(Account account, boolean merge).");
  }
  return update(account,false);
}

{
  for (  Account account : accounts) {
    if (EntityCacheUtil.getResult(AccountModelImpl.ENTITY_CACHE_ENABLED,Account.class,account.getPrimaryKey(),this) == null) {
      cacheResult(account);
    }
  }
}

{
  for (  BookmarksFolder bookmarksFolder : bookmarksFolders) {
    if (entityCache.getResult(BookmarksFolderModelImpl.ENTITY_CACHE_ENABLED,BookmarksFolderImpl.class,bookmarksFolder.getPrimaryKey()) == null) {
      cacheResult(bookmarksFolder);
    }
 else {
      bookmarksFolder.resetOriginalValues();
    }
  }
}

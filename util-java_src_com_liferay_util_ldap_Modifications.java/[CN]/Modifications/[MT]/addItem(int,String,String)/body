{
  BasicAttribute basicAttribute=new BasicAttribute(id);
  if (Validator.isNotNull(value)) {
    basicAttribute.add(value);
  }
  ModificationItem item=new ModificationItem(modificationOp,basicAttribute);
  _items.add(item);
  return item;
}

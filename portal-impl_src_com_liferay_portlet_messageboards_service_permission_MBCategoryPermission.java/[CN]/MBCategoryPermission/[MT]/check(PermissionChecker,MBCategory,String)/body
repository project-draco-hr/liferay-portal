{
  if (!contains(permissionChecker,category,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),MBCategory.class.getName(),category.getCategoryId(),actionId);
  }
}

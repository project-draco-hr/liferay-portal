{
  try {
    File tempFile=File.createTempFile("temp",".scss");
    tempFile.deleteOnExit();
    write(tempFile,input);
    return compileFile(tempFile.getCanonicalPath(),includeDirName,imgDirName);
  }
 catch (  Exception e) {
    throw new JniSassCompilerException(e);
  }
}

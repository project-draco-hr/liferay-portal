{
  final JniSassCompiler sassCompiler=new JniSassCompiler();
  for (  String fileName : fileNames) {
    File file=new File(fileName);
    if (!isValidFile(file)) {
      continue;
    }
    write(getOutputFile(file),sassCompiler.compileFile(fileName,"",false));
  }
}

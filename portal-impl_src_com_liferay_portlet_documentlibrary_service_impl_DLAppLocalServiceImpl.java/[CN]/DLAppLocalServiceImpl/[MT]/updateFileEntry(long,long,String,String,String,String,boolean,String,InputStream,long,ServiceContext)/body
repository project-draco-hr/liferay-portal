{
  LocalRepository localRepository=getLocalRepository();
  return localRepository.updateFileEntry(userId,fileEntryId,sourceFileName,title,description,changeLog,majorVersion,extraSettings,is,size,serviceContext);
}

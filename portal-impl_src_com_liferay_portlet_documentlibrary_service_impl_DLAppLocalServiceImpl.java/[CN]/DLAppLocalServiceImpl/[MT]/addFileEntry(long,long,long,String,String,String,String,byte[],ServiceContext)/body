{
  if (bytes == null) {
    throw new FileSizeException();
  }
  InputStream is=new UnsyncByteArrayInputStream(bytes);
  return dlRepositoryLocalService.addFileEntry(userId,groupId,folderId,title,description,changeLog,extraSettings,is,bytes.length,serviceContext);
}

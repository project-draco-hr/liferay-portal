{
  LocalRepository localRepository=getFileEntryLocalRepository(fileEntryId);
  return RepositoryTrashUtil.moveFileEntryFromTrash(userId,localRepository.getRepositoryId(),fileEntryId,newFolderId,serviceContext);
}

{
  if (bytes == null) {
    bytes=new byte[0];
  }
  InputStream is=new UnsyncByteArrayInputStream(bytes);
  return addFileEntry(userId,repositoryId,folderId,title,description,changeLog,is,bytes.length,serviceContext);
}

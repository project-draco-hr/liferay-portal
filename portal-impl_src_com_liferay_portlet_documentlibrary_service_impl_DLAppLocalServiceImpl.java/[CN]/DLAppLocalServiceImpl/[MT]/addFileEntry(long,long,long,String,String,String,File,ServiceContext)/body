{
  try {
    if (file == null) {
      throw new FileSizeException();
    }
    InputStream is=new FileInputStream(file);
    return addFileEntry(userId,repositoryId,folderId,title,description,changeLog,is,file.length(),serviceContext);
  }
 catch (  FileNotFoundException fnfe) {
    throw new FileSizeException();
  }
}

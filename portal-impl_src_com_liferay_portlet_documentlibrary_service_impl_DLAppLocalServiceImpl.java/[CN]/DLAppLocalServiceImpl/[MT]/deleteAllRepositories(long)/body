{
  List<LocalRepository> localRepositories=repositoryLocalService.getGroupLocalRepositoryImpl(groupId);
  for (  LocalRepository localRepository : localRepositories) {
    deleteRepository(localRepository);
  }
}

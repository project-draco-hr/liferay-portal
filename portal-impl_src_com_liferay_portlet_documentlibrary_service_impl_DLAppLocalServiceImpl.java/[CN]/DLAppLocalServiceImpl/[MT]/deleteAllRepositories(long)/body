{
  List<LocalRepository> localRepositories=repositoryLocalService.getLocalRepositoryImplByGroupId(groupId);
  for (  LocalRepository localRepository : localRepositories) {
    deleteRepository(localRepository);
  }
}

{
  long fileEntryId=fileEntry.getFileEntryId();
  LocalRepository localRepository=getLocalRepository(0,fileEntryId,0);
  localRepository.updateAsset(userId,fileEntry,fileVersion,assetCategoryIds,assetTagNames);
}

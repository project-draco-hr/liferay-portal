{
  String value=null;
  try {
    value=TagUtils.getInstance().message(pageContext,null,null,key);
  }
 catch (  Exception e) {
    _log.error(e);
    throw new LanguageException(key,e);
  }
  if (value == null) {
    value=defaultValue;
  }
  return value;
}

{
  String[] scriptingLanguages=getScriptingLanguages(taskAssignmentSelector,properties);
  for (  String scriptingLanguage : scriptingLanguages) {
    _taskAssignmentSelectors.put(scriptingLanguage,taskAssignmentSelector);
  }
}

{
  RuntimeLogic logic=new PortletLogic(_servletContext,_request,_response,_portletId);
  StringBuilder sb=new StringBuilder();
  logic.processContent(sb,new HashMap<String,String>());
  return sb.toString();
}

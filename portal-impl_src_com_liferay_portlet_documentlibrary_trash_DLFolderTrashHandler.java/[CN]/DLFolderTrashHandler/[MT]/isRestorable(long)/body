{
  DLFolder dlFolder=fetchDLFolder(classPK);
  if ((dlFolder == null) || ((dlFolder.getParentFolderId() > 0) && (DLFolderLocalServiceUtil.fetchFolder(dlFolder.getParentFolderId()) == null))) {
    return false;
  }
  return !dlFolder.isInTrashContainer();
}

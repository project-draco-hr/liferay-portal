{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(permissionId,com.liferay.portal.model.Role.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(permissionId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(permissionId,com.liferay.portal.model.Role.class.getName());
    }
  }
}

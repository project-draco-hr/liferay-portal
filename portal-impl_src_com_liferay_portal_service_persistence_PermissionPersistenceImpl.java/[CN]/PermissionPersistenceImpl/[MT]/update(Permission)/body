{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Permission permission) method. Use update(Permission permission, boolean merge) instead.");
  }
  return update(permission,false);
}

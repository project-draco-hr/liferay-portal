{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Permission permission) method. To improve performance, call update(Permission permission, boolean merge).");
  }
  return update(permission,false);
}

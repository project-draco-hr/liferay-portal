{
  for (  Permission permission : permissions) {
    if (EntityCacheUtil.getResult(PermissionModelImpl.ENTITY_CACHE_ENABLED,Permission.class,permission.getPrimaryKey(),this) == null) {
      cacheResult(permission);
    }
  }
}

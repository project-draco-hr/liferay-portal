{
  if (_initialValue instanceof Cloneable) {
    try {
      Method method=_initialValue.getClass().getMethod(_METHOD_CLONE);
      return (T)method.invoke(_initialValue);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
  return _initialValue;
}

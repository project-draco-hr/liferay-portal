{
  if (isEscapedModel()) {
    return (SCLicense)this;
  }
 else {
    return (SCLicense)Proxy.newProxyInstance(SCLicense.class.getClassLoader(),new Class[]{SCLicense.class},new AutoEscapeBeanHandler(this));
  }
}

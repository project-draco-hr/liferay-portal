{
  String newChannelMode=request.getParameter(WSRPWindowChannelURL.NEW_CHANNEL_MODE);
  if (Validator.isNotNull(newChannelMode)) {
    return WSRPToContainerMap.mapChannelModeToContainer(newChannelMode);
  }
 else {
    return null;
  }
}

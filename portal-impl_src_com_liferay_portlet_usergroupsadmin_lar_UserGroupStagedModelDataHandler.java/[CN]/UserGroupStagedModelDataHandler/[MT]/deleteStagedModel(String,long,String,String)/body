{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  UserGroup userGroup=UserGroupLocalServiceUtil.fetchUserGroupByUuidAndCompanyId(uuid,group.getCompanyId());
  if (userGroup != null) {
    UserGroupLocalServiceUtil.deleteUserGroup(userGroup);
  }
}

{
  String larPath=ExportImportPathUtil.getModelPath(portletDataContext,userGroup.getModelClassName(),userGroup.getPrimaryKey());
  if (isPrivate) {
    larPath.concat("-private");
  }
 else {
    larPath.concat("-public");
  }
  larPath.concat("-layout.lar");
  return larPath;
}

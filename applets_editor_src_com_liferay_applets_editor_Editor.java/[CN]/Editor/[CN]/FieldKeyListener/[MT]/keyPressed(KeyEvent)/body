{
  if (e.isControlDown()) {
switch (e.getKeyCode()) {
case KeyEvent.VK_F:
      _findField.requestFocus();
    e.consume();
  break;
case KeyEvent.VK_R:
_replaceField.requestFocus();
e.consume();
break;
}
}
}

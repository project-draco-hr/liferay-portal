{
  if (_undoMgr.canUndo()) {
    try {
      _undoMgr.undo();
    }
 catch (    CannotUndoException ex) {
      ex.printStackTrace();
    }
    _textPane.requestFocus();
  }
 else {
    getToolkit().beep();
  }
  updateUndoState();
}

{
  if (_textPane.getSelectedText() != null) {
    StringSelection data=new StringSelection(_textPane.getSelectedText());
    getSystemClipboard().setContents(data,data);
  }
  _textPane.requestFocus();
}

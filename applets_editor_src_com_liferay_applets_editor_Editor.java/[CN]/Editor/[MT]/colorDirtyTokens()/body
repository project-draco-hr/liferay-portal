{
  for (int ii=0; ii < _lastTokens.size(); ++ii) {
    Token tok=(Token)_lastTokens.get(ii);
    if (tok.isDirty()) {
      _doc.setCharacterAttributes(tok.getBeg(),tok.getOffset(),tok.getStyle());
    }
  }
}

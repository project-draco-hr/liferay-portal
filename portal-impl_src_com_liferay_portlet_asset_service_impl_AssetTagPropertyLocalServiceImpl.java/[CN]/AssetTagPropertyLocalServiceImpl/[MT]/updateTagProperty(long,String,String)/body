{
  validate(key,value);
  AssetTagProperty property=assetTagPropertyPersistence.findByPrimaryKey(tagPropertyId);
  property.setModifiedDate(new Date());
  property.setKey(key);
  property.setValue(value);
  assetTagPropertyPersistence.update(property,false);
  return property;
}

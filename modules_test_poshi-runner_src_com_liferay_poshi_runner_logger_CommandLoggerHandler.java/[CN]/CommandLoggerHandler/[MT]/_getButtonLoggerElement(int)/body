{
  LoggerElement buttonLoggerElement=new LoggerElement();
  buttonLoggerElement.setAttribute("data-btnlinkid","command-" + btnLinkId);
  buttonLoggerElement.setClassName("btn expand-toggle");
  return buttonLoggerElement;
}

{
  Folder folder=DLAppTestUtil.addFolder(group.getGroupId(),DLFolderConstants.DEFAULT_PARENT_FOLDER_ID,ServiceTestUtil.randomString(_FOLDER_NAME_MAX_LENGTH));
  return (DLFolder)folder.getModel();
}

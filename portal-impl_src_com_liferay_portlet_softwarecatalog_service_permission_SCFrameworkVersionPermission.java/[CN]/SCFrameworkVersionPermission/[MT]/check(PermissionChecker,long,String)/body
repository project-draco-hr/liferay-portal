{
  if (!contains(permissionChecker,frameworkVersionId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),SCFrameworkVersion.class.getName(),frameworkVersionId,actionId);
  }
}

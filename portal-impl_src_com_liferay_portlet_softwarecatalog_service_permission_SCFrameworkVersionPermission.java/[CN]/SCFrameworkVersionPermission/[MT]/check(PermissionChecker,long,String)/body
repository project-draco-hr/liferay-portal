{
  if (!contains(permissionChecker,frameworkVersionId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,SCFrameworkVersion.class.getName(),frameworkVersionId,actionId);
  }
}

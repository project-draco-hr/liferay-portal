{
  ShoppingCategoryPermission.check(getPermissionChecker(),categoryId,ActionKeys.UPDATE);
  return ShoppingCategoryLocalServiceUtil.updateCategory(getUser().getCompanyId(),categoryId,parentCategoryId,name,description);
}

{
  String updateDLFileEntry="update SCLicense " + "set companyId = (select pe.companyId " + "from SCLicenses_SCProductEntries lpe, SCProductEntry pe "+ "where SCLicense.licenseId=lpe.licenseId and "+ "lpe.productEntryId=pe.productEntryId);";
  runSQL(updateDLFileEntry);
}

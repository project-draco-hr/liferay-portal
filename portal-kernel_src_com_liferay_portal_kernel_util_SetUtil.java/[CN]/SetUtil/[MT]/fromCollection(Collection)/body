{
  if ((c != null) && (c instanceof Set)) {
    return (Set)c;
  }
  if ((c == null) || (c.size() == 0)) {
    return new HashSet();
  }
  Set set=new HashSet(c.size());
  Iterator itr=c.iterator();
  while (itr.hasNext()) {
    set.add(itr.next());
  }
  return set;
}

{
  long classNameId=PortalUtil.getClassNameId(User.class);
  runSQL("update Contact_ set classNameId = " + classNameId);
  runSQL("update Contact_ set classPK = (select User_.userId from User_ " + "where User_.contactId = Contact_.contactId)");
}

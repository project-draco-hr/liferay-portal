{
  RequestDispatcher requestDispatcher=servletContext.getRequestDispatcher(_TOP);
  requestDispatcher.include(request,response);
  requestDispatcher=servletContext.getRequestDispatcher(_BOTTOM);
  requestDispatcher.include(request,response);
}

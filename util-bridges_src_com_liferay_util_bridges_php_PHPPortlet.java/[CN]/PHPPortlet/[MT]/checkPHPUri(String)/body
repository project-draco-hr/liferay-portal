{
  if (Validator.isNotNull(phpUri) && (!PortalUtil.isValidResourceId(phpUri) || !Validator.isFilePath(phpUri,false))) {
    throw new PortletException("URI " + phpUri + " is not accessible by this portlet");
  }
}

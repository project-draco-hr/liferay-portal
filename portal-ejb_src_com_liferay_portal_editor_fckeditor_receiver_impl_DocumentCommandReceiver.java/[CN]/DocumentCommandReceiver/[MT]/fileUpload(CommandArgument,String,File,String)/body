{
  try {
    Group group=GroupLocalServiceUtil.getGroup(arg.getCompanyId(),arg.getCurrentGroupName());
    DLFolder folder=_getFolder(group.getGroupId(),arg.getCurrentFolder());
    DLFileEntryServiceUtil.addFileEntry(folder.getFolderId(),fileName,fileName,StringPool.BLANK,FileUtil.getBytes(file),true,true);
  }
 catch (  Exception e) {
    throw new FCKException(e);
  }
  return "0";
}

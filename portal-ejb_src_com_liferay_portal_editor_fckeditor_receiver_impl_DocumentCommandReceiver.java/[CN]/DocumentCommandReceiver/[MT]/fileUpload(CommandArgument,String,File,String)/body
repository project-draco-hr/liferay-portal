{
  try {
    DLFolder folder=_getFolder(arg.getGroupId(),arg.getCurrentFolder());
    DLFileEntryServiceUtil.addFileEntry(folder.getFolderId(),fileName,fileName,StringPool.BLANK,FileUtil.getBytes(file),true,true);
  }
 catch (  Exception e) {
    throw new FCKException(e);
  }
  return "0";
}

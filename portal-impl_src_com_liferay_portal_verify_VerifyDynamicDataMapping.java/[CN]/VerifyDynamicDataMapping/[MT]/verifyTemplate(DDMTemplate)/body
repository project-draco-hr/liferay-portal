{
  if (ddmTemplate.getType() != DDMTemplateConstants.TEMPLATE_TYPE_FORM) {
    return;
  }
  String script=verifySchema(ddmTemplate.getScript(),ddmTemplate.getDefaultLanguageId());
  updateDDMTemplate(ddmTemplate,script);
}

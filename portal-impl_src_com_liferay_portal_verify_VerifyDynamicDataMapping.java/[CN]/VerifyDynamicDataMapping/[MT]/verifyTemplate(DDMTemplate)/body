{
  if (template.getType() != DDMTemplateConstants.TEMPLATE_TYPE_FORM) {
    return;
  }
  String script=verifySchema(template.getScript(),template.getDefaultLanguageId());
  updateTemplate(template,script);
}

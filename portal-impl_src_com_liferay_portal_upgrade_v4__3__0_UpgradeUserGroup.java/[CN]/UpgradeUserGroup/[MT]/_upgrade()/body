{
  PKUpgradeColumnImpl pkUpgradeColumn=new PKUpgradeColumnImpl("userGroupId",true);
  UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(UserGroupImpl.TABLE_NAME,UserGroupImpl.TABLE_COLUMNS,pkUpgradeColumn);
  upgradeTable.updateTable();
  ValueMapper userGroupIdMapper=pkUpgradeColumn.getValueMapper();
  AvailableMappersUtil.setUserGroupIdMapper(userGroupIdMapper);
  ResourceUtil.upgradePrimKey(userGroupIdMapper,UserGroup.class.getName());
  CounterLocalServiceUtil.reset(UserGroup.class.getName());
}

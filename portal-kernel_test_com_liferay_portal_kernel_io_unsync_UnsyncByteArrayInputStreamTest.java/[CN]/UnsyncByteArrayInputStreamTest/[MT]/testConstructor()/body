{
  UnsyncByteArrayInputStream unsyncBais=new UnsyncByteArrayInputStream(_BUFFER);
  assertEquals(_BUFFER_SIZE,unsyncBais.available());
  unsyncBais=new UnsyncByteArrayInputStream(_BUFFER,_BUFFER_SIZE / 2,_BUFFER_SIZE / 2);
  assertEquals(_BUFFER_SIZE / 2,unsyncBais.available());
}

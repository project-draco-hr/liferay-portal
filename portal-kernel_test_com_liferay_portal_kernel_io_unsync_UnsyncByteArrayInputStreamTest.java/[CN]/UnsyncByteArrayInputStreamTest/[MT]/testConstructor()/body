{
  UnsyncByteArrayInputStream ubais=new UnsyncByteArrayInputStream(_BUFFER);
  assertEquals(_BUFFER_SIZE,ubais.available());
  ubais=new UnsyncByteArrayInputStream(_BUFFER,_BUFFER_SIZE / 2,_BUFFER_SIZE / 2);
  assertEquals(_BUFFER_SIZE / 2,ubais.available());
}

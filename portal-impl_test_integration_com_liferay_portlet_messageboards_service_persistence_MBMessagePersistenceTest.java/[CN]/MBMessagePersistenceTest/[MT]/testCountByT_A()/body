{
  try {
    _persistence.countByT_A(ServiceTestUtil.nextLong(),ServiceTestUtil.randomBoolean());
    _persistence.countByT_A(0L,ServiceTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

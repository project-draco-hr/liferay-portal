{
  Path path=FileServerTestUtil.createNotExistFile(Paths.get("testNotExistFile"));
  _embeddedChannel.writeInbound(new FileRequest(path,0,false));
  Queue<Object> queue=_embeddedChannel.outboundMessages();
  Assert.assertEquals(1,queue.size());
  Assert.assertEquals(new FileResponse(path,FileResponse.FILE_NOT_FOUND,-1,false),queue.poll());
}

{
  String emailPageUpdatedSubject=typedSettings.getValue("emailPageUpdatedSubject");
  if (Validator.isNotNull(emailPageUpdatedSubject)) {
    return emailPageUpdatedSubject;
  }
  return ContentUtil.get(typedSettings.getValue(PropsKeys.WIKI_EMAIL_PAGE_UPDATED_SUBJECT));
}

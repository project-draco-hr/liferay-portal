{
  String emailPageAddedBody=typedSettings.getValue("emailPageAddedBody");
  if (Validator.isNotNull(emailPageAddedBody)) {
    return emailPageAddedBody;
  }
  return ContentUtil.get(typedSettings.getValue(PropsKeys.WIKI_EMAIL_PAGE_ADDED_BODY));
}

{
  String emailPageUpdatedBody=typedSettings.getValue("emailPageUpdatedBody");
  if (Validator.isNotNull(emailPageUpdatedBody)) {
    return emailPageUpdatedBody;
  }
  return ContentUtil.get(typedSettings.getValue(PropsKeys.WIKI_EMAIL_PAGE_UPDATED_BODY));
}

{
  try {
    pushPath(path);
    String listPath=getFullPath(StringPool.BLANK);
    popPath();
    return FileUtil.read(listPath);
  }
 catch (  IOException ioe) {
    _log.error(ioe,ioe);
  }
  return null;
}

{
  try {
    pushPath(path);
    String fullPath=getFullPath(StringPool.BLANK);
    popPath();
    return FileUtil.read(fullPath);
  }
 catch (  IOException ioe) {
    _log.error(ioe,ioe);
  }
  return null;
}

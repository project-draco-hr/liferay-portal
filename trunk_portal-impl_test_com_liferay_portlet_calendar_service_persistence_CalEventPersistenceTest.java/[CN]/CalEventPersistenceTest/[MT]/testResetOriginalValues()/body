{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  CalEvent newCalEvent=addCalEvent();
  _persistence.clearCache();
  CalEventModelImpl existingCalEventModelImpl=(CalEventModelImpl)_persistence.findByPrimaryKey(newCalEvent.getPrimaryKey());
  assertTrue(Validator.equals(existingCalEventModelImpl.getUuid(),existingCalEventModelImpl.getOriginalUuid()));
  assertEquals(existingCalEventModelImpl.getGroupId(),existingCalEventModelImpl.getOriginalGroupId());
}

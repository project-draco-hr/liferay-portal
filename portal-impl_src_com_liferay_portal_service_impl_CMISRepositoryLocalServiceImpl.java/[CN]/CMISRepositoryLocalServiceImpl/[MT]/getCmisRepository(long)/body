{
  Repository repositoryImpl=repositoryLocalService.getRepositoryImpl(repositoryId);
  CMISRepositoryHandler cmisRepositoryHandler=repositoryImpl.getCapability(CMISRepositoryHandler.class);
  return (CMISRepository)cmisRepositoryHandler.getCmisRepository();
}

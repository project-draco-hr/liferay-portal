{
  AffineTransformOp op=new AffineTransformOp(AffineTransform.getScaleInstance(factor,factor),null);
  BufferedImage bufferedImage=getBufferedImage(renderedImage);
  return op.filter(bufferedImage,null);
}

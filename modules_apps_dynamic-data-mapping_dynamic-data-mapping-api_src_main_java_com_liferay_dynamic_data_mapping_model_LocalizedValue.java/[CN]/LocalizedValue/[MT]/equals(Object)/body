{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof LocalizedValue)) {
    return false;
  }
  LocalizedValue localizedValue=(LocalizedValue)obj;
  if (Validator.equals(_defaultLocale,localizedValue._defaultLocale) && Validator.equals(_values,localizedValue._values)) {
    return true;
  }
  return false;
}

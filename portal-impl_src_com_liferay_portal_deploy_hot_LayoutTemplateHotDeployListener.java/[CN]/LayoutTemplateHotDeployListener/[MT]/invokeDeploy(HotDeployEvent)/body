{
  try {
    doInvokeDeploy(hotDeployEvent);
  }
 catch (  Throwable t) {
    throwHotDeployException(hotDeployEvent,"Error registering layout templates for " + hotDeployEvent.getServletContextName(),t);
  }
}

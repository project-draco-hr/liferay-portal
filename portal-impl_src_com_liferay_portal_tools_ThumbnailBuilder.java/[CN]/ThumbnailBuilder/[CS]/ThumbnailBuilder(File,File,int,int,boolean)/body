{
  try {
    if (!originalFile.exists()) {
      return;
    }
    if (!overwrite) {
      if (thumbnailFile.lastModified() > originalFile.lastModified()) {
        return;
      }
    }
    ImageBag imageBag=_imageProcessorUtil.read(originalFile);
    RenderedImage renderedImage=_imageProcessorUtil.scale(imageBag.getRenderedImage(),height,width);
    ImageIO.write(renderedImage,imageBag.getType(),thumbnailFile);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

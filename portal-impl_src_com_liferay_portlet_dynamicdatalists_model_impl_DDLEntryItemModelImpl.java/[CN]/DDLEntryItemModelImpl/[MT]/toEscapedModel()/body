{
  if (isEscapedModel()) {
    return (DDLEntryItem)this;
  }
 else {
    return (DDLEntryItem)Proxy.newProxyInstance(DDLEntryItem.class.getClassLoader(),new Class[]{DDLEntryItem.class},new AutoEscapeBeanHandler(this));
  }
}

{
  ClassData classData=children.get(className);
  if (classData == null) {
    classData=new ClassData(className);
    ClassData previousClassData=children.putIfAbsent(className,classData);
    if (previousClassData != null) {
      classData=previousClassData;
    }
  }
  return classData;
}

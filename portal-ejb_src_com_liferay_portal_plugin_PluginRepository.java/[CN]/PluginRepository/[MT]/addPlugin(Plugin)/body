{
  _artifactURLIndex.put(plugin.getArtifactURL(),plugin);
  _moduleIdIndex.put(plugin.getModuleId(),plugin);
  _addToGroupAndArtifactIndex(plugin.getGroupId(),plugin.getArtifactId(),plugin);
  _plugins.add(plugin);
  _tags.addAll(plugin.getTags());
}

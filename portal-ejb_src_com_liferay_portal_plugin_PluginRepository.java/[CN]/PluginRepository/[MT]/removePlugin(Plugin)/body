{
  _artifactURLIndex.remove(plugin.getArtifactURL());
  _moduleIdIndex.remove(plugin.getModuleId());
  _removeFromGroupAndArtifactIndex(plugin.getGroupId(),plugin.getArtifactId(),plugin.getModuleId());
  _plugins.remove(plugin);
}

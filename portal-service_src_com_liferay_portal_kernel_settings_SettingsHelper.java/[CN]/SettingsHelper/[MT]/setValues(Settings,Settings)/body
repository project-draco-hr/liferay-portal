{
  for (  String name : sourceSettings.getSetKeys()) {
    String[] values=sourceSettings.getValues(name,StringPool.EMPTY_ARRAY);
    if (values.length == 1) {
      targetSettings.setValue(name,values[0]);
    }
 else {
      targetSettings.setValues(name,values);
    }
  }
}

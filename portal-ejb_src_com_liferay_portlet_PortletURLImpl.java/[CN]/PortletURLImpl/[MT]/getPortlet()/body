{
  if (_portlet == null) {
    try {
      _portlet=PortletServiceUtil.getPortletById(PortalUtil.getCompanyId(_req),_portletName);
    }
 catch (    SystemException se) {
      _log.error(se.getMessage());
    }
  }
  return _portlet;
}

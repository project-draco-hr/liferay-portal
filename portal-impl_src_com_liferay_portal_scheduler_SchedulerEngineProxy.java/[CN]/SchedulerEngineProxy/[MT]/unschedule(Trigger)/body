{
  try {
    _delegate.unschedule(trigger.getJobName(),trigger.getGroupName());
  }
 catch (  SchedulerException se) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to unschedule job: " + trigger.getGroupName() + ":"+ trigger.getJobName(),se);
    }
  }
}

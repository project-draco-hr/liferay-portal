{
  try {
    _schedulerEngine.unschedule(trigger.getJobName(),trigger.getGroupName());
  }
 catch (  SchedulerException se) {
    _log.error("Unable to unschedule job {jobName=" + trigger.getJobName() + ", groupName="+ trigger.getGroupName()+ "}",se);
  }
}

{
  SchedulerRequest schedulerRequest=SchedulerRequest.createRegisterRequest(groupName,interval,startDate,endDate,description,destinationName,message);
  MessageBusUtil.sendMessage(DestinationNames.SCHEDULER,schedulerRequest);
}

{
  SchedulerRequest schedulerRequest=SchedulerRequest.createRegisterRequest(groupName,cronText,startDate,endDate,description,destinationName,message);
  MessageBusUtil.sendMessage(DestinationNames.SCHEDULER,schedulerRequest);
}

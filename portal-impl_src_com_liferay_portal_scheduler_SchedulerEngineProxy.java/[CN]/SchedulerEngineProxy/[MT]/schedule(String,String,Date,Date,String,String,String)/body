{
  SchedulerRequest schedulerRequest=SchedulerRequest.createRegisterRequest(groupName,cronText,startDate,endDate,description,destinationName,messageBody);
  MessageBusUtil.sendMessage(DestinationNames.SCHEDULER_ENGINE,schedulerRequest);
}

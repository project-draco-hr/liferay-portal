{
  try {
    SchedulerRequest schedulerRequest=new SchedulerRequest(SchedulerRequest.COMMAND_RETRIEVE,null,groupName,null,null,null,null,null,null);
    List<SchedulerRequest> schedulerRequests=(List<SchedulerRequest>)MessageBusUtil.sendSynchronizedMessage(DestinationNames.SCHEDULER,DestinationNames.SCHEDULER_RESPONSE,schedulerRequest);
    return schedulerRequests;
  }
 catch (  Exception e) {
    throw new SchedulerException(e);
  }
}

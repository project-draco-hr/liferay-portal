{
  try {
    _delegate.unschedule(jobName,groupName);
  }
 catch (  SchedulerException se) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to unschedule job: " + groupName + ":"+ jobName,se);
    }
  }
}

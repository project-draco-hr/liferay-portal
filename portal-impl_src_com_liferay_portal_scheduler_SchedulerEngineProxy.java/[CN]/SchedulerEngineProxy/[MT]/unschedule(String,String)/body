{
  try {
    _schedulerEngine.unschedule(jobName,groupName);
  }
 catch (  SchedulerException se) {
    _log.error("Unable to unschedule job {jobName=" + jobName + ", groupName="+ groupName+ "}",se);
  }
}

{
  SchedulerRequest schedulerRequest=SchedulerRequest.createRegisterRequest(groupName,interval,startDate,endDate,description,destinationName,messageBody);
  MessageBusUtil.sendMessage(DestinationNames.SCHEDULER_ENGINE,schedulerRequest);
}

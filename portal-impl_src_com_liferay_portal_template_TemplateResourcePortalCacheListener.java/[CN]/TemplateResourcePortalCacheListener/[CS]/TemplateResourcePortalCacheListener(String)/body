{
  String portalCacheName=TemplateResource.class.getName();
  portalCacheName=portalCacheName.concat(StringPool.POUND).concat(templateResourceLoaderName);
  _portalCache=SingleVMPoolUtil.getCache(portalCacheName);
}

{
  List<MBBan> bans=null;
  try {
    bans=MBBanLocalServiceUtil.getBans(group.getGroupId(),QueryUtil.ALL_POS,QueryUtil.ALL_POS);
  }
 catch (  Exception ex) {
  }
  if ((bans == null) || bans.isEmpty()) {
    return null;
  }
  return bans.get(0);
}

{
  _bundleContext=bundleContext;
  _log=new Logger(bundleContext);
  List<Release> releases=_releaseLocalService.getReleases(QueryUtil.ALL_POS,QueryUtil.ALL_POS);
  for (  Release release : releases) {
    publish(release);
  }
}

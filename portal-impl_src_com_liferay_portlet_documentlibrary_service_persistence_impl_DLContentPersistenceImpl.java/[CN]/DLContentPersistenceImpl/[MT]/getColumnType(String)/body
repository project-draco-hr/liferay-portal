{
  Integer type=DLContentModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DLContentModelImpl.TABLE_NAME);
  }
  return type;
}

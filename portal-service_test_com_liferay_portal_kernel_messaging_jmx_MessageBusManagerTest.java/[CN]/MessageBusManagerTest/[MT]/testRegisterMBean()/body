{
  _mBeanServer.registerMBean(new MessageBusManager(_mockery.mock(MessageBus.class)),MessageBusManager.createObjectName());
  assertTrue(_mBeanServer.isRegistered(MessageBusManager.createObjectName()));
}

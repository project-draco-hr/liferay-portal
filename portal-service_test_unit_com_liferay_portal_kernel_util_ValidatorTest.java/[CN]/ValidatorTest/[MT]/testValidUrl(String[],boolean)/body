{
  if (valid) {
    for (    String url : urls) {
      if (!Validator.isUrl(url)) {
        Assert.fail(url);
      }
    }
  }
 else {
    for (    String url : urls) {
      if (Validator.isUrl(url)) {
        Assert.fail(url);
      }
    }
  }
}

{
  if (valid) {
    for (    String uri : uris) {
      if (!Validator.isUri(uri)) {
        Assert.fail(uri);
      }
    }
  }
 else {
    for (    String uri : uris) {
      if (Validator.isUri(uri)) {
        Assert.fail(uri);
      }
    }
  }
}

{
  Document document=null;
  try {
    document=getVersionedDocument(companyId,repositoryId,fileName,versionLabel);
    document.delete(true);
  }
 catch (  NoSuchFileException nsfe) {
    logFailedDeletion(companyId,repositoryId,fileName,versionLabel,nsfe);
  }
}

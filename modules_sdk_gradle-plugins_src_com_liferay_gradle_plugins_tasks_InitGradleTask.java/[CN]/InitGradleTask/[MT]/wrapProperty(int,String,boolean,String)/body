{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < indentCount; i++) {
    sb.append('\t');
  }
  sb.append(name);
  sb.append(" = ");
  if (quoteValue) {
    sb.append('\"');
  }
  sb.append(value);
  if (quoteValue) {
    sb.append('\"');
  }
  return sb.toString();
}

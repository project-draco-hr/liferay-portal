{
  PollsChoice model=new PollsChoiceImpl();
  model.setUuid(Html.escape(getUuid()));
  model.setChoiceId(getChoiceId());
  model.setQuestionId(getQuestionId());
  model.setName(Html.escape(getName()));
  model.setDescription(Html.escape(getDescription()));
  if (true) {
    model=(PollsChoice)Proxy.newProxyInstance(PollsChoice.class.getClassLoader(),new Class[]{PollsChoice.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

{
  Document doc=getArticleDocument(companyId,groupId,articleId,version,title,description,content,type,displayDate,tagsEntries,expandoBridge);
  SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
}

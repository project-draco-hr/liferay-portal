{
  try {
    StringBuilder sb=new StringBuilder();
    com.liferay.portal.kernel.xml.Document contentDoc=SAXReaderUtil.read(content);
    Element root=contentDoc.getRootElement();
    _getIndexableContent(sb,doc,root);
    return sb.toString();
  }
 catch (  Exception e) {
    e.printStackTrace();
    return content;
  }
}

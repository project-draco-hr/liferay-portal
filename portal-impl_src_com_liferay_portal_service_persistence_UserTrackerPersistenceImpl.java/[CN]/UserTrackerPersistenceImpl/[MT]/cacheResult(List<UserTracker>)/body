{
  for (  UserTracker userTracker : userTrackers) {
    if (EntityCacheUtil.getResult(UserTrackerModelImpl.ENTITY_CACHE_ENABLED,UserTracker.class,userTracker.getPrimaryKey(),this) == null) {
      cacheResult(userTracker);
    }
  }
}

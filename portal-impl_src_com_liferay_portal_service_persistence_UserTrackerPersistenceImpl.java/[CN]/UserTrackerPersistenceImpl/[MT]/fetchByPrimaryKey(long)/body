{
  UserTracker userTracker=(UserTracker)EntityCacheUtil.getResult(UserTrackerModelImpl.ENTITY_CACHE_ENABLED,UserTrackerImpl.class,userTrackerId,this);
  if (userTracker == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (userTracker == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      userTracker=(UserTracker)session.get(UserTrackerImpl.class,Long.valueOf(userTrackerId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (userTracker == null)) {
        EntityCacheUtil.putResult(UserTrackerModelImpl.ENTITY_CACHE_ENABLED,UserTrackerImpl.class,userTrackerId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(userTracker);
      }
      closeSession(session);
    }
  }
  return userTracker;
}

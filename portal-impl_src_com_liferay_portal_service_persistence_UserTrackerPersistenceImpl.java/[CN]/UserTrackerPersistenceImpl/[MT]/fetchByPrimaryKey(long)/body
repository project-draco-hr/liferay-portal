{
  Session session=null;
  try {
    session=openSession();
    return (UserTracker)session.get(UserTrackerImpl.class,new Long(userTrackerId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

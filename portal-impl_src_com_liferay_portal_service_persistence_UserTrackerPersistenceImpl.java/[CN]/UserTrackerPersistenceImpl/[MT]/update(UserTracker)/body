{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserTracker userTracker) method. To improve performance, call update(UserTracker userTracker, boolean merge).");
  }
  return update(userTracker,false);
}

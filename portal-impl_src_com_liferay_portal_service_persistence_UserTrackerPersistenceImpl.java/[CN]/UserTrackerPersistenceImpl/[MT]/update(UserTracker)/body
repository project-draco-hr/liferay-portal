{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserTracker userTracker) method. Use update(UserTracker userTracker, boolean merge) instead.");
  }
  return update(userTracker,false);
}

{
  List<String> guestDefaultActions=new UniqueList<String>(getActions(actionsMap,name));
  Element guestDefaultsElement=getPermissionsChildElement(parentElement,"guest-defaults");
  guestDefaultActions.addAll(readActionKeys(guestDefaultsElement));
  return guestDefaultActions;
}

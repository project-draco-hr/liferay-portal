{
  List<String> supportsActions=new ArrayList<String>(getActions(actionsMap,name));
  Element supportsElement=getPermissionsChildElement(parentElement,"supports");
  supportsActions.addAll(readActionKeys(supportsElement));
  return ListUtil.unique(supportsActions);
}

{
  List<String> guestUnsupportedActions=getActions(actionsMap,name);
  Element guestUnsupportedElement=getPermissionsChildElement(parentElement,"guest-unsupported");
  readActionKeys(guestUnsupportedElement,guestUnsupportedActions);
  checkGuestUnsupportedActions(guestUnsupportedActions,guestDefaultActions);
}

{
  List<String> communityDefaultActions=new UniqueList<String>(getActions(actionsMap,name));
  Element communityDefaultsElement=getPermissionsChildElement(parentElement,"community-defaults");
  communityDefaultActions.addAll(readActionKeys(communityDefaultsElement));
  setActions(actionsMap,name,communityDefaultActions);
}

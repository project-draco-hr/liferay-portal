{
  DLFileRank fileRank1=(DLFileRank)obj1;
  DLFileRank fileRank2=(DLFileRank)obj2;
  int value=DateUtil.compareTo(fileRank1.getCreateDate(),fileRank2.getCreateDate());
  if (_asc) {
    return value;
  }
 else {
    return -value;
  }
}

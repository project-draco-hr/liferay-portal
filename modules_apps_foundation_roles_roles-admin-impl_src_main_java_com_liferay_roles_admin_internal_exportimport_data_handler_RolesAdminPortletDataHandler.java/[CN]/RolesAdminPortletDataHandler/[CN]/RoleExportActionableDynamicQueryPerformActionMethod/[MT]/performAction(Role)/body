{
  if (!_export) {
    return;
  }
  if (!_portletDataContext.getBooleanParameter(NAMESPACE,"system-roles") && _allSystemRoleNames.contains(role.getName())) {
    return;
  }
  _performActionMethod.performAction(role);
}

{
  if (integerWrapper == null) {
    return 1;
  }
  if (getValue() > integerWrapper.getValue()) {
    return 1;
  }
 else   if (getValue() < integerWrapper.getValue()) {
    return -1;
  }
 else {
    return 0;
  }
}

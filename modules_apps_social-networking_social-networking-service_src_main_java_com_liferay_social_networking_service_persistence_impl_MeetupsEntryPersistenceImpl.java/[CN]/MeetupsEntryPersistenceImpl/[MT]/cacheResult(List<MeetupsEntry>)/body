{
  for (  MeetupsEntry meetupsEntry : meetupsEntries) {
    if (entityCache.getResult(MeetupsEntryModelImpl.ENTITY_CACHE_ENABLED,MeetupsEntryImpl.class,meetupsEntry.getPrimaryKey()) == null) {
      cacheResult(meetupsEntry);
    }
 else {
      meetupsEntry.resetOriginalValues();
    }
  }
}

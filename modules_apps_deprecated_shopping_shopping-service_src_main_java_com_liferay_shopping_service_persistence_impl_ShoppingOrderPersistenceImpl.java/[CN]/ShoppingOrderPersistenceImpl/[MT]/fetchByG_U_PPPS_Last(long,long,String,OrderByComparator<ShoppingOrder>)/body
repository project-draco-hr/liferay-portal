{
  int count=countByG_U_PPPS(groupId,userId,ppPaymentStatus);
  if (count == 0) {
    return null;
  }
  List<ShoppingOrder> list=findByG_U_PPPS(groupId,userId,ppPaymentStatus,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

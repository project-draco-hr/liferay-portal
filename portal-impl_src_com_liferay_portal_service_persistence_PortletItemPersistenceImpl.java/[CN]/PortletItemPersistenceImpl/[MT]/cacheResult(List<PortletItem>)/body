{
  for (  PortletItem portletItem : portletItems) {
    if (EntityCacheUtil.getResult(PortletItemModelImpl.ENTITY_CACHE_ENABLED,PortletItem.class,portletItem.getPrimaryKey(),this) == null) {
      cacheResult(portletItem);
    }
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PortletItem portletItem) method. Use update(PortletItem portletItem, boolean merge) instead.");
  }
  return update(portletItem,false);
}

{
  if ((_fileEntryDisplayContextHelper.isCheckedOut() && !_fileEntryDisplayContextHelper.isLockedByMe()) || (_fileVersionDisplayContextHelper.isPending() && _isEnableFileEntryDrafts())) {
    return true;
  }
  return false;
}

{
  if (_fileEntryDisplayContextHelper.isCheckedOutByMe() || (_fileVersionDisplayContextHelper.isPending() && _dlPortletInstanceSettings.isEnableFileEntryDrafts())) {
    return true;
  }
  return false;
}

{
  if (_fileEntryDisplayContextHelper.isCheckedOutByOther() || (_fileVersionDisplayContextHelper.isPending() && _dlPortletInstanceSettings.isEnableFileEntryDrafts())) {
    return true;
  }
  return false;
}

{
  if ((_fileEntryDisplayContextHelper.isCheckedOut() || _fileVersionDisplayContextHelper.isPending()) && !_dlPortletInstanceSettings.isEnableFileEntryDrafts()) {
    return true;
  }
  return false;
}

{
  Registry registry=RegistryUtil.getRegistry();
  MVCActionCommand mvcActionCommand=registry.getService(serviceReference);
  String commandName=(String)serviceReference.getProperty("mvc.command.name");
  _mvcActionCommandCache.put(commandName,mvcActionCommand);
  return mvcActionCommand;
}

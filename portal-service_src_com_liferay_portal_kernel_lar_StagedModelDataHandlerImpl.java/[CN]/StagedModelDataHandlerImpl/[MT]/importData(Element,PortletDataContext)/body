{
  String path=entityElement.attributeValue("path");
  T entity=(T)portletDataContext.getZipEntryAsObject(entityElement,path);
  if (!portletDataContext.isPathNotProcessed(path)) {
    return;
  }
  importData(entity,path,portletDataContext);
}

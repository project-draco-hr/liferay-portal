{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  Website website=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  if (website != null) {
    WebsiteLocalServiceUtil.deleteWebsite(website);
  }
}

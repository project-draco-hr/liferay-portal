{
  String backURL=request.getHeader(HttpHeaders.REFERER);
  if (Validator.isNull(backURL)) {
    backURL="javascript:history.go(-1)";
  }
  request.setAttribute("liferay-ui:discussion:backURL",String.valueOf(backURL));
}

{
  try {
    _persistence.countByR_I_S(RandomTestUtil.nextLong(),RandomTestUtil.randomBoolean(),new int[]{RandomTestUtil.nextInt(),0});
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

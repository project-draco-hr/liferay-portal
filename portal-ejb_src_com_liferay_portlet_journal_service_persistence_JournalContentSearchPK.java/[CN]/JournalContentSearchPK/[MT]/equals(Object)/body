{
  if (obj == null) {
    return false;
  }
  JournalContentSearchPK pk=null;
  try {
    pk=(JournalContentSearchPK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((portletId.equals(pk.portletId)) && (layoutId.equals(pk.layoutId)) && (ownerId.equals(pk.ownerId))&& (articleId.equals(pk.articleId))) {
    return true;
  }
 else {
    return false;
  }
}

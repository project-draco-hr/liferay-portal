{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(BTEntry.class,BTEntry.class.getClassLoader());
  dynamicQuery.add(RestrictionsFactoryUtil.eq("btEntryId",ServiceTestUtil.nextLong()));
  List<BTEntry> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

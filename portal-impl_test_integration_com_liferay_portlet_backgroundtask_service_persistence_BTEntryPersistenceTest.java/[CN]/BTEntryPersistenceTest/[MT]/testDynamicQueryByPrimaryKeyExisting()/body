{
  BTEntry newBTEntry=addBTEntry();
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(BTEntry.class,BTEntry.class.getClassLoader());
  dynamicQuery.add(RestrictionsFactoryUtil.eq("btEntryId",newBTEntry.getBtEntryId()));
  List<BTEntry> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(1,result.size());
  BTEntry existingBTEntry=result.get(0);
  Assert.assertEquals(existingBTEntry,newBTEntry);
}

{
  List<MBBan> bans=null;
  try {
    bans=MBBanLocalServiceUtil.getBans(group.getGroupId(),0,1);
    if (!bans.isEmpty()) {
      return bans.get(0);
    }
  }
 catch (  Exception e) {
  }
  return null;
}

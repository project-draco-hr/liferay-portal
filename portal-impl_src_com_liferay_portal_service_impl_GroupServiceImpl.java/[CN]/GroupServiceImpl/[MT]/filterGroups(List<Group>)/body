{
  List<Group> filteredGroups=new ArrayList<Group>();
  for (  Group group : groups) {
    if (GroupPermissionUtil.contains(getPermissionChecker(),group.getGroupId(),ActionKeys.VIEW)) {
      filteredGroups.add(group);
    }
  }
  return filteredGroups;
}

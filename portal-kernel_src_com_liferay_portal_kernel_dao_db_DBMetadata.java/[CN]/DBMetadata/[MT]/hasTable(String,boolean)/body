{
  if (caseSensitive) {
    if (doHasTable(tableName)) {
      return true;
    }
    return false;
  }
  if (doHasTable(StringUtil.toLowerCase(tableName)) || doHasTable(StringUtil.toUpperCase(tableName)) || doHasTable(tableName)) {
    return true;
  }
  return false;
}

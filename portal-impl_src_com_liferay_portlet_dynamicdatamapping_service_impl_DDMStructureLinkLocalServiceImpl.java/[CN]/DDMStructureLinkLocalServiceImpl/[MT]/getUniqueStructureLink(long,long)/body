{
  List<DDMStructureLink> structureLinks=ddmStructureLinkPersistence.findByC_C(classNameId,classPK);
  if (structureLinks.isEmpty()) {
    throw new NoSuchStructureLinkException();
  }
  return structureLinks.get(0);
}

{
  long repositoryId=getInternalRepositoryId(folderId,fileEntryId,fileVersionId);
  if (repositoryId != 0) {
    return repositoryId;
  }
  repositoryId=getExternalRepositoryId(folderId,fileEntryId,fileVersionId);
  if (repositoryId == 0) {
    throw new InvalidRepositoryIdException(String.format("No DLFolder or RepositoryEntry found with folderId = %s",folderId));
  }
  return repositoryId;
}

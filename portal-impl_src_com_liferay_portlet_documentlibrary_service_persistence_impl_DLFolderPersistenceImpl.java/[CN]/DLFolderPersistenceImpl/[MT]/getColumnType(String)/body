{
  Integer type=DLFolderModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DLFolderModelImpl.TABLE_NAME);
  }
  return type;
}

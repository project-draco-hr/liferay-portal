{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ClusterNodeStatus)) {
    return false;
  }
  ClusterNodeStatus clusterNodeStatus=(ClusterNodeStatus)obj;
  if (_clusterNode.equals(clusterNodeStatus._clusterNode) && _address.equals(clusterNodeStatus._address)) {
    return true;
  }
  return false;
}

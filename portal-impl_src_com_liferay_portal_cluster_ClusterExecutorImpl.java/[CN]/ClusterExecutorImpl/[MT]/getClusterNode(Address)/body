{
  _readLock.lock();
  ClusterNode clusterNode=null;
  try {
    if (_addressMap.containsKey(address)) {
      clusterNode=_addressMap.get(address);
    }
  }
  finally {
    _readLock.unlock();
  }
  return clusterNode;
}

{
  ClusterRequest clusterRequest=ClusterRequest.createClusterRequest(_localClusterNode);
  try {
    _controlJChannel.send(null,clusterRequest);
  }
 catch (  Exception e) {
    _log.error("Unable to send notify message",e);
  }
}

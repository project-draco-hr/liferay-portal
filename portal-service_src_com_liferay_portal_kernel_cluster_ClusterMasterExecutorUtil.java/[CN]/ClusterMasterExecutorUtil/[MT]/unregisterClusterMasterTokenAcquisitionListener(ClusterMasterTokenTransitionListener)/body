{
  ClusterMasterExecutor clusterMasterExecutor=getClusterMasterExecutor();
  if (clusterMasterExecutor == null) {
    return;
  }
  _clusterMasterExecutor.unregisterClusterMasterTokenAcquisitionListener(clusterMasterTokenAcquisitionListener);
}

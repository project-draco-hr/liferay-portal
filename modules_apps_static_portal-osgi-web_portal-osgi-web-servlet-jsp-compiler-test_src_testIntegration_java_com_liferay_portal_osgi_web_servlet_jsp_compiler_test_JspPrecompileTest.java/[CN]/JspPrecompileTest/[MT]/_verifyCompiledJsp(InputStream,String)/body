{
  try (InputStreamReader inputStreamReader=new InputStreamReader(inputStream);UnsyncBufferedReader unsyncBufferedReader=new UnsyncBufferedReader(inputStreamReader)){
    String line=null;
    while (true) {
      line=unsyncBufferedReader.readLine();
      Assert.assertNotNull(line);
      if (line.contains(string)) {
        return;
      }
    }
  }
 }

{
  clusterLinkConfiguration=Configurable.createConfigurable(ClusterLinkConfiguration.class,properties);
  if (!clusterLinkConfiguration.enabled() && (_clusterEventListener != null)) {
    deactivate();
  }
 else   if (clusterLinkConfiguration.enabled() && (_clusterEventListener == null)) {
    initialize();
  }
}

{
  if (!_fileEntryDisplayContextHelper.isCheckinActionAvailable()) {
    return;
  }
  _addJavaScriptUIItem(new JavaScriptToolbarItem(),toolbarItems,"icon-lock",DLUIItemKeys.CHECKIN,LanguageUtil.get(_request,"checkin"),getSubmitFormJavaScript(Constants.CHECKIN,null));
}

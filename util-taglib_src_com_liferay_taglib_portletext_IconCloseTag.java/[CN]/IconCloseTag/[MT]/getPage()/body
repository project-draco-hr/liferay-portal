{
  if (FileAvailabilityUtil.isAvailable(getServletContext(),_PAGE)) {
    return _PAGE;
  }
  PortletDisplay portletDisplay=(PortletDisplay)pageContext.getAttribute("portletDisplay");
  if (!portletDisplay.isShowCloseIcon()) {
    return null;
  }
  setCssClass("portlet-close portlet-close-icon");
  setImage("../portlet/close");
  setMessage("remove");
  String onClick="Liferay.Portlet.close('#p_p_id_".concat(portletDisplay.getId()).concat("_'); return false;");
  setOnClick(onClick);
  setToolTip(false);
  setUrl(portletDisplay.getURLClose());
  return super.getPage();
}

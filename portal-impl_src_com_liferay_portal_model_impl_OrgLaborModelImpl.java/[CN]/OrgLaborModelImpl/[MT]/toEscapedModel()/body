{
  if (isEscapedModel()) {
    return (OrgLabor)this;
  }
 else {
    return (OrgLabor)Proxy.newProxyInstance(OrgLabor.class.getClassLoader(),new Class[]{OrgLabor.class},new AutoEscapeBeanHandler(this));
  }
}

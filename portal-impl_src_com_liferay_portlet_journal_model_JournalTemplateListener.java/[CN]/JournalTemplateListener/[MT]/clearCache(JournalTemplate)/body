{
  JournalContentUtil.clearCache();
  CacheUtil.clearCache(template.getCompanyId());
  MultiVMKeyPoolUtil.remove(StringResourceRepository.class.getName(),template.getCompanyId() + template.getGroupId() + template.getTemplateId());
}

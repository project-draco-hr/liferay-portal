{
  if (!requestInterpreter.hasClassName(className)) {
    return false;
  }
  String requestPortletId=getSocialRequestPortletId(request);
  if (Validator.isNull(requestPortletId) || requestInterpreter.getPortletId().equals(requestPortletId)) {
    return true;
  }
  return false;
}

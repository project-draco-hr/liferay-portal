{
  JournalArticle article=_journalArticleLocalService.getLatestArticle(classPK);
  if ((article.getFolderId() > 0) && (_journalFolderLocalService.fetchFolder(article.getFolderId()) == null)) {
    return false;
  }
  return !article.isInTrashContainer();
}

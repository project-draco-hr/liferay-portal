{
  try {
    WikiPage page=WikiPageLocalServiceUtil.getPage(_classPK);
    return PortletFileRepositoryUtil.getPortletFileEntry(themeDisplay.getScopeGroupId(),page.getAttachmentsFolderId(),fileName);
  }
 catch (  PortalException pe) {
    return null;
  }
}

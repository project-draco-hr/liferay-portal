{
  super.processMailMessage(mailMessage,locale);
  if (htmlFormat) {
    try {
      String processedBody=BBCodeTranslatorUtil.getHTML(mailMessage.getBody());
      mailMessage.setBody(processedBody);
    }
 catch (    Exception e) {
      _log.error("Could not parse message " + mailId + " "+ e.getMessage());
    }
  }
}

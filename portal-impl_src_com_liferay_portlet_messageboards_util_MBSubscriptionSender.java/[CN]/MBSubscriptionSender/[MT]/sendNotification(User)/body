{
  sendEmailNotification(user);
  if (creatorUserId == user.getUserId()) {
    if (_log.isDebugEnabled()) {
      _log.debug("Skip notification for context user " + creatorUserId);
    }
    return;
  }
  sendUserNotification(user);
}

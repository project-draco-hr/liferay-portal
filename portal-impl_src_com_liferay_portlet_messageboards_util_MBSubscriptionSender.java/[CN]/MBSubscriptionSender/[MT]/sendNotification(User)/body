{
  sendEmailNotification(user);
  if (contextUserId == user.getUserId()) {
    if (_log.isDebugEnabled()) {
      _log.debug("Skip notification for context user " + contextUserId);
    }
    return;
  }
  sendUserNotification(user);
}

{
  if (((format == null) && (_format != null)) || ((format != null) && (_format == null)) || ((format != null) && (_format != null) && !format.equals(_format))) {
    if (!XSS_ALLOW_FORMAT) {
      format=XSSUtil.strip(format);
    }
    _format=format;
    setModified(true);
  }
}

{
  AssetCategory assetCategory=(AssetCategory)obj;
  Document document=new DocumentImpl();
  document.addUID(PORTLET_ID,assetCategory.getCategoryId());
  SearchEngineUtil.deleteDocument(getSearchEngineId(),assetCategory.getCompanyId(),document.get(Field.UID),isCommitImmediately());
}

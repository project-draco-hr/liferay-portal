{
  Document document=getDocument(assetCategory);
  if (document != null) {
    SearchEngineUtil.updateDocument(getSearchEngineId(),assetCategory.getCompanyId(),document,isCommitImmediately());
  }
}

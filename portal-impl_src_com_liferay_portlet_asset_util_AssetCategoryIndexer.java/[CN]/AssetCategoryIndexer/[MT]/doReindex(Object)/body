{
  AssetCategory assetCategory=(AssetCategory)obj;
  Document document=getDocument(assetCategory);
  if (document != null) {
    SearchEngineUtil.updateDocument(getSearchEngineId(),assetCategory.getCompanyId(),document);
  }
}

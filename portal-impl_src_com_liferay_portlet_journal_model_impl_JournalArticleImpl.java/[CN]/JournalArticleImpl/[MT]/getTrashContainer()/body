{
  JournalFolder folder=null;
  try {
    folder=getFolder();
  }
 catch (  NoSuchFolderException nsfe) {
    return null;
  }
  if (folder.isInTrash()) {
    return folder;
  }
  return folder.getTrashContainer();
}

{
  Set<String> availableLanguageIds=SetUtil.fromArray(super.getAvailableLanguageIds());
  String[] availableContentLanguageIdsArray=LocalizationUtil.getAvailableLanguageIds(getContent());
  for (  String availableLanguageId : availableContentLanguageIdsArray) {
    availableLanguageIds.add(availableLanguageId);
  }
  return availableLanguageIds.toArray(new String[availableLanguageIds.size()]);
}

{
  String connectionReleaseMode=PropsUtil.get(Environment.RELEASE_CONNECTIONS);
  if (Validator.isNotNull(connectionReleaseMode)) {
    cfg.setProperty(Environment.RELEASE_CONNECTIONS,connectionReleaseMode);
  }
}

{
  JournalFeed journalFeed=(JournalFeed)EntityCacheUtil.getResult(JournalFeedModelImpl.ENTITY_CACHE_ENABLED,JournalFeedImpl.class,id,this);
  if (journalFeed == _nullJournalFeed) {
    return null;
  }
  if (journalFeed == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      journalFeed=(JournalFeed)session.get(JournalFeedImpl.class,Long.valueOf(id));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (!hasException && (journalFeed == null)) {
        EntityCacheUtil.putResult(JournalFeedModelImpl.ENTITY_CACHE_ENABLED,JournalFeedImpl.class,id,_nullJournalFeed);
      }
 else {
        cacheResult(journalFeed);
      }
      closeSession(session);
    }
  }
  return journalFeed;
}

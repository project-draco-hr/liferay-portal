{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalFeed journalFeed) method. To improve performance, call update(JournalFeed journalFeed, boolean merge).");
  }
  return update(journalFeed,false);
}

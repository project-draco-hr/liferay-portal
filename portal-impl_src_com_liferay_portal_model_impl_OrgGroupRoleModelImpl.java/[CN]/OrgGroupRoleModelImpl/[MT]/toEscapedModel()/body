{
  if (isEscapedModel()) {
    return (OrgGroupRole)this;
  }
 else {
    return (OrgGroupRole)Proxy.newProxyInstance(OrgGroupRole.class.getClassLoader(),new Class[]{OrgGroupRole.class},new AutoEscapeBeanHandler(this));
  }
}

{
  OrgGroupRole model=new OrgGroupRoleImpl();
  model.setOrganizationId(getOrganizationId());
  model.setGroupId(getGroupId());
  model.setRoleId(getRoleId());
  if (true) {
    model=(OrgGroupRole)Proxy.newProxyInstance(OrgGroupRole.class.getClassLoader(),new Class[]{OrgGroupRole.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

{
  ConcurrentHashMap<String,Lock> lockGroup=_lockGroupMap.remove(groupName);
  if (lockGroup == null) {
    return null;
  }
  if (unlock == true) {
    for (    Lock lock : lockGroup.values()) {
      lock.unlock();
    }
  }
  return lockGroup;
}

{
  if (unlock == true) {
    for (    Map<String,Lock> lockGroup : _lockGroupMap.values()) {
      for (      Lock lock : lockGroup.values()) {
        lock.unlock();
      }
    }
  }
  _lockGroupMap.clear();
}

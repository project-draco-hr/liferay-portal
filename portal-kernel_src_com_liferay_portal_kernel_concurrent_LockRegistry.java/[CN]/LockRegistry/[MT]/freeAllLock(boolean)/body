{
  if (unlock == true) {
    for (    Map<String,ReentrantLock> lockGroup : _lockGroupMap.values()) {
      for (      ReentrantLock lock : lockGroup.values()) {
        lock.unlock();
      }
    }
  }
  _lockGroupMap.clear();
  _prematureLockReleases.clear();
}

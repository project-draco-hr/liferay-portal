{
  Map<String,Lock> lockGroup=_lockGroupMap.get(groupName);
  if (lockGroup == null) {
    return null;
  }
  Lock lock=lockGroup.remove(key);
  if (lock == null) {
    return null;
  }
  if (unlock) {
    lock.unlock();
  }
  return lock;
}

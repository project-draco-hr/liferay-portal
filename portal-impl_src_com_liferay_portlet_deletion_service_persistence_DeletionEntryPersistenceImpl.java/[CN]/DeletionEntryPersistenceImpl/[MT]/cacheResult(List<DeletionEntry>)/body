{
  for (  DeletionEntry deletionEntry : deletionEntries) {
    if (EntityCacheUtil.getResult(DeletionEntryModelImpl.ENTITY_CACHE_ENABLED,DeletionEntryImpl.class,deletionEntry.getPrimaryKey(),this) == null) {
      cacheResult(deletionEntry);
    }
  }
}

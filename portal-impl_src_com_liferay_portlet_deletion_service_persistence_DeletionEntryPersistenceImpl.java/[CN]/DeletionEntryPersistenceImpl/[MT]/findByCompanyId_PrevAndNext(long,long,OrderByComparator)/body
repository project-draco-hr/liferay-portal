{
  DeletionEntry deletionEntry=findByPrimaryKey(entryId);
  Session session=null;
  try {
    session=openSession();
    DeletionEntry[] array=new DeletionEntryImpl[3];
    array[0]=getByCompanyId_PrevAndNext(session,deletionEntry,companyId,orderByComparator,true);
    array[1]=deletionEntry;
    array[2]=getByCompanyId_PrevAndNext(session,deletionEntry,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

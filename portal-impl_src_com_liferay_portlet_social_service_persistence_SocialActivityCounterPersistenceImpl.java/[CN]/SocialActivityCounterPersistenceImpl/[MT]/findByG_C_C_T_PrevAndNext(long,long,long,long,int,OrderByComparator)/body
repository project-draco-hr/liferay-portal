{
  SocialActivityCounter socialActivityCounter=findByPrimaryKey(activityCounterId);
  Session session=null;
  try {
    session=openSession();
    SocialActivityCounter[] array=new SocialActivityCounterImpl[3];
    array[0]=getByG_C_C_T_PrevAndNext(session,socialActivityCounter,groupId,classNameId,classPK,type,orderByComparator,true);
    array[1]=socialActivityCounter;
    array[2]=getByG_C_C_T_PrevAndNext(session,socialActivityCounter,groupId,classNameId,classPK,type,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

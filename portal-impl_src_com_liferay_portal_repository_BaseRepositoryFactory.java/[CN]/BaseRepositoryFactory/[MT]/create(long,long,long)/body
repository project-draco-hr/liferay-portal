{
  T liferayRepository=createInternalRepository(folderId,fileEntryId,fileVersionId);
  if (liferayRepository != null) {
    return liferayRepository;
  }
  return createExternalRepository(folderId,fileEntryId,fileVersionId);
}

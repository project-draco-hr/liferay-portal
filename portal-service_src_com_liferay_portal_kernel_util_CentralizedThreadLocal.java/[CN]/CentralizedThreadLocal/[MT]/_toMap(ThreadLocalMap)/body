{
  Map<CentralizedThreadLocal<?>,Object> map=new HashMap<CentralizedThreadLocal<?>,Object>(threadLocals._table.length);
  for (  Entry entry : threadLocals._table) {
    map.put(entry._key,entry._value);
  }
  return map;
}

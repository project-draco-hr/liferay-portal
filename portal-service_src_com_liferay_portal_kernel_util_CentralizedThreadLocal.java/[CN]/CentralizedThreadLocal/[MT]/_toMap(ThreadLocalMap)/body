{
  Map<CentralizedThreadLocal<?>,Object> map=new HashMap<CentralizedThreadLocal<?>,Object>(threadLocalMap._table.length);
  for (  Entry entry : threadLocalMap._table) {
    map.put(entry._key,entry._value);
  }
  return map;
}

{
  if (value == null) {
    return;
  }
  if (value instanceof Closeable) {
    Closeable closable=(Closeable)value;
    try {
      closable.close();
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
}

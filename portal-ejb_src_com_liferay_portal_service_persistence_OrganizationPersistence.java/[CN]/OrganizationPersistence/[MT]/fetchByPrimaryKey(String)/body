{
  Session session=null;
  try {
    session=openSession();
    return (Organization)session.get(OrganizationImpl.class,organizationId);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

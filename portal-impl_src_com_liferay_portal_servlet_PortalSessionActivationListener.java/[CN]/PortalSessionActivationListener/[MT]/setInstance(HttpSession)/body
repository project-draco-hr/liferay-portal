{
  TransientValue<PortalSessionActivationListener> transientValue=new TransientValue<>(PortalSessionActivationListener.getInstance());
  session.setAttribute(PortalSessionActivationListener.class.getName(),transientValue);
}

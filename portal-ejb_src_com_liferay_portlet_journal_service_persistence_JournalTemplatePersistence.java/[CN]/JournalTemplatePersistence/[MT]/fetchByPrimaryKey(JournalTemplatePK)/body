{
  Session session=null;
  try {
    session=openSession();
    return (JournalTemplate)session.get(JournalTemplateImpl.class,journalTemplatePK);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

{
  SchedulerResponse schedulerResponse=_quartzSchedulerEngine.getScheduledJob(_TEST_JOB_NAME_0,_MEMORY_TEST_GROUP_NAME);
  Message message=schedulerResponse.getMessage();
  JobState jobState=(JobState)message.get(SchedulerEngine.JOB_STATE);
  Assert.assertNotNull(jobState.getExceptions());
  _quartzSchedulerEngine.suppressError(_TEST_JOB_NAME_0,_MEMORY_TEST_GROUP_NAME);
  schedulerResponse=_quartzSchedulerEngine.getScheduledJob(_TEST_JOB_NAME_0,_MEMORY_TEST_GROUP_NAME);
  message=schedulerResponse.getMessage();
  jobState=(JobState)message.get(SchedulerEngine.JOB_STATE);
  Assert.assertNull(jobState.getExceptions());
}

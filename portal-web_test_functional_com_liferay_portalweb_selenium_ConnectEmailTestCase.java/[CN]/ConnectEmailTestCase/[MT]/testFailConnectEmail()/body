{
  try {
    selenium.connectToEmailAccount("ThisIsNotCorrectEmailAddress","ThisIsNotCorrectEmailPassword");
  }
 catch (  Throwable t) {
    assertTrue(t.getMessage().contains("Invalid credentials"));
  }
}

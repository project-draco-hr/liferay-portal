{
  if (Validator.isNull(_statusCallback)) {
    _statusCallback=PrefsPropsUtil.getString(PortletPropsKeys.SMS_TWILIO_STATUS_CALLBACK,PortletPropsValues.SMS_TWILIO_STATUS_CALLBACK);
  }
  return _statusCallback;
}

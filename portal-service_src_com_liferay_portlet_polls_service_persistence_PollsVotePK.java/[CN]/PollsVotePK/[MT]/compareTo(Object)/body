{
  if (obj == null) {
    return -1;
  }
  PollsVotePK pk=(PollsVotePK)obj;
  int value=0;
  value=questionId.compareTo(pk.questionId);
  if (value != 0) {
    return value;
  }
  if (userId < pk.userId) {
    value=-1;
  }
 else   if (userId > pk.userId) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  return 0;
}

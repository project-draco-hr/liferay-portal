{
  List<SCProductVersion> results=SCProductVersionLocalServiceUtil.getProductVersions(getProductEntryId(),0,1);
  SCProductVersion lastVersion=null;
  if (!results.isEmpty()) {
    lastVersion=results.get(0);
  }
  return lastVersion;
}

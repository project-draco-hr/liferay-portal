{
  int count=countByC_T(classNameId,templateId);
  if (count == 0) {
    return null;
  }
  List<JournalArticle> list=findByC_T(classNameId,templateId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  if (isEscapedModel()) {
    return (Counter)this;
  }
 else {
    return (Counter)Proxy.newProxyInstance(Counter.class.getClassLoader(),new Class[]{Counter.class},new AutoEscapeBeanHandler(this));
  }
}

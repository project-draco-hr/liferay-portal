{
  CounterImpl counterImpl=new CounterImpl();
  CounterModelImpl counterModelImpl=(CounterModelImpl)counterImpl;
  counterImpl.setName(getName());
  counterImpl.setCurrentId(getCurrentId());
  return counterImpl;
}

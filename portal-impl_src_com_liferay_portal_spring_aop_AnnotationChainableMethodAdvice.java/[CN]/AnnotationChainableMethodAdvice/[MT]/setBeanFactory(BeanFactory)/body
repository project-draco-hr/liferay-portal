{
  if (beanFactory == null) {
    _beanFactoryAnnotations.remove(_beanFactory);
  }
 else {
    _beanFactory=beanFactory;
    _beanFactoryAnnotations.putIfAbsent(_beanFactory,new ConcurrentHashMap<MethodTargetClassKey,Annotation[]>());
  }
}

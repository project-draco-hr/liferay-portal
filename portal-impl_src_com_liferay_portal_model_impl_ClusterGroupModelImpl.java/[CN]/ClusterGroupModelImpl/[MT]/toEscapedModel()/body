{
  if (isEscapedModel()) {
    return (ClusterGroup)this;
  }
 else {
    return (ClusterGroup)Proxy.newProxyInstance(ClusterGroup.class.getClassLoader(),new Class[]{ClusterGroup.class},new AutoEscapeBeanHandler(this));
  }
}

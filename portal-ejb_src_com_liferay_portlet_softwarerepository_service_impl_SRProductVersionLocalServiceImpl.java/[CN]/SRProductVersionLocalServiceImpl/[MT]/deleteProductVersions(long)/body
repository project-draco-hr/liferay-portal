{
  Iterator itr=SRProductVersionUtil.findByProductEntryId(productEntryId).iterator();
  while (itr.hasNext()) {
    SRProductVersion productVersion=(SRProductVersion)itr.next();
    deleteProductVersion(productVersion);
  }
}

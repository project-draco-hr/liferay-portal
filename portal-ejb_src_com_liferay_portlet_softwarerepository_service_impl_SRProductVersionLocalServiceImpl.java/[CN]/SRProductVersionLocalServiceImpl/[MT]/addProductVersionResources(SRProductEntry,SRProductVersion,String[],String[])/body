{
  ResourceLocalServiceUtil.addModelResources(productVersion.getCompanyId(),productEntry.getGroupId(),productVersion.getUserId(),SRProductVersion.class.getName(),productVersion.getPrimaryKey(),communityPermissions,guestPermissions);
}

{
  Set<Map.Entry<Object,Object>> entrySet=new TreeSet<Map.Entry<Object,Object>>(new Comparator<Map.Entry<Object,Object>>(){
    @Override public int compare(    Map.Entry<Object,Object> o1,    Map.Entry<Object,Object> o2){
      String key1=String.valueOf(o1.getKey());
      String key2=String.valueOf(o2.getKey());
      if (_comparator == null) {
        return key1.compareTo(key2);
      }
      return _comparator.compare(key1,key2);
    }
  }
);
  entrySet.addAll(super.entrySet());
  return entrySet;
}

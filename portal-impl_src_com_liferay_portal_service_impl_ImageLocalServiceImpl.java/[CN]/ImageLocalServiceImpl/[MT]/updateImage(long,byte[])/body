{
  Image image=null;
  try {
    image=ImageToolUtil.getImage(bytes);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
  return updateImage(imageId,image.getTextObj(),image.getType(),image.getHeight(),image.getWidth(),image.getSize());
}

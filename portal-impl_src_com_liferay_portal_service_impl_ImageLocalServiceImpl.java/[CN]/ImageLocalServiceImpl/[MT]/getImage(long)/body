{
  try {
    if (imageId > 0) {
      return imagePersistence.findByPrimaryKey(imageId);
    }
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn("Unable to get image " + imageId + ": "+ e.getMessage());
    }
  }
  return null;
}

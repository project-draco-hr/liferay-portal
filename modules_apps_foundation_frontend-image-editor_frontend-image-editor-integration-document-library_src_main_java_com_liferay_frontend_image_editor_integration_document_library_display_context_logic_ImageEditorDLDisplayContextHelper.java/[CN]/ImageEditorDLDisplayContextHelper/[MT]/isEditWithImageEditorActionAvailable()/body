{
  if (_isEditWithImageEditorActionAvailable == null) {
    _isEditWithImageEditorActionAvailable=DLFileEntryPermission.contains(_themeDisplay.getPermissionChecker(),_fileEntry,ActionKeys.UPDATE) && (!_fileEntry.isCheckedOut() || _fileEntry.hasLock());
  }
  return _isEditWithImageEditorActionAvailable;
}

{
  Integer type=AssetTagStatsModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ AssetTagStatsModelImpl.TABLE_NAME);
  }
  return type;
}

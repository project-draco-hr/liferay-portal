{
  Serializable serializable=entityCache.getResult(AssetTagStatsModelImpl.ENTITY_CACHE_ENABLED,AssetTagStatsImpl.class,primaryKey);
  if (serializable == nullModel) {
    return null;
  }
  AssetTagStats assetTagStats=(AssetTagStats)serializable;
  if (assetTagStats == null) {
    Session session=null;
    try {
      session=openSession();
      assetTagStats=(AssetTagStats)session.get(AssetTagStatsImpl.class,primaryKey);
      if (assetTagStats != null) {
        cacheResult(assetTagStats);
      }
 else {
        entityCache.putResult(AssetTagStatsModelImpl.ENTITY_CACHE_ENABLED,AssetTagStatsImpl.class,primaryKey,nullModel);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(AssetTagStatsModelImpl.ENTITY_CACHE_ENABLED,AssetTagStatsImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return assetTagStats;
}

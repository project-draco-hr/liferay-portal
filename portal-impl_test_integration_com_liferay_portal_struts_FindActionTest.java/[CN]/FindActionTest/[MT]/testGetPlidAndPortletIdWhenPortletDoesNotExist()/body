{
  addLayouts(false,false);
  try {
    BaseFindActionHelper.getPlidAndPortletId(getThemeDisplay(),_blogsEntry.getGroupId(),_assetLayout.getPlid(),_PORTLET_IDS);
    Assert.fail();
  }
 catch (  NoSuchLayoutException nsle) {
  }
}

{
  addLayouts(false);
  try {
    FindAction.getPlidAndPortletId(getThemeDisplay(),_blogsEntry.getGroupId(),_assetLayout.getPlid(),_PORTLET_IDS);
    Assert.fail("The page should have not be found");
  }
 catch (  NoSuchLayoutException nsle) {
  }
}

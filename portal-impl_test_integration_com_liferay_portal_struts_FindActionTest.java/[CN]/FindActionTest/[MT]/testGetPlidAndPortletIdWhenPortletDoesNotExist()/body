{
  createPages(false);
  try {
    FindAction.getPlidAndPortletId(createThemeDisplay(),_blogsEntry.getGroupId(),_assetLayout.getPlid(),_PORTLET_IDS);
    Assert.fail("The page should have not be found");
  }
 catch (  NoSuchLayoutException nsle) {
  }
}

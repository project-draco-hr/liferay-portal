{
  createPages(false);
  try {
    FindAction.getPlidAndPortletId(createThemeDisplay(),_blogsEntry.getGroupId(),_asset.getPlid(),_portletIds);
    Assert.fail("The page should have not be found");
  }
 catch (  NoSuchLayoutException nsle) {
  }
}

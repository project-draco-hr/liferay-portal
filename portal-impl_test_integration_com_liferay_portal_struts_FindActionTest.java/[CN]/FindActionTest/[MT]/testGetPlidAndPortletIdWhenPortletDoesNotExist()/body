{
  addLayouts(false,false);
  try {
    BaseFindActionHelper.getPlidAndPortletId(getThemeDisplay(),_blogsEntry.getGroupId(),_assetLayout.getPlid(),_portletIds);
    Assert.fail();
  }
 catch (  NoSuchLayoutException nsle) {
  }
}

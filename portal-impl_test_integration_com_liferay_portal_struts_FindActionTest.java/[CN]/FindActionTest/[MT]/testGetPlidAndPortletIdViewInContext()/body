{
  createPages(true);
  Object[] plidAndPorltetId=FindAction.getPlidAndPortletId(createThemeDisplay(),_blogsEntry.getGroupId(),_asset.getPlid(),_portletIds);
  Assert.assertEquals(_blog.getPlid(),plidAndPorltetId[0]);
  Assert.assertEquals(PortletKeys.BLOGS,plidAndPorltetId[1]);
}

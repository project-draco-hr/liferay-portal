{
  Set<Long> newUserPKsSet=SetUtil.fromArray(userPKs);
  Set<Long> oldUserPKsSet=SetUtil.fromArray(roleToUserTableMapper.getRightPrimaryKeys(companyProvider.getCompanyId(),pk));
  Set<Long> removeUserPKsSet=new HashSet<Long>(oldUserPKsSet);
  removeUserPKsSet.removeAll(newUserPKsSet);
  for (  long removeUserPK : removeUserPKsSet) {
    roleToUserTableMapper.deleteTableMapping(companyProvider.getCompanyId(),pk,removeUserPK);
  }
  newUserPKsSet.removeAll(oldUserPKsSet);
  for (  long newUserPK : newUserPKsSet) {
    roleToUserTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,newUserPK);
  }
}

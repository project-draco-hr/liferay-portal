{
  Role role=fetchByPrimaryKey(pk);
  if (role == null) {
    roleToGroupTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,group.getPrimaryKey());
  }
 else {
    roleToGroupTableMapper.addTableMapping(role.getCompanyId(),pk,group.getPrimaryKey());
  }
}

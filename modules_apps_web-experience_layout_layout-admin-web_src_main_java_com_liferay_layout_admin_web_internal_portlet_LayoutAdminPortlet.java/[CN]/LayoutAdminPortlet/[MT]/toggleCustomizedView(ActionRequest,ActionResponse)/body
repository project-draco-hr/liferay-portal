{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  actionRequest.setAttribute(WebKeys.REDIRECT,PortalUtil.getLayoutURL(themeDisplay.getLayout(),themeDisplay));
}

{
  ActionResponseImpl actionResponseImpl=new ActionResponseImpl();
  actionResponseImpl.init(actionRequestImpl,response,portletName,user,layout,windowState,portletMode);
  return actionResponseImpl;
}

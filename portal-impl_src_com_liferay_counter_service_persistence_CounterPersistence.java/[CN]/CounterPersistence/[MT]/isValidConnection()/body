{
  try {
    PreparedStatement preparedStatement=_connection.prepareStatement(_COUNT_COUNTER);
    preparedStatement.execute();
  }
 catch (  SQLException e) {
    DataAccess.cleanUp(_connection);
    if (_log.isDebugEnabled()) {
      _log.debug(e,e);
    }
    return false;
  }
  return true;
}

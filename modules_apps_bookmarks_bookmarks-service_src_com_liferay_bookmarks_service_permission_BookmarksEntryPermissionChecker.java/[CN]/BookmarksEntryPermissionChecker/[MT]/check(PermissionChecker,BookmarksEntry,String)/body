{
  if (!contains(permissionChecker,entry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),BookmarksEntry.class.getName(),entry.getEntryId(),actionId);
  }
}

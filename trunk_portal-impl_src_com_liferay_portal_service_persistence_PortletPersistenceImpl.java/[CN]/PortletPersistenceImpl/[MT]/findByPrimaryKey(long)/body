{
  Portlet portlet=fetchByPrimaryKey(id);
  if (portlet == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + id);
    }
    throw new NoSuchPortletException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + id);
  }
  return portlet;
}

{
  PortletStatistics portletStats=_portletStatisticsByPortletId.get(portletId);
  if (portletStats == null) {
    throw new MonitoringException("No portlet found with id: " + portletId);
  }
  return portletStats.getEventStatistics();
}

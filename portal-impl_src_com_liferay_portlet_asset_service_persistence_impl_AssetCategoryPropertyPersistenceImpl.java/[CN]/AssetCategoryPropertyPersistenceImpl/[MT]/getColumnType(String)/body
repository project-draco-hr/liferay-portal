{
  Integer type=AssetCategoryPropertyModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ AssetCategoryPropertyModelImpl.TABLE_NAME);
  }
  return type;
}

{
  try {
    _persistence.countByG_CPK(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
    _persistence.countByG_CPK(0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

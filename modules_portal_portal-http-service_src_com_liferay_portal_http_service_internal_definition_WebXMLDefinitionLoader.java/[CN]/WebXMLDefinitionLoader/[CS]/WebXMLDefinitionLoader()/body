{
  Class<?> clazz=getClass();
  ClassLoader classLoader=clazz.getClassLoader();
  Document document=SAXReaderUtil.read(classLoader.getResource(_DEFAULT_WEB_PATH));
  _defaultWebXmlRootElement=document.getRootElement();
}

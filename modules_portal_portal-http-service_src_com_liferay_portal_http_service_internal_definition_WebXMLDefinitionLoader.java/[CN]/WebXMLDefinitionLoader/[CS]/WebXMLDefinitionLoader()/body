{
  Class<?> clazz=getClass();
  ClassLoader classLoader=clazz.getClassLoader();
  Document document=SAXReaderUtil.read(classLoader.getResource("com/liferay/portal/http/service/internal" + "/dependencies/default-web.xml"));
  _defaultWebXmlRootElement=document.getRootElement();
}

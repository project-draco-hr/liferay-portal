{
  List<String> dispatchers=new ArrayList<String>();
  for (  Element dispatcherElement : element.elements("dispatcher")) {
    dispatchers.add(StringUtil.toUpperCase(dispatcherElement.getTextTrim()));
  }
  return dispatchers;
}

{
  if ((mdrRuleGroupElements == null) || mdrRuleGroupElements.isEmpty()) {
    return;
  }
  for (  Element mdrRuleGroupElement : mdrRuleGroupElements) {
    String path=mdrRuleGroupElement.attributeValue("path");
    if (!portletDataContext.isPathNotProcessed(path)) {
      continue;
    }
    MDRRuleGroup mdrRuleGroup=(MDRRuleGroup)portletDataContext.getZipEntryAsObject(path);
    importMDRRuleGroup(portletDataContext,groupId,path,mdrRuleGroup,mdrRuleGroupElement);
  }
}

{
  if (obj == null) {
    return -1;
  }
  SRFrameworkVersionImpl srFrameworkVersion=(SRFrameworkVersionImpl)obj;
  int value=0;
  if (getPriority() < srFrameworkVersion.getPriority()) {
    value=-1;
  }
 else   if (getPriority() > srFrameworkVersion.getPriority()) {
    value=1;
  }
 else {
    value=0;
  }
  if (value != 0) {
    return value;
  }
  value=getName().compareTo(srFrameworkVersion.getName());
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  boolean defaultLDAPPasswordPolicyEnabled=GetterUtil.get(PropsUtil.get(PropsKeys.LDAP_PASSWORD_POLICY_ENABLED),false);
  if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.LDAP_PASSWORD_POLICY_ENABLED,defaultLDAPPasswordPolicyEnabled)) {
    return true;
  }
 else {
    return false;
  }
}

{
  int count=countByG_NotL(groupId,layoutUuid);
  if (count == 0) {
    return null;
  }
  List<JournalArticle> list=findByG_NotL(groupId,layoutUuid,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

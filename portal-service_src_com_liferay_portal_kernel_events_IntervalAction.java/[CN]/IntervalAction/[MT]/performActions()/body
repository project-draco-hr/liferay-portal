{
  int pages=_total / _interval;
  for (int i=0; i <= pages; i++) {
    int end=_start + _interval;
    if (end > _total) {
      end=_total;
      if (end == _start) {
        break;
      }
    }
    performActions(_start,end);
  }
}

{
  if (isEscapedModel()) {
    return (ResourceAction)this;
  }
 else {
    return (ResourceAction)Proxy.newProxyInstance(ResourceAction.class.getClassLoader(),new Class[]{ResourceAction.class},new AutoEscapeBeanHandler(this));
  }
}

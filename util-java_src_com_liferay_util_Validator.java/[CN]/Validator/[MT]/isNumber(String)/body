{
  if (isNull(number)) {
    return false;
  }
  char[] c=number.toCharArray();
  if (c.length > 0) {
    if (!isDigit(c[0]) && c[0] != '-') {
      return false;
    }
  }
  for (int i=1; i < c.length; i++) {
    if (!isDigit(c[i])) {
      return false;
    }
  }
  return true;
}

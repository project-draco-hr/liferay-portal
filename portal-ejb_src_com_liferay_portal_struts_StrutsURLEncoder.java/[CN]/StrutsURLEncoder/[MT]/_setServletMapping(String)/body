{
  if (servletMapping != null) {
    if (servletMapping.endsWith("/*")) {
      int pos=0;
      if (servletMapping.startsWith(_mainPath)) {
        pos=_mainPath.length() - 1;
      }
      _servletMapping=servletMapping.substring(pos,servletMapping.length() - 1);
    }
  }
}

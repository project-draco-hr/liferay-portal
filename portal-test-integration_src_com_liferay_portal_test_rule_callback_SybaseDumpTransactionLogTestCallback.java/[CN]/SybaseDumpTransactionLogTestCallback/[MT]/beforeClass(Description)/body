{
  SybaseDumpTransactionLog sybaseDumpTransactionLog=description.getAnnotation(SybaseDumpTransactionLog.class);
  if (sybaseDumpTransactionLog != null) {
    SybaseDump sybaseDump=sybaseDumpTransactionLog.dumpBefore();
    if (!sybaseDump.equals(SybaseDump.CLASS) && !sybaseDump.equals(SybaseDump.CLASS_AND_METHOD)) {
      return null;
    }
  }
  dumpTransactionLog();
  return null;
}

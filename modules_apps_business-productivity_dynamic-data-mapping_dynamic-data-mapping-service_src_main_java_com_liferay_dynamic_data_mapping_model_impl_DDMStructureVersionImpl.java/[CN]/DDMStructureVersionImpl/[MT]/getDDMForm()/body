{
  if (_ddmForm == null) {
    try {
      _ddmForm=DDMStructureVersionLocalServiceUtil.deserialize(getDefinition());
    }
 catch (    Exception e) {
      _log.error(e,e);
      return new DDMForm();
    }
  }
  return new DDMForm(_ddmForm);
}

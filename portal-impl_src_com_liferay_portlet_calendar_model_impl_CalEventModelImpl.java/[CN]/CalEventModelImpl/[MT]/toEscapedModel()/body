{
  if (isEscapedModel()) {
    return (CalEvent)this;
  }
 else {
    return (CalEvent)Proxy.newProxyInstance(CalEvent.class.getClassLoader(),new Class[]{CalEvent.class},new AutoEscapeBeanHandler(this));
  }
}

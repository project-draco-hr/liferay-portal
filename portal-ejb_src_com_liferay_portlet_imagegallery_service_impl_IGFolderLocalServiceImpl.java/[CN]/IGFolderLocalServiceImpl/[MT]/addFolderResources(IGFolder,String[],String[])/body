{
  ResourceLocalServiceUtil.addModelResources(folder.getCompanyId(),folder.getGroupId(),folder.getUserId(),IGFolder.class.getName(),folder.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

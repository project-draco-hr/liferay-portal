{
  PollsChoice existingChoice=PollsChoiceLocalServiceUtil.fetchPollsChoiceByUuidAndGroupId(uuid,portletDataContext.getCompanyGroupId());
  Map<Long,Long> choiceIds=(Map<Long,Long>)portletDataContext.getNewPrimaryKeysMap(PollsChoice.class);
  choiceIds.put(choiceId,existingChoice.getChoiceId());
}

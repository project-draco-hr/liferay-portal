{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoRow expandoRow) method. Use update(ExpandoRow expandoRow, boolean merge) instead.");
  }
  return update(expandoRow,false);
}

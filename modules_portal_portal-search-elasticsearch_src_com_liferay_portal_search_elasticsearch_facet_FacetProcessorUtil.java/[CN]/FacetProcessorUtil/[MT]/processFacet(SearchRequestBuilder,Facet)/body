{
  FacetProcessor facetProcessor=_facetProcessors.get(facet.getClass().getName());
  if (facetProcessor == null) {
    facetProcessor=_defaultFacetProcessor;
  }
  facetProcessor.processFacet(searchRequestBuilder,facet);
}

{
  try {
    Role role=RoleLocalServiceUtil.getRole(getCompanyId(),RoleImpl.POWER_USER);
    return RoleLocalServiceUtil.hasUserRole(getUserId(),getCompanyId(),RoleImpl.POWER_USER,true);
  }
 catch (  Exception e) {
    return false;
  }
}

{
  if (isDefaultUser()) {
    return false;
  }
  int max=PropsValues.MY_SITES_MAX_ELEMENTS;
  if (max == 1) {
    max++;
  }
  List<Group> groups=getMySiteGroups(max);
  return !groups.isEmpty();
}

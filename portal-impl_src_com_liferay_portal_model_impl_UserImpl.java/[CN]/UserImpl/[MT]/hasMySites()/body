{
  if (isDefaultUser()) {
    return false;
  }
  int max=PropsValues.MY_SITES_MAX_ELEMENTS;
  if (max == 1) {
    max++;
  }
  List<Group> groups=getMySites(true,max);
  if (groups.size() > 0) {
    return true;
  }
 else {
    return false;
  }
}

{
  String emailAddress=super.getEmailAddress();
  EmailAddressGenerator emailAddressGenerator=(EmailAddressGenerator)EmailAddressGeneratorFactory.getInstance();
  if (emailAddressGenerator.isFake(emailAddress)) {
    emailAddress=StringPool.BLANK;
  }
  return emailAddress;
}

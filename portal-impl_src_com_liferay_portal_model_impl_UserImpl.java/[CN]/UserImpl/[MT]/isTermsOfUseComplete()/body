{
  if (isDefaultUser()) {
    return true;
  }
  boolean termsOfUseRequired=PrefsPropsUtil.getBoolean(getCompanyId(),PropsKeys.TERMS_OF_USE_REQUIRED,PropsValues.TERMS_OF_USE_REQUIRED);
  if (termsOfUseRequired) {
    return super.isAgreedToTermsOfUse();
  }
  return true;
}

{
  try {
    ExpandoTable table=null;
    try {
      table=ExpandoTableLocalServiceUtil.getDefaultTable(User.class.getName());
    }
 catch (    NoSuchTableException nste) {
      table=ExpandoTableLocalServiceUtil.addDefaultTable(User.class.getName());
    }
    ExpandoColumnLocalServiceUtil.addColumn(table.getTableId(),name,type,defaultValue);
  }
 catch (  Exception e) {
    _log.error(e);
  }
}

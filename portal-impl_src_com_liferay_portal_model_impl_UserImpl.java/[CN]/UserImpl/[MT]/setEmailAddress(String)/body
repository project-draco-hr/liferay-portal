{
  if (Validator.isNull(emailAddress) && !PropsValues.USERS_EMAIL_ADDRESS_REQUIRED) {
    emailAddress=String.valueOf(this.getUserId()) + PropsValues.USERS_NO_EMAIL_ADDRESS_NAME;
  }
  super.setEmailAddress(emailAddress);
}

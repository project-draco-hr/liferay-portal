{
  String emailAddress=super.getEmailAddress();
  if ((!_displayDummyEmailAddress) && (StringUtil.endsWith(emailAddress,PropsValues.USERS_NO_EMAIL_ADDRESS_NAME))) {
    emailAddress=StringPool.BLANK;
  }
  return emailAddress;
}

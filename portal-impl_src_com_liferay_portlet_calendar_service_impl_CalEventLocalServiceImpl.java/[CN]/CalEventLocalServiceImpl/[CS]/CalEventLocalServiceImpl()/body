{
  if (GetterUtil.getBoolean(PropsUtil.get(PropsKeys.CALENDAR_EXTERNAL_ENABLED))) {
    String implClass=PropsUtil.get(PropsKeys.CALENDAR_EXTERNAL_IMPL_CLASS);
    try {
      _service=(ExternalCalEventService)Class.forName(implClass).newInstance();
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
}

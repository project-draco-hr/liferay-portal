{
  List<CalEvent> localEvents=getEvents(groupId,cal,type);
  List<CalEvent> remoteEvents=getRemoteEventsByDay(groupId,cal,type,extCalServerUrl,extCalId,extCalUserId,extCalPassword,extCalSessionId);
  return removeDuplicateEvents(localEvents,remoteEvents);
}

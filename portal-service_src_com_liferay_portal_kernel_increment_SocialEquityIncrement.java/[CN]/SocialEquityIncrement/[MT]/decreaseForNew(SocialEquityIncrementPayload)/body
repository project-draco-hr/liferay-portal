{
  SocialEquityIncrementPayload payload=_payload.clone();
  payload.setEquityValue(new SocialEquityValue(_payload.getEquityValue().getK() - delta.getEquityValue().getK(),_payload.getEquityValue().getB() - delta.getEquityValue().getB()));
  return new SocialEquityIncrement(payload);
}

{
  if ((!Files.isDirectory(filePath) && fileName.startsWith("~$")) || (!Files.isDirectory(filePath) && fileName.startsWith("~") && fileName.endsWith(".tmp"))) {
    return true;
  }
 else {
    return false;
  }
}

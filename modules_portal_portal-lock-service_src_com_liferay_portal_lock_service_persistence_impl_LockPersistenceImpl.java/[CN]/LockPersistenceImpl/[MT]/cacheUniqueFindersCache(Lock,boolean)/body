{
  if (isNew) {
    Object[] args=new Object[]{lock.getClassName(),lock.getKey()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_K,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_K,args,lock);
  }
 else {
    LockModelImpl lockModelImpl=(LockModelImpl)lock;
    if ((lockModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_C_K.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{lock.getClassName(),lock.getKey()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_K,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_K,args,lock);
    }
  }
}

{
  List<WikiNode> nodes=wikiNodePersistence.findByGroupId(groupId);
  for (  WikiNode node : nodes) {
    deleteNode(node);
  }
  PortletFileRepositoryUtil.deletePortletRepository(groupId,PortletKeys.WIKI);
}

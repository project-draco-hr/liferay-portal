{
  List<WikiPage> wikiPages=wikiPagePersistence.findByNodeId(wikiNode.getNodeId());
  for (  WikiPage wikiPage : wikiPages) {
    wikiPageLocalService.updateStatus(userId,wikiPage,status,status,serviceContext);
  }
}

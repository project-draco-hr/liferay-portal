{
  node.setName(TrashUtil.appendTrashNamespace(node.getName()));
  wikiNodePersistence.update(node,false);
  return updateStatus(userId,node,WorkflowConstants.STATUS_IN_TRASH,new ServiceContext());
}

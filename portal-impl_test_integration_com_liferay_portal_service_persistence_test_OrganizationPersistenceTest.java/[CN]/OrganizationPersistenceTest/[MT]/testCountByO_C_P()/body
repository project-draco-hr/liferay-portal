{
  try {
    _persistence.countByO_C_P(RandomTestUtil.nextLong(),RandomTestUtil.nextLong(),RandomTestUtil.nextLong());
    _persistence.countByO_C_P(0L,0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

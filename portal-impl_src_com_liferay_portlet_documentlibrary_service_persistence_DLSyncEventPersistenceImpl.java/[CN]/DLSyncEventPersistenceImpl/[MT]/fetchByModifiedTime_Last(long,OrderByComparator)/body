{
  int count=countByModifiedTime(modifiedTime);
  if (count == 0) {
    return null;
  }
  List<DLSyncEvent> list=findByModifiedTime(modifiedTime,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

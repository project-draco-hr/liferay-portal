{
  int count=countByModifiedDate(modifiedDate);
  List<DLSyncEvent> list=findByModifiedDate(modifiedDate,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

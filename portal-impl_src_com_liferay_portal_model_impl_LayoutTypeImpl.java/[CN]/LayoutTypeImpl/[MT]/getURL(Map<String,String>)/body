{
  String url=_layoutTypeController.getURL();
  if (Validator.isNull(url)) {
    url=_URL;
  }
  return StringUtil.replace(url,StringPool.DOLLAR_AND_OPEN_CURLY_BRACE,StringPool.CLOSE_CURLY_BRACE,variables);
}

{
  ThemeDisplay themeDisplay=(ThemeDisplay)request.getAttribute(WebKeys.THEME_DISPLAY);
  return panelCategoryHelper.containsPortlet(themeDisplay.getPpid(),getKey(),themeDisplay.getPermissionChecker(),group);
}

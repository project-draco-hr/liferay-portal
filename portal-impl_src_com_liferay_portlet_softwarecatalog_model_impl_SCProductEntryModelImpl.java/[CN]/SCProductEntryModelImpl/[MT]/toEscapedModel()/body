{
  if (isEscapedModel()) {
    return (SCProductEntry)this;
  }
 else {
    return (SCProductEntry)Proxy.newProxyInstance(SCProductEntry.class.getClassLoader(),new Class[]{SCProductEntry.class},new AutoEscapeBeanHandler(this));
  }
}

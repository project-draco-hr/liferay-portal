{
  if ((repoGroupId != _repoGroupId) || ((repoGroupId != null) && !repoGroupId.equals(_repoGroupId))) {
    _repoGroupId=repoGroupId;
    if (_originalRepoGroupId == null) {
      _originalRepoGroupId=repoGroupId;
    }
  }
}

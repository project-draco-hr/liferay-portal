{
  if ((repoArtifactId != _repoArtifactId) || ((repoArtifactId != null) && !repoArtifactId.equals(_repoArtifactId))) {
    _repoArtifactId=repoArtifactId;
    if (_originalRepoArtifactId == null) {
      _originalRepoArtifactId=repoArtifactId;
    }
  }
}

{
  List<ServiceRegistration<UserNotificationDefinition>> serviceRegistrations=_userNotificationDefinitionServiceRegistrations.get(portletId);
  if (!serviceRegistrations.isEmpty()) {
    for (    ServiceRegistration<UserNotificationDefinition> serviceRegistration : serviceRegistrations) {
      serviceRegistration.unregister();
    }
  }
}

{
  ServiceRegistration<UserNotificationDefinition> serviceRegistration=_userNotificationDefinitionServiceRegistrations.get(portletId);
  if (serviceRegistration != null) {
    serviceRegistration.unregister();
  }
}

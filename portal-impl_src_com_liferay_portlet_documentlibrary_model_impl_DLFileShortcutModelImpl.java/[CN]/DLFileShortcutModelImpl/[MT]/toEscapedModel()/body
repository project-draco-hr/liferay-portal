{
  if (isEscapedModel()) {
    return (DLFileShortcut)this;
  }
 else {
    return (DLFileShortcut)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

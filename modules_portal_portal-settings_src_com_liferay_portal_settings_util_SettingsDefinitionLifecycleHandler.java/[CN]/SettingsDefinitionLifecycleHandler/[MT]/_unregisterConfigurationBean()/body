{
  _configurationBeanServiceRegistration.unregister();
  _configurationBeanServiceRegistration=null;
  if (_log.isDebugEnabled()) {
    _log.debug("Unregistered configuration bean service " + SettingsDefinitionUtil.getConfigurationBeanClassName(_settingsDefinition));
  }
}

{
  _configurationBeanServiceRegistration=_bundleContext.registerService(_settingsDefinition.getConfigurationBeanClass(),_configurationBeanBuilder.getConfigurationBean(),_emptDictionary);
  if (_log.isDebugEnabled()) {
    String className=SettingsDefinitionUtil.getConfigurationBeanClassName(_settingsDefinition);
    _log.debug("Registered configuration bean service " + className);
  }
}

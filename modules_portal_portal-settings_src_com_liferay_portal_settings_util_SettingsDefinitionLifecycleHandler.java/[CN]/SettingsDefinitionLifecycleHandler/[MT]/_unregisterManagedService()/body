{
  _managedServiceServiceRegistration.unregister();
  _managedServiceServiceRegistration=null;
  if (_log.isDebugEnabled()) {
    _log.debug("Unregistered listener for changes in configurationPid " + SettingsDefinitionUtil.getConfigurationPid(_settingsDefinition));
  }
}

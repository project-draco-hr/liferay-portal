{
  _managedServiceServiceRegistration.unregister();
  _managedServiceServiceRegistration=null;
  if (_log.isDebugEnabled()) {
    String pid=SettingsDefinitionUtil.getConfigurationPid(_settingsDefinition);
    _log.debug("Unregistered listener for changes in configurationPid " + pid);
  }
}

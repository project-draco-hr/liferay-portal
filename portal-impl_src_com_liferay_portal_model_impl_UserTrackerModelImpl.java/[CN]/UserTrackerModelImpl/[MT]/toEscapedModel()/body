{
  if (isEscapedModel()) {
    return (UserTracker)this;
  }
 else {
    return (UserTracker)Proxy.newProxyInstance(UserTracker.class.getClassLoader(),new Class[]{UserTracker.class},new AutoEscapeBeanHandler(this));
  }
}

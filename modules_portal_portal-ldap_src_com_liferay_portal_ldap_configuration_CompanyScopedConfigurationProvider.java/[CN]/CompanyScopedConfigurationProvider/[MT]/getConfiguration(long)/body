{
  T t=_configurations.get(companyId);
  if (t == null) {
    t=_configurations.get(0L);
  }
  if (t == null) {
    throw new IllegalArgumentException("No " + getMetatype().getName() + " instance found for companyId: "+ companyId);
  }
  return t;
}

{
  Session session=null;
  try {
    session=openSession();
    return (MBMailing)session.get(MBMailingImpl.class,new Long(mailingId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

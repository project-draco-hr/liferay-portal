{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBMailing mbMailing) method. Use update(MBMailing mbMailing, boolean merge) instead.");
  }
  return update(mbMailing,false);
}

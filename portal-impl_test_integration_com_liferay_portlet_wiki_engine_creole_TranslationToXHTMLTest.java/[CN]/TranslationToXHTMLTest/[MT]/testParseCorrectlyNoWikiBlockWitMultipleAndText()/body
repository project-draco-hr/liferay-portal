{
  String result=dosToUnix(translate("nowikiblock-9.creole"));
  Assert.assertEquals("<pre>public interface Foo {" + _EOL + "void foo();"+ _EOL+ "}"+ _EOL+ "</pre><p>Outside preserve </p>",result);
}

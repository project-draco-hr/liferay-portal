{
  Integer type=MBMessageModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ MBMessageModelImpl.TABLE_NAME);
  }
  return type;
}

{
  for (int i=0; i < roleIds.length; i++) {
    long roleId=roleIds[i];
    try {
      UserGroupRoleUtil.remove(new UserGroupRolePK(userId,groupId,roleId));
    }
 catch (    NoSuchUserGroupRoleException nsugre) {
    }
  }
  PermissionCacheUtil.clearCache();
}

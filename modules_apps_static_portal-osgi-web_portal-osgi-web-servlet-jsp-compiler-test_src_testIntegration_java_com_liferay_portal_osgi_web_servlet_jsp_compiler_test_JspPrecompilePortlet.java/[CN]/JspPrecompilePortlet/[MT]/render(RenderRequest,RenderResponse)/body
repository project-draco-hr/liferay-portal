{
  String jsp=renderRequest.getParameter(_JSP_PARAMETER_NAME);
  if (jsp == null) {
    throw new IllegalArgumentException(_JSP_PARAMETER_NAME + " query must not be null");
  }
  PortletContext portletContext=getPortletContext();
  PortletRequestDispatcher portletRequestDispatcher=portletContext.getRequestDispatcher(jsp);
  portletRequestDispatcher.include(renderRequest,renderResponse);
}

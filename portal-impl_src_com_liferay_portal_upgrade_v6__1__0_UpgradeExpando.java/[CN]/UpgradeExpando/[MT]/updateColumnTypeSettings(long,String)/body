{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update ExpandoColumn set typeSettings = ? where columnId = ?");
    ps.setString(1,typeSettings);
    ps.setLong(2,columnId);
    ps.executeUpdate();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

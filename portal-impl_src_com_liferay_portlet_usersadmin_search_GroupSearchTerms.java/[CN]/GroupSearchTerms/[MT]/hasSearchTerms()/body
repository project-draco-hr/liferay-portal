{
  if (isAdvancedSearch()) {
    if (Validator.isNotNull(name) || Validator.isNotNull(description)) {
      return true;
    }
  }
 else {
    if (Validator.isNotNull(keywords)) {
      return true;
    }
  }
  return false;
}

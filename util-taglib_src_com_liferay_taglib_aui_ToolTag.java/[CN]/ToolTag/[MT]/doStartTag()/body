{
  HttpServletRequest request=(HttpServletRequest)pageContext.getRequest();
  if (_icon == null) {
    _icon=_id;
  }
  PanelTag parentTag=(PanelTag)findAncestorWithClass(this,PanelTag.class);
  parentTag.addTool(this);
  return EVAL_PAGE;
}

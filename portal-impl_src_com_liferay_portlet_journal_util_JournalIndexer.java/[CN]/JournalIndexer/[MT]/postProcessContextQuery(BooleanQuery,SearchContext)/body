{
  Integer status=(Integer)searchContext.getAttribute("status");
  if ((Validator.isNotNull(status)) && (status != WorkflowConstants.STATUS_ANY)) {
    contextQuery.addRequiredTerm("status",status);
  }
}

{
  List<JournalArticle> articles=JournalArticleLocalServiceUtil.getCompanyArticles(companyId,StatusConstants.APPROVED,start,end);
  for (  JournalArticle article : articles) {
    reindex(article);
  }
}

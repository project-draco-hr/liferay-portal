{
  long companyId=ServiceTestUtil.nextLong();
  long repositoryId=ServiceTestUtil.nextLong();
  String path=ServiceTestUtil.randomString();
  DLContent addDLContent=_dlContentLocalService.addContent(companyId,repositoryId,path,Store.VERSION_DEFAULT,_DATA_VERSION_1);
  DLContent getDLContent=_dlContentLocalService.getContent(companyId,repositoryId,path);
  Assert.assertEquals(addDLContent,getDLContent);
}

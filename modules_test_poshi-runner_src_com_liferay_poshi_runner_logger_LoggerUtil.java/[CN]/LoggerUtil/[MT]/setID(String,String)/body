{
  if (!isLoggerStarted()) {
    return;
  }
  String escapedNewID=StringEscapeUtils.escapeEcmaScript(newID);
  String escapedOldID=StringEscapeUtils.escapeEcmaScript(oldID);
  _javascriptExecutor.executeScript("setID(" + escapedOldID + ", '"+ escapedNewID+ "');");
}

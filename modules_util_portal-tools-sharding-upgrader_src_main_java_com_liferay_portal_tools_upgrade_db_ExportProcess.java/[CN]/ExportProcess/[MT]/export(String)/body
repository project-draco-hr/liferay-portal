{
  List<String> tableNamesWithoutCompanyId=_dbProvider.getTableNamesWithoutCompanyId();
  List<Long> companyIds=_dbProvider.getCompanyIds();
  for (  Long companyId : companyIds) {
    _exportCompany(companyId,tableNamesWithoutCompanyId,outputFilePath);
  }
}

{
  Map<String,String> arguments=ArgumentsUtil.parseArguments(args);
  boolean osgiModule=GetterUtil.getBoolean(arguments.get("upgrade.osgi.module"),true);
  String upgradeBaseDirName=arguments.get("upgrade.base.dir");
  String upgradeTableDirName=arguments.get("upgrade.table.dir");
  try {
    new UpgradeTableBuilder(osgiModule,upgradeBaseDirName,upgradeTableDirName);
  }
 catch (  Exception e) {
    ArgumentsUtil.processMainException(arguments,e);
  }
}

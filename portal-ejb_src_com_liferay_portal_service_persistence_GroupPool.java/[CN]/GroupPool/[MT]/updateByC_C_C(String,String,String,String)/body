{
  if (_log.isInfoEnabled()) {
    _log.info("Update C_C_C" + " " + companyId.toString() + " "+ className.toString()+ " "+ classPK.toString());
  }
  return _instance._putByC_C_C(companyId,className,classPK,pk,true);
}

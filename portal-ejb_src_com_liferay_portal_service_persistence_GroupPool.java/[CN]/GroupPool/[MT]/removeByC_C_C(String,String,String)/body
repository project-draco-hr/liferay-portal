{
  if (_log.isInfoEnabled()) {
    _log.info("Remove C_C_C" + " " + companyId.toString() + " "+ className.toString()+ " "+ classPK.toString());
  }
  return _instance._removeByC_C_C(companyId,className,classPK);
}

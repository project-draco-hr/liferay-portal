{
  if (_log.isInfoEnabled()) {
    _log.info("Put C_C_C" + " " + companyId.toString() + " "+ className.toString()+ " "+ classPK.toString());
  }
  return _instance._putByC_C_C(companyId,className,classPK,pk,false);
}

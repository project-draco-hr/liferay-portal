{
  String pk=_instance._getByC_F(companyId,friendlyURL);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_F" + " " + companyId.toString() + " "+ friendlyURL.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

{
  int pos=className.indexOf(".impl.");
  if (pos != -1) {
    className=className.substring(0,pos + 1) + className.substring(pos + 6);
  }
  if (className.endsWith("Impl")) {
    className=className.substring(0,className.length() - 4);
  }
  return className;
}

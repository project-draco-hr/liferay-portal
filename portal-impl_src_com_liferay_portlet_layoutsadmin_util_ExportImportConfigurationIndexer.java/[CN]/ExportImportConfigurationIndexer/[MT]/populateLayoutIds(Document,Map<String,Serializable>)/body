{
  if (!settingsMap.containsKey("layoutIdMap")) {
    return;
  }
  Map<Number,Boolean> layoutIdMap=(Map<Number,Boolean>)settingsMap.get("layoutIdMap");
  document.addKeyword("layoutIds",ArrayUtil.toLongArray(layoutIdMap.keySet()));
}

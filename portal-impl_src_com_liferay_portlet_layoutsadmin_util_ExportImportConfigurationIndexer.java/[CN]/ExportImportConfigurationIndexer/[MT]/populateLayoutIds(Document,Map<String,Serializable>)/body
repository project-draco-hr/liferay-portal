{
  if (!settingsMap.containsKey("layoutIdMap")) {
    return;
  }
  Map<String,Boolean> layoutIdMap=(Map<String,Boolean>)settingsMap.get("layoutIdMap");
  Set<String> layoutIdSet=layoutIdMap.keySet();
  document.addKeyword("layoutIds",layoutIdSet.toArray(new String[layoutIdSet.size()]));
}

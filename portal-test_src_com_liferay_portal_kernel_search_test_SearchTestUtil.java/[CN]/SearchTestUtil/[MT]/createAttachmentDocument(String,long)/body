{
  Document document=createDocument(entryClassName,entryClassPK);
  DocumentHelper helper=new DocumentHelper(document);
  helper.setAttachmentOwnerKey(ATTACHMENT_OWNER_CLASS_NAME_ID,ATTACHMENT_OWNER_CLASS_PK);
  return document;
}

{
  Integer type=ExpandoRowModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ExpandoRowModelImpl.TABLE_NAME);
  }
  return type;
}

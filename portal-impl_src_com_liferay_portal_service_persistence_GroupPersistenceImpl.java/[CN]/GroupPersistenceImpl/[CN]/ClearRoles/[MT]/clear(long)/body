{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(groupId,com.liferay.portal.model.Role.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(groupId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(groupId,com.liferay.portal.model.Role.class.getName());
    }
  }
}

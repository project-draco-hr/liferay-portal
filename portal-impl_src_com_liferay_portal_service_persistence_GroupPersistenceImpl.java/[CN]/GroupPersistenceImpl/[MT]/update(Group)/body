{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Group group) method. To improve performance, call update(Group group, boolean merge).");
  }
  return update(group,false);
}

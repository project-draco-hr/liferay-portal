{
  Group group=fetchByPrimaryKey(groupId);
  if (group == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + groupId);
    }
    throw new NoSuchGroupException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + groupId);
  }
  return group;
}

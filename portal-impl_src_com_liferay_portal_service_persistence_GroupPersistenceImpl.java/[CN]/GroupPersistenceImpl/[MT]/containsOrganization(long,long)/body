{
  Object[] finderArgs=new Object[]{new Long(pk),new Long(organizationPK)};
  Object result=FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_ORGANIZATION,finderArgs,this);
  if (result == null) {
    try {
      Boolean value=Boolean.valueOf(containsOrganization.contains(pk,organizationPK));
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_ORGANIZATION,finderArgs,value);
      return value.booleanValue();
    }
 catch (    Exception e) {
      throw processException(e);
    }
  }
 else {
    return ((Boolean)result).booleanValue();
  }
}

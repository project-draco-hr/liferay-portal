{
  fileElement=super.getFileElement(commandArgument,fileElement,fileEntry);
  if (!AudioProcessorUtil.hasAudio(fileEntry.getFileVersion())) {
    fileElement.setAttribute("errorMessage",LanguageUtil.get(commandArgument.getLocale(),"the-audio-preview-is-not-yet-ready.-please-try-again-" + "later"));
  }
  return fileElement;
}

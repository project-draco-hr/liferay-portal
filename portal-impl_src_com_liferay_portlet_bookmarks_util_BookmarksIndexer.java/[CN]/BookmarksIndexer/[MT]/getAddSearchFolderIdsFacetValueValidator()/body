{
  return new FacetValueValidator(){
    public boolean check(    SearchContext searchContext,    String primKey){
      try {
        BookmarksFolderServiceUtil.getFolder(GetterUtil.getLong(primKey));
      }
 catch (      Exception e) {
        return false;
      }
      return true;
    }
  }
;
}

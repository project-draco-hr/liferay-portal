{
  interval=PropsValues.FREEMARKER_ENGINE_MODIFICATION_CHECK_INTERVAL;
  if (interval > 0) {
    checkInterval=true;
  }
  initTemplateResourceParsers(PropsValues.FREEMARKER_ENGINE_TEMPLATE_LOADERS);
}

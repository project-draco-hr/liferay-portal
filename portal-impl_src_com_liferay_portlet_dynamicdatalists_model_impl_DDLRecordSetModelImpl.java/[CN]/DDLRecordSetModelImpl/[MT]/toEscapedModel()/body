{
  if (isEscapedModel()) {
    return (DDLRecordSet)this;
  }
 else {
    return (DDLRecordSet)Proxy.newProxyInstance(DDLRecordSet.class.getClassLoader(),new Class[]{DDLRecordSet.class},new AutoEscapeBeanHandler(this));
  }
}

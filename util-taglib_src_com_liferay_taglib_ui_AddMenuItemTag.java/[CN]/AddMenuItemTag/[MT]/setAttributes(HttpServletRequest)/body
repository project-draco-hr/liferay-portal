{
  List<AddMenuItem> menuItemList=(List<AddMenuItem>)request.getAttribute("liferay-ui:add-menu:addMenuItems");
  if (menuItemList != null) {
    AddMenuItem menuItem=new AddMenuItem(_title,_url);
    menuItemList.add(menuItem);
  }
}

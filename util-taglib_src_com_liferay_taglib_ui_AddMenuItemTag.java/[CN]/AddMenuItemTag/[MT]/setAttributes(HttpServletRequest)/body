{
  List<AddMenuItem> menuItemList=(List<AddMenuItem>)request.getAttribute("liferay-ui:add-menu:menuItems");
  if (menuItemList != null) {
    AddMenuItem menuItem=new AddMenuItem(_title,_url);
    menuItemList.add(menuItem);
  }
}

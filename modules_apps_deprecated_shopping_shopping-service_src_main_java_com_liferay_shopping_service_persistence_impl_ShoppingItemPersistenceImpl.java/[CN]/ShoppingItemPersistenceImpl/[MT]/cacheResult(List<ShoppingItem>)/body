{
  for (  ShoppingItem shoppingItem : shoppingItems) {
    if (entityCache.getResult(ShoppingItemModelImpl.ENTITY_CACHE_ENABLED,ShoppingItemImpl.class,shoppingItem.getPrimaryKey()) == null) {
      cacheResult(shoppingItem);
    }
 else {
      shoppingItem.resetOriginalValues();
    }
  }
}

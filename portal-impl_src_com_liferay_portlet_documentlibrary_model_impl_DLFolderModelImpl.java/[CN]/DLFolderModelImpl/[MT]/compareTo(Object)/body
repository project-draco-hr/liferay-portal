{
  if (obj == null) {
    return -1;
  }
  DLFolderImpl dlFolder=(DLFolderImpl)obj;
  int value=0;
  value=getParentFolderId().compareTo(dlFolder.getParentFolderId());
  if (value != 0) {
    return value;
  }
  value=getName().toLowerCase().compareTo(dlFolder.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

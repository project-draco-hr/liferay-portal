{
  if (isEscapedModel()) {
    return (DLFolder)this;
  }
 else {
    return (DLFolder)Proxy.newProxyInstance(DLFolder.class.getClassLoader(),new Class[]{DLFolder.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (BookmarksEntry)this;
  }
 else {
    return (BookmarksEntry)Proxy.newProxyInstance(BookmarksEntry.class.getClassLoader(),new Class[]{BookmarksEntry.class},new AutoEscapeBeanHandler(this));
  }
}

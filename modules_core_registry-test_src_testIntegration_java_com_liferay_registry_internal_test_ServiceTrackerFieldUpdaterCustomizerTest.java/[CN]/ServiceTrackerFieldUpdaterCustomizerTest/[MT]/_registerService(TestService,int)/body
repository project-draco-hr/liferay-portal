{
  Hashtable<String,Object> hashtable=new Hashtable<>();
  if (ranking != 0) {
    hashtable.put("service.ranking",ranking);
  }
  return _bundleContext.registerService(TestService.class,testService,hashtable);
}

{
  Set<String> pqls=new HashSet<>();
  pqls.add("test test");
  pqls.add("true AND true");
  pqls.add("test == test");
  for (  String pql : pqls) {
    _validateValueError(pql,"Invalid value: " + pql);
  }
}

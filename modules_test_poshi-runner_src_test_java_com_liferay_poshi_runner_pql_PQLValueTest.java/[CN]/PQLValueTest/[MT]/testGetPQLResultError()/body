{
  Set<String> pqls=new HashSet<>();
  pqls.add("test test");
  pqls.add("true AND true");
  pqls.add("test == test");
  for (  String pql : pqls) {
    _validateGetPQLResultError(pql,"Invalid value: " + pql);
  }
}

{
  _connect();
  _msn.addMsnListener(new MSNMessageAdapter(_msn,to,msg));
  try {
    Thread.sleep(1500);
    _msn.doCallWait(to);
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      _log.warn(e);
    }
  }
}

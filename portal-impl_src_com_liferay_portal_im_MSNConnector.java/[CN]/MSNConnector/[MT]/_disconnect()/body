{
  try {
    if (_msn.isLoggedIn()) {
      _msn.logout();
    }
  }
 catch (  Exception e) {
    if (_log.isWarnEnabled()) {
      if (_log.isWarnEnabled()) {
        _log.warn(e);
      }
    }
  }
}

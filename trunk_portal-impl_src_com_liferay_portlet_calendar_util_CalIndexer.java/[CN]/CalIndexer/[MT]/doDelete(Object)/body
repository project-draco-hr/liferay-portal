{
  CalEvent event=(CalEvent)obj;
  Document document=new DocumentImpl();
  document.addUID(PORTLET_ID,event.getEventId());
  SearchEngineUtil.deleteDocument(event.getCompanyId(),document.get(Field.UID));
}

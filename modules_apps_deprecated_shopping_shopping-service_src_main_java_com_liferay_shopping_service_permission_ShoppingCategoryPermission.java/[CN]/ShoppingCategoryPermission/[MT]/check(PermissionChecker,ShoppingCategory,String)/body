{
  if (!contains(permissionChecker,category,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,ShoppingCategory.class.getName(),category.getCategoryId(),actionId);
  }
}

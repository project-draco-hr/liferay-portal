{
  try (InputStream inputStream=LiferayIndexCreationHelper.class.getResourceAsStream(file)){
    return StringUtil.read(inputStream);
  }
 catch (  IOException ioe) {
    throw new RuntimeException(ioe);
  }
}

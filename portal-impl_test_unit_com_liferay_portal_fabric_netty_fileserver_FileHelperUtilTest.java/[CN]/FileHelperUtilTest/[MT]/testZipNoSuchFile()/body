{
  try {
    FileHelperUtil.zip(Paths.get("NoSuchFile"),FileHelperUtil.TEMP_DIR_PATH,CompressionLevel.NO_COMPRESSION);
  }
 catch (  IOException ioe) {
    Assert.assertSame(NoSuchFileException.class,ioe.getClass());
  }
}

{
  try {
    ProcessOutputStream processOutputStream=ProcessContext.getProcessOutputStream();
    ReflectionTestUtil.setFieldValue(processOutputStream,"_objectOutputStream",_oldObjectOutputStream);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  _thread.interrupt();
  return true;
}

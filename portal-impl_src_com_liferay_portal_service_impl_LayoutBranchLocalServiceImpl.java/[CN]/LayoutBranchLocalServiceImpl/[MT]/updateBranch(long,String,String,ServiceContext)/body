{
  LayoutBranch branch=layoutBranchPersistence.fetchByPrimaryKey(branchId);
  branch.setName(name);
  branch.setDescription(description);
  layoutBranchPersistence.update(branch,false);
  return branch;
}

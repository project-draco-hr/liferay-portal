{
  CategoriesEntryPermission.check(getPermissionChecker(),entryId,ActionKeys.UPDATE);
  return categoriesEntryLocalService.updateEntry(getUserId(),entryId,vocabularyId,parentEntryId,name,properties);
}

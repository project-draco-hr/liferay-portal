{
  InputStream inputStream=createBundle(clazz);
  Long bundleId=ModuleFrameworkUtilAdapter.addBundle(clazz.getName(),inputStream);
  ModuleFrameworkUtilAdapter.startBundle(bundleId);
  return bundleId;
}

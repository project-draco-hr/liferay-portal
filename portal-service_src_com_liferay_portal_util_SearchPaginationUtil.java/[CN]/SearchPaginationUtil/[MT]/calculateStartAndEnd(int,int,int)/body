{
  if ((total > 0) && (start >= total)) {
    int delta=end - start;
    int cur=start / delta;
    start=0;
    if (cur > 0) {
      start=(cur - 1) * delta;
    }
    end=start + delta;
  }
  return new int[]{start,end};
}

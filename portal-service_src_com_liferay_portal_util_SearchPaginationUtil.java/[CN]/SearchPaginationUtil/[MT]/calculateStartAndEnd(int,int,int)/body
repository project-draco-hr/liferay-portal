{
  if (total <= 0) {
    return new int[]{0,0};
  }
  int[] startAndEnd={start,end};
  int delta=end - start;
  while ((start > 0) && (start >= total)) {
    int cur=start / delta;
    startAndEnd=calculateStartAndEnd(cur,delta);
    start=startAndEnd[0];
  }
  return startAndEnd;
}

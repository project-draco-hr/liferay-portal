{
  int[] startAndEnd={start,end};
  if ((total > 0) && (start >= total)) {
    int delta=end - start;
    int cur=start / delta;
    startAndEnd=calculateStartAndEnd(cur,delta);
  }
  return startAndEnd;
}

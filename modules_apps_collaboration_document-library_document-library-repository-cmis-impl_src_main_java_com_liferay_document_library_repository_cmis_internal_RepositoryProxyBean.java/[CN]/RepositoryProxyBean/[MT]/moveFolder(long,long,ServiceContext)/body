{
  try (ClassLoaderSetter classLoaderSetter=new ClassLoaderSetter(_classLoader)){
    Folder folder=_repository.moveFolder(folderId,newParentFolderId,serviceContext);
    return newFolderProxyBean(folder);
  }
 }

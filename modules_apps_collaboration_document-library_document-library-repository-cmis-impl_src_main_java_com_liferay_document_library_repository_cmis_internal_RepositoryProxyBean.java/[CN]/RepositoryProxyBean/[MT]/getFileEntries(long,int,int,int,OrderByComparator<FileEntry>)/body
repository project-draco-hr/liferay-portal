{
  try (ClassLoaderSetter classLoaderSetter=new ClassLoaderSetter(_classLoader)){
    List<FileEntry> fileEntries=_repository.getFileEntries(folderId,status,start,end,obc);
    return toFileEntryProxyBeans(fileEntries);
  }
 }

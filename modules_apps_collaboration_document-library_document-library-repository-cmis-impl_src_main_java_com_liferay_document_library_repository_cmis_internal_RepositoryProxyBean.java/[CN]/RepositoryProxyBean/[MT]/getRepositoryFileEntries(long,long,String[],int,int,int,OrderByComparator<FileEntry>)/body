{
  try (ClassLoaderSetter classLoaderSetter=new ClassLoaderSetter(_classLoader)){
    List<FileEntry> repositoryFileEntries=_repository.getRepositoryFileEntries(userId,rootFolderId,mimeTypes,status,start,end,obc);
    return toFileEntryProxyBeans(repositoryFileEntries);
  }
 }

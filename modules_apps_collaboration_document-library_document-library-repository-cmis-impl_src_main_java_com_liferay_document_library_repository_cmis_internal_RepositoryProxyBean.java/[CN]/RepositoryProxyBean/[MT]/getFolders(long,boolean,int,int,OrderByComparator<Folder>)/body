{
  try (ClassLoaderSetter classLoaderSetter=new ClassLoaderSetter(_classLoader)){
    List<Folder> folders=_repository.getFolders(parentFolderId,includeMountFolders,start,end,obc);
    return toFolderProxyBeans(folders);
  }
 }

{
  try (ClassLoaderSetter classLoaderSetter=new ClassLoaderSetter(_classLoader)){
    FileVersion fileVersion=_repository.getFileVersion(fileVersionId);
    return newFileVersionProxyBean(fileVersion);
  }
 }

{
  LayoutPrototype layoutPrototype=(LayoutPrototype)EntityCacheUtil.getResult(LayoutPrototypeModelImpl.ENTITY_CACHE_ENABLED,LayoutPrototypeImpl.class,layoutPrototypeId);
  if (layoutPrototype == _nullLayoutPrototype) {
    return null;
  }
  if (layoutPrototype == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      layoutPrototype=(LayoutPrototype)session.get(LayoutPrototypeImpl.class,Long.valueOf(layoutPrototypeId));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (layoutPrototype != null) {
        cacheResult(layoutPrototype);
      }
 else       if (!hasException) {
        EntityCacheUtil.putResult(LayoutPrototypeModelImpl.ENTITY_CACHE_ENABLED,LayoutPrototypeImpl.class,layoutPrototypeId,_nullLayoutPrototype);
      }
      closeSession(session);
    }
  }
  return layoutPrototype;
}

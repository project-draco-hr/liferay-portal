{
  LayoutPrototype layoutPrototype=(LayoutPrototype)EntityCacheUtil.getResult(LayoutPrototypeModelImpl.ENTITY_CACHE_ENABLED,LayoutPrototypeImpl.class,layoutPrototypeId,this);
  if (layoutPrototype == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (layoutPrototype == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      layoutPrototype=(LayoutPrototype)session.get(LayoutPrototypeImpl.class,Long.valueOf(layoutPrototypeId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (layoutPrototype == null)) {
        EntityCacheUtil.putResult(LayoutPrototypeModelImpl.ENTITY_CACHE_ENABLED,LayoutPrototypeImpl.class,layoutPrototypeId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(layoutPrototype);
      }
      closeSession(session);
    }
  }
  return layoutPrototype;
}

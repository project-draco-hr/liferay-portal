{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(LayoutPrototype layoutPrototype) method. Use update(LayoutPrototype layoutPrototype, boolean merge) instead.");
  }
  return update(layoutPrototype,false);
}

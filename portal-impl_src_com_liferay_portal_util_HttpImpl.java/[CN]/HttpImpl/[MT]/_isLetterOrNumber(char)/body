{
  if (((CharPool.NUMBER_0 <= c) && (c <= CharPool.NUMBER_9)) || ((CharPool.UPPER_CASE_A <= c) && (c <= CharPool.UPPER_CASE_Z)) || ((CharPool.LOWER_CASE_A <= c) && (c <= CharPool.LOWER_CASE_Z))) {
    return true;
  }
  return false;
}

{
  if (((CharPool.NUMBER_0 <= targetChar) && (targetChar <= CharPool.NUMBER_9)) || ((CharPool.UPPER_CASE_A <= targetChar) && (targetChar <= CharPool.UPPER_CASE_Z)) || ((CharPool.LOWER_CASE_A <= targetChar) && (targetChar <= CharPool.LOWER_CASE_Z))) {
    return true;
  }
  return false;
}

{
  byte[] bytes=null;
  InputStream inputStream=URLtoInputStream(location,method,headers,cookies,auth,body,parts,response,followRedirects);
  if (inputStream != null) {
    bytes=FileUtil.getBytes(inputStream);
    inputStream.close();
  }
  return bytes;
}

{
  byte[] bytes=null;
  InputStream is=URLtoInputStream(location,method,headers,cookies,auth,body,parts,response,followRedirects);
  if (is != null) {
    bytes=FileUtil.getBytes(is);
    is.close();
  }
  return bytes;
}

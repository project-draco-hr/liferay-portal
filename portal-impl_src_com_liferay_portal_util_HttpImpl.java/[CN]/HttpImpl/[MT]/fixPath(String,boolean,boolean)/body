{
  if (path == null) {
    return StringPool.BLANK;
  }
  if (leading) {
    path=path.replaceAll("^/+",StringPool.BLANK);
  }
  if (trailing) {
    path=path.replaceAll("/+$",StringPool.BLANK);
  }
  return path;
}

{
  if (!safeFileName.equals(fileName)) {
    if (_store.hasFile(companyId,repositoryId,fileName,DLFileEntryConstants.DEFAULT_VERSION)) {
      _store.updateFile(companyId,portletId,groupId,repositoryId,fileName,safeFileName,true);
    }
  }
}

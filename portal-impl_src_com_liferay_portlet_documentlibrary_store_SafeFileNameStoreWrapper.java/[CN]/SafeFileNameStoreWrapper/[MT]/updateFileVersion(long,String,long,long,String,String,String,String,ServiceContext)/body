{
  String safeFileName=FileUtil.encodeSafeFileName(fileName);
  renameUnsafeFile(companyId,portletId,groupId,repositoryId,fileName,safeFileName);
  _store.updateFileVersion(companyId,portletId,groupId,repositoryId,safeFileName,fromVersionNumber,toVersionNumber,sourceFileName,serviceContext);
}

{
  String utilClassName=method.getDeclaringClass().getName().concat("Util");
  Class<?> utilClass=Thread.currentThread().getContextClassLoader().loadClass(utilClassName);
  return utilClass.getMethod(method.getName(),method.getParameterTypes());
}

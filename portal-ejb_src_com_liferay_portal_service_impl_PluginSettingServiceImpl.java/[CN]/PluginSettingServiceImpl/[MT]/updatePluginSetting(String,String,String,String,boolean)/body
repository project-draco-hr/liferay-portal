{
  if (!RoleLocalServiceUtil.hasUserRole(getUserId(),companyId,RoleImpl.ADMINISTRATOR)) {
    throw new PrincipalException();
  }
  return PluginSettingLocalServiceUtil.updatePluginSetting(companyId,pluginId,pluginType,roles,active);
}

{
  File file=_scaleImage(renderedImage,fileVersion.getMimeType(),PrefsPropsUtil.getInteger(maxDimensionPropsKey));
  try {
    addFileToStore(fileVersion.getCompanyId(),THUMBNAIL_PATH,filePath,file);
  }
  finally {
    FileUtil.delete(file);
  }
}

{
  RenderedImage thumbnailRenderedImage=ImageProcessorUtil.scale(renderedImage,height,width);
  byte[] bytes=ImageProcessorUtil.getBytes(thumbnailRenderedImage,contentType);
  return FileUtil.createTempFile(bytes);
}

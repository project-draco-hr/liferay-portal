{
  boolean hasImages=false;
  try {
    Image image=ImageServiceUtil.getImage(imageId);
    if (image != null) {
      hasImages=true;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return hasImages;
}

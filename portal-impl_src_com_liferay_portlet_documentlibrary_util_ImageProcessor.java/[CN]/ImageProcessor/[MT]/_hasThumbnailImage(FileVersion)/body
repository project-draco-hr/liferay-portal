{
  try {
    return DLStoreUtil.hasFile(fileVersion.getCompanyId(),REPOSITORY_ID,getThumbnailFilePath(fileVersion,fileVersion.getExtension()));
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return false;
}

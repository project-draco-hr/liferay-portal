{
  if (!_fileEntries.contains(fileVersion.getFileVersionId()) && _isSupportedImage(fileVersion) && !hasImages(fileVersion)) {
    _fileEntries.add(fileVersion.getFileVersionId());
    MessageBusUtil.sendMessage(DestinationNames.DOCUMENT_LIBRARY_IMAGE_PROCESSOR,fileVersion);
  }
}

{
  File file=_scaleImage(renderedImage,fileVersion.getMimeType(),PrefsPropsUtil.getInteger(maxHeightPropsKey),PrefsPropsUtil.getInteger(maxWidthPropsKey));
  try {
    addFileToStore(fileVersion.getCompanyId(),THUMBNAIL_PATH,filePath,file);
  }
  finally {
    FileUtil.delete(file);
  }
}

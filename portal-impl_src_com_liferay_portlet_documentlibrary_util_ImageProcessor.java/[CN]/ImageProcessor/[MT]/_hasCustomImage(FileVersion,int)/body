{
  try {
    String type=_getImageType(fileVersion);
    return DLStoreUtil.hasFile(fileVersion.getCompanyId(),REPOSITORY_ID,_getCustomFilePath(fileVersion,type,index));
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return false;
}

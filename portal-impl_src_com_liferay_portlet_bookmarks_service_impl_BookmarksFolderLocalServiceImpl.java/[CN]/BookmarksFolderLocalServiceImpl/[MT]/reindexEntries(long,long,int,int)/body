{
  List<BookmarksEntry> entries=bookmarksEntryPersistence.findByG_F(groupId,folderId,entryStart,entryEnd);
  for (  BookmarksEntry entry : entries) {
    bookmarksEntryLocalService.reindex(entry);
  }
}

{
  List<BookmarksEntry> entries=bookmarksEntryPersistence.findByFolderId(folderId,entryStart,entryEnd);
  for (  BookmarksEntry entry : entries) {
    bookmarksEntryLocalService.reIndex(entry);
  }
}

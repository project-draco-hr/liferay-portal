{
  Iterator<BookmarksFolder> itr=bookmarksFolderPersistence.findByG_P(groupId,folderId).iterator();
  while (itr.hasNext()) {
    BookmarksFolder folder=itr.next();
    folderIds.add(new Long(folder.getFolderId()));
    getSubfolderIds(folderIds,folder.getGroupId(),folder.getFolderId());
  }
}

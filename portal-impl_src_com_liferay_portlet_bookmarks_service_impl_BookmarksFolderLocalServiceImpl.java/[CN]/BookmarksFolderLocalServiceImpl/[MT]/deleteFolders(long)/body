{
  Iterator<BookmarksFolder> itr=bookmarksFolderPersistence.findByG_P(groupId,BookmarksFolderImpl.DEFAULT_PARENT_FOLDER_ID).iterator();
  while (itr.hasNext()) {
    BookmarksFolder folder=itr.next();
    deleteFolder(folder);
  }
}

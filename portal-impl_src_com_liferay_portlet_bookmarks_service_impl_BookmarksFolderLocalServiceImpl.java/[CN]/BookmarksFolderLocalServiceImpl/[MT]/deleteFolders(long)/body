{
  Iterator itr=bookmarksFolderPersistence.findByG_P(groupId,BookmarksFolderImpl.DEFAULT_PARENT_FOLDER_ID).iterator();
  while (itr.hasNext()) {
    BookmarksFolder folder=(BookmarksFolder)itr.next();
    deleteFolder(folder);
  }
}

{
  String toTitle=null;
  try {
    DLFileEntry fileEntry=DLFileEntryLocalServiceUtil.getFileEntry(getGroupId(),getToFolderId(),getToName());
    toTitle=fileEntry.getTitle();
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return toTitle;
}

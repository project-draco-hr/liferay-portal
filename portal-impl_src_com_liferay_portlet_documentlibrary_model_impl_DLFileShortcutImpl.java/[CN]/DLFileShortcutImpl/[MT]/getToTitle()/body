{
  String toTitle=null;
  try {
    DLFileEntry fileEntry=DLAppLocalServiceUtil.getFileEntry(getToGroupId(),getToFolderId(),getToName());
    toTitle=fileEntry.getTitle();
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return toTitle;
}

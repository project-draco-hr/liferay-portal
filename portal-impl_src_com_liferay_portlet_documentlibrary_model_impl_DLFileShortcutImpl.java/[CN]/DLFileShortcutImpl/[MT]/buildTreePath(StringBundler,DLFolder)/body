{
  if (dlFolder == null) {
    sb.append(StringPool.SLASH);
  }
 else {
    buildTreePath(sb,dlFolder.getParentFolder());
    sb.append(dlFolder.getFolderId());
    sb.append(StringPool.SLASH);
  }
}

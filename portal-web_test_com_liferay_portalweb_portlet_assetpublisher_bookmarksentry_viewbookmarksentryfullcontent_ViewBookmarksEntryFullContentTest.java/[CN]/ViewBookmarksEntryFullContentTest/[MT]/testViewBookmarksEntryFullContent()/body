{
  selenium.open("/web/guest/home/");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Asset Publisher Test Page")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.clickAt("link=Asset Publisher Test Page",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  Thread.sleep(5000);
  assertTrue(selenium.isPartialText("//div/h3","AP Bookmarks Entry Name"));
  assertEquals(RuntimeVariables.replace("AP Bookmarks Entry Name (http://www.liferay.com)"),selenium.getText("//div[2]/a"));
  assertFalse(selenium.isElementPresent("//th[1]"));
  assertEquals(RuntimeVariables.replace("View in Context \u00bb"),selenium.getText("//div[2]/div/a"));
}

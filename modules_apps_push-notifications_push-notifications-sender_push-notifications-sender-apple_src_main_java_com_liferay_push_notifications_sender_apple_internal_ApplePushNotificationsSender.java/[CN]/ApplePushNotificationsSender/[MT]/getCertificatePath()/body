{
  if (Validator.isNull(_certificatePath)) {
    _certificatePath=PrefsPropsUtil.getString(PortletPropsKeys.APPLE_CERTIFICATE_PATH,PortletPropsValues.APPLE_CERTIFICATE_PATH);
  }
  return _certificatePath;
}

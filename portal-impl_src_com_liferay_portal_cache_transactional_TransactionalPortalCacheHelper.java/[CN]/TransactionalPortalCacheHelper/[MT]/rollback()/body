{
  if (!PropsValues.TRANSACTIONAL_CACHE_ENABLED) {
    return;
  }
  Map<PortalCache<? extends Serializable,?>,Map<? extends Serializable,?>> portalCacheMap=_popPortalCacheMap();
  portalCacheMap.clear();
}

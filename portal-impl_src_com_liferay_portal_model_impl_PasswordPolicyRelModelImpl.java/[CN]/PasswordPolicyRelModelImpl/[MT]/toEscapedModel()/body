{
  if (isEscapedModel()) {
    return (PasswordPolicyRel)this;
  }
 else {
    return (PasswordPolicyRel)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  long groupId=RandomTestUtil.randomLong();
  Group group=setUpGroup(groupId);
  Mockito.when(group.isStagingGroup()).thenReturn(true);
  Assert.assertEquals(true,_indexer.isStagingGroup(groupId));
}

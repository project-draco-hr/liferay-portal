{
  List<MBMessage> messages=MBMessageLocalServiceUtil.getThreadMessages(threadId,WorkflowConstants.STATUS_ANY);
  propagateRolePermissions(actionRequest,className,messageId,roleIds,new ArrayList<MBCategory>(),messages);
}

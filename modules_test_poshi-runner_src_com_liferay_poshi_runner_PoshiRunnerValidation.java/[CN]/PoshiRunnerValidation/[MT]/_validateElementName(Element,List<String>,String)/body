{
  if (!possibleElementNames.contains(element.getName())) {
    _logExceptions.add(new Exception("Missing " + possibleElementNames + " element\n"+ filePath+ ":"+ element.attributeValue("line-number")));
  }
}

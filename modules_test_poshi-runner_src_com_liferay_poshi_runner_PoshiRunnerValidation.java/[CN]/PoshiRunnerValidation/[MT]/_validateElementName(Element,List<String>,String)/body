{
  String elementName=element.getName();
  if (!possibleElementNames.contains(elementName)) {
    throw new Exception("Missing " + possibleElementNames + " element\n"+ filePath+ ":"+ element.attributeValue("line-number"));
  }
}

{
  List<Element> childElements=element.elements();
  if (!childElements.isEmpty()) {
    _logExceptions.add(new Exception("Invalid child elements\n" + filePath + ":"+ element.attributeValue("line-number")));
  }
}

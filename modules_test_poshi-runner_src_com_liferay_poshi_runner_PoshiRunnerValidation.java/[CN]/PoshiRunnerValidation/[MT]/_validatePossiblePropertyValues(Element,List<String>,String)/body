{
  String value=element.attributeValue("value");
  if (!possiblePropertyValues.contains(value)) {
    _exceptions.add(new Exception("Invalid " + value + " property value\n"+ filePath+ ":"+ element.attributeValue("line-number")));
  }
}

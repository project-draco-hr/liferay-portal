{
  List<Element> elseElements=element.elements("else");
  if (elseElements.size() > 1) {
    throw new Exception("Too many else elements\n" + filePath + ":"+ element.attributeValue("line-number"));
  }
}

{
  if (value == null) {
    return defaultValue;
  }
  String trimValue=value.trim();
  if (trimValue.indexOf(CharPool.RETURN) != -1) {
    trimValue=StringUtil.replace(trimValue,StringPool.RETURN_NEW_LINE,StringPool.NEW_LINE);
  }
  return trimValue;
}

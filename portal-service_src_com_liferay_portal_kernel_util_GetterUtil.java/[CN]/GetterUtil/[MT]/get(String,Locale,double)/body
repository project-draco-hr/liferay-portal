{
  if (value != null) {
    NumberFormat format=NumberFormat.getInstance(locale);
    try {
      return format.parse(value.trim()).doubleValue();
    }
 catch (    Exception e) {
    }
  }
  return defaultValue;
}

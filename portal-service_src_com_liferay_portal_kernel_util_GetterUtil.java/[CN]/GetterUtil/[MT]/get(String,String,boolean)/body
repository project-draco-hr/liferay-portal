{
  if (value == null) {
    return defaultValue;
  }
  if (!trim) {
    return StringUtil.replace(value,StringPool.RETURN_NEW_LINE,StringPool.NEW_LINE);
  }
  return StringUtil.replace(value.trim(),StringPool.RETURN_NEW_LINE,StringPool.NEW_LINE);
}

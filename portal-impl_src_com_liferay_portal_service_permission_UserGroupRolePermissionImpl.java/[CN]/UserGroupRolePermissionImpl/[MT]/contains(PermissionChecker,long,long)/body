{
  return (permissionChecker.isCommunityOwner(groupId) || GroupPermissionUtil.contains(permissionChecker,groupId,ActionKeys.ASSIGN_USER_ROLES) || RolePermissionUtil.contains(permissionChecker,roleId,ActionKeys.ASSIGN_MEMBERS));
}

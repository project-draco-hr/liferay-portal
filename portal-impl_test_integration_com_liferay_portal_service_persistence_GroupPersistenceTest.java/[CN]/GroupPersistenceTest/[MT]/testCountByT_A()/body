{
  try {
    _persistence.countByT_A(ServiceTestUtil.nextInt(),ServiceTestUtil.randomBoolean());
    _persistence.countByT_A(0,ServiceTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  try {
    _persistence.countByT_A(RandomTestUtil.nextInt(),RandomTestUtil.randomBoolean());
    _persistence.countByT_A(0,RandomTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

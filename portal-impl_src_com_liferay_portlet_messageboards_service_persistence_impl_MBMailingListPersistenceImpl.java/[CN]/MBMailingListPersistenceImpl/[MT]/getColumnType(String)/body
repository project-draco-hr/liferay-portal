{
  Integer type=MBMailingListModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ MBMailingListModelImpl.TABLE_NAME);
  }
  return type;
}

{
  String newContent=_TEST_CONTENT + "\n" + System.currentTimeMillis();
  InputStream inputStream=new UnsyncByteArrayInputStream(newContent.getBytes());
  return DLAppServiceUtil.updateFileEntry(fileEntryId,fileName,ContentTypes.TEXT_PLAIN,fileName,null,null,false,inputStream,newContent.length(),_serviceContext);
}

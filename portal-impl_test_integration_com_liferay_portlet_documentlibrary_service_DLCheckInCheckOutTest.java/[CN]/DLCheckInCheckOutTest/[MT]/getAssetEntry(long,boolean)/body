{
  AssetEntry assetEntry=AssetEntryLocalServiceUtil.fetchEntry(DLFileEntryConstants.getClassName(),assetClassPk);
  if (expectExists) {
    Assert.assertNotNull(assetEntry);
  }
 else {
    Assert.assertNull(assetEntry);
  }
  return assetEntry;
}

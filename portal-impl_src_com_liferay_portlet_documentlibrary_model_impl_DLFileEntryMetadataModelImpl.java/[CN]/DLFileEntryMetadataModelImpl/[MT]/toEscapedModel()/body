{
  if (isEscapedModel()) {
    return (DLFileEntryMetadata)this;
  }
 else {
    return (DLFileEntryMetadata)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

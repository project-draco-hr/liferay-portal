{
  Thread.sleep(1000 * TestPropsValues.JUNIT_DELAY_FACTOR);
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext();
  Hits hits=TrashEntryLocalServiceUtil.search(serviceContext.getCompanyId(),serviceContext.getScopeGroupId(),serviceContext.getUserId(),keywords,QueryUtil.ALL_POS,QueryUtil.ALL_POS,null);
  return hits.getLength();
}

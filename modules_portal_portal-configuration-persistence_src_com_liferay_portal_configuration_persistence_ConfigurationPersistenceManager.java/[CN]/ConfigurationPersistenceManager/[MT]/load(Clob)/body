{
  ReaderInputStream readerInputStream=new ReaderInputStream(clob.getCharacterStream());
  Dictionary<?,?> dictionary=ConfigurationHandler.read(readerInputStream);
  clob.free();
  return dictionary;
}

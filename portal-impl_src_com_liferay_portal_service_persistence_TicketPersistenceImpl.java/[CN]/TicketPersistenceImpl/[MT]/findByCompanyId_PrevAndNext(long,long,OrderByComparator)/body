{
  Ticket ticket=findByPrimaryKey(ticketId);
  Session session=null;
  try {
    session=openSession();
    Ticket[] array=new TicketImpl[3];
    array[0]=getByCompanyId_PrevAndNext(session,ticket,companyId,orderByComparator,true);
    array[1]=ticket;
    array[2]=getByCompanyId_PrevAndNext(session,ticket,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

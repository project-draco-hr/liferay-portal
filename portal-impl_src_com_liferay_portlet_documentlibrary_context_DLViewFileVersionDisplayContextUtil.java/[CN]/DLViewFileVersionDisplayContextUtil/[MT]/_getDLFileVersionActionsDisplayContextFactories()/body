{
  try {
    Registry registry=RegistryUtil.getRegistry();
    return registry.getServices(DLViewFileVersionDisplayContextFactory.class,null);
  }
 catch (  Exception e) {
    throw new SystemException(e);
  }
}

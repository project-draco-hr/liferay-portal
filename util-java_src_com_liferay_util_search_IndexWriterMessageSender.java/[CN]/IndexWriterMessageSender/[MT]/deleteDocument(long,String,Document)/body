{
  try {
    IndexWriterRequest request=new IndexWriterRequest(IndexWriterRequest.DELETE,companyId,uid,doc);
    MessageBusUtil.sendMessage(SearchEngineUtil.INDEX_WRITER_DESTINATION,JSONUtil.serialize(request));
  }
 catch (  MarshallException me) {
    throw new SearchException(me);
  }
}

{
  try {
    IndexWriterRequest req=new IndexWriterRequest(IndexWriterRequest.UPDATE,companyId,uid,doc);
    MessageBusUtil.sendMessage(SearchEngineUtil.INDEX_WRITER_DESTINATION,JSONUtil.serialize(req));
  }
 catch (  MarshallException me) {
    throw new SearchException(me);
  }
}

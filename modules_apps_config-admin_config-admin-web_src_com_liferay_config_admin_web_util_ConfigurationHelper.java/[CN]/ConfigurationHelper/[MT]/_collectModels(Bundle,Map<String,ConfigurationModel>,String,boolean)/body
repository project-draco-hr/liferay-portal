{
  MetaTypeInformation metaTypeInformation=_metaTypeService.getMetaTypeInformation(bundle);
  if (metaTypeInformation == null) {
    return;
  }
  String[] pids=null;
  if (factory) {
    pids=metaTypeInformation.getFactoryPids();
  }
 else {
    pids=metaTypeInformation.getPids();
  }
  for (  String pid : pids) {
    ConfigurationModel model=_getModel(bundle,pid,factory,locale);
    if (model == null) {
      continue;
    }
    modelMap.put(pid,model);
  }
}

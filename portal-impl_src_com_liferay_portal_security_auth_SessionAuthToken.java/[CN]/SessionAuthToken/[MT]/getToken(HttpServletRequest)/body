{
  HttpSession session=request.getSession();
  String authenticationToken=(String)session.getAttribute(WebKeys.AUTHENTICATION_TOKEN);
  if (Validator.isNull(authenticationToken)) {
    authenticationToken=PwdGenerator.getPassword();
    session.setAttribute(WebKeys.AUTHENTICATION_TOKEN,authenticationToken);
  }
  return authenticationToken;
}

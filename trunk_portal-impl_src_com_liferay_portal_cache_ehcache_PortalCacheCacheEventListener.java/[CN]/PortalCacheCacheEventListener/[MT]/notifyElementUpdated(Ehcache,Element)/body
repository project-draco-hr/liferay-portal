{
  Serializable key=element.getKey();
  _cacheListener.notifyEntryUpdated(_portalCache,String.valueOf(key),element.getObjectValue());
  if (_log.isDebugEnabled()) {
    _log.debug("Updated " + key + " in "+ ehcache.getName());
  }
}

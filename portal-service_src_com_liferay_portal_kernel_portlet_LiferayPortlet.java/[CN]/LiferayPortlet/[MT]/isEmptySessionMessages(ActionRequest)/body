{
  if (SessionMessages.isEmpty(actionRequest)) {
    return true;
  }
  int sessionMessagesSize=SessionMessages.size(actionRequest);
  if (SessionMessages.contains(actionRequest,PortalUtil.getPortletId(actionRequest) + SessionMessages.KEY_SUFFIX_DELETE_SUCCESS_DATA)) {
    sessionMessagesSize--;
  }
  if (SessionMessages.contains(actionRequest,PortalUtil.getPortletId(actionRequest) + SessionMessages.KEY_SUFFIX_FORCE_SEND_REDIRECT)) {
    sessionMessagesSize--;
  }
  if (sessionMessagesSize == 0) {
    return true;
  }
  return false;
}

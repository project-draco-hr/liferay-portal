{
  if ((byteBufferResponse.getStatus() == HttpServletResponse.SC_OK) && (byteBufferResponse.getBufferSize() < PropsValues.CACHE_CONTENT_THRESHOLD_SIZE)) {
    return true;
  }
 else {
    return false;
  }
}

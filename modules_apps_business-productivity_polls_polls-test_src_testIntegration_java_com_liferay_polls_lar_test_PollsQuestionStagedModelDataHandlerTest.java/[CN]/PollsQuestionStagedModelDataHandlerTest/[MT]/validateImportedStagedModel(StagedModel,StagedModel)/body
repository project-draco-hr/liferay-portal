{
  super.validateImportedStagedModel(stagedModel,importedStagedModel);
  PollsQuestion question=(PollsQuestion)stagedModel;
  PollsQuestion importedQuestion=(PollsQuestion)importedStagedModel;
  Assert.assertEquals(question.getTitle(),importedQuestion.getTitle());
  Assert.assertEquals(question.getDescription(),importedQuestion.getDescription());
  Assert.assertEquals(question.getExpirationDate(),importedQuestion.getExpirationDate());
  Assert.assertEquals(question.getLastVoteDate(),importedQuestion.getLastVoteDate());
}

{
  try {
    if (_lookupService == null) {
      _lookupService=new LookupService(GEO_DATA_LOCATION,LookupService.GEOIP_MEMORY_CACHE);
    }
  }
 catch (  IOException ioe) {
    _log.error(ioe.getMessage());
  }
}

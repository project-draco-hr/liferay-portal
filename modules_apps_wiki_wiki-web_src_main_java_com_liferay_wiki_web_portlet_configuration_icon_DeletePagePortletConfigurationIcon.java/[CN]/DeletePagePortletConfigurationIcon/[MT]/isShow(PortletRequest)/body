{
  try {
    if (!_page.isDraft() && WikiPagePermissionChecker.contains(themeDisplay.getPermissionChecker(),_page.getNodeId(),HtmlUtil.unescape(_page.getTitle()),ActionKeys.DELETE)) {
      return true;
    }
 else     if (_page.isDraft() && WikiPagePermissionChecker.contains(themeDisplay.getPermissionChecker(),_page,ActionKeys.DELETE)) {
      return true;
    }
  }
 catch (  PortalException pe) {
  }
  return false;
}

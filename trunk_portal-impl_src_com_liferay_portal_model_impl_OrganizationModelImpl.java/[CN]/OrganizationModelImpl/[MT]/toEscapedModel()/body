{
  if (isEscapedModel()) {
    return (Organization)this;
  }
 else {
    return (Organization)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

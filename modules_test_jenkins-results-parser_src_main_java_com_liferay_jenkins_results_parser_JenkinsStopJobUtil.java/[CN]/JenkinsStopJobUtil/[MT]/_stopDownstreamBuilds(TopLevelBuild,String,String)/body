{
  List<DownstreamBuild> downstreamBuilds=topLevelBuild.getDownstreamBuilds("running");
  for (  DownstreamBuild downstreamBuild : downstreamBuilds) {
    _stopBuild(downstreamBuild,username,password);
  }
}

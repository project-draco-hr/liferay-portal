{
  if (args.length < 3) {
    System.out.println("usage: JenkinsStopJobUtil <jobURL> <username> <password>");
    System.exit(1);
  }
  try {
    stopJenkinsJob(args[0],args[1],args[2]);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}

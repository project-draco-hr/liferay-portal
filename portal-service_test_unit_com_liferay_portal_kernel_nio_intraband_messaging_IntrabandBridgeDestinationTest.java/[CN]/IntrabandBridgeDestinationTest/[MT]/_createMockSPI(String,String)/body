{
  MockSPI mockSPI=new MockSPI(){
    @Override public RegistrationReference getRegistrationReference(){
      return _mockRegistrationReference;
    }
  }
;
  mockSPI.spiProviderName=spiProviderName;
  mockSPI.spiConfiguration=new SPIConfiguration(spiId,null,-1,null,null,null);
  return mockSPI;
}

{
  try {
    for (    String languageId : PropsValues.LUCENE_SPELL_CHECKER_SUPPORTED_LOCALES) {
      indexDictionary(searchContext.getCompanyId(),languageId);
    }
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}

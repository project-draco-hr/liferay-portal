{
  int length=word.length();
  if (length < _MINIMUM_WORD_LENGTH) {
    return false;
  }
  if (SpellCheckerUtil.validateWordExists(localizedFieldName,word,indexReaders)) {
    return false;
  }
  return true;
}

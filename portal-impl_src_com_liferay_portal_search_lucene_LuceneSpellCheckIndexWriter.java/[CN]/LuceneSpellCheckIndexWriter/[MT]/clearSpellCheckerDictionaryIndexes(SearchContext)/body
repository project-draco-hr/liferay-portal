{
  Term term=new Term(com.liferay.portal.kernel.search.Field.TYPE,SPELL_CHECKER_TYPE);
  try {
    LuceneHelperUtil.deleteDocuments(searchContext.getCompanyId(),term);
  }
 catch (  IOException e) {
    throw new SearchException(e);
  }
}

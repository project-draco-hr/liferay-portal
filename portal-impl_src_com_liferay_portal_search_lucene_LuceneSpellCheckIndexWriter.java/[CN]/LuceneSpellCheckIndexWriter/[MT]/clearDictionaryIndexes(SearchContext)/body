{
  Term term=new Term(com.liferay.portal.kernel.search.Field.TYPE,DICTIONARY_TYPE);
  try {
    LuceneHelperUtil.deleteDocuments(searchContext.getCompanyId(),term);
  }
 catch (  IOException e) {
    throw new SearchException(e);
  }
}

{
  announcementsEntryPersistence.remove(entry);
  resourceLocalService.deleteResource(entry.getCompanyId(),AnnouncementsEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,entry.getEntryId());
  announcementsFlagLocalService.deleteFlags(entry.getEntryId());
}

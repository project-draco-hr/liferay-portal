{
  if (actionId.equals(ActionKeys.VIEW)) {
    TrashHandler trashHandler=TrashHandlerRegistryUtil.getTrashHandler(entryClassName);
    TrashRenderer trashRenderer=trashHandler.getTrashRenderer(entryClassPK);
    return trashRenderer.hasViewPermission(permissionChecker);
  }
  return false;
}

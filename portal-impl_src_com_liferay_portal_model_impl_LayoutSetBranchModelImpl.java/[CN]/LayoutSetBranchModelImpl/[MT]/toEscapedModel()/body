{
  if (isEscapedModel()) {
    return (LayoutSetBranch)this;
  }
 else {
    return (LayoutSetBranch)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

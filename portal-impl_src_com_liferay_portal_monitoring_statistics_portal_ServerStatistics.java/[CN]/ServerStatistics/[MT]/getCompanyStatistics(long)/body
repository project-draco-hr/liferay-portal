{
  CompanyStatistics statistics=_companyStatisticsByCompanyId.get(companyId);
  if (statistics == null) {
    throw new MonitoringException("No statistics found for: " + companyId);
  }
  return statistics;
}

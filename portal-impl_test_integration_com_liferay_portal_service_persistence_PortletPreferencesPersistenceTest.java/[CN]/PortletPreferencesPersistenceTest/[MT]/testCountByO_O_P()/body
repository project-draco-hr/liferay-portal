{
  try {
    _persistence.countByO_O_P(RandomTestUtil.nextLong(),RandomTestUtil.nextInt(),RandomTestUtil.nextLong());
    _persistence.countByO_O_P(0L,0,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  if (isEscapedModel()) {
    return (ResourcePermission)this;
  }
 else {
    return (ResourcePermission)Proxy.newProxyInstance(ResourcePermission.class.getClassLoader(),new Class[]{ResourcePermission.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (obj == null) {
    return false;
  }
  UserGroupRolePK pk=null;
  try {
    pk=(UserGroupRolePK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((userId.equals(pk.userId)) && (groupId == pk.groupId) && (roleId.equals(pk.roleId))) {
    return true;
  }
 else {
    return false;
  }
}

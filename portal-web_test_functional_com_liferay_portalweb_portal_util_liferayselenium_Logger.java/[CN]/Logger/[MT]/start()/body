{
  String primaryTestSuiteName=_liferaySelenium.getPrimaryTestSuiteName();
  String htmlFileName=_TEST_BASEDIR + "/test/functional-generated/" + StringUtil.replace(primaryTestSuiteName,".","/")+ ".html";
  if (_loggerStarted) {
    return;
  }
  if (FileUtil.exists(htmlFileName)) {
    _webDriver.get("file:///" + htmlFileName);
  }
 else {
    _webDriver.get("file:///" + _TEST_BASEDIR + "/test/functional/com/liferay/"+ "portalweb/portal/util/liferayselenium/dependencies/"+ "Logger.html");
  }
  _loggerStarted=true;
  initializeTooltips();
}

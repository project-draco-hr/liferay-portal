{
  try {
    WebElement webElement=_webDriver.findElement(By.xpath(xPath));
    return (String)_javascriptExecutor.executeScript("var element = arguments[0]; return element.innerHTML;",webElement);
  }
 catch (  Exception e) {
    return null;
  }
}

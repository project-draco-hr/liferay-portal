{
  selenium.open("/web/guest/home/");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isVisible("xPath=(//a[@class='express_login'])[2]")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.saveScreenShotAndSource();
  assertEquals(RuntimeVariables.replace("Login as richard"),selenium.getText("xPath=(//a[@class='express_login'])[2]"));
  selenium.clickAt("xPath=(//a[@class='express_login'])[2]",RuntimeVariables.replace("Login as richard"));
  selenium.waitForPageToLoad("30000");
  selenium.saveScreenShotAndSource();
  assertEquals(RuntimeVariables.replace("Richard Editor"),selenium.getText("//a[@class='user-fullname']"));
  assertEquals(RuntimeVariables.replace("You are signed in as Richard Editor."),selenium.getText("//section[@id='portlet_58']/div/div/div"));
}

{
  WikiPage page=(WikiPage)classedModel;
  String fileName=ServiceTestUtil.randomString() + ".txt";
  Class<?> clazz=getClass();
  byte[] bytes=FileUtil.getBytes(clazz.getResourceAsStream("dependencies/OSX_Test.docx"));
  File file=null;
  if ((bytes != null) && (bytes.length > 0)) {
    file=FileUtil.createTempFile(bytes);
  }
  WikiPageLocalServiceUtil.addPageAttachment(TestPropsValues.getUserId(),page.getNodeId(),page.getTitle(),fileName,file);
}

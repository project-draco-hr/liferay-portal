{
  if (isEscapedModel()) {
    return (ExpandoTable)this;
  }
 else {
    return (ExpandoTable)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  mockStatic(DLAppLocalServiceUtil.class);
  FileEntry fileEntry=mock(FileEntry.class);
  when(fileEntry.getTitle()).thenReturn("File Entry Title");
  when(DLAppLocalServiceUtil.getFileEntryByUuidAndGroupId(Matchers.anyString(),Matchers.anyLong())).thenReturn(fileEntry);
}

{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < folderIds.size(); i++) {
    sb.append(table);
    sb.append(".folderId = ? ");
    if ((i + 1) != folderIds.size()) {
      sb.append("OR ");
    }
  }
  return sb.toString();
}

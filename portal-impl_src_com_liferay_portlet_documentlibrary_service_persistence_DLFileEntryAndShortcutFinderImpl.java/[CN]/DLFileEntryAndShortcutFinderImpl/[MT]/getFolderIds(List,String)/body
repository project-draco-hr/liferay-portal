{
  StringMaker sm=new StringMaker();
  for (int i=0; i < folderIds.size(); i++) {
    sm.append(table);
    sm.append(".folderId = ? ");
    if ((i + 1) != folderIds.size()) {
      sm.append("OR ");
    }
  }
  return sm.toString();
}

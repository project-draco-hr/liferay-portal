{
  if (isEscapedModel()) {
    return (WikiPage)this;
  }
 else {
    return (WikiPage)Proxy.newProxyInstance(WikiPage.class.getClassLoader(),new Class[]{WikiPage.class},new AutoEscapeBeanHandler(this));
  }
}

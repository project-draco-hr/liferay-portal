{
  Country country=findByPrimaryKey(countryId);
  Session session=null;
  try {
    session=openSession();
    Country[] array=new CountryImpl[3];
    array[0]=getByZipCodeRequired_PrevAndNext(session,country,zipCodeRequired,orderByComparator,true);
    array[1]=country;
    array[2]=getByZipCodeRequired_PrevAndNext(session,country,zipCodeRequired,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

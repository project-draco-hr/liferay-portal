{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Country country) method. To improve performance, call update(Country country, boolean merge).");
  }
  return update(country,false);
}

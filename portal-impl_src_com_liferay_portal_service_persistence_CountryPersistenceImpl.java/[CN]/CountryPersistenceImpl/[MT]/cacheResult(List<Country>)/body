{
  for (  Country country : countries) {
    if (EntityCacheUtil.getResult(CountryModelImpl.ENTITY_CACHE_ENABLED,Country.class,country.getPrimaryKey(),this) == null) {
      cacheResult(country);
    }
  }
}

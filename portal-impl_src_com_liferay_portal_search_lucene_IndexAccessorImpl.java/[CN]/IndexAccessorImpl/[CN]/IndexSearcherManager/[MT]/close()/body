{
  IndexSearcher indexSearcher=_indexSearcherAtomicReference.getAndSet(null);
  if (indexSearcher == null) {
    return;
  }
  release(indexSearcher);
  PortalExecutorManagerUtil.shutdown(_LUCENE_SEARCHER_MANAGER_THREAD_POOL);
}

{
  if (_isIndexReadOnly()) {
    return;
  }
  try {
    for (    Document document : documents) {
      _indexWriter.addDocument(document);
    }
    _batchCount++;
  }
  finally {
    _commit();
  }
}

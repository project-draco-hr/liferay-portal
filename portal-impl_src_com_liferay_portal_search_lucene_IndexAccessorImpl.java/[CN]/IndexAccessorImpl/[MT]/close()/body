{
  if (SPIUtil.isSPI()) {
    return;
  }
  try {
    _indexSearcherManager.close();
    _indexWriter.close();
  }
 catch (  Exception e) {
    _log.error("Closing Lucene writer failed for " + _companyId,e);
  }
}

{
  if (SearchEngineUtil.isIndexReadOnly()) {
    return;
  }
synchronized (this) {
    _initIndexWriter();
    try {
      _indexWriter.deleteDocuments(term);
      _documentCount++;
    }
  finally {
      _cleanUp(false);
    }
  }
}

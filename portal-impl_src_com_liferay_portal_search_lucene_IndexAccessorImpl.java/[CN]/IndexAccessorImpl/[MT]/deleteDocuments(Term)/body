{
  if (SearchEngineUtil.isIndexReadOnly()) {
    return;
  }
synchronized (this) {
    initIndexWriter();
    try {
      _indexWriter.deleteDocuments(term);
    }
  finally {
      cleanUp();
    }
  }
}

{
  if (_indexWriter == null) {
    _indexWriter=new IndexWriter(getLuceneDir(),LuceneHelperUtil.getAnalyzer(),IndexWriter.MaxFieldLength.LIMITED);
    _indexWriter.setMergeFactor(PropsValues.LUCENE_MERGE_FACTOR);
  }
}

{
  List<ControlMenuCategory> controlMenuCategories=_controlMenuCategoryServiceTrackerMap.getService(controlMenuCategoryKey);
  if (controlMenuCategories == null) {
    return Collections.emptyList();
  }
  return new ArrayList<>(controlMenuCategories);
}

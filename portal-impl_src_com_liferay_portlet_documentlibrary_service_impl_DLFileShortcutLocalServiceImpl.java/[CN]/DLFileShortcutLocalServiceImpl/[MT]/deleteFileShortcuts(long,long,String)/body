{
  List<DLFileShortcut> fileShortcuts=dlFileShortcutPersistence.findByG_TF_TN(groupId,toFolderId,toName);
  for (  DLFileShortcut fileShortcut : fileShortcuts) {
    deleteFileShortcut(fileShortcut);
  }
}

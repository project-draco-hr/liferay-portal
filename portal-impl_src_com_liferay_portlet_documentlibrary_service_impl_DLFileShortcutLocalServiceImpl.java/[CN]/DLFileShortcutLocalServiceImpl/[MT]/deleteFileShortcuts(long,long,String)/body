{
  List<DLFileShortcut> fileShortcuts=dlFileShortcutPersistence.findByTG_TF_TN(toGroupId,toFolderId,toName);
  for (  DLFileShortcut fileShortcut : fileShortcuts) {
    deleteFileShortcut(fileShortcut);
  }
}

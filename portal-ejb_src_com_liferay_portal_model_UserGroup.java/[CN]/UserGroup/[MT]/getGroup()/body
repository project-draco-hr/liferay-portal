{
  Group group=null;
  try {
    group=GroupLocalServiceUtil.getUserGroupGroup(getCompanyId(),getUserGroupId());
  }
 catch (  Exception e) {
    group=new Group();
    _log.error(e);
  }
  return group;
}

{
  PortletDisplay portletDisplay=themeDisplay.getPortletDisplay();
  return contains(themeDisplay.getPermissionChecker(),themeDisplay.getScopeGroupId(),portletDisplay.getRootPortletId(),controller,actionId);
}

{
  if (!contains(permissionChecker,fileEntryTypeId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DLFileEntryType.class.getName(),fileEntryTypeId,actionId);
  }
}

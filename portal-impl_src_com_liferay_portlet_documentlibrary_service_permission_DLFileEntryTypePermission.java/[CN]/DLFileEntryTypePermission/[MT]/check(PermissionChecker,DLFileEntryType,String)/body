{
  if (!contains(permissionChecker,fileEntryType,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DLFileEntryType.class.getName(),fileEntryType.getFileEntryTypeId(),actionId);
  }
}

{
  LineData lineData=(LineData)children.get(Integer.valueOf(lineNumber));
  if (lineData == null) {
    throw new IllegalStateException("No instrument data for class " + name + " line "+ lineNumber);
  }
  lineData.touchJump(branchNumber,branch,hits);
}

{
  if (layoutSet != null) {
    return GetterUtil.get(layoutSet.getSettingsProperty(layoutSet.getPrivateLayout() + "-robots.txt"),getDefaultRobots(layoutSet.getVirtualHost()));
  }
  return getDefaultRobots(null);
}

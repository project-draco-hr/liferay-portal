{
  if (layoutSet == null) {
    return getDefaultRobots(null);
  }
  Group group=layoutSet.getGroup();
  return GetterUtil.get(group.getTypeSettingsProperty(layoutSet.isPrivateLayout() + "-robots.txt"),getDefaultRobots(PortalUtil.getVirtualHostname(layoutSet)));
}

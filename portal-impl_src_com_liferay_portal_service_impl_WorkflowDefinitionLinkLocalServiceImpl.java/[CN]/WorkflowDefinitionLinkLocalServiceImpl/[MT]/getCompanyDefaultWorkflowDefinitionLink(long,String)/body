{
  long classNameId=PortalUtil.getClassNameId(className);
  Group group=groupLocalService.getCompanyGroup(companyId);
  return workflowDefinitionLinkPersistence.findByG_C_C(group.getGroupId(),companyId,classNameId);
}

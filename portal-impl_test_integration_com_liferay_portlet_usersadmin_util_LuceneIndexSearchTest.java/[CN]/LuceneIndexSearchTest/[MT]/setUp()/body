{
  FinderCacheUtil.clearCache();
  Hits hits=getHits(QueryUtil.ALL_POS,QueryUtil.ALL_POS);
  _initialUsersCount=hits.getLength();
  for (int i=0; i < 5; i++) {
    User user=UserTestUtil.addUser(ServiceTestUtil.randomString(),false,ServiceTestUtil.randomString(),ServiceTestUtil.randomString(),new long[]{TestPropsValues.getGroupId()});
    _users.add(user);
  }
}

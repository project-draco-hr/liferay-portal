{
  if (!UserGroupPermission.contains(getPermissionChecker(),groupId,userGroupId,ActionKeys.UPDATE) && !UserGroupPermission.contains(getPermissionChecker(),groupId,userGroupId,ActionKeys.ASSIGN_USERS)) {
    throw new PrincipalException();
  }
  UserLocalServiceUtil.unsetUserGroupUsers(userGroupId,userIds);
}

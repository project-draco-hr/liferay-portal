{
  if (!UserGroupPermissionUtil.contains(getPermissionChecker(),userGroupId,ActionKeys.UPDATE) && !UserGroupPermissionUtil.contains(getPermissionChecker(),userGroupId,ActionKeys.ASSIGN_USERS)) {
    throw new PrincipalException();
  }
  userLocalService.unsetUserGroupUsers(userGroupId,userIds);
}

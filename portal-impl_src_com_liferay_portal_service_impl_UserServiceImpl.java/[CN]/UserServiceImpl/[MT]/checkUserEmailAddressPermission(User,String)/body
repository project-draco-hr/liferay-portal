{
  PermissionChecker permissionChecker=getPermissionChecker();
  boolean adminEditiableEmailAddress=PropsValues.FIELD_EDITABLE_COM_LIFERAY_PORTAL_MODEL_USER_EMAILADDRESS_ADMIN;
  if (adminEditiableEmailAddress && !permissionChecker.isCompanyAdmin()) {
    throw new UserEmailAddressException();
  }
  if (!user.hasCompanyMx() && user.hasCompanyMx(emailAddress)) {
    Company company=companyPersistence.findByPrimaryKey(user.getCompanyId());
    if (!company.isStrangersWithMx()) {
      throw new ReservedUserEmailAddressException();
    }
  }
}

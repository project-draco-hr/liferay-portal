{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(DLFileEntry dlFileEntry) method. To improve performance, call update(DLFileEntry dlFileEntry, boolean merge).");
  }
  return update(dlFileEntry,false);
}

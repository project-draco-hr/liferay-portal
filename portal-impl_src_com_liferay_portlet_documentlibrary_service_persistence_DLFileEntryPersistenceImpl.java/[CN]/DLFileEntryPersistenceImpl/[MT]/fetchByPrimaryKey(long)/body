{
  DLFileEntry result=(DLFileEntry)EntityCacheUtil.getResult(DLFileEntryModelImpl.ENTITY_CACHE_ENABLED,DLFileEntry.class,fileEntryId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      DLFileEntry dlFileEntry=(DLFileEntry)session.get(DLFileEntryImpl.class,new Long(fileEntryId));
      cacheResult(dlFileEntry);
      return dlFileEntry;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (DLFileEntry)result;
  }
}

{
  DLFileEntry dlFileEntry=(DLFileEntry)EntityCacheUtil.getResult(DLFileEntryModelImpl.ENTITY_CACHE_ENABLED,DLFileEntryImpl.class,fileEntryId,this);
  if (dlFileEntry == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (dlFileEntry == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      dlFileEntry=(DLFileEntry)session.get(DLFileEntryImpl.class,Long.valueOf(fileEntryId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (dlFileEntry == null)) {
        EntityCacheUtil.putResult(DLFileEntryModelImpl.ENTITY_CACHE_ENABLED,DLFileEntryImpl.class,fileEntryId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(dlFileEntry);
      }
      closeSession(session);
    }
  }
  return dlFileEntry;
}

{
  for (  DLFileEntry dlFileEntry : dlFileEntries) {
    if (EntityCacheUtil.getResult(DLFileEntryModelImpl.ENTITY_CACHE_ENABLED,DLFileEntry.class,dlFileEntry.getPrimaryKey(),this) == null) {
      cacheResult(dlFileEntry);
    }
  }
}

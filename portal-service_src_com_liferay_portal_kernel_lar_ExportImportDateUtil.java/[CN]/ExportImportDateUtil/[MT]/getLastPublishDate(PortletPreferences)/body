{
  long lastPublishDate=GetterUtil.getLong(jxPortletPreferences.getValue("last-publish-date",StringPool.BLANK));
  if (lastPublishDate == 0) {
    return null;
  }
  return new Date(lastPublishDate);
}

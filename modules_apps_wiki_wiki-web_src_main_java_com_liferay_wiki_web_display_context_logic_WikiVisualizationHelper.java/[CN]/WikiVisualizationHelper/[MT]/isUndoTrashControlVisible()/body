{
  String logicalPath=_wikiRequestHelper.getLogicalPath();
  if (logicalPath.endsWith("view_page_activities") || logicalPath.endsWith("view_page_attachments")) {
    return false;
  }
  return true;
}

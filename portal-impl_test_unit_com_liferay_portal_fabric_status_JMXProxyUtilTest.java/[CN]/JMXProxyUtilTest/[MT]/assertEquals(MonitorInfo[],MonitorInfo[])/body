{
  Assert.assertEquals(monitorInfos1.length,monitorInfos2.length);
  for (int i=0; i < monitorInfos1.length; i++) {
    Assert.assertEquals(monitorInfos1[i].getClassName(),monitorInfos2[i].getClassName());
    Assert.assertEquals(monitorInfos1[i].getIdentityHashCode(),monitorInfos2[i].getIdentityHashCode());
    Assert.assertEquals(monitorInfos1[i].getLockedStackDepth(),monitorInfos2[i].getLockedStackDepth());
    Assert.assertEquals(monitorInfos1[i].getLockedStackFrame(),monitorInfos2[i].getLockedStackFrame());
  }
}

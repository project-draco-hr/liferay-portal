{
  Assert.assertEquals(threadInfos1.length,threadInfos2.length);
  for (int i=0; i < threadInfos1.length; i++) {
    Assert.assertEquals(threadInfos1[i].toString(),threadInfos2[i].toString());
  }
}

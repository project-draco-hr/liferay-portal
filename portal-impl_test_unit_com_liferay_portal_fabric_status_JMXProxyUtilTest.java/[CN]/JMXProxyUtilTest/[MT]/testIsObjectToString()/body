{
  Assert.assertTrue(JMXProxyUtil.isObjectToString(Object.class.getMethod("toString")));
  Assert.assertFalse(JMXProxyUtil.isObjectToString(Object.class.getMethod("wait")));
class OverrideClass {
    @Override public String toString(){
      return super.toString();
    }
  }
  Assert.assertFalse(JMXProxyUtil.isObjectToString(OverrideClass.class.getMethod("toString")));
}

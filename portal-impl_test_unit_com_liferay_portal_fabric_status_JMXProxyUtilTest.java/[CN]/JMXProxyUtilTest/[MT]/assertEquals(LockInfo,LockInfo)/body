{
  if (lockInfo1 == lockInfo2) {
    return;
  }
  Assert.assertEquals(lockInfo1.getClassName(),lockInfo2.getClassName());
  Assert.assertEquals(lockInfo1.getIdentityHashCode(),lockInfo2.getIdentityHashCode());
}

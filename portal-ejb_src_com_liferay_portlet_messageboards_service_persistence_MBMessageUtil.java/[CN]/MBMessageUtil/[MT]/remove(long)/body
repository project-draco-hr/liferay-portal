{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(messageId));
  }
  com.liferay.portlet.messageboards.model.MBMessage mbMessage=getPersistence().remove(messageId);
  if (listener != null) {
    listener.onAfterRemove(mbMessage);
  }
  return mbMessage;
}

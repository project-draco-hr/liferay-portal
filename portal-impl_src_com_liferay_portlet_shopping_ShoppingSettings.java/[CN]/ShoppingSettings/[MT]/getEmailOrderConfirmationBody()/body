{
  String emailOrderConfirmationBody=typedSettings.getValue("emailOrderConfirmationBody");
  if (Validator.isNotNull(emailOrderConfirmationBody)) {
    return emailOrderConfirmationBody;
  }
  return ContentUtil.get(typedSettings.getValue(PropsKeys.SHOPPING_EMAIL_ORDER_CONFIRMATION_BODY));
}

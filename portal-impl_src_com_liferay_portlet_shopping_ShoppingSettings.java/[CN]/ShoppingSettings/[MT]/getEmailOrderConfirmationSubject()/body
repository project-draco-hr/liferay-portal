{
  String emailOrderConfirmationSubject=typedSettings.getValue("emailOrderConfirmationSubject");
  if (Validator.isNotNull(emailOrderConfirmationSubject)) {
    return emailOrderConfirmationSubject;
  }
  return ContentUtil.get(typedSettings.getValue(PropsKeys.SHOPPING_EMAIL_ORDER_CONFIRMATION_SUBJECT));
}

{
  if (_fileEntry != null) {
    DLFileEntryServiceUtil.deleteFileEntry(_fileEntry.getGroupId(),_fileEntry.getFolderId(),_fileEntry.getName());
  }
  if (_folder != null) {
    DLFolderServiceUtil.deleteFolder(_folder.getFolderId());
  }
  super.tearDown();
}

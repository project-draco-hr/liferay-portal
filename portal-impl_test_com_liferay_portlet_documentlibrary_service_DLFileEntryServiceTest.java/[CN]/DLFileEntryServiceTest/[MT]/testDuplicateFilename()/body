{
  String description=StringPool.BLANK;
  String[] tagsEntries=null;
  boolean addCommunityPermissions=true;
  boolean addGuestPermissions=true;
  String filename="helloworld.txt";
  String extraSettings=StringPool.BLANK;
  String mockTextFile="Hello World text file";
  byte[] byteArray=mockTextFile.getBytes();
  DLFileEntryServiceUtil.addFileEntry(_folder.getFolderId(),filename,filename,description,tagsEntries,extraSettings,byteArray,addCommunityPermissions,addGuestPermissions);
  try {
    DLFileEntryServiceUtil.addFileEntry(_folder.getFolderId(),filename,filename,description,tagsEntries,extraSettings,byteArray,addCommunityPermissions,addGuestPermissions);
    fail("Able to add two files of the name " + filename);
  }
 catch (  DuplicateFileException dfe) {
  }
}

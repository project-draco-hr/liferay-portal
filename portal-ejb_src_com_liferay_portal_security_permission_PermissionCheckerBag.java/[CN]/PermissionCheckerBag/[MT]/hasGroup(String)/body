{
  String key=groupId;
  Boolean value=(Boolean)_hasGroup.get(key);
  if (value == null) {
    value=new Boolean(GroupLocalServiceUtil.hasUserGroup(_userId,groupId));
    _hasGroup.put(key,value);
  }
  return value.booleanValue();
}

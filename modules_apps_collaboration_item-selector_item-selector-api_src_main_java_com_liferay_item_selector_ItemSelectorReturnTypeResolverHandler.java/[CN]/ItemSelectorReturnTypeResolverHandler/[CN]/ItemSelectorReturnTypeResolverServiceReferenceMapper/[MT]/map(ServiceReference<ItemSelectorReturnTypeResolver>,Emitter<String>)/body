{
  ItemSelectorReturnTypeResolver itemSelectorReturnTypeResolver=_bundleContext.getService(serviceReference);
  try {
    emitter.emit(_getKey(itemSelectorReturnTypeResolver.getItemSelectorReturnTypeClass(),itemSelectorReturnTypeResolver.getModelClass()));
  }
  finally {
    _bundleContext.ungetService(serviceReference);
  }
}

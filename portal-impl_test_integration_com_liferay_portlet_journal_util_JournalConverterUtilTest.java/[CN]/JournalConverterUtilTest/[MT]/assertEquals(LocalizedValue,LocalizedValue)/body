{
  Set<Locale> expectedAvailableLocales=expectedLocalizedValue.getAvailableLocales();
  for (  Locale expectedLocale : expectedAvailableLocales) {
    String expectedValue=expectedLocalizedValue.getValue(expectedLocale);
    String actualValue=actualLocalizedValue.getValue(expectedLocale);
    Assert.assertEquals(expectedValue,actualValue);
  }
}

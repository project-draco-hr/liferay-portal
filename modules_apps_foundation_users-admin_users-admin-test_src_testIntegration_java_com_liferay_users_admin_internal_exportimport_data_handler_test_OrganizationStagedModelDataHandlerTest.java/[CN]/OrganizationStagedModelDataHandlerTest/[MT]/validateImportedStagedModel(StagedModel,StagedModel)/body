{
  super.validateImportedStagedModel(stagedModel,importedStagedModel);
  Organization organization=(Organization)stagedModel;
  Organization importedOrganization=(Organization)importedStagedModel;
  Assert.assertEquals(organization.getName(),importedOrganization.getName());
  Assert.assertEquals(organization.getType(),importedOrganization.getType());
  Assert.assertEquals(organization.isRecursable(),importedOrganization.isRecursable());
  Assert.assertEquals(organization.getComments(),importedOrganization.getComments());
}

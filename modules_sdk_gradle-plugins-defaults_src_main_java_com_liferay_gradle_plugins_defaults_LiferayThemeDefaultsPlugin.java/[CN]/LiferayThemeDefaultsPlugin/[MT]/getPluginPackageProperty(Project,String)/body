{
  try {
    Properties properties=FileUtil.readProperties(project,"src/WEB-INF/liferay-plugin-package.properties");
    return Boolean.parseBoolean(properties.getProperty(key));
  }
 catch (  IOException ioe) {
    throw new GradleException("Unable to read liferay-plugin-package.properties",ioe);
  }
}

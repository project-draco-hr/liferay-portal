{
  for (int i=0; i < count; i++) {
    JournalArticle article=JournalTestUtil.addArticle(group.getGroupId(),RandomTestUtil.randomString(),RandomTestUtil.randomString(100));
    assetEntries.add(getAssetEntry(article));
  }
  return assetEntries;
}

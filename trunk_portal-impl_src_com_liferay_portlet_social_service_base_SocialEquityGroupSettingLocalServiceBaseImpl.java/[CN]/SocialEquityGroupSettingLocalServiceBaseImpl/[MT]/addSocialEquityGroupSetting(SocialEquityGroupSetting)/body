{
  socialEquityGroupSetting.setNew(true);
  socialEquityGroupSetting=socialEquityGroupSettingPersistence.update(socialEquityGroupSetting,false);
  Indexer indexer=IndexerRegistryUtil.getIndexer(getModelClassName());
  if (indexer != null) {
    try {
      indexer.reindex(socialEquityGroupSetting);
    }
 catch (    SearchException se) {
      if (_log.isWarnEnabled()) {
        _log.warn(se,se);
      }
    }
  }
  return socialEquityGroupSetting;
}

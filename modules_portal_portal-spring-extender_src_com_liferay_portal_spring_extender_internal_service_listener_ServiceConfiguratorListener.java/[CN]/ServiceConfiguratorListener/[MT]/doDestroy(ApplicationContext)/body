{
  try {
    _serviceConfigurator.destroy();
  }
 catch (  Exception e) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to destroy services in application context " + applicationContext.getDisplayName(),e);
    }
  }
}

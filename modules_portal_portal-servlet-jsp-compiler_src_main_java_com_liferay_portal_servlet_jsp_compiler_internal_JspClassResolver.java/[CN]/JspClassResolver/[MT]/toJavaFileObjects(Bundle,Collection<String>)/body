{
  if ((resources == null) || resources.isEmpty()) {
    return Collections.emptyList();
  }
  List<JavaFileObject> javaFileObjects=new ArrayList<>(resources.size());
  for (  String resource : resources) {
    javaFileObjects.add(getJavaFileObject(bundle.getResource(resource),resource));
  }
  return javaFileObjects;
}

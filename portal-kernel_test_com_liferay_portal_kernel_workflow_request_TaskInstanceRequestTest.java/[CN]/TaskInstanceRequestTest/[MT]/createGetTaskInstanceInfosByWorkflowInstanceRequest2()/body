{
  final long workflowInstanceId=1;
  final boolean completed=false;
  _context.checking(new Expectations(){
{
      oneOf(_factory).createCredential(0);
      will(returnValue(null));
      oneOf(_manager).getTaskInstanceInfosByWorkflowInstance(workflowInstanceId,completed);
    }
  }
);
  TaskInstanceRequest request=TaskInstanceRequest.createGetTaskInstanceInfosByWorkflowInstanceRequest(workflowInstanceId,completed);
  assertTrue(request.hasReturnValue());
  request.execute(_manager);
}

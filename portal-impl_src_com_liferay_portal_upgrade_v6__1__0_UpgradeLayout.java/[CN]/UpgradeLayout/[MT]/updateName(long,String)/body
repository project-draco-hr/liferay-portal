{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update Layout set name = ? where plid = " + plid);
    ps.setString(1,name);
    ps.executeUpdate();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

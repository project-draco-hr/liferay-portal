{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update Layout set title = ? where plid = " + plid);
    ps.setString(1,title);
    ps.executeUpdate();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

{
  try {
    Long newValue=new Long(CounterLocalServiceUtil.increment(Counter.class.getName()));
    if (_trackPKs) {
      _pkMap.put(oldValue,newValue);
    }
    return newValue;
  }
 catch (  Exception e) {
    throw new UpgradeException(e);
  }
}

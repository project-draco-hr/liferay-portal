{
  ResourceLocalServiceUtil.addResources(event.getCompanyId(),event.getGroupId(),event.getUserId(),CalEvent.class.getName(),String.valueOf(event.getPrimaryKey()),false,addCommunityPermissions,addGuestPermissions);
}

{
  CalEventLocalUtil.clearEventsPool(event.getGroupId());
  ResourceLocalServiceUtil.deleteResource(event.getCompanyId(),CalEvent.class.getName(),ResourceImpl.TYPE_CLASS,ResourceImpl.SCOPE_INDIVIDUAL,String.valueOf(event.getPrimaryKey()));
  CalEventUtil.remove(event.getEventId());
}

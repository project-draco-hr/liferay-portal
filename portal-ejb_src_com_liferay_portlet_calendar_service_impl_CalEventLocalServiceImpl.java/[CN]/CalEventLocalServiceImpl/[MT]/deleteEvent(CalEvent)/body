{
  CalEventLocalUtil.clearEventsPool(event.getGroupId());
  ResourceLocalServiceUtil.deleteResource(event.getCompanyId(),CalEvent.class.getName(),Resource.TYPE_CLASS,Resource.SCOPE_INDIVIDUAL,event.getPrimaryKey().toString());
  CalEventUtil.remove(event.getEventId());
}

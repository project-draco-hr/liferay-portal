{
  if (!contains(permissionChecker,license,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),SCLicense.class.getName(),license.getLicenseId(),actionId);
  }
}

{
  for (  ShoppingCoupon shoppingCoupon : shoppingCoupons) {
    if (entityCache.getResult(ShoppingCouponModelImpl.ENTITY_CACHE_ENABLED,ShoppingCouponImpl.class,shoppingCoupon.getPrimaryKey()) == null) {
      cacheResult(shoppingCoupon);
    }
 else {
      shoppingCoupon.resetOriginalValues();
    }
  }
}

{
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext(group.getGroupId());
  BlogsEntry entry=addEntry(false);
  BlogsEntryLocalServiceUtil.addEntryResources(entry.getEntryId(),serviceContext.getGroupPermissions(),serviceContext.getGuestPermissions());
}

{
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext(_group.getGroupId());
  serviceContext.setGroupPermissions(new String[]{ActionKeys.ADD_DISCUSSION});
  serviceContext.setGuestPermissions(new String[]{ActionKeys.VIEW});
  BlogsEntry entry=testAddEntry(false);
  BlogsEntryLocalServiceUtil.addEntryResources(entry.getEntryId(),true,true);
}

{
  User user=TestPropsValues.getUser();
  int initialCount=BlogsEntryLocalServiceUtil.getGroupUserEntriesCount(group.getGroupId(),user.getUserId(),new Date(),queryStatusNotInTrash);
  addEntryTrashAndEntryNotTrash(user);
  int actualCount=BlogsEntryLocalServiceUtil.getGroupUserEntriesCount(group.getGroupId(),user.getUserId(),new Date(),queryStatusNotInTrash);
  Assert.assertEquals(initialCount + 1,actualCount);
}

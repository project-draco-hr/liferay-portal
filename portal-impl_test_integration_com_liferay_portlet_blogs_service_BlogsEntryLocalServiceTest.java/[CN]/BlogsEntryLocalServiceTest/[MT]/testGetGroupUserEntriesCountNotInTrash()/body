{
  int initialCount=BlogsEntryLocalServiceUtil.getGroupUserEntriesCount(_group.getGroupId(),_user.getUserId(),new Date(),_queryStatusNotInTrash);
  addEntryInTrashAndEntryNotInTrash();
  int actualCount=BlogsEntryLocalServiceUtil.getGroupUserEntriesCount(_group.getGroupId(),_user.getUserId(),new Date(),_queryStatusNotInTrash);
  Assert.assertEquals(initialCount + 1,actualCount);
}

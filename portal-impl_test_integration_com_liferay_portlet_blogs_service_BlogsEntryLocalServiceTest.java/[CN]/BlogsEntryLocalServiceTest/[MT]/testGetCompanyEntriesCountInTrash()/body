{
  User user=TestPropsValues.getUser();
  int initialCount=BlogsEntryLocalServiceUtil.getCompanyEntriesCount(user.getCompanyId(),new Date(),queryStatusInTrash);
  addEntryTrashAndEntryNotTrash(user);
  int actualCount=BlogsEntryLocalServiceUtil.getCompanyEntriesCount(user.getCompanyId(),new Date(),queryStatusInTrash);
  Assert.assertEquals(initialCount + 1,actualCount);
}

{
  BlogsEntry expectedEntry=testAddEntry(true);
  BlogsEntry actualEntry=BlogsEntryLocalServiceUtil.getBlogsEntry(expectedEntry.getEntryId());
  BlogsTestUtil.assertEquals(expectedEntry,actualEntry);
}

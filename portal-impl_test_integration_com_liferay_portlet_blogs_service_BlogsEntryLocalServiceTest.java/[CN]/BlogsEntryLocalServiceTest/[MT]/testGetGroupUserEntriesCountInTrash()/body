{
  User user=TestPropsValues.getUser();
  int initialCount=BlogsEntryLocalServiceUtil.getGroupUserEntriesCount(group.getGroupId(),user.getUserId(),new Date(),queryStatusInTrash);
  addEntryTrashAndEntryNotTrash(user);
  int actualCount=BlogsEntryLocalServiceUtil.getGroupUserEntriesCount(group.getGroupId(),user.getUserId(),new Date(),queryStatusInTrash);
  Assert.assertEquals(initialCount + 1,actualCount);
}

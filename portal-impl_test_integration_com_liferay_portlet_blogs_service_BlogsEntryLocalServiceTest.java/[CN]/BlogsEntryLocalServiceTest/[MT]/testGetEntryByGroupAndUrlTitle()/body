{
  BlogsEntry originalEntry=addEntry(false);
  BlogsEntry actualEntry=BlogsEntryLocalServiceUtil.getEntry(originalEntry.getGroupId(),originalEntry.getUrlTitle());
  BlogsTestUtil.assertEqualEntry(originalEntry,actualEntry);
}

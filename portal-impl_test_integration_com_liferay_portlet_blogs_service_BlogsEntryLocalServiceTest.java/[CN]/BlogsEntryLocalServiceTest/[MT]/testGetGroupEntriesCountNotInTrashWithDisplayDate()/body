{
  User user=TestPropsValues.getUser();
  int initialCount=BlogsEntryLocalServiceUtil.getGroupEntriesCount(group.getGroupId(),new Date(),QUERY_NOT_IN_TRASH);
  addEntryTrashAndEntryNotTrash(user);
  int actualCount=BlogsEntryLocalServiceUtil.getGroupEntriesCount(group.getGroupId(),new Date(),QUERY_NOT_IN_TRASH);
  Assert.assertEquals(initialCount + 1,actualCount);
}

{
  _writer=new LuceneIndexWriterImpl();
  _searcher=new LuceneIndexSearcherImpl();
  Destination destination=new ParallelDispatchedDestination(SearchEngineUtil.INDEX_WRITER_DESTINATION,5,10);
  MessageBusUtil.addDestination(destination);
  MessageBusUtil.registerMessageListener(destination.getName(),new LuceneIndexWriterListener());
}

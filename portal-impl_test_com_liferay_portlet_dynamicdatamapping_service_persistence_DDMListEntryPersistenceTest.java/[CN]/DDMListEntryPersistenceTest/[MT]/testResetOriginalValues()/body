{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  DDMListEntry newDDMListEntry=addDDMListEntry();
  _persistence.clearCache();
  DDMListEntryModelImpl existingDDMListEntryModelImpl=(DDMListEntryModelImpl)_persistence.findByPrimaryKey(newDDMListEntry.getPrimaryKey());
  assertTrue(Validator.equals(existingDDMListEntryModelImpl.getUuid(),existingDDMListEntryModelImpl.getOriginalUuid()));
  assertEquals(existingDDMListEntryModelImpl.getGroupId(),existingDDMListEntryModelImpl.getOriginalGroupId());
}

{
  subject=GetterUtil.getString(subject).toLowerCase();
  while (subject.startsWith("re:") || subject.startsWith("re>") || subject.startsWith("fw:")|| subject.startsWith("fw>")) {
    subject=subject.substring(3).trim();
  }
  return subject;
}

{
  ThemeDisplay themeDisplay=initThemeDisplay(company,group,layout,VIRTUAL_HOST_NAME);
  String friendlyURLUsingVirtualHost=PortalUtil.getLayoutURL(layout,themeDisplay,false);
  themeDisplay=initThemeDisplay(company,group,controlPanelLayout,VIRTUAL_HOST_NAME);
  String friendlyURLFromControlPanel=PortalUtil.getLayoutURL(layout,themeDisplay,false);
  Assert.assertEquals(friendlyURLUsingVirtualHost,friendlyURLFromControlPanel);
}

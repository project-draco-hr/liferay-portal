{
  while (length > 0) {
    int blockSize=Math.min(length,_inputBuffer.remaining());
    _inputBuffer.put(bytes,offset,blockSize);
    _doDecode(false);
    length-=blockSize;
    offset+=blockSize;
  }
  if (_autoFlush) {
    _doFlush();
  }
}

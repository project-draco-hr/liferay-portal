{
  List<StagedModel> stagedModels=new ArrayList<StagedModel>();
  WikiNode node=WikiTestUtil.addNode(TestPropsValues.getUserId(),group.getGroupId(),ServiceTestUtil.randomString(),ServiceTestUtil.randomString());
  WikiPage page=WikiTestUtil.addPage(TestPropsValues.getUserId(),group.getGroupId(),node.getNodeId(),ServiceTestUtil.randomString(),true);
  stagedModels.add(page);
  WikiPage draftPage=WikiTestUtil.addPage(TestPropsValues.getUserId(),group.getGroupId(),node.getNodeId(),ServiceTestUtil.randomString(),false);
  stagedModels.add(draftPage);
  return stagedModels;
}

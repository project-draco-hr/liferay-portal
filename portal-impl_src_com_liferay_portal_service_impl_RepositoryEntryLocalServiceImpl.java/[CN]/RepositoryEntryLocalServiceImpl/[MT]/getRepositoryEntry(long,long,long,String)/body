{
  RepositoryEntry repositoryEntry=repositoryEntryPersistence.fetchByR_M(repositoryId,objectId);
  if (repositoryEntry != null) {
    return repositoryEntry;
  }
  return addRepositoryEntry(userId,groupId,repositoryId,objectId,new ServiceContext());
}

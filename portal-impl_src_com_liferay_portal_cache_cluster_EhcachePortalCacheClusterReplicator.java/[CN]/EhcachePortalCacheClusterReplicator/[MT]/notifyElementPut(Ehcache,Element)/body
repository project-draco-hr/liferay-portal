{
  if (!_replicatePuts) {
    return;
  }
  PortalCacheClusterEvent portalCacheClusterEvent=new PortalCacheClusterEvent(ehcache.getName(),element.getObjectKey(),PortalCacheClusterEventType.PUT);
  if (_replicatePutsViaCopy) {
    portalCacheClusterEvent.setElementValue(element.getObjectValue());
  }
  PortalCacheClusterLinkUtil.sendEvent(portalCacheClusterEvent);
}

{
  if (!ClusterReplicationThreadLocal.isReplicate()) {
    return;
  }
  _clusterLinkCacheReplicator.notifyEntryPut(_getPortalCache(ehcache),(K)element.getObjectKey(),(V)element.getObjectValue(),element.getTimeToLive());
}

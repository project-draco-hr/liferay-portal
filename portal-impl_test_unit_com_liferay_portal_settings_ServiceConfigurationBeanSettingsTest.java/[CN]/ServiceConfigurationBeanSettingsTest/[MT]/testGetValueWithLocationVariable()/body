{
  Assert.assertEquals(TEMPLATE_CONTENTS,_serviceConfigurationBeanSettings.getValue("locationVariableValue","defaultValue"));
  List<String> requestedLocations=_mockResourceManager.getRequestedLocations();
  Assert.assertEquals(1,requestedLocations.size());
  Assert.assertEquals("template.ftl",requestedLocations.get(0));
}

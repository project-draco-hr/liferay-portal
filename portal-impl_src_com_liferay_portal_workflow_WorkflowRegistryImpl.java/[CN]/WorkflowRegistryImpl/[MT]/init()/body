{
  Properties props=PropsUtil.getProperties("workflow.process.name",true);
  if (props != null) {
    for (    Object key : props.keySet()) {
      _workflowProcessNames.put((String)key,props.getProperty((String)key));
    }
  }
}

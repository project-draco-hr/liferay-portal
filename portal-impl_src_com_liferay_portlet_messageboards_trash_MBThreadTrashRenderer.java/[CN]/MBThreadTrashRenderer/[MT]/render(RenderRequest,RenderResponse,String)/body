{
  if (template.equals(AssetRenderer.TEMPLATE_ABSTRACT) || template.equals(AssetRenderer.TEMPLATE_FULL_CONTENT)) {
    renderRequest.setAttribute(WebKeys.MESSAGE_BOARDS_MESSAGE,_rootMessage);
    return "/html/portlet/message_boards/asset/" + template + ".jsp";
  }
  return null;
}

{
  List<DDMStructureEntrySoap> soapModels=new ArrayList<DDMStructureEntrySoap>(models.size());
  for (  DDMStructureEntry model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new DDMStructureEntrySoap[soapModels.size()]);
}

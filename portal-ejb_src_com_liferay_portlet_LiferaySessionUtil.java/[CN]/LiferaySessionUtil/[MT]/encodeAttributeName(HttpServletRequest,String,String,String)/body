{
  String servletContextName=(String)req.getAttribute(WebKeys.SERVLET_CONTEXT_NAME);
  if (servletContextName != null) {
    portletName=portletName + Portlet.WAR_SEPARATOR + servletContextName;
  }
  return PortletSessionImpl.getPortletScopeName(portletName,layoutId,attributeName);
}

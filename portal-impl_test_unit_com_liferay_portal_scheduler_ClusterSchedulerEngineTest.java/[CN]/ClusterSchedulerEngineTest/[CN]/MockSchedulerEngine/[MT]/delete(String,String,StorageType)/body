{
  SchedulerResponse schedulerResponse=_defaultJobs.remove(_getFullName(jobName,groupName,storageType));
  if (schedulerResponse != null) {
    return;
  }
  throw new SchedulerException("No jobs with name " + jobName + "in group "+ groupName);
}

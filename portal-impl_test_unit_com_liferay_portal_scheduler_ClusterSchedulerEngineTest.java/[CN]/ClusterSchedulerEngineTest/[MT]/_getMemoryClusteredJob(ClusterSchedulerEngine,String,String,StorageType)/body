{
  if (storageType != StorageType.MEMORY_CLUSTERED) {
    return null;
  }
  Map<String,SchedulerResponse> allJobs=_getMemoryClusteredJobs(clusterSchedulerEngine);
  return allJobs.get(groupName.concat(StringPool.PERIOD).concat(jobName));
}

{
  try {
    ServletContext servletContext=getServletContext();
    HttpServletRequest request=getServletRequest();
    StringServletResponse stringResponse=getServletResponse();
    doTag(getPage(),_align,_allowScriptAccess,_base,_bgcolor,_devicefont,_flashvars,_height,_id,_loop,_menu,_movie,_play,_quality,_salign,_scale,_swliveconnect,_version,_width,_wmode,servletContext,request,stringResponse);
    pageContext.getOut().print(stringResponse.getString());
    return EVAL_PAGE;
  }
 catch (  Exception e) {
    throw new JspException(e);
  }
}

{
  String urlString=url + urlSuffix;
  if (urlString.endsWith("json")) {
    urlString+="?pretty";
  }
  write(new File(dir,urlSuffix),JenkinsResultsParserUtil.downloadToString(JenkinsResultsParserUtil.getLocalURL(urlString)));
}

{
  try {
    context.importComments(Layout.class,GetterUtil.getLong(data),context.getPlid(),context.getGroupId());
    return null;
  }
 catch (  Exception e) {
    throw new PortletDataException(e);
  }
}

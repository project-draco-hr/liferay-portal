{
  List<Resource> folders=new ArrayList<Resource>();
  folders.add(DDMWebDavUtil.toResource(webDavRequest,DDMWebDavUtil.TYPE_STRUCTURES,getRootPath(),true));
  folders.add(DDMWebDavUtil.toResource(webDavRequest,DDMWebDavUtil.TYPE_TEMPLATES,getRootPath(),true));
  return folders;
}

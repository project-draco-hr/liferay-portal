{
  PortalCache cache=_cacheMap.get(name);
  if (cache == null) {
    cache=new PooledMemcachePortalCache(_memcachedClientFactory,_timeout,_timeoutTimeUnit);
    _cacheMap.put(name,cache);
  }
  return cache;
}

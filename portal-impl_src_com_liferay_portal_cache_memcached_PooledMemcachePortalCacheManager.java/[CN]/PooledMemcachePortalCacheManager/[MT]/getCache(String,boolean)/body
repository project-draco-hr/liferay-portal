{
  PortalCache portalCache=_portalCaches.get(name);
  if (portalCache == null) {
    portalCache=new PooledMemcachePortalCache(_memcachedClientFactory,_timeout,_timeoutTimeUnit);
    portalCache.setDebug(_debug);
    _portalCaches.put(name,portalCache);
  }
  return portalCache;
}

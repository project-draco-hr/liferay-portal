{
  final Writer writer=charPipe.getWriter();
  new Thread(){
    public void run(){
      try {
        _writerGeneric=new UnsyncTeeWriter(writer,new FileWriter(_outputDir + "/sample.sql"));
        createSample();
        _writerGeneric.close();
        charPipe.close();
      }
 catch (      Exception e) {
        e.printStackTrace();
      }
    }
  }
.start();
}

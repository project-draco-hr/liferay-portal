{
  Writer writer=_tempFileWriters.get(tableName);
  if (writer == null) {
    writer=new FileWriter(_outputDir + "/" + _TEMP_DIR+ "/"+ tableName);
    _tempFileWriters.put(tableName,writer);
  }
  writer.write(sql);
}

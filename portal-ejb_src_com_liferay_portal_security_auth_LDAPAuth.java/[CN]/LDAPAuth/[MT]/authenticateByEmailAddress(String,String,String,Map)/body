{
  try {
    return authenticate(companyId,emailAddress,StringPool.BLANK,password);
  }
 catch (  Exception e) {
    _log.error(StackTraceUtil.getStackTrace(e));
    throw new AuthException(e);
  }
}

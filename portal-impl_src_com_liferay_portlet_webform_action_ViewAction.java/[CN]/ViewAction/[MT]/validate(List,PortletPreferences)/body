{
  for (int i=1; i < 10; i++) {
    String fieldLabel=prefs.getValue("fieldLabel" + i,StringPool.BLANK);
    if ((Validator.isNotNull(fieldLabel)) && (Validator.isNull((String)fieldValues.get(i - 1)))) {
      return false;
    }
  }
  return true;
}

{
  EmailAddress emailAddress=EmailAddressUtil.findByPrimaryKey(emailAddressId);
  CommonPermission.checkPermission(getPermissionChecker(),emailAddress.getClassName(),emailAddress.getClassPK(),ActionKeys.VIEW);
  return emailAddress;
}

{
  CommonPermission.checkPermission(getPermissionChecker(),className,classPK,ActionKeys.UPDATE);
  return EmailAddressLocalServiceUtil.addEmailAddress(getUserId(),className,classPK,address,typeId,primary);
}

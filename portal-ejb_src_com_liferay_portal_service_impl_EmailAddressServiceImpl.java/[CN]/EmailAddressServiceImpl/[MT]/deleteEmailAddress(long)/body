{
  EmailAddress emailAddress=EmailAddressUtil.findByPrimaryKey(emailAddressId);
  CommonPermission.checkPermission(getPermissionChecker(),emailAddress.getClassName(),emailAddress.getClassPK(),ActionKeys.UPDATE);
  EmailAddressLocalServiceUtil.deleteEmailAddress(emailAddressId);
}

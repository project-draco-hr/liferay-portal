{
  String virtualHostname=name + "." + RandomTestUtil.randomString(3);
  String shardDefaultName=GetterUtil.getString(PropsUtil.get(PropsKeys.SHARD_DEFAULT_NAME));
  return CompanyLocalServiceUtil.addCompany(name,virtualHostname,virtualHostname,shardDefaultName,false,0,true);
}

{
  Iterable<PanelCategory> panelCategories=_childPanelCategoryServiceTrackerMap.getService(parentPanelCategory.getKey());
  if (panelCategories == null) {
    return Collections.emptyList();
  }
  return panelCategories;
}

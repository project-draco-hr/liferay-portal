{
  Iterable<PanelCategory> panelCategories=_childPanelCategoriesServiceTrackerMap.getService(parentPanelCategory.getKey());
  if (panelCategories == null) {
    return Collections.emptyList();
  }
  return panelCategories;
}

{
  Iterable<PanelCategory> panelCategories=_serviceTrackerMap.getService(panelCategory.getKey());
  if (panelCategories == null) {
    return Collections.emptyList();
  }
  return panelCategories;
}

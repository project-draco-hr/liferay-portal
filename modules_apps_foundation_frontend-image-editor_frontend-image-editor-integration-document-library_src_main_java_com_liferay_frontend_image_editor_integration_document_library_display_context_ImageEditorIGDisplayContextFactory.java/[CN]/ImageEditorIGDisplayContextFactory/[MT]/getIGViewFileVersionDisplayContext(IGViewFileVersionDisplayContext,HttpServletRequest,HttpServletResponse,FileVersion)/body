{
  Object model=fileVersion.getModel();
  if (model instanceof DLFileVersion) {
    return new ImageEditorIGViewFileVersionDisplayContext(parentIGViewFileVersionDisplayContext,request,response,fileVersion,_resourceBundleLoader);
  }
  return parentIGViewFileVersionDisplayContext;
}

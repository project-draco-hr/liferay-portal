{
  Map<String,List<StagedModel>> dependentStagedModelsMap=new HashMap<>();
  MBMessage message=MBTestUtil.addMessage(group.getGroupId());
  addDependentStagedModel(dependentStagedModelsMap,MBMessage.class,message);
  return dependentStagedModelsMap;
}

{
  for (  DDLRecord ddlRecord : ddlRecords) {
    if (EntityCacheUtil.getResult(DDLRecordModelImpl.ENTITY_CACHE_ENABLED,DDLRecordImpl.class,ddlRecord.getPrimaryKey(),this) == null) {
      cacheResult(ddlRecord);
    }
  }
}

{
  if (Validator.isNotNull(_entry1ClassName) && Validator.isNotNull(_entry1Uuid)) {
    return;
  }
  AssetEntry entry1=AssetEntryLocalServiceUtil.fetchAssetEntry(_assetLink.getEntryId1());
  if (entry1 == null) {
    return;
  }
  _entry1ClassName=entry1.getClassName();
  _entry1Uuid=entry1.getClassUuid();
}

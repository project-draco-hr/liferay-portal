{
  mockStatic(LinkbackConsumerUtil.class,new CallsRealMethods());
  Class<?> clazz=getClass();
  replace(method(LinkbackConsumerUtil.class,"addNewTrackback")).with(clazz.getMethod("addNewTrackback",Long.TYPE,String.class,String.class));
}

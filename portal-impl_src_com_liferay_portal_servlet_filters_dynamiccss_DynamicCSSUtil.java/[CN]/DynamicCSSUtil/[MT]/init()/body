{
  String scriptPath="com/liferay/portal/servlet/filters/dynamiccss/main.rb";
  try {
    _rubyScript=ContentUtil.get(PortalClassLoaderUtil.getClassLoader(),scriptPath);
    _rubyExecutor.eval(null,new HashMap<String,Object>(),null,"require 'rubygems'\nrequire 'sass'");
  }
 catch (  Exception se) {
    _log.error(se,se);
  }
}

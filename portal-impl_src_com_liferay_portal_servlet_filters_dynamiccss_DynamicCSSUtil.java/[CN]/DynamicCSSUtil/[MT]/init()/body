{
  try {
    String rubyScript=StringUtil.read(ClassLoaderUtil.getPortalClassLoader(),"com/liferay/portal/servlet/filters/dynamiccss" + "/dependencies/main.rb");
    RubyExecutor rubyExecutor=new RubyExecutor();
    _scriptingContainer=rubyExecutor.getScriptingContainer();
    _scriptObject=_scriptingContainer.runScriptlet(rubyScript);
    RTLCSSUtil.init();
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

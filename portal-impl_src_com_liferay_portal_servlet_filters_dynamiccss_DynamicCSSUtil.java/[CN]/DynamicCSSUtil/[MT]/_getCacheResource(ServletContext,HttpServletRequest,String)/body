{
  String cacheFileSuffix=StringPool.BLANK;
  if (PortalUtil.isRightToLeft(request)) {
    cacheFileSuffix="_rtl";
  }
  return servletContext.getResource(SassToCssBuilder.getCacheFileName(resourcePath,cacheFileSuffix));
}

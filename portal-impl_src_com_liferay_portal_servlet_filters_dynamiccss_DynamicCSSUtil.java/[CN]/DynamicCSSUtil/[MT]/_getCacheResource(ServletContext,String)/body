{
  int pos=resourcePath.lastIndexOf(StringPool.SLASH);
  String cacheFileName=resourcePath.substring(0,pos + 1) + ".sass-cache/" + resourcePath.substring(pos + 1);
  return servletContext.getResource(cacheFileName);
}

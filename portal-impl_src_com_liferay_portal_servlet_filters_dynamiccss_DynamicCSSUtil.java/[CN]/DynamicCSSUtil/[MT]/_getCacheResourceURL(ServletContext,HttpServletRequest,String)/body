{
  String suffix=StringPool.BLANK;
  if (PortalUtil.isRightToLeft(request) && !RTLCSSUtil.isExcludedPath(resourcePath)) {
    suffix="_rtl";
  }
  return servletContext.getResource(SassToCssBuilder.getCacheFileName(resourcePath,suffix));
}

{
  File sassTempDir=(File)servletContext.getAttribute(_SASS_DIR_ATTRIBUTE);
  if (sassTempDir != null) {
    return sassTempDir;
  }
  File tempDir=(File)servletContext.getAttribute(JavaConstants.JAVAX_SERVLET_CONTEXT_TEMPDIR);
  sassTempDir=new File(tempDir,_SASS_DIR);
  sassTempDir.mkdirs();
  servletContext.setAttribute(_SASS_DIR_ATTRIBUTE,sassTempDir);
  return sassTempDir;
}

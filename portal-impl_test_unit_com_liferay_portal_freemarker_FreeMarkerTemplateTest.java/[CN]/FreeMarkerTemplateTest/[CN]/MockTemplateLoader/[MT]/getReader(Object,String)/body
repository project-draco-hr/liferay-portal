{
  if (templateSource == _TEMPLATE_FILE_NAME) {
    return new StringReader(_TEST_TEMPLATE_CONTENT);
  }
  throw new ParseException("Unable to get reader for template source " + templateSource,0,0);
}

{
  Integer type=SubscriptionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SubscriptionModelImpl.TABLE_NAME);
  }
  return type;
}

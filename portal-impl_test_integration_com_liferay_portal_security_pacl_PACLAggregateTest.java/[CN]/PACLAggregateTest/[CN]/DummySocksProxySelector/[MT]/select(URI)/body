{
  if (_socketAddresses.contains(uri.getHost() + StringPool.COLON + uri.getPort())) {
    return Collections.singletonList(new Proxy(Type.SOCKS,new InetSocketAddress(0)));
  }
  return Collections.singletonList(Proxy.NO_PROXY);
}

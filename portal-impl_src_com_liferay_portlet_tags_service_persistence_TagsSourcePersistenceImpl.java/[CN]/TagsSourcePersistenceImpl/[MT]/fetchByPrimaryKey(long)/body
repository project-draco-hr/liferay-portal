{
  Session session=null;
  try {
    session=openSession();
    return (TagsSource)session.get(TagsSourceImpl.class,new Long(sourceId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

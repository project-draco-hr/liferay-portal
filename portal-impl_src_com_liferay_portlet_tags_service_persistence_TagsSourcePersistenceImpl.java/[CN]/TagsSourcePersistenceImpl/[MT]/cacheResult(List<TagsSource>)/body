{
  for (  TagsSource tagsSource : tagsSources) {
    if (EntityCacheUtil.getResult(TagsSourceModelImpl.ENTITY_CACHE_ENABLED,TagsSource.class,tagsSource.getPrimaryKey(),this) == null) {
      cacheResult(tagsSource);
    }
  }
}

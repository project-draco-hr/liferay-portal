{
  for (  TagsSource tagsSource : tagsSources) {
    if (EntityCacheUtil.getResult(TagsSourceModelImpl.ENTITY_CACHE_ENABLED,TagsSourceImpl.class,tagsSource.getPrimaryKey(),this) == null) {
      cacheResult(tagsSource);
    }
  }
}

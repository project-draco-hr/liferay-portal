{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TagsSource tagsSource) method. Use update(TagsSource tagsSource, boolean merge) instead.");
  }
  return update(tagsSource,false);
}

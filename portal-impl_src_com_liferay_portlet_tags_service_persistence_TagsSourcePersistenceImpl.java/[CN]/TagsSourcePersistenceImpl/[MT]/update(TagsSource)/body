{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TagsSource tagsSource) method. To improve performance, call update(TagsSource tagsSource, boolean merge).");
  }
  return update(tagsSource,false);
}

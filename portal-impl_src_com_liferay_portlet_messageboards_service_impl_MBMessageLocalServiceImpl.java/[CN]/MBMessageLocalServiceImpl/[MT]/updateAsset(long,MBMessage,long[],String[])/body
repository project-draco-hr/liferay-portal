{
  if (message.isDiscussion()) {
    return;
  }
  assetEntryLocalService.updateEntry(userId,message.getGroupId(),MBMessage.class.getName(),message.getMessageId(),assetCategoryIds,assetTagNames,true,null,null,null,null,ContentTypes.TEXT_HTML,message.getSubject(),null,null,null,0,0,null,false);
}

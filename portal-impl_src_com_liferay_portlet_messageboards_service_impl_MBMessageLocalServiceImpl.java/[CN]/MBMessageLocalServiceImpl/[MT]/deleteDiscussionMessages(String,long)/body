{
  try {
    long classNameId=PortalUtil.getClassNameId(className);
    MBDiscussion discussion=MBDiscussionUtil.findByC_C(classNameId,classPK);
    List messages=MBMessageUtil.findByT_P(discussion.getThreadId(),MBMessageImpl.DEFAULT_PARENT_MESSAGE_ID,0,1);
    if (messages.size() > 0) {
      MBMessage message=(MBMessage)messages.get(0);
      MBThreadLocalServiceUtil.deleteThread(message.getThreadId());
    }
    MBDiscussionUtil.remove(discussion.getDiscussionId());
  }
 catch (  NoSuchDiscussionException nsde) {
  }
}

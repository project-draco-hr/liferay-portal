{
  MBDiscussion discussion=mbDiscussionPersistence.fetchByC_C(classNameId,classPK);
  if (discussion == null) {
    return 0;
  }
  int count=mbMessagePersistence.countByThreadId(discussion.getThreadId());
  if (count >= 1) {
    return count - 1;
  }
 else {
    return 0;
  }
}

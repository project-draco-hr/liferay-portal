{
  if (message.isDiscussion()) {
    return;
  }
  tagsAssetLocalService.updateAsset(userId,message.getCategory().getGroupId(),MBMessage.class.getName(),message.getMessageId(),null,tagsEntries,null,null,null,null,ContentTypes.TEXT_HTML,message.getSubject(),null,null,null,0,0,null,false);
}

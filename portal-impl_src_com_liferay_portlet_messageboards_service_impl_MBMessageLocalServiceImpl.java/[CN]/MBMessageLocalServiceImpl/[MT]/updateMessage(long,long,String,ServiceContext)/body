{
  MBMessage message=mbMessagePersistence.findByPrimaryKey(messageId);
  return updateMessage(userId,messageId,message.getSubject(),body,null,null,message.getPriority(),message.isAllowPingbacks(),serviceContext);
}

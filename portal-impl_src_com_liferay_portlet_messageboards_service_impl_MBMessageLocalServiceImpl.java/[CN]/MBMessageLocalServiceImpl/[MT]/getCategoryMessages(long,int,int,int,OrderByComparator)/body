{
  if (status == StatusConstants.ANY) {
    return mbMessagePersistence.findByCategoryId(categoryId,start,end,obc);
  }
 else {
    return mbMessagePersistence.findByCA_S(categoryId,status,start,end,obc);
  }
}

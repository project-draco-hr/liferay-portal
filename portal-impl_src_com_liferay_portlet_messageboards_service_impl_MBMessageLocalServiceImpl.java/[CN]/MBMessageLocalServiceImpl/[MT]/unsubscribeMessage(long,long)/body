{
  MBMessage message=mbMessagePersistence.findByPrimaryKey(messageId);
  socialEquityLogLocalService.deactivateEquityLogs(userId,MBMessage.class.getName(),messageId,ActionKeys.SUBSCRIBE,StringPool.BLANK);
  subscriptionLocalService.deleteSubscription(userId,MBThread.class.getName(),message.getThreadId());
}

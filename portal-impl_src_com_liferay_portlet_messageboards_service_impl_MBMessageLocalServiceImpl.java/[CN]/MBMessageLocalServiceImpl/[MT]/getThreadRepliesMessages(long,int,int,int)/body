{
  if (status == StatusConstants.ANY) {
    return mbMessagePersistence.findByThreadReplies(threadId,start,end);
  }
 else {
    return mbMessagePersistence.findByT_R_S(threadId,status,start,end);
  }
}

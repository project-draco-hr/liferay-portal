{
  if (status == StatusConstants.APPROVED) {
    return mbMessagePersistence.countByG_C(groupId,categoryId);
  }
 else {
    return mbMessagePersistence.countByG_CA_S(groupId,categoryId,status);
  }
}

{
  if (status == StatusConstants.ANY) {
    return mbMessagePersistence.findByG_C(groupId,categoryId,start,end);
  }
 else {
    return mbMessagePersistence.findByG_CA_S(groupId,categoryId,status,start,end);
  }
}

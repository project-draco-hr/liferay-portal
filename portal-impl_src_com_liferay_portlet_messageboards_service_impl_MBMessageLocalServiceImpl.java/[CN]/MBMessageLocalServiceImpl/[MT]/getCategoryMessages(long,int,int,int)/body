{
  if (status == StatusConstants.ANY) {
    return mbMessagePersistence.findByCategoryId(categoryId,start,end);
  }
 else {
    return mbMessagePersistence.findByCA_S(categoryId,status,start,end);
  }
}

{
  int modelCount=PropsValues.VERIFY_PROCESS_CONCURRENCY_THRESHOLD;
  VerifiableUUIDModel[] verifiableUUIDModels=new VerifiableUUIDModel[modelCount];
  for (int i=0; i < modelCount; i++) {
    verifiableUUIDModels[i]=new VerifiableUUIDModel(){
      @Override public String getPrimaryKeyColumnName(){
        return _UNKNOWN;
      }
      @Override public String getTableName(){
        return _UNKNOWN;
      }
    }
;
  }
  VerifyUUID.verify(verifiableUUIDModels);
}

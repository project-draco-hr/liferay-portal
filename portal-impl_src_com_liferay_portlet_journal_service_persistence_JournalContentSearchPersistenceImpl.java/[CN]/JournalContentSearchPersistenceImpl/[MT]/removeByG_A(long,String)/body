{
  Iterator<JournalContentSearch> itr=findByG_A(groupId,articleId).iterator();
  while (itr.hasNext()) {
    JournalContentSearch journalContentSearch=itr.next();
    remove(journalContentSearch);
  }
}

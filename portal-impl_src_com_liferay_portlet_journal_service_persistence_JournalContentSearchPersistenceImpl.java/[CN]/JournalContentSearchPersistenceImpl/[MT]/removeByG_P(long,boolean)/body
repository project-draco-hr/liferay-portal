{
  Iterator<JournalContentSearch> itr=findByG_P(groupId,privateLayout).iterator();
  while (itr.hasNext()) {
    JournalContentSearch journalContentSearch=itr.next();
    remove(journalContentSearch);
  }
}

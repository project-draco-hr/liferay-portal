{
  Iterator<JournalContentSearch> itr=findByG_P_A(groupId,privateLayout,articleId).iterator();
  while (itr.hasNext()) {
    JournalContentSearch journalContentSearch=itr.next();
    remove(journalContentSearch);
  }
}

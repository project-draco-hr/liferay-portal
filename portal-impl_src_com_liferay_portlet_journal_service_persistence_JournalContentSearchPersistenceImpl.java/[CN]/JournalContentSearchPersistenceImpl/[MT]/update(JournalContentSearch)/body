{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalContentSearch journalContentSearch) method. To improve performance, call update(JournalContentSearch journalContentSearch, boolean merge).");
  }
  return update(journalContentSearch,false);
}

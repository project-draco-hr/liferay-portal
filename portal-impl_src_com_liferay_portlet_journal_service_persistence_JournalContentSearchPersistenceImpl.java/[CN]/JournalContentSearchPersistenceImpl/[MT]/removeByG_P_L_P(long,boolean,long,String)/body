{
  Iterator<JournalContentSearch> itr=findByG_P_L_P(groupId,privateLayout,layoutId,portletId).iterator();
  while (itr.hasNext()) {
    JournalContentSearch journalContentSearch=itr.next();
    remove(journalContentSearch);
  }
}

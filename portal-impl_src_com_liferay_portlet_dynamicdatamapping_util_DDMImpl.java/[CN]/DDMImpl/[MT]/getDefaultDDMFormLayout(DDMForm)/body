{
  DDMFormLayout ddmFormLayout=new DDMFormLayout();
  for (  DDMFormField ddmFormField : ddmForm.getDDMFormFields()) {
    ddmFormLayout.addDDMFormLayoutRow(getDefaultDDMFormLayoutRow(ddmFormField));
  }
  return ddmFormLayout;
}

{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update JournalArticleImage set elName = ? where " + "articleImageId = ?");
    ps.setString(1,elName);
    ps.setLong(2,articleImageId);
    ps.execute();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

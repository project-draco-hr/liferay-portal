{
  MethodCache.remove(LayoutRevisionService.class);
  _service=service;
  ReferenceRegistry.registerReference(LayoutRevisionServiceUtil.class,"_service");
  MethodCache.remove(LayoutRevisionService.class);
}

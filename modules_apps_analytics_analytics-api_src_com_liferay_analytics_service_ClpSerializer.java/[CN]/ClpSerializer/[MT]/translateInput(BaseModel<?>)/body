{
  Class<?> oldModelClass=oldModel.getClass();
  String oldModelClassName=oldModelClass.getName();
  if (oldModelClassName.equals(AnalyticsEventClp.class.getName())) {
    return translateInputAnalyticsEvent(oldModel);
  }
  return oldModel;
}

{
  String[] folderColumns=StringUtil.split(_dlPortletInstanceSettings.getFolderColumns());
  if (!_dlActionsDisplayContext.isShowActions()) {
    folderColumns=ArrayUtil.remove(folderColumns,"action");
  }
  return folderColumns;
}

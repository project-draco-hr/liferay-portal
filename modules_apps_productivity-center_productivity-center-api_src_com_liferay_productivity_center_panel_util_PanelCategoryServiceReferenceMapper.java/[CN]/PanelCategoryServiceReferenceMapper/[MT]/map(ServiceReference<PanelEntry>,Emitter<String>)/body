{
  String panelCategoryKey=(String)serviceReference.getProperty("panel.category.key");
  if (Validator.isNull(panelCategoryKey)) {
    _log.error("Unable to register panel entry because of missing " + "service property \"panel.category.key\"");
  }
 else {
    emitter.emit(panelCategoryKey);
  }
}

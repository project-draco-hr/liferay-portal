{
  try {
    KBArticleServiceUtil.deleteTempAttachment(groupId,resourcePrimKey,fileName,tempFolderName);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

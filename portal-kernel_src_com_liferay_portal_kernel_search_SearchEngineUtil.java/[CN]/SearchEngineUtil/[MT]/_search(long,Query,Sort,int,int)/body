{
  ClassLoader contextClassLoader=_getContextClassLoader();
  try {
    return _instance._getSearcher().search(companyId,query,sort,start,end);
  }
  finally {
    _setContextClassLoader(contextClassLoader);
  }
}

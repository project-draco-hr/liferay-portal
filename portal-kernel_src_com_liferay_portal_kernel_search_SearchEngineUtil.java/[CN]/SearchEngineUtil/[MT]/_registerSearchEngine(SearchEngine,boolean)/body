{
  _registeredSearchEngines.put(engine.getName(),engine);
  _registeredSearchEnginesClassLoaders.put(engine.getName(),Thread.currentThread().getContextClassLoader());
  if (current) {
    _setCurrentSearchEngine(engine.getName());
  }
}

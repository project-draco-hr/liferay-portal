{
  try {
    _syncFilePersistence.updateByFilePathName(parentSyncFile.getFilePathName(),state,uiEvent);
  }
 catch (  SQLException sqle) {
    if (_logger.isDebugEnabled()) {
      _logger.debug(sqle.getMessage(),sqle);
    }
  }
}

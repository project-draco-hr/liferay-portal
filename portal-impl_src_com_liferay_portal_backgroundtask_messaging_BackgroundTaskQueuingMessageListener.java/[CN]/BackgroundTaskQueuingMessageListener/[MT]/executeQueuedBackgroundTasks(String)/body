{
  BackgroundTask backgroundTask=BackgroundTaskManagerUtil.fetchFirstBackgroundTask(taskExecutorClassName,BackgroundTaskConstants.STATUS_QUEUED);
  if (backgroundTask == null) {
    return;
  }
  BackgroundTaskManagerUtil.resumeBackgroundTask(backgroundTask.getBackgroundTaskId());
}

{
  long[] categoryIds=getCategoryIds(groupId,MBCategoryConstants.DEFAULT_PARENT_CATEGORY_ID);
  if (categoryIds.length == 0) {
    return 0;
  }
 else {
    return mbCategoryFinder.filterCountByS_G_U_P(groupId,userId,categoryIds);
  }
}

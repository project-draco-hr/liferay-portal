{
  MBCategory category=mbCategoryPersistence.findByPrimaryKey(categoryId);
  MBCategoryPermission.check(getPermissionChecker(),category,ActionKeys.UPDATE);
  return mbCategoryLocalService.moveCategoryFromTrash(getUserId(),categoryId,newCategoryId);
}

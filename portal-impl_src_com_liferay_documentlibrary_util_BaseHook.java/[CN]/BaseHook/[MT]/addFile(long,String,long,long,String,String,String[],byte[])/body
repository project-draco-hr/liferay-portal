{
  InputStream is=new ByteArrayInputStream(byteArray);
  try {
    addFile(companyId,portletId,groupId,repositoryId,fileName,properties,tagsEntries,is);
  }
  finally {
    try {
      is.close();
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
}

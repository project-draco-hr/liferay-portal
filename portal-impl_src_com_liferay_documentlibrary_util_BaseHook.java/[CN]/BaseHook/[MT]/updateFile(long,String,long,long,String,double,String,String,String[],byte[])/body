{
  InputStream is=new ByteArrayInputStream(byteArray);
  try {
    updateFile(companyId,portletId,groupId,repositoryId,fileName,versionNumber,sourceFileName,properties,tagsEntries,is);
  }
  finally {
    try {
      is.close();
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
}

{
  long nodeId=getNodeId(virtualWiki);
  try {
    WikiPage wikiPage=WikiPageLocalServiceUtil.getPage(nodeId,topicName,true);
    return wikiPage.getTitle();
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return null;
}

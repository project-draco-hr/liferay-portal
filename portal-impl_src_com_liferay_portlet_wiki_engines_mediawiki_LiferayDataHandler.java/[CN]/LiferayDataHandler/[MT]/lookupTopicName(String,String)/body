{
  long nodeId=getNodeId(virtualWiki);
  try {
    WikiPage page=WikiPageLocalServiceUtil.getPage(nodeId,topicName,true);
    return page.getTitle();
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return null;
}

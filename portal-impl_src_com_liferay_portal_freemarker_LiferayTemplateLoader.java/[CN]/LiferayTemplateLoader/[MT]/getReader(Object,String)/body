{
  Reader reader=null;
  for (  FreeMarkerTemplateLoader freeMarkerTemplateLoader : _freeMarkerTemplateLoaders) {
    reader=freeMarkerTemplateLoader.getReader(templateSource,encoding);
  }
  return reader;
}

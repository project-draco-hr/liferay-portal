{
  Reader reader=null;
  for (int i=0; (reader == null) && (i < _templateLoaders.length); i++) {
    if (_templateLoaders[i] != null) {
      reader=_templateLoaders[i].getReader(templateSource,encoding);
    }
  }
  return reader;
}

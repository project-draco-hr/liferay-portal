{
  Object source=null;
  for (int i=0; (source == null) && (i < _templateLoaders.length); i++) {
    if (_templateLoaders[i] != null) {
      source=_templateLoaders[i].findTemplateSource(name);
    }
  }
  return source;
}

{
  List<SocialRequest> membershipRequests=socialRequestPersistence.findByC_C(className,classPK);
  for (  SocialRequest membershipRequest : membershipRequests) {
    deleteRequest(membershipRequest);
  }
}

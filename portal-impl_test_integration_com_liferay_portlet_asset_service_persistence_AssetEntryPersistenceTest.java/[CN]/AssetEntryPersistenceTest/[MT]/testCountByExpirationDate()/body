{
  try {
    _persistence.countByExpirationDate(ServiceTestUtil.nextDate());
    _persistence.countByExpirationDate(ServiceTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

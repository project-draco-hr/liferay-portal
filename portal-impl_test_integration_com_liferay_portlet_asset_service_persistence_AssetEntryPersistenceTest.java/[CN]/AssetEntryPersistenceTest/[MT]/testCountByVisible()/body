{
  try {
    _persistence.countByVisible(ServiceTestUtil.randomBoolean());
    _persistence.countByVisible(ServiceTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

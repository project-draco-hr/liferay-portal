{
  try {
    _persistence.countByPublishDate(ServiceTestUtil.nextDate());
    _persistence.countByPublishDate(ServiceTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

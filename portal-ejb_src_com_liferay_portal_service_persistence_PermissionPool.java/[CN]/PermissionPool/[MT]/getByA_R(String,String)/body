{
  String pk=_instance._getByA_R(actionId,resourceId);
  if (_log.isInfoEnabled()) {
    _log.info("Get A_R" + " " + actionId.toString() + " "+ resourceId.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

{
  Indexer mbMessageIndexer=IndexerRegistryUtil.getIndexer(MBMessageIndexer.class.getName());
  assertNotNull(mbMessageIndexer);
  Indexer mbThreadIndexer=IndexerRegistryUtil.getIndexer(MBThreadIndexer.class.getName());
  assertNotNull(mbThreadIndexer);
}

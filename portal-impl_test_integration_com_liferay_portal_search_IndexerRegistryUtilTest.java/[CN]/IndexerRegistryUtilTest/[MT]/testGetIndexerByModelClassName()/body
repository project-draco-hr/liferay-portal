{
  Indexer userIndexer=IndexerRegistryUtil.getIndexer(User.class.getName());
  assertNotNull(userIndexer);
  Indexer userGroupIndexer=IndexerRegistryUtil.getIndexer(UserGroup.class.getName());
  assertNotNull(userGroupIndexer);
}

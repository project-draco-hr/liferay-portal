{
  WorkflowLog workflowLog=getWorkflowLog(workflowTask);
  if (workflowLog == null) {
    return LanguageUtil.get(_workflowTaskRequestHelper.getRequest(),WorkflowTaskConstants.NEVER);
  }
 else {
    return _dateFormatDateTime.format(workflowLog.getCreateDate());
  }
}

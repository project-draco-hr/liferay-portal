{
  User curUser=null;
  if (workflowLog.getUserId() != 0) {
    curUser=_users.get(workflowLog.getUserId());
  }
  if ((curUser != null) && (workflowLog.getAuditUserId() == curUser.getUserId())) {
    return true;
  }
  return false;
}

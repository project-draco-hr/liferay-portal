{
  User curUser=null;
  if (workflowLog.getUserId() != 0) {
    curUser=_users.get(workflowLog.getUserId());
  }
  return (curUser != null) && (workflowLog.getAuditUserId() == curUser.getUserId());
}

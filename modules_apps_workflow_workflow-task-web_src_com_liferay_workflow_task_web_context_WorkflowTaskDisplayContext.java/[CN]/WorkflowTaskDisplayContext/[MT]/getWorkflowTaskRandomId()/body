{
  String randomId=StringPool.BLANK;
  ResultRow resultRow=(ResultRow)_renderRequest.getAttribute(WebKeys.SEARCH_CONTAINER_RESULT_ROW);
  if (resultRow != null) {
    randomId=StringUtil.randomId();
  }
  return randomId;
}

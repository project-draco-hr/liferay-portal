{
  PortletPreferencesImpl preferences=(PortletPreferencesImpl)PortletPreferencesTestUtil.fetchLayoutPreferences(layout,portlet);
  Assert.assertNull("Any portlet preferences was expected to be defined for " + "(layoutId: " + layout.getPlid() + " and portletId: "+ portlet.getPortletId()+ ")",preferences);
}

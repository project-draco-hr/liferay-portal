{
  if (_counterMatcher) {
    return beanName.equals(_COUNTER_SERVICE_BEAN_NAME);
  }
 else   if (!beanName.equals(_COUNTER_SERVICE_BEAN_NAME) && beanName.endsWith(_SERVICE_SUFFIX)) {
    return true;
  }
  return false;
}

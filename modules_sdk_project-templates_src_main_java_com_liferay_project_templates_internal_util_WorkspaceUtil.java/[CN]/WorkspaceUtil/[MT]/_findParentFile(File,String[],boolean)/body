{
  if (dir == null) {
    return null;
  }
  for (  String fileName : fileNames) {
    File file=new File(dir,fileName);
    if (file.exists()) {
      return dir;
    }
  }
  if (checkParents) {
    return _findParentFile(dir.getParentFile(),fileNames,checkParents);
  }
  return null;
}

{
  try {
    AuthenticationUtils.startSession(userId,password);
    RepositoryServiceSoapBindingStub repositoryService=WebServiceFactory.getRepositoryService();
    Query query=new Query(Constants.QUERY_LANG_LUCENE,"TEXT:'" + keywords + "'");
    QueryResult queryResult=repositoryService.query(_SPACES_STORE,query,false);
    ResultSet resultSet=queryResult.getResultSet();
    return resultSet.getRows();
  }
  finally {
    AuthenticationUtils.endSession();
  }
}

{
  MBMessageLocalService mbMessageLocalService=MBMessageLocalServiceFactory.getService();
  return mbMessageLocalService.updateMessage(userId,messageId,categoryId,subject,body,files,priority,tagsEntries,prefs);
}

{
  return getService().addMessage(userId,userName,groupId,categoryId,threadId,parentMessageId,subject,body,format,files,anonymous,guestEmail,guestName,guestURL,priority,allowPingbacks,serviceContext);
}

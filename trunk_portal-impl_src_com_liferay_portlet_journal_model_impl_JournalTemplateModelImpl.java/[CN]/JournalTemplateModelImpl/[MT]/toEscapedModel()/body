{
  if (isEscapedModel()) {
    return (JournalTemplate)this;
  }
 else {
    return (JournalTemplate)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

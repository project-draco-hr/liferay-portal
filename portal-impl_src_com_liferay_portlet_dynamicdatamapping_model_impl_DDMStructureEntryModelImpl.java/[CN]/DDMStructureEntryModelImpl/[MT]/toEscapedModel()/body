{
  if (isEscapedModel()) {
    return (DDMStructureEntry)this;
  }
 else {
    return (DDMStructureEntry)Proxy.newProxyInstance(DDMStructureEntry.class.getClassLoader(),new Class[]{DDMStructureEntry.class},new AutoEscapeBeanHandler(this));
  }
}

{
  List<DDMStructureEntry> models=new ArrayList<DDMStructureEntry>(soapModels.length);
  for (  DDMStructureEntrySoap soapModel : soapModels) {
    models.add(toModel(soapModel));
  }
  return models;
}

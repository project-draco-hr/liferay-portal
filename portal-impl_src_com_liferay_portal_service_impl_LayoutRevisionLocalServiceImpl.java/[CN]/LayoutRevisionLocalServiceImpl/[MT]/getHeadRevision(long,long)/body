{
  List<LayoutRevision> revisions=layoutRevisionPersistence.findByB_P_S(branchId,plid,WorkflowConstants.STATUS_APPROVED,0,1);
  if (!revisions.isEmpty()) {
    return revisions.get(0);
  }
 else {
    return null;
  }
}

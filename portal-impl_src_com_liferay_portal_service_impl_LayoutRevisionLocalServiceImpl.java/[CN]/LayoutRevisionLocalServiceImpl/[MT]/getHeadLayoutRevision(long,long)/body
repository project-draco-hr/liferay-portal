{
  List<LayoutRevision> layoutRevisions=layoutRevisionPersistence.findByL_P_S(layoutBranchId,plid,WorkflowConstants.STATUS_APPROVED,0,1);
  if (!layoutRevisions.isEmpty()) {
    return layoutRevisions.get(0);
  }
 else {
    throw new NoSuchLayoutRevisionException();
  }
}

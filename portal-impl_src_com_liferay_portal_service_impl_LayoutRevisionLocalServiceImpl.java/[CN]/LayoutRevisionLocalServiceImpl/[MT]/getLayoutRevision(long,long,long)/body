{
  List<LayoutRevision> layoutRevisions=layoutRevisionPersistence.findByL_P_L(layoutSetBranchId,plid,layoutBranchId,0,1,new LayoutRevisionIdComparator(false));
  if (!layoutRevisions.isEmpty()) {
    return layoutRevisions.get(0);
  }
  throw new NoSuchLayoutRevisionException();
}

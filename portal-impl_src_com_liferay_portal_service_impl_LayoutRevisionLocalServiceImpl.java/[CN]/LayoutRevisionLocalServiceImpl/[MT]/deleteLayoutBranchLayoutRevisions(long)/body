{
  List<LayoutRevision> layoutRevisions=layoutRevisionPersistence.findByLayoutBranchId(layoutBranchId);
  for (  LayoutRevision layoutRevision : layoutRevisions) {
    deleteLayoutRevision(layoutRevision);
  }
}

{
  List<LayoutRevision> layoutRevisions=layoutRevisionPersistence.findByL_P_V(layoutSetBranchId,plid,variationName,0,1,new LayoutRevisionIdComparator(false));
  if (!layoutRevisions.isEmpty()) {
    return layoutRevisions.get(0);
  }
  throw new NoSuchLayoutRevisionException();
}

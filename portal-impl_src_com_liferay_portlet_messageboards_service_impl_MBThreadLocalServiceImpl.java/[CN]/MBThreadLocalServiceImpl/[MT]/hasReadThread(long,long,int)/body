{
  User user=userPersistence.findByPrimaryKey(userId);
  if (user.isDefaultUser()) {
    return true;
  }
  int readThreadMessageCount=mbMessageFlagPersistence.countByU_T_F(userId,threadId,MBMessageFlagImpl.READ_FLAG);
  if (readThreadMessageCount != threadMessageCount) {
    return false;
  }
 else {
    return true;
  }
}

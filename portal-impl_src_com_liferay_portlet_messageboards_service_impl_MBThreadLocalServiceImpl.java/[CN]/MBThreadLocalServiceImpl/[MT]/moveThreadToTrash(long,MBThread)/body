{
  if (thread.getCategoryId() == MBCategoryConstants.DISCUSSION_CATEGORY_ID) {
    return thread;
  }
  return updateStatus(userId,thread.getThreadId(),WorkflowConstants.STATUS_IN_TRASH,WorkflowConstants.STATUS_ANY);
}

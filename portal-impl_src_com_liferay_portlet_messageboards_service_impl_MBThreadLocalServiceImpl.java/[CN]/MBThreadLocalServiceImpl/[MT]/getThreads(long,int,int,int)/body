{
  if (status == StatusConstants.ANY) {
    return mbThreadPersistence.findByCategoryId(categoryId,start,end);
  }
 else {
    return mbThreadPersistence.findByC_S(categoryId,status,start,end);
  }
}

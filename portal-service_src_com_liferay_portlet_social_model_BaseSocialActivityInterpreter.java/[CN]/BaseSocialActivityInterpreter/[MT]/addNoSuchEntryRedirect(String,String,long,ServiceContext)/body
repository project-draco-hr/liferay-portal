{
  PortletURL urlView=getViewEntryPortletURL(className,classPK,serviceContext);
  if (urlView == null) {
    return url;
  }
  return HttpUtil.setParameter(url,"noSuchEntryRedirect",urlView.toString());
}

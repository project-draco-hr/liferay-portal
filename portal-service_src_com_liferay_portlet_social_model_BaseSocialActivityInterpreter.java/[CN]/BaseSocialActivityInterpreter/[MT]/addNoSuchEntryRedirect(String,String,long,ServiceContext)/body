{
  PortletURL portletURL=getViewEntryPortletURL(className,classPK,serviceContext);
  if (portletURL == null) {
    return url;
  }
  return HttpUtil.setParameter(url,"noSuchEntryRedirect",portletURL.toString());
}

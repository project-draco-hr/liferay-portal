{
  ResourceBundle resourceBundle=ResourceBundleUtil.getBundle("content.Language",serviceContext.getLocale(),getClass());
  String title=LanguageUtil.get(resourceBundle,HtmlUtil.escape(key));
  if (link == null) {
    return title;
  }
  return buildLink(link,title);
}

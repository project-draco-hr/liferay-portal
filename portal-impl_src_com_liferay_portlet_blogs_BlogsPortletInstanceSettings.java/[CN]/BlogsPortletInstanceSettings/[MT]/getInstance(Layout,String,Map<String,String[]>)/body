{
  Settings settings=SettingsFactoryUtil.getSettings(new PortletInstanceSettingsLocator(layout,portletId));
  return new BlogsPortletInstanceSettings(new ParameterMapSettings(parameterMap,settings));
}

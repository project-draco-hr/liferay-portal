{
  if (format.equals("rss")) {
    addSearchResultRSS(root,title,link,updated,summary,tags,ratings,score);
  }
 else {
    addSearchResultAtom(root,title,link,updated,summary,tags,ratings,score);
  }
}

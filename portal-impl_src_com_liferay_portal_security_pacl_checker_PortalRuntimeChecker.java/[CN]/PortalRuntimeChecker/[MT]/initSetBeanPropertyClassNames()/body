{
  _setBeanPropertyClassNames=getPropertySet("security-manager-set-bean-property");
  if (_log.isDebugEnabled()) {
    Set<String> classNames=new TreeSet<String>(_setBeanPropertyClassNames);
    for (    String className : classNames) {
      _log.debug("Allowing set bean property on class " + className);
    }
  }
}

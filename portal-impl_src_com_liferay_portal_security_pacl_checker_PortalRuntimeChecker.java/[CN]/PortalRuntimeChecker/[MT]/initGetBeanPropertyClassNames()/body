{
  _getBeanPropertyClassNames=getPropertySet("security-manager-get-bean-property");
  if (_log.isDebugEnabled()) {
    Set<String> classNames=new TreeSet<String>(_getBeanPropertyClassNames);
    for (    String className : classNames) {
      _log.debug("Allowing get bean property name on class " + className);
    }
  }
}

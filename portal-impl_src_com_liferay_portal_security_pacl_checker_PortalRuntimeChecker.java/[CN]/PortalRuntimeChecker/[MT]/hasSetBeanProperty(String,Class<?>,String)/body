{
  String className=clazz.getName();
  if (servletContextName.equals(getServletContextName())) {
    return true;
  }
  Set<String> _setBeanPropertyClassNames=_setBeanPropertyClassNamesMap.get(servletContextName);
  if (_setBeanPropertyClassNames == null) {
    return false;
  }
  if (_setBeanPropertyClassNames.contains(className)) {
    return true;
  }
  if (Validator.isNotNull(property)) {
    if (_setBeanPropertyClassNames.contains(className.concat(StringPool.POUND).concat(property))) {
      return true;
    }
  }
  return false;
}

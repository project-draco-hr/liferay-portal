{
  BaseRepository baseRepository=createExternalRepositoryImpl(repositoryId,classNameId);
  LocalRepository localRepository=baseRepository.getLocalRepository();
  return new CapabilityLocalRepository(localRepository,getDefaultExternalCapabilities(),getDefaultExternalExports());
}

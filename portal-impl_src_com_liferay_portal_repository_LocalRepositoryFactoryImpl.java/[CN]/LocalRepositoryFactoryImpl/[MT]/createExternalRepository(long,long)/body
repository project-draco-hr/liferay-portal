{
  RepositoryCreator externalRepositoryCreator=getExternalRepositoryCreator();
  LocalRepository localRepository=externalRepositoryCreator.createLocalRepository(repositoryId);
  return new CapabilityLocalRepository(localRepository,getExternalSupportedCapabilities(),getExternalExportedCapabilityClasses());
}

{
  LocalRepository localRepository=createLiferayRepository(folderId,fileEntryId,fileVersionId);
  if (localRepository != null) {
    return localRepository;
  }
  long repositoryId=getRepositoryId(folderId,fileEntryId,fileVersionId);
  BaseRepository baseRepository=(BaseRepository)RepositoryFactoryUtil.create(repositoryId);
  return baseRepository.getLocalRepository();
}

{
  URL url=_getEntryInBundleOrFragments(name);
  if (url == null) {
    if (name.startsWith("/")) {
      name=name.substring(1);
    }
    if (!name.startsWith("META-INF/resources/")) {
      url=_getEntryInBundleOrFragments("META-INF/resources/" + name);
    }
  }
  return url;
}

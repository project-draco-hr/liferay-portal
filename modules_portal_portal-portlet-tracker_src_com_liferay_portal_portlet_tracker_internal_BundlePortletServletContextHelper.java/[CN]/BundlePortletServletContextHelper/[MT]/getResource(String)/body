{
  URL url=_getResourceInBundleOrFragments(name);
  if (url == null) {
    if (name.startsWith("/")) {
      name=name.substring(1);
    }
    if (!name.startsWith("META-INF/resources/")) {
      url=_getResourceInBundleOrFragments("META-INF/resources/" + name);
    }
  }
  return url;
}

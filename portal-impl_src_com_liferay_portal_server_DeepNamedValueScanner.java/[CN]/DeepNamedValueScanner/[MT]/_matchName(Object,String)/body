{
  if (name == null) {
    return;
  }
  _matchingCount++;
  name=name.toLowerCase();
  if (name.contains(_value)) {
    if (_skipFirstCount > 0) {
      _skipFirstCount--;
      return;
    }
    _matchedValue=value;
    _scanning=false;
  }
}

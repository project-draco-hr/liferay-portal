{
  if (name == null) {
    return;
  }
  _matchingCount++;
  if (name.toLowerCase().contains(_value)) {
    if (_skipFirstCount > 0) {
      _skipFirstCount--;
      return;
    }
    _matchedValue=value;
    _scanning=false;
  }
}

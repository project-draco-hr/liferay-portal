{
  if (name == null) {
    return;
  }
  _matchingCount++;
  name=StringUtil.toLowerCase(name);
  if (name.contains(_value)) {
    if (_skipFirstCount > 0) {
      _skipFirstCount--;
      return;
    }
    _matchedValue=value;
    _scanning=false;
  }
}

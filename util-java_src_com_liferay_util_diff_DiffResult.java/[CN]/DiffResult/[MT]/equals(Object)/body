{
  DiffResult diffResult=(DiffResult)obj;
  if ((diffResult.getChangeType().equals(_changeType)) && (diffResult.getLineNumber() == _lineNumber) && (diffResult.getChangedLines().equals(_changedLines))) {
    return true;
  }
  return false;
}

{
  StringMaker sm=new StringMaker();
  sm.append("Line: ");
  sm.append(_lineNumber);
  sm.append("\n");
  Iterator<String> itr=_changedLines.iterator();
  while (itr.hasNext()) {
    sm.append(itr.next());
    if (itr.hasNext()) {
      sm.append("\n");
    }
  }
  return sm.toString();
}

{
  StringBuilder sb=new StringBuilder();
  sb.append("Line: ");
  sb.append(_lineNumber);
  sb.append("\n");
  Iterator<String> itr=_changedLines.iterator();
  while (itr.hasNext()) {
    sb.append(itr.next());
    if (itr.hasNext()) {
      sb.append("\n");
    }
  }
  return sb.toString();
}

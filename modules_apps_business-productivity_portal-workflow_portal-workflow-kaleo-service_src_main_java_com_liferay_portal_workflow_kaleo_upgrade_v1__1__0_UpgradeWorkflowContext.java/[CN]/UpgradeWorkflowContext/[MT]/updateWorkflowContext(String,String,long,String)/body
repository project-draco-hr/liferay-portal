{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update " + tableName + " set workflowContext = ? where "+ fieldName+ " = ?");
    ps.setString(1,workflowContext);
    ps.setLong(2,fieldValue);
    ps.executeUpdate();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

{
  try (PreparedStatement ps=connection.prepareStatement("update " + tableName + " set workflowContext = ? where "+ fieldName+ " = ?")){
    ps.setString(1,workflowContext);
    ps.setLong(2,fieldValue);
    ps.executeUpdate();
  }
 }

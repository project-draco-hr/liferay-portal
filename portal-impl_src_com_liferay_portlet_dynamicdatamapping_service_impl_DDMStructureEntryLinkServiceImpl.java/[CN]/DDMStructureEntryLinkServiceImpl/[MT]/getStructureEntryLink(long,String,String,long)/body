{
  DDMStructureEntryPermission.check(getPermissionChecker(),groupId,structureKey,ActionKeys.VIEW);
  return ddmStructureEntryLinkLocalService.getStructureEntryLink(structureKey,className,classPK);
}

{
  DDMPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_STRUCTURE_ENTRY);
  return ddmStructureEntryLinkLocalService.addStructureEntryLink(structureId,className,classPK,serviceContext);
}

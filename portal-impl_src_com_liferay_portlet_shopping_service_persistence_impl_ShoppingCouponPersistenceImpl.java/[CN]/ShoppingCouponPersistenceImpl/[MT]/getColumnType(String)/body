{
  Integer type=ShoppingCouponModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ShoppingCouponModelImpl.TABLE_NAME);
  }
  return type;
}

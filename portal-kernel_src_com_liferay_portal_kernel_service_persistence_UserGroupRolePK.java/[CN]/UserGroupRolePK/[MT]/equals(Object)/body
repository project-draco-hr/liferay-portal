{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof UserGroupRolePK)) {
    return false;
  }
  UserGroupRolePK pk=(UserGroupRolePK)obj;
  if ((userId == pk.userId) && (groupId == pk.groupId) && (roleId == pk.roleId)) {
    return true;
  }
 else {
    return false;
  }
}

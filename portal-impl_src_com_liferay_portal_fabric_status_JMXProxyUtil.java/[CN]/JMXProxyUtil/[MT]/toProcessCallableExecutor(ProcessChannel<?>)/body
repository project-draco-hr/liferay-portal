{
  return new ProcessCallableExecutor(){
    @Override public <V extends Serializable>NoticeableFuture<V> execute(    ProcessCallable<V> processCallable) throws ProcessException {
      return processChannel.write(processCallable);
    }
  }
;
}

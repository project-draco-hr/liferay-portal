{
  IGImage igImage=igImageFinder.fetchByAnyImageId(imageId);
  if (igImage != null) {
    IGImagePermission.check(getPermissionChecker(),igImage,ActionKeys.VIEW);
  }
  return imageLocalService.getImage(imageId);
}

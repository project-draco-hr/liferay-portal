{
  DLFileEntry fileEntry=dlFileEntryFinder.fetchByAnyImageId(imageId);
  if (fileEntry != null) {
    DLFileEntryPermission.check(getPermissionChecker(),fileEntry,ActionKeys.VIEW);
  }
  return imageLocalService.getImage(imageId);
}

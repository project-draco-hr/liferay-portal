{
  MemoryPortalCache<K,V> memoryPortalCache=_memoryPortalCaches.remove(portalCacheName);
  memoryPortalCache.destroy();
  aggregatedPortalCacheManagerListener.notifyPortalCacheRemoved(portalCacheName);
}

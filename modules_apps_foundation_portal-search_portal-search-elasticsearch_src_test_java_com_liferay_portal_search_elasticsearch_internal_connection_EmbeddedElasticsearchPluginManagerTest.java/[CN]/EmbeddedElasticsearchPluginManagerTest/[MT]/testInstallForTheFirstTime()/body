{
  install();
  Mockito.verify(_pluginManager).downloadAndExtract(Mockito.eq(_PLUGIN_NAME),Mockito.<Terminal>any(),Mockito.eq(true));
  Mockito.verify(_pluginZipFactory).createPluginZip("/plugins/" + _PLUGIN_NAME + "-"+ Version.CURRENT+ ".zip");
  Mockito.verify(_pluginZip).delete();
}

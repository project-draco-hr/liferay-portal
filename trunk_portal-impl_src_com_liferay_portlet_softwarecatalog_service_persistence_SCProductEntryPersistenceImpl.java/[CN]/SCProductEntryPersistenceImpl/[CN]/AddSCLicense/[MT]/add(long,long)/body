{
  if (!_persistenceImpl.containsSCLicense.contains(productEntryId,licenseId)) {
    ModelListener<com.liferay.portlet.softwarecatalog.model.SCLicense>[] scLicenseListeners=scLicensePersistence.getListeners();
    for (    ModelListener<SCProductEntry> listener : listeners) {
      listener.onBeforeAddAssociation(productEntryId,com.liferay.portlet.softwarecatalog.model.SCLicense.class.getName(),licenseId);
    }
    for (    ModelListener<com.liferay.portlet.softwarecatalog.model.SCLicense> listener : scLicenseListeners) {
      listener.onBeforeAddAssociation(licenseId,SCProductEntry.class.getName(),productEntryId);
    }
    _sqlUpdate.update(new Object[]{new Long(productEntryId),new Long(licenseId)});
    for (    ModelListener<SCProductEntry> listener : listeners) {
      listener.onAfterAddAssociation(productEntryId,com.liferay.portlet.softwarecatalog.model.SCLicense.class.getName(),licenseId);
    }
    for (    ModelListener<com.liferay.portlet.softwarecatalog.model.SCLicense> listener : scLicenseListeners) {
      listener.onAfterAddAssociation(licenseId,SCProductEntry.class.getName(),productEntryId);
    }
  }
}

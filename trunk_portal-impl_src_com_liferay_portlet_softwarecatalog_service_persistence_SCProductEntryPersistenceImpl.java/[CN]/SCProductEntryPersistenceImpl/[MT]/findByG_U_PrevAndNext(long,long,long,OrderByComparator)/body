{
  SCProductEntry scProductEntry=findByPrimaryKey(productEntryId);
  Session session=null;
  try {
    session=openSession();
    SCProductEntry[] array=new SCProductEntryImpl[3];
    array[0]=getByG_U_PrevAndNext(session,scProductEntry,groupId,userId,orderByComparator,true);
    array[1]=scProductEntry;
    array[2]=getByG_U_PrevAndNext(session,scProductEntry,groupId,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

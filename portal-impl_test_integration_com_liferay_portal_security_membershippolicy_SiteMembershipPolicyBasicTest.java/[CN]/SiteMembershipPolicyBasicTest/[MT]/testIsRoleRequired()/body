{
  long[] userIds=addUsers();
  long[] standardSiteIds=addStandardGroups();
  long[] requiredRoleIds=addRequiredRoles();
  Assert.assertTrue(SiteMembershipPolicyUtil.isRoleRequired(userIds[0],standardSiteIds[0],requiredRoleIds[0]));
}

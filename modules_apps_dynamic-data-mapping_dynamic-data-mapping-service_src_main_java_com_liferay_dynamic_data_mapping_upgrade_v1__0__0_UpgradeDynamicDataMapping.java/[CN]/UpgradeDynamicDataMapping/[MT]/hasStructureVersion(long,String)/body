{
  PreparedStatement ps=null;
  ResultSet rs=null;
  try {
    ps=connection.prepareStatement("select * from DDMStructureVersion where structureId = ? and " + "version = ?");
    ps.setLong(1,structureId);
    ps.setString(2,version);
    rs=ps.executeQuery();
    return rs.next();
  }
  finally {
    DataAccess.cleanUp(ps,rs);
  }
}

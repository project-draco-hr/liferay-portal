{
  try {
    DLFolder dlFolder=_dlFolderLocalService.getFolder(groupId,parentFolderId,name);
    return dlFolder.getFolderId();
  }
 catch (  PortalException e) {
    if (_log.isWarnEnabled()) {
      _log.warn("Unable to get DLfolder ID " + e);
    }
    return 0;
  }
}

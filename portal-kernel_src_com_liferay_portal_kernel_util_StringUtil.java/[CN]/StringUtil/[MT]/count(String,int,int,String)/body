{
  if ((s == null) || s.isEmpty() || ((end - start) < 1)|| (text == null)|| text.isEmpty()) {
    return 0;
  }
  int count=0;
  int pos=start;
  while ((pos < end) && (pos=s.indexOf(text,pos)) != -1) {
    if (pos < end) {
      count++;
    }
    pos+=text.length();
  }
  return count;
}

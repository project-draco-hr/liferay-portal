{
  if (isNew) {
    Object[] args=new Object[]{appModelImpl.getRemoteAppId()};
    finderCache.putResult(FINDER_PATH_COUNT_BY_REMOTEAPPID,args,Long.valueOf(1));
    finderCache.putResult(FINDER_PATH_FETCH_BY_REMOTEAPPID,args,appModelImpl);
  }
 else {
    if ((appModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_REMOTEAPPID.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{appModelImpl.getRemoteAppId()};
      finderCache.putResult(FINDER_PATH_COUNT_BY_REMOTEAPPID,args,Long.valueOf(1));
      finderCache.putResult(FINDER_PATH_FETCH_BY_REMOTEAPPID,args,appModelImpl);
    }
  }
}

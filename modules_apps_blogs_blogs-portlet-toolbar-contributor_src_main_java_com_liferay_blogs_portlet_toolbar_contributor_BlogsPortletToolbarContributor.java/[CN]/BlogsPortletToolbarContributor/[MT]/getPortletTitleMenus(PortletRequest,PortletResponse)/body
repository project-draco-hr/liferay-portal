{
  Menu addEntryPortletTitleMenu=getAddEntryPortletTitleMenu(portletRequest);
  if (addEntryPortletTitleMenu == null) {
    return Collections.emptyList();
  }
  List<Menu> menus=new ArrayList<>();
  menus.add(addEntryPortletTitleMenu);
  return menus;
}

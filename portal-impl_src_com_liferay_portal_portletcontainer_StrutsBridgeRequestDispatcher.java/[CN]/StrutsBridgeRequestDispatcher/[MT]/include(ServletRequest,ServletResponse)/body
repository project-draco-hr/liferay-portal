{
  if (servletRequest.getAttribute(JavaConstants.JAVAX_PORTLET_REQUEST) != null) {
    invoke(servletRequest,servletResponse,true);
  }
 else {
    _requestDispatcher.include(servletRequest,servletResponse);
  }
}

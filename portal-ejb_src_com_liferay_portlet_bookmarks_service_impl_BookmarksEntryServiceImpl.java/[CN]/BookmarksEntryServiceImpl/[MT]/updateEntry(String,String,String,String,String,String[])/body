{
  BookmarksEntryPermission.check(getPermissionChecker(),entryId,ActionKeys.UPDATE);
  return BookmarksEntryLocalServiceUtil.updateEntry(getUser().getCompanyId(),entryId,folderId,name,url,comments,tagsEntries);
}

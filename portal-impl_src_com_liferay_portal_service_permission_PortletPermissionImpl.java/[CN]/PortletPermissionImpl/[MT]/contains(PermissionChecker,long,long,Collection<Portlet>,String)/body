{
  for (  Portlet portlet : portlets) {
    if (permissionChecker.hasPermission(groupId,portlet.getPortletId(),portlet.getPortletId(),ActionKeys.ACCESS_IN_CONTROL_PANEL)) {
      return true;
    }
  }
  return false;
}

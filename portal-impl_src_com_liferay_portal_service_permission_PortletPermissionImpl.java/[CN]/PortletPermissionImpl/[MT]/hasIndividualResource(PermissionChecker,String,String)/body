{
  int count=ResourcePermissionLocalServiceUtil.getResourcePermissionsCount(permissionChecker.getCompanyId(),name,ResourceConstants.SCOPE_INDIVIDUAL,primKey);
  if (count == 0) {
    return false;
  }
  return true;
}

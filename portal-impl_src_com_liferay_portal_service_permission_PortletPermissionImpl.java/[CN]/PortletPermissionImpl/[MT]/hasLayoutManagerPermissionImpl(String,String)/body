{
  portletId=PortletImpl.getRootPortletId(portletId);
  List layoutManagerDefaultActions=ResourceActionsUtil.getPortletResourceLayoutManagerDefaultActions(portletId);
  return layoutManagerDefaultActions.contains(actionId);
}

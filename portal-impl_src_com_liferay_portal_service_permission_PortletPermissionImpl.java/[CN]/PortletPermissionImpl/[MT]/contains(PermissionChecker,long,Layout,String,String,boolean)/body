{
  Portlet portlet=PortletLocalServiceUtil.getPortletById(portletId);
  if (portlet != null) {
    return contains(permissionChecker,groupId,layout,portlet,actionId,strict);
  }
 else {
    return doContains(permissionChecker,groupId,layout,portletId,actionId,strict);
  }
}

{
  Iterator<JournalArticleImage> itr=findByG_A_V(groupId,articleId,version).iterator();
  while (itr.hasNext()) {
    JournalArticleImage journalArticleImage=itr.next();
    remove(journalArticleImage);
  }
}

{
  journalArticleImage=toUnwrappedModel(journalArticleImage);
  Session session=null;
  try {
    session=openSession();
    if (journalArticleImage.isCachedModel()) {
      journalArticleImage=(JournalArticleImage)session.get(JournalArticleImageImpl.class,journalArticleImage.getPrimaryKeyObj());
    }
    if (journalArticleImage != null) {
      session.delete(journalArticleImage);
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  if (journalArticleImage != null) {
    clearCache(journalArticleImage);
  }
  return journalArticleImage;
}

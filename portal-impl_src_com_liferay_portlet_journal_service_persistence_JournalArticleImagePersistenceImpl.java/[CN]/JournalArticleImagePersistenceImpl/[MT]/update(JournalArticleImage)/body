{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalArticleImage journalArticleImage) method. To improve performance, call update(JournalArticleImage journalArticleImage, boolean merge).");
  }
  return update(journalArticleImage,false);
}

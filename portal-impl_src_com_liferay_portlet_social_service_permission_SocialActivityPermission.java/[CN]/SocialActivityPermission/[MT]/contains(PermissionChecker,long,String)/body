{
  if (permissionChecker.isGroupAdmin(groupId) || permissionChecker.isGroupOwner(groupId)) {
    return true;
  }
  if (permissionChecker.hasPermission(groupId,PortletKeys.SOCIAL_ACTIVITY,0,actionId)) {
    return true;
  }
  return false;
}

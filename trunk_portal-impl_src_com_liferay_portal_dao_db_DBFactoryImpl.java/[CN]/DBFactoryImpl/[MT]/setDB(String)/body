{
  if (_db == null) {
    _db=getDB(type);
    if (_db == null) {
      _log.error("No DB implementation exists for " + type);
    }
 else {
      if (_log.isDebugEnabled()) {
        _log.debug("Using DB implementation " + _db.getClass().getName() + " for "+ type);
      }
    }
  }
}

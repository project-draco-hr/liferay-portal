{
  return new ServiceReferenceMapper<T>(){
    @Override public void map(    ServiceReference<?> serviceReference,    Emitter<T> emitter){
      T value=(T)serviceReference.getProperty(property);
      if (value != null) {
        emitter.emit(value);
      }
    }
  }
;
}

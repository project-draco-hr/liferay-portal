{
  if ((frequency != CronText.DAILY_FREQUENCY) && (frequency != CronText.MINUTELY_FREQUENCY) && (frequency != CronText.MONTHLY_FREQUENCY)&& (frequency != CronText.NO_FREQUENCY)&& (frequency != CronText.WEEKLY_FREQUENCY)&& (frequency != CronText.YEARLY_FREQUENCY)) {
    throw new IllegalArgumentException(String.valueOf(frequency));
  }
  _frequency=frequency;
}

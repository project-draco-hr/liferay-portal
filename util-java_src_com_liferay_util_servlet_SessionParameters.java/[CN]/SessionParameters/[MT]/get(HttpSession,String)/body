{
  if (!USE_SESSION_PARAMETERS) {
    return parameter;
  }
  Map parameters=_getParameters(ses);
  String newParameter=(String)parameters.get(parameter);
  if (newParameter == null) {
    newParameter=PwdGenerator.getPassword(PwdGenerator.KEY3,10) + "_" + parameter;
    parameters.put(parameter,newParameter);
  }
  return newParameter;
}

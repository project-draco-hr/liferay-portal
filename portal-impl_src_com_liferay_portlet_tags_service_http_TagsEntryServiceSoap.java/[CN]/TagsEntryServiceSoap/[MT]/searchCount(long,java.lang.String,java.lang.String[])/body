{
  try {
    int returnValue=TagsEntryServiceUtil.searchCount(groupId,name,properties);
    return returnValue;
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

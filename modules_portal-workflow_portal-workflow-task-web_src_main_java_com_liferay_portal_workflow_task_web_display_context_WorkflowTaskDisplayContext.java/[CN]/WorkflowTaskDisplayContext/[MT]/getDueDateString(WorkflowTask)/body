{
  if (workflowTask.getDueDate() == null) {
    return LanguageUtil.get(_workflowTaskRequestHelper.getRequest(),"never");
  }
 else {
    return _dateFormatDateTime.format(workflowTask.getDueDate());
  }
}

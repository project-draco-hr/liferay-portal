{
  List<Subscription> subscriptions=subscriptionPersistence.findByC_C_C(calEvent.getCompanyId(),classNameLocalService.getClassNameId(CalEvent.class),calEvent.getEventId());
  for (  Subscription subscription : subscriptions) {
    importSubscription(subscription,calendarBookingId);
  }
}

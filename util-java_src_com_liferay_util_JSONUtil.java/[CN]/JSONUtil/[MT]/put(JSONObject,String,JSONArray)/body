{
  if (value == null) {
    value=new JSONArray();
  }
  try {
    jsonObj.put(key,value);
  }
 catch (  JSONException jsone) {
    if (_log.isWarnEnabled()) {
      _log.warn(jsone,jsone);
    }
  }
}

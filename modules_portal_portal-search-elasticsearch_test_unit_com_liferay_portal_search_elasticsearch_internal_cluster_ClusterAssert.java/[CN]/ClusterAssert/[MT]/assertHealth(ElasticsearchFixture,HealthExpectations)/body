{
  IdempotentRetryAssert.retryAssert(3,TimeUnit.SECONDS,new Callable<Void>(){
    @Override public Void call() throws Exception {
      ClusterHealthResponse clusterHealthResponse=elasticsearchFixture.getClusterHealthResponse();
      _assertHealth(clusterHealthResponse,healthExpectations);
      return null;
    }
  }
);
}

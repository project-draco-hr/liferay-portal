{
  Assert.assertEquals("activePrimaryShards",healthExpectations.activePrimaryShards,clusterHealthResponse.getActivePrimaryShards());
  Assert.assertEquals("activeShards",healthExpectations.activeShards,clusterHealthResponse.getActiveShards());
  Assert.assertEquals("numberOfDataNodes",healthExpectations.numberOfDataNodes,clusterHealthResponse.getNumberOfDataNodes());
  Assert.assertEquals("numberOfNodes",healthExpectations.numberOfNodes,clusterHealthResponse.getNumberOfNodes());
  Assert.assertEquals("status",healthExpectations.status,clusterHealthResponse.getStatus());
  Assert.assertEquals("unassignedShards",healthExpectations.unassignedShards,clusterHealthResponse.getUnassignedShards());
}

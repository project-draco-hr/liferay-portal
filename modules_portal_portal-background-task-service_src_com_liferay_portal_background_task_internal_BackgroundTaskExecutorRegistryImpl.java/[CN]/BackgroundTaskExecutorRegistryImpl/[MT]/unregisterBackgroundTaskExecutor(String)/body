{
  if (!_backgroundTaskExecutorRegistrations.containsKey(className)) {
    return;
  }
  ServiceRegistration<BackgroundTaskExecutor> serviceRegistration=_backgroundTaskExecutorRegistrations.get(className);
  serviceRegistration.unregister();
}

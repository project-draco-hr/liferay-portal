{
  if (_ddmForm == null) {
    try {
      _ddmForm=DDMFormJSONDeserializerUtil.deserialize(getDefinition());
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
  return new DDMForm(_ddmForm);
}

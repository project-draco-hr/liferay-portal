{
  List<MBCategory> categories=mbCategoryPersistence.findByG_P(groupId,MBCategoryConstants.DEFAULT_PARENT_CATEGORY_ID);
  for (  MBCategory category : categories) {
    mbCategoryLocalService.deleteCategory(category);
  }
}

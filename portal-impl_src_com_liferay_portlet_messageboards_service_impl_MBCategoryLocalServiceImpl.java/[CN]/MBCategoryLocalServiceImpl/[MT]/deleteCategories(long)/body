{
  Iterator itr=mbCategoryPersistence.findByG_P(groupId,MBCategoryImpl.DEFAULT_PARENT_CATEGORY_ID).iterator();
  while (itr.hasNext()) {
    MBCategory category=(MBCategory)itr.next();
    deleteCategory(category);
  }
}

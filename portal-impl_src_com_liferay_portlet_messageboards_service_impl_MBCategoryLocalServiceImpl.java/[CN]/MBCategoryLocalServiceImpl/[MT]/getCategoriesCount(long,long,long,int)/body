{
  if (status == WorkflowConstants.STATUS_ANY) {
    return mbCategoryPersistence.countByNotC_G_P(groupId,excludedCategoryId,parentCategoryId);
  }
  return mbCategoryPersistence.countByNotC_G_P_S(groupId,excludedCategoryId,parentCategoryId,status);
}

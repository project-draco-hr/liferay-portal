{
  if ((_dlActionsDisplayContextHelper.isCheckedOut() && !_dlActionsDisplayContextHelper.isLockedByMe()) || (_dlActionsDisplayContextHelper.isPending() && _isDLFileEntryDraftsEnabled())) {
    return true;
  }
  return false;
}

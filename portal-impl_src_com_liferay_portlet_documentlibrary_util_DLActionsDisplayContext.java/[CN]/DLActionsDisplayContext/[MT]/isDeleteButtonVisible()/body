{
  if (_dlActionsDisplayContextHelper.hasDeletePermission() && !_isFileEntryCheckedOutByOther() && (!_dlActionsDisplayContextHelper.isDLFileEntry() || !_isTrashEnabled())) {
    return true;
  }
  return false;
}

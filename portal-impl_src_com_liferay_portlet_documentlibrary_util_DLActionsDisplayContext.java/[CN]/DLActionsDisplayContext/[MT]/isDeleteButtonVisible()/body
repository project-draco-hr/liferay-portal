{
  if (_fileEntryDisplayContextHelper.hasDeletePermission() && !_isFileEntryCheckedOutByOther() && (!_fileEntryDisplayContextHelper.isDLFileEntry() || !_isTrashEnabled())) {
    return true;
  }
  return false;
}

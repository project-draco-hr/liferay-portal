{
  String[] fileEntryColumns=StringUtil.split(_dlPortletInstanceSettings.getFileEntryColumns());
  if (!isShowActions()) {
    fileEntryColumns=ArrayUtil.remove(fileEntryColumns,"action");
  }
  return fileEntryColumns;
}

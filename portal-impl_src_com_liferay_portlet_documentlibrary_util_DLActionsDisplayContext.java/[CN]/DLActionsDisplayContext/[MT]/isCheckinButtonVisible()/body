{
  if (_dlActionsDisplayContextHelper.hasUpdatePermission() && _dlActionsDisplayContextHelper.isLockedByMe() && _dlActionsDisplayContextHelper.isSupportsLocking()) {
    return true;
  }
  return false;
}

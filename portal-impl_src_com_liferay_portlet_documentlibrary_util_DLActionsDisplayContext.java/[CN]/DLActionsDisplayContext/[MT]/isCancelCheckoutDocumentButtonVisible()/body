{
  if ((_dlActionsDisplayContextHelper.hasUpdatePermission() && _dlActionsDisplayContextHelper.isLockedByMe()) || (_dlActionsDisplayContextHelper.isCheckedOut() && _dlActionsDisplayContextHelper.hasOverrideCheckoutPermission())) {
    return true;
  }
  return false;
}

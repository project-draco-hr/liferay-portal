{
  if (_dlActionsDisplayContextHelper.hasDeletePermission() && _dlActionsDisplayContextHelper.isDLFileEntry() && !_isFileEntryCheckedOutByOther()&& _isTrashEnabled()) {
    return true;
  }
  return false;
}

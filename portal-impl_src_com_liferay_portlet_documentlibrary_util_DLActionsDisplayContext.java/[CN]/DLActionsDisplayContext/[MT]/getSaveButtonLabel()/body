{
  String saveButtonLabel="save";
  FileVersion fileVersion=_dlActionsDisplayContextHelper.getFileVersion();
  if ((fileVersion == null) || _dlActionsDisplayContextHelper.isApproved() || _dlActionsDisplayContextHelper.isDraft()) {
    saveButtonLabel="save-as-draft";
  }
  return saveButtonLabel;
}

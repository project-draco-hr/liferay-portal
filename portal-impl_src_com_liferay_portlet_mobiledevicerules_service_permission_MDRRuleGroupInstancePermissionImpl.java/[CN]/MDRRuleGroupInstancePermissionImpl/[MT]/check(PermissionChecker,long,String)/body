{
  if (!contains(permissionChecker,ruleGroupInstanceId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),MDRRuleGroupInstance.class.getName(),ruleGroupInstanceId,actionId);
  }
}

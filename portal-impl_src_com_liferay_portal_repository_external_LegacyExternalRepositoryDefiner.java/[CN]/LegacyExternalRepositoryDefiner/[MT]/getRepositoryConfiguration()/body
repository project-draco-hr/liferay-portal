{
  try {
    BaseRepository baseRepository=ExternalRepositoryFactoryUtil.getInstance(getClassName());
    return new LegacyRepositoryConfiguration(baseRepository.getSupportedConfigurations(),baseRepository.getSupportedParameters());
  }
 catch (  Exception e) {
    throw new SystemException(e);
  }
}

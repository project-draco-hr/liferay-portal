{
  Object value=null;
  try {
    value=StandardBasicTypes.LONG.nullSafeGet(rs,names[0]);
  }
 catch (  SQLException sqle1) {
    try {
      value=new Long(GetterUtil.getLong(StandardBasicTypes.STRING.nullSafeGet(rs,names[0])));
    }
 catch (    SQLException sqle2) {
      throw sqle1;
    }
  }
  if (value == null) {
    return new Long(DEFAULT_VALUE);
  }
 else {
    return value;
  }
}

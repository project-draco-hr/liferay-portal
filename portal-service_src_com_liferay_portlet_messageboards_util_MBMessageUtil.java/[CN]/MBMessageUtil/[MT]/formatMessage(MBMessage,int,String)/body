{
  String msgBody=message.getBody();
  if (messageBodyLength > 0) {
    msgBody=StringUtil.shorten(msgBody,messageBodyLength);
  }
  if (message.isFormatBBCode()) {
    return formatMessageBodyToBBCode(message.getBody(),pathThemeImages);
  }
  return msgBody;
}

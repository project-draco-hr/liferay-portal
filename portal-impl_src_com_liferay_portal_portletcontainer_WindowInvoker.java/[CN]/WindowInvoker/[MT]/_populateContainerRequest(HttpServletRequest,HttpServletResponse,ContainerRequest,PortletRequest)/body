{
  containerReq.setRoles(_getRoles(req));
  containerReq.setUserInfo(UserInfoFactory.getUserInfo(req,_portletModel));
  containerReq.setAttribute(PortletRequest.CCPP_PROFILE,_getCCPPProfile(req));
}

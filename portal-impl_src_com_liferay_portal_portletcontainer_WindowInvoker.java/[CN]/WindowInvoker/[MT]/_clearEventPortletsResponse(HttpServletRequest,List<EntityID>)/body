{
  Layout layout=(Layout)request.getAttribute(WebKeys.LAYOUT);
  for (  EntityID entityID : eventUpdatedPortlets) {
    clearResponse(request.getSession(),layout.getPlid(),entityID.getPortletWindowName(),LanguageUtil.getLanguageId(request));
  }
}

{
  ThemeDisplay themeDisplay=(ThemeDisplay)liferayPortletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  PortletURL addAssetURL=null;
  if (BlogsPermission.contains(themeDisplay.getPermissionChecker(),themeDisplay.getScopeGroupId(),ActionKeys.ADD_ENTRY)) {
    addAssetURL=liferayPortletResponse.createRenderURL(PortletKeys.BLOGS);
    addAssetURL.setParameter("struts_action","/blogs/edit_entry");
  }
  return addAssetURL;
}

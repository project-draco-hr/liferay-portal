{
  Integer type=ClusterGroupModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ ClusterGroupModelImpl.TABLE_NAME);
  }
  return type;
}

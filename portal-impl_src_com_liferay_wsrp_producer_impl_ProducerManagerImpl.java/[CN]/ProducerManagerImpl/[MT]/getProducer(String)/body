{
  try {
    WSRPProducer producerModel=WSRPProducerLocalServiceUtil.getProducer(producerKey);
    return new ProducerImpl(producerModel);
  }
 catch (  Exception e) {
    throw new ProducerException(e);
  }
}

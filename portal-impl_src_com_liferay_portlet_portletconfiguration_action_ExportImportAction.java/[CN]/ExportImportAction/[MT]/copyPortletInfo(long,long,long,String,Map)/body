{
  Map parameterMap=getStagingParameters(parameters);
  byte[] data=LayoutLocalServiceUtil.exportPortletInfo(sourcePlid,portletId,parameterMap);
  ByteArrayInputStream bais=new ByteArrayInputStream(data);
  LayoutLocalServiceUtil.importPortletInfo(creatorUserId,targetPlid,portletId,parameterMap,bais);
}

{
  if (isEscapedModel()) {
    return (DDMStructureLink)this;
  }
 else {
    return (DDMStructureLink)Proxy.newProxyInstance(DDMStructureLink.class.getClassLoader(),new Class[]{DDMStructureLink.class},new AutoEscapeBeanHandler(this));
  }
}

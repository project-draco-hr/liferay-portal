{
  PortletPermissionUtil.check(getPermissionChecker(),serviceContext.getPlid(),PortletKeys.MESSAGE_BOARDS,ActionKeys.BAN_USER);
  return mbBanLocalService.addBan(getUserId(),banUserId,serviceContext);
}

{
  DDMStructure structure=ddmStructurePersistence.fetchByG_S(groupId,structureKey);
  if (structure != null) {
    throw new StructureDuplicateStructureKeyException("Duplicate structure key: " + structureKey + " found in group: "+ groupId);
  }
  validate(nameMap,xsd);
}

{
  List<DDMStructure> structures=ddmStructurePersistence.findByG_P(groupId,structureId);
  for (  DDMStructure structure : structures) {
    structureIds.add(structure.getStructureId());
    getChildStructureIds(structureIds,structure.getGroupId(),structure.getParentStructureId());
  }
}

{
  ddmStructurePersistence.remove(structure);
  resourceLocalService.deleteResource(structure.getCompanyId(),DDMStructure.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,structure.getStructureKey());
  ddmStructureLinkLocalService.deleteStructureStructureLinks(structure.getStructureId());
}

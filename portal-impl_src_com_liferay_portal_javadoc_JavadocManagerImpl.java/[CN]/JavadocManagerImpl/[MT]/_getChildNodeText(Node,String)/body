{
  Node childNode=rootNode.selectSingleNode(childNodeName);
  if (childNode == null) {
    return null;
  }
  String text=childNode.getText();
  if (text == null) {
    return null;
  }
  text=text.trim();
  if (text.length() == 0) {
    return null;
  }
  return text;
}

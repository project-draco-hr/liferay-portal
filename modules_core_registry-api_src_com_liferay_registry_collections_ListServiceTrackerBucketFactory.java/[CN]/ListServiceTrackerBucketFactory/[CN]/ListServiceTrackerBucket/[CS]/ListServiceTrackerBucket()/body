{
  if (_comparator == null) {
    _serviceTuples=new TreeSet<ServiceReferenceServiceTuple<S>>();
  }
 else {
    _serviceTuples=new TreeSet<ServiceReferenceServiceTuple<S>>(new ServiceReferenceServiceTupleComparator<S>(_comparator));
  }
}

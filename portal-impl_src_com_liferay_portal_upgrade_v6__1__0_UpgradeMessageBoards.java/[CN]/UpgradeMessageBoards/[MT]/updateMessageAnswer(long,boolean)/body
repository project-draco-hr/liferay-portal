{
  PreparedStatement ps=null;
  try {
    ps=connection.prepareStatement("update MBMessage set answer = ? where messageId = " + messageId);
    ps.setBoolean(1,answer);
    ps.executeUpdate();
  }
  finally {
    DataAccess.cleanUp(ps);
  }
}

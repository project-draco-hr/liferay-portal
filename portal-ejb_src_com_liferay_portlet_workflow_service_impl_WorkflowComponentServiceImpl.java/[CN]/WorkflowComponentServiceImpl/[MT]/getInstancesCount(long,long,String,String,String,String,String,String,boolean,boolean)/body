{
  try {
    String xml=getInstancesCountXml(definitionId,instanceId,workflowName,workflowVersion,gtStartDate,ltStartDate,gtEndDate,ltEndDate,hideEndedTasks,andOperator);
    return parseInt(xml,"count");
  }
 catch (  Exception e) {
    throw new WorkflowComponentException(e);
  }
}

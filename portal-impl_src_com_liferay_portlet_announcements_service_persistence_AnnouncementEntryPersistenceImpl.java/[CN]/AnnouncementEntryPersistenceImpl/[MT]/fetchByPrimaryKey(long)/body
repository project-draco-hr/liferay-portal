{
  Session session=null;
  try {
    session=openSession();
    return (AnnouncementEntry)session.get(AnnouncementEntryImpl.class,new Long(entryId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

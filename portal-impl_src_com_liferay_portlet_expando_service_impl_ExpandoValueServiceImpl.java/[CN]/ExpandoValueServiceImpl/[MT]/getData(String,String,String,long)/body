{
  ExpandoColumn column=expandoColumnLocalService.getColumn(className,tableName,columnName);
  ExpandoColumnPermission.check(getPermissionChecker(),column,ActionKeys.VIEW);
  return expandoValueLocalService.getData(className,tableName,columnName,classPK);
}

{
  String[] articleIds=null;
  String[] titles=null;
  String[] descriptions=null;
  String[] contents=null;
  boolean andOperator=false;
  String[] tags=null;
  if (Validator.isNotNull(keywords)) {
    articleIds=CustomSQLUtil.keywords(keywords,false);
    titles=CustomSQLUtil.keywords(keywords);
    descriptions=CustomSQLUtil.keywords(keywords,false);
    contents=CustomSQLUtil.keywords(keywords,false);
    tags=CustomSQLUtil.keywords(keywords,false);
  }
 else {
    andOperator=true;
  }
  return findByC_G_A_V_T_D_C_T_S_T_D_S_R_T(companyId,groupId,articleIds,version,titles,descriptions,contents,type,new String[]{structureId},new String[]{templateId},displayDateGT,displayDateLT,status,reviewDate,tags,andOperator,start,end,obc);
}

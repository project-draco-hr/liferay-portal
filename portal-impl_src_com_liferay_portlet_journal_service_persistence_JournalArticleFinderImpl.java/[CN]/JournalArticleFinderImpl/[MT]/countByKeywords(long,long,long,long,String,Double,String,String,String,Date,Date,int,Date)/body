{
  List<Long> folderIds=new ArrayList<Long>();
  folderIds.add(folderId);
  return countByKeywords(companyId,groupId,folderIds,classNameId,keywords,version,type,structureId,templateId,displayDateGT,displayDateLT,status,reviewDate);
}

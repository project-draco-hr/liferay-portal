{
  List<Long> folderIds=new ArrayList<Long>();
  folderIds.add(folderId);
  return filterCountByKeywords(companyId,groupId,folderIds,classNameId,keywords,version,type,structureId,templateId,displayDateGT,displayDateLT,status,reviewDate);
}

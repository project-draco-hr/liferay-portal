{
  List<AuthFailure> authFailures=ListUtil.fromArray(_authFailures.get(key));
  if (ListUtil.remove(authFailures,authFailure)) {
    _authFailures.put(key,authFailures.toArray(new AuthFailure[authFailures.size()]));
  }
}

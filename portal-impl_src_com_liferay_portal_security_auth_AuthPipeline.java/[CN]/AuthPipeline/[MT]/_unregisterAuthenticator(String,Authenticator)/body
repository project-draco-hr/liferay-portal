{
  List<Authenticator> authenticators=ListUtil.fromArray(_authenticators.get(key));
  if (ListUtil.remove(authenticators,authenticator)) {
    _authenticators.put(key,authenticators.toArray(new Authenticator[authenticators.size()]));
  }
}

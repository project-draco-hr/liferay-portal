{
  if (_oAuthEnabled) {
    _oAuthConsumer.sign(httpRequest);
  }
 else {
    httpRequest.setHeader("Sync-JWT",_token);
  }
  return _httpClient.execute(_httpHost,httpRequest,handler,httpContext);
}

{
  httpRequest.setHeader("Sync-Build",String.valueOf(ReleaseInfo.getBuildNumber()));
  if (_oAuthEnabled) {
    _oAuthConsumer.sign(httpRequest);
  }
 else {
    httpRequest.setHeader("Sync-JWT",_token);
  }
}

{
  List<DataSampleProcessor> processors=_dataSampleProcessors.get(namespace);
  if (processors == null) {
    processors=new ArrayList<DataSampleProcessor>();
    _dataSampleProcessors.put(namespace,processors);
  }
  processors.add(dataSampleProcessor);
}

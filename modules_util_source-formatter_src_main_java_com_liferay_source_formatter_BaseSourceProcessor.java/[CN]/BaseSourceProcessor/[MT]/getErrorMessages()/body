{
  List<SourceFormatterMessage> errorMessages=new ArrayList<>();
  for (  Map.Entry<String,List<SourceFormatterMessage>> entry : _messagesMap.entrySet()) {
    errorMessages.addAll(entry.getValue());
  }
  return errorMessages;
}

{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  Resource newResource=addResource();
  _persistence.clearCache();
  ResourceModelImpl existingResourceModelImpl=(ResourceModelImpl)_persistence.findByPrimaryKey(newResource.getPrimaryKey());
  Assert.assertEquals(existingResourceModelImpl.getCodeId(),existingResourceModelImpl.getOriginalCodeId());
  Assert.assertTrue(Validator.equals(existingResourceModelImpl.getPrimKey(),existingResourceModelImpl.getOriginalPrimKey()));
}

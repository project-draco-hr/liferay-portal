{
  Map<Object,SPI> spis=(Map<Object,SPI>)ReflectionTestUtil.getFieldValue(MPIHelperUtil.class,"_spis");
  Object spiKey=_createSPIKey(spi.getSPIProviderName(),spiId);
  spis.put(spiKey,spi);
  return spiKey;
}

{
  Class<?> spiKeyClass=Class.forName(MPIHelperUtil.class.getName().concat("$SPIKey"));
  Constructor<?> spiKeyConstructor=spiKeyClass.getConstructor(String.class,String.class);
  return spiKeyConstructor.newInstance(spiProviderName,spiId);
}

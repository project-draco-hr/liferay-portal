{
  Object propertyValue=dictionary.get(property);
  if (propertyValue instanceof String) {
    String[] propertyValues=StringUtil.split((String)propertyValue,StringPool.COMMA);
    dictionary.put(property,propertyValues);
  }
}

{
  mockStatic(CalendarFactoryUtil.class);
  when(CalendarFactoryUtil.getCalendar()).thenReturn(new GregorianCalendar());
  Assert.assertEquals(DateUtil.getDaysBetween(date1,date2,_timeZone),expected);
}

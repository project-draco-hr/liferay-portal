{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  try {
    return MBMessagePermission.contains(themeDisplay.getPermissionChecker(),getMBMessage(portletRequest),ActionKeys.SUBSCRIBE);
  }
 catch (  PortalException pe) {
  }
  return false;
}

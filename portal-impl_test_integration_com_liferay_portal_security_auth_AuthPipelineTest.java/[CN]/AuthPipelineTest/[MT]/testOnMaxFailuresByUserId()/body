{
  long companyId=0;
  String key="auth.max.failures";
  long userId=RandomTestUtil.randomLong();
  _atomicState.reset();
  try {
    AuthPipeline.onMaxFailuresByUserId(key,companyId,userId,null,null);
  }
 catch (  AuthException ae) {
  }
  Assert.assertTrue(_atomicState.isSet());
}

{
  long companyId=0;
  String key="auth.failure";
  String screenName=RandomTestUtil.randomString();
  _atomicState.reset();
  try {
    AuthPipeline.onFailureByScreenName(key,companyId,screenName,null,null);
  }
 catch (  AuthException ae) {
  }
  Assert.assertTrue(_atomicState.isSet());
}

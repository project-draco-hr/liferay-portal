{
  resourceLocalService.addModelResources(structureEntry.getCompanyId(),structureEntry.getGroupId(),structureEntry.getUserId(),DDMStructureEntry.class.getName(),structureEntry.getStructureKey(),communityPermissions,guestPermissions);
}

{
  ddmStructureEntryPersistence.remove(structureEntry);
  resourceLocalService.deleteResource(structureEntry.getCompanyId(),DDMStructureEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,structureEntry.getStructureKey());
}

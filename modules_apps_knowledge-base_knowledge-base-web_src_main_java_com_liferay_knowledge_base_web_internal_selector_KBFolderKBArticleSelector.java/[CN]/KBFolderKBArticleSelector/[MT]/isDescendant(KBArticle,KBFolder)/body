{
  if (ancestorKBFolder.getKbFolderId() == KBFolderConstants.DEFAULT_PARENT_FOLDER_ID) {
    return true;
  }
  KBFolder parentKBFolder=_kbFolderLocalService.fetchKBFolder(kbArticle.getKbFolderId());
  while ((parentKBFolder != null) && !parentKBFolder.equals(ancestorKBFolder)) {
    parentKBFolder=_kbFolderLocalService.fetchKBFolder(parentKBFolder.getParentKBFolderId());
  }
  if (parentKBFolder != null) {
    return true;
  }
  return false;
}

{
  mbThreadFlag.setNew(true);
  mbThreadFlag=mbThreadFlagPersistence.update(mbThreadFlag,false);
  Indexer indexer=IndexerRegistryUtil.getIndexer(getModelClassName());
  if (indexer != null) {
    try {
      indexer.reindex(mbThreadFlag);
    }
 catch (    SearchException se) {
      if (_log.isWarnEnabled()) {
        _log.warn(se,se);
      }
    }
  }
  return mbThreadFlag;
}

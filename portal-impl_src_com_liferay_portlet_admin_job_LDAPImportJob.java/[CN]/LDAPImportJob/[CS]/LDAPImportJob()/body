{
  try {
    long rawInterval=PrefsPropsUtil.getLong(PropsUtil.LDAP_IMPORT_INTERVAL,PropsValues.LDAP_IMPORT_INTERVAL);
    if (_log.isDebugEnabled()) {
      _log.debug("Interval " + rawInterval + " minutes");
    }
    _interval=rawInterval * Time.MINUTE;
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(Status.class,_dynamicQueryClassLoader);
  dynamicQuery.add(RestrictionsFactoryUtil.eq("statusId",RandomTestUtil.nextLong()));
  List<Status> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

{
  int i=1;
  while (true) {
    String jenkinsLocalURL=project.getProperty("jenkins.local.url[" + hostNamePrefix + "-"+ i+ "]");
    if ((jenkinsLocalURL != null) && (jenkinsLocalURL.length() > 0)) {
      i++;
      continue;
    }
    return i - 1;
  }
}

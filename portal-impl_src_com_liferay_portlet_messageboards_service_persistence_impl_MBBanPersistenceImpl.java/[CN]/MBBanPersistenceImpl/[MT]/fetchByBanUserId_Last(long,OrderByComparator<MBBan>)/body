{
  int count=countByBanUserId(banUserId);
  if (count == 0) {
    return null;
  }
  List<MBBan> list=findByBanUserId(banUserId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

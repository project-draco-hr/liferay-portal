{
  for (  FieldTypes type : FieldTypes.values()) {
    String fieldName="Field" + type.ordinal();
    Value fieldValue=createFieldValue(_fieldValues.get(type),localizedValues);
    ddmFormValues.addDDMFormFieldValue(DDLRecordTestUtil.createDDMFormFieldValue(fieldName,fieldValue));
  }
}

{
  List<JournalArticleResource> resources=new ArrayList<JournalArticleResource>();
  for (  BinaryContent binaryContent : binaryContents) {
    resources.add(((JournalResourceContent)binaryContent).getJournalResource());
  }
  return resources;
}

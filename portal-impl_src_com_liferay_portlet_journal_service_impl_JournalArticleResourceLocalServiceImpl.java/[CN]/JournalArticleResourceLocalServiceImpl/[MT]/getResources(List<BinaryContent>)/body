{
  List<JournalArticleResource> resources=new ArrayList<JournalArticleResource>(binaryContents.size());
  for (  BinaryContent binaryContent : binaryContents) {
    MirageArticleResource resource=(MirageArticleResource)binaryContent;
    resources.add(resource.getArticleResource());
  }
  return resources;
}

{
  try {
    User user=UserLocalServiceUtil.getUserById(userId);
    if (user.hasOrganization()) {
      return _options;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return new MultiValueMap();
}

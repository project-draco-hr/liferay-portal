{
  List<UpgradeProcess> upgradeProcesses=new ArrayList<>();
  upgradeProcesses.add(new UpgradePortletId());
  _releaseLocalService.updateRelease("com.liferay.users.admin.service",upgradeProcesses,1,1,false);
}

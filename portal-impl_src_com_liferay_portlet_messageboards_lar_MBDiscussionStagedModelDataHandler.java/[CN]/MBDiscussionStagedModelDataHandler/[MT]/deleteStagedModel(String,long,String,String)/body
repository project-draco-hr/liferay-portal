{
  MBDiscussion discussion=MBDiscussionLocalServiceUtil.fetchMBDiscussionByUuidAndGroupId(uuid,groupId);
  if (discussion != null) {
    MBDiscussionLocalServiceUtil.deleteMBDiscussion(discussion);
  }
}

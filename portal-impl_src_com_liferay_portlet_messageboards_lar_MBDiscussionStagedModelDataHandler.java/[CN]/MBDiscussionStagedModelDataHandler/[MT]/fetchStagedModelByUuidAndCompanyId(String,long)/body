{
  List<MBDiscussion> discussions=MBDiscussionLocalServiceUtil.getMBDiscussionsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<MBDiscussion>());
  if (ListUtil.isEmpty(discussions)) {
    return null;
  }
  return discussions.get(0);
}

{
  if (!anytime) {
    return true;
  }
  if (Validator.isNotNull(content)) {
    return true;
  }
  if (Validator.isNotNull(title)) {
    return true;
  }
  if (status != WorkflowConstants.STATUS_ANY) {
    return true;
  }
  return false;
}

{
  if (s == null) {
    throw new NullPointerException();
  }
  _sb.setLength(0);
  _sb.setLength(s.length() + defaultInitSize);
  if (collect) {
    _getInfo(new Throwable());
  }
  _sb.append(s);
}

{
  if (_upgraded) {
    try {
      if (_log.isInfoEnabled()) {
        _log.info("Adding indexes");
      }
      DBUtil.getInstance().runSQLTemplate("indexes.sql",false);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
}

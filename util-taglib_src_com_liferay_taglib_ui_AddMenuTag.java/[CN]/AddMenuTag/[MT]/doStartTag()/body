{
  HttpServletRequest request=(HttpServletRequest)pageContext.getRequest();
  request.setAttribute("liferay-ui:add-menu:add-count",new IntegerWrapper());
  request.setAttribute("liferay-ui:add-menu:menuItemList",new ArrayList<AddMenuItem>());
  return EVAL_BODY_BUFFERED;
}

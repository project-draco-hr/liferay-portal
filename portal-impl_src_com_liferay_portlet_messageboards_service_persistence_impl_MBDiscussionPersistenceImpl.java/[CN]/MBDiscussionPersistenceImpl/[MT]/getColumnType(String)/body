{
  Integer type=MBDiscussionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ MBDiscussionModelImpl.TABLE_NAME);
  }
  return type;
}

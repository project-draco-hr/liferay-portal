{
  if (isEscapedModel()) {
    return (ResourceCode)this;
  }
 else {
    return (ResourceCode)Proxy.newProxyInstance(ResourceCode.class.getClassLoader(),new Class[]{ResourceCode.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (ResourceCode)this;
  }
 else {
    return (ResourceCode)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if ((!destinationName.equals(DestinationNames.DEVICE_RECOGNITION_PROVIDER)) || (messageListener == _proxyMessageListener) || !(messageListener instanceof ProxyMessageListener)) {
    return false;
  }
 else {
    return true;
  }
}

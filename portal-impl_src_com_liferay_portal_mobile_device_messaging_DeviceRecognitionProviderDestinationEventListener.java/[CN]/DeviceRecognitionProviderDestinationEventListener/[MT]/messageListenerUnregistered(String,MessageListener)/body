{
  if (!isProceed(destinationName,messageListener)) {
    return;
  }
  DeviceDetectionUtil deviceDetectionUtil=new DeviceDetectionUtil();
  deviceDetectionUtil.setDeviceRecognitionProvider(_directDeviceRecognitionProvider);
}

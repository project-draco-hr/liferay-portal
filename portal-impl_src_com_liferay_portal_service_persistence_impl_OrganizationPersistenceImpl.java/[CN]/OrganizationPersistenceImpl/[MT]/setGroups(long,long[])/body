{
  Set<Long> newGroupPKsSet=SetUtil.fromArray(groupPKs);
  Set<Long> oldGroupPKsSet=SetUtil.fromArray(organizationToGroupTableMapper.getRightPrimaryKeys(companyProvider.getCompanyId(),pk));
  Set<Long> removeGroupPKsSet=new HashSet<Long>(oldGroupPKsSet);
  removeGroupPKsSet.removeAll(newGroupPKsSet);
  for (  long removeGroupPK : removeGroupPKsSet) {
    organizationToGroupTableMapper.deleteTableMapping(companyProvider.getCompanyId(),pk,removeGroupPK);
  }
  newGroupPKsSet.removeAll(oldGroupPKsSet);
  for (  long newGroupPK : newGroupPKsSet) {
    organizationToGroupTableMapper.addTableMapping(companyProvider.getCompanyId(),pk,newGroupPK);
  }
}

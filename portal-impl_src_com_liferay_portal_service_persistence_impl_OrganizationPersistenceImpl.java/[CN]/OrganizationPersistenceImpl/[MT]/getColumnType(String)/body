{
  Integer type=OrganizationModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ OrganizationModelImpl.TABLE_NAME);
  }
  return type;
}

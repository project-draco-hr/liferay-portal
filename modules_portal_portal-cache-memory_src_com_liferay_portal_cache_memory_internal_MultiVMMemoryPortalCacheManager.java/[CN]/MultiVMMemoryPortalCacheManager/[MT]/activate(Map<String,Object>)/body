{
  setClusterAware(true);
  setMpiOnly(true);
  setName(PortalCacheManagerNames.MULTI_VM);
  initialize();
  if (_log.isDebugEnabled()) {
    _log.debug("Activated " + PortalCacheManagerNames.MULTI_VM + " "+ PortalCacheManagerTypes.MEMORY);
  }
}

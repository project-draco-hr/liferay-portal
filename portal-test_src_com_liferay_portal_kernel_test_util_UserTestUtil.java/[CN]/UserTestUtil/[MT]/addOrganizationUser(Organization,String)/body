{
  User organizationUser=addUser(organization.getGroupId());
  UserLocalServiceUtil.addOrganizationUser(organization.getOrganizationId(),organizationUser.getUserId());
  addUserGroupRole(organizationUser.getUserId(),organization.getGroupId(),roleName);
  return organizationUser;
}

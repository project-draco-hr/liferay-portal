{
  selenium.open("/user/joebloggs/home/");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (selenium.isElementPresent("link=Communities I Own")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  selenium.clickAt("link=Communities I Own",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  selenium.type("_29_name",RuntimeVariables.replace("Tags Message Board Community"));
  selenium.clickAt("//input[@value='Search Communities']",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  selenium.clickAt("//tr[@class='portlet-section-body results-row']/td[1]/a",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  selenium.clickAt("link=Tags Admin Page",RuntimeVariables.replace(""));
  selenium.waitForPageToLoad("30000");
  for (int second=0; ; second++) {
    if (second >= 60) {
      fail("timeout");
    }
    try {
      if (RuntimeVariables.replace("selenium").equals(selenium.getText("//div[@class='tags-container']/span[1]/a[1]"))) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  assertEquals(RuntimeVariables.replace("selenium"),selenium.getText("//div[@class='tags-container']/span[1]/a[1]"));
  assertEquals(RuntimeVariables.replace("selenium1"),selenium.getText("//div[@class='tags-container']/span[2]/a[1]"));
  assertEquals(RuntimeVariables.replace("selenium2"),selenium.getText("//div[@class='tags-container']/span[3]/a[1]"));
  assertEquals(RuntimeVariables.replace("selenium3"),selenium.getText("//div[@class='tags-container']/span[4]/a[1]"));
}

{
  super(String.format("Password for user %s must not contain dictionary words " + "such as: %s",userId,_getDictionaryWordsString(dictionaryWords)),PASSWORD_CONTAINS_TRIVIAL_WORDS);
  this.userId=userId;
  this.dictionaryWords=dictionaryWords;
}

{
  Map<String,Serializable> attributes=new HashMap<String,Serializable>();
  for (  ExpandoColumn column : getAttributeColumns()) {
    attributes.put(column.getName(),getAttribute(column.getName(),secure));
  }
  return attributes;
}

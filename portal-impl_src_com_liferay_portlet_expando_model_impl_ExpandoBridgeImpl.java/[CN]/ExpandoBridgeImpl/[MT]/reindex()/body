{
  if (!isIndexEnabled()) {
    return;
  }
  Indexer indexer=IndexerRegistryUtil.getIndexer(_className);
  if (indexer != null) {
    try {
      indexer.reindex(_className,_classPK);
    }
 catch (    Exception e) {
      _log.error(e,e);
    }
  }
}

{
  if (isEscapedModel()) {
    return (LayoutPrototype)this;
  }
 else {
    return (LayoutPrototype)Proxy.newProxyInstance(LayoutPrototype.class.getClassLoader(),new Class[]{LayoutPrototype.class},new AutoEscapeBeanHandler(this));
  }
}

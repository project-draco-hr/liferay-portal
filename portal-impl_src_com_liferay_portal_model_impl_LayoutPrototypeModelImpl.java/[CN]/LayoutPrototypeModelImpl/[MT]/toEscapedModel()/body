{
  if (isEscapedModel()) {
    return (LayoutPrototype)this;
  }
 else {
    return (LayoutPrototype)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

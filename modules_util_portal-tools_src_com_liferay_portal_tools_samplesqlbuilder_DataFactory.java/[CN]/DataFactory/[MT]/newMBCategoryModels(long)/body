{
  List<MBCategoryModel> mbCategoryModels=new ArrayList<>(_maxMBCategoryCount);
  for (int i=1; i <= _maxMBCategoryCount; i++) {
    mbCategoryModels.add(newMBCategoryModel(groupId,i));
  }
  return mbCategoryModels;
}

{
  if (isEscapedModel()) {
    return (EmailAddress)this;
  }
 else {
    return (EmailAddress)Proxy.newProxyInstance(EmailAddress.class.getClassLoader(),new Class[]{EmailAddress.class},new AutoEscapeBeanHandler(this));
  }
}

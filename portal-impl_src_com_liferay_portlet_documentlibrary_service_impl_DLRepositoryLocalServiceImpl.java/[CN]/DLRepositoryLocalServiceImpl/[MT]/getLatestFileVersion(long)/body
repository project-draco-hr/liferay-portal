{
  List<DLFileVersion> fileVersions=dlFileVersionPersistence.findByFileEntryId(fileEntryId,0,1,new FileVersionVersionComparator());
  if (fileVersions.isEmpty()) {
    throw new NoSuchFileVersionException();
  }
  return fileVersions.get(0);
}

{
  if (((event.getType() & BundleEvent.STOPPED) != BundleEvent.STOPPED) && ((event.getType() & BundleEvent.UNRESOLVED) != BundleEvent.UNRESOLVED)) {
    return;
  }
  _resourceMaps.remove(event.getBundle());
}

{
  Integer type=MBStatsUserModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ MBStatsUserModelImpl.TABLE_NAME);
  }
  return type;
}

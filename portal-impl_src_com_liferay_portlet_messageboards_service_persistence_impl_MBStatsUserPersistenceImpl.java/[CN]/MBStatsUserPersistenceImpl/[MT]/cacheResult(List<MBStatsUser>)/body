{
  for (  MBStatsUser mbStatsUser : mbStatsUsers) {
    if (entityCache.getResult(MBStatsUserModelImpl.ENTITY_CACHE_ENABLED,MBStatsUserImpl.class,mbStatsUser.getPrimaryKey()) == null) {
      cacheResult(mbStatsUser);
    }
 else {
      mbStatsUser.resetOriginalValues();
    }
  }
}

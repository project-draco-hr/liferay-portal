{
  Object value=sourceReadMethod.invoke(source);
  if (value == null) {
    return;
  }
  Class<?> targetClazz=getCollectionType(targetWriteMethod);
  targetWriteMethod.invoke(target,copyCollection((Collection<?>)value,targetClazz));
}

{
  Long beanValue=null;
  if (bean != null) {
    try {
      beanValue=(Long)PropertyUtils.getProperty(bean,param);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
  if (beanValue == null) {
    return defaultValue;
  }
 else {
    return beanValue.longValue();
  }
}

{
  _atomicState.reset();
  try {
    SanitizerUtil.sanitize(1,1,1,TestSanitizer.class.getName(),1,"contentType","bytes".getBytes());
  }
 catch (  Exception e) {
    Assert.fail();
  }
  Assert.assertTrue(_atomicState.isSet());
}

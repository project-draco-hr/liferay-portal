{
  try {
    String returnValue=SanitizerUtil.sanitize(1,1,1,TestSanitizer.class.getName(),1,"contentType","s");
    Assert.assertEquals("1:1",returnValue);
  }
 catch (  Exception e) {
    Assert.fail();
  }
}

{
  request.setAttribute(DDLWebKeys.DYNAMIC_DATA_LISTS_RECORD,_record);
  request.setAttribute(DDLWebKeys.DYNAMIC_DATA_LISTS_RECORD_VERSION,_recordVersion);
  return super.include(request,response,template);
}

{
  setUpRange(_request,"bytes=0-999");
  List<Range> ranges=ServletResponseUtil.getRanges(_request,_response,1000);
  Assert.assertEquals(ranges.size(),1);
  assertRange(ranges.get(0),0,999,1000);
}

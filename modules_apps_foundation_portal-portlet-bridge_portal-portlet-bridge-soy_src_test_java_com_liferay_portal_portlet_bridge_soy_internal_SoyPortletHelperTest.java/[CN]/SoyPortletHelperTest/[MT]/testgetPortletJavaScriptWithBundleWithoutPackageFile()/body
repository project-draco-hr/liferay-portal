{
  SoyPortletHelper soyPortletHelper=new SoyPortletHelper(_mockBundle);
  String portletJavaScript=soyPortletHelper.getPortletJavaScript(_mockTemplate,"View",StringUtil.randomString(),Collections.<String>emptySet());
  Assert.assertEquals(StringPool.BLANK,portletJavaScript);
}

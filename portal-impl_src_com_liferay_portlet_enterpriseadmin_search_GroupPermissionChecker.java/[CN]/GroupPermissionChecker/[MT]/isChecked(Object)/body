{
  Group group=(Group)obj;
  try {
    return PermissionLocalServiceUtil.hasRolePermission(_role.getRoleId(),group.getCompanyId(),_resourceName,ResourceImpl.SCOPE_GROUP,String.valueOf(group.getGroupId()),_actionId);
  }
 catch (  Exception e) {
    _log.error(e);
    return false;
  }
}

{
  ResourceLocalServiceUtil.addModelResources(entry.getCompanyId(),entry.getGroupId(),entry.getUserId(),BlogsEntry.class.getName(),String.valueOf(entry.getPrimaryKey()),communityPermissions,guestPermissions);
}

{
  try {
    Filter filter=_bundleContext.createFilter("(&(jaxws=true)(soap.address=*))");
    _serverServiceTracker=new ServiceTracker<>(_bundleContext,filter,new ServerServiceTrackerCustomizer(bus));
  }
 catch (  InvalidSyntaxException ise) {
    throw new RuntimeException(ise);
  }
  _serverServiceTracker.open();
}

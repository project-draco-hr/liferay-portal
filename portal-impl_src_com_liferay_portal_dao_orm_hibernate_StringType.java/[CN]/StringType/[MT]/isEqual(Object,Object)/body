{
  boolean equal=super.isEqual(x,y);
  if (!equal) {
    if (((x == null) || x.equals(StringPool.BLANK)) && ((y == null) || y.equals(StringPool.BLANK))) {
      equal=true;
    }
 else {
      equal=false;
    }
  }
  return equal;
}

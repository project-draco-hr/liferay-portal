{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  User user=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  if (user != null) {
    UserLocalServiceUtil.deleteUser(user);
  }
}

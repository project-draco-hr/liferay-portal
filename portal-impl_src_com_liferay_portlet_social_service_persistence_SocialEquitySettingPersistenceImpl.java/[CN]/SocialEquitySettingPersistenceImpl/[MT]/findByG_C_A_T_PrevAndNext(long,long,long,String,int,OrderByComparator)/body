{
  SocialEquitySetting socialEquitySetting=findByPrimaryKey(equitySettingId);
  Session session=null;
  try {
    session=openSession();
    SocialEquitySetting[] array=new SocialEquitySettingImpl[3];
    array[0]=getByG_C_A_T_PrevAndNext(session,socialEquitySetting,groupId,classNameId,actionId,type,orderByComparator,true);
    array[1]=socialEquitySetting;
    array[2]=getByG_C_A_T_PrevAndNext(session,socialEquitySetting,groupId,classNameId,actionId,type,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

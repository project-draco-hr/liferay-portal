{
  SocialEquitySetting socialEquitySetting=(SocialEquitySetting)EntityCacheUtil.getResult(SocialEquitySettingModelImpl.ENTITY_CACHE_ENABLED,SocialEquitySettingImpl.class,equitySettingId,this);
  if (socialEquitySetting == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (socialEquitySetting == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      socialEquitySetting=(SocialEquitySetting)session.get(SocialEquitySettingImpl.class,Long.valueOf(equitySettingId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (socialEquitySetting == null)) {
        EntityCacheUtil.putResult(SocialEquitySettingModelImpl.ENTITY_CACHE_ENABLED,SocialEquitySettingImpl.class,equitySettingId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(socialEquitySetting);
      }
      closeSession(session);
    }
  }
  return socialEquitySetting;
}

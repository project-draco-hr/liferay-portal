{
  for (  String name : names) {
    name=name.trim().toLowerCase();
    TagsEntry entry=tagsEntryPersistence.fetchByG_N(groupId,name);
    if (entry == null) {
      addEntryToGroup(userId,groupId,null,name,null,PropsValues.TAGS_PROPERTIES_DEFAULT,Boolean.TRUE,Boolean.TRUE,null,null);
    }
  }
}

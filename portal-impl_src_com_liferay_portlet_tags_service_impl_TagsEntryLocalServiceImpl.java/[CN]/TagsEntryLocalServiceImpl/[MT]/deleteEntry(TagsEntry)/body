{
  tagsPropertyLocalService.deleteProperties(entry.getEntryId());
  resourceLocalService.deleteResource(entry.getCompanyId(),TagsEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,entry.getEntryId());
  tagsEntryPersistence.remove(entry);
}

{
  TagsVocabulary vocabulary=tagsVocabularyLocalService.getVocabulary(companyId,vocabularyName);
  TagsEntry entry=getEntry(companyId,parentEntryName);
  return tagsEntryPersistence.findByC_P_V(companyId,entry.getEntryId(),vocabulary.getVocabularyId());
}

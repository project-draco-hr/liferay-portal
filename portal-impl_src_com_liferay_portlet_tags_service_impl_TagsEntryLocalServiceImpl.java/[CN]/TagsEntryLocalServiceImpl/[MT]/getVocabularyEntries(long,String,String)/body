{
  TagsVocabulary vocabulary=tagsVocabularyLocalService.getVocabulary(companyId,vocabularyName);
  TagsEntry entry=getEntry(companyId,parentName);
  return tagsEntryPersistence.findByC_V_P(companyId,vocabulary.getVocabularyId(),entry.getEntryId());
}

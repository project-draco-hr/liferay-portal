{
  if (Validator.isNull(name)) {
    throw new EntryNameException();
  }
 else {
    char[] c=name.toCharArray();
    for (int i=0; i < c.length; i++) {
      if (!Validator.isChar(c[i]) && !Validator.isDigit(c[i]) && (c[i] != ' ')) {
        throw new EntryNameException();
      }
    }
  }
}

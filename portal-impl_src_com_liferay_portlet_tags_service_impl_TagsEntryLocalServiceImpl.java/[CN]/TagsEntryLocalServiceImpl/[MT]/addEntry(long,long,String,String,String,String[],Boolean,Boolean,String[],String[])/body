{
  long groupId=PortalUtil.getScopeGroupId(plid);
  return addEntryToGroup(userId,groupId,parentEntryName,name,vocabularyName,properties,addCommunityPermissions,addGuestPermissions,communityPermissions,guestPermissions);
}

{
  TagsVocabulary vocabulary=tagsVocabularyLocalService.getGroupVocabulary(groupId,vocabularyName);
  TagsEntry entry=getEntry(groupId,parentEntryName);
  return tagsEntryPersistence.findByP_V(entry.getEntryId(),vocabulary.getVocabularyId());
}

{
  long nodeId=getNodeId(virtualWiki);
  try {
    int pagesCount=WikiPageLocalServiceUtil.getPagesCount(nodeId,topicName,true);
    if (pagesCount > 0) {
      return 1;
    }
  }
 catch (  SystemException se) {
    _log.error(se,se);
  }
  return null;
}

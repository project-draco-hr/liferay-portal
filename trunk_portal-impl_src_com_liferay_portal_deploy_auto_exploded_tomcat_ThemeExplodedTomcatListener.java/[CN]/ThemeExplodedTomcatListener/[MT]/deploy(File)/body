{
  if (_log.isDebugEnabled()) {
    _log.debug("Invoking deploy for " + file.getPath());
  }
  File docBaseDir=getDocBaseDir(file,"WEB-INF/liferay-look-and-feel.xml");
  if (docBaseDir == null) {
    return;
  }
  if (_log.isInfoEnabled()) {
    _log.info("Modifying themes for " + file.getPath());
  }
  _deployer.explodedTomcatDeploy(file,docBaseDir,null);
  if (_log.isInfoEnabled()) {
    _log.info("Themes for " + file.getPath() + " modified successfully");
  }
  copyContextFile(file);
}

{
  if (!contains(permissionChecker,layoutSetBranchId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),LayoutSetBranch.class.getName(),layoutSetBranchId,actionId);
  }
}

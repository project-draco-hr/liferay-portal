{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MembershipRequest membershipRequest) method. To improve performance, call update(MembershipRequest membershipRequest, boolean merge).");
  }
  return update(membershipRequest,false);
}

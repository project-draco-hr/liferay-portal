{
  MembershipRequest membershipRequest=(MembershipRequest)EntityCacheUtil.getResult(MembershipRequestModelImpl.ENTITY_CACHE_ENABLED,MembershipRequestImpl.class,membershipRequestId,this);
  if (membershipRequest == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (membershipRequest == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      membershipRequest=(MembershipRequest)session.get(MembershipRequestImpl.class,Long.valueOf(membershipRequestId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (membershipRequest == null)) {
        EntityCacheUtil.putResult(MembershipRequestModelImpl.ENTITY_CACHE_ENABLED,MembershipRequestImpl.class,membershipRequestId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(membershipRequest);
      }
      closeSession(session);
    }
  }
  return membershipRequest;
}

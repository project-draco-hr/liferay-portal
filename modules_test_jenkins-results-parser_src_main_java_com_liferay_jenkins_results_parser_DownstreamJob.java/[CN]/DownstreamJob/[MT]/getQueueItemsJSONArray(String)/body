{
  JSONObject jsonObject=JenkinsResultsParserUtil.toJSONObject(masterURL + "/queue/api/json?tree=items[actions[parameters" + "[name,value]],task[name,url]]",false);
  return jsonObject.getJSONArray("items");
}

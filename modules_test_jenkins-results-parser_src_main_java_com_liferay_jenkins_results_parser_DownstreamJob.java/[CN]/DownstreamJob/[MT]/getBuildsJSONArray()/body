{
  JSONObject jsonObject=JenkinsResultsParserUtil.toJSONObject("http://" + master + "/job/"+ name+ "/api/json?tree=builds[actions[parameters"+ "[name,type,value]],building,duration,number,result,url]",false);
  return jsonObject.getJSONArray("builds");
}

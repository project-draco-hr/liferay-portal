{
  BackgroundTaskStatus backgroundTaskStatus=_backgroundTaskStatusRegistry.fetch(backgroundTaskId);
  if (backgroundTaskStatus != null) {
    return backgroundTaskStatus.getJSONString();
  }
  return StringPool.BLANK;
}

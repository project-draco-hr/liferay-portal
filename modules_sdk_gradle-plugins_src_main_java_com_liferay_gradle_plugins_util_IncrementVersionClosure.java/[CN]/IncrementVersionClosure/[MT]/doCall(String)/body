{
  VersionNumber versionNumber=VersionNumber.parse(version);
  VersionNumber newVersionNumber=new VersionNumber(versionNumber.getMajor() + _majorIncrement,versionNumber.getMinor() + _minorIncrement,versionNumber.getMicro() + _microIncrement,versionNumber.getQualifier());
  return newVersionNumber.toString();
}

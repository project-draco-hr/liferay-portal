{
  if (!clazz.isAnnotationPresent(_DDM_FORM_ANNOTATION)) {
    throw new IllegalArgumentException("The provided type is not supported.");
  }
  DDMForm ddmForm=new DDMForm();
  addDDMFormFields(clazz,ddmForm);
  return ddmForm;
}

{
  if ((_discussionComment == null) || !_discussionTaglibHelper.isRatingsEnabled()) {
    return false;
  }
  return !TrashUtil.isInTrash(_discussionComment.getModelClassName(),_discussionComment.getCommentId());
}

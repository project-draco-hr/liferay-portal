{
  Map<Locale,String> titleMap=new HashMap<>();
  for (  Locale locale : _locales) {
    titleMap.put(locale,title);
  }
  return updateArticle(article,titleMap,content,workflowEnabled,approved,serviceContext);
}

{
  if (isEscapedModel()) {
    return (ServiceComponent)this;
  }
 else {
    return (ServiceComponent)Proxy.newProxyInstance(ServiceComponent.class.getClassLoader(),new Class[]{ServiceComponent.class},new AutoEscapeBeanHandler(this));
  }
}

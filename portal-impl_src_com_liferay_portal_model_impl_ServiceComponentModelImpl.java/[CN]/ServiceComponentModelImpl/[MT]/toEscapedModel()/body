{
  if (isEscapedModel()) {
    return (ServiceComponent)this;
  }
 else {
    return (ServiceComponent)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

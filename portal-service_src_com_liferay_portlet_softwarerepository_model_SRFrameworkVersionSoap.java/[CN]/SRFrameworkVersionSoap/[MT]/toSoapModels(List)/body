{
  List soapModels=new ArrayList(models.size());
  for (int i=0; i < models.size(); i++) {
    SRFrameworkVersion model=(SRFrameworkVersion)models.get(i);
    soapModels.add(toSoapModel(model));
  }
  return (SRFrameworkVersionSoap[])soapModels.toArray(new SRFrameworkVersionSoap[0]);
}

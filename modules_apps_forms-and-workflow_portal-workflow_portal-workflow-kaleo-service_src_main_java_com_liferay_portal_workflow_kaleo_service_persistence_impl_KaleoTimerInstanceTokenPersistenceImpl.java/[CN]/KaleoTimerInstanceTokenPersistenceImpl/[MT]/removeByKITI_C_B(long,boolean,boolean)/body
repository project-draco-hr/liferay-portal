{
  for (  KaleoTimerInstanceToken kaleoTimerInstanceToken : findByKITI_C_B(kaleoInstanceTokenId,completed,blocking,QueryUtil.ALL_POS,QueryUtil.ALL_POS,null)) {
    remove(kaleoTimerInstanceToken);
  }
}

{
  int count=countByNotRemindBy(remindBy);
  if (count == 0) {
    return null;
  }
  List<CalEvent> list=findByNotRemindBy(remindBy,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

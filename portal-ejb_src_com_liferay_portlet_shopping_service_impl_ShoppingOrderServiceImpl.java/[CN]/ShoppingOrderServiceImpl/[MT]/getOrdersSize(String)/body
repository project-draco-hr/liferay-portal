{
  int count=0;
  if (Validator.isNull(ppPaymentStatus)) {
    count=ShoppingOrderUtil.countByUserId(getUserId());
  }
 else {
    count=ShoppingOrderUtil.countByU_PPPS(getUserId(),ShoppingUtil.getPpPaymentStatus(ppPaymentStatus));
  }
  return count;
}

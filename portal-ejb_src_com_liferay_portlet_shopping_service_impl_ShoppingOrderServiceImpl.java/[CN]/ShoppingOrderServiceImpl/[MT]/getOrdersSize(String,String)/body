{
  int count=0;
  if (Validator.isNull(ppPaymentStatus)) {
    count=ShoppingOrderUtil.countByCompanyId(companyId);
  }
 else {
    count=ShoppingOrderUtil.countByC_PPPS(companyId,ShoppingUtil.getPpPaymentStatus(ppPaymentStatus));
  }
  return count;
}

{
  Map<String,Object> parameters=new HashMap<>();
  parameters.put("inline",GetterUtil.getBoolean(ddmFormField.getProperty("inline")));
  parameters.put("options",getOptions(ddmFormField,ddmFormFieldRenderingContext));
  return parameters;
}

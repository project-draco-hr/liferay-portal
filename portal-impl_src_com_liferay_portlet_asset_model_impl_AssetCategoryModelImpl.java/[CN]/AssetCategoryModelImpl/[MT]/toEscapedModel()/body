{
  if (isEscapedModel()) {
    return (AssetCategory)this;
  }
 else {
    return (AssetCategory)Proxy.newProxyInstance(AssetCategory.class.getClassLoader(),new Class[]{AssetCategory.class},new AutoEscapeBeanHandler(this));
  }
}

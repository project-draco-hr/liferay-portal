{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  WSRPConsumer wsrpConsumer=WSRPConsumerLocalServiceUtil.fetchWSRPConsumerByUuidAndCompanyId(uuid,group.getCompanyId());
  if (wsrpConsumer != null) {
    deleteStagedModel(wsrpConsumer);
  }
}

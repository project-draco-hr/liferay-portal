{
  if (isEscapedModel()) {
    return (BrowserTracker)this;
  }
 else {
    return (BrowserTracker)Proxy.newProxyInstance(BrowserTracker.class.getClassLoader(),new Class[]{BrowserTracker.class},new AutoEscapeBeanHandler(this));
  }
}

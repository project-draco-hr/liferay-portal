{
  if (isEscapedModel()) {
    return (BrowserTracker)this;
  }
 else {
    return (BrowserTracker)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

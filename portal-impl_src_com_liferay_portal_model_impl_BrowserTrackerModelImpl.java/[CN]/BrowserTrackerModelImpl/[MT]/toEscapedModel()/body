{
  if (isEscapedModel()) {
    return (BrowserTracker)this;
  }
 else {
    BrowserTracker model=new BrowserTrackerImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setBrowserTrackerId(getBrowserTrackerId());
    model.setUserId(getUserId());
    model.setBrowserKey(getBrowserKey());
    model=(BrowserTracker)Proxy.newProxyInstance(BrowserTracker.class.getClassLoader(),new Class[]{BrowserTracker.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

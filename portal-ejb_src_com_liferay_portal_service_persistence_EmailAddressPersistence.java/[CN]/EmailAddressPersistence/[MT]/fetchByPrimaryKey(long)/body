{
  Session session=null;
  try {
    session=openSession();
    return (EmailAddress)session.get(EmailAddressImpl.class,new Long(emailAddressId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

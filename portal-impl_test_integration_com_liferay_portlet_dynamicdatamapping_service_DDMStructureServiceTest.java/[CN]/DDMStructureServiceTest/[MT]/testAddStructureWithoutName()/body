{
  String storageType=StorageType.XML.getValue();
  try {
    addStructure(_classNameId,null,StringPool.BLANK,getTestStructureXsd(storageType),storageType,DDMStructureConstants.TYPE_DEFAULT);
    Assert.fail();
  }
 catch (  StructureNameException sne) {
  }
}

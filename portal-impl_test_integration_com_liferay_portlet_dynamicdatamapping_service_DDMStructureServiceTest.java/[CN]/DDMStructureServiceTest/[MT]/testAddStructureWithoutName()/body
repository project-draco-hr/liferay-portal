{
  String storageType=StorageType.XML.getValue();
  try {
    addStructure(_CLASS_NAME_ID,null,StringPool.BLANK,getTestStructureDefinition(storageType),storageType,DDMStructureConstants.TYPE_DEFAULT);
    Assert.fail();
  }
 catch (  StructureNameException sne) {
  }
}

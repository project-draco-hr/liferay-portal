{
  String structureKey=ServiceTestUtil.randomString();
  String storageType=StorageType.XML.getValue();
  try {
    addStructure(_classNameId,structureKey,"Test Structure 1",getTestStructureXsd(storageType),storageType,DDMStructureConstants.TYPE_DEFAULT);
    addStructure(_classNameId,structureKey,"Test Structure 2",getTestStructureXsd(storageType),storageType,DDMStructureConstants.TYPE_DEFAULT);
    Assert.fail();
  }
 catch (  StructureDuplicateStructureKeyException sdske) {
  }
}

{
  DDMStructure structure=addStructure(_testClassNameId,"Test Structure");
  DDMStructure fetchStructure=DDMStructureLocalServiceUtil.fetchStructure(structure.getGroupId(),structure.getStructureKey());
  Assert.assertNotNull(fetchStructure);
}

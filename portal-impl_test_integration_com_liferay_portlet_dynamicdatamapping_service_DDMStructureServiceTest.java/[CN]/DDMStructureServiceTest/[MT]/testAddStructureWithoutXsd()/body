{
  String storageType=StorageType.XML.getValue();
  int type=DDMStructureConstants.TYPE_DEFAULT;
  try {
    addStructure(_testClassNameId,null,"Test Structure",StringPool.BLANK,storageType,type);
    Assert.fail("Should not be able to add Structure because " + "xsd is empty");
  }
 catch (  Exception e) {
  }
}

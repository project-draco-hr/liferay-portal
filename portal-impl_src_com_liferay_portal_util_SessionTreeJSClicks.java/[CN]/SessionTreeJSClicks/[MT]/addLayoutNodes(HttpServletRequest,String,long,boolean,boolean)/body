{
  try {
    List<String> layoutIds=getLayoutsChildrenIds(request,layoutId,privateLayout,recursive);
    addNodes(request,treeId,layoutIds.toArray(new String[layoutIds.size()]));
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

{
  Reference<T> reference=referenceFactory.createReference(realReference,_referenceQueue);
  _finalizeActions.put(reference,finalizeAction);
  if (!THREAD_ENABLED) {
    _pollingCleanup();
  }
  return reference;
}

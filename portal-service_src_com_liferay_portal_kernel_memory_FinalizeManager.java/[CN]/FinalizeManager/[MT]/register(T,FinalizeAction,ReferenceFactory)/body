{
  Reference<T> newReference=referenceFactory.createReference(reference,_referenceQueue);
  _finalizeActions.put(newReference,finalizeAction);
  if (!THREAD_ENABLED) {
    _pollingCleanup();
  }
  return newReference;
}

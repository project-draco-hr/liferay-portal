{
  String[] isbns=StringUtil.split(getParameter(actionRequest,"isbns").toUpperCase(),StringPool.SPACE);
  Arrays.sort(isbns);
  setPreference(actionRequest,"isbns",isbns);
  super.processAction(portletConfig,actionRequest,actionResponse);
}

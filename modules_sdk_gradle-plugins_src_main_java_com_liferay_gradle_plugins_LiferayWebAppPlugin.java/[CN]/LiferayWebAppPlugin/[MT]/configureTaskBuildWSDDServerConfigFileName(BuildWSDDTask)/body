{
  Project project=buildWSDDTask.getProject();
  File serverConfigFile=new File(getWebAppDir(project),"WEB-INF/server-config.wsdd");
  buildWSDDTask.setServerConfigFileName(project.relativePath(serverConfigFile));
}

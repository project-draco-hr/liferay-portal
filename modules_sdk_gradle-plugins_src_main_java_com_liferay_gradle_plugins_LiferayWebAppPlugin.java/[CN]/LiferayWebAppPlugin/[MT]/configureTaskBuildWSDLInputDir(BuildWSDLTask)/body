{
  File inputDir=buildWSDLTask.getInputDir();
  if (!inputDir.exists()) {
    inputDir=new File(getWebAppDir(buildWSDLTask.getProject()),"WEB-INF/wsdl");
    buildWSDLTask.setInputDir(inputDir);
  }
}

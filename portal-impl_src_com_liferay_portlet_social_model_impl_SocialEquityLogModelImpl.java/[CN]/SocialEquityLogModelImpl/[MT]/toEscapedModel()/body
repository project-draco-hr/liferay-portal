{
  if (isEscapedModel()) {
    return (SocialEquityLog)this;
  }
 else {
    return (SocialEquityLog)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

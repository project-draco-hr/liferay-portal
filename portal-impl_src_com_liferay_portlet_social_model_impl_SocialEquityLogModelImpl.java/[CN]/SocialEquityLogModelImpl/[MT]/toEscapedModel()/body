{
  if (isEscapedModel()) {
    return (SocialEquityLog)this;
  }
 else {
    return (SocialEquityLog)Proxy.newProxyInstance(SocialEquityLog.class.getClassLoader(),new Class[]{SocialEquityLog.class},new AutoEscapeBeanHandler(this));
  }
}

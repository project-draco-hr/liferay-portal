{
  SocialActivityHandler<T> socialActivityHandler=getSocialActivityHandler(model.getModelClassName());
  socialActivityHandler.addUniqueActivity(userId,createDate,model,type,extraData,receiverUserId);
}

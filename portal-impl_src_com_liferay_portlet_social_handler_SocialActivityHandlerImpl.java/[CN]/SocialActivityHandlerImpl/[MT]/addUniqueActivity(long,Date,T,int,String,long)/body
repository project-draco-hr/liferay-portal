{
  SocialActivityHandler<T> socialActivityHandler=getSocialActivityHandler(classedModel.getModelClassName());
  socialActivityHandler.addUniqueActivity(userId,createDate,classedModel,type,extraData,receiverUserId);
}

{
  SocialActivityHandler socialActivityHandler=getSocialActivityHandler(className);
  socialActivityHandler.addUniqueActivity(userId,groupId,createDate,className,classPK,type,extraData,receiverUserId);
}

{
  resourceLocalService.addResources(review.getCompanyId(),review.getGroupId(),review.getUserId(),TasksProposal.class.getName(),review.getProposalId(),false,addCommunityPermissions,addGuestPermissions);
}

{
  try {
    return fileEntry1.getFolder().getUuid().equals(fileEntry2.getFolder().getUuid()) && fileEntry1.getSize() == fileEntry2.getSize() && fileEntry1.getVersion() == fileEntry2.getVersion() && fileEntry1.getVersionUserUuid().equals(fileEntry2.getVersionUserUuid());
  }
 catch (  SystemException e) {
    return false;
  }
}

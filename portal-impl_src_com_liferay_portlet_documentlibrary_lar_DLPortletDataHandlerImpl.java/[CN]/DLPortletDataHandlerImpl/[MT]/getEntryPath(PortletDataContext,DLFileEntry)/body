{
  return context.getPortletPath(PortletKeys.DOCUMENT_LIBRARY) + "/entries/" + entry.getFileEntryId()+ CharPool.FORWARD_SLASH+ entry.getVersion()+ CharPool.FORWARD_SLASH+ entry.getFileEntryId()+ ".xml";
}

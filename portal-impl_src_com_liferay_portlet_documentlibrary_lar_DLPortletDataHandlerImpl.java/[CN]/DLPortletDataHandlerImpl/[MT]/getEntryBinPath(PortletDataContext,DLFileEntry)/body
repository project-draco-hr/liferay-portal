{
  return context.getPortletPath(PortletKeys.DOCUMENT_LIBRARY) + "/bin/" + entry.getFileEntryId()+ CharPool.FORWARD_SLASH+ entry.getVersion()+ CharPool.FORWARD_SLASH+ entry.getTitleWithExtension();
}

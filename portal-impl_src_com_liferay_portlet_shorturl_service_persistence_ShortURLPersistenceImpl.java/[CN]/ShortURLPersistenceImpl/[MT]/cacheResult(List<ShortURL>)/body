{
  for (  ShortURL shortURL : shortURLs) {
    if (EntityCacheUtil.getResult(ShortURLModelImpl.ENTITY_CACHE_ENABLED,ShortURLImpl.class,shortURL.getPrimaryKey(),this) == null) {
      cacheResult(shortURL);
    }
  }
}

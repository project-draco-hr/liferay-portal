{
  String uniqueUrlTitle=urlTitle + StringPool.DASH + suffix;
  int maxLength=ModelHintsUtil.getMaxLength(KBArticle.class.getName(),"urlTitle");
  return StringUtil.shorten(uniqueUrlTitle,maxLength,StringPool.DASH + suffix);
}

{
  IndexName indexName=new IndexName(testName.getMethodName());
  IndexCreationHelper indexCreationHelper=new LiferayIndexCreationHelper(_elasticsearchFixture.getIndicesAdminClient(),indexName);
  return _elasticsearchFixture.createIndex(indexName,indexCreationHelper);
}

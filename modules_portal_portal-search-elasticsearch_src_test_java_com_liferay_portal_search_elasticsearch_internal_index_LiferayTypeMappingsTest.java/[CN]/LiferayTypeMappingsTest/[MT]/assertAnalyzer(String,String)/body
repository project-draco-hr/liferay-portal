{
  IdempotentRetryAssert.retryAssert(10,TimeUnit.SECONDS,new Callable<Void>(){
    @Override public Void call() throws Exception {
      Assert.assertEquals(analyzer,getAnalyzer(getFieldMapping(field),field));
      return null;
    }
  }
);
}

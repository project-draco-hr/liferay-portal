{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PluginSetting pluginSetting) method. To improve performance, call update(PluginSetting pluginSetting, boolean merge).");
  }
  return update(pluginSetting,false);
}

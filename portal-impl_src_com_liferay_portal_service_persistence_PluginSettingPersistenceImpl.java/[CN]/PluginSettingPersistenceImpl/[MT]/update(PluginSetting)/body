{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PluginSetting pluginSetting) method. Use update(PluginSetting pluginSetting, boolean merge) instead.");
  }
  return update(pluginSetting,false);
}

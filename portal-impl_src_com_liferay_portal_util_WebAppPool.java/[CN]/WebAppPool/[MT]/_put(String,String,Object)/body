{
  Map<String,Object> map=(Map<String,Object>)_webAppPool.get(webAppId);
  if (map == null) {
    map=new ConcurrentHashMap<String,Object>();
    _webAppPool.put(webAppId,map);
  }
  map.put(key,obj);
}

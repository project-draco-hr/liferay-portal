{
  Map<String,Object> map=_webAppPool.get(webAppId);
  if (map == null) {
    map=new ConcurrentHashMap<String,Object>();
    Map<String,Object> existingMap=_webAppPool.putIfAbsent(webAppId,map);
    if (existingMap != null) {
      map=existingMap;
    }
  }
  map.put(key,obj);
}

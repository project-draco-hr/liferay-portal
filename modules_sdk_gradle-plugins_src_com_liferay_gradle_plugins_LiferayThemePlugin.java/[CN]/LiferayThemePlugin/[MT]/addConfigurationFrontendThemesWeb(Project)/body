{
  Configuration configuration=GradleUtil.addConfiguration(project,FRONTEND_THEMES_WEB_CONFIGURATION_NAME);
  configuration.setDescription("Configures com.liferay.frontend.themes.web for compiling themes.");
  configuration.setVisible(false);
  GradleUtil.executeIfEmpty(configuration,new Action<Configuration>(){
    @Override public void execute(    Configuration configuration){
      addDependenciesFrontendThemesWeb(project);
    }
  }
);
  return configuration;
}

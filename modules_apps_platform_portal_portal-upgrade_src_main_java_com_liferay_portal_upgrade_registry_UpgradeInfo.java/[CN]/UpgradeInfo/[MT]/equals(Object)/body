{
  if (this == object) {
    return true;
  }
  if (!(object instanceof UpgradeInfo)) {
    return false;
  }
  UpgradeInfo upgradeInfo=(UpgradeInfo)object;
  if (Validator.equals(_fromSchemaVersionString,upgradeInfo._fromSchemaVersionString) && Validator.equals(_toSchemaVersionString,upgradeInfo._toSchemaVersionString) && Validator.equals(_upgradeStep,upgradeInfo._upgradeStep)) {
    return true;
  }
  return false;
}

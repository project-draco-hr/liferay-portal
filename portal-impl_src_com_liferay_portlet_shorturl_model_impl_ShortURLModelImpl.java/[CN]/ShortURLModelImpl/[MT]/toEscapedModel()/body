{
  if (isEscapedModel()) {
    return (ShortURL)this;
  }
 else {
    return (ShortURL)Proxy.newProxyInstance(ShortURL.class.getClassLoader(),new Class[]{ShortURL.class},new AutoEscapeBeanHandler(this));
  }
}

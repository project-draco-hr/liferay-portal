{
  try {
    new NettyFabricAgentConfig(null);
    Assert.fail();
  }
 catch (  NullPointerException npe) {
  }
  File repositoryFolder=new File("RepositoryFolder");
  NettyFabricAgentConfig nettyFabricAgentConfig=new NettyFabricAgentConfig(repositoryFolder);
  Assert.assertEquals(repositoryFolder.toPath(),nettyFabricAgentConfig.getRepositoryPath());
}

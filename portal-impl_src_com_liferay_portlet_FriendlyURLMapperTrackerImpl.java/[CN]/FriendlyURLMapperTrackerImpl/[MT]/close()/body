{
  for (  Map.Entry<FriendlyURLMapper,ServiceRegistration<?>> friendlyURLMapperServiceRegistrationEntry : _serviceRegistrations.entrySet()) {
    ServiceRegistration<?> serviceRegistration=friendlyURLMapperServiceRegistrationEntry.getValue();
    serviceRegistration.unregister();
  }
  _serviceTracker.close();
}

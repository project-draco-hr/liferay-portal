{
  int pos=recipient.indexOf(StringPool.AT);
  String smtpServerSubdomain=PropsUtil.get(PropsUtil.SMTP_SERVER_SUBDOMAIN);
  String mx=recipient.substring(pos + smtpServerSubdomain.length() + 2);
  return CompanyLocalServiceUtil.getCompanyByMx(mx);
}

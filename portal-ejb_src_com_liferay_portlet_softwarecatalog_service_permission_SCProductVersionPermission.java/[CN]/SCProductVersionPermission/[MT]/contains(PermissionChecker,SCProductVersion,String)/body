{
  SCProductEntry productEntry=productVersion.getProductEntry();
  return permissionChecker.hasPermission(productEntry.getGroupId(),SCProductVersion.class.getName(),productVersion.getPrimaryKey(),actionId);
}

{
  String roleName=role.getName();
  if ((roleName.equals(RoleConstants.ORGANIZATION_ADMINISTRATOR) || roleName.equals(RoleConstants.ORGANIZATION_OWNER)) && hasMembershipProtected(permissionChecker,groupId,userId)) {
    return true;
  }
  return false;
}

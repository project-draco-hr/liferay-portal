{
  MBDiscussion model=new MBDiscussionImpl();
  model.setDiscussionId(getDiscussionId());
  model.setClassNameId(getClassNameId());
  model.setClassPK(getClassPK());
  model.setThreadId(getThreadId());
  if (true) {
    model=(MBDiscussion)Proxy.newProxyInstance(MBDiscussion.class.getClassLoader(),new Class[]{MBDiscussion.class},new ReadOnlyBeanHandler(model));
  }
  return model;
}

{
  if (isEscapedModel()) {
    return (MBDiscussion)this;
  }
 else {
    return (MBDiscussion)Proxy.newProxyInstance(MBDiscussion.class.getClassLoader(),new Class[]{MBDiscussion.class},new AutoEscapeBeanHandler(this));
  }
}

{
  int count=countByC_C_T_M(classNameId,classPK,type,mode);
  List<DDMTemplate> list=findByC_C_T_M(classNameId,classPK,type,mode,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

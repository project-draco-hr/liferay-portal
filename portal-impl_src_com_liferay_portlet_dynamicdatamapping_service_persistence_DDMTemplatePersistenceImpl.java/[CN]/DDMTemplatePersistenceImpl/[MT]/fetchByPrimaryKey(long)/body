{
  DDMTemplate ddmTemplate=(DDMTemplate)EntityCacheUtil.getResult(DDMTemplateModelImpl.ENTITY_CACHE_ENABLED,DDMTemplateImpl.class,templateId,this);
  if (ddmTemplate == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (ddmTemplate == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      ddmTemplate=(DDMTemplate)session.get(DDMTemplateImpl.class,Long.valueOf(templateId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (ddmTemplate == null)) {
        EntityCacheUtil.putResult(DDMTemplateModelImpl.ENTITY_CACHE_ENABLED,DDMTemplateImpl.class,templateId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(ddmTemplate);
      }
      closeSession(session);
    }
  }
  return ddmTemplate;
}

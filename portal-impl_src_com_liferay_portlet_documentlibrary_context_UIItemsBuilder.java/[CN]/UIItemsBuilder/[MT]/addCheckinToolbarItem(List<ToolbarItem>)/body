{
  if (!_fileEntryDisplayContextHelper.isCheckinActionAvailable()) {
    return;
  }
  _addJavascriptUIItem(new JavascriptToolbarItem(),toolbarItems,"icon-lock",DLUIItemKeys.CHECKIN,UnicodeLanguageUtil.get(_request,"checkin"),getSubmitFormJavascript(Constants.CHECKIN,null));
}

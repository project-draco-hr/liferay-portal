{
  Content content=addContent();
  Content newContent=_service.getContent(content.getCompanyId(),content.getRepositoryId(),content.getPath(),content.getVersion());
  assertEquals(content,newContent);
}

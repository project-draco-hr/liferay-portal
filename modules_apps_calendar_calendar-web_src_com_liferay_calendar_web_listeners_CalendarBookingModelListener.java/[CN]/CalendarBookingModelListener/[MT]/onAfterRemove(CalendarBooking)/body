{
  CalEvent calEvent=_calEventLocalService.fetchCalEventByUuidAndGroupId(calendarBooking.getUuid(),calendarBooking.getGroupId());
  if (calEvent != null) {
    _calEventLocalService.deleteCalEvent(calEvent);
  }
}

{
  Iterator itr=scProductScreenshotPersistence.findByProductEntryId(productEntryId).iterator();
  while (itr.hasNext()) {
    SCProductScreenshot productScreenshot=(SCProductScreenshot)itr.next();
    deleteProductScreenshot(productScreenshot);
  }
}

{
  if (PropsValues.AUTH_TOKEN_CHECK_ENABLED && PropsValues.JSON_SERVICE_AUTH_TOKEN_ENABLED) {
    if ((_whiteList.size() > 0) && AuthSettingsUtil.isAccessAllowed(request,_whiteList)) {
      return;
    }
    AuthTokenUtil.check(request);
  }
}

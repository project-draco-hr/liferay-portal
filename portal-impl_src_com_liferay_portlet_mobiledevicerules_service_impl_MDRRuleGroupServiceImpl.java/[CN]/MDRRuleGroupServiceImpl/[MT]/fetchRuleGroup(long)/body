{
  MDRRuleGroup ruleGroup=mdrRuleGroupLocalService.fetchRuleGroup(ruleGroupId);
  if (ruleGroup != null) {
    MDRRuleGroupPermissionUtil.check(getPermissionChecker(),ruleGroup.getGroupId(),ruleGroupId,ActionKeys.VIEW);
  }
  return ruleGroup;
}

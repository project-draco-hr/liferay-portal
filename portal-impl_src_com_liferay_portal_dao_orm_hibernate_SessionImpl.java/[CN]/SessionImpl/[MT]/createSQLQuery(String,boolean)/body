{
  try {
    queryString=SQLTransformer.transformFromJpqlToHql(queryString);
    return DoPrivilegedUtil.wrap(new SQLQueryImpl(_session.createSQLQuery(queryString),strictName),true);
  }
 catch (  Exception e) {
    throw ExceptionTranslator.translate(e);
  }
}

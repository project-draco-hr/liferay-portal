{
  if (!contains(permissionChecker,folder,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),BookmarksFolder.class.getName(),folder.getFolderId(),actionId);
  }
}

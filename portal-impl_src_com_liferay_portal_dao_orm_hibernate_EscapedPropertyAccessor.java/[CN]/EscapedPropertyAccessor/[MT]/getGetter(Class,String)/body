{
  if (propertyName.charAt(propertyName.length() - 1) == CharPool.UNDERLINE) {
    propertyName=propertyName.substring(0,propertyName.length() - 1);
  }
  return super.getGetter(clazz,propertyName);
}

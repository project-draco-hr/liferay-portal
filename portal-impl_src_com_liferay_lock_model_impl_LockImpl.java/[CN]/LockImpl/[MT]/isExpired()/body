{
  if (_expirationTime <= 0) {
    return false;
  }
 else {
    Date now=new Date();
    if (now.getTime() > (_date.getTime() + _expirationTime)) {
      return true;
    }
 else {
      return false;
    }
  }
}

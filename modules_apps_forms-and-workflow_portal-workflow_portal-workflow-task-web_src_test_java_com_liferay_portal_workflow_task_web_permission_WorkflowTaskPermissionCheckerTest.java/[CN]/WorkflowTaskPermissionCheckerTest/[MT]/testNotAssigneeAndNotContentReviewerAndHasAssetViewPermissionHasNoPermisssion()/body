{
  PermissionChecker permissionChecker=mockPermissionChecker(false,false,false);
  mockAssetRendererPermissionChecker(true);
  Assert.assertFalse(_workflowTaskPermissionChecker.hasPermission(RandomTestUtil.randomLong(),mockWorkflowTask(),permissionChecker));
}

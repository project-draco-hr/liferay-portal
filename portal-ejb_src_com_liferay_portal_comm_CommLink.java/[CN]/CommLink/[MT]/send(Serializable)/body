{
  try {
    if (_channel == null) {
      return;
    }
    Message msg=new Message(null,null,obj);
    _channel.send(msg);
  }
 catch (  Exception e) {
    _log.error("Error sending message",e);
  }
}

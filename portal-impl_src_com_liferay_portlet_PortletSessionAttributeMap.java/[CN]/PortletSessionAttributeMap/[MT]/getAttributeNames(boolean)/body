{
  Enumeration<String> enumeration=session.getAttributeNames();
  if (scopePrefix == null) {
    return enumeration;
  }
  return new MappingEnumeration<>(enumeration,new AttributeNameMapper(scopePrefix,removePrefix));
}

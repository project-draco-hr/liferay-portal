{
  ResourceLocalServiceUtil.addModelResources(image.getCompanyId(),folder.getGroupId(),image.getUserId(),IGImage.class.getName(),image.getPrimaryKey().toString(),communityPermissions,guestPermissions);
}

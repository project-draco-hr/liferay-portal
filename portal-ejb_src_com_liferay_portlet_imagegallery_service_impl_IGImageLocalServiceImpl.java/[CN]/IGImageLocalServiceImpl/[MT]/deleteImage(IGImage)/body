{
  ImageLocalUtil.remove(image.getLargeImageId());
  ImageLocalUtil.remove(image.getSmallImageId());
  ResourceLocalServiceUtil.deleteResource(image.getCompanyId(),IGImage.class.getName(),Resource.TYPE_CLASS,Resource.SCOPE_INDIVIDUAL,image.getPrimaryKey().toString());
  IGImageUtil.remove(image.getPrimaryKey());
}

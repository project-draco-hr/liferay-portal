{
  if (isEscapedModel()) {
    return (ShoppingCoupon)this;
  }
 else {
    return (ShoppingCoupon)Proxy.newProxyInstance(ShoppingCoupon.class.getClassLoader(),new Class[]{ShoppingCoupon.class},new AutoEscapeBeanHandler(this));
  }
}

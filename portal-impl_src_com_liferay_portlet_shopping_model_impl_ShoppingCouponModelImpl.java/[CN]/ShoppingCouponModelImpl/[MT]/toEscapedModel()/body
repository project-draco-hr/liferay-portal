{
  if (isEscapedModel()) {
    return (ShoppingCoupon)this;
  }
 else {
    return (ShoppingCoupon)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

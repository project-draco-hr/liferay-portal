{
  KaleoLog kaleoLog=(KaleoLog)entityCache.getResult(KaleoLogModelImpl.ENTITY_CACHE_ENABLED,KaleoLogImpl.class,primaryKey);
  if (kaleoLog == _nullKaleoLog) {
    return null;
  }
  if (kaleoLog == null) {
    Session session=null;
    try {
      session=openSession();
      kaleoLog=(KaleoLog)session.get(KaleoLogImpl.class,primaryKey);
      if (kaleoLog != null) {
        cacheResult(kaleoLog);
      }
 else {
        entityCache.putResult(KaleoLogModelImpl.ENTITY_CACHE_ENABLED,KaleoLogImpl.class,primaryKey,_nullKaleoLog);
      }
    }
 catch (    Exception e) {
      entityCache.removeResult(KaleoLogModelImpl.ENTITY_CACHE_ENABLED,KaleoLogImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return kaleoLog;
}

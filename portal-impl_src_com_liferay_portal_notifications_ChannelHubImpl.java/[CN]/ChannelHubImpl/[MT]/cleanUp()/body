{
  Lock readLock=_readWriteLock.readLock();
  try {
    readLock.lock();
    for (    Channel channel : _channels.values()) {
      channel.cleanUp();
    }
  }
  finally {
    readLock.unlock();
  }
}

{
  Lock writeLock=_readWriteLock.writeLock();
  try {
    writeLock.lock();
    for (    Channel channel : _channels.values()) {
      channel.close();
    }
    _channels.clear();
  }
  finally {
    writeLock.unlock();
  }
}

{
  Iterator<Map.Entry<Long,Channel>> iterator=_channels.entrySet().iterator();
  while (iterator.hasNext()) {
    Channel channel=iterator.next().getValue();
    channel.close();
    iterator.remove();
  }
}

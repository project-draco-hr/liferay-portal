{
  for (  String keywords : _searchKeywords) {
    checkSortedSearchResultsOrder(keywords,_descending,SINGLE_INT,Sort.INT_TYPE);
  }
  for (  String keywords : _searchKeywordsOdd) {
    checkSortedSearchResultsOrder(keywords,_descendingOdd,SINGLE_INT,Sort.INT_TYPE);
  }
}

{
  DDLRecord record=_ddlRecordLocalService.fetchDDLRecord(GetterUtil.getLong(primKey));
  if (record == null) {
    return;
  }
  record.setModifiedDate(new Date());
  _ddlRecordLocalService.updateDDLRecord(record);
}

{
  int count=_counter.getAndIncrement();
  if (count >= _priorities.length) {
    throw new IllegalStateException("Cannot create more than " + _priorities.length + " channels");
  }
  return new ClusterLinkPortalCacheClusterChannel(_clusterLink,DestinationNames.CACHE_REPLICATION,_priorities[count]);
}

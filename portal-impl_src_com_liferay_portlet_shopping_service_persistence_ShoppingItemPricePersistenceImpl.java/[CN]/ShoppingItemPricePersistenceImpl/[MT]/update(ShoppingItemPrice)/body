{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingItemPrice shoppingItemPrice) method. To improve performance, call update(ShoppingItemPrice shoppingItemPrice, boolean merge).");
  }
  return update(shoppingItemPrice,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingItemPrice shoppingItemPrice) method. Use update(ShoppingItemPrice shoppingItemPrice, boolean merge) instead.");
  }
  return update(shoppingItemPrice,false);
}

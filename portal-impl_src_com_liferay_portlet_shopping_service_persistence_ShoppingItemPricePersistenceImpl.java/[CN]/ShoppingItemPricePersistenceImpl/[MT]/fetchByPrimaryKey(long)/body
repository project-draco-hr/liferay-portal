{
  Session session=null;
  try {
    session=openSession();
    return (ShoppingItemPrice)session.get(ShoppingItemPriceImpl.class,new Long(itemPriceId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

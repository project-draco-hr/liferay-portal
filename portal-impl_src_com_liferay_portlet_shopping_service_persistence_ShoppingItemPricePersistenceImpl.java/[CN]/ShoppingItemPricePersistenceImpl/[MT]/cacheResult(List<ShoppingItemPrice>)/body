{
  for (  ShoppingItemPrice shoppingItemPrice : shoppingItemPrices) {
    if (EntityCacheUtil.getResult(ShoppingItemPriceModelImpl.ENTITY_CACHE_ENABLED,ShoppingItemPrice.class,shoppingItemPrice.getPrimaryKey(),this) == null) {
      cacheResult(shoppingItemPrice);
    }
  }
}

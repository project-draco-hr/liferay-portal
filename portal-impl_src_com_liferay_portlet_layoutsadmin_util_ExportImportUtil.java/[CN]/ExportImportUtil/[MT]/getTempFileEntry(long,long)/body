{
  String[] tempFileEntryNames=LayoutServiceUtil.getTempFileEntryNames(groupId,TEMP_FOLDER_NAME);
  if (tempFileEntryNames.length == 0) {
    return null;
  }
  return TempFileUtil.getTempFile(groupId,userId,tempFileEntryNames[0],TEMP_FOLDER_NAME);
}

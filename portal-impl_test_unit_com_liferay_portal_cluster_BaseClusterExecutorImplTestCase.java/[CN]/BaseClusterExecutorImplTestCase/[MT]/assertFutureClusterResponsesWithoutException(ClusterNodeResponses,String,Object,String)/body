{
  List<String> expectedClusterNodeIds=new ArrayList<>();
  expectedClusterNodeIds.add(expectedClusterNodeId);
  assertFutureClusterResponsesWithoutException(clusterNodeResponses,expectedUUID,exceptedResult,expectedClusterNodeIds);
}

{
  List<Address> expectedAddresses=new ArrayList<Address>();
  expectedAddresses.add(expectedAddress);
  assertFutureClusterResponsesWithoutException(clusterNodeResponses,expectedUUID,exceptedResult,expectedAddresses);
}

{
  if (isEscapedModel()) {
    return (DDLRecordVersion)this;
  }
 else {
    return (DDLRecordVersion)Proxy.newProxyInstance(DDLRecordVersion.class.getClassLoader(),new Class[]{DDLRecordVersion.class},new AutoEscapeBeanHandler(this));
  }
}

{
  PrincipalSessionBean.setThreadValues(_sc);
  return SRProductVersionServiceFactory.getTxImpl().updateProductVersion(productVersionId,version,changeLog,downloadPageURL,directDownloadURL,repoStoreArtifact,frameworkVersionIds);
}

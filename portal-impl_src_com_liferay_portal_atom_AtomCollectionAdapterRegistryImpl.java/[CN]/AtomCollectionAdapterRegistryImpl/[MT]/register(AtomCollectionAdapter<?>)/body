{
  AtomCollectionAdapter<?> existingCollection=_atomCollectionAdapters.put(atomCollectionAdapter.getCollectionName(),atomCollectionAdapter);
  if (existingCollection != null) {
    throw new AtomException("Atom collection with the same name already exist: " + existingCollection.getCollectionName());
  }
}

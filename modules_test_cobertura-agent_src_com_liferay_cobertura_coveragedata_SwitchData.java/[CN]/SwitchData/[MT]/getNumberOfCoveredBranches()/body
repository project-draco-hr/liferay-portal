{
  lock.lock();
  try {
    int ret=(defaultHits > 0) ? 1 : 0;
    for (int i=hits.length - 1; i >= 0; i--) {
      if (hits[i] > 0)       ret++;
    }
    return ret;
  }
  finally {
    lock.unlock();
  }
}

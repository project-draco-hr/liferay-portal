{
  String[] deleteUserGroupIds=StringUtil.split(ParamUtil.getString(req,"deleteUserGroupIds"));
  for (int i=0; i < deleteUserGroupIds.length; i++) {
    UserGroupServiceUtil.deleteUserGroup(deleteUserGroupIds[i]);
  }
}

{
  Group group=ServiceTestUtil.addGroup();
  User user=ServiceTestUtil.addUser(null,true,new long[]{group.getGroupId()});
  giveSiteAdminRole(user,group);
  testGroup(user,group,null,null,true,false,true,false,true,true,true);
}

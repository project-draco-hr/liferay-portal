{
  Group group=GroupTestUtil.addGroup();
  Locale esLocale=new Locale("es","ES");
  group=GroupTestUtil.updateDisplaySettings(group.getGroupId(),null,esLocale);
  Assert.assertEquals(esLocale,PortalUtil.getSiteDefaultLocale(group.getGroupId()));
}

{
  if (!contains(permissionChecker,message,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),MBMessage.class.getName(),message.getMessageId(),actionId);
  }
}

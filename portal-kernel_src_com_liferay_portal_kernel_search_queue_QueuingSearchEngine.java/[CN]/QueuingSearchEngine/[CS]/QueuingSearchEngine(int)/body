{
  _queuingInvocationHandler=new QueuingInvocationHandler(capacity);
  Class<?> clazz=getClass();
  _indexWriter=(IndexWriter)ProxyUtil.newProxyInstance(clazz.getClassLoader(),new Class[]{IndexWriter.class},_queuingInvocationHandler);
}

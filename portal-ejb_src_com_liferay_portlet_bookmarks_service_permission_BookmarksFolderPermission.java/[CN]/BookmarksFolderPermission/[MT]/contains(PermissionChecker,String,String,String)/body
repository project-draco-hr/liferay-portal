{
  if (Validator.equals(folderId,BookmarksFolderImpl.DEFAULT_PARENT_FOLDER_ID)) {
    return PortletPermission.contains(permissionChecker,plid,PortletKeys.BOOKMARKS,actionId);
  }
 else {
    return contains(permissionChecker,folderId,actionId);
  }
}

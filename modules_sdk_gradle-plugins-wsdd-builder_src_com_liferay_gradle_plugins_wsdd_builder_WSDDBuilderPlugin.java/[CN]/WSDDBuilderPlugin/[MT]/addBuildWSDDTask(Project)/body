{
  TaskContainer taskContainer=project.getTasks();
  Task task=taskContainer.create(BUILD_WSDD_TASK_NAME,BuildWSDDTask.class);
  task.setDescription("Runs Liferay WSDD Builder.");
  task.setGroup("build");
  return task;
}

{
  try {
    releaseResourceBlock(permissionedModel.getResourceBlockId());
  }
 catch (  NoSuchResourceBlockException nsrbe) {
    if (_log.isWarnEnabled()) {
      _log.warn("Resource block " + permissionedModel.getResourceBlockId() + " missing");
    }
  }
}

{
  Portal portal=Mockito.mock(Portal.class);
  when(portal.getClassName(SearchTestUtil.ATTACHMENT_OWNER_CLASS_NAME_ID)).thenReturn(SearchTestUtil.ATTACHMENT_OWNER_CLASS_NAME);
  PortalUtil portalUtil=new PortalUtil();
  portalUtil.setPortal(portal);
}

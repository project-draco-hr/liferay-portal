{
  Long longValue=13L;
  long[] convertedLongs=_converter.convertType(ExpandoColumnConstants.LONG_ARRAY,longValue.toString());
  Assert.assertEquals(1,convertedLongs.length);
  Assert.assertEquals(longValue.longValue(),convertedLongs[0]);
}

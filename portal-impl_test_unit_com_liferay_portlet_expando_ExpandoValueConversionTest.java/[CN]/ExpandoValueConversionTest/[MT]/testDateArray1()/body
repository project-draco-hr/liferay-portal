{
  long time1=System.currentTimeMillis();
  long time2=System.currentTimeMillis();
  String[] dates=new String[]{String.valueOf(time1),String.valueOf(time2)};
  Date[] convertedDates=_converter.convertType(ExpandoColumnConstants.DATE_ARRAY,dates);
  Assert.assertEquals(2,convertedDates.length);
  Assert.assertEquals(time1,convertedDates[0].getTime());
  Assert.assertEquals(time2,convertedDates[1].getTime());
}

{
  try {
    Collection<String> booleans=new ArrayList<String>();
    booleans.add("true");
    booleans.add("other");
    boolean[] converted=_converter.convertType(ExpandoColumnConstants.BOOLEAN_ARRAY,booleans);
    Assert.fail();
  }
 catch (  Exception e) {
  }
}

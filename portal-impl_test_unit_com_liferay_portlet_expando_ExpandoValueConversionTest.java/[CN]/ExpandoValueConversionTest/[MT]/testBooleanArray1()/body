{
  boolean[] convertedBoooleans=_converter.convertType(ExpandoColumnConstants.BOOLEAN_ARRAY,"true");
  Assert.assertEquals(1,convertedBoooleans.length);
  Assert.assertTrue(convertedBoooleans[0]);
}

{
  int orgGroupPermissionsCount=PermissionUserFinder.countByOrgGroupPermissions(companyId,name,primKey,actionId);
  if (orgGroupPermissionsCount > 0) {
    return PermissionUserFinder.findByUserAndOrgGroupPermission(companyId,name,primKey,actionId,searchTerms,begin,end);
  }
 else {
    return PermissionUserFinder.findByPermissionAndRole(companyId,groupId,name,primKey,actionId,searchTerms,begin,end);
  }
}

{
  String title=getSearchKeywords();
  title+=ServiceTestUtil.randomString(_FILE_ENTRY_TITLE_MAX_LENGTH - title.length());
  FileEntry fileEntry=DLAppTestUtil.addFileEntry(groupId,folderId,ServiceTestUtil.randomString() + ".txt",title,approved);
  return (DLFileEntry)fileEntry.getModel();
}

{
  String title=getSearchKeywords();
  title+=RandomTestUtil.randomString(_FILE_ENTRY_TITLE_MAX_LENGTH - title.length());
  FileEntry fileEntry=DLAppTestUtil.addFileEntry(groupId,folderId,RandomTestUtil.randomString() + ".txt",title,approved);
  return (DLFileEntry)fileEntry.getModel();
}

{
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(group.getGroupId(),TestPropsValues.getUserId());
  FileEntry fileEntry=DLAppTestUtil.addFileEntryWithWorkflow(TestPropsValues.getUserId(),groupId,folderId,RandomTestUtil.randomString() + ".txt",getSearchKeywords(),approved,serviceContext);
  return (DLFileEntry)fileEntry.getModel();
}

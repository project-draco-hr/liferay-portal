{
  String tableNameFieldName=getTableNameFieldName();
  return "select c1." + tableNameFieldName + " from "+ "information_schema.columns c1 where c1.table_schema='"+ schema+ "' and c1.column_name='companyId' group by "+ "c1."+ tableNameFieldName+ " order by c1."+ tableNameFieldName;
}

{
  AssetEntry entry=null;
  if (_assetEntryId > 0 || assetLink.getEntryId1() == _assetEntryId) {
    entry=AssetEntryLocalServiceUtil.getEntry(assetLink.getEntryId2());
  }
 else {
    entry=AssetEntryLocalServiceUtil.getEntry(assetLink.getEntryId1());
  }
  return entry;
}

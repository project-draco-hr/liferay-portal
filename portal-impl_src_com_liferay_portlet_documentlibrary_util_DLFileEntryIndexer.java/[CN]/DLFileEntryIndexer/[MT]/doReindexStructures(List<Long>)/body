{
  List<DLFileEntry> dlFileEntries=DLFileEntryLocalServiceUtil.getFileEntriesByStructureIds(ArrayUtil.toLongArray(structureIds));
  for (  DLFileEntry dlFileEntry : dlFileEntries) {
    doReindex(dlFileEntry);
  }
}

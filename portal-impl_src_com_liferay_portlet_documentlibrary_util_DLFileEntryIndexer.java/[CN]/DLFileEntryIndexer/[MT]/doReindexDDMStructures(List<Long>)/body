{
  List<DLFileEntry> dlFileEntries=DLFileEntryLocalServiceUtil.getFileEntriesByStructureIds(ArrayUtil.toLongArray(ddmStructureIds));
  for (  DLFileEntry dlFileEntry : dlFileEntries) {
    doReindex(dlFileEntry);
  }
}

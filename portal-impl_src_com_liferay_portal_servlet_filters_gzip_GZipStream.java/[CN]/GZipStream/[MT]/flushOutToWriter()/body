{
  WriterOutputStream writerOutputStream=new WriterOutputStream(_response.getWriter());
  writerOutputStream.write(_unsyncByteArrayOutputStream.unsafeGetByteArray(),0,_unsyncByteArrayOutputStream.size());
  writerOutputStream.flush();
  writerOutputStream.close();
}

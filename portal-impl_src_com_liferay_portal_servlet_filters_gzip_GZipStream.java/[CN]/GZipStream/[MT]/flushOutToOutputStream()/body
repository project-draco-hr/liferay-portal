{
  OutputStream outputStream=_response.getOutputStream();
  outputStream.write(_unsyncByteArrayOutputStream.unsafeGetByteArray(),0,_unsyncByteArrayOutputStream.size());
  outputStream.flush();
  outputStream.close();
}

{
  ServiceRegistration<PermissionUpdateHandler> serviceRegistration=_serviceRegistrations.remove(permissionUpdateHandler);
  if (serviceRegistration != null) {
    serviceRegistration.unregister();
  }
}

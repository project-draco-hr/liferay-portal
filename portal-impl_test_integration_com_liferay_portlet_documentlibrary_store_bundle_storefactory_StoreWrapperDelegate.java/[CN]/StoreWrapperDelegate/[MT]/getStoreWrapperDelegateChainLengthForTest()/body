{
  if (!(_store instanceof StoreWrapperDelegate)) {
    return 1;
  }
  StoreWrapperDelegate storeWrapperDelegate=(StoreWrapperDelegate)_store;
  int innerStoreWrapperChainLength=storeWrapperDelegate.getStoreWrapperDelegateChainLengthForTest();
  return 1 + innerStoreWrapperChainLength;
}

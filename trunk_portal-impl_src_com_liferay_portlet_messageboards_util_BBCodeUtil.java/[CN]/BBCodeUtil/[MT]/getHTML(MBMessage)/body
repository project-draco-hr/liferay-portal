{
  String body=message.getBody();
  try {
    body=getHTML(body);
  }
 catch (  Exception e) {
    _log.error("Could not parse message " + message.getMessageId() + " "+ e.getMessage());
  }
  return body;
}

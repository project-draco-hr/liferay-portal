{
  List<AnnouncementsDelivery> deliveries=new ArrayList<AnnouncementsDelivery>(AnnouncementsEntryImpl.TYPES.length);
  for (  String type : AnnouncementsEntryImpl.TYPES) {
    deliveries.add(getUserDelivery(userId,type));
  }
  return deliveries;
}

{
  long folderId=DLFolderConstants.DEFAULT_PARENT_FOLDER_ID;
  if (!rootFolder) {
    folderId=parentFolder.getFolderId();
  }
  return DLAppTestUtil.addFileEntry(group.getGroupId(),folderId,"Title.txt");
}

{
  FileEntry fileEntry=addFileEntry(group.getGroupId(),parentFolder.getFolderId());
  boolean hasDiscussion=CommentManagerUtil.hasDiscussion(DLFileEntryConstants.getClassName(),fileEntry.getFileEntryId());
  Assert.assertEquals(PropsValues.DL_FILE_ENTRY_COMMENTS_ENABLED,hasDiscussion);
}

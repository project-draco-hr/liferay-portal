{
  try (PrefsPropsReplacement prefsPropsReplacement=new PrefsPropsReplacement(PropsKeys.DL_FILE_MAX_SIZE,1L)){
    String fileName=RandomTestUtil.randomString();
    ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(group.getGroupId());
    byte[] bytes=RandomTestUtil.randomBytes();
    DLAppServiceUtil.addFileEntry(group.getGroupId(),parentFolder.getFolderId(),fileName,ContentTypes.TEXT_PLAIN,fileName,StringPool.BLANK,StringPool.BLANK,bytes,serviceContext);
  }
 }

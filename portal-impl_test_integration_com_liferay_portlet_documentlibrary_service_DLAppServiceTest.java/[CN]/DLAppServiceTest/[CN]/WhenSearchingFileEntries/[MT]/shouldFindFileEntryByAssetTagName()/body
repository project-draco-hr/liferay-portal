{
  String fileName=RandomTestUtil.randomString() + ".txt";
  ServiceContext serviceContext=ServiceContextTestUtil.getServiceContext(group.getGroupId());
  String[] assetTagNames=new String[]{"hello","world"};
  serviceContext.setAssetTagNames(assetTagNames);
  FileEntry fileEntry=DLAppServiceUtil.addFileEntry(group.getGroupId(),parentFolder.getFolderId(),fileName,ContentTypes.TEXT_PLAIN,fileName,StringPool.BLANK,StringPool.BLANK,CONTENT.getBytes(),serviceContext);
  search(fileEntry,false,"hello",true);
  search(fileEntry,false,"world",true);
  search(fileEntry,false,"liferay",false);
}

{
  long fileEntryMetadataCount=DLFileEntryMetadataLocalServiceUtil.getFileEntryMetadataCount(fileEntry.getFileEntryId(),fileEntry.getLatestFileVersion().getFileVersionId());
  if (fileEntryMetadataCount == 0) {
    _instance.trigger(fileEntry);
  }
}

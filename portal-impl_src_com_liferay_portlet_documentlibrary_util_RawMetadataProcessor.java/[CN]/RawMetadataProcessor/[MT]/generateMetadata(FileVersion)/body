{
  long fileEntryMetadataCount=DLFileEntryMetadataLocalServiceUtil.getFileEntryMetadataCount(fileVersion.getFileEntryId(),fileVersion.getFileVersionId());
  if (fileEntryMetadataCount == 0) {
    _instance.trigger(fileVersion);
  }
}

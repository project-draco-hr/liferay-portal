{
  if (!PropsValues.SCHEDULER_ENABLED) {
    return;
  }
  try {
    _scheduler.start();
    clearJobs();
  }
 catch (  Exception e) {
    throw new SchedulerException("Unable to start scheduler",e);
  }
}

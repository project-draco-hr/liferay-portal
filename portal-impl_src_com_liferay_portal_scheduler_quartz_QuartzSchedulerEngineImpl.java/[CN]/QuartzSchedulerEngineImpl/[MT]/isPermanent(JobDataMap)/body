{
  String destinationName=jobDataMap.getString(DESTINATION_NAME);
  Message message=(Message)jobDataMap.get(MESSAGE);
  if (destinationName.equals(DestinationNames.SCHEDULER_DISPATCH)) {
    return false;
  }
 else   if (destinationName.equals(DestinationNames.SCHEDULER_SCRIPTING) && !message.getBoolean(PERMANENT_FLAG)) {
    return false;
  }
 else {
    return true;
  }
}

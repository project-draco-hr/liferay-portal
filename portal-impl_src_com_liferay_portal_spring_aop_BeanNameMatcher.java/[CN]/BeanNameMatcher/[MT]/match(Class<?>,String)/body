{
  if (_beanNamePattern == null) {
    throw new IllegalStateException("Bean name pattern must be set");
  }
  return StringUtil.wildcardMatches(beanName,_beanNamePattern,CharPool.QUESTION,CharPool.STAR,CharPool.PERCENT,true);
}

{
  if ((obj == null) || (!(obj instanceof Version))) {
    return false;
  }
  Version version=(Version)obj;
  if (version.toString().equals(UNKNOWN) || (toString().equals(UNKNOWN))) {
    return false;
  }
  return toString().equals(version.toString());
}

{
  if (equals(version)) {
    return true;
  }
  if (getMajor().equals(version.getMajor())) {
    if (getMinor().equals("*")) {
      return true;
    }
    if (getMinor().equals(version.getMinor())) {
      if (getBugFix().equals("*")) {
        return true;
      }
      if (getBugFix().equals(version.getBugFix())) {
        if ((getBuildNumber().equals("*")) || getBuildNumber().equals(version.getBuildNumber())) {
          return true;
        }
      }
    }
  }
  return false;
}

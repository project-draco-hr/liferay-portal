{
  for (  SchedulerEntry schedulerEntry : schedulerEntries) {
    MessageListener messageListener=(MessageListener)InstanceFactory.newInstance(PortalClassLoaderUtil.getClassLoader(),schedulerEntry.getEventListenerClass());
    messageListeners.put(schedulerEntry.getEventListenerClass(),messageListener);
  }
}

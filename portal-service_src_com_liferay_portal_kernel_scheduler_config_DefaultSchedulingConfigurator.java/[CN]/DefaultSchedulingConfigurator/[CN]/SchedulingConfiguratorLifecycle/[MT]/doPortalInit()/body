{
  for (  SchedulerEntry schedulerEntry : _schedulerEntries) {
    try {
      SchedulerEngineHelperUtil.schedule(schedulerEntry,_storageType,null,_exceptionsMaxSize);
    }
 catch (    Exception e) {
      _log.error("Unable to schedule " + schedulerEntry,e);
    }
  }
}

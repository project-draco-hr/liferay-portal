{
  Session session=SessionManager.getSession(syncAccountId);
  SyncAccount syncAccount=SyncAccountService.fetchSyncAccount(syncAccountId);
  return session.executePost(syncAccount.getUrl() + "/api/jsonws" + urlPath,parameters);
}

{
  Dictionary<String,Object> properties=new Hashtable<>();
  properties.put(URLConstants.URL_HANDLER_PROTOCOL,new String[]{"lpkg"});
  return bundleContext.registerService(URLStreamHandlerService.class.getName(),new LiferayPackageURLStreamHandlerService(),properties);
}

{
  Closure<Void> closure=new Closure<Void>(null){
    @SuppressWarnings("unused") public void doCall(    AntBuilder antBuilder){
      _invokeAntMethodUnzip(antBuilder,source,destination,cutDirs,includes,excludes);
    }
  }
;
  project.ant(closure);
}

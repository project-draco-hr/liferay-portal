{
  PollsVote pollsVote=fetchByPrimaryKey(voteId);
  if (pollsVote == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + voteId);
    }
    throw new NoSuchVoteException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + voteId);
  }
  return pollsVote;
}

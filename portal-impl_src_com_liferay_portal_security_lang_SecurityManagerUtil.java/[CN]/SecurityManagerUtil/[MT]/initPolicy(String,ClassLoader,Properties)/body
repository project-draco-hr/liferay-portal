{
  if (isDefault() || isNone()) {
    return;
  }
  PACLPolicy paclPolicy=PACLPolicyManager.buildPACLPolicy(servletContextName,classLoader,properties);
  PACLPolicyManager.register(classLoader,paclPolicy);
}

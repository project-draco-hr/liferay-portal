{
  if (_portalSecurityManagerStrategy != PortalSecurityManagerStrategy.SMART) {
    return;
  }
  if (ServerDetector.isWebSphere()) {
    if (_originalSecurityManager == null) {
      System.setSecurityManager(null);
    }
  }
}

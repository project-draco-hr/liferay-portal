{
  StringBuilder sb=new StringBuilder();
  sb.append(_protocol);
  sb.append(_host);
  if (_port > 0) {
    sb.append(':');
    sb.append(_port);
  }
  sb.append(_databaseName);
  sb.append(_params);
  return sb.toString();
}

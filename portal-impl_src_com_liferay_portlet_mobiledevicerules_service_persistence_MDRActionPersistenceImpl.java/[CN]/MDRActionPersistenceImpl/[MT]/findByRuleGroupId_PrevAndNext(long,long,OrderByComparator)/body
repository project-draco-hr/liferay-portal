{
  MDRAction mdrAction=findByPrimaryKey(actionId);
  Session session=null;
  try {
    session=openSession();
    MDRAction[] array=new MDRActionImpl[3];
    array[0]=getByRuleGroupId_PrevAndNext(session,mdrAction,ruleGroupId,orderByComparator,true);
    array[1]=mdrAction;
    array[2]=getByRuleGroupId_PrevAndNext(session,mdrAction,ruleGroupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  MDRAction mdrAction=fetchByPrimaryKey(actionId);
  if (mdrAction == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + actionId);
    }
    throw new NoSuchActionException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + actionId);
  }
  return mdrAction;
}

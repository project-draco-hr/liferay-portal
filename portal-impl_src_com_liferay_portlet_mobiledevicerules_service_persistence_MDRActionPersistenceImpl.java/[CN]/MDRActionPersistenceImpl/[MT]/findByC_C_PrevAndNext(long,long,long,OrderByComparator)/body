{
  MDRAction mdrAction=findByPrimaryKey(actionId);
  Session session=null;
  try {
    session=openSession();
    MDRAction[] array=new MDRActionImpl[3];
    array[0]=getByC_C_PrevAndNext(session,mdrAction,classNameId,classPK,orderByComparator,true);
    array[1]=mdrAction;
    array[2]=getByC_C_PrevAndNext(session,mdrAction,classNameId,classPK,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

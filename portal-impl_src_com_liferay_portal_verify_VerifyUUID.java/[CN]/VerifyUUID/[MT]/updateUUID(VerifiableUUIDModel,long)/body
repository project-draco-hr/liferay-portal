{
  String uuid=PortalUUIDUtil.generate();
  DB db=DBFactoryUtil.getDB();
  db.runSQL("update " + verifiableUUIDModel.getTableName() + " set uuid_ = '"+ uuid+ "' where "+ verifiableUUIDModel.getPrimaryKeyColumnName()+ " = "+ pk);
}

{
  SoapExtenderConfiguration soapExtenderConfiguration=getSoapExtenderConfiguration();
  String[] handlersFilters=soapExtenderConfiguration.handlers();
  if (handlersFilters != null) {
    for (    String handlerFilter : handlersFilters) {
      addTCCLDependency(false,Handler.class,handlerFilter,"addHandler","removeHandler");
    }
  }
}

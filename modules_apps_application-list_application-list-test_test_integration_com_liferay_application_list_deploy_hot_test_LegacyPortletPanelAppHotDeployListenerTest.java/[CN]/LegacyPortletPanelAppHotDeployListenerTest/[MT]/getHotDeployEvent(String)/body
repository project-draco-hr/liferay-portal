{
  Class<?> clazz=getClass();
  ClassLoader classLoader=clazz.getClassLoader();
  ResourceLoader resourceLoader=new ConstantResourceLoader(liferayPortletDescriptor,classLoader);
  ServletContext servletContext=new RandomlyNamedMockServletContext("/",resourceLoader);
  return new HotDeployEvent(servletContext,classLoader);
}

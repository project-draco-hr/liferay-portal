{
  int serviceCount=_hotDeployListener.getServiceRegistrationsSize();
  _hotDeployListener.invokeDeploy(getHotDeployEvent(_DESCRIPTOR_WITH_CONTROL_PANEL_ENTRY));
  Assert.assertEquals(serviceCount + 1,_hotDeployListener.getServiceRegistrationsSize());
}

{
  try {
    int returnValue=TagsEntryServiceUtil.searchCount(companyId,name,properties);
    return returnValue;
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

{
  ShoppingOrderItem shoppingOrderItem=_instance._get(shoppingOrderItemPK);
  _log.info("Get " + shoppingOrderItemPK + " is "+ ((shoppingOrderItem == null) ? "NOT " : "")+ "in cache");
  return shoppingOrderItem;
}

{
  if (_servlets.isEmpty()) {
    PortalUtil.sendError(HttpServletResponse.SC_SERVICE_UNAVAILABLE,new ServletException("Module framework is unavailable"),request,response);
    return;
  }
  HttpServlet httpServlet=_servlets.get(0);
  httpServlet.service(request,response);
}

{
  ClassLoader classLoader=ClassLoaderPool.getClassLoader(_servletContextName);
  if (classLoader == null) {
    Thread currentThread=Thread.currentThread();
    classLoader=currentThread.getContextClassLoader();
  }
  return classLoader;
}

{
  super(persistenceImpl.getDataSource(),"INSERT INTO Groups_UserGroups (groupId, userGroupId) VALUES (?, ?)");
  _persistenceImpl=persistenceImpl;
  declareParameter(new SqlParameter(Types.BIGINT));
  declareParameter(new SqlParameter(Types.BIGINT));
  compile();
}

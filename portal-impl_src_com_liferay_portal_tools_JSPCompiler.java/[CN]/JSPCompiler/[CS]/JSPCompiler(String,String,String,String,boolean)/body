{
  _compiler=compiler;
  if (!_compiler.equals("jikes")) {
    _compiler="javac";
  }
  _classPath=StringUtil.replace(classPath,";",System.getProperty("path.separator"));
  _directory=directory;
  _checkTimeStamp=checkTimeStamp;
  _compile(new File(directory));
}

{
  if (_log.isDebugEnabled()) {
    _log.debug("Search query " + query.toString());
  }
  SearchEngine searchEngine=_searchEngines.get(searchEngineId);
  return searchEngine.getSearcher().search(companyId,query,new Sort[]{sort},start,end);
}

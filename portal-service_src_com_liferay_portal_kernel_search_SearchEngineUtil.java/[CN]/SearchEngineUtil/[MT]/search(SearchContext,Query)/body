{
  if (_log.isDebugEnabled()) {
    _log.debug("Search query " + query.toString());
  }
  SearchEngine searchEngine=_searchEngines.get(searchContext.getSearchEngineId());
  return searchEngine.getSearcher().search(searchContext,query);
}

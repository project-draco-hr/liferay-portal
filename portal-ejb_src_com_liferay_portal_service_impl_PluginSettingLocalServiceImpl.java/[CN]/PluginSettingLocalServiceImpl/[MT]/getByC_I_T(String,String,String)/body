{
  List pluginSettings=PluginSettingUtil.findByC_I_T(companyId,pluginId,pluginType);
  if (pluginSettings.size() < 1) {
    return null;
  }
 else {
    return (PluginSetting)pluginSettings.get(0);
  }
}

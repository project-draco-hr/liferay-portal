{
  int count=countByG_A_V(groupId,articleId,version);
  if (count == 0) {
    return null;
  }
  List<JournalArticleImage> list=findByG_A_V(groupId,articleId,version,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

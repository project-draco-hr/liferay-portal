{
  long companyId=0;
  Map userInfo=(Map)portletRequest.getAttribute(RenderRequest.USER_INFO);
  if (userInfo != null) {
    companyId=GetterUtil.getLong((String)userInfo.get("liferay.company.id"));
  }
  return companyId;
}

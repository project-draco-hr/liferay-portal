{
  String successURL=settingsProperties.getProperty("successURL");
  if (Validator.isNotNull(successURL) && !Validator.isUrl(successURL)) {
    throw new RecordSetSettingsException("The property \"successURL\" is not a URL");
  }
}

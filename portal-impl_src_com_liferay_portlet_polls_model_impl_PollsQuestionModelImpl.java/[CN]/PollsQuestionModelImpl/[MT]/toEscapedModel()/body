{
  if (isEscapedModel()) {
    return (PollsQuestion)this;
  }
 else {
    return (PollsQuestion)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (PollsQuestion)this;
  }
 else {
    return (PollsQuestion)Proxy.newProxyInstance(PollsQuestion.class.getClassLoader(),new Class[]{PollsQuestion.class},new AutoEscapeBeanHandler(this));
  }
}

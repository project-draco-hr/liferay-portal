{
  boolean enabled=false;
  try {
    int pagesCount=WikiPageLocalServiceUtil.getPagesCount("classic_wiki");
    if (pagesCount > 0) {
      enabled=true;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return enabled;
}

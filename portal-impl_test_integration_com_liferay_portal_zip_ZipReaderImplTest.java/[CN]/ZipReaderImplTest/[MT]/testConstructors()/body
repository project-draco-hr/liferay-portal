{
  ZipReader zipReader=new ZipReaderImpl(_getDependencyAsInputStream(_ZIP_FILE_PATH));
  Assert.assertNotNull(zipReader);
  zipReader=new ZipReaderImpl(_getDependencyAsFile(_ZIP_FILE_PATH));
  Assert.assertNotNull(zipReader);
}

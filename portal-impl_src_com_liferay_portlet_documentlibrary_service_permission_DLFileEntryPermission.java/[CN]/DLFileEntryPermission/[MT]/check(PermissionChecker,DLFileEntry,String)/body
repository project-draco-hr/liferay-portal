{
  if (!contains(permissionChecker,dlFileEntry,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),DLFileEntry.class.getName(),dlFileEntry.getFileEntryId(),actionId);
  }
}

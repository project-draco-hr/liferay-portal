{
  if (!fileEntry.containsPermission(permissionChecker,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),FileEntry.class.getName(),fileEntry.getFileEntryId(),actionId);
  }
}

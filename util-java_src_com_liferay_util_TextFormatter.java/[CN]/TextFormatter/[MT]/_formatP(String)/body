{
  StringBuilder sb=new StringBuilder(s.toLowerCase());
  for (int i=0; i < s.length(); i++) {
    char c=s.charAt(i);
    if (Character.isUpperCase(c) && (i > 0) && ((i + 1) < s.length())) {
      if (Character.isUpperCase(c) && Character.isLowerCase(s.charAt(i + 1))) {
        sb.insert(i,CharPool.DASH);
      }
 else       if (Character.isLowerCase(s.charAt(i - 1)) && Character.isUpperCase(c)) {
        sb.insert(i,CharPool.DASH);
      }
    }
  }
  return sb.toString();
}

{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(licenseId));
  }
  com.liferay.portlet.softwarecatalog.model.SCLicense scLicense=getPersistence().remove(licenseId);
  if (listener != null) {
    listener.onAfterRemove(scLicense);
  }
  return scLicense;
}

{
  journalStructure=toUnwrappedModel(journalStructure);
  Session session=null;
  try {
    session=openSession();
    if (journalStructure.isCachedModel()) {
      journalStructure=(JournalStructure)session.get(JournalStructureImpl.class,journalStructure.getPrimaryKeyObj());
    }
    session.delete(journalStructure);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(journalStructure);
  return journalStructure;
}

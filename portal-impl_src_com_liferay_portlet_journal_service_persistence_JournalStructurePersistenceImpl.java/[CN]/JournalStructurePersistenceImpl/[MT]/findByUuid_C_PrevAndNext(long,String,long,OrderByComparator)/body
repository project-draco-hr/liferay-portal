{
  JournalStructure journalStructure=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalStructure[] array=new JournalStructureImpl[3];
    array[0]=getByUuid_C_PrevAndNext(session,journalStructure,uuid,companyId,orderByComparator,true);
    array[1]=journalStructure;
    array[2]=getByUuid_C_PrevAndNext(session,journalStructure,uuid,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

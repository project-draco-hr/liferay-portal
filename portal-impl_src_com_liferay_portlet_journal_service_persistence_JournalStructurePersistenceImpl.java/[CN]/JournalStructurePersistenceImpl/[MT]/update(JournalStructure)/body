{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalStructure journalStructure) method. To improve performance, call update(JournalStructure journalStructure, boolean merge).");
  }
  return update(journalStructure,false);
}

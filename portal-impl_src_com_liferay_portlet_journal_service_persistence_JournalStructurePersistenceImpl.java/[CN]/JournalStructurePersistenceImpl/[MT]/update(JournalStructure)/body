{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalStructure journalStructure) method. Use update(JournalStructure journalStructure, boolean merge) instead.");
  }
  return update(journalStructure,false);
}

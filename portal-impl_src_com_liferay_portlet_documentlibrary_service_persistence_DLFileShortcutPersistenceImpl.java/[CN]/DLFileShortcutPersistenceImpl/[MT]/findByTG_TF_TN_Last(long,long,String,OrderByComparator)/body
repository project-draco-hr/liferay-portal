{
  int count=countByTG_TF_TN(toGroupId,toFolderId,toName);
  List<DLFileShortcut> list=findByTG_TF_TN(toGroupId,toFolderId,toName,count - 1,count,orderByComparator);
  if (list.isEmpty()) {
    StringBundler msg=new StringBundler(8);
    msg.append(_NO_SUCH_ENTITY_WITH_KEY);
    msg.append("toGroupId=");
    msg.append(toGroupId);
    msg.append(", toFolderId=");
    msg.append(toFolderId);
    msg.append(", toName=");
    msg.append(toName);
    msg.append(StringPool.CLOSE_CURLY_BRACE);
    throw new NoSuchFileShortcutException(msg.toString());
  }
 else {
    return list.get(0);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(DLFileShortcut dlFileShortcut) method. To improve performance, call update(DLFileShortcut dlFileShortcut, boolean merge).");
  }
  return update(dlFileShortcut,false);
}

{
  _invalidClassNames=SetUtil.fromArray(PropsValues.JSON_SERVICE_INVALID_CLASS_NAMES);
  if (_log.isDebugEnabled()) {
    for (    String invalidClassName : _invalidClassNames) {
      _log.debug("Invalid class name " + invalidClassName);
    }
  }
}

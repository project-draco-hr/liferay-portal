{
  String className=ParamUtil.getString(request,"serviceClassName");
  if (className.contains(".service.http.") && className.endsWith("ServiceJSON")) {
    return true;
  }
 else {
    return false;
  }
}

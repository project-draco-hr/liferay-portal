{
  if (returnObj instanceof JSONSerializable) {
    return ((JSONSerializable)returnObj).toJSONString();
  }
  return JSONFactoryUtil.createJSONSerializer().exclude("*.class").serialize(returnObj);
}

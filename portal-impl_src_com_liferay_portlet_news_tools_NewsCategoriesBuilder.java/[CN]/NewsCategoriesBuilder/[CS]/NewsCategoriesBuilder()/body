{
  try {
    String newCategories=HttpUtil.URLtoString("http://w.moreover.com/categories/category_list.tsv2");
    newCategories=newCategories.substring(0,newCategories.length() - 1);
    File file=new File("../portal-impl/src/com/liferay/portlet/news/dependencies/" + "categories.tsv");
    FileUtil.write(file,newCategories,true);
  }
 catch (  Exception e) {
  }
}

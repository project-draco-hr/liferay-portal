{
  if (_closed) {
    throw new IOException("Stream closed");
  }
  bufferLock.lock();
  try {
    waitUntilNotFull();
    buffer[writeIndex]=(char)c;
    increaseWriteIndex(1);
  }
  finally {
    bufferLock.unlock();
  }
}

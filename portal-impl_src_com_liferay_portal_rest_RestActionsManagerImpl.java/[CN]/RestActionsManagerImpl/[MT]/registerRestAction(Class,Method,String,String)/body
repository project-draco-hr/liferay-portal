{
  RestActionConfig cfg=new RestActionConfig(actionClass,actionMethod,path,method);
  RestActionConfigSet set=new RestActionConfigSet(cfg.getActionPath());
  int ndx=_listBS.find(set);
  if (ndx < 0) {
    _list.add(set);
  }
 else {
    set=_list.get(ndx);
  }
  set.add(cfg);
}

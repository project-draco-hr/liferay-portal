{
  if (isEscapedModel()) {
    return (MBCategory)this;
  }
 else {
    return (MBCategory)Proxy.newProxyInstance(MBCategory.class.getClassLoader(),new Class[]{MBCategory.class},new AutoEscapeBeanHandler(this));
  }
}

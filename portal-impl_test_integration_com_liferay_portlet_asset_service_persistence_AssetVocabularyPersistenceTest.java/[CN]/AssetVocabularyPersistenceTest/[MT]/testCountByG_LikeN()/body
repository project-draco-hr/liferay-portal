{
  try {
    _persistence.countByG_LikeN(ServiceTestUtil.nextLong(),StringPool.BLANK);
    _persistence.countByG_LikeN(0L,StringPool.NULL);
    _persistence.countByG_LikeN(0L,(String)null);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

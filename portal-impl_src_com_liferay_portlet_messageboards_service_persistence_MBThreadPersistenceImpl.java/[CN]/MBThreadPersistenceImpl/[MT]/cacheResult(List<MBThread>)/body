{
  for (  MBThread mbThread : mbThreads) {
    if (EntityCacheUtil.getResult(MBThreadModelImpl.ENTITY_CACHE_ENABLED,MBThread.class,mbThread.getPrimaryKey(),this) == null) {
      cacheResult(mbThread);
    }
  }
}

{
  MBThread mbThread=(MBThread)EntityCacheUtil.getResult(MBThreadModelImpl.ENTITY_CACHE_ENABLED,MBThreadImpl.class,threadId,this);
  if (mbThread == _nullMBThread) {
    return null;
  }
  if (mbThread == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      mbThread=(MBThread)session.get(MBThreadImpl.class,Long.valueOf(threadId));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (!hasException && (mbThread == null)) {
        EntityCacheUtil.putResult(MBThreadModelImpl.ENTITY_CACHE_ENABLED,MBThreadImpl.class,threadId,_nullMBThread);
      }
 else {
        cacheResult(mbThread);
      }
      closeSession(session);
    }
  }
  return mbThread;
}

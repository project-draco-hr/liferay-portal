{
  MBThread mbThread=(MBThread)EntityCacheUtil.getResult(MBThreadModelImpl.ENTITY_CACHE_ENABLED,MBThreadImpl.class,threadId,this);
  if (mbThread == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (mbThread == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      mbThread=(MBThread)session.get(MBThreadImpl.class,Long.valueOf(threadId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (mbThread == null)) {
        EntityCacheUtil.putResult(MBThreadModelImpl.ENTITY_CACHE_ENABLED,MBThreadImpl.class,threadId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(mbThread);
      }
      closeSession(session);
    }
  }
  return mbThread;
}

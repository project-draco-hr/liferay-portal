{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MBThread mbThread) method. To improve performance, call update(MBThread mbThread, boolean merge).");
  }
  return update(mbThread,false);
}

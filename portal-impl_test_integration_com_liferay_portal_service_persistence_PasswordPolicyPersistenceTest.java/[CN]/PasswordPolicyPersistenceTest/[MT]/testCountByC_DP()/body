{
  try {
    _persistence.countByC_DP(ServiceTestUtil.nextLong(),ServiceTestUtil.randomBoolean());
    _persistence.countByC_DP(0L,ServiceTestUtil.randomBoolean());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

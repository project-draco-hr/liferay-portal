{
  long recordSetId=ParamUtil.getLong(actionRequest,"recordSetId");
  if (recordSetId > 0) {
    return new long[]{recordSetId};
  }
  return StringUtil.split(ParamUtil.getString(actionRequest,"recordSetIds"),0L);
}

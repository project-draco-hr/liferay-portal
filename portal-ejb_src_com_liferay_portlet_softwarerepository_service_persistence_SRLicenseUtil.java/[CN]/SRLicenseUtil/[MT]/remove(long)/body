{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(licenseId));
  }
  com.liferay.portlet.softwarerepository.model.SRLicense srLicense=getPersistence().remove(licenseId);
  if (listener != null) {
    listener.onAfterRemove(srLicense);
  }
  return srLicense;
}

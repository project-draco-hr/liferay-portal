{
  JournalArticle article=JournalArticleLocalServiceUtil.fetchLatestArticle(classPK);
  if (article == null) {
    article=JournalArticleLocalServiceUtil.getArticle(classPK);
  }
  return contains(permissionChecker,article,actionId);
}

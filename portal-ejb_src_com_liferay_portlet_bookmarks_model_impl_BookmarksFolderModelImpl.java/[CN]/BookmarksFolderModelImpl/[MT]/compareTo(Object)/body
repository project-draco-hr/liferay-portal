{
  if (obj == null) {
    return -1;
  }
  BookmarksFolderImpl bookmarksFolder=(BookmarksFolderImpl)obj;
  int value=0;
  value=getParentFolderId().compareTo(bookmarksFolder.getParentFolderId());
  if (value != 0) {
    return value;
  }
  value=getName().toLowerCase().compareTo(bookmarksFolder.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

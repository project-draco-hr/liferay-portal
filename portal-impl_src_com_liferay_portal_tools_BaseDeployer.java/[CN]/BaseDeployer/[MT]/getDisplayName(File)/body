{
  String displayName=srcFile.getName();
  if (displayName.toLowerCase().endsWith(".war")) {
    displayName=displayName.substring(0,displayName.length() - 4);
  }
  if (appServerType.startsWith("jboss") && Validator.isNotNull(jbossPrefix) && displayName.startsWith(jbossPrefix)) {
    displayName=displayName.substring(1,displayName.length());
  }
  return displayName;
}

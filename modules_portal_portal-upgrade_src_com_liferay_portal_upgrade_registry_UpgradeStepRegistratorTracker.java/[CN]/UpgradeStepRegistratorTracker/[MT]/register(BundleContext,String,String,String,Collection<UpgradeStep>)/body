{
  UpgradeStep[] upgradeStepsArray=new UpgradeStep[upgradeSteps.size()];
  return register(bundleContext,bundleSymbolicName,fromSchemaVersionString,toSchemaVersionString,upgradeSteps.toArray(upgradeStepsArray));
}

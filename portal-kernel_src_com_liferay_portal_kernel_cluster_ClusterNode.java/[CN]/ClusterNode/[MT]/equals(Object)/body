{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ClusterNode)) {
    return false;
  }
  ClusterNode clusterNode=(ClusterNode)obj;
  if (Validator.equals(_clusterNodeId,clusterNode._clusterNodeId) && Validator.equals(_bindInetAddress,clusterNode._bindInetAddress) && Validator.equals(_portalInetSocketAddress,clusterNode._portalInetSocketAddress)&& Validator.equals(_portalProtocol,clusterNode._portalProtocol)) {
    return true;
  }
  return false;
}

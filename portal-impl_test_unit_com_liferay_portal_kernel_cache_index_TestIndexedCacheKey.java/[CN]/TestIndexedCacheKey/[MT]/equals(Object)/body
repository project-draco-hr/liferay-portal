{
  TestIndexedCacheKey testIndexedCacheKey=(TestIndexedCacheKey)obj;
  if ((testIndexedCacheKey._indexedLong == _indexedLong) && (testIndexedCacheKey._nonIndexedLong == _nonIndexedLong)) {
    return true;
  }
  return false;
}

{
  TestIndexedCacheKey testIndexedCacheKey=(TestIndexedCacheKey)obj;
  if ((testIndexedCacheKey._indexedLong == _indexedLong) && (testIndexedCacheKey._unindexedLong == _unindexedLong)) {
    return true;
  }
  return false;
}

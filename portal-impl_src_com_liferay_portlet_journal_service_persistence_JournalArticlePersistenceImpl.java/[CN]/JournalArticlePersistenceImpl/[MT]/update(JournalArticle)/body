{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalArticle journalArticle) method. Use update(JournalArticle journalArticle, boolean merge) instead.");
  }
  return update(journalArticle,false);
}

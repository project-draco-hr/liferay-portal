{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JournalArticle journalArticle) method. To improve performance, call update(JournalArticle journalArticle, boolean merge).");
  }
  return update(journalArticle,false);
}

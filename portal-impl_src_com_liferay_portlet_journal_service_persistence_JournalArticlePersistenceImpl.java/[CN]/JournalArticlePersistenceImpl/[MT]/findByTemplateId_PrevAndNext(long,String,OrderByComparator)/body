{
  JournalArticle journalArticle=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalArticle[] array=new JournalArticleImpl[3];
    array[0]=getByTemplateId_PrevAndNext(session,journalArticle,templateId,orderByComparator,true);
    array[1]=journalArticle;
    array[2]=getByTemplateId_PrevAndNext(session,journalArticle,templateId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

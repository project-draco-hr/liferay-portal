{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByG_U_C_NotST_PrevAndNext(id,groupId,userId,classNameId,status,orderByComparator);
  }
  JournalArticle journalArticle=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalArticle[] array=new JournalArticleImpl[3];
    array[0]=filterGetByG_U_C_NotST_PrevAndNext(session,journalArticle,groupId,userId,classNameId,status,orderByComparator,true);
    array[1]=journalArticle;
    array[2]=filterGetByG_U_C_NotST_PrevAndNext(session,journalArticle,groupId,userId,classNameId,status,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

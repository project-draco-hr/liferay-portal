{
  for (  JournalArticle journalArticle : journalArticles) {
    if (EntityCacheUtil.getResult(JournalArticleModelImpl.ENTITY_CACHE_ENABLED,JournalArticle.class,journalArticle.getPrimaryKey(),this) == null) {
      cacheResult(journalArticle);
    }
  }
}

{
  int count=countByG_U_C_NotS(groupId,userId,classNameId,status);
  List<JournalArticle> list=findByG_U_C_NotS(groupId,userId,classNameId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  JournalArticle journalArticle=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalArticle[] array=new JournalArticleImpl[3];
    array[0]=getByStructureId_PrevAndNext(session,journalArticle,structureId,orderByComparator,true);
    array[1]=journalArticle;
    array[2]=getByStructureId_PrevAndNext(session,journalArticle,structureId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

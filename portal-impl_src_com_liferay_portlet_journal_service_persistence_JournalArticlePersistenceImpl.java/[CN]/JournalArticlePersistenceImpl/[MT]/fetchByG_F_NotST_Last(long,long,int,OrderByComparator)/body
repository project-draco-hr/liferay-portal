{
  int count=countByG_F_NotST(groupId,folderId,status);
  List<JournalArticle> list=findByG_F_NotST(groupId,folderId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

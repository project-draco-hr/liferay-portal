{
  int count=countByG_C_NotST(groupId,classNameId,status);
  if (count == 0) {
    return null;
  }
  List<JournalArticle> list=findByG_C_NotST(groupId,classNameId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

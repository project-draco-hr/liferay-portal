{
  Session session=SessionManager.getSession(_syncAccountId);
  SyncAccount syncAccount=SyncAccountService.fetchSyncAccount(getSyncAccountId());
  session.executeAsynchronousGet(syncAccount.getUrl() + urlPath,_handler);
}

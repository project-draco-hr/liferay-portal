{
  ShoppingOrder order1=(ShoppingOrder)obj1;
  ShoppingOrder order2=(ShoppingOrder)obj2;
  int value=order1.getCreateDate().compareTo(order2.getCreateDate());
  if (_asc) {
    return value;
  }
 else {
    return -value;
  }
}

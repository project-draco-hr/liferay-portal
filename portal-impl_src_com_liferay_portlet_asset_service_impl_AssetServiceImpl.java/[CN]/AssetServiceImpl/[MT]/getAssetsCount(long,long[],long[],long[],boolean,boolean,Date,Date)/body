{
  long[][] viewableTagIds=getViewableTagIds(tagIds,notTagIds);
  tagIds=viewableTagIds[0];
  notTagIds=viewableTagIds[1];
  return assetLocalService.getAssetsCount(groupId,classNameIds,tagIds,notTagIds,andOperator,excludeZeroViewCount,publishDate,expirationDate);
}

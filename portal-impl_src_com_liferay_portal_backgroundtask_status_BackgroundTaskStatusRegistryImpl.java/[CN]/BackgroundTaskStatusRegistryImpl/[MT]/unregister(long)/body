{
  Lock writeLock=_readWriteLock.writeLock();
  writeLock.lock();
  try {
    BackgroundTaskStatus backgroundTaskStatus=_backgroundTaskStatusMap.remove(backgroundTaskId);
    return backgroundTaskStatus;
  }
  finally {
    writeLock.unlock();
  }
}

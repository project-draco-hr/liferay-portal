{
  Integer type=DLFileShortcutModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DLFileShortcutModelImpl.TABLE_NAME);
  }
  return type;
}

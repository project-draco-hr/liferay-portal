{
  Class[] argTypes={GenericPortlet.class};
  Object[] args={portlet};
  Object provider=_getProvider(portlet);
  ServletContext ctx=(ServletContext)_invoke(provider,"getServletContext",argTypes,args);
  return ctx;
}

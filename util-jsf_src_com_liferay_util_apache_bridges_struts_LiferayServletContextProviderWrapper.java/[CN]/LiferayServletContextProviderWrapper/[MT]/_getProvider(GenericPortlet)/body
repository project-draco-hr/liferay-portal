{
  PortletContext portletCtx=portlet.getPortletContext();
  if (_provider == null) {
    _provider=(ServletContextProvider)portletCtx.getAttribute(STRUTS_BRIDGES_CONTEXT_PROVIDER);
  }
  return _provider;
}

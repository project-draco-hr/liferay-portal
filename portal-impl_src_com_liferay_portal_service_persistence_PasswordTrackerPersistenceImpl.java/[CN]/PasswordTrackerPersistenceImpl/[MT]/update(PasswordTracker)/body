{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PasswordTracker passwordTracker) method. Use update(PasswordTracker passwordTracker, boolean merge) instead.");
  }
  return update(passwordTracker,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PasswordTracker passwordTracker) method. To improve performance, call update(PasswordTracker passwordTracker, boolean merge).");
  }
  return update(passwordTracker,false);
}

{
  Session session=null;
  try {
    session=openSession();
    return (PasswordTracker)session.get(PasswordTrackerImpl.class,new Long(passwordTrackerId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  File[] files=new File[]{new File(_fileName + ".properties"),new File(_fileName + ".script"),new File(_fileName + ".log"),new File(_fileName + ".data"),new File(_fileName + ".backup")};
  for (  File file : files) {
    if (file.exists()) {
      file.delete();
    }
  }
}

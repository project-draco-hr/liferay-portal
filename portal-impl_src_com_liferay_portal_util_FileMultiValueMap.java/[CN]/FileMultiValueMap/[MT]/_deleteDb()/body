{
  File[] dbFiles=new File[]{new File(_dbFileName + ".properties"),new File(_dbFileName + ".script"),new File(_dbFileName + ".log"),new File(_dbFileName + ".data"),new File(_dbFileName + ".backup")};
  for (  File file : dbFiles) {
    if (file.exists()) {
      file.delete();
    }
  }
}

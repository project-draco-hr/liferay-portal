{
  Set<V> values2=getAll(key);
  if (values == null || values.size() == 0) {
    return values2;
  }
  if (values2 == null) {
    values=new HashSet<V>();
  }
  for (  V value : values) {
    if (!values2.contains(value)) {
      values2.add(value);
      put(key,value);
    }
  }
  return values2;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoValue expandoValue) method. To improve performance, call update(ExpandoValue expandoValue, boolean merge).");
  }
  return update(expandoValue,false);
}

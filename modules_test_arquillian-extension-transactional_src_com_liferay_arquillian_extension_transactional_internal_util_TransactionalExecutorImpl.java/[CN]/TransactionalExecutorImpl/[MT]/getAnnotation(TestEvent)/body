{
  Method testMethod=testEvent.getTestMethod();
  Transactional transactional=testMethod.getAnnotation(Transactional.class);
  if (transactional != null) {
    return transactional;
  }
  return getAnnotation((ClassEvent)testEvent);
}

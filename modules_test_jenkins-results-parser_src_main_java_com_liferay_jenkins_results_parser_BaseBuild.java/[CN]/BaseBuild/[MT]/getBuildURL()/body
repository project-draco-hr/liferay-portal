{
  if ((master == null) || (master.length() == 0)) {
    return null;
  }
  StringBuilder sb=new StringBuilder();
  sb.append(getJobURL());
  sb.append(buildNumber);
  sb.append("/");
  return sb.toString();
}

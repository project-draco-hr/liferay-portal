{
  if (tree == null) {
    tree="actions[parameters[*]],number,result,runs[number,url]";
  }
  return JenkinsResultsParserUtil.toJSONObject(JenkinsResultsParserUtil.getLocalURL(buildURL + "/api/json?pretty&tree=" + tree));
}

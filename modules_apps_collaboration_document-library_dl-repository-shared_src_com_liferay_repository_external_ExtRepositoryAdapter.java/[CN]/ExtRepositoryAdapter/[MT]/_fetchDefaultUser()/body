{
  try {
    return userLocalService.getDefaultUser(getCompanyId());
  }
 catch (  PortalException pe) {
    _log.error("Unable to get default user for company " + getCompanyId(),pe);
    return null;
  }
}

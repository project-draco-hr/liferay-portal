{
  Iterator<IGFolder> itr=igFolderPersistence.findByG_P(groupId,IGFolderImpl.DEFAULT_PARENT_FOLDER_ID).iterator();
  while (itr.hasNext()) {
    IGFolder folder=itr.next();
    deleteFolder(folder);
  }
}

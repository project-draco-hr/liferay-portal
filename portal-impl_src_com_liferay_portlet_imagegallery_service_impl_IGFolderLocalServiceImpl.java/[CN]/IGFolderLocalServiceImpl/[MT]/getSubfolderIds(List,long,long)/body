{
  Iterator itr=igFolderPersistence.findByG_P(groupId,folderId).iterator();
  while (itr.hasNext()) {
    IGFolder folder=(IGFolder)itr.next();
    folderIds.add(new Long(folder.getFolderId()));
    getSubfolderIds(folderIds,folder.getGroupId(),folder.getFolderId());
  }
}

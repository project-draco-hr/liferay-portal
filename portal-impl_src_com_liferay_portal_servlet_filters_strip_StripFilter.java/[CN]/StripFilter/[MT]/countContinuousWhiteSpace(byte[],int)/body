{
  int count=0;
  for (int i=offset; i < oldByteArray.length; i++) {
    char c=(char)oldByteArray[i];
    if ((c == CharPool.NEW_LINE) || (c == CharPool.RETURN) || (c == CharPool.TAB)|| Character.isWhitespace(c)) {
      count++;
    }
 else {
      return count;
    }
  }
  return count;
}

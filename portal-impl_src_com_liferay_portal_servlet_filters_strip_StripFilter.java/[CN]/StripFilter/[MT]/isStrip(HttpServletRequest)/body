{
  if (!ParamUtil.get(req,_STRIP,true)) {
    return false;
  }
 else {
    String lifecycle=ParamUtil.getString(req,"p_p_lifecycle");
    if (lifecycle.equals(PortletRequest.ACTION_PHASE) && LiferayWindowState.isExclusive(req)) {
      return false;
    }
 else {
      return true;
    }
  }
}

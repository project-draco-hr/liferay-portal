{
  if (!ParamUtil.getBoolean(req,_STRIP,true)) {
    return false;
  }
 else {
    String lifecycle=ParamUtil.getString(req,"p_p_lifecycle");
    if (lifecycle.equals("1") && LiferayWindowState.isExclusive(req)) {
      return false;
    }
 else {
      return true;
    }
  }
}

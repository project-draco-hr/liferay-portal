{
  BookmarksFolderPermission.check(getPermissionChecker(),serviceContext.getPlid(),parentFolderId,ActionKeys.ADD_FOLDER);
  return bookmarksFolderLocalService.addFolder(getUserId(),parentFolderId,name,description,serviceContext);
}

{
  if (isEscapedModel()) {
    return (AnnouncementsFlag)this;
  }
 else {
    AnnouncementsFlag model=new AnnouncementsFlagImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setFlagId(getFlagId());
    model.setUserId(getUserId());
    model.setCreateDate(getCreateDate());
    model.setEntryId(getEntryId());
    model.setValue(getValue());
    model=(AnnouncementsFlag)Proxy.newProxyInstance(AnnouncementsFlag.class.getClassLoader(),new Class[]{AnnouncementsFlag.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

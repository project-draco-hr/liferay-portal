{
  if (isEscapedModel()) {
    return (AnnouncementsFlag)this;
  }
 else {
    return (AnnouncementsFlag)Proxy.newProxyInstance(AnnouncementsFlag.class.getClassLoader(),new Class[]{AnnouncementsFlag.class},new AutoEscapeBeanHandler(this));
  }
}

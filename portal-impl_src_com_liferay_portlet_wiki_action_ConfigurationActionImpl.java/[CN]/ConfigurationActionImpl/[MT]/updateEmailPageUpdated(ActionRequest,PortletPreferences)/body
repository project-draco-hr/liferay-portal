{
  boolean emailPageUpdatedEnabled=ParamUtil.getBoolean(req,"emailPageUpdatedEnabled");
  String emailPageUpdatedSubjectPrefix=ParamUtil.getString(req,"emailPageUpdatedSubjectPrefix");
  String emailPageUpdatedBody=ParamUtil.getString(req,"emailPageUpdatedBody");
  String emailPageUpdatedSignature=ParamUtil.getString(req,"emailPageUpdatedSignature");
  if (Validator.isNull(emailPageUpdatedSubjectPrefix)) {
    SessionErrors.add(req,"emailPageUpdatedSubjectPrefix");
  }
 else   if (Validator.isNull(emailPageUpdatedBody)) {
    SessionErrors.add(req,"emailPageUpdatedBody");
  }
 else {
    prefs.setValue("email-page-updated-enabled",String.valueOf(emailPageUpdatedEnabled));
    prefs.setValue("email-page-updated-subject-prefix",emailPageUpdatedSubjectPrefix);
    prefs.setValue("email-page-updated-body",emailPageUpdatedBody);
    prefs.setValue("email-page-updated-signature",emailPageUpdatedSignature);
  }
}

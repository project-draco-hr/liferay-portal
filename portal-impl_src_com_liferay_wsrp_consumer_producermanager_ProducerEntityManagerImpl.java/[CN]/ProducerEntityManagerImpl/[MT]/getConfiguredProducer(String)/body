{
  try {
    WSRPConfiguredProducer configuredProducer=WSRPConfiguredProducerLocalServiceUtil.getConfiguredProducer(GetterUtil.getLong(configuredProducerId));
    return configuredProducer;
  }
 catch (  Exception e) {
    throw new WSRPConsumerException(e.getMessage());
  }
}

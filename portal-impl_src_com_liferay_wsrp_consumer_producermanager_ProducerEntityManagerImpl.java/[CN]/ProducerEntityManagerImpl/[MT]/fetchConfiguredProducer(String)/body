{
  try {
    return WSRPConfiguredProducerLocalServiceUtil.getWSRPConfiguredProducer(Long.parseLong(configuredProducerId));
  }
 catch (  Exception se) {
    throw new WSRPConsumerException(se.getMessage());
  }
}

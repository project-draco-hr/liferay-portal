{
  WSRPConfiguredProducer configuredProducer=getConfiguredProducer(producerEntityId);
  if (configuredProducer == null) {
    throw new WSRPConsumerException("No configured producer exists for producer entity id " + producerEntityId);
  }
  return populateConfiguredProducer(configuredProducer);
}

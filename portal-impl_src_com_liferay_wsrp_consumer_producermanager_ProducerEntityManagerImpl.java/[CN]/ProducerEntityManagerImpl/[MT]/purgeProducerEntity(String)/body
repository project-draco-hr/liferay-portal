{
  try {
    WSRPConfiguredProducerLocalServiceUtil.deleteWSRPConfiguredProducer(Long.parseLong(producerEntityId));
  }
 catch (  Exception se) {
    throw new WSRPConsumerException(se.getMessage());
  }
}

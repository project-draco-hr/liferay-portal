{
  String pk=_instance._getByC_C(className,classPK);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_C" + " " + className.toString() + " "+ classPK.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

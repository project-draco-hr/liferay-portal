{
  if (obj == null) {
    return false;
  }
  Lock lock=(Lock)obj;
  if (getClassName().equals(lock.getClassName()) && getPrimaryKey().equals(lock.getPrimaryKey())) {
    return true;
  }
 else {
    return false;
  }
}

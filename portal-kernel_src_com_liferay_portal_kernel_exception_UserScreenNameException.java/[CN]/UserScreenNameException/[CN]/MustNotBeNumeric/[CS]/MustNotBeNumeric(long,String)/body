{
  super(String.format("Screen name %s for user %s must not be numeric because " + "the portal property \"%s\" is disabled",screenName,userId,PropsKeys.USERS_SCREEN_NAME_ALLOW_NUMERIC));
  this.userId=userId;
  this.screenName=screenName;
}

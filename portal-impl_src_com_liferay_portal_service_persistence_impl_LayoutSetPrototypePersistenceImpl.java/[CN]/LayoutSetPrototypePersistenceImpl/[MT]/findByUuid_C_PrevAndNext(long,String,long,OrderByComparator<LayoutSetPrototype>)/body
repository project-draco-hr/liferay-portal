{
  LayoutSetPrototype layoutSetPrototype=findByPrimaryKey(layoutSetPrototypeId);
  Session session=null;
  try {
    session=openSession();
    LayoutSetPrototype[] array=new LayoutSetPrototypeImpl[3];
    array[0]=getByUuid_C_PrevAndNext(session,layoutSetPrototype,uuid,companyId,orderByComparator,true);
    array[1]=layoutSetPrototype;
    array[2]=getByUuid_C_PrevAndNext(session,layoutSetPrototype,uuid,companyId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

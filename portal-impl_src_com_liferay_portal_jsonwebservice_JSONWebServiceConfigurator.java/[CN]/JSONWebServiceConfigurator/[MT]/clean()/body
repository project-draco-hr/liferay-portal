{
  int count=JSONWebServiceActionsManagerUtil.unregisterJSONWebServiceActions(_servletContextName);
  _registeredActionsCount-=count;
  if (_log.isDebugEnabled()) {
    _log.debug("Removed " + count + " existing JSON Web Service actions that belonged to "+ _servletContextName);
  }
}

{
  int removedCount=JSONWebServiceActionsManagerUtil.unregisterJSONWebServiceActions(_portletServletContextName);
  _registeredActionsCount-=removedCount;
  if (_log.isDebugEnabled()) {
    _log.debug("Removed " + removedCount + " existing JSON Web Service actions for "+ _portletServletContextName);
  }
}

{
  for (int second=0; ; second++) {
    if (second >= 240) {
      fail("timeout");
    }
    try {
      if (selenium.isTextPresent("Welcome to Liferay Portal")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
}

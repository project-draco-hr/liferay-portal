{
  try {
    ServiceReference<?>[] serviceReferences=_bundleContext.getAllServiceReferences(VerifyProcess.class.getName(),"(&(objectClass=" + VerifyProcess.class.getName() + ")(verify.process.name=com.liferay.portal.ldap))");
    if (ArrayUtil.isEmpty(serviceReferences)) {
      throw new IllegalStateException("Could not locate VerifyProcess");
    }
    return (VerifyProcess)_bundleContext.getService(serviceReferences[0]);
  }
 catch (  InvalidSyntaxException e) {
    throw new IllegalStateException("Could not locate VerifyProcess");
  }
}

{
  List<ControlMenuEntry> controlMenuEntries=_serviceTrackerMap.getService(controlMenuCategory.getKey());
  if (controlMenuEntries == null) {
    return Collections.emptyList();
  }
  return controlMenuEntries;
}

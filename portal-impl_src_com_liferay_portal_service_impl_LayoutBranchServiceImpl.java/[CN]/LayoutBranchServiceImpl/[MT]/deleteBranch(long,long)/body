{
  if (!getPermissionChecker().hasPermission(groupId,LayoutBranch.class.getName(),branchId,ActionKeys.DELETE)) {
    throw new PrincipalException();
  }
  layoutBranchLocalService.deleteBranch(branchId);
}

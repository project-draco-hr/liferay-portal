{
  if (!getPermissionChecker().hasPermission(groupId,LayoutBranch.class.getName(),branchId,ActionKeys.UPDATE)) {
    throw new PrincipalException();
  }
  return layoutBranchLocalService.updateBranch(branchId,name,description,serviceContext);
}

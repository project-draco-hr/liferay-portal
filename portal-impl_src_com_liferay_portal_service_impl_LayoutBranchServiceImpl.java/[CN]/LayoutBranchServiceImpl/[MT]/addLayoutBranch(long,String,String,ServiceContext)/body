{
  LayoutBranchPermissionUtil.check(getPermissionChecker(),groupId,0,ActionKeys.ADD_LAYOUT_BRANCH);
  return layoutBranchLocalService.addLayoutBranch(getUserId(),groupId,name,description,serviceContext);
}

{
  Session session=null;
  try {
    session=openSession();
    return (PasswordTracker)session.get(PasswordTracker.class,passwordTrackerId);
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

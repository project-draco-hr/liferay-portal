{
  if (object == this) {
    return true;
  }
  if (!object.getClass().equals(getClass())) {
    return false;
  }
  DestinationConfiguration destinationConfiguration=(DestinationConfiguration)object;
  return destinationConfiguration.getDestinationName().equals(_destinationName);
}

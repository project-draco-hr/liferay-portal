{
  int count=countByG_S_D(groupId,status,displayDate);
  if (count == 0) {
    return null;
  }
  List<BlogsEntry> list=findByG_S_D(groupId,status,displayDate,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

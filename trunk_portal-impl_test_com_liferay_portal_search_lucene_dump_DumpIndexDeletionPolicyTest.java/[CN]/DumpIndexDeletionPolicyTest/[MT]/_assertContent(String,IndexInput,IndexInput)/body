{
  for (long i=0; i < sourceIndexInput.length(); i++) {
    if (sourceIndexInput.readByte() != targetIndexInput.readByte()) {
      fail(fileName + " has different source and target byte value at " + i);
    }
  }
  sourceIndexInput.close();
  targetIndexInput.close();
}

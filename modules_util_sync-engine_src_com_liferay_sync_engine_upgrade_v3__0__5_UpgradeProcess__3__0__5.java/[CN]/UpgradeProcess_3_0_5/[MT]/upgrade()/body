{
  SyncFilePersistence syncFilePersistence=SyncFileService.getSyncFilePersistence();
  syncFilePersistence.executeRaw("ALTER TABLE `SyncFile` ADD COLUMN userId LONG BEFORE userName;");
  syncFilePersistence.executeRaw("ALTER TABLE `SyncFile` ADD COLUMN userName VARCHAR(16777216) " + "BEFORE state;");
}

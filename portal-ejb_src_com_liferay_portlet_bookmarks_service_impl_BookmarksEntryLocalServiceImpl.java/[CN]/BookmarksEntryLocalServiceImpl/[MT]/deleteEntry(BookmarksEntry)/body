{
  TagsAssetLocalServiceUtil.deleteAsset(BookmarksEntry.class.getName(),String.valueOf(entry.getEntryId()));
  ResourceLocalServiceUtil.deleteResource(entry.getCompanyId(),BookmarksEntry.class.getName(),ResourceImpl.SCOPE_INDIVIDUAL,entry.getEntryId());
  BookmarksEntryUtil.remove(entry.getEntryId());
}

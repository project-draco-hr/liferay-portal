{
  ResourceLocalServiceUtil.deleteResource(entry.getCompanyId(),BookmarksEntry.class.getName(),Resource.TYPE_CLASS,Resource.SCOPE_INDIVIDUAL,entry.getPrimaryKey().toString());
  BookmarksEntryUtil.remove(entry.getEntryId());
}

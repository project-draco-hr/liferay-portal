{
  ResourceLocalServiceUtil.addModelResources(entry.getCompanyId(),folder.getGroupId(),entry.getUserId(),BookmarksEntry.class.getName(),String.valueOf(entry.getPrimaryKey()),communityPermissions,guestPermissions);
}

{
  ResourceLocalServiceUtil.addResources(entry.getCompanyId(),folder.getGroupId(),entry.getUserId(),BookmarksEntry.class.getName(),String.valueOf(entry.getPrimaryKey()),false,addCommunityPermissions,addGuestPermissions);
}

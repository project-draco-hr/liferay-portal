{
  Properties resourceLimits=PrefsPropsUtil.getProperties(PropsKeys.IMAGEMAGICK_RESOURCE_LIMIT,true);
  if (resourceLimits.isEmpty()) {
    resourceLimits=PropsUtil.getProperties(PropsKeys.IMAGEMAGICK_RESOURCE_LIMIT,true);
  }
  return resourceLimits;
}

{
  if (PropsValues.DL_FILE_ENTRY_PREVIEW_ENABLED) {
    if (!DLStoreUtil.hasFile(fileVersion.getCompanyId(),REPOSITORY_ID,getPreviewFilePath(fileVersion,1))) {
      return false;
    }
  }
  return hasThumbnails(fileVersion);
}

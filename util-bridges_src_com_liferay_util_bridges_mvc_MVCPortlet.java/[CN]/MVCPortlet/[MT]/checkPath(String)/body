{
  if (Validator.isNotNull(path) && (!path.startsWith(templatePath) || path.contains(StringPool.DOUBLE_PERIOD) || !PortalUtil.isValidResourceId(path))) {
    throw new PortletException("Path " + path + " is not accessible by this portlet");
  }
}

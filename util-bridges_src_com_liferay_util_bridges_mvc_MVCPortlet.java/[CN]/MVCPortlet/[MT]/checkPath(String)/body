{
  if (Validator.isNotNull(path) && (!path.startsWith(templatePath) || !Validator.isFilePath(path,false) || !PortalUtil.isValidResourceId(path))) {
    throw new PortletException("Path " + path + " is not accessible by this portlet");
  }
}

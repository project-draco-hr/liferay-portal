{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(wikiPagePK));
  }
  com.liferay.portlet.wiki.model.WikiPage wikiPage=getPersistence().remove(wikiPagePK);
  if (listener != null) {
    listener.onAfterRemove(wikiPage);
  }
  return wikiPage;
}

{
  long contentId=_counterLocalService.increment();
  OutputBlob outputBlob=new OutputBlob(new UnsyncByteArrayInputStream(bytes),bytes.length);
  Content content=new Content(contentId,companyId,portletId,groupId,repositoryId,path,Hook.DEFAULT_VERSION,outputBlob,bytes.length);
  _contentPersistence.update(content);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetTagProperty assetTagProperty) method. Use update(AssetTagProperty assetTagProperty, boolean merge) instead.");
  }
  return update(assetTagProperty,false);
}

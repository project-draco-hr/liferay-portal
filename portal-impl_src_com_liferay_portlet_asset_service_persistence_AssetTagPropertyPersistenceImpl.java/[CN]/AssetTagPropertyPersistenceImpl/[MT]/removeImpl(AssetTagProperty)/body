{
  assetTagProperty=toUnwrappedModel(assetTagProperty);
  Session session=null;
  try {
    session=openSession();
    if (assetTagProperty.isCachedModel()) {
      assetTagProperty=(AssetTagProperty)session.get(AssetTagPropertyImpl.class,assetTagProperty.getPrimaryKeyObj());
    }
    session.delete(assetTagProperty);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(assetTagProperty);
  return assetTagProperty;
}

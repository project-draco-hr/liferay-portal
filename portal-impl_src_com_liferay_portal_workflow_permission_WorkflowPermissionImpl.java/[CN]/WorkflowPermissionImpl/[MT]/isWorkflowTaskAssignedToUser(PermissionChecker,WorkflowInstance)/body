{
  List<WorkflowTask> workflowTasks=WorkflowTaskManagerUtil.getWorkflowTasksByWorkflowInstance(permissionChecker.getCompanyId(),0,workflowInstance.getWorkflowInstanceId(),Boolean.FALSE,0,100,null);
  for (  WorkflowTask workflowTask : workflowTasks) {
    if ((workflowTask.isAssignedToSingleUser()) && (workflowTask.getAssigneeUserId() == permissionChecker.getUserId())) {
      return true;
    }
  }
  return false;
}

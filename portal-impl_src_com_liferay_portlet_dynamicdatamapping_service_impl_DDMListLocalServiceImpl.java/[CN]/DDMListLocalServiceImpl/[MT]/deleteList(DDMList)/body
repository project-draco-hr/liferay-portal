{
  ddmListPersistence.remove(list);
  ddmListEntryLocalService.deleteListEntries(list.getListId());
  resourceLocalService.deleteResource(list.getCompanyId(),DDMList.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,list.getListId());
}

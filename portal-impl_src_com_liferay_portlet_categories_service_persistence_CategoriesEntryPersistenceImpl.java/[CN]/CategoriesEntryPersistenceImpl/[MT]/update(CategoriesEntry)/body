{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(CategoriesEntry categoriesEntry) method. Use update(CategoriesEntry categoriesEntry, boolean merge) instead.");
  }
  return update(categoriesEntry,false);
}

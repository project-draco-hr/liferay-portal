{
  String minimalSuffix=_getMinimalSuffix(minimal);
  File file=new File("../sql/create" + minimalSuffix + "/create"+ minimalSuffix+ "-firebird.sql");
  StringBuffer sb=new StringBuffer();
  sb.append("create database '" + _databaseName + ".gdb' page_size 8192 user 'sysdba' password 'masterkey';\n");
  sb.append("connect '" + _databaseName + ".gdb' user 'sysdba' password 'masterkey';\n");
  sb.append(_readSQL("../sql/portal" + minimalSuffix + "/portal"+ minimalSuffix+ "-firebird.sql",_FIREBIRD[0],";\n"));
  FileUtil.write(file,sb.toString());
}

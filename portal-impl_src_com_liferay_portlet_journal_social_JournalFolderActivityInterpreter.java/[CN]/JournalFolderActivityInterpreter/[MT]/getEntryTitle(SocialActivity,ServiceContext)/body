{
  JournalFolder folder=JournalFolderLocalServiceUtil.getFolder(activity.getClassPK());
  if (folder.isInTrash()) {
    return TrashUtil.getOriginalTitle(folder.getName());
  }
  return folder.getName();
}

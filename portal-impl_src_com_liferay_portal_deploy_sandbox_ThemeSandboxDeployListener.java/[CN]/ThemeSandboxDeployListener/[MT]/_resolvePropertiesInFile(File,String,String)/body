{
  String content=FileUtil.read(target);
  String portalVersion=ReleaseInfo.getVersion();
  content=StringUtil.replace(content,new String[]{"${theme-id}","${theme-name}","${portal-version}"},new String[]{themeId,themeName,portalVersion});
  FileUtil.write(target,content);
}

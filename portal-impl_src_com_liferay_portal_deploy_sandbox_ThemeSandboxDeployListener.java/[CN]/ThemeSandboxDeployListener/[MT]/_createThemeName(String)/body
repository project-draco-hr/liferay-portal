{
  String themeName=dirName.substring(0,dirName.length() - THEME_SUFFIX.length());
  themeName=StringUtil.replace(themeName,new String[]{StringPool.UNDERLINE,StringPool.MINUS},new String[]{StringPool.SPACE,StringPool.SPACE});
  return StringUtil.upperCaseFirstLetter(themeName);
}

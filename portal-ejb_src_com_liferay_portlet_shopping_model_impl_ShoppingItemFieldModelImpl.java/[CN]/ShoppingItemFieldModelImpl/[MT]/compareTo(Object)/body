{
  if (obj == null) {
    return -1;
  }
  ShoppingItemFieldImpl shoppingItemField=(ShoppingItemFieldImpl)obj;
  int value=0;
  value=getItemId().compareTo(shoppingItemField.getItemId());
  if (value != 0) {
    return value;
  }
  value=getName().toLowerCase().compareTo(shoppingItemField.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}

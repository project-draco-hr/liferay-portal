{
  List<Layout> layouts=LayoutLocalServiceUtil.getNoPermissionLayouts(role);
  for (  Layout layout : layouts) {
    verifyModel(role.getCompanyId(),Layout.class.getName(),layout.getPlid(),role,0);
  }
}

{
  Integer type=MeetupsEntryModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ MeetupsEntryModelImpl.TABLE_NAME);
  }
  return type;
}

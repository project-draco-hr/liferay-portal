{
  TestSuite testSuite=new TestSuite();
  testSuite.addTestSuite(AddUserTest.class);
  testSuite.addTestSuite(AddUserPassword2NullTest.class);
  testSuite.addTestSuite(TearDownUserTest.class);
  return testSuite;
}

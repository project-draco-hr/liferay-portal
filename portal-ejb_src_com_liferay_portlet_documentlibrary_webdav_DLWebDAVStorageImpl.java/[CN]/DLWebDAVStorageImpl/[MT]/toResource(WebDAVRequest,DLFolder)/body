{
  String href=getRootPath() + webDavReq.getPath() + StringPool.SLASH+ folder.getName();
  Resource resource=new BaseResourceImpl(href,true,folder.getCreateDate(),folder.getModifiedDate());
  resource.setModel(folder);
  return resource;
}

{
  String href=getRootPath() + webDavReq.getPath() + StringPool.SLASH+ folder.getFolderId();
  Resource resource=new BaseResourceImpl(href,folder.getName(),true,folder.getCreateDate(),folder.getModifiedDate());
  resource.setModel(folder);
  return resource;
}

{
  String body=getBody();
  if (translate) {
    try {
      body=BBCodeUtil.getHTML(body);
    }
 catch (    Exception e) {
      _log.error("Could not parse message " + getMessageId() + " "+ e.getMessage());
    }
  }
  return body;
}

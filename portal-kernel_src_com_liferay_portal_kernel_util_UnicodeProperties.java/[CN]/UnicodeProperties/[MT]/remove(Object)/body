{
  if (key == null) {
    return null;
  }
  String value=super.remove(key);
  if (value == null) {
    return null;
  }
  String keyString=(String)key;
  _length-=keyString.length() + value.length() + 2;
  return value;
}

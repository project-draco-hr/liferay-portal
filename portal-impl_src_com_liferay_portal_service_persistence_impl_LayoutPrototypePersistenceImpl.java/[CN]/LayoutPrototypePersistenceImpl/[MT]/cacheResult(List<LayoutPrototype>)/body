{
  for (  LayoutPrototype layoutPrototype : layoutPrototypes) {
    if (entityCache.getResult(LayoutPrototypeModelImpl.ENTITY_CACHE_ENABLED,LayoutPrototypeImpl.class,layoutPrototype.getPrimaryKey()) == null) {
      cacheResult(layoutPrototype);
    }
 else {
      layoutPrototype.resetOriginalValues();
    }
  }
}

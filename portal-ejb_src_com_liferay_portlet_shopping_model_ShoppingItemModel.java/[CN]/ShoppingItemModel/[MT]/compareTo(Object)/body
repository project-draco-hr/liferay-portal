{
  if (obj == null) {
    return -1;
  }
  ShoppingItem shoppingItem=(ShoppingItem)obj;
  int value=0;
  value=getItemId().compareTo(shoppingItem.getItemId());
  if (value != 0) {
    return value;
  }
  return 0;
}

{
  StoreFactory storeFactory=StoreFactory.getInstance();
  Store store=storeFactory.getStore("test");
  Assert.assertTrue(isStoreWrapperDelegate(store,FirstTestStoreWrapper.Delegate.class.getName()));
}

{
  StoreFactory storeFactory=StoreFactory.getInstance();
  Store store=storeFactory.getStore("test");
  Assert.assertTrue(_isAssignableFrom(store,FirstStoreWrapper.FirstDelegatorStore.class.getName()));
}

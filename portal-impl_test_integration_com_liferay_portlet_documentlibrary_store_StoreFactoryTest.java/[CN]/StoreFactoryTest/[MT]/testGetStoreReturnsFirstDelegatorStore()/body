{
  StoreFactory storeFactory=StoreFactory.getInstance();
  Store store=storeFactory.getStore("test");
  Assert.assertTrue(isAssignableFrom(store,FirstStoreWrapper.FirstDelegatorStore.class.getName()));
}

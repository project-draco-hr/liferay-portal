{
  try (PreparedStatement ps=connection.prepareStatement("update KaleoNotificationRecipient set recipientClassName = " + "'ADDRESS' where recipientClassName is null or " + "recipientClassName = ''")){
    ps.executeUpdate();
  }
 catch (  SQLException sqle) {
  }
}

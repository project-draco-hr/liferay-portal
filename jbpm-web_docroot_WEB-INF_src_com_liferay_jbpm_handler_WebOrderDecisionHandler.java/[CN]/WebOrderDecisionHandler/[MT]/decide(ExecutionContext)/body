{
  ContextInstance contextInstance=executionContext.getContextInstance();
  String decision=null;
  String buyerResponse=(String)contextInstance.getVariable("radio:perform-another-order:yes,no");
  if (buyerResponse.equals("yes")) {
    decision="another";
  }
 else {
    decision="finished";
  }
  return decision;
}

{
  DDMStructurePermission.check(getPermissionChecker(),groupId,structureKey,ActionKeys.UPDATE);
  return ddmStructureLocalService.updateStructure(groupId,structureKey,parentStructureId,nameMap,descriptionMap,xsd,serviceContext);
}

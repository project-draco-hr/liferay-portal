{
  DDMPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_STRUCTURE_ENTRY);
  return ddmStructureLocalService.addStructure(getUserId(),groupId,classNameId,structureKey,autoStructureKey,name,description,xsd,storageType,serviceContext);
}

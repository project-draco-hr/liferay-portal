{
  if (structureId == 0) {
    structureId=groupId;
  }
  SubscriptionPermissionUtil.check(getPermissionChecker(),DDMStructure.class.getName(),structureId);
  ddmStructureLocalService.subscribeStructure(groupId,userId,structureId);
}

{
  boolean defaultLDAPExportGroupEnabled=GetterUtil.get(PropsUtil.get(PropsKeys.LDAP_EXPORT_GROUP_ENABLED),false);
  if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.LDAP_EXPORT_GROUP_ENABLED,defaultLDAPExportGroupEnabled)) {
    return true;
  }
 else {
    return false;
  }
}

{
  boolean defaultLDAPImportEnabled=GetterUtil.get(PropsUtil.get(PropsKeys.LDAP_IMPORT_ENABLED),false);
  if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.LDAP_IMPORT_ENABLED,defaultLDAPImportEnabled)) {
    return true;
  }
 else {
    return false;
  }
}

{
  SCProductEntry productEntry1=(SCProductEntry)obj1;
  SCProductEntry productEntry2=(SCProductEntry)obj2;
  int value=StringUtil.toLowerCase(productEntry1.getName()).compareTo(StringUtil.toLowerCase(productEntry2.getName()));
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}

{
  if (getSelLayout() == null) {
    return GroupPermissionUtil.contains(_themeDisplay.getPermissionChecker(),getSelGroup(),ActionKeys.ADD_LAYOUT);
  }
 else {
    return LayoutPermissionUtil.contains(_themeDisplay.getPermissionChecker(),getSelLayout(),ActionKeys.ADD_LAYOUT);
  }
}

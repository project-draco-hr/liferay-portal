{
  Map<String,PortletURL> portletURLs=new HashMap<>();
  portletURLs.put("addLayoutURL",getAddLayoutURL(null,LayoutConstants.DEFAULT_PLID));
  portletURLs.put("editLayoutURL",getEditLayoutURL(null));
  return portletURLs;
}

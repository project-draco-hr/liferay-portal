{
  boolean failOnException=false;
  try {
    failOnException=PrefsPropsUtil.getBoolean(PropsKeys.MAIL_THROWS_EXCEPTION_ON_FAILURE);
  }
 catch (  SystemException se) {
    _log.error("Error getting preference: " + PropsKeys.MAIL_THROWS_EXCEPTION_ON_FAILURE);
  }
  return failOnException;
}

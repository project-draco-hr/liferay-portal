{
  boolean failOnException=false;
  try {
    failOnException=PrefsPropsUtil.getBoolean(PropsKeys.MAIL_THROW_EXCEPTION_WHEN_FAILS,false);
  }
 catch (  SystemException e) {
    _log.error("Error getting preference: " + PropsKeys.MAIL_THROW_EXCEPTION_WHEN_FAILS);
  }
  return failOnException;
}

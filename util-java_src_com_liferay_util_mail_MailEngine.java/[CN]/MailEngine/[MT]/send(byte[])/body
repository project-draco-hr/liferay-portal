{
  try {
    Session session=getSession();
    Message msg=new MimeMessage(session,new UnsyncByteArrayInputStream(msgByteArray));
    _send(session,msg,null);
  }
 catch (  Exception e) {
    throw new MailEngineException(e);
  }
}

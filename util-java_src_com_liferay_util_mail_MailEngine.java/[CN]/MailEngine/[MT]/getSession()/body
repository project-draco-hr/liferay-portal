{
  Session session=(Session)JNDIUtil.lookup(new InitialContext(),MAIL_SESSION,true);
  session.setDebug(_log.isDebugEnabled());
  if (_log.isDebugEnabled()) {
    session.getProperties().list(System.out);
  }
  return session;
}

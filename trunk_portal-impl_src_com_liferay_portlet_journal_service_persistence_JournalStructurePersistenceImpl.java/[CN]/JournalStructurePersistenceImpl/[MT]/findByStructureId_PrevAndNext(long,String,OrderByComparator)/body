{
  JournalStructure journalStructure=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalStructure[] array=new JournalStructureImpl[3];
    array[0]=getByStructureId_PrevAndNext(session,journalStructure,structureId,orderByComparator,true);
    array[1]=journalStructure;
    array[2]=getByStructureId_PrevAndNext(session,journalStructure,structureId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

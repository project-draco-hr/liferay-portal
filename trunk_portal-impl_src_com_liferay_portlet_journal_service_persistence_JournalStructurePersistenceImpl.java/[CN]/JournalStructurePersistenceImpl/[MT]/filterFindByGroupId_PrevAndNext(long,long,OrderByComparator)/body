{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByGroupId_PrevAndNext(id,groupId,orderByComparator);
  }
  JournalStructure journalStructure=findByPrimaryKey(id);
  Session session=null;
  try {
    session=openSession();
    JournalStructure[] array=new JournalStructureImpl[3];
    array[0]=filterGetByGroupId_PrevAndNext(session,journalStructure,groupId,orderByComparator,true);
    array[1]=journalStructure;
    array[2]=filterGetByGroupId_PrevAndNext(session,journalStructure,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

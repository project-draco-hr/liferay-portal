{
  super(String.format("Email name address %s does not validate with %s",emailAddress,ClassUtil.getClassName(emailAddressValidator)));
  _emailAddress=emailAddress;
  _emailAddressValidator=emailAddressValidator;
}

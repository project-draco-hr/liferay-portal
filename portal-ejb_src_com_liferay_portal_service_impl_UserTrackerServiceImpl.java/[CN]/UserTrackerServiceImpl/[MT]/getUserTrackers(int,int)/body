{
  User user=getUser();
  if (!hasAdministrator(user.getCompanyId())) {
    throw new PrincipalException();
  }
  return UserTrackerLocalServiceUtil.getUserTrackers(user.getCompanyId(),begin,end);
}

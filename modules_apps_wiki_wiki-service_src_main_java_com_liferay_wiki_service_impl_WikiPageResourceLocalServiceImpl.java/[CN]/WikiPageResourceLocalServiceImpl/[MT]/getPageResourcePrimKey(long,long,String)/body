{
  WikiPageResource pageResource=wikiPageResourcePersistence.fetchByN_T(nodeId,title);
  if (pageResource == null) {
    pageResource=addPageResource(groupId,nodeId,title);
  }
  return pageResource.getResourcePrimKey();
}

{
  long plid=getThemeDisplayPlid(false,false);
  Object[] defaultLayout=TestServicePreAction.INSTANCE.getDefaultSiteLayout(_user);
  Layout layout=(Layout)defaultLayout[0];
  Assert.assertEquals(layout.getPlid(),plid);
}

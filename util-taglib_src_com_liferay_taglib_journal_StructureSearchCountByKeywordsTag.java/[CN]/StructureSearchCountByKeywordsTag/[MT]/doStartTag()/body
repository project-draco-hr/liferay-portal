{
  int count;
  try {
    count=JournalStructureLocalServiceUtil.searchCount(_companyId,_groupId,_keywords);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,count);
  return SKIP_BODY;
}

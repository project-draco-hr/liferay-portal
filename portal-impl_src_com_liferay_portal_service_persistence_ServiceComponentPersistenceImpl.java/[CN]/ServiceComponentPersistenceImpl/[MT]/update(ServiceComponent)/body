{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ServiceComponent serviceComponent) method. To improve performance, call update(ServiceComponent serviceComponent, boolean merge).");
  }
  return update(serviceComponent,false);
}

{
  if ((_readSolrClient != null) && !(solrRequest instanceof IsUpdateRequest)) {
    return _readSolrClient.request(solrRequest,collection);
  }
 else {
    return _writeSolrClient.request(solrRequest,collection);
  }
}

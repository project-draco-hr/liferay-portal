{
  Phone phone=phonePersistence.findByPrimaryKey(phoneId);
  CommonPermissionUtil.checkPermission(getPermissionChecker(),phone.getClassNameId(),phone.getClassPK(),ActionKeys.VIEW);
  return phone;
}

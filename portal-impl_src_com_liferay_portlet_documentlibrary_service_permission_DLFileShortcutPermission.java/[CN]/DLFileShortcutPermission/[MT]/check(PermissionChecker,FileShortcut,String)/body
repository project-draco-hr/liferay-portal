{
  if (!contains(permissionChecker,fileShortcut,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker,FileShortcut.class.getName(),fileShortcut.getFileShortcutId(),actionId);
  }
}

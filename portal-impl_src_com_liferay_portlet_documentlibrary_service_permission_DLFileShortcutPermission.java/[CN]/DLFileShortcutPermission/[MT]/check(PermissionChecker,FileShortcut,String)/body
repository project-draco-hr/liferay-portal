{
  if (!contains(permissionChecker,fileShortcut,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),FileShortcut.class.getName(),fileShortcut.getFileShortcutId(),actionId);
  }
}

{
  Level level=loggingEvent.getLevel();
  if (level.equals(Level.ERROR) || level.equals(Level.FATAL)) {
    ConcurrentAssertUtil.caughtFailure("Test failed due to logged error: " + loggingEvent.getMessage());
  }
}

{
  if (!context.addPrimaryKey(IGPortletDataHandlerImpl.class,"deleteData")) {
    IGFolderLocalServiceUtil.deleteFolders(context.getScopeGroupId());
    IGImageLocalServiceUtil.deleteImages(context.getScopeGroupId(),IGFolderConstants.DEFAULT_PARENT_FOLDER_ID);
  }
  return null;
}

{
  StringBundler sb=new StringBundler(4);
  sb.append(portletDataContext.getSourcePortletPath(PortletKeys.IMAGE_GALLERY));
  sb.append("/folders/");
  sb.append(folderId);
  if (expandoAttributes) {
    sb.append("-expando");
  }
  sb.append(".xml");
  return sb.toString();
}

{
  List<Object> fields=new ArrayList<>();
  for (  String fieldName : fieldNames) {
    Map<String,Object> field=getField(ddmFormFieldValuesMap.get(fieldName),locale);
    fields.add(field);
  }
  return fields;
}

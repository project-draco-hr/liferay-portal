{
  int completedKBCommentsCount=0;
  try {
    completedKBCommentsCount=_kbSuggestionListDisplayContext.getCompletedKBCommentsCount();
  }
 catch (  PortalException pe) {
    _log.error("Unable to obtain completed knowledge base comments count " + "for group " + _kbSuggestionListDisplayContext.getGroupId());
  }
  return String.format("%s (%s)",LanguageUtil.get(_resourceBundle,"completed"),completedKBCommentsCount);
}

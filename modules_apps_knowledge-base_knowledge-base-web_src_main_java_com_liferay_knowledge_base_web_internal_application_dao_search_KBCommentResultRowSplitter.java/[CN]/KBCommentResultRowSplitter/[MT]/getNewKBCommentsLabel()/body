{
  int newKBCommentsCount=0;
  try {
    newKBCommentsCount=_kbSuggestionListDisplayContext.getNewKBCommentsCount();
  }
 catch (  PortalException pe) {
    _log.error("Unable to obtain new knowledge base comments count for " + "group " + _kbSuggestionListDisplayContext.getGroupId());
  }
  return String.format("%s (%s)",LanguageUtil.get(_resourceBundle,"new"),newKBCommentsCount);
}

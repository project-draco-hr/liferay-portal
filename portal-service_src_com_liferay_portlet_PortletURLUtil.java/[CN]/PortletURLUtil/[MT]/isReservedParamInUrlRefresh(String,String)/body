{
  if ((_PORTLET_URL_REFRESH_URL_RESERVED_PARAMETERS == null) || (_PORTLET_URL_REFRESH_URL_RESERVED_PARAMETERS.length == 0) || (_PORTLET_URL_REFRESH_URL_RESERVED_PARAMETERS == null)) {
    return false;
  }
 else {
    for (int i=0; i < _PORTLET_URL_REFRESH_URL_RESERVED_PARAMETERS.length; i++) {
      String reservedParam=namespace.concat(_PORTLET_URL_REFRESH_URL_RESERVED_PARAMETERS[i]);
      if (parameter.equals(reservedParam)) {
        return true;
      }
    }
    return false;
  }
}

{
  ServiceTestUtil.setUser(TestPropsValues.getUser());
  _bundleIds=ModuleFrameworkTestUtil.getBundleIds(StoreWrapper.class,"(store.type=" + _STORE_TYPE + ")");
  ModuleFrameworkTestUtil.stopBundles(_bundleIds);
}

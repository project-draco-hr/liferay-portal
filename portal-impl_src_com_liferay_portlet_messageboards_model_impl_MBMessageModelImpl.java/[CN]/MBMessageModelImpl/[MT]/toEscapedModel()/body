{
  if (isEscapedModel()) {
    return (MBMessage)this;
  }
 else {
    return (MBMessage)Proxy.newProxyInstance(MBMessage.class.getClassLoader(),new Class[]{MBMessage.class},new AutoEscapeBeanHandler(this));
  }
}

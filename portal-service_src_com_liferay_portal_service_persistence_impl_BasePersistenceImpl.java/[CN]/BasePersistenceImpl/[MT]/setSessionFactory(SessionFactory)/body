{
  _sessionFactory=sessionFactory;
  _dialect=_sessionFactory.getDialect();
  _db=DBFactoryUtil.getDB(_dialect);
  _databaseOrderByMaxColumns=GetterUtil.getInteger(PropsUtil.get(PropsKeys.DATABASE_ORDER_BY_MAX_COLUMNS,new Filter(_db.getType())));
}

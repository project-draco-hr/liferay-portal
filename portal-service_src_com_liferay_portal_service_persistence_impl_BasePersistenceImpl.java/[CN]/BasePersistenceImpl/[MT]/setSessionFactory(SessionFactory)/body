{
  _sessionFactory=sessionFactory;
  _dialect=_sessionFactory.getDialect();
  _db=DBFactoryUtil.getDB(_dialect);
  _databaseOrderByColumnsMaximum=GetterUtil.getInteger(PropsUtil.get(PropsKeys.DATABASE_ORDER_BY_COLUMNS_MAXIMUM,new Filter(_db.getType())));
}

{
  try {
    BeanReferenceRefreshUtil.refresh(applicationContext.getAutowireCapableBeanFactory());
  }
 catch (  Exception e) {
    _log.error("Unexpected error while refreshing " + applicationContext.getDisplayName() + " which may"+ "cause memory leaks on multiple redeployments");
  }
}

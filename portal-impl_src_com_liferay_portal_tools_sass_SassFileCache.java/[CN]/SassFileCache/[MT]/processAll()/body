{
  while (!_futures.isEmpty()) {
    Future<TimedSassFragment> future=_futures.poll();
    TimedSassFragment fragment=future.get();
    System.out.println("Parsed " + fragment.getFileName() + " in "+ fragment.getElapsedTime()+ " ms");
  }
  for (  SassFile file : _sassFileCache.values()) {
    file.writeCacheFiles();
  }
}

{
  FreeMarkerThemeUtil themeUtil=new FreeMarkerThemeUtilImpl(request.getServletContext(),request,response,contextObjects);
  contextObjects.put(themeName,themeUtil);
  contextObjects.put("theme",themeUtil);
}

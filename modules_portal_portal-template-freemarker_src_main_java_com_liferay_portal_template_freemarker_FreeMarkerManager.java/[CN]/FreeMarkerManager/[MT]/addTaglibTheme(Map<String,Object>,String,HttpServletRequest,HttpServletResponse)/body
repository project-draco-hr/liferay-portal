{
  FreeMarkerThemeHelper themeHelper=new FreeMarkerThemeHelperImpl(request.getServletContext(),request,response,contextObjects);
  contextObjects.put(themeName,themeHelper);
  contextObjects.put("theme",themeHelper);
}

{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(emailAddressId));
  }
  com.liferay.portal.model.EmailAddress emailAddress=getPersistence().remove(emailAddressId);
  if (listener != null) {
    listener.onAfterRemove(emailAddress);
  }
  return emailAddress;
}

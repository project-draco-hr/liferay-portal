{
  listener=new InvokerMessageListener(listener);
  Set<MessageListener> listeners=new HashSet<MessageListener>(ListUtil.fromArray(_listeners));
  listeners.add(listener);
  _listeners=listeners.toArray(new MessageListener[listeners.size()]);
  setListenersCount(listeners.size());
}

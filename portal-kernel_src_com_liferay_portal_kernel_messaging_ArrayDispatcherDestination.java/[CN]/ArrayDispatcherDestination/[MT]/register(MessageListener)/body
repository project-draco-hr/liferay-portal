{
  listener=new MessageListenerInvoker(listener);
  Set<MessageListener> listeners=new HashSet<MessageListener>(Arrays.asList(_listeners));
  listeners.add(listener);
  _listeners=listeners.toArray(new MessageListener[listeners.size()]);
}

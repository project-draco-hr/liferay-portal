{
  _mbeanServer.registerMBean(new MessageBusManager(_context.mock(MessageBus.class)),MessageBusManager.createObjectName());
  Assert.assertTrue(_mbeanServer.isRegistered(MessageBusManager.createObjectName()));
}

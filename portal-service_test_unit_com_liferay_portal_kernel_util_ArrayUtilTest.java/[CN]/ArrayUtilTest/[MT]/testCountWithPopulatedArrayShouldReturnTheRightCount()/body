{
  String[] array={"a","b","c"};
  PredicateFilter<Object> predicateFilter=new PredicateFilter<Object>(){
    @Override public boolean filter(    Object o){
      return o.toString().equals("b");
    }
  }
;
  Assert.assertEquals(1,ArrayUtil.count(array,predicateFilter));
}

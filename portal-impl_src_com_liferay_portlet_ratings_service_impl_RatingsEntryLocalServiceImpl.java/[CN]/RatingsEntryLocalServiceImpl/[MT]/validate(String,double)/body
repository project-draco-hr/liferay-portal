{
  if (className.equals(MBDiscussion.class.getName()) || className.equals(MBMessage.class.getName())) {
    if ((score >= -1) && (score <= 1)) {
      return;
    }
  }
 else   if ((score >= 0) || (score <= PropsValues.RATINGS_DEFAULT_NUMBER_OF_STARS)) {
    return;
  }
  throw new EntryScoreException();
}

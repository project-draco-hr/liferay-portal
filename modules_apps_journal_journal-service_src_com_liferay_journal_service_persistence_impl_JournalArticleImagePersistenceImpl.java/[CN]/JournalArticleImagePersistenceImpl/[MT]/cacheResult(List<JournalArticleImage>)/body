{
  for (  JournalArticleImage journalArticleImage : journalArticleImages) {
    if (entityCache.getResult(JournalArticleImageModelImpl.ENTITY_CACHE_ENABLED,JournalArticleImageImpl.class,journalArticleImage.getPrimaryKey()) == null) {
      cacheResult(journalArticleImage);
    }
 else {
      journalArticleImage.resetOriginalValues();
    }
  }
}

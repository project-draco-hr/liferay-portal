{
  int count=countByTempImage(tempImage);
  if (count == 0) {
    return null;
  }
  List<JournalArticleImage> list=findByTempImage(tempImage,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

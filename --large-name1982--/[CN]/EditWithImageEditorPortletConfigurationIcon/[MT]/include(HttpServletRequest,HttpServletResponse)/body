{
  try {
    request.setAttribute(ImageEditorIntegrationDLWebKeys.IMAGE_EDITOR_INTEGRATION_DL_FILE_VERSION,getFileVersion(request));
    return super.include(request,response);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}

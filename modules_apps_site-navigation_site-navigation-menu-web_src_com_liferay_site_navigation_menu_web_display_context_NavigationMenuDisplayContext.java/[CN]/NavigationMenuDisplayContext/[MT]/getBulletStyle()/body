{
  if (_bulletStyle != null) {
    return _bulletStyle;
  }
  _bulletStyle=ParamUtil.getString(_request,"bulletStyle",_navigationMenuPortletInstanceConfiguration.bulletStyle());
  if (Validator.isNull(_bulletStyle)) {
    ThemeDisplay themeDisplay=(ThemeDisplay)_request.getAttribute(WebKeys.THEME_DISPLAY);
    _bulletStyle=GetterUtil.getString(themeDisplay.getThemeSetting("bullet-style"),_navigationMenuWebConfiguration.defaultBulletStyle());
  }
  return _bulletStyle;
}

{
  for (  DownstreamJob downstreamJob : downstreamJobs) {
    downstreamJob.update();
  }
  if (getDownstreamJobs().size() == getDownstreamJobs("completed").size()) {
    setStatus("completed");
    return;
  }
  setStatus("running");
}

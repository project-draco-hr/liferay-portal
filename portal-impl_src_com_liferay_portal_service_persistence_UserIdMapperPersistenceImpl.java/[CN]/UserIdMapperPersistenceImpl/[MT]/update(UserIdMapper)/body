{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserIdMapper userIdMapper) method. Use update(UserIdMapper userIdMapper, boolean merge) instead.");
  }
  return update(userIdMapper,false);
}

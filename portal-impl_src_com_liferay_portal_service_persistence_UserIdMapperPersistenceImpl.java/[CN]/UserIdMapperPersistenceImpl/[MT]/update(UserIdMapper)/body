{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserIdMapper userIdMapper) method. To improve performance, call update(UserIdMapper userIdMapper, boolean merge).");
  }
  return update(userIdMapper,false);
}

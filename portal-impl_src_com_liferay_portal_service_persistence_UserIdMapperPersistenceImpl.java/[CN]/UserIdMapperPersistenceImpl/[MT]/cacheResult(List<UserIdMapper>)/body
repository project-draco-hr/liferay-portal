{
  for (  UserIdMapper userIdMapper : userIdMappers) {
    if (EntityCacheUtil.getResult(UserIdMapperModelImpl.ENTITY_CACHE_ENABLED,UserIdMapper.class,userIdMapper.getPrimaryKey(),this) == null) {
      cacheResult(userIdMapper);
    }
  }
}

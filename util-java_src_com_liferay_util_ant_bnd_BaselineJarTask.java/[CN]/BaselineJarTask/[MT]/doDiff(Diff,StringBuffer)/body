{
  String output=String.format("%s%-3s %-10s %s",sb,getShortDelta(diff.getDelta()),diff.getType().toString().toLowerCase(),diff.getName());
  _project.log(output,Project.MSG_WARN);
  if (_printWriter != null) {
    _printWriter.println(output);
  }
  sb.append("\t");
  for (  Diff curDiff : diff.getChildren()) {
    if (curDiff.getDelta() == Delta.UNCHANGED) {
      continue;
    }
    doDiff(curDiff,sb);
  }
  sb.deleteCharAt(sb.length() - 1);
}

{
  MBMessageFlag mbMessageFlag=findByPrimaryKey(messageFlagId);
  Session session=null;
  try {
    session=openSession();
    MBMessageFlag[] array=new MBMessageFlagImpl[3];
    array[0]=getByM_F_PrevAndNext(session,mbMessageFlag,messageId,flag,orderByComparator,true);
    array[1]=mbMessageFlag;
    array[2]=getByM_F_PrevAndNext(session,mbMessageFlag,messageId,flag,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

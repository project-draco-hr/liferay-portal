{
  try {
    InputStream in=Utility.class.getClassLoader().getResourceAsStream(fileName);
    Properties properties=new Properties();
    properties.load(in);
    return properties;
  }
 catch (  Exception e) {
    WSRPXHelper.throwX(ErrorCodes.PROPERTY_FILE_NOT_FOUND,e);
  }
  return null;
}

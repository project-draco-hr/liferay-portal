{
  try {
    runInNewClassLoader(new Callable<Boolean>(){
      public Boolean call() throws Exception {
        throw new Exception();
      }
    }
.getClass());
    fail();
  }
 catch (  Exception e) {
  }
}

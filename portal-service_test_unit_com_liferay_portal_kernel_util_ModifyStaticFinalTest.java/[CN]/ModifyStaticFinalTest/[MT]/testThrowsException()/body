{
  try {
    Callable<Boolean> callable=new Callable<Boolean>(){
      public Boolean call() throws Exception {
        throw new Exception();
      }
    }
;
    runInNewClassLoader((Class<? extends Callable<Boolean>>)callable.getClass());
    fail();
  }
 catch (  Exception e) {
  }
}

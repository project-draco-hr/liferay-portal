{
  TaskContainer taskContainer=project.getTasks();
  taskContainer.withType(PoshiRunnerTask.class,new Action<PoshiRunnerTask>(){
    @Override public void execute(    PoshiRunnerTask poshiRunnerTask){
      configurePoshiRunnerTask(poshiRunnerTask,poshiRunnerExtension);
    }
  }
);
}

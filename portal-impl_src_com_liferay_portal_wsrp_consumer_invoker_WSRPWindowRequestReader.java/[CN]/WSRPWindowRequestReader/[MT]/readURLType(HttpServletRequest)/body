{
  String urlType=request.getParameter(WSRPWindowChannelURL.PORTLET_ACTION);
  if (urlType != null) {
    return WSRPToContainerMap.mapURLTypeToContainer(urlType);
  }
 else {
    throw new RuntimeException("Bad WSRP URL");
  }
}

{
  String newWindowState=request.getParameter(WSRPWindowChannelURL.NEW_WINDOW_STATE);
  if (Validator.isNotNull(newWindowState)) {
    return WSRPToContainerMap.mapWindowStateToContainer(newWindowState);
  }
 else {
    return null;
  }
}

{
  Reader reader=null;
  try {
    reader=templateResource.getReader();
    _velocityEngine.evaluate(_velocityContext,writer,templateResource.getTemplateId(),reader);
  }
  finally {
    if (reader != null) {
      reader.close();
    }
  }
}

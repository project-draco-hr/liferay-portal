{
  int height=jsonObject.getInt("height");
  int width=jsonObject.getInt("width");
  int x=jsonObject.getInt("x");
  int y=jsonObject.getInt("y");
  if ((renderedImage.getHeight() == height) && (renderedImage.getWidth() == width) && (x == 0)&& (y == 0)) {
    return false;
  }
  return true;
}

{
  for (  Map.Entry<Locale,String> friendlyURLEntry : friendlyURLMap.entrySet()) {
    String friendlyURL=friendlyURLEntry.getValue();
    validateFriendlyURL(groupId,privateLayout,layoutId,friendlyURL);
  }
}

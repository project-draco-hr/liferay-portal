{
  Group group=layout.getGroup();
  if (group.isGuest() && !_hasGuestViewPermission(layout)) {
    LayoutTypeException lte=new LayoutTypeException(LayoutTypeException.FIRST_LAYOUT_PERMISSION);
    throw lte;
  }
  validateFirstLayout(layout.getType());
}

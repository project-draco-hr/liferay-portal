{
  Group group=layoutSetPrototype.getGroup();
  Layout layout=layoutPersistence.fetchByUUID_G_P(layoutUuid,group.getGroupId(),privateLayout);
  if (layout != null) {
    return true;
  }
  return false;
}

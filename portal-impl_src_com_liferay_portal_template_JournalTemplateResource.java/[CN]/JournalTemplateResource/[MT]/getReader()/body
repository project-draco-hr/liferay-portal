{
  if (_templateContent == null) {
    return null;
  }
  String xsl=_templateContent.getXsl();
  return new UnsyncBufferedReader(new InputStreamReader(new UnsyncByteArrayInputStream(xsl.getBytes()),DEFAUT_ENCODING));
}

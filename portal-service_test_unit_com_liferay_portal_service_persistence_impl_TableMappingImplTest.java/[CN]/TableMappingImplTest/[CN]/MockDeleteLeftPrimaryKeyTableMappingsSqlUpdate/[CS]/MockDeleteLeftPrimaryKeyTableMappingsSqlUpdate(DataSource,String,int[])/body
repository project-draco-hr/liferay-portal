{
  Assert.assertSame(_dataSource,dataSource);
  Assert.assertEquals("DELETE FROM " + _tableName + " WHERE "+ _leftColumnName+ " = ?",sql);
  Assert.assertArrayEquals(new int[]{Types.BIGINT},types);
}

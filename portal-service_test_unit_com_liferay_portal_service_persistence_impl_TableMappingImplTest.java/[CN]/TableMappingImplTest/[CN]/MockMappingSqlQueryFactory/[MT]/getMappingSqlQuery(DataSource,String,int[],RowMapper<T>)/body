{
  int count=_counter++;
  if (count == 0) {
    return (MappingSqlQuery<T>)new MockGetRightPrimaryKeysByLeftPrimaryKeyMappingSqlQuery(dataSource,sql,types,RowMapper.PRIMARY_KEY);
  }
  if (count == 1) {
    return (MappingSqlQuery<T>)new MockGetLeftPrimaryKeysByRightPrimaryKeyMappingSqlQuery(dataSource,sql,types,RowMapper.PRIMARY_KEY);
  }
  return null;
}

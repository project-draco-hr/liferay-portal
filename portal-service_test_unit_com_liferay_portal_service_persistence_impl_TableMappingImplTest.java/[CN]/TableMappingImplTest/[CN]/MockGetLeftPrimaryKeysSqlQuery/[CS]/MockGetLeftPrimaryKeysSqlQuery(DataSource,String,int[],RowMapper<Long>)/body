{
  Assert.assertSame(_dataSource,dataSource);
  Assert.assertEquals("SELECT " + _leftColumnName + " FROM "+ _tableName+ " WHERE "+ _rightColumnName+ "=?",sql);
  Assert.assertArrayEquals(new int[]{java.sql.Types.BIGINT},types);
  Assert.assertSame(RowMapper.PRIMARY_KEY,rowMapper);
}

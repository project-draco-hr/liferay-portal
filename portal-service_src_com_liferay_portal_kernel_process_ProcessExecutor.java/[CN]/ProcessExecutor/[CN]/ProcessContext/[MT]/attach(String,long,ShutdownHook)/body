{
  HeartbeatThread attachThread=new HeartbeatThread(message,interval,shutdownHook);
  boolean result=_attachThreadReference.compareAndSet(null,attachThread);
  if (result) {
    attachThread.start();
  }
  return result;
}

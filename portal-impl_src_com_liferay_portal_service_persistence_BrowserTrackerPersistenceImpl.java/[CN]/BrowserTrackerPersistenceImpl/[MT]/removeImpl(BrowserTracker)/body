{
  browserTracker=toUnwrappedModel(browserTracker);
  Session session=null;
  try {
    session=openSession();
    if (browserTracker.isCachedModel()) {
      browserTracker=(BrowserTracker)session.get(BrowserTrackerImpl.class,browserTracker.getPrimaryKeyObj());
    }
    session.delete(browserTracker);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(browserTracker);
  return browserTracker;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BrowserTracker browserTracker) method. Use update(BrowserTracker browserTracker, boolean merge) instead.");
  }
  return update(browserTracker,false);
}

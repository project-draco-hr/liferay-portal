{
  Map<String,Collection<String>> resourceMap=_resourceMaps.get(bundleWiring.getBundle());
  if (resourceMap == null) {
    resourceMap=new HashMap<String,Collection<String>>();
  }
  resourceMap.put(path,resources);
}

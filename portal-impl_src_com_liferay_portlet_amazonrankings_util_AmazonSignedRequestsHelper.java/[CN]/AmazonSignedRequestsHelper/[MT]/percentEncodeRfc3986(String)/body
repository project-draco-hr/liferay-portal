{
  String encodedString=null;
  try {
    encodedString=URLEncoder.encode(inputString,UTF8_CHARSET).replace(StringPool.PLUS,"%2B").replace(StringPool.ASTERISK,"%2A").replace("%7E","~");
  }
 catch (  UnsupportedEncodingException e) {
    encodedString=inputString;
  }
  return encodedString;
}

{
  IdempotentRetryAssert.retryAssert(10,TimeUnit.MINUTES,new Callable<Void>(){
    @Override public Void call() throws Exception {
      ClusterHealthResponse clusterHealthResponse=elasticsearchFixture.getClusterHealthResponse(healthExpectations);
      _assertHealth(clusterHealthResponse,healthExpectations);
      return null;
    }
  }
);
}

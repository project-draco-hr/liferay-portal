{
  Integer type=SocialActivityCounterModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SocialActivityCounterModelImpl.TABLE_NAME);
  }
  return type;
}

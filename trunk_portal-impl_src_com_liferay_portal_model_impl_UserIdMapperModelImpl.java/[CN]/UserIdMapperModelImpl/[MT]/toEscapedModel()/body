{
  if (isEscapedModel()) {
    return (UserIdMapper)this;
  }
 else {
    return (UserIdMapper)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

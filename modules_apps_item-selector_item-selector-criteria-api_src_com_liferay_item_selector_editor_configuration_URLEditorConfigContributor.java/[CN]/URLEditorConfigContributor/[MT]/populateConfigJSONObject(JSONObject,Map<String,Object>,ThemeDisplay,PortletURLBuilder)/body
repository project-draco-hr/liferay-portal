{
  PortletURL itemSelectorURL=getItemSelectorPortletURL(inputEditorTaglibAttributes,portletURLBuilder,new URLItemSelectorCriterion());
  if (itemSelectorURL != null) {
    jsonObject.put("filebrowserBrowseUrl",itemSelectorURL.toString());
  }
}

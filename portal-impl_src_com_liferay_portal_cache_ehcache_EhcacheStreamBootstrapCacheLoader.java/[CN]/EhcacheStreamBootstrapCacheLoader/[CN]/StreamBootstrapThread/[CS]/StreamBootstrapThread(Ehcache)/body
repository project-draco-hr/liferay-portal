{
  if (_log.isDebugEnabled()) {
    _log.debug("Bootstrap thread for cache " + ehcache.getName() + " asynchronously.");
  }
  _ehcache=ehcache;
  setDaemon(true);
  setPriority(Thread.NORM_PRIORITY);
}

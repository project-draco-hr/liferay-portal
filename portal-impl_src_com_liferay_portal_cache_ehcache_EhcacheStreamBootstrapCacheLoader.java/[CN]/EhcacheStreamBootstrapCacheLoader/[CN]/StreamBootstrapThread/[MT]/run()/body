{
  try {
    doLoad(_ehcache);
  }
 catch (  RemoteCacheException e) {
    if (_log.isWarnEnabled()) {
      _log.warn("Error asynchronously performing streamBootstrap.");
    }
  }
 finally {
    _ehcache=null;
  }
}

{
  if (isEscapedModel()) {
    return (ExpandoColumn)this;
  }
 else {
    ExpandoColumn model=new ExpandoColumnImpl();
    model.setNew(isNew());
    model.setEscapedModel(true);
    model.setColumnId(getColumnId());
    model.setTableId(getTableId());
    model.setName(HtmlUtil.escape(getName()));
    model.setType(getType());
    model=(ExpandoColumn)Proxy.newProxyInstance(ExpandoColumn.class.getClassLoader(),new Class[]{ExpandoColumn.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

{
  Iterator<SCProductScreenshot> itr=findByProductEntryId(productEntryId).iterator();
  while (itr.hasNext()) {
    SCProductScreenshot scProductScreenshot=itr.next();
    remove(scProductScreenshot);
  }
}

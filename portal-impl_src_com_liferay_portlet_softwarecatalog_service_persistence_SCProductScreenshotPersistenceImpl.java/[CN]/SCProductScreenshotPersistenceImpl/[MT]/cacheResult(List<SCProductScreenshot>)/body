{
  for (  SCProductScreenshot scProductScreenshot : scProductScreenshots) {
    if (EntityCacheUtil.getResult(SCProductScreenshotModelImpl.ENTITY_CACHE_ENABLED,SCProductScreenshot.class,scProductScreenshot.getPrimaryKey(),this) == null) {
      cacheResult(scProductScreenshot);
    }
  }
}

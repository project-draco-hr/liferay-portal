{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCProductScreenshot scProductScreenshot) method. To improve performance, call update(SCProductScreenshot scProductScreenshot, boolean merge).");
  }
  return update(scProductScreenshot,false);
}

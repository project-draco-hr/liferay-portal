{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SCProductScreenshot scProductScreenshot) method. Use update(SCProductScreenshot scProductScreenshot, boolean merge) instead.");
  }
  return update(scProductScreenshot,false);
}

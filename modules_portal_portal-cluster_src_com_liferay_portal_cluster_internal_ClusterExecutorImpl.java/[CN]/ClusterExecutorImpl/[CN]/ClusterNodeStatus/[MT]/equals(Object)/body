{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ClusterNodeStatus)) {
    return false;
  }
  ClusterNodeStatus clusterNodeStatus=(ClusterNodeStatus)obj;
  if (Validator.equals(_address,clusterNodeStatus._address) && Validator.equals(_clusterNode,clusterNodeStatus._clusterNode)) {
    return true;
  }
  return false;
}

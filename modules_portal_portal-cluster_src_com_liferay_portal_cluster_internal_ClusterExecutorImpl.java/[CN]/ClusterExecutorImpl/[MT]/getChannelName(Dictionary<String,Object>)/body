{
  String channelName=GetterUtil.getString(properties.get(ClusterPropsKeys.CHANNEL_NAME_CONTROL));
  if (Validator.isNull(channelName)) {
    channelName=_props.get(PropsKeys.CLUSTER_LINK_CHANNEL_NAME_CONTROL);
  }
  return channelName;
}

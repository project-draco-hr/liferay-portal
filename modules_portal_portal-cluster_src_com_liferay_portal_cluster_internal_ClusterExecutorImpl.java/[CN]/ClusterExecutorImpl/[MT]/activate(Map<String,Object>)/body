{
  clusterExecutorConfiguration=Configurable.createConfigurable(ClusterExecutorConfiguration.class,properties);
  String controlChannelProperties=getControlChannelProperties(properties);
  initialize(controlChannelProperties);
}

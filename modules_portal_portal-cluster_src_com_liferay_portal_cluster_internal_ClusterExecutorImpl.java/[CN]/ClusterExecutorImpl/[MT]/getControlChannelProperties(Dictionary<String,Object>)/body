{
  String controlChannelProperties=GetterUtil.getString(properties.get(ClusterPropsKeys.CHANNEL_PROPERTIES_CONTROL));
  if (Validator.isNull(controlChannelProperties)) {
    controlChannelProperties=_props.get(PropsKeys.CLUSTER_LINK_CHANNEL_PROPERTIES_CONTROL);
  }
  return controlChannelProperties;
}

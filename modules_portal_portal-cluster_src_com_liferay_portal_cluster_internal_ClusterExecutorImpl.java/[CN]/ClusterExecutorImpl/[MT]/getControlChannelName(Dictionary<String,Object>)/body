{
  String controlChannelName=GetterUtil.getString(properties.get(ClusterPropsKeys.CHANNEL_NAME_CONTROL));
  if (Validator.isNull(controlChannelName)) {
    controlChannelName=_props.get(PropsKeys.CLUSTER_LINK_CHANNEL_NAME_CONTROL);
  }
  return controlChannelName;
}

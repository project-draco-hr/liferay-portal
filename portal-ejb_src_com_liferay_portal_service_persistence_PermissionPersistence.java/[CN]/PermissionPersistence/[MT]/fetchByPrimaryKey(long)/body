{
  Session session=null;
  try {
    session=openSession();
    return (Permission)session.get(PermissionImpl.class,new Long(permissionId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

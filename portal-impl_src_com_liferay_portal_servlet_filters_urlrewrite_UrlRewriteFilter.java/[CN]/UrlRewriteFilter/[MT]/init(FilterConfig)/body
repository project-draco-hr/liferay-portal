{
  super.init(filterConfig);
  _urlRewriteFilter=new org.tuckey.web.filters.urlrewrite.UrlRewriteFilter();
  try {
    _urlRewriteFilter.init(filterConfig);
  }
 catch (  ServletException se) {
    _urlRewriteFilter=null;
    getLog().error(se);
  }
}

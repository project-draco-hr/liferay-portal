{
  DDMStructureLinkManager ddmStructureLinkManager=_instance._serviceTracker.getService();
  if (ddmStructureLinkManager == null) {
    return _dummyDDMStructureLinkManagerImpl;
  }
  return ddmStructureLinkManager;
}

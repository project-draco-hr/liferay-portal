{
  if (_templateContent == null) {
    return null;
  }
  URLConnection urlConnection=_templateContent.openConnection();
  return new UnsyncBufferedReader(new InputStreamReader(urlConnection.getInputStream(),DEFAUT_ENCODING));
}

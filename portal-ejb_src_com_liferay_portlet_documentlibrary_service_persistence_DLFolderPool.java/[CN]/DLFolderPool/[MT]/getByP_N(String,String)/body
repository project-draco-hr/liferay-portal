{
  String pk=_instance._getByP_N(parentFolderId,name);
  if (_log.isInfoEnabled()) {
    _log.info("Get P_N" + " " + parentFolderId.toString() + " "+ name.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

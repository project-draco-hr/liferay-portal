{
  try {
    String messages=ParamUtil.getString(req,"messages");
    int msgList[]=StringUtil.split(messages,",",-1);
    MailUtil.deleteMessages(MailUtil.getStore(req),MailUtil.getCurrentFolder(req),msgList);
  }
 catch (  Exception e) {
  }
  return null;
}

{
  Document doc=getAddEntryDocument(companyId,groupId,userId,categoryId,entryId,title,content);
  IndexWriter writer=null;
  try {
    writer=LuceneUtil.getWriter(companyId);
    writer.addDocument(doc);
  }
  finally {
    if (writer != null) {
      LuceneUtil.write(companyId);
    }
  }
}

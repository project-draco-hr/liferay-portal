{
  Document doc=getEntryDocument(companyId,groupId,userId,userName,entryId,title,content,displayDate,tagsEntries,expandoBridge);
  SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
}

{
  Map<Locale,String> titleMap=new HashMap<Locale,String>();
  Map<Locale,String> descriptionMap=new HashMap<Locale,String>();
  for (  Locale locale : getLocales()) {
    titleMap.put(locale,title.concat(LocaleUtil.toLanguageId(locale)));
    descriptionMap.put(locale,description.concat(LocaleUtil.toLanguageId(locale)));
  }
  return PollsQuestionLocalServiceUtil.updateQuestion(question.getUserId(),question.getQuestionId(),titleMap,descriptionMap,0,0,0,0,0,true,question.getChoices(),ServiceTestUtil.getServiceContext());
}

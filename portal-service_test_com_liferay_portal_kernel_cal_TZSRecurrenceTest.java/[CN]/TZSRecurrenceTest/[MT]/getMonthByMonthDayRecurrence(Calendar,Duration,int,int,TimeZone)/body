{
  TZSRecurrence recurrence=new TZSRecurrence(dtStart,duration,Recurrence.MONTHLY);
  int[] monthDays={monthDay};
  recurrence.setByMonthDay(monthDays);
  recurrence.setInterval(interval);
  recurrence.setTimeZone(tz);
  return recurrence;
}

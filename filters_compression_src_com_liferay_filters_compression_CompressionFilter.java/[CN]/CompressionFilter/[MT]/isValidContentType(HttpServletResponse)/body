{
  String contentType=GetterUtil.getString(res.getContentType());
  if (Validator.isNull(contentType) || contentType.toLowerCase().indexOf("text/") != -1) {
    return true;
  }
 else {
    return false;
  }
}

{
  Map<String,Map<String,String>> queryMap=_getQueryMap(locale);
  String cacheKey=_getQueryMapCacheKey(fieldName,attributeName,attributeValue);
  Map<String,String> queryResult=queryMap.get(cacheKey);
  return queryResult;
}

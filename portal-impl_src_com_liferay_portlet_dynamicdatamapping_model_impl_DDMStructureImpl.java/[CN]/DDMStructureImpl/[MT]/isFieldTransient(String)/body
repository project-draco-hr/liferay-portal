{
  if (!hasField(fieldName)) {
    throw new StructureFieldException();
  }
  Map<String,Map<String,String>> transientFieldsMap=getTransientFieldsMap(getDefaultLanguageId());
  return transientFieldsMap.containsKey(fieldName);
}

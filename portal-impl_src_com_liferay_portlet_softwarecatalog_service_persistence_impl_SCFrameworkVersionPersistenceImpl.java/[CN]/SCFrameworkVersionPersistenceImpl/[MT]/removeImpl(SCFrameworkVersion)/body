{
  scFrameworkVersion=toUnwrappedModel(scFrameworkVersion);
  scFrameworkVersionToSCProductVersionTableMapper.deleteLeftPrimaryKeyTableMappings(0,scFrameworkVersion.getPrimaryKey());
  Session session=null;
  try {
    session=openSession();
    if (!session.contains(scFrameworkVersion)) {
      scFrameworkVersion=(SCFrameworkVersion)session.get(SCFrameworkVersionImpl.class,scFrameworkVersion.getPrimaryKeyObj());
    }
    if (scFrameworkVersion != null) {
      session.delete(scFrameworkVersion);
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  if (scFrameworkVersion != null) {
    clearCache(scFrameworkVersion);
  }
  return scFrameworkVersion;
}

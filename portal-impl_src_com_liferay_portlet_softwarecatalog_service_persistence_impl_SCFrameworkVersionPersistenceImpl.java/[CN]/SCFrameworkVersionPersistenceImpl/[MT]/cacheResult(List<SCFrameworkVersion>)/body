{
  for (  SCFrameworkVersion scFrameworkVersion : scFrameworkVersions) {
    if (EntityCacheUtil.getResult(SCFrameworkVersionModelImpl.ENTITY_CACHE_ENABLED,SCFrameworkVersionImpl.class,scFrameworkVersion.getPrimaryKey()) == null) {
      cacheResult(scFrameworkVersion);
    }
 else {
      scFrameworkVersion.resetOriginalValues();
    }
  }
}

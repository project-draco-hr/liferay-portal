{
  for (  SCFrameworkVersion scFrameworkVersion : scFrameworkVersions) {
    if (entityCache.getResult(SCFrameworkVersionModelImpl.ENTITY_CACHE_ENABLED,SCFrameworkVersionImpl.class,scFrameworkVersion.getPrimaryKey()) == null) {
      cacheResult(scFrameworkVersion);
    }
 else {
      scFrameworkVersion.resetOriginalValues();
    }
  }
}

{
  try {
    _persistence.countByCA_K(ServiceTestUtil.nextLong(),StringPool.BLANK);
    _persistence.countByCA_K(0L,StringPool.NULL);
    _persistence.countByCA_K(0L,(String)null);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  Revision revision=findByPrimaryKey(revisionId);
  Session session=null;
  try {
    session=openSession();
    Revision[] array=new RevisionImpl[3];
    array[0]=getByBranchId_PrevAndNext(session,revision,branchId,orderByComparator,true);
    array[1]=revision;
    array[2]=getByBranchId_PrevAndNext(session,revision,branchId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  for (  Revision revision : revisions) {
    if (EntityCacheUtil.getResult(RevisionModelImpl.ENTITY_CACHE_ENABLED,RevisionImpl.class,revision.getPrimaryKey(),this) == null) {
      cacheResult(revision);
    }
  }
}

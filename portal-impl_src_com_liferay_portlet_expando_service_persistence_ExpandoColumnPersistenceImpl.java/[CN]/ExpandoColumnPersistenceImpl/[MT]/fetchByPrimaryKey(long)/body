{
  ExpandoColumn expandoColumn=(ExpandoColumn)EntityCacheUtil.getResult(ExpandoColumnModelImpl.ENTITY_CACHE_ENABLED,ExpandoColumnImpl.class,columnId,this);
  if (expandoColumn == _nullExpandoColumn) {
    return null;
  }
  if (expandoColumn == null) {
    Session session=null;
    boolean hasException=false;
    try {
      session=openSession();
      expandoColumn=(ExpandoColumn)session.get(ExpandoColumnImpl.class,Long.valueOf(columnId));
    }
 catch (    Exception e) {
      hasException=true;
      throw processException(e);
    }
 finally {
      if (!hasException && (expandoColumn == null)) {
        EntityCacheUtil.putResult(ExpandoColumnModelImpl.ENTITY_CACHE_ENABLED,ExpandoColumnImpl.class,columnId,_nullExpandoColumn);
      }
 else {
        cacheResult(expandoColumn);
      }
      closeSession(session);
    }
  }
  return expandoColumn;
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ExpandoColumn expandoColumn) method. Use update(ExpandoColumn expandoColumn, boolean merge) instead.");
  }
  return update(expandoColumn,false);
}

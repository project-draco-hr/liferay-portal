{
  ExpandoColumn expandoColumn=findByPrimaryKey(columnId);
  Session session=null;
  try {
    session=openSession();
    ExpandoColumn[] array=new ExpandoColumnImpl[3];
    array[0]=getByT_N_PrevAndNext(session,expandoColumn,tableId,name,orderByComparator,true);
    array[1]=expandoColumn;
    array[2]=getByT_N_PrevAndNext(session,expandoColumn,tableId,name,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  String tmpName=name.concat(StringPool.UNDERLINE).concat(locale.toString());
  Field field=_fields.get(tmpName);
  if (field == null) {
    field=_fields.get(name);
  }
  if (field == null) {
    return StringPool.BLANK;
  }
  return field.getValue();
}

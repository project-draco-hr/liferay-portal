{
  if (locale == null) {
    return name;
  }
  String languageId=LocaleUtil.toLanguageId(locale);
  String localizedName=name.concat(StringPool.UNDERLINE).concat(languageId);
  return localizedName;
}

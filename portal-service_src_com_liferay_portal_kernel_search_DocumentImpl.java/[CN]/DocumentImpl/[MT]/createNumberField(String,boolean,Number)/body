{
  if (value == null) {
    return;
  }
  String stringValue=String.valueOf(value);
  createSortableNumericField(name,typifySortable,stringValue,value.getClass());
  createField(name,stringValue);
}

{
  if (folderId == BookmarksFolderConstants.DEFAULT_PARENT_FOLDER_ID) {
    return BookmarksResourcePermissionChecker.contains(permissionChecker,groupId,actionId);
  }
 else {
    BookmarksFolder folder=_bookmarksFolderLocalService.getBookmarksFolder(folderId);
    return contains(permissionChecker,folder,actionId);
  }
}

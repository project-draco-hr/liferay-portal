{
  StringMaker sm=new StringMaker();
  sm.append("new Array(");
  for (int i=0; i < array.length; i++) {
    sm.append("'" + array[i] + "'");
    if (i + 1 < array.length) {
      sm.append(",");
    }
  }
  sm.append(")");
  return sm.toString();
}

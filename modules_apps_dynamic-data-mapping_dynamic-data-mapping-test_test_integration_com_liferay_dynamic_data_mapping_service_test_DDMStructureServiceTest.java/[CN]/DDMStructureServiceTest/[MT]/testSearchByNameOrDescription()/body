{
  addStructure(_CLASS_NAME_ID,"Contact","Contact");
  addStructure(_CLASS_NAME_ID,"Event","Event");
  List<DDMStructure> structures=DDMStructureLocalServiceUtil.search(TestPropsValues.getCompanyId(),new long[]{group.getGroupId()},_CLASS_NAME_ID,"Contact","Event",null,DDMStructureConstants.TYPE_DEFAULT,false,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StructureIdComparator(true),WorkflowConstants.STATUS_APPROVED);
  Assert.assertEquals("Contact",getStructureName(structures.get(0)));
  Assert.assertEquals("Event",getStructureName(structures.get(1)));
}

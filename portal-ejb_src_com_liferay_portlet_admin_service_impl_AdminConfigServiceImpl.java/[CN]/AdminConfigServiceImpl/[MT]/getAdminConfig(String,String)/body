{
  List configs=(List)_configsPool.get(companyId + "." + type);
  if (configs == null) {
    configs=AdminConfigUtil.findByC_T(companyId,type,0,1);
    _configsPool.put(companyId + "." + type,configs);
  }
  return configs;
}

{
  if ((list == null) || (list.isEmpty()) || (remove == null)|| (remove.isEmpty())) {
    return list;
  }
  list=copy(list);
  for (  E element : remove) {
    list.remove(element);
  }
  return list;
}

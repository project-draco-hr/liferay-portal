{
  if (deepEquals) {
    return list.remove(item);
  }
 else {
    Iterator<E> itr=list.iterator();
    while (itr.hasNext()) {
      E curItem=itr.next();
      if (curItem == item) {
        itr.remove();
        return true;
      }
    }
  }
  return false;
}

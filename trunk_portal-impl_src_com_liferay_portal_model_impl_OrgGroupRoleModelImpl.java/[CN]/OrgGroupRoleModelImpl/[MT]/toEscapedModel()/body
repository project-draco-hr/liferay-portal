{
  if (isEscapedModel()) {
    return (OrgGroupRole)this;
  }
 else {
    return (OrgGroupRole)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

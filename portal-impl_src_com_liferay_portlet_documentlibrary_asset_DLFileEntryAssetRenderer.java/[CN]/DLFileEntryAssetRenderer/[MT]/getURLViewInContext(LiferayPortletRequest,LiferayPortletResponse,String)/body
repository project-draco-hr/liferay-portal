{
  ThemeDisplay themeDisplay=(ThemeDisplay)liferayPortletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  return themeDisplay.getPathMain() + "/document_library/find_file_entry?noSuchEntryRedirect=" + HttpUtil.encodeURL(noSuchEntryRedirect)+ "&p_l_id="+ themeDisplay.getPlid()+ "&fileEntryId="+ _fileEntry.getFileEntryId();
}

{
  if (!_fileVersion.isApproved() && (_fileVersion.getVersion() != DLFileEntryConstants.DEFAULT_VERSION)) {
    return _fileVersion.getFileVersionId();
  }
 else {
    return _fileEntry.getFileEntryId();
  }
}

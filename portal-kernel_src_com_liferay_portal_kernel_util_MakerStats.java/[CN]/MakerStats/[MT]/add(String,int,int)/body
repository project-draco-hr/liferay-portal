{
  SizeSample stat=null;
synchronized (_map) {
    stat=_map.get(caller);
    if (stat == null) {
      stat=new SizeSample(caller,initSize);
      _map.put(caller,stat);
    }
    _count++;
  }
synchronized (stat) {
    stat.add(finalSize);
  }
}

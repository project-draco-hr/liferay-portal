{
  String[] classNames=activityInterpreter.getClassNames();
  Map<String,SocialActivityInterpreter> activityInterpreters=_activityInterpreters.get(activityInterpreter.getSelector());
  if (activityInterpreters == null) {
    return;
  }
  for (  String className : classNames) {
    activityInterpreters.remove(className);
  }
}

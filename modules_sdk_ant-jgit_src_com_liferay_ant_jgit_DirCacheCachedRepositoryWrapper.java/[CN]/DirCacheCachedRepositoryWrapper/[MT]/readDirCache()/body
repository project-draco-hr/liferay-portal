{
  if (_dirCache == null) {
synchronized (DirCacheCachedRepositoryWrapper.class) {
      if (_dirCache == null) {
        _dirCache=repository.readDirCache();
      }
    }
  }
  return _dirCache;
}

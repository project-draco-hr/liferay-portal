{
  HttpServletRequest request=_portal.getHttpServletRequest(renderRequest);
  request.setAttribute(WebKeys.IP_GEOCODER,new IPGeocoderHelper(_ipGeocoder));
  super.render(renderRequest,renderResponse);
}

{
  if (isEscapedModel()) {
    return (EmailAddress)this;
  }
 else {
    return (EmailAddress)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

{
  if (!contains(permissionChecker,category,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),AssetCategory.class.getName(),category.getCategoryId(),actionId);
  }
}

{
  if (!contains(permissionChecker,categoryId,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),AssetCategory.class.getName(),categoryId,actionId);
  }
}

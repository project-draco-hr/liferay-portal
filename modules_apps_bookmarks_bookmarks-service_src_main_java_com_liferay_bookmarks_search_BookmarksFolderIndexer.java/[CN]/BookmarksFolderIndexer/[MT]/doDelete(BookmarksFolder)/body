{
  Document document=new DocumentImpl();
  document.addUID(CLASS_NAME,bookmarksFolder.getFolderId());
  SearchEngineUtil.deleteDocument(getSearchEngineId(),bookmarksFolder.getCompanyId(),document.get(Field.UID),isCommitImmediately());
}

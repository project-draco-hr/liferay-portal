{
  if (!bookmarksFolder.isApproved() && !bookmarksFolder.isInTrash()) {
    return;
  }
  Document document=getDocument(bookmarksFolder);
  SearchEngineHelperUtil.updateDocument(getSearchEngineId(),bookmarksFolder.getCompanyId(),document,isCommitImmediately());
}

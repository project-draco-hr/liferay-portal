{
  Document document=getDocument(bookmarksFolder);
  if (!bookmarksFolder.isApproved() && !bookmarksFolder.isInTrash()) {
    return;
  }
  SearchEngineUtil.updateDocument(getSearchEngineId(),bookmarksFolder.getCompanyId(),document,isCommitImmediately());
}

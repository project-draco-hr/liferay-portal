{
  Session session=null;
  try {
    session=openSession();
    return (DLFileShortcut)session.get(DLFileShortcut.class,new Long(fileShortcutId));
  }
 catch (  HibernateException he) {
    throw new SystemException(he);
  }
 finally {
    closeSession(session);
  }
}

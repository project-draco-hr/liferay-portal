{
  _s3Configuration=Configurable.createConfigurable(S3Configuration.class,properties);
  try {
    _s3Service=getS3Service();
    _s3Bucket=getS3Bucket();
  }
 catch (  S3ServiceException s3se) {
    throw new IllegalArgumentException(s3se);
  }
}

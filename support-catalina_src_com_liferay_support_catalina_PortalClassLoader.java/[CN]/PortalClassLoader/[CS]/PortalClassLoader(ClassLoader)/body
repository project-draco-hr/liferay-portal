{
  super(PortalClassLoaderUtil.getClassLoader() != null ? PortalClassLoaderUtil.getClassLoader() : null);
  if (PortalClassLoaderUtil.getClassLoader() == null) {
    _log.error("Portal class loader is not available to override the " + "default Catalina web class loader");
  }
}

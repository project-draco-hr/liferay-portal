{
  if (isEscapedModel()) {
    return (RatingsStats)this;
  }
 else {
    return (RatingsStats)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

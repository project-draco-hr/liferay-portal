{
  if (isEscapedModel()) {
    return (RatingsStats)this;
  }
 else {
    return (RatingsStats)Proxy.newProxyInstance(RatingsStats.class.getClassLoader(),new Class[]{RatingsStats.class},new AutoEscapeBeanHandler(this));
  }
}

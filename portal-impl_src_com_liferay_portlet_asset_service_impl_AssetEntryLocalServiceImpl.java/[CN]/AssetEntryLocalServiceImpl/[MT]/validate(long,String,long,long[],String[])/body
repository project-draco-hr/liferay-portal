{
  if (ExportImportThreadLocal.isImportInProcess()) {
    return;
  }
  for (  AssetEntryValidator assetEntryValidator : assetEntryValidatorRegistry.getAssetEntryValidators(className)) {
    assetEntryValidator.validate(groupId,className,classTypePK,categoryIds,tagNames);
  }
}

{
  List<AssetLink> parentLinks=assetLinkLocalService.getReverseLinks(entryId,AssetLinkConstants.TYPE_CHILD);
  if (parentLinks.isEmpty()) {
    return null;
  }
  AssetLink parentLink=parentLinks.get(0);
  return getEntry(parentLink.getEntryId1());
}

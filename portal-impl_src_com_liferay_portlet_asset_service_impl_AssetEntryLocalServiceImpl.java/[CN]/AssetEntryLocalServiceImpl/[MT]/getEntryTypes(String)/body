{
  AssetEntryType[] entryTypes=new AssetEntryType[AssetUtil.ASSET_ENTRY_TYPE_CLASS_NAMES.length];
  for (int i=0; i < AssetUtil.ASSET_ENTRY_TYPE_CLASS_NAMES.length; i++) {
    entryTypes[i]=getEntryType(AssetUtil.ASSET_ENTRY_TYPE_CLASS_NAMES[i],languageId);
  }
  return entryTypes;
}

{
  if ((tagIds.length == 0) && (notTagIds.length == 0)) {
    return assetEntryFinder.findEntries(groupId,classNameIds,null,null,null,null,excludeZeroViewCount,publishDate,expirationDate,start,end);
  }
 else   if (andOperator) {
    return assetEntryFinder.findByAndTagIds(groupId,classNameIds,tagIds,notTagIds,null,null,null,null,excludeZeroViewCount,publishDate,expirationDate,start,end);
  }
 else {
    return assetEntryFinder.findByOrTagIds(groupId,classNameIds,tagIds,notTagIds,null,null,null,null,excludeZeroViewCount,publishDate,expirationDate,start,end);
  }
}

{
  int index=pathInfo.indexOf(DLUtil.MANUAL_CHECKIN_REQUIRED_PATH);
  if (index >= 0) {
    pathInfo=pathInfo.substring(0,index) + pathInfo.substring(index + DLUtil.MANUAL_CHECKIN_REQUIRED_PATH.length(),pathInfo.length());
  }
  return pathInfo;
}

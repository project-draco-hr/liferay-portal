{
  int index=pathInfo.indexOf(DLUtil.MANUAL_CHECK_IN_REQUIRED_PATH);
  if (index >= 0) {
    pathInfo=pathInfo.substring(0,index) + pathInfo.substring(index + DLUtil.MANUAL_CHECK_IN_REQUIRED_PATH.length(),pathInfo.length());
  }
  return pathInfo;
}

{
  OutputStreamProvider outputStreamProvider=_outputStreamProviders.getService(outputStreamProviderName);
  if (outputStreamProvider == null) {
    throw new IllegalArgumentException("Report provider with name " + outputStreamProviderName + " is not registered");
  }
  return outputStreamProvider;
}

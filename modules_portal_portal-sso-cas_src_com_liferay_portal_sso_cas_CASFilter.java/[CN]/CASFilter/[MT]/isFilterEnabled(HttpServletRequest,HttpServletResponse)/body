{
  try {
    long companyId=PortalUtil.getCompanyId(request);
    if (PrefsPropsUtil.getBoolean(companyId,PropsKeys.CAS_AUTH_ENABLED,_casConfiguration.enabled())) {
      return true;
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return false;
}

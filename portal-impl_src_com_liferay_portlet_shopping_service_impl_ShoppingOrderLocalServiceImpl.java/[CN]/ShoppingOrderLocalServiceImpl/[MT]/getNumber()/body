{
  String number=PwdGenerator.getPassword(PwdGenerator.KEY1 + PwdGenerator.KEY2,12);
  ShoppingOrder order=shoppingOrderPersistence.fetchByNumber(number);
  if (order != null) {
    return order.getNumber();
  }
  return number;
}

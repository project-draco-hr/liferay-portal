{
  String number=PwdGenerator.getPassword(PwdGenerator.KEY1 + PwdGenerator.KEY2,12);
  try {
    ShoppingOrder shoppingOrder=shoppingOrderPersistence.findByNumber(number);
    return shoppingOrder.getNumber();
  }
 catch (  NoSuchOrderException nsoe) {
    return number;
  }
}

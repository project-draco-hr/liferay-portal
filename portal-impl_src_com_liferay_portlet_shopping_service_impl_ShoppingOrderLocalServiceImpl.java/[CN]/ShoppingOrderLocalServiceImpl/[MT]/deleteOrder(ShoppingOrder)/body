{
  ShoppingOrderItemUtil.removeByOrderId(order.getOrderId());
  MBMessageLocalServiceUtil.deleteDiscussionMessages(ShoppingOrder.class.getName(),order.getOrderId());
  ShoppingOrderUtil.remove(order.getOrderId());
}

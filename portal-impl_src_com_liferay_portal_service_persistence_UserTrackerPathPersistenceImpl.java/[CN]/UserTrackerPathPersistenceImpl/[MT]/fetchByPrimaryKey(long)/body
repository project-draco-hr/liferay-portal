{
  UserTrackerPath result=(UserTrackerPath)EntityCacheUtil.getResult(UserTrackerPathModelImpl.ENTITY_CACHE_ENABLED,UserTrackerPath.class,userTrackerPathId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      UserTrackerPath userTrackerPath=(UserTrackerPath)session.get(UserTrackerPathImpl.class,new Long(userTrackerPathId));
      cacheResult(userTrackerPath);
      return userTrackerPath;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (UserTrackerPath)result;
  }
}

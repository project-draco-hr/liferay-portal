{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserTrackerPath userTrackerPath) method. To improve performance, call update(UserTrackerPath userTrackerPath, boolean merge).");
  }
  return update(userTrackerPath,false);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserTrackerPath userTrackerPath) method. Use update(UserTrackerPath userTrackerPath, boolean merge) instead.");
  }
  return update(userTrackerPath,false);
}

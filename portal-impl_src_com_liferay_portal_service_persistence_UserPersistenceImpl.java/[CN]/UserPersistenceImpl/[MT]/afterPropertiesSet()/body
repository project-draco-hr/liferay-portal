{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.portal.util.PropsUtil.get("value.object.listener.com.liferay.portal.model.User")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<User>> listenersList=new ArrayList<ModelListener<User>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<User>)InstanceFactory.newInstance(getClassLoader(),listenerClassName));
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
  userToGroupTableMapping=TableMappingFactory.getTableMapping("Users_Groups","userId","groupId",this,groupPersistence);
  userToOrganizationTableMapping=TableMappingFactory.getTableMapping("Users_Orgs","userId","organizationId",this,organizationPersistence);
  userToRoleTableMapping=TableMappingFactory.getTableMapping("Users_Roles","userId","roleId",this,rolePersistence);
  userToTeamTableMapping=TableMappingFactory.getTableMapping("Users_Teams","userId","teamId",this,teamPersistence);
  userToUserGroupTableMapping=TableMappingFactory.getTableMapping("Users_UserGroups","userId","userGroupId",this,userGroupPersistence);
}

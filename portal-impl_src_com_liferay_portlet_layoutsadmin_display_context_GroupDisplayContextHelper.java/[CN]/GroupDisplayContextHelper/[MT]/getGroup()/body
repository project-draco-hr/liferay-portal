{
  if (_group != null) {
    return _group;
  }
  if (PropsValues.STAGING_DISABLE_LIVE_SITE_LOCKING && (getSelGroup() != null)) {
    _group=getSelGroup();
  }
 else   if (getStagingGroup() != null) {
    _group=getStagingGroup();
  }
 else {
    _group=getLiveGroup();
  }
  return _group;
}

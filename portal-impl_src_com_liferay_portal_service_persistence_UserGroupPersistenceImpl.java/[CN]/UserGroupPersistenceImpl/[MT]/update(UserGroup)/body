{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserGroup userGroup) method. To improve performance, call update(UserGroup userGroup, boolean merge).");
  }
  return update(userGroup,false);
}

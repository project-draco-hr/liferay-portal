{
  UserGroup userGroup=(UserGroup)EntityCacheUtil.getResult(UserGroupModelImpl.ENTITY_CACHE_ENABLED,UserGroupImpl.class,userGroupId,this);
  if (userGroup == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (userGroup == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      userGroup=(UserGroup)session.get(UserGroupImpl.class,Long.valueOf(userGroupId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (userGroup == null)) {
        EntityCacheUtil.putResult(UserGroupModelImpl.ENTITY_CACHE_ENABLED,UserGroupImpl.class,userGroupId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(userGroup);
      }
      closeSession(session);
    }
  }
  return userGroup;
}

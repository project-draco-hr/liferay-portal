{
  for (  UserGroup userGroup : userGroups) {
    if (EntityCacheUtil.getResult(UserGroupModelImpl.ENTITY_CACHE_ENABLED,UserGroup.class,userGroup.getPrimaryKey(),this) == null) {
      cacheResult(userGroup);
    }
  }
}

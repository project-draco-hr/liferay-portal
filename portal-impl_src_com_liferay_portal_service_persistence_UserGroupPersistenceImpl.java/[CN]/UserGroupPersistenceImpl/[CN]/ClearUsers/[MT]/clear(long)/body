{
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onBeforeClearAssociation(userGroupId,com.liferay.portal.model.User.class.getName());
    }
  }
  _sqlUpdate.update(new Object[]{new Long(userGroupId)});
  if (_listeners.length > 0) {
    for (    ModelListener listener : _listeners) {
      listener.onAfterClearAssociation(userGroupId,com.liferay.portal.model.User.class.getName());
    }
  }
}

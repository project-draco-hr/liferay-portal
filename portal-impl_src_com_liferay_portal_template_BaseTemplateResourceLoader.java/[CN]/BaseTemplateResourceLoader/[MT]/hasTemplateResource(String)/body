{
  try {
    TemplateResource templateResource=getTemplateResource(templateId);
    if (templateResource != null) {
      return true;
    }
  }
 catch (  Exception ex) {
    _log.warn(ex,ex);
  }
  return false;
}

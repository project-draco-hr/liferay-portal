{
  if (_outputCharBuffer.position() > 0) {
    _writer.write(_outputCharBuffer.array(),0,_outputCharBuffer.position());
    if (flushWriter) {
      _writer.flush();
    }
    _outputCharBuffer.rewind();
  }
}

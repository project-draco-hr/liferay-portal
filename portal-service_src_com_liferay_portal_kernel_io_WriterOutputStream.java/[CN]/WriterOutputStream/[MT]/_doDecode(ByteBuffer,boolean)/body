{
  while (true) {
    CoderResult coderResult=_charsetDecoder.decode(inputBuffer,_outputBuffer,endOfInput);
    if (coderResult.isOverflow()) {
      flush();
    }
 else     if (coderResult.isUnderflow()) {
      if (_autoFlush) {
        flush();
      }
      break;
    }
 else {
      throw new IOException("Unexcepted coder result " + coderResult);
    }
  }
}

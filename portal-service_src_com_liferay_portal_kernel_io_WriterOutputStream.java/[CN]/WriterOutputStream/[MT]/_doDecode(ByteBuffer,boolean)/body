{
  while (true) {
    CoderResult coderResult=_charsetDecoder.decode(inputByteBuffer,_outputCharBuffer,endOfInput);
    if (coderResult.isOverflow()) {
      _flush(false);
    }
 else     if (coderResult.isUnderflow()) {
      if (_autoFlush) {
        _flush(true);
      }
      break;
    }
 else {
      throw new IOException("Unexcepted coder result " + coderResult);
    }
  }
}

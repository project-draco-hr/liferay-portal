{
  for (  String searchForJar : _libJars) {
    if (sourcePath.equals(_VERSIONS_PATH) && _excludeJars.contains(searchForJar)) {
      continue;
    }
    Assert.assertTrue(sourcePath + " is missing the reference to " + searchForJar,searchInJars.contains(searchForJar));
  }
}

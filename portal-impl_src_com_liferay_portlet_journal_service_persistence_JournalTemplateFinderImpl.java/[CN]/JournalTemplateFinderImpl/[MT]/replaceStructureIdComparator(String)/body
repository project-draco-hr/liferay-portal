{
  String replaceWith="(structureId != ? AND structureId IS NOT NULL) AND";
  if (CustomSQLUtil.isVendorOracle()) {
    replaceWith="(structureId IS NOT NULL) AND";
  }
  return StringUtil.replace(sql,"(structureId = ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]",replaceWith);
}

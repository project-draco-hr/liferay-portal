{
  try {
    byte[] bytes=null;
    if (is != null) {
      bytes=FileUtil.getBytes(is);
    }
    return updateImage(userId,imageId,folderId,name,description,bytes,contentType,serviceContext);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

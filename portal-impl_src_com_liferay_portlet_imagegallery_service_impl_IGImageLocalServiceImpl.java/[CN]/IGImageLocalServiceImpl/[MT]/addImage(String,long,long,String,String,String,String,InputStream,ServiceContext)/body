{
  try {
    byte[] bytes=FileUtil.getBytes(is);
    return addImage(uuid,userId,folderId,name,description,contentType,fileName,bytes,serviceContext);
  }
 catch (  IOException ioe) {
    throw new ImageSizeException(ioe);
  }
}

{
  try {
    byte[] bytes=FileUtil.getBytes(is);
    return addImage(uuid,userId,folderId,name,description,fileName,bytes,contentType,serviceContext);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

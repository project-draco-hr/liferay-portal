{
  if ((PrefsPropsUtil.getLong(PropsKeys.IG_IMAGE_MAX_SIZE) > 0) && ((bytes == null) || (bytes.length > PrefsPropsUtil.getLong(PropsKeys.IG_IMAGE_MAX_SIZE)))) {
    throw new ImageSizeException();
  }
}

{
  Image largeImage=imageLocalService.getImage(image.getLargeImageId());
  if (largeImage == null) {
    return;
  }
  String contentType=MimeTypesUtil.getContentType(largeImage.getType());
  updateAsset(userId,image,contentType,assetCategoryIds,assetTagNames);
}

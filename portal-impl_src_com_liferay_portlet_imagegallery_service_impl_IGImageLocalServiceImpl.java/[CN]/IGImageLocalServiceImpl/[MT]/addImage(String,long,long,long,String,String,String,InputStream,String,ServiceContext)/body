{
  try {
    byte[] bytes=FileUtil.getBytes(is);
    return addImage(uuid,userId,groupId,folderId,name,description,fileName,bytes,contentType,serviceContext);
  }
 catch (  IOException ioe) {
    throw new SystemException(ioe);
  }
}

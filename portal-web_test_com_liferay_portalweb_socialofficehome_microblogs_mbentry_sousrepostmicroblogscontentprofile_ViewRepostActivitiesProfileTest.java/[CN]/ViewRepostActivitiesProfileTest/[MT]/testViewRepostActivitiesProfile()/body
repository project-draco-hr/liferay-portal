{
  selenium.open("/web/joebloggs/so/profile");
  for (int second=0; ; second++) {
    if (second >= 90) {
      fail("timeout");
    }
    try {
      if (selenium.isVisible("xpath=(//div[@class='lfr-contact-name']/a)[2]")) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
  assertEquals(RuntimeVariables.replace("Joe Bloggs"),selenium.getText("xpath=(//div[@class='lfr-contact-name']/a)[2]"));
  assertEquals(RuntimeVariables.replace("Reposted From Joe: Microblogs Post"),selenium.getText("//div[@class='activity-title']"));
  assertEquals(RuntimeVariables.replace("Social01 Office01 User01"),selenium.getText("//div[@class='activity-body']/a"));
  assertEquals(RuntimeVariables.replace("Microblogs Post"),selenium.getText("xpath=(//div[@class='activity-title'])[2]"));
}

{
  if (obj == null) {
    return false;
  }
  JournalStructurePK pk=null;
  try {
    pk=(JournalStructurePK)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if ((companyId == pk.companyId) && (groupId == pk.groupId) && (structureId.equals(pk.structureId))) {
    return true;
  }
 else {
    return false;
  }
}

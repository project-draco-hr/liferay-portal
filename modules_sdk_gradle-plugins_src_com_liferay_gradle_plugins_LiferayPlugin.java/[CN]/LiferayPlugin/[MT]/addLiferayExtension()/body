{
  String projectName=project.getName();
  if (projectName.endsWith("-theme")) {
    _liferayExtension=addExtension(EXTENSION_NAME,LiferayThemeExtension.class);
  }
 else {
    _liferayExtension=addExtension(EXTENSION_NAME,LiferayExtension.class);
  }
}

{
  String projectName=project.getName();
  if (projectName.endsWith("-theme")) {
    _liferayExtension=addExtension(PLUGIN_NAME,LiferayThemeExtension.class);
  }
 else {
    _liferayExtension=addExtension(PLUGIN_NAME,LiferayExtension.class);
  }
}

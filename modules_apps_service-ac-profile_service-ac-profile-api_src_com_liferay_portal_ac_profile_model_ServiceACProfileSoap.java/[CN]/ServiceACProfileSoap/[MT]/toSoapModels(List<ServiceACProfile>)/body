{
  List<ServiceACProfileSoap> soapModels=new ArrayList<ServiceACProfileSoap>(models.size());
  for (  ServiceACProfile model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new ServiceACProfileSoap[soapModels.size()]);
}

{
  resourceLocalService.addModelResources(documentType.getCompanyId(),documentType.getGroupId(),documentType.getUserId(),DLDocumentType.class.getName(),documentType.getDocumentTypeId(),communityPermissions,guestPermissions);
}

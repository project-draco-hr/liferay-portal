{
  WikiPage wikiPage=_wikiPageLocalService.fetchLatestPage(GetterUtil.getLong(primKey),WorkflowConstants.STATUS_ANY,true);
  if (wikiPage == null) {
    return;
  }
  wikiPage.setModifiedDate(new Date());
  _wikiPageLocalService.updateWikiPage(wikiPage);
}

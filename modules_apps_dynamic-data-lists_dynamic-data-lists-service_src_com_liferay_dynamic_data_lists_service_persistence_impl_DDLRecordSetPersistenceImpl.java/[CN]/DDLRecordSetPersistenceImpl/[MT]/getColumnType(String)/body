{
  Integer type=DDLRecordSetModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DDLRecordSetModelImpl.TABLE_NAME);
  }
  return type;
}

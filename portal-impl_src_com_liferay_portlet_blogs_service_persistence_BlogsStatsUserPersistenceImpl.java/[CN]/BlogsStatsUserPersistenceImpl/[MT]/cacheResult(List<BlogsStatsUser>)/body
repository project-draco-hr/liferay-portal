{
  for (  BlogsStatsUser blogsStatsUser : blogsStatsUsers) {
    if (EntityCacheUtil.getResult(BlogsStatsUserModelImpl.ENTITY_CACHE_ENABLED,BlogsStatsUser.class,blogsStatsUser.getPrimaryKey(),this) == null) {
      cacheResult(blogsStatsUser);
    }
  }
}

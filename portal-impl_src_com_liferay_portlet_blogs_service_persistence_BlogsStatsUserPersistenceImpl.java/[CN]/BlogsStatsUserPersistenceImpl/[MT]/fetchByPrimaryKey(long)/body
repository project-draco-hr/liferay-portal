{
  Session session=null;
  try {
    session=openSession();
    return (BlogsStatsUser)session.get(BlogsStatsUserImpl.class,new Long(statsUserId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

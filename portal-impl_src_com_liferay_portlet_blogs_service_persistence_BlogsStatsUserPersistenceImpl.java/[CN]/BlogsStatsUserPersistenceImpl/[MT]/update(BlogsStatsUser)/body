{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BlogsStatsUser blogsStatsUser) method. To improve performance, call update(BlogsStatsUser blogsStatsUser, boolean merge).");
  }
  return update(blogsStatsUser,false);
}

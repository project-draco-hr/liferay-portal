{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BlogsStatsUser blogsStatsUser) method. Use update(BlogsStatsUser blogsStatsUser, boolean merge) instead.");
  }
  return update(blogsStatsUser,false);
}

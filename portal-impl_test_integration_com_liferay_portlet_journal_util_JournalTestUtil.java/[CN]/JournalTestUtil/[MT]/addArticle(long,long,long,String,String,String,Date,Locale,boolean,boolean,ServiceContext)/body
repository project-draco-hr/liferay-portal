{
  Map<Locale,String> titleMap=new HashMap<Locale,String>();
  for (  Locale locale : _locales) {
    titleMap.put(locale,title);
  }
  Map<Locale,String> descriptionMap=new HashMap<Locale,String>();
  for (  Locale locale : _locales) {
    descriptionMap.put(locale,description);
  }
  Map<Locale,String> contentMap=new HashMap<Locale,String>();
  for (  Locale locale : _locales) {
    contentMap.put(locale,content);
  }
  return addArticle(groupId,folderId,classNameId,titleMap,descriptionMap,contentMap,expirationDate,defaultLocale,workflowEnabled,approved,serviceContext);
}

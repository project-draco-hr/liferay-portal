{
  Map<Locale,String> titleMap=new HashMap<Locale,String>();
  for (  Locale locale : _locales) {
    titleMap.put(locale,title);
  }
  Map<Locale,String> descriptionMap=new HashMap<Locale,String>();
  for (  Locale locale : _locales) {
    descriptionMap.put(locale,title);
  }
  ServiceContext serviceContext=ServiceTestUtil.getServiceContext();
  if (workflowEnabled) {
    serviceContext.setWorkflowAction(WorkflowConstants.ACTION_SAVE_DRAFT);
    if (approved) {
      serviceContext.setWorkflowAction(WorkflowConstants.ACTION_PUBLISH);
    }
  }
  String localizedContent=createLocalizedContent(content,defaultLocale);
  boolean indexable=true;
  return JournalArticleLocalServiceUtil.addArticle(TestPropsValues.getUserId(),groupId,folderId,JournalArticleConstants.CLASSNAME_ID_DEFAULT,0,StringPool.BLANK,true,JournalArticleConstants.VERSION_DEFAULT,titleMap,descriptionMap,localizedContent,"general",null,null,null,1,1,1965,0,0,0,0,0,0,0,true,0,0,0,0,0,true,indexable,false,null,null,null,null,serviceContext);
}

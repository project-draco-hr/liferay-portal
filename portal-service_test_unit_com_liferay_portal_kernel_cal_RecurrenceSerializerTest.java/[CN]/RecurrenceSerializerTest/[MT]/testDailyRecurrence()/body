{
  Calendar recurrenceCalendar=new GregorianCalendar(2010,0,2,3,4,5);
  Recurrence recurrence=getRecurrence(recurrenceCalendar,Recurrence.DAILY);
  recurrence.setWeekStart(Calendar.SUNDAY);
  recurrence.setInterval(1);
  Assert.assertEquals("0 4 3 1/1 * ? *",toCronText(recurrence));
}

{
  Calendar recurrenceCalendar=new GregorianCalendar(2010,0,2,3,4,5);
  Recurrence recurrence=getRecurrence(recurrenceCalendar,Recurrence.NO_RECURRENCE);
  Assert.assertEquals("0 4 3 2 1 ? 2010",toCronText(recurrence));
}

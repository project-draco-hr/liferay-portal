{
  List<DLFolder> dlFolders=dlFolderPersistence.findByCompanyId(companyId);
  for (  DLFolder dlFolder : dlFolders) {
    dlFolder.setTreePath(dlFolder.buildTreePath());
    dlFolderPersistence.update(dlFolder);
  }
}

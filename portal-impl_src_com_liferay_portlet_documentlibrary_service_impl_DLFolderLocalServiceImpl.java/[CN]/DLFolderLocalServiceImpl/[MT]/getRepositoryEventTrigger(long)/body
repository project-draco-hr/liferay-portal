{
  LocalRepository localRepository=RepositoryProviderUtil.getFolderLocalRepository(folderId);
  if (!localRepository.isCapabilityProvided(RepositoryEventTriggerCapability.class)) {
    return _dummyRepositoryEventTrigger;
  }
  return localRepository.getCapability(RepositoryEventTriggerCapability.class);
}

{
  Iterator itr=dlFolderPersistence.findByG_P(groupId,DLFolderImpl.DEFAULT_PARENT_FOLDER_ID).iterator();
  while (itr.hasNext()) {
    DLFolder folder=(DLFolder)itr.next();
    deleteFolder(folder);
  }
}

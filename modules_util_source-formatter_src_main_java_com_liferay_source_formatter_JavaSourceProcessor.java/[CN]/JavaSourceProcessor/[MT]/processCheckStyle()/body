{
  if (!portalSource) {
    return;
  }
  File configurationFile=getFile("checkstyle.xml",PORTAL_MAX_DIR_LEVEL);
  List<SourceFormatterMessage> sourceFormatterMessages=CheckStyleUtil.process(getAbsolutePath(configurationFile),_ungeneratedFiles);
  for (  SourceFormatterMessage sourceFormatterMessage : sourceFormatterMessages) {
    printError(sourceFormatterMessage.getFileName(),sourceFormatterMessage.toString());
  }
}

{
  Matcher matcher=_upgradeExtendsPattern.matcher(content);
  if (!matcher.find()) {
    return;
  }
  if (content.contains("\tupgradeTable(") && !content.contains("void upgradeTable(")) {
    processErrorMessage(fileName,"Use UpgradeProcess.alter: " + fileName);
  }
}

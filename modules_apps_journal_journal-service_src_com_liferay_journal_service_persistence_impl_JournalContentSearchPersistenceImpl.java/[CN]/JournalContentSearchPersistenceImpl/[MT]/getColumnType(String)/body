{
  Integer type=JournalContentSearchModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ JournalContentSearchModelImpl.TABLE_NAME);
  }
  return type;
}

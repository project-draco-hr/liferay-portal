{
  String setting1=null;
  String setting2=null;
  if (reportInstance != null) {
    try {
      JSONObject jsonObj=JSONFactoryUtil.createJSONObject(reportInstance.getTypeSettings());
      setting1=jsonObj.getString("setting1");
      setting2=jsonObj.getString("setting2");
    }
 catch (    JSONException jsone) {
    }
  }
  context.put("setting1",setting1);
  context.put("setting2",setting2);
}

{
  WikiRequestHelper wikiRequestHelper=new WikiRequestHelper(request);
  WikiGroupServiceOverriddenConfiguration wikiGroupServiceConfiguration=wikiRequestHelper.getWikiGroupServiceSettings();
  return wikiGroupServiceConfiguration.enableRss();
}

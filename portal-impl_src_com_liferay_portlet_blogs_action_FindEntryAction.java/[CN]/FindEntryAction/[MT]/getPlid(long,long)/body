{
  if (plid != LayoutImpl.DEFAULT_PLID) {
    try {
      Layout layout=LayoutLocalServiceUtil.getLayout(plid);
      LayoutTypePortlet layoutTypePortlet=(LayoutTypePortlet)layout.getLayoutType();
      if (layoutTypePortlet.hasPortletId(PortletKeys.BLOGS)) {
        return plid;
      }
    }
 catch (    NoSuchLayoutException nsle) {
    }
  }
  BlogsEntry entry=BlogsEntryLocalServiceUtil.getEntry(entryId);
  plid=PortalUtil.getPlidFromPortletId(entry.getGroupId(),false,PortletKeys.BLOGS);
  if (plid == LayoutImpl.DEFAULT_PLID) {
    throw new NoSuchLayoutException("No public page was found with the Blogs portlet.");
  }
 else {
    return plid;
  }
}

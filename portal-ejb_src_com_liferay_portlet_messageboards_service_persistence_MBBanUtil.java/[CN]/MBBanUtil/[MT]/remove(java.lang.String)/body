{
  ModelListener listener=_getListener();
  if (listener != null) {
    listener.onBeforeRemove(findByPrimaryKey(banId));
  }
  com.liferay.portlet.messageboards.model.MBBan mbBan=getPersistence().remove(banId);
  if (listener != null) {
    listener.onAfterRemove(mbBan);
  }
  return mbBan;
}

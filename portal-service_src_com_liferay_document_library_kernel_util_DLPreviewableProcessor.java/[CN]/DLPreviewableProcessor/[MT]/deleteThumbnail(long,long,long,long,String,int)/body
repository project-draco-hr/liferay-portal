{
  try {
    String thumbnailFilePath=getThumbnailFilePath(groupId,fileEntryId,fileVersionId,thumbnailType,index);
    DLStoreUtil.deleteDirectory(companyId,REPOSITORY_ID,thumbnailFilePath);
  }
 catch (  Exception e) {
  }
}

{
  int ptr=mOutputPtr;
  if ((ptr + 3) >= mOutputBufLen) {
    if (mOut == null) {
      return;
    }
    flushBuffer();
    ptr=mOutputPtr;
  }
  char[] buf=mOutputBuffer;
  buf[ptr++]=' ';
  buf[ptr++]='/';
  buf[ptr++]='>';
  mOutputPtr=ptr;
}

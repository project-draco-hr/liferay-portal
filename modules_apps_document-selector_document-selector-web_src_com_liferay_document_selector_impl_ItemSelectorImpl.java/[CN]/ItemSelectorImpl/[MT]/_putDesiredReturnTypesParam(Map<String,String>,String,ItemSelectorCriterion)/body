{
  Set<Class<?>> desiredReturnTypes=itemSelectorCriterion.getDesiredReturnTypes();
  Set<Class<?>> availableReturnTypes=itemSelectorCriterion.getAvailableReturnTypes();
  if (desiredReturnTypes.size() != availableReturnTypes.size()) {
    params.put(paramPrefix + "desiredReturnTypes",_getCommaSeparatedList(desiredReturnTypes,new Function<Class<?>,String>(){
      @Override public String apply(      Class<?> clazz){
        return clazz.getName();
      }
    }
));
  }
}

{
  try {
    LayoutTypeAccessPolicy layoutTypeAccessPolicy=getLayoutTypeAccessPolicy();
    return layoutTypeAccessPolicy.isViewLayoutAllowed(PermissionThreadLocal.getPermissionChecker(),getLayout());
  }
 catch (  PortalException pe) {
    _log.error(pe);
    return false;
  }
}

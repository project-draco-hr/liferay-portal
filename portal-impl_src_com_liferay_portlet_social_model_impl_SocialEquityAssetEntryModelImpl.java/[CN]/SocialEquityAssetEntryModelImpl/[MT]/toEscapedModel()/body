{
  if (isEscapedModel()) {
    return (SocialEquityAssetEntry)this;
  }
 else {
    return (SocialEquityAssetEntry)Proxy.newProxyInstance(SocialEquityAssetEntry.class.getClassLoader(),new Class[]{SocialEquityAssetEntry.class},new AutoEscapeBeanHandler(this));
  }
}

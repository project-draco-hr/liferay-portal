{
  if (isEscapedModel()) {
    return (SocialEquityAssetEntry)this;
  }
 else {
    return (SocialEquityAssetEntry)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

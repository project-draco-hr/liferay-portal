{
  PortletPreferences prefs=req.getPreferences();
  String movie=prefs.getValue("movie",StringPool.BLANK);
  if (Validator.isNull(movie)) {
    return mapping.findForward("/portal/portlet_not_setup");
  }
 else {
    return mapping.findForward("portlet.flash.view");
  }
}

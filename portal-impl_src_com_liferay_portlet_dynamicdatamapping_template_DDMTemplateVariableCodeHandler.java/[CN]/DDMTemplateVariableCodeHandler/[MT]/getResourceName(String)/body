{
  if (isCommonResource(dataType)) {
    dataType="common";
  }
  StringBundler sb=new StringBundler(3);
  sb.append(_templatePath);
  sb.append(dataType);
  sb.append(".ftl");
  return sb.toString();
}

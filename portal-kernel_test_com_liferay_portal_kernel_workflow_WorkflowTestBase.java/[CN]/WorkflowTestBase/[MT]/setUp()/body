{
  synchronousMessageSender=context.mock(SingleDestinationSynchronousMessageSender.class);
  workflowDefinitionManagerProxy=new WorkflowDefinitionManagerProxy(synchronousMessageSender);
  workflowInstanceManagerProxy=new WorkflowInstanceManagerProxy(synchronousMessageSender);
  taskInstanceManagerProxy=new TaskInstanceManagerProxy(synchronousMessageSender);
  factory=context.mock(UserCredentialFactory.class);
  new WorkflowUtil(workflowDefinitionManagerProxy,workflowInstanceManagerProxy,taskInstanceManagerProxy,null,factory);
}

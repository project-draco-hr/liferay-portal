{
  SocialEquityLog socialEquityLog=findByPrimaryKey(equityLogId);
  Session session=null;
  try {
    session=openSession();
    SocialEquityLog[] array=new SocialEquityLogImpl[3];
    array[0]=getByUserId_PrevAndNext(session,socialEquityLog,userId,orderByComparator,true);
    array[1]=socialEquityLog;
    array[2]=getByUserId_PrevAndNext(session,socialEquityLog,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

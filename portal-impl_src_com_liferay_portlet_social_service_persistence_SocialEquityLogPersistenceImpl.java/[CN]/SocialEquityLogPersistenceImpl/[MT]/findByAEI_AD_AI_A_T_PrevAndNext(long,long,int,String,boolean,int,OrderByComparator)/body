{
  SocialEquityLog socialEquityLog=findByPrimaryKey(equityLogId);
  Session session=null;
  try {
    session=openSession();
    SocialEquityLog[] array=new SocialEquityLogImpl[3];
    array[0]=getByAEI_AD_AI_A_T_PrevAndNext(session,socialEquityLog,assetEntryId,actionDate,actionId,active,type,orderByComparator,true);
    array[1]=socialEquityLog;
    array[2]=getByAEI_AD_AI_A_T_PrevAndNext(session,socialEquityLog,assetEntryId,actionDate,actionId,active,type,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  SocialEquityLog socialEquityLog=findByPrimaryKey(equityLogId);
  Session session=null;
  try {
    session=openSession();
    SocialEquityLog[] array=new SocialEquityLogImpl[3];
    array[0]=getByU_AEI_A_A_PrevAndNext(session,socialEquityLog,userId,assetEntryId,actionId,active,orderByComparator,true);
    array[1]=socialEquityLog;
    array[2]=getByU_AEI_A_A_PrevAndNext(session,socialEquityLog,userId,assetEntryId,actionId,active,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

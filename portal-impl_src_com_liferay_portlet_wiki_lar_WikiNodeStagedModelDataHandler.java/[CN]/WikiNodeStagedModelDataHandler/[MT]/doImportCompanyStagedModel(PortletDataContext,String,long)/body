{
  WikiNode existingNode=WikiNodeLocalServiceUtil.fetchNodeByUuidAndGroupId(uuid,portletDataContext.getCompanyGroupId());
  Map<Long,Long> nodeIds=(Map<Long,Long>)portletDataContext.getNewPrimaryKeysMap(WikiNode.class);
  nodeIds.put(nodeId,existingNode.getNodeId());
}

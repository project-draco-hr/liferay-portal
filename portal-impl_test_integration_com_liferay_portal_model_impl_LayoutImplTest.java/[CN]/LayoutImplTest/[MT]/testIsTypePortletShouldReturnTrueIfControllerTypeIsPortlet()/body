{
  LayoutType layoutType=_layout.getLayoutType();
  Field declaredField=ReflectionUtil.getDeclaredField(LayoutTypeImpl.class,"_layoutTypeController");
  declaredField.set(layoutType,new LayoutTypeControllerImpl(LayoutConstants.TYPE_PORTLET));
  for (  String type : _TYPES) {
    if (type.equals(LayoutConstants.TYPE_PORTLET)) {
      continue;
    }
    _layout.setType(type);
    Assert.assertTrue(_layout.isTypePortlet());
  }
}

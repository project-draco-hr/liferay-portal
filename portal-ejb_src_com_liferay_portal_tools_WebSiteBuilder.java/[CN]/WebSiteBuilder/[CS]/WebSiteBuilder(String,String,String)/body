{
  try {
    _orionWebXmlDevelopment=System.getProperty("app.server.orion.web.xml.development");
    _orionWebXmlJspCheck=System.getProperty("app.server.orion.web.xml.jsp.check");
    _portalExtProperties=portalExtProperties;
    _availableWebApps=availableWebApps;
    _orionConfigDir=orionConfigDir;
    List webSites=getWebSites();
    _buildOrionASP(webSites);
    _buildWebSites(webSites);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

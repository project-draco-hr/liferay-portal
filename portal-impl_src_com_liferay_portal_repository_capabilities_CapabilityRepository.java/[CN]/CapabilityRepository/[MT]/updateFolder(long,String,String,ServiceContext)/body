{
  Repository repository=getRepository();
  Folder folder=repository.updateFolder(folderId,title,description,serviceContext);
  _repositoryEventTrigger.trigger(RepositoryEventType.Update.class,Folder.class,folder);
  return folder;
}

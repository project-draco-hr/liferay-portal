{
  Repository repository=getRepository();
  _repositoryEventTrigger.trigger(RepositoryEventType.Delete.class,Repository.class,repository);
  SyncCapability syncCapability=getInternalCapability(SyncCapability.class);
  if (syncCapability != null) {
    syncCapability.destroyDocumentRepository(this);
  }
  repository.deleteAll();
}

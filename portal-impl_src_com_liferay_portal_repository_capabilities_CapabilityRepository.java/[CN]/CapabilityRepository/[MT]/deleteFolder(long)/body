{
  Repository repository=getRepository();
  Folder folder=repository.getFolder(folderId);
  _repositoryEventHandler.trigger(RepositoryEventType.Delete.class,Folder.class,folder);
  repository.deleteFolder(folderId);
}

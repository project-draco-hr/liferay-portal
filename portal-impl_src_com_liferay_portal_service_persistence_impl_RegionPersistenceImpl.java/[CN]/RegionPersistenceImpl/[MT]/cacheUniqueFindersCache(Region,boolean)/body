{
  if (isNew) {
    Object[] args=new Object[]{region.getCountryId(),region.getRegionCode()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_R,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_R,args,region);
  }
 else {
    RegionModelImpl regionModelImpl=(RegionModelImpl)region;
    if ((regionModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_C_R.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{region.getCountryId(),region.getRegionCode()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_R,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_R,args,region);
    }
  }
}

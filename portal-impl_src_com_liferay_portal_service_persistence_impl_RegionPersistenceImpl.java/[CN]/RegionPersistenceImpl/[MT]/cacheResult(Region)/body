{
  entityCache.putResult(RegionModelImpl.ENTITY_CACHE_ENABLED,RegionImpl.class,region.getPrimaryKey(),region);
  finderCache.putResult(FINDER_PATH_FETCH_BY_C_R,new Object[]{region.getCountryId(),region.getRegionCode()},region);
  region.resetOriginalValues();
}

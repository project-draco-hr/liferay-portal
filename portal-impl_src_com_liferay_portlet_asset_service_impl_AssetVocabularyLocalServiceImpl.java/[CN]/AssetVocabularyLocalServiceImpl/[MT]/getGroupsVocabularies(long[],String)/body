{
  List<AssetVocabulary> groupVocabularies=assetVocabularyPersistence.findByGroupId(groupIds);
  if (Validator.isNull(className)) {
    return groupVocabularies;
  }
  return AssetUtil.filterVocabulariesBySelectedClassName(groupVocabularies,className);
}

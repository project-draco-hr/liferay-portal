{
  if (isEscapedModel()) {
    return (ExpandoRowValues)this;
  }
 else {
    ExpandoRowValues model=new ExpandoRowValuesImpl();
    model.setEscapedModel(true);
    model.setRowId(getRowId());
    model.setValueId(getValueId());
    model=(ExpandoRowValues)Proxy.newProxyInstance(ExpandoRowValues.class.getClassLoader(),new Class[]{ExpandoRowValues.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

{
  wallEntry=toUnwrappedModel(wallEntry);
  Session session=null;
  try {
    session=openSession();
    if (!session.contains(wallEntry)) {
      wallEntry=(WallEntry)session.get(WallEntryImpl.class,wallEntry.getPrimaryKeyObj());
    }
    if (wallEntry != null) {
      session.delete(wallEntry);
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  if (wallEntry != null) {
    clearCache(wallEntry);
  }
  return wallEntry;
}

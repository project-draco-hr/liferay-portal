{
  RepositoryEntry repositoryEntry=repositoryEntryPersistence.fetchByPrimaryKey(fileEntryId);
  if (repositoryEntry == null) {
    throw new NoSuchFileEntryException("No CMIS file entry with fileEntryId=" + fileEntryId);
  }
  return new ObjectIdImpl(repositoryEntry.getMappedId());
}

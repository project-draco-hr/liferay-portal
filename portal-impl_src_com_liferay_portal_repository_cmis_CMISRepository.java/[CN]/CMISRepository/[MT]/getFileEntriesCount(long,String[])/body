{
  try {
    Session session=getSession();
    List<String> cmisDocumentIds=getCmisDocumentIds(session,folderId,mimeTypes);
    return cmisDocumentIds.size();
  }
 catch (  PortalException pe) {
    throw new RepositoryException(pe);
  }
}

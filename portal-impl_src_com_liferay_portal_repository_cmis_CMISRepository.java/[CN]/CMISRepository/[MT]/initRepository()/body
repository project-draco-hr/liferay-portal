{
  try {
    Session session=getSession();
    session.getRepositoryInfo();
  }
 catch (  Exception e) {
    processException(e);
    throw new RepositoryException("Unable to initialize CMIS session",e);
  }
}

{
  try {
    Session session=getSession();
    session.getRepositoryInfo();
  }
 catch (  IllegalArgumentException iae) {
    throw new RepositoryException("Unable to initialize CMIS session",iae);
  }
}

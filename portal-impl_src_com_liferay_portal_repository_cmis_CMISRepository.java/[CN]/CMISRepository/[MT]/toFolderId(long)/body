{
  RepositoryEntry repositoryEntry=repositoryEntryPersistence.fetchByPrimaryKey(folderId);
  if (repositoryEntry == null) {
    throw new NoSuchFolderException("No CMIS folder with folderId=" + folderId);
  }
  return new ObjectIdImpl(repositoryEntry.getMappedId());
}

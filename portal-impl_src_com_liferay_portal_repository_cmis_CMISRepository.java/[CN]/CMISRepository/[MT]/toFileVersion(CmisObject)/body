{
  if (object instanceof Document) {
    return new CMISFileVersion(this,getRepositoryId(),toRepositoryEntryId(object.getId()),(Document)object);
  }
 else {
    throw new NoSuchFileVersionException("CMIS object is not a file version " + object);
  }
}

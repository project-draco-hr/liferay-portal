{
  try {
    Session session=getSession();
    session.getRootFolder().deleteTree(true,UnfileObject.DELETE,true);
  }
 catch (  Exception e) {
    String repositoryId=getTypeSettingsProperties().getProperty(REPOSITORY_ID);
    _log.error("Unexpected error deleting CMIS repository " + repositoryId,e);
  }
}

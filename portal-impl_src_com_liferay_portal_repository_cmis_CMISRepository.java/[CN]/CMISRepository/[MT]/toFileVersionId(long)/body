{
  RepositoryEntry repositoryEntry=repositoryEntryPersistence.fetchByPrimaryKey(fileVersionId);
  if (repositoryEntry == null) {
    throw new NoSuchFileEntryException("No CMIS file version with fileVersionId=" + fileVersionId);
  }
  return new ObjectIdImpl(repositoryEntry.getMappedId());
}

{
  String objectId=getId(session,folderId,true,title);
  if (objectId == null) {
    objectId=getId(session,folderId,false,title);
  }
  if (objectId == null) {
    return false;
  }
 else {
    return true;
  }
}

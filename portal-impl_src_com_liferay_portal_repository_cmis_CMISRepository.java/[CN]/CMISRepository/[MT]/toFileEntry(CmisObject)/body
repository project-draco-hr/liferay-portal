{
  if (cmisObject instanceof Document) {
    return new CMISFileEntry(this,getRepositoryId(),toRepositoryEntryId(cmisObject.getId()),(Document)cmisObject);
  }
 else {
    throw new NoSuchFileEntryException("CMIS object is not a file entry " + cmisObject);
  }
}

{
  if (object instanceof Document) {
    return new CMISFileEntry(this,getRepositoryId(),toEntryId(object.getId()),(Document)object);
  }
 else {
    throw new NoSuchFileEntryException("CMIS object is not a file entry " + object);
  }
}

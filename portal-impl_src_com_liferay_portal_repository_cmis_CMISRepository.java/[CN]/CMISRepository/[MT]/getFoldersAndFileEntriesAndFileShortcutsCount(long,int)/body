{
  List<Long> folderIds=new ArrayList<Long>(1);
  folderIds.add(folderId);
  int count=getFoldersFileEntriesCount(folderIds,status);
  count+=dlAppHelperLocalService.getFileShortcutsCount(getGroupId(),folderId,status);
  return count;
}

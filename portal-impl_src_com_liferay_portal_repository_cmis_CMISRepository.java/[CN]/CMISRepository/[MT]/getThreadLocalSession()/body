{
  HttpSession httpSession=HttpSessionThreadLocal.getHttpSession();
  if (httpSession == null) {
    return null;
  }
  TransientWrapper<Session> transientWrapper=(TransientWrapper<Session>)httpSession.getAttribute(SESSION_ATTRIBUTE_KEY);
  if (transientWrapper == null) {
    return null;
  }
  return transientWrapper.getTransientObject();
}

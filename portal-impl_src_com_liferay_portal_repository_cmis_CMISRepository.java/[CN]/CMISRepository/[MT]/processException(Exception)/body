{
  if (e instanceof CmisRuntimeException && e.getMessage().contains("authorized")) {
    throw new PrincipalException(e);
  }
 else   if (e instanceof CmisPermissionDeniedException) {
    throw new PrincipalException(e);
  }
}

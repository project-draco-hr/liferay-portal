{
  if ((e instanceof CmisRuntimeException && e.getMessage().contains("authorized")) || (e instanceof CmisPermissionDeniedException)) {
    String message=e.getMessage();
    try {
      message="Unable to login with user " + getLogin();
    }
 catch (    Exception e2) {
    }
    throw new PrincipalException(message,e);
  }
}

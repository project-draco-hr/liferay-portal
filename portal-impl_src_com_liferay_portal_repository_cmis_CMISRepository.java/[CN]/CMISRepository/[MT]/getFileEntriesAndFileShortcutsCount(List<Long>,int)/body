{
  int count=0;
  for (  long folderId : folderIds) {
    count+=getFileEntriesCount(folderId);
  }
  count+=dlAppHelperLocalService.getFileShortcutsCount(getGroupId(),folderIds,status);
  return count;
}

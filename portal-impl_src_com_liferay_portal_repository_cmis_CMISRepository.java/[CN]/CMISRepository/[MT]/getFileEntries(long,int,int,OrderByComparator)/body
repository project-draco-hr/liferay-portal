{
  try {
    Session session=getSession();
    List<String> objectIds=getObjectIds(session,folderId,true,start,end,obc);
    List<FileEntry> fileEntries=new ArrayList<FileEntry>(objectIds.size());
    for (    String objectId : objectIds) {
      Document document=(Document)session.getObject(new ObjectIdImpl(objectId));
      fileEntries.add(toFileEntry(document));
    }
    return fileEntries;
  }
 catch (  SystemException se) {
    throw se;
  }
catch (  Exception e) {
    throw new RepositoryException(e);
  }
}

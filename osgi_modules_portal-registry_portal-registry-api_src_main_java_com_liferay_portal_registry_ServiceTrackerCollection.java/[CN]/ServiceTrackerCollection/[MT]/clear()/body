{
  Iterator<Entry<S,ServiceRegistration<S>>> iterator=_serviceRegistrations.entrySet().iterator();
  while (iterator.hasNext()) {
    Entry<S,ServiceRegistration<S>> entry=iterator.next();
    entry.getValue().unregister();
    iterator.remove();
  }
}

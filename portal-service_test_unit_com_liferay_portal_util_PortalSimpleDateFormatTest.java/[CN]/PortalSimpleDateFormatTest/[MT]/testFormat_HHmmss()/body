{
  DateFormat dateFormat=new PortalSimpleDateFormat("HHmmss",LocaleUtil.getDefault());
  dateFormat.setTimeZone(TimeZoneUtil.getDefault());
  Assert.assertEquals("224000",dateFormat.format(getTime()));
}

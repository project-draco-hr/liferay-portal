{
  WebDriverWait webDriverWait=new WebDriverWait(this,TestPropsValues.TIMEOUT_IMPLICIT_WAIT);
  if (locator.startsWith("//")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.xpath(locator)));
  }
 else   if (locator.startsWith("class=")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.className(locator.substring(6))));
  }
 else   if (locator.startsWith("css=")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.cssSelector(locator.substring(4))));
  }
 else   if (locator.startsWith("link=")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.linkText(locator.substring(5))));
  }
 else   if (locator.startsWith("name=")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.name(locator.substring(5))));
  }
 else   if (locator.startsWith("tag=")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.tagName(locator.substring(4))));
  }
 else   if (locator.startsWith("xpath=") || locator.startsWith("xPath=")) {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.xpath(locator.substring(6))));
  }
 else {
    return webDriverWait.until(ExpectedConditions.presenceOfElementLocated(By.id(locator)));
  }
}

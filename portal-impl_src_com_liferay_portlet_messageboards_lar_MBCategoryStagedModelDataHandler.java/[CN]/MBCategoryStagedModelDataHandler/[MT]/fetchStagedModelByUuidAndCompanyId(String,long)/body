{
  List<MBCategory> categories=MBCategoryLocalServiceUtil.getMBCategoriesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<MBCategory>());
  if (ListUtil.isEmpty(categories)) {
    return null;
  }
  return categories.get(0);
}

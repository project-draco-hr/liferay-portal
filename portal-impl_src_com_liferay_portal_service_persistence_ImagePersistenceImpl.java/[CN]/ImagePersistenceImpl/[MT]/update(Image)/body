{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Image image) method. To improve performance, call update(Image image, boolean merge).");
  }
  return update(image,false);
}

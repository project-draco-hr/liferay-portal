{
  Image result=(Image)EntityCacheUtil.getResult(ImageModelImpl.ENTITY_CACHE_ENABLED,Image.class,imageId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      Image image=(Image)session.get(ImageImpl.class,new Long(imageId));
      cacheResult(image);
      return image;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (Image)result;
  }
}

{
  RestConfigurator restConfigurator=new RestConfigurator();
  restConfigurator.setRestActionsManager(RestActionsManagerUtil.getRestActionsManager());
  try {
    restConfigurator.configure(classLoader);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}

{
  DLFolder dlFolder=getFolder();
  while (dlFolder != null) {
    if (dlFolder.isInTrash()) {
      return dlFolder;
    }
    try {
      dlFolder=dlFolder.getParentFolder();
    }
 catch (    Exception e) {
      return null;
    }
  }
  return null;
}

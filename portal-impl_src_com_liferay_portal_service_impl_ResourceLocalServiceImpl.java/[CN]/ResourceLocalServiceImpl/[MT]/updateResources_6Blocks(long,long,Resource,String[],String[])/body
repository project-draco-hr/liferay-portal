{
  Role role=roleLocalService.getDefaultGroupRole(groupId);
  resourceBlockLocalService.setIndividualScopePermissions(companyId,groupId,resource.getName(),GetterUtil.getLong(resource.getPrimKey()),role.getRoleId(),Arrays.asList(groupPermissions));
  role=roleLocalService.getRole(companyId,RoleConstants.GUEST);
  resourceBlockLocalService.setIndividualScopePermissions(companyId,groupId,resource.getName(),GetterUtil.getLong(resource.getPrimKey()),role.getRoleId(),Arrays.asList(guestPermissions));
}

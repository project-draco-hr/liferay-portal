{
  ModelPermissions modelPermissions=ModelPermissionsFactory.create(companyId,groupId,groupPermissions,guestPermissions);
  addModelResources(companyId,groupId,userId,resource,modelPermissions,permissionedModel);
}

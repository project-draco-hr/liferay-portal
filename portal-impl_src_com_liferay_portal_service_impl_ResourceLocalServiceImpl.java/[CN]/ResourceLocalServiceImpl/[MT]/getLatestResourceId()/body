{
  List<Resource> resources=resourcePersistence.findAll(0,1,new ResourceComparator());
  if (resources.size() == 0) {
    return 0;
  }
 else {
    Resource resource=resources.get(0);
    return resource.getResourceId();
  }
}

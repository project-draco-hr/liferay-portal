{
  Role role=getRole(groupId);
  resourcePermissionLocalService.setResourcePermissions(role.getRoleId(),communityPermissions,resource.getResourceId());
  role=roleLocalService.getRole(companyId,RoleConstants.GUEST);
  resourcePermissionLocalService.setResourcePermissions(role.getRoleId(),guestPermissions,resource.getResourceId());
}

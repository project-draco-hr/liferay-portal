{
  Role guestRole=roleLocalService.getRole(companyId,RoleConstants.GUEST);
  resourceBlockLocalService.setIndividualScopePermissions(resource.getCompanyId(),groupId,resource.getName(),GetterUtil.getLong(resource.getPrimKey()),guestRole.getRoleId(),actionIds);
}

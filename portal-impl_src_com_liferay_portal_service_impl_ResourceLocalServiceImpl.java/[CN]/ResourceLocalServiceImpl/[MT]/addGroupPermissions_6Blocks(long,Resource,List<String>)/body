{
  Role role=roleLocalService.getDefaultGroupRole(groupId);
  resourceBlockLocalService.setIndividualScopePermissions(resource.getCompanyId(),groupId,resource.getName(),GetterUtil.getLong(resource.getPrimKey()),role.getRoleId(),actionIds);
}

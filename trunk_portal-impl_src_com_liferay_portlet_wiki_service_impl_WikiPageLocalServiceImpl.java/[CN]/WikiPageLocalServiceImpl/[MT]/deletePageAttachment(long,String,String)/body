{
  if (Validator.isNull(fileName)) {
    return;
  }
  WikiPage page=getPage(nodeId,title);
  socialEquityLogLocalService.deactivateEquityLogs(WikiPage.class.getName(),page.getResourcePrimKey(),ActionKeys.ADD_ATTACHMENT,fileName.substring(1));
  long companyId=page.getCompanyId();
  String portletId=CompanyConstants.SYSTEM_STRING;
  long repositoryId=CompanyConstants.SYSTEM;
  try {
    dlLocalService.deleteFile(companyId,portletId,repositoryId,fileName);
  }
 catch (  NoSuchFileException nsfe) {
  }
}

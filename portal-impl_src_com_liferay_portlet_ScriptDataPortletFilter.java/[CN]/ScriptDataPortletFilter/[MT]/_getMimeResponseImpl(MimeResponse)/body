{
  while (!(mimeResponse instanceof MimeResponseImpl) && (mimeResponse instanceof PortletResponseWrapper)) {
    PortletResponseWrapper portletResponseWrapper=(PortletResponseWrapper)mimeResponse;
    mimeResponse=(MimeResponse)portletResponseWrapper.getResponse();
  }
  return (MimeResponseImpl)mimeResponse;
}

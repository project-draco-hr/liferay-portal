{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  RatingsEntry entry=RatingsEntryLocalServiceUtil.fetchRatingsEntryByUuidAndCompanyId(uuid,group.getCompanyId());
  if (entry != null) {
    deleteStagedModel(entry);
  }
}

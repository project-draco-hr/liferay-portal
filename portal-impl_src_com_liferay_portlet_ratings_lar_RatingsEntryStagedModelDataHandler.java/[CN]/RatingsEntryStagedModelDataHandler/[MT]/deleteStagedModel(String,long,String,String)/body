{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  RatingsEntry entry=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  if (entry != null) {
    RatingsEntryLocalServiceUtil.deleteRatingsEntry(entry);
  }
}

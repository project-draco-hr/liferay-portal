{
  Integer type=JournalFolderModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ JournalFolderModelImpl.TABLE_NAME);
  }
  return type;
}

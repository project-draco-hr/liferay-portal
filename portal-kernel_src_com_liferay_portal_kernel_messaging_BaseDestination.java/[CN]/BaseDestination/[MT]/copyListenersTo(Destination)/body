{
  for (  MessageListener listener : _listeners) {
    InvokerMessageListener invokerListener=(InvokerMessageListener)listener;
    targetDestination.register(invokerListener.getMessageListener(),invokerListener.getClassLoader());
  }
}

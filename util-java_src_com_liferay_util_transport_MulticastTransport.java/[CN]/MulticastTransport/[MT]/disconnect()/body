{
  if (_address != null) {
    try {
      _socket.leaveGroup(_address);
      _address=null;
    }
 catch (    IOException e) {
      if (_log.isErrorEnabled()) {
        _log.error("Unable to leave group",e);
      }
    }
  }
  _connected=false;
  this.interrupt();
  _socket.close();
}

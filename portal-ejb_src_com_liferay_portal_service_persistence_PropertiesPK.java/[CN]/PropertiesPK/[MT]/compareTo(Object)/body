{
  if (obj == null) {
    return -1;
  }
  PropertiesPK pk=(PropertiesPK)obj;
  int value=0;
  value=companyId.compareTo(pk.companyId);
  if (value != 0) {
    return value;
  }
  value=type.compareTo(pk.type);
  if (value != 0) {
    return value;
  }
  return 0;
}

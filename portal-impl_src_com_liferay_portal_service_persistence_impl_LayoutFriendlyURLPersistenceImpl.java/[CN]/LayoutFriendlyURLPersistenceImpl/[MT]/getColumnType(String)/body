{
  Integer type=LayoutFriendlyURLModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ LayoutFriendlyURLModelImpl.TABLE_NAME);
  }
  return type;
}

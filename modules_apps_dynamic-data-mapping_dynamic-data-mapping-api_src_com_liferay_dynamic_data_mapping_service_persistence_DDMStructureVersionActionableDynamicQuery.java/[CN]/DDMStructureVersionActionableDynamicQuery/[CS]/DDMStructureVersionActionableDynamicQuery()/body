{
  setBaseLocalService(DDMStructureVersionLocalServiceUtil.getService());
  setClass(DDMStructureVersion.class);
  setClassLoader(PortalClassLoaderUtil.getClassLoader());
  setPrimaryKeyPropertyName("structureVersionId");
}

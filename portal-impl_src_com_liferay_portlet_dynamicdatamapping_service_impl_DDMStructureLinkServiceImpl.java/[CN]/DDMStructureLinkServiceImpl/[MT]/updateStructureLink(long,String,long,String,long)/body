{
  DDMStructurePermission.check(getPermissionChecker(),groupId,structureKey,ActionKeys.UPDATE);
  return ddmStructureLinkLocalService.updateStructureLink(structureLinkId,structureKey,groupId,className,classPK);
}

{
  BodyContent bodyContent=getBodyContent();
  _bodyContentString=bodyContent.getString();
  ServletRequest req=pageContext.getRequest();
  IntegerWrapper iconCount=(IntegerWrapper)req.getAttribute("liferay-ui:icon-list:icon-count");
  Boolean singleIcon=(Boolean)req.getAttribute("liferay-ui:icon-list:single-icon");
  if ((iconCount != null) && (iconCount.getValue() == 1) && (singleIcon == null)) {
    bodyContent.clearBody();
    req.setAttribute("liferay-ui:icon-list:single-icon",Boolean.TRUE);
    return EVAL_BODY_AGAIN;
  }
 else {
    return SKIP_BODY;
  }
}

{
  try {
    Role role=RoleLocalServiceUtil.getRole(getCompanyId(),Role.POWER_USER);
    return UserLocalServiceUtil.hasRoleUser(role.getRoleId(),getUserId());
  }
 catch (  Exception e) {
    return false;
  }
}

{
  List<Long> layoutIds;
  try {
    layoutIds=JournalContentSearchLocalServiceUtil.getLayoutIds(_groupId,_privateLayout,_articleId);
  }
 catch (  SystemException se) {
    throw new JspException(se);
  }
  pageContext.setAttribute(_var,layoutIds);
  return SKIP_BODY;
}

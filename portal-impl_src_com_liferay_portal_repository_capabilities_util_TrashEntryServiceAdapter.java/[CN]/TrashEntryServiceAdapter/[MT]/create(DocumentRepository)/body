{
  if (documentRepository instanceof LocalRepository) {
    return new TrashEntryServiceAdapter(TrashEntryLocalServiceUtil.getService());
  }
  return new TrashEntryServiceAdapter(TrashEntryLocalServiceUtil.getService(),TrashEntryServiceUtil.getService());
}

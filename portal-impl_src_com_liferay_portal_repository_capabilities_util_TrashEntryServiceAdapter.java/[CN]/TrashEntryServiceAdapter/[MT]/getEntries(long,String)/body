{
  List<TrashEntry> trashEntries=null;
  if (_trashEntryService != null) {
    trashEntries=_trashEntryService.getEntries(repositoryId,className);
  }
 else {
    trashEntries=_trashEntryLocalService.getEntries(repositoryId,className);
  }
  return trashEntries;
}

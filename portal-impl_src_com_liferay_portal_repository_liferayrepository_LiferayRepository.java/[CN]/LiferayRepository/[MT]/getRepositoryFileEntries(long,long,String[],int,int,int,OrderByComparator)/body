{
  List<DLFileEntry> dlFileEntries=dlFileEntryService.getGroupFileEntries(getGroupId(),userId,toFolderId(rootFolderId),getRepositoryId(),mimeTypes,status,start,end,obc);
  return toFileEntries(dlFileEntries);
}

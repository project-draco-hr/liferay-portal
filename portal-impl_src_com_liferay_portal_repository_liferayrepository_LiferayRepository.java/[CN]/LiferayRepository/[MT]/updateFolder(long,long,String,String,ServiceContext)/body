{
  initByFolder(folderId);
  DLFolder dlFolder=DLRepositoryServiceUtil.updateFolder(getFolderId(folderId),getFolderId(parentFolderId),title,description,serviceContext);
  return new LiferayFolder(dlFolder);
}

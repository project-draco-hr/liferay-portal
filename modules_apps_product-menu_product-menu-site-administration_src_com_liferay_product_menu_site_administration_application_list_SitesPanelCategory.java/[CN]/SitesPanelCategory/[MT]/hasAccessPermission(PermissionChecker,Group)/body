{
  List<PanelCategory> childPanelCategories=_panelCategoryRegistry.getChildPanelCategories(this,permissionChecker,group);
  if (childPanelCategories.isEmpty()) {
    return false;
  }
  return super.hasAccessPermission(permissionChecker,group);
}

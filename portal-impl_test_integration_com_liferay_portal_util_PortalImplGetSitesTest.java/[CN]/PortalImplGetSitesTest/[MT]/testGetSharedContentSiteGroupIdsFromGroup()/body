{
  Group group=GroupTestUtil.addGroup();
  _groups.add(group);
  _user=UserTestUtil.addGroupAdminUser(group);
  _users.add(_user);
  Assert.assertTrue(ArrayUtil.contains(getSharedContentSiteGroupIds(),group.getGroupId()));
}

{
  Layout layout=LayoutTestUtil.addLayout(_group);
  Group layoutScopeGroup=GroupTestUtil.addGroup(_groupAdminUser.getUserId(),layout);
  Assert.assertTrue(ArrayUtil.contains(getSharedContentSiteGroupIds(),layoutScopeGroup.getGroupId()));
}

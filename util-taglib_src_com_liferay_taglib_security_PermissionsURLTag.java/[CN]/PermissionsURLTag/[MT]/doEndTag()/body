{
  try {
    doTag(_redirect,_modelResource,_modelResourceDescription,_resourcePrimKey,_var,true,pageContext);
  }
 catch (  Exception e) {
    if (e instanceof JspException) {
      throw (JspException)e;
    }
 else {
      throw new JspException(e);
    }
  }
  return EVAL_PAGE;
}

{
  Iterator iterator=_listeners.iterator();
  while (iterator.hasNext()) {
    StateChangedListener listener=(StateChangedListener)iterator.next();
    listener.stateChanged(new StateChangedEvent(this));
  }
}

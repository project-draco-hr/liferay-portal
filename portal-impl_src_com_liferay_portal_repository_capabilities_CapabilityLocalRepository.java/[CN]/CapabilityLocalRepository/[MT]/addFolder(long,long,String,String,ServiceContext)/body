{
  LocalRepository repository=getRepository();
  Folder folder=repository.addFolder(userId,parentFolderId,title,description,serviceContext);
  _repositoryEventTrigger.trigger(RepositoryEventType.Add.class,Folder.class,folder);
  return folder;
}

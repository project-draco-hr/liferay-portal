{
  List<DDMTemplate> templates=DDMTemplateLocalServiceUtil.getDDMTemplatesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<DDMTemplate>());
  if (ListUtil.isEmpty(templates)) {
    return null;
  }
  return templates.get(0);
}

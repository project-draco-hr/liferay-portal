{
  if (stagedModel == null) {
    return DDMTemplate.class.getName();
  }
  DDMTemplate template=(DDMTemplate)stagedModel;
  return ManifestSummary.getManifestSummaryKey(DDMTemplate.class.getName(),template.getClassName());
}

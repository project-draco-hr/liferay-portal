{
  try {
    if (_clusterMasterExecutor.isMaster()) {
      _searchEngineHelper.initialize(company.getCompanyId());
    }
  }
 catch (  Exception e) {
    _log.error("Unable to initialize search engine for company " + company,e);
  }
}

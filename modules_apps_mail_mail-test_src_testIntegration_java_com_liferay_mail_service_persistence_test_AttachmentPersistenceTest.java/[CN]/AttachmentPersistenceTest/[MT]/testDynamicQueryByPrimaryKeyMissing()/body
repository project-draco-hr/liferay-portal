{
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(Attachment.class,_dynamicQueryClassLoader);
  dynamicQuery.add(RestrictionsFactoryUtil.eq("attachmentId",RandomTestUtil.nextLong()));
  List<Attachment> result=_persistence.findWithDynamicQuery(dynamicQuery);
  Assert.assertEquals(0,result.size());
}

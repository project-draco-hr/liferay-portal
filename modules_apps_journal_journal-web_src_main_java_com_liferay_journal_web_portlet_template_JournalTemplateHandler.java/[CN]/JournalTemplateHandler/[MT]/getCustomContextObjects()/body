{
  Map<String,Object> contextObjects=new HashMap<>();
  try {
    contextObjects.put("journalContentUtil",JournalContentUtil.getJournalContent());
  }
 catch (  SecurityException se) {
    _log.error(se,se);
  }
  return contextObjects;
}

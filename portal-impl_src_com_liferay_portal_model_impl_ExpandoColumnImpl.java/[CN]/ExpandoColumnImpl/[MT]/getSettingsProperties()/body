{
  if (_settingsProperties == null) {
    _settingsProperties=new SafeProperties();
    try {
      PropertiesUtil.load(_settingsProperties,super.getSettings());
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
  return _settingsProperties;
}

{
  if (!autoFeedId) {
    if ((Validator.isNull(feedId)) || (Validator.isNumber(feedId)) || (feedId.indexOf(StringPool.SPACE) != -1)) {
      throw new FeedIdException();
    }
    try {
      journalFeedPersistence.findByG_F(groupId,feedId);
      throw new DuplicateFeedIdException();
    }
 catch (    NoSuchFeedException nsfe) {
    }
  }
  validate(companyId,groupId,name,description,structureId,targetLayoutFriendlyUrl,contentField);
}

{
  if (isEscapedModel()) {
    return (DDMStorageLink)this;
  }
 else {
    return (DDMStorageLink)Proxy.newProxyInstance(DDMStorageLink.class.getClassLoader(),new Class[]{DDMStorageLink.class},new AutoEscapeBeanHandler(this));
  }
}

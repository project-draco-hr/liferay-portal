{
  String emailArticleAddedBody=preferences.getValue("email-article-added-body",StringPool.BLANK);
  if (Validator.isNotNull(emailArticleAddedBody)) {
    return emailArticleAddedBody;
  }
 else {
    return ContentUtil.get(PropsUtil.get(PropsKeys.JOURNAL_EMAIL_ARTICLE_ADDED_BODY));
  }
}

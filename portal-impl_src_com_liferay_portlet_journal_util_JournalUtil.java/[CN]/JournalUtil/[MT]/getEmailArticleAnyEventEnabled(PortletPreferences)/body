{
  if (getEmailArticleAddedEnabled(preferences) || getEmailArticleApprovalDeniedEnabled(preferences) || getEmailArticleApprovalGrantedEnabled(preferences)|| getEmailArticleApprovalRequestedEnabled(preferences)|| getEmailArticleReviewEnabled(preferences)|| getEmailArticleUpdatedEnabled(preferences)) {
    return true;
  }
  return false;
}

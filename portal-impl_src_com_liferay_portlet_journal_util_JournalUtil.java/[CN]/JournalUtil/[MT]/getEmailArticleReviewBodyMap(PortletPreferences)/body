{
  Map<Locale,String> map=LocalizationUtil.getLocalizationMap(preferences,"emailArticleReviewBody");
  Locale defaultLocale=LocaleUtil.getSiteDefault();
  String defaultValue=map.get(defaultLocale);
  if (Validator.isNotNull(defaultValue)) {
    return map;
  }
  map.put(defaultLocale,ContentUtil.get(PropsUtil.get(PropsKeys.JOURNAL_EMAIL_ARTICLE_REVIEW_BODY)));
  return map;
}

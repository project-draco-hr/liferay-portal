{
  StringBundler className=new StringBundler(128);
  long classPK=getSubscriptionToStructureClass(groupId,ddmStructureId,className);
  return SubscriptionLocalServiceUtil.isSubscribed(companyId,userId,className.toString(),classPK);
}

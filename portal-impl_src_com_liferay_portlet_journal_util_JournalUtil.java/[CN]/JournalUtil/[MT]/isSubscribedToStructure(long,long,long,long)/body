{
  String className=DDMStructure.class.getName();
  long classPK=ddmStructureId;
  if (ddmStructureId == 0) {
    className+=JournalArticle.class.getName();
    classPK=groupId;
  }
  return SubscriptionLocalServiceUtil.isSubscribed(companyId,userId,className,classPK);
}

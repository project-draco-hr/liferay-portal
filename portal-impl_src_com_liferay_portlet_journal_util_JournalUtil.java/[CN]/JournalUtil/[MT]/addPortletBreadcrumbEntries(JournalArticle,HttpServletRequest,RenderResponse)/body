{
  JournalFolder folder=article.getFolder();
  if (folder.getFolderId() != JournalFolderConstants.DEFAULT_PARENT_FOLDER_ID) {
    addPortletBreadcrumbEntries(folder,request,renderResponse);
  }
  PortletURL portletURL=renderResponse.createRenderURL();
  portletURL.setParameter("struts_action","/article/view_article");
  portletURL.setParameter("articlePK",String.valueOf(article.getPrimaryKey()));
  PortalUtil.addPortletBreadcrumbEntry(request,article.getTitle(),portletURL.toString());
}

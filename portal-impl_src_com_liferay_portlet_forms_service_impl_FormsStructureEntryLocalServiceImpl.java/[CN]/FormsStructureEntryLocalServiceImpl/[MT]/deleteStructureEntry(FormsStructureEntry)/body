{
  resourceLocalService.deleteResource(structureEntry.getCompanyId(),FormsStructureEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,structureEntry.getStructureId());
  formsStructureEntryPersistence.remove(structureEntry);
}

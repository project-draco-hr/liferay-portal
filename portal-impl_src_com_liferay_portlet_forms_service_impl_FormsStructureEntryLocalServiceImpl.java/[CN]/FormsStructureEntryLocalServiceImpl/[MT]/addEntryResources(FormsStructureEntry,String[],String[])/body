{
  resourceLocalService.addModelResources(entry.getCompanyId(),entry.getGroupId(),entry.getUserId(),FormsStructureEntry.class.getName(),entry.getFormStructureId(),communityPermissions,guestPermissions);
}

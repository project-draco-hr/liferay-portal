{
  expandoValueLocalService.deleteValues(FormsStructureEntry.class.getName(),entry.getPrimaryKey());
  resourceLocalService.deleteResource(entry.getCompanyId(),FormsStructureEntry.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,entry.getFormStructureId());
  formsStructureEntryPersistence.remove(entry);
}

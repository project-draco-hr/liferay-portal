{
  Integer type=DDMStructureLayoutModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DDMStructureLayoutModelImpl.TABLE_NAME);
  }
  return type;
}

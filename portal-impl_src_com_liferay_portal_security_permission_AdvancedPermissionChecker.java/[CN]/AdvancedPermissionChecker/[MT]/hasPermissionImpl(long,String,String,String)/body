{
  try {
    if (!signedIn) {
      return hasGuestPermission(groupId,name,primKey,actionId);
    }
    if (ResourceBlockLocalServiceUtil.isSupported(name)) {
      return hasUserPermissionImpl(groupId,name,primKey,actionId,true);
    }
    return hasUserPermissionImpl(groupId,name,primKey,actionId,true);
  }
 catch (  Exception e) {
    _log.error(e,e);
    return false;
  }
}

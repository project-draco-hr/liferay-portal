{
  Session session=getSession();
  SyncAccount syncAccount=SyncAccountService.fetchSyncAccount(_syncAccountId);
  _httpPost=new HttpPost(syncAccount.getUrl() + "/api/jsonws" + urlPath);
  session.execute(_httpPost,parameters,_handler);
}

{
  SearchResult searchResult=assertThatSearchSingleDocumentReturnsOneSearchResult(createMBMessageDocument());
  Assert.assertEquals(MBMESSAGE_CLASS_NAME,searchResult.getClassName());
  Assert.assertEquals(SearchTestUtil.ENTRY_CLASS_PK,searchResult.getClassPK());
  List<MBMessage> mbMessages=searchResult.getMBMessages();
  Assert.assertTrue("MBMessageLocalService must not be invoked at all",mbMessages.isEmpty());
  verifyZeroInteractions(mbMessageLocalService);
  Assert.assertNull(searchResult.getSummary());
  assertThatFileEntryTuplesAndVersionsAreEmpty(searchResult);
}

{
  Set<String> verifyProcessNames=_verifyProcesses.keySet();
  for (  String verifyProcessName : verifyProcessNames) {
    executeVerifyProcess(verifyProcessName,outputStreamProviderName,"verify-" + verifyProcessName);
  }
}

{
  VerifyProcess verifyProcess=_verifyProcesses.getService(verifyProcessName);
  if (verifyProcess == null) {
    throw new IllegalArgumentException("No verify process with name " + verifyProcessName);
  }
  return verifyProcess;
}

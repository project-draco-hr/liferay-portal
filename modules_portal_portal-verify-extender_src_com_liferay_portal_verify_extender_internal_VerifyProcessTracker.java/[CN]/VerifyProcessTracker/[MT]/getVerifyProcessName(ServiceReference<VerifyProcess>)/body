{
  String verifyProcessName=(String)serviceReference.getProperty("verify.process.name");
  if ((verifyProcessName == null) || verifyProcessName.equals("")) {
    throw new IllegalArgumentException("Verify processes must specify the property " + "\"verify.process.name\"");
  }
  return verifyProcessName;
}

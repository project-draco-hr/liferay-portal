{
  VerifyProcess verifyProcess=getVerifyProcess(verifierName);
  if (verifyProcess != null) {
    System.out.println("Registered verifier: " + verifierName);
  }
 else {
    System.out.println("No verifier registered with name: " + verifierName);
  }
}

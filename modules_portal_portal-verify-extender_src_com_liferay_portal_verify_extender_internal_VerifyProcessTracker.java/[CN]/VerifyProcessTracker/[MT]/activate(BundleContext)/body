{
  try {
    _verifiers=ServiceTrackerMapFactory.singleValueMap(bundleContext,VerifyProcess.class,"verifier.name");
    _verifiers.open();
  }
 catch (  InvalidSyntaxException ise) {
    throw new IllegalStateException(ise);
  }
}

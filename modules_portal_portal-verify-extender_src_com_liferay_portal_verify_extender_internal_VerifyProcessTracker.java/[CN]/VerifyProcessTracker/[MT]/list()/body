{
  for (  Map.Entry<String,VerifyProcess> entry : _verifyProcesses.entrySet()) {
    System.out.println("Verify process " + ClassUtil.getClassName(entry.getValue()) + " is registered with the name "+ entry.getKey());
  }
}

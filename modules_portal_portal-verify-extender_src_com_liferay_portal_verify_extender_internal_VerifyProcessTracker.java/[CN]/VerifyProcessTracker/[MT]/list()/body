{
  for (  Map.Entry<String,VerifyProcess> entry : _verifyProcesses.entrySet()) {
    if (_log.isInfoEnabled()) {
      _log.info("Verifier " + entry.getKey() + " of type "+ ClassUtil.getClassName(entry.getValue()));
    }
  }
}

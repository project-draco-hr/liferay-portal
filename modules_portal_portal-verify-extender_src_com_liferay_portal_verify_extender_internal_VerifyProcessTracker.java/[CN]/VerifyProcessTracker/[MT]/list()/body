{
  int i=1;
  for (  Map.Entry<String,VerifyProcess> entry : _verifyProcesses.entrySet()) {
    System.out.println(String.format("[%1$5d] Verify process '%2$s' is registered with the " + "name '%3$s'",i++,ClassUtil.getClassName(entry.getValue()),entry.getKey()));
  }
}

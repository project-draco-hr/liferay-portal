{
  if (isEscapedModel()) {
    return (PortletPreferences)this;
  }
 else {
    return (PortletPreferences)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

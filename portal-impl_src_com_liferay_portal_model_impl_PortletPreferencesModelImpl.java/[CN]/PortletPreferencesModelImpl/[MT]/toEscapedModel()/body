{
  if (isEscapedModel()) {
    return (PortletPreferences)this;
  }
 else {
    return (PortletPreferences)Proxy.newProxyInstance(PortletPreferences.class.getClassLoader(),new Class[]{PortletPreferences.class},new AutoEscapeBeanHandler(this));
  }
}

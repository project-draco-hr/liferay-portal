{
  if (!PropsValues.HIBERNATE_CACHE_USE_SECOND_LEVEL_CACHE) {
    return;
  }
  DDMStructureLayout newDDMStructureLayout=addDDMStructureLayout();
  _persistence.clearCache();
  DDMStructureLayoutModelImpl existingDDMStructureLayoutModelImpl=(DDMStructureLayoutModelImpl)_persistence.findByPrimaryKey(newDDMStructureLayout.getPrimaryKey());
  Assert.assertTrue(Validator.equals(existingDDMStructureLayoutModelImpl.getUuid(),existingDDMStructureLayoutModelImpl.getOriginalUuid()));
  Assert.assertEquals(existingDDMStructureLayoutModelImpl.getGroupId(),existingDDMStructureLayoutModelImpl.getOriginalGroupId());
  Assert.assertEquals(existingDDMStructureLayoutModelImpl.getStructureVersionId(),existingDDMStructureLayoutModelImpl.getOriginalStructureVersionId());
}

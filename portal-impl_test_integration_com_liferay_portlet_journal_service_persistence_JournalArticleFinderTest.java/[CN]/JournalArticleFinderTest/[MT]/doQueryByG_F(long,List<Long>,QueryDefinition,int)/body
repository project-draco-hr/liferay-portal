{
  int actualCount=JournalArticleFinderUtil.countByG_F(groupId,folderIds,queryDefinition);
  Assert.assertEquals(expectedCount,actualCount);
  actualCount=((List)JournalArticleFinderUtil.findByG_F(groupId,folderIds,queryDefinition)).size();
  Assert.assertEquals(expectedCount,actualCount);
}

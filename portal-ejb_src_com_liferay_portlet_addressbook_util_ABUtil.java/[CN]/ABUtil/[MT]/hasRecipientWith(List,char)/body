{
  Letter letter=new Letter((new Character(l)).toString());
  int result=Collections.binarySearch(recipients,letter,new LetterComparator());
  if (result >= 0) {
    return true;
  }
 else {
    return false;
  }
}

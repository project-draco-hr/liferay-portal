{
  Level level=loggingEvent.getLevel();
  if (level.equals(Level.ERROR) || level.equals(Level.FATAL)) {
    ConcurrentAssertUtil.caughtError("Test failed due to logged error: " + loggingEvent.getMessage());
  }
}

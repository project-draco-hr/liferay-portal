{
  if (loggingEvent.getLevel().equals(Level.WARN) || loggingEvent.getLevel().equals(Level.ERROR) || loggingEvent.getLevel().equals(Level.FATAL)) {
    Assert.fail("Method failed due to logged error or warning: " + loggingEvent.getMessage());
  }
}

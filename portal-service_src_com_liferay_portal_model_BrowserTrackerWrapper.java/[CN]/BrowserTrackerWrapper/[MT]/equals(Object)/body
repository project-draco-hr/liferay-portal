{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof BrowserTrackerWrapper)) {
    return false;
  }
  BrowserTrackerWrapper browserTrackerWrapper=(BrowserTrackerWrapper)obj;
  if (Validator.equals(_browserTracker,browserTrackerWrapper._browserTracker)) {
    return true;
  }
  return false;
}

{
  try (PreparedStatement ps=connection.prepareStatement("select groupId from Layout where plid = " + plid);ResultSet rs=ps.executeQuery()){
    if (rs.next()) {
      return rs.getLong("groupId");
    }
  }
   return 0;
}

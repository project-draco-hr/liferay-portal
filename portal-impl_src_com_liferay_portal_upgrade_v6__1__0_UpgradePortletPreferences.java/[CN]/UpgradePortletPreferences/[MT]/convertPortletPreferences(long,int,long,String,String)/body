{
  long portletPreferencesId=getPortletPreferencesId(ownerId,ownerType,plid,portletId);
  if (portletPreferencesId == 0) {
    addPortletPreferences(ownerId,ownerType,plid,portletId,preferences);
  }
}

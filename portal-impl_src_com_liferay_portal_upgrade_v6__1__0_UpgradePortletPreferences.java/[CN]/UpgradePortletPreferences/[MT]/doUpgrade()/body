{
  updatePortalPreferences();
  updatePortletPreferences();
  updatePortletPreferencesOwner();
  upgrade(UpgradeCommunityProperties.class);
  runSQL("create index IX_D1F795F1 on PortalPreferences (ownerId, " + "ownerType)");
}

{
  ShoppingItem newShoppingItem=addShoppingItem();
  DynamicQuery dynamicQuery=DynamicQueryFactoryUtil.forClass(ShoppingItem.class,ShoppingItem.class.getClassLoader());
  dynamicQuery.add(RestrictionsFactoryUtil.eq("itemId",newShoppingItem.getItemId()));
  List<Object> result=_persistence.findWithDynamicQuery(dynamicQuery);
  assertEquals(1,result.size());
  ShoppingItem existingShoppingItem=(ShoppingItem)result.get(0);
  assertEquals(existingShoppingItem,newShoppingItem);
}

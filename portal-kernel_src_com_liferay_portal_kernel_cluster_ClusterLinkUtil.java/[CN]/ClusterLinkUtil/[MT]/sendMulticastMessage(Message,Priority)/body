{
  if (_clusterLink == null) {
    _log.warn("ClusterLinkUtil has not been initialized! Will not send any " + "multicast message.");
    return;
  }
  _clusterLink.sendMulticastMessage(message,priority);
}

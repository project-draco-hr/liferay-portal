{
  if (_clusterLink == null) {
    _log.warn("ClusterLinkUtil has not been initialized! Will not send any " + "unicast message.");
    return;
  }
  _clusterLink.sendUnicastMessage(address,message,priority);
}

{
  if (_clusterLink == null) {
    _log.warn("ClusterLinkUtil has not been initialized! Will return an " + "empty Address List.");
    return new ArrayList<Address>();
  }
  return _clusterLink.getAddresses();
}

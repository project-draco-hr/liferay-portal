{
  DDMPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_TEMPLATE);
  return ddmTemplateLocalService.addTemplate(structureId,name,description,type,language,script,serviceContext);
}

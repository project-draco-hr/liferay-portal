{
  List<DDMTemplate> ddmTemplates=new ArrayList<>();
  ddmTemplates.addAll(ddmTemplatePersistence.filterFindByG_C_C(groupId,classNameId,classPK));
  if (!includeAncestorTemplates) {
    return ddmTemplates;
  }
  ddmTemplates.addAll(ddmTemplatePersistence.filterFindByG_C_C(PortalUtil.getAncestorSiteGroupIds(groupId),classNameId,classPK));
  return ddmTemplates;
}

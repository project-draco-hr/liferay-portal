{
  JournalFeed feed=_journalFeedLocalService.fetchJournalFeed(GetterUtil.getLong(primKey));
  if (feed == null) {
    return;
  }
  feed.setModifiedDate(new Date());
  _journalFeedLocalService.updateJournalFeed(feed);
}

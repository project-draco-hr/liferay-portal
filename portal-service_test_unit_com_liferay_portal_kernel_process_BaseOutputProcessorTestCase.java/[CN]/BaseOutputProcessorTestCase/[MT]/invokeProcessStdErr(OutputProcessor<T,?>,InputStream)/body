{
  Method processStdErrBridgeMethod=ReflectionUtil.getBridgeMethod(outputProcessor.getClass(),"processStdErr",InputStream.class);
  return (T)processStdErrBridgeMethod.invoke(outputProcessor,inputStream);
}

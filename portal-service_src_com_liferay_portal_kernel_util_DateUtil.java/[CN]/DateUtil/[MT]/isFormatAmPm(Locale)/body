{
  SimpleDateFormat simpleDataFormat=(SimpleDateFormat)DateFormat.getTimeInstance(DateFormat.SHORT,locale);
  String timeFormatPattern=simpleDataFormat.toPattern();
  if (timeFormatPattern.contains("a")) {
    return true;
  }
  return false;
}

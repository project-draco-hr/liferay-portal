{
  if (comment == null) {
    return null;
  }
  comment=ToolsUtil.stripFullyQualifiedClassNames(comment,_imports);
  if (!comment.contains("* @deprecated ") || _hasAnnotation(abstractBaseJavaEntity,"Deprecated")) {
    return comment;
  }
  return comment + indent + "@Deprecated\n";
}

{
  serviceContext=(ServiceContext)serviceContext.clone();
  serviceContext.setWorkflowAction(WorkflowConstants.ACTION_PUBLISH);
  String title=getSearchKeywords();
  title+=ServiceTestUtil.randomString(_NODE_NAME_MAX_LENGTH - title.length());
  return WikiNodeLocalServiceUtil.addNode(TestPropsValues.getUserId(),title,ServiceTestUtil.randomString(),serviceContext);
}

{
  BookmarksEntry entry=_bookmarksEntryLocalService.fetchBookmarksEntry(GetterUtil.getLong(primKey));
  if (entry == null) {
    return;
  }
  entry.setModifiedDate(new Date());
  _bookmarksEntryLocalService.updateBookmarksEntry(entry);
}

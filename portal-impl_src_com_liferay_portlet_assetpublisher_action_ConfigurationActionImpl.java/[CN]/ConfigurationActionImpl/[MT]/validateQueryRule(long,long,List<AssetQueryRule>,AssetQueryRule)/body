{
  String name=queryRule.getName();
  if (name.equals("assetTags")) {
    AssetTagLocalServiceUtil.checkTags(userId,groupId,queryRule.getValues());
  }
  if (queryRules.contains(queryRule)) {
    throw new DuplicateAssetQueryRuleException(queryRule.isContains(),queryRule.isAndOperator(),queryRule.getName());
  }
}

{
  List<CategoriesVocabularySoap> soapModels=new ArrayList<CategoriesVocabularySoap>(models.size());
  for (  CategoriesVocabulary model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new CategoriesVocabularySoap[soapModels.size()]);
}

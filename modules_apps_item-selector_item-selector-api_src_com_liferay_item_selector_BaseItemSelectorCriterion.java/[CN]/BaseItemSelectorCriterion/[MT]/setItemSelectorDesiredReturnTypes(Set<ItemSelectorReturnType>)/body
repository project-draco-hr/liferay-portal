{
  if (!_availableItemSelectorReturnTypes.containsAll(itemSelectorDesiredReturnTypes)) {
    throw new IllegalArgumentException("Desired return types must be a subset of available return " + "types");
  }
  _itemSelectorDesiredReturnTypes=itemSelectorDesiredReturnTypes;
}

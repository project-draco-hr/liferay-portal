{
  Hits hits=null;
  List<AssetVocabulary> assetVocabularies=new ArrayList<AssetVocabulary>();
  hits=getHits(groupId,title,start,end);
  List<Document> documents=hits.toList();
  for (  Document document : documents) {
    long vocabularyId=GetterUtil.getLong(document.get("vocabularyId"));
    AssetVocabulary assetVocabulary=getVocabulary(vocabularyId);
    assetVocabularies.add(assetVocabulary);
  }
  return assetVocabularies;
}

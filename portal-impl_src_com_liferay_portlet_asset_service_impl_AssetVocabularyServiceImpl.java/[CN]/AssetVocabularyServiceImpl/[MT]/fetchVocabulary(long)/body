{
  AssetVocabulary vocabulary=assetVocabularyLocalService.fetchAssetVocabulary(vocabularyId);
  if (vocabulary != null) {
    AssetVocabularyPermission.check(getPermissionChecker(),vocabulary,ActionKeys.VIEW);
  }
  return vocabulary;
}

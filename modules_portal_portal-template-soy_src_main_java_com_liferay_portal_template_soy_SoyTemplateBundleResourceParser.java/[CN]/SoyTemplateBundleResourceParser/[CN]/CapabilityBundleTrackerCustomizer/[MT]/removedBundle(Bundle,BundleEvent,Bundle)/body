{
  BundleWiring bundleWiring=bundle.adapt(BundleWiring.class);
  List<BundleCapability> capabilities=bundleWiring.getCapabilities(_namespace);
  for (  BundleCapability bundleCapability : capabilities) {
    String providerBundleKey=getCapabilityPrefix(bundleCapability);
    _bundleProvidersMap.remove(providerBundleKey);
  }
}

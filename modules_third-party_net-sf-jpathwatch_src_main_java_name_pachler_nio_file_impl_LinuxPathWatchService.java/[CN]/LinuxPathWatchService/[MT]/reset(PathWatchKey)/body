{
  if (!pathWatchKey.isValid())   return false;
  if (pathWatchKey.hasPendingWatchEvents()) {
    if (!hasMasterThread)     pendingWatchKeys.add(pathWatchKey);
  }
 else   signalledWatchKeys.remove(pathWatchKey);
  return true;
}

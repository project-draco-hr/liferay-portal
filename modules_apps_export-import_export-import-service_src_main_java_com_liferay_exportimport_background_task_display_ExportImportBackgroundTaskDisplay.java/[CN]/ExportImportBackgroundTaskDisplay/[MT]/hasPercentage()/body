{
  if (!hasBackgroundTaskStatus()) {
    return false;
  }
  if ((_allProgressBarCountersTotal > 0) && (!Validator.equals(_cmd,Constants.PUBLISH_TO_REMOTE) || (getPercentage() < PERCENTAGE_MAX))) {
    return true;
  }
  return false;
}

{
  if (_log.isInfoEnabled()) {
    long uptime=(System.currentTimeMillis() - _startTime) / 1000;
    _log.info("The " + _service + " service has been disconnected after "+ uptime+ "secs of uptime.");
  }
}

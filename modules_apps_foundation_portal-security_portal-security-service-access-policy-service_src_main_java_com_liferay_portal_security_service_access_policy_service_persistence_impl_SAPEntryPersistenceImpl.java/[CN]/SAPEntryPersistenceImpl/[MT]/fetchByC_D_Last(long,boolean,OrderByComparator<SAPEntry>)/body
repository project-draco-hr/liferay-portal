{
  int count=countByC_D(companyId,defaultSAPEntry);
  if (count == 0) {
    return null;
  }
  List<SAPEntry> list=findByC_D(companyId,defaultSAPEntry,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

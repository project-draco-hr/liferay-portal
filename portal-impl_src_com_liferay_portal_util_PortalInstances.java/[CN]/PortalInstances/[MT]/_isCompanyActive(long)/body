{
  try {
    Company company=CompanyLocalServiceUtil.fetchCompany(companyId);
    if (company != null) {
      return company.isActive();
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return false;
}

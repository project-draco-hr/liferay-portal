{
  _companyIds=new long[0];
  _ignoreHosts=SetUtil.fromArray(PropsUtil.getArray(PropsUtil.VIRTUAL_HOSTS_IGNORE_HOSTS));
  _ignorePaths=SetUtil.fromArray(PropsUtil.getArray(PropsUtil.VIRTUAL_HOSTS_IGNORE_PATHS));
}

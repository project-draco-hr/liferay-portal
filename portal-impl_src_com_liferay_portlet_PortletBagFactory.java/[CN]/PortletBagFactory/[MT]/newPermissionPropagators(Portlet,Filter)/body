{
  ServiceTrackerList<PermissionPropagator> permissionPropagatorInstances=getServiceTrackerList(PermissionPropagator.class,portlet,filter);
  if (Validator.isNotNull(portlet.getPermissionPropagatorClass())) {
    PermissionPropagator permissionPropagatorInstance=(PermissionPropagator)newInstance(PermissionPropagator.class,portlet.getPermissionPropagatorClass());
    permissionPropagatorInstances.add(permissionPropagatorInstance);
  }
  return permissionPropagatorInstances;
}

{
  if (Validator.isNull(portlet.getIndexerClass())) {
    return null;
  }
  Indexer indexerInstance=(Indexer)newInstance(Indexer.class,portlet.getIndexerClass());
  IndexerRegistryUtil.register(indexerInstance);
  return indexerInstance;
}

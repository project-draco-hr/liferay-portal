{
  Map<String,Object> properties=new HashMap<>();
  properties.put("javax.portlet.name",portlet.getPortletId());
  return ServiceTrackerCollections.list(clazz,"(|(javax.portlet.name=" + portlet.getPortletId() + ")(javax.portlet.name=ALL))",properties);
}

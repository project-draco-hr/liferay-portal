{
  Class<?> classObj=_classLoader.loadClass(PortletProps.class.getName());
  java.lang.reflect.Method method=classObj.getMethod("get",String.class);
  return (String)method.invoke(null,propertyKey);
}

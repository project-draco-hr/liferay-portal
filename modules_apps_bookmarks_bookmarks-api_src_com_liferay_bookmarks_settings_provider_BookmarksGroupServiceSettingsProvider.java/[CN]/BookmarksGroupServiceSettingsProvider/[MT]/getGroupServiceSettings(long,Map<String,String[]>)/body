{
  Settings settings=_settingsFactory.getSettings(new GroupServiceSettingsLocator(groupId,BookmarksConstants.SERVICE_NAME));
  return new BookmarksGroupServiceSettings(new ParameterMapSettings(parameterMap,settings));
}

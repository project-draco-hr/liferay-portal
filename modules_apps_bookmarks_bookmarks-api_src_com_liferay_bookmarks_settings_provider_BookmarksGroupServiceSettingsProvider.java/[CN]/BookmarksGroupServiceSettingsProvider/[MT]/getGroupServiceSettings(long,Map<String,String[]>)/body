{
  Settings settings=_settingsFactory.getGroupServiceSettings(groupId,BookmarksConstants.SERVICE_NAME);
  return new BookmarksGroupServiceSettings(new ParameterMapSettings(parameterMap,settings));
}

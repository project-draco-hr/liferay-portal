{
  try {
    Content content=createContent();
    hook.addDirectory(content.getCompanyId(),content.getRepositoryId(),content.getPath());
  }
 catch (  Exception e) {
    fail("testAddDirectory failed!");
  }
}

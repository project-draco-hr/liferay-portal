{
  try {
    Content content=createContent();
    hook.reindex(new String[]{String.valueOf(content.getCompanyId()),content.getPortletId(),content.getPath(),String.valueOf(content.getRepositoryId())});
  }
 catch (  Exception e) {
    fail("testReindex failed!");
  }
}

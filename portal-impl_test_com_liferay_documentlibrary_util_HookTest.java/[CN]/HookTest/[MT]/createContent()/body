{
  long contentId=nextLong();
  byte[] bytes=new byte[1024];
  Blob data=new OutputBlob(new ByteArrayInputStream(bytes),bytes.length);
  Content content=new Content(contentId,nextLong(),randomString(),nextLong(),nextLong(),randomString(),Hook.DEFAULT_VERSION,data,data.length());
  return content;
}

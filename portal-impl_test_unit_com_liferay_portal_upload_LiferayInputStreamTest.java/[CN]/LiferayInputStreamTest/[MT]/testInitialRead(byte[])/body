{
  _request.setContent(content);
  _liferayInputStream=new LiferayInputStream(_request);
  byte[] bytes=new byte[content.length];
  Assert.assertEquals(content.length,_liferayInputStream.read(bytes,0,bytes.length));
  Assert.assertArrayEquals(content,bytes);
}

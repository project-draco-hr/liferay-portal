{
  Session session=null;
  try {
    session=openSession();
    LayoutSetPrototype layoutSetPrototype=(LayoutSetPrototype)session.get(LayoutSetPrototypeImpl.class,new Long(layoutSetPrototypeId));
    if (layoutSetPrototype == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No LayoutSetPrototype exists with the primary key " + layoutSetPrototypeId);
      }
      throw new NoSuchLayoutSetPrototypeException("No LayoutSetPrototype exists with the primary key " + layoutSetPrototypeId);
    }
    return remove(layoutSetPrototype);
  }
 catch (  NoSuchLayoutSetPrototypeException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(LayoutSetPrototype layoutSetPrototype) method. Use update(LayoutSetPrototype layoutSetPrototype, boolean merge) instead.");
  }
  return update(layoutSetPrototype,false);
}

{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  try {
    return MBCategoryPermission.contains(themeDisplay.getPermissionChecker(),_category,ActionKeys.SUBSCRIBE);
  }
 catch (  PortalException pe) {
  }
  return false;
}

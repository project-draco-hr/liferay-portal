{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  try {
    return MBCategoryPermission.contains(themeDisplay.getPermissionChecker(),getCategory(portletRequest),ActionKeys.SUBSCRIBE);
  }
 catch (  PortalException pe) {
  }
  return false;
}

{
  long userId=PortletConstants.getUserId(oldRootPortletId);
  String instanceId=PortletConstants.getInstanceId(oldRootPortletId);
  return PortletConstants.assemblePortletId(newRootPortletId,userId,instanceId);
}

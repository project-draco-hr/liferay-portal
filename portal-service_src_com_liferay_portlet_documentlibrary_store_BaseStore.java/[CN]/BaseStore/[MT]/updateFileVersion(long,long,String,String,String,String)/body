{
  InputStream is=getFileAsStream(companyId,repositoryId,fileName,fromVersionLabel);
  updateFile(companyId,repositoryId,fileName,toVersionLabel,sourceFileName,is);
  deleteFile(companyId,repositoryId,fileName,fromVersionLabel);
}

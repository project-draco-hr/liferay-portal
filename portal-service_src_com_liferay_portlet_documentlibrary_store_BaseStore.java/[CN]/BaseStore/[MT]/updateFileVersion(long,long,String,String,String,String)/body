{
  InputStream is=getFileAsStream(companyId,repositoryId,fileName,fromVersionNumber);
  updateFile(companyId,repositoryId,fileName,toVersionNumber,sourceFileName,is);
  deleteFile(companyId,repositoryId,fileName,fromVersionNumber);
}

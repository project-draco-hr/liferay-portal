{
  InputStream is=new UnsyncByteArrayInputStream(bytes);
  try {
    updateFile(companyId,repositoryId,fileName,versionNumber,sourceFileName,is);
  }
  finally {
    try {
      is.close();
    }
 catch (    IOException ioe) {
      _log.error(ioe);
    }
  }
}

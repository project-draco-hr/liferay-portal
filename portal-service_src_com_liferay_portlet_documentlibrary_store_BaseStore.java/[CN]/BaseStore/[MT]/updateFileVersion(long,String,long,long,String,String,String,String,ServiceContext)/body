{
  InputStream is=getFileAsStream(companyId,repositoryId,fileName,fromVersionNumber);
  updateFile(companyId,portletId,groupId,repositoryId,fileName,toVersionNumber,sourceFileName,serviceContext,is);
  deleteFile(companyId,portletId,repositoryId,fileName,fromVersionNumber);
}

{
  if (!contains(permissionChecker,ruleGroup,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),MDRRuleGroup.class.getName(),ruleGroup.getRuleGroupId(),actionId);
  }
}

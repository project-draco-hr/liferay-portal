{
  GroupPermissionUtil.check(getPermissionChecker(),groupId,ActionKeys.MANAGE_LAYOUTS);
  return tasksProposalLocalService.addProposal(getUserId(),groupId,name,description,classNameId,classPK,reviewerId,addCommunityPermissions,addGuestPermissions);
}

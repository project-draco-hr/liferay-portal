{
  if (isEscapedModel()) {
    return (TasksProposal)this;
  }
 else {
    return (TasksProposal)Proxy.newProxyInstance(TasksProposal.class.getClassLoader(),new Class[]{TasksProposal.class},new AutoEscapeBeanHandler(this));
  }
}

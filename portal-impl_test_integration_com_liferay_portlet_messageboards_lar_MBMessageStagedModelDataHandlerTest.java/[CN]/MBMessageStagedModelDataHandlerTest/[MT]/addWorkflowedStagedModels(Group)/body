{
  List<StagedModel> stagedModels=new ArrayList<>();
  stagedModels.add(MBTestUtil.addMessageWithWorkflow(group.getGroupId(),true));
  stagedModels.add(MBTestUtil.addMessageWithWorkflow(group.getGroupId(),false));
  return stagedModels;
}

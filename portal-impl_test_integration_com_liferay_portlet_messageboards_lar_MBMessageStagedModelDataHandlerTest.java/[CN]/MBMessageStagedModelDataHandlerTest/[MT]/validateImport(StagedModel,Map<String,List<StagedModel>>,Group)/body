{
  super.validateImport(stagedModel,dependentStagedModelsMap,group);
  MBMessage message=(MBMessage)getStagedModel(stagedModel.getUuid(),group);
  Assert.assertEquals(1,message.getAttachmentsFileEntriesCount());
}

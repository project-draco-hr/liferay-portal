{
  List<StagedModel> dependentStagedModels=dependentStagedModelsMap.get(MBCategory.class.getName());
  MBCategory category=(MBCategory)dependentStagedModels.get(0);
  return MBTestUtil.addMessageWithWorkflow(group.getGroupId(),category.getCategoryId(),true);
}

{
  liferaySelenium.assertElementPresent(locator);
  if (liferaySelenium.isText(locator,pattern)) {
    String text=liferaySelenium.getText(locator);
    BaseTestCase.fail("Expected \"" + pattern + "\" to not match \""+ text+ "\" found at \""+ locator+ "\"");
  }
}

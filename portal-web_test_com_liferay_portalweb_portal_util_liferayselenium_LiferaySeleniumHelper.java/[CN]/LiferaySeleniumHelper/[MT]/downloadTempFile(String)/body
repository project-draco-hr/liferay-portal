{
  if (!_BROWSER_TYPE.equals("*chrome") && !_BROWSER_TYPE.equals("*firefox") && !_BROWSER_TYPE.equals("*iehta")&& !_BROWSER_TYPE.equals("*iexplore")) {
    return;
  }
  try {
    String command=RuntimeVariables.replace(_SELENIUM_EXECUTABLE_AUTOIT_DIR + TestPropsValues.SELENIUM_DOWNLOAD_FILE);
    Runtime runtime=Runtime.getRuntime();
    Thread.sleep(5000);
    runtime.exec(command);
    Thread.sleep(30000);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

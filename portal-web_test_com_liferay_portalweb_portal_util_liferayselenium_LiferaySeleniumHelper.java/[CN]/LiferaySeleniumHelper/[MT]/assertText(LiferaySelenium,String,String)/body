{
  liferaySelenium.assertElementPresent(locator);
  if (liferaySelenium.isNotText(locator,pattern)) {
    String text=liferaySelenium.getText(locator);
    BaseTestCase.fail("Expected \"" + pattern + "\" to match \""+ text+ "\" found at \""+ locator+ "\"");
  }
}

{
  if (isEscapedModel()) {
    return (RatingsEntry)this;
  }
 else {
    return (RatingsEntry)Proxy.newProxyInstance(RatingsEntry.class.getClassLoader(),new Class[]{RatingsEntry.class},new AutoEscapeBeanHandler(this));
  }
}

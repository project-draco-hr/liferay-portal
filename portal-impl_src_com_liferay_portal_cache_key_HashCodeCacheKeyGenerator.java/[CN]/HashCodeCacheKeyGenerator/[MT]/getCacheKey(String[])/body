{
  long hashCode=0;
  for (  String key : keys) {
    if (key == null) {
      continue;
    }
    for (int i=0; i < key.length(); i++) {
      hashCode=31 * hashCode + key.charAt(i);
    }
  }
  return StringUtil.toHexString(hashCode);
}

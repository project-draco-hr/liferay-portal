{
  Integer type=SocialActivityLimitModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SocialActivityLimitModelImpl.TABLE_NAME);
  }
  return type;
}

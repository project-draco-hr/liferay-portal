{
  String expressionString=ddmFormField.getVisibilityExpression();
  if (!isValidBooleanExpression(expressionString)) {
    throw new MustSetValidVisibilityExpression(ddmFormField.getName(),expressionString);
  }
}

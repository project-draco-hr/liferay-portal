{
  if (isSupportsAlterColumnType()) {
    runSQL("alter_column_type WikiPageResource title varchar(255) null");
  }
 else {
    UpgradeTable upgradeTable=new DefaultUpgradeTableImpl(WikiPageResourceImpl.TABLE_NAME,WikiPageResourceImpl.TABLE_COLUMNS);
    upgradeTable.setCreateSQL(WikiPageResourceImpl.TABLE_SQL_CREATE);
    upgradeTable.updateTable();
  }
}

{
  try (ZipFile zipFile=new ZipFile(file);Properties properties=new Properties();InputStream inputStream=zipFile.getInputStream(new ZipEntry("WEB-INF/liferay-plugin-package.properties"))){
    properties.load(inputStream);
    return properties.getProperty("serlvet-context-name");
  }
 }

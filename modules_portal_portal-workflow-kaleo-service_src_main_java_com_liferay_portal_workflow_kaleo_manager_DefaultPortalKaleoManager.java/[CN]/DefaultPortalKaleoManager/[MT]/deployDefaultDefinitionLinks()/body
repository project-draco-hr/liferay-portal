{
  List<Company> companies=CompanyLocalServiceUtil.getCompanies(false);
  for (  Company company : companies) {
    PortalKaleoManagerUtil.deployDefaultDefinitionLinks(company.getCompanyId());
  }
}

{
  com.liferay.portlet.addressbook.model.ABList abList=null;
  if (checkPool) {
    abList=ABListPool.get(abListHBM.getPrimaryKey());
  }
  if (abList == null) {
    abList=new com.liferay.portlet.addressbook.model.ABList(abListHBM.getListId(),abListHBM.getUserId(),abListHBM.getName());
    ABListPool.put(abList.getPrimaryKey(),abList);
  }
  return abList;
}

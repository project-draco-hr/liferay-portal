{
  if (isEscapedModel()) {
    return (SCProductVersion)this;
  }
 else {
    return (SCProductVersion)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

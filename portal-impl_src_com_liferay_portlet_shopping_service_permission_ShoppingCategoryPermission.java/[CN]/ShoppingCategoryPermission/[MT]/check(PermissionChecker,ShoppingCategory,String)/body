{
  if (!contains(permissionChecker,category,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),ShoppingCategory.class.getName(),category.getCategoryId(),actionId);
  }
}

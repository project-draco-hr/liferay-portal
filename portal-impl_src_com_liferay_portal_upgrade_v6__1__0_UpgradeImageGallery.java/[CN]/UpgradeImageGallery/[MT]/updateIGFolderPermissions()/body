{
  removeConflictingIGPermissions(_IG_FOLDER_CLASS_NAME,DLFolder.class.getName());
  runSQL("update ResourcePermission set name = '" + DLFolder.class.getName() + "' where name = '"+ _IG_FOLDER_CLASS_NAME+ "'");
}

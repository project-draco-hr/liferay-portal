{
  BuildCSSTask buildCSSTask=GradleUtil.addTask(project,BUILD_CSS_TASK_NAME,BuildCSSTask.class);
  buildCSSTask.setGroup(BasePlugin.BUILD_GROUP);
  buildCSSTask.setDescription("Build CSS files.");
  return buildCSSTask;
}

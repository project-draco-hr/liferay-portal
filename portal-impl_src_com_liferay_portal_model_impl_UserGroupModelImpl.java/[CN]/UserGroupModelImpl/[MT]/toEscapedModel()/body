{
  if (isEscapedModel()) {
    return (UserGroup)this;
  }
 else {
    return (UserGroup)Proxy.newProxyInstance(UserGroup.class.getClassLoader(),new Class[]{UserGroup.class},new AutoEscapeBeanHandler(this));
  }
}

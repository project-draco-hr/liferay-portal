{
  if (_log.isDebugEnabled()) {
    _log.debug(e);
  }
  String url=_getOriginalURL(request);
  _log.warn("Reject processAction for " + url + " on "+ portlet.getPortletId());
  return ActionResult.EMPTY_ACTION_RESULT;
}

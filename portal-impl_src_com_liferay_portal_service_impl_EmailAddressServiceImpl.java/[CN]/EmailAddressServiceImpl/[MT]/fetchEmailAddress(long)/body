{
  EmailAddress emailAddress=emailAddressPersistence.fetchByPrimaryKey(emailAddressId);
  try {
    CommonPermissionUtil.check(getPermissionChecker(),emailAddress.getClassNameId(),emailAddress.getClassPK(),ActionKeys.VIEW);
    return emailAddress;
  }
 catch (  Exception e) {
    return null;
  }
}

{
  EmailAddress emailAddress=EmailAddressUtil.findByPrimaryKey(emailAddressId);
  CommonPermissionUtil.checkPermission(getPermissionChecker(),emailAddress.getClassNameId(),emailAddress.getClassPK(),ActionKeys.UPDATE);
  return EmailAddressLocalServiceUtil.updateEmailAddress(emailAddressId,address,typeId,primary);
}

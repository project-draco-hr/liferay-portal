{
  CommonPermissionUtil.checkPermission(getPermissionChecker(),className,classPK,ActionKeys.VIEW);
  return EmailAddressLocalServiceUtil.getEmailAddresses(getUser().getCompanyId(),className,classPK);
}

{
  EmailAddress emailAddress=EmailAddressUtil.findByPrimaryKey(emailAddressId);
  CommonPermission.checkPermission(getPermissionChecker(),emailAddress.getClassNameId(),emailAddress.getClassPK(),ActionKeys.VIEW);
  return emailAddress;
}

{
  if (isEscapedModel()) {
    return (UserTrackerPath)this;
  }
 else {
    return (UserTrackerPath)Proxy.newProxyInstance(UserTrackerPath.class.getClassLoader(),new Class[]{UserTrackerPath.class},new AutoEscapeBeanHandler(this));
  }
}

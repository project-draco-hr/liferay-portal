{
  if (isEscapedModel()) {
    return (UserTrackerPath)this;
  }
 else {
    return (UserTrackerPath)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

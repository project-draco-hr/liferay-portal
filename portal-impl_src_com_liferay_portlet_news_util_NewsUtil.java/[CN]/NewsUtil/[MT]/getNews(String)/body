{
  Feed feed=(Feed)NewsUtil.getFeedMap().get(xml);
  if (feed == null) {
    return null;
  }
 else {
    WebCacheItem wci=new NewsConverter();
    return (News)WebCachePoolUtil.get(feed.getShortName() + ";" + xml,wci);
  }
}

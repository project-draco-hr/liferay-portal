{
  Connection con=null;
  PreparedStatement ps=null;
  ResultSet rs=null;
  try {
    con=DataAccess.getConnection();
    ps=con.prepareStatement("select TE.entryId, TE.companyId, TE.userId, TP.propertyId, " + "TP.value from TagsEntry as TE, TagsProperty as TP where " + "TE.entryId = TP.entryId and TE.vocabularyId <= 0 "+ "and TP.key_ = 'category'");
    rs=ps.executeQuery();
    while (rs.next()) {
      long entryId=rs.getLong("TE.entryId");
      long companyId=rs.getLong("TE.companyId");
      long userId=rs.getLong("TE.userId");
      long propertyId=rs.getLong("TP.propertyId");
      String value=rs.getString("TP.value");
      long vocabularyId=getVocabularyId(companyId,userId,value);
      ps=con.prepareStatement("update TagsEntry set vocabularyId = ? where entryId = ?");
      ps.setLong(1,vocabularyId);
      ps.setLong(2,entryId);
      ps.executeUpdate();
      ps=con.prepareStatement("delete from TagsProperty where propertyId = ?");
      ps.setLong(1,propertyId);
      ps.executeUpdate();
      ps.close();
    }
  }
  finally {
    DataAccess.cleanUp(con,ps,rs);
  }
}

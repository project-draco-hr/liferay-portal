{
  EntityCacheUtil.putResult(RegionModelImpl.ENTITY_CACHE_ENABLED,RegionImpl.class,region.getPrimaryKey(),region);
  FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_R,new Object[]{Long.valueOf(region.getCountryId()),region.getRegionCode()},region);
  region.resetOriginalValues();
}

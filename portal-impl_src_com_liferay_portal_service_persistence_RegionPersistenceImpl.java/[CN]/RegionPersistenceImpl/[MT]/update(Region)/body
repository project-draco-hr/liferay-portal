{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Region region) method. To improve performance, call update(Region region, boolean merge).");
  }
  return update(region,false);
}

{
  Region region=fetchByPrimaryKey(regionId);
  if (region == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + regionId);
    }
    throw new NoSuchRegionException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + regionId);
  }
  return region;
}

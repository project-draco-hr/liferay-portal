{
  for (  Region region : regions) {
    if (EntityCacheUtil.getResult(RegionModelImpl.ENTITY_CACHE_ENABLED,Region.class,region.getPrimaryKey(),this) == null) {
      cacheResult(region);
    }
  }
}

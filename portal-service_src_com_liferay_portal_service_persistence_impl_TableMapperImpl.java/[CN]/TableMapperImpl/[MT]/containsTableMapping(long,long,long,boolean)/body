{
  long[] rightPrimaryKeys=getPrimaryKeys(getLeftToRightPortalCache(companyId),getRightPrimaryKeysSqlQuery,companyId,leftPrimaryKey,updateCache);
  if (Arrays.binarySearch(rightPrimaryKeys,rightPrimaryKey) < 0) {
    return false;
  }
 else {
    return true;
  }
}

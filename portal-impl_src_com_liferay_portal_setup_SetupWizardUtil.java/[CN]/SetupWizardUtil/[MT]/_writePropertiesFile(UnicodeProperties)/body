{
  try {
    FileUtil.write(PropsValues.LIFERAY_HOME,PROPERTIES_FILE_NAME,unicodeProperties.toString());
    return FileUtil.exists(PropsValues.LIFERAY_HOME + StringPool.SLASH + PROPERTIES_FILE_NAME);
  }
 catch (  IOException ioe) {
    _log.error(ioe,ioe);
    return false;
  }
}

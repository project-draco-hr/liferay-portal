{
  Registry registry=RegistryUtil.getRegistry();
  String completeFilter="(&(objectClass=" + serviceClass.getName() + ")"+ filter+ ")";
  _serviceTracker=registry.trackServices(registry.getFilter(completeFilter));
}

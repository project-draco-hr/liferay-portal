{
  CacheKeyGenerator cacheKeyGenerator=null;
  if (_cacheKeyGeneratorCacheName != null) {
    cacheKeyGenerator=CacheKeyGeneratorUtil.getCacheKeyGenerator(_cacheKeyGeneratorCacheName);
  }
 else {
    cacheKeyGenerator=CacheKeyGeneratorUtil.getCacheKeyGenerator(FinderCache.class.getName());
  }
  return cacheKeyGenerator.getCacheKey(sb);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PresenceUser presenceUser) method. Use update(PresenceUser presenceUser, boolean merge) instead.");
  }
  return update(presenceUser,false);
}

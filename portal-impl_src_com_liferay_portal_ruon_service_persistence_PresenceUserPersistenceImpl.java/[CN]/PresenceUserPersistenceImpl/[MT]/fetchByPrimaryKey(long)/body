{
  Session session=null;
  try {
    session=openSession();
    return (PresenceUser)session.get(PresenceUserImpl.class,new Long(presenceUserId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

{
  Session session=null;
  try {
    session=openSession();
    session.delete(presenceUser);
    session.flush();
    return presenceUser;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
    FinderCache.clearCache(PresenceUser.class.getName());
  }
}

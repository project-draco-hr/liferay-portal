{
  if (Validator.isNull(userUuid)) {
    return _user.getUserId();
  }
  try {
    User user=UserLocalServiceUtil.getUserByUuid(userUuid,_user.getCompanyId());
    return user.getUserId();
  }
 catch (  Exception e) {
    return _user.getUserId();
  }
}

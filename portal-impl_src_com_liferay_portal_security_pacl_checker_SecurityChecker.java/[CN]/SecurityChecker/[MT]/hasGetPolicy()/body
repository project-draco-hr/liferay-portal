{
  Class<?> callerClass8=Reflection.getCallerClass(8);
  if (isGlassfishJ2EEInstanceListener(callerClass8.getEnclosingClass()) && CheckerUtil.isAccessControllerDoPrivileged(9)) {
    logGetPolicy(callerClass8,8);
    return true;
  }
  return false;
}

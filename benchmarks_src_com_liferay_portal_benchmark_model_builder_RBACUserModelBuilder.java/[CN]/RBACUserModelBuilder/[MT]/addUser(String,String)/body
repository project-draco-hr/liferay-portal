{
  validate();
  if (_contact == null) {
    throw new IllegalStateException("Must create a contact first");
  }
  _user=new User(_companyId,_generator.generate(),createScreenName(_contact.getFirstName(),_contact.getLastName()),createEmail(_contact.getFirstName(),_contact.getLastName(),domain),password,_contact);
}

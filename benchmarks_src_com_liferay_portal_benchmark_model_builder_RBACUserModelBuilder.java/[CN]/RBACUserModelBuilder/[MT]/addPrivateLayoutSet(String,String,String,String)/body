{
  validate();
  if ((_user == null) || (_user.getPrivateGroup() == null)) {
    throw new IllegalStateException("Must create a user and private group first");
  }
  _user.setPrivateLayoutSet(new LayoutSet(_generator.generate(),_user.getPrivateGroup().getGroupId(),_companyId,true,themeId,themeColorSchemeId,wapThemeId,wapColorSchemeId));
}

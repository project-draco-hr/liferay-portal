{
  validate();
  if (_user == null) {
    throw new IllegalStateException("Must create a user first");
  }
  _user.setPrivateGroup(new Group(_generator.generate(),_companyId,_user.getUserId(),ClassName.USER.getClassNameId(),_user.getUserId()));
}

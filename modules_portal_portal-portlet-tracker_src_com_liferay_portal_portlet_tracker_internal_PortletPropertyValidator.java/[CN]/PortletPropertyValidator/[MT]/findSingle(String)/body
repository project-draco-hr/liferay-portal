{
  for (  String temp : startsWith) {
    if (str.startsWith(temp)) {
      return true;
    }
  }
  for (  String temp : singleValue) {
    if (str.equals(temp)) {
      return true;
    }
  }
  return false;
}

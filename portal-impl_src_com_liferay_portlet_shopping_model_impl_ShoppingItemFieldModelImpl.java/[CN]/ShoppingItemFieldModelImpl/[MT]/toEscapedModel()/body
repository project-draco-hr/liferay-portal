{
  if (isEscapedModel()) {
    return (ShoppingItemField)this;
  }
 else {
    return (ShoppingItemField)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

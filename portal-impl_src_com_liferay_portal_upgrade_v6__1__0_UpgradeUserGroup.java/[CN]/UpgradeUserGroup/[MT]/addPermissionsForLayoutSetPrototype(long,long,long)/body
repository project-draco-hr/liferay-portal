{
  if (PropsValues.PERMISSIONS_USER_CHECK_ALGORITHM == 6) {
    addResourcePermission(companyId,userId,layoutSetPrototypeId);
  }
 else   if (PropsValues.PERMISSIONS_USER_CHECK_ALGORITHM == 5) {
    addResource(companyId);
    long layoutSetPrototypeResourceId=addResource(layoutSetPrototypeId);
    addPermission(companyId,ActionKeys.DELETE,layoutSetPrototypeResourceId);
    addPermission(companyId,ActionKeys.PERMISSIONS,layoutSetPrototypeResourceId);
    addPermission(companyId,ActionKeys.UPDATE,layoutSetPrototypeResourceId);
    addPermission(companyId,ActionKeys.VIEW,layoutSetPrototypeResourceId);
  }
}

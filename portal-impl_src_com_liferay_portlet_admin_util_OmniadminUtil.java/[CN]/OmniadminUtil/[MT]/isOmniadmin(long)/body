{
  try {
    User user=UserLocalServiceUtil.fetchUser(userId);
    if (user == null) {
      return false;
    }
 else {
      return isOmniadmin(user);
    }
  }
 catch (  SystemException se) {
    return false;
  }
}

{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  return applyFallbackKeys(PortletConstants.getRootPortletId(portletId),new PortletPreferencesSettings(getGroupPortletPreferences(group.getCompanyId(),groupId,portletId)));
}

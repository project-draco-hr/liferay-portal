{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  return applyFallbackKeys(new PortletPreferencesSettings(getGroupPortletPreferences(group.getCompanyId(),groupId,portletId)),PortletConstants.getRootPortletId(portletId));
}

{
  ResourceManager resourceManager=getResourceManager(settingsId);
  return new PropertiesSettings(getPortletProperties(settingsId,resourceManager),resourceManager,getPortalPropertiesSettings(settingsId));
}

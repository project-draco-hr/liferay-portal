{
  CascadingStyleSheet cascadingStyleSheet=CSSReader.readFromString(css,CCharset.CHARSET_UTF_8_OBJ,ECSSVersion.CSS30);
  CSSWriter cssWriter=new CSSWriter(new CSSWriterSettings(ECSSVersion.CSS30,true));
  return cssWriter.getCSSAsString(cascadingStyleSheet);
}

{
  try {
    indexKeywords(searchContext.getCompanyId(),searchContext.getLanguageId(),PropsKeys.INDEX_SEARCH_QUERY_SUGGESTION_FILE,Field.KEYWORD_SEARCH,KEYWORD_QUERY_TYPE,0);
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}

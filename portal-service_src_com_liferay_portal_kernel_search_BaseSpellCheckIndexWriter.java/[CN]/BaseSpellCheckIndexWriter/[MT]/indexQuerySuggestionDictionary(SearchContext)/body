{
  try {
    indexKeywords(searchContext.getCompanyId(),searchContext.getLanguageId(),PropsKeys.INDEX_SEARCH_QUERY_SUGGESTION_DICTIONARY,Field.KEYWORD_SEARCH,QUERY_SUGGESTION_TYPE,0);
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}

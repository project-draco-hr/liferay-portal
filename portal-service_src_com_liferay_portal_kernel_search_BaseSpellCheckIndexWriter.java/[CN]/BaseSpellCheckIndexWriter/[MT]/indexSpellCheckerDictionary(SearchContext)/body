{
  try {
    indexKeywords(searchContext.getCompanyId(),searchContext.getLanguageId(),PropsKeys.INDEX_SEARCH_SPELL_CHECKER_DICTIONARY,Field.SPELL_CHECK_WORD,SPELL_CHECKER_TYPE,0);
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}

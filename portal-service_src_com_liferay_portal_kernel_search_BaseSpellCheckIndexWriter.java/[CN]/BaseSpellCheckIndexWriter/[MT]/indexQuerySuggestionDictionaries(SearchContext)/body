{
  try {
    for (    String languageId : _SUPPORTED_LOCALES) {
      indexKeywords(searchContext.getCompanyId(),languageId,PropsKeys.INDEX_SEARCH_QUERY_SUGGESTION_DICTIONARY,Field.KEYWORD_SEARCH,QUERY_SUGGESTION_TYPE,0);
    }
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}

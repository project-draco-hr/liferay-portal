{
  try (InputStream inputstream=url.openStream()){
    _policy=Policy.getInstance(inputstream);
  }
 catch (  Exception e) {
    throw new IllegalStateException("Policy could not be initialized",e);
  }
}

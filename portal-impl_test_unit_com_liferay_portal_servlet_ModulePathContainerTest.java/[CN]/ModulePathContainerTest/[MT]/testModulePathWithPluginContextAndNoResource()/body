{
  String modulePath="/plugin-context:";
  Assert.assertEquals("/plugin-context",ComboServlet.getModuleContextPath(modulePath));
  Assert.assertEquals(StringPool.BLANK,ComboServlet.getResourcePath(modulePath));
}

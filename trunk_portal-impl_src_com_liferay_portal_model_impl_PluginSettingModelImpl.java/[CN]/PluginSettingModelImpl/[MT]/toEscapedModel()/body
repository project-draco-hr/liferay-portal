{
  if (isEscapedModel()) {
    return (PluginSetting)this;
  }
 else {
    return (PluginSetting)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

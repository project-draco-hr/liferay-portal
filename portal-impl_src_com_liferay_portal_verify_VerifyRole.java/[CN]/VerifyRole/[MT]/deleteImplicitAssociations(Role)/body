{
  try (Connection con=DataAccess.getUpgradeOptimizedConnection()){
    runSQL(con,"delete from UserGroupGroupRole where roleId = " + role.getRoleId());
    runSQL(con,"delete from UserGroupRole where roleId = " + role.getRoleId());
  }
 }

{
  Document doc=getFileDocument(companyId,portletId,groupId,repositoryId,fileName,fileEntryId,properties,modifiedDate,tagsEntries);
  if (doc != null) {
    SearchEngineUtil.updateDocument(companyId,doc.get(Field.UID),doc);
  }
}

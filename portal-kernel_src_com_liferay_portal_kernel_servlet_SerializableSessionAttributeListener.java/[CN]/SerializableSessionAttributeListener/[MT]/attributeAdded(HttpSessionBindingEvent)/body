{
  Object obj=event.getValue();
  if (!(obj instanceof Serializable)) {
    _log.error(obj.getClass().getName() + " is not serializable and will prevent this session from " + "being replicated");
  }
}

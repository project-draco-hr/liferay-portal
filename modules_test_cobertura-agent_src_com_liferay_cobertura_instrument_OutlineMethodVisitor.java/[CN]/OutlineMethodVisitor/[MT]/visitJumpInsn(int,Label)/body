{
  if ((_currentLine != 0) && !"<clinit>".equals(_methodNode.name) && (opcode != Opcodes.GOTO)&& (opcode != Opcodes.JSR)) {
    _classData.addLineJump(_currentLine,_currentJump);
    _jumpLabels.add(label);
  }
  super.visitJumpInsn(opcode,label);
}

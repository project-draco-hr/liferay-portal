{
  BaseModel<?> parentBaseModel=getSearchableAssetEntryParentBaseModel(_group,_serviceContext);
  for (  String unsortedValue : unsortedValues) {
    addSearchableAssetEntry(parentBaseModel,unsortedValue,ddmStructure,ddmTemplate,_serviceContext);
  }
}

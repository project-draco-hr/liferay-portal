{
  BaseModel<?> parentBaseModel=getSearchableAssetParentBaseModel(_group,_serviceContext);
  for (  String unsortedValue : unsortedValues) {
    addSearchableAsset(parentBaseModel,unsortedValue,ddmStructure,ddmTemplate,_serviceContext);
  }
}

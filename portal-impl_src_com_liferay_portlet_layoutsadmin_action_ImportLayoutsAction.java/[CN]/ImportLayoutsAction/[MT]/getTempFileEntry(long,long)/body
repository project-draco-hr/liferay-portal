{
  String[] tempFileEntryNames=LayoutServiceUtil.getTempFileEntryNames(groupId,_TEMP_FOLDER_NAME);
  if (tempFileEntryNames.length == 0) {
    return null;
  }
  return TempFileUtil.getTempFile(groupId,userId,tempFileEntryNames[0],_TEMP_FOLDER_NAME);
}

{
  MBCategory category=message.getCategory();
  if (MBBanLocalServiceUtil.hasBan(category.getGroupId(),permissionChecker.getUserId())) {
    return false;
  }
 else {
    return permissionChecker.hasPermission(category.getGroupId(),MBMessage.class.getName(),message.getMessageId(),actionId);
  }
}

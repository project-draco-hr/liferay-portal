{
  try {
    _persistence.countByNotC_G_P(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
    _persistence.countByNotC_G_P(0L,0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

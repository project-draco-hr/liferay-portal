{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PollsVote pollsVote) method. To improve performance, call update(PollsVote pollsVote, boolean merge).");
  }
  return update(pollsVote,false);
}

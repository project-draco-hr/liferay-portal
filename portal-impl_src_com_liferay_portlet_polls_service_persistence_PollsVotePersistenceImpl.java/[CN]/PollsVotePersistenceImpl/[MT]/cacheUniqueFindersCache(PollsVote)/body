{
  if (pollsVote.isNew()) {
    Object[] args=new Object[]{pollsVote.getUuid(),pollsVote.getGroupId()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_UUID_G,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_UUID_G,args,pollsVote);
    args=new Object[]{pollsVote.getQuestionId(),pollsVote.getUserId()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_Q_U,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_Q_U,args,pollsVote);
  }
 else {
    PollsVoteModelImpl pollsVoteModelImpl=(PollsVoteModelImpl)pollsVote;
    if ((pollsVoteModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_UUID_G.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{pollsVote.getUuid(),pollsVote.getGroupId()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_UUID_G,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_UUID_G,args,pollsVote);
    }
    if ((pollsVoteModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_Q_U.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{pollsVote.getQuestionId(),pollsVote.getUserId()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_Q_U,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_Q_U,args,pollsVote);
    }
  }
}

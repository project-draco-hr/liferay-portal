{
  for (  PollsVote pollsVote : pollsVotes) {
    if (EntityCacheUtil.getResult(PollsVoteModelImpl.ENTITY_CACHE_ENABLED,PollsVote.class,pollsVote.getPrimaryKey(),this) == null) {
      cacheResult(pollsVote);
    }
  }
}

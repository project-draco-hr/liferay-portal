{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MembershipInvitation membershipInvitation) method. Use update(MembershipInvitation membershipInvitation, boolean merge) instead.");
  }
  return update(membershipInvitation,false);
}

{
  ComboServletStaticURLGenerator comboServletStaticURLGenerator=new ComboServletStaticURLGenerator();
  comboServletStaticURLGenerator.setPortletResourceAccessors(PortletResourceAccessor.headerPortalCss);
  comboServletStaticURLGenerator.setVisitedURLs(new HashSet<String>());
  comboServletStaticURLGenerator.setURLPrefix(_URL_PREFIX);
  Portlet portlet1=buildPortlet("/portlet1","/css/main1.css","/css/main.css");
  Portlet portlet2=buildPortlet("/portlet2","/css/main2.css","/css/main3.css");
  List<String> urls1=comboServletStaticURLGenerator.generate(toList(portlet1,portlet2));
  comboServletStaticURLGenerator.setVisitedURLs(new HashSet<String>());
  List<String> urls2=comboServletStaticURLGenerator.generate(toList(portlet2,portlet1));
  Assert.assertEquals(urls1,urls2);
}

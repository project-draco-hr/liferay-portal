{
  StringMaker sm=new StringMaker();
  for (int i=0; i < roles.size(); i++) {
    sm.append(table);
    sm.append(".roleId = ?");
    if ((i + 1) < roles.size()) {
      sm.append(" OR ");
    }
  }
  return sm.toString();
}

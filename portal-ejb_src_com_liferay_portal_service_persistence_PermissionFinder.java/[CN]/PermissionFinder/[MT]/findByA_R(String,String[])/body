{
  List permissions=new ArrayList();
  for (int i=0; i < resourceIds.length; i++) {
    Permission permission=PermissionUtil.fetchByA_R(actionId,resourceIds[i]);
    if (permission != null) {
      permissions.add(permission);
    }
  }
  return permissions;
}

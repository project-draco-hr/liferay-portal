{
  JSONObject jsonObject=JenkinsResultsParserUtil.toJSONObject("http://" + master + "/build/"+ jobName+ "/api/json?tree=builds[actions[parameters"+ "[name,type,value]],building,duration,number,result,url]",false);
  return jsonObject.getJSONArray("builds");
}

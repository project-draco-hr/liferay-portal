{
  List<TrashEntry> trashEntries=ActionUtil.getTrashEntries(resourceRequest);
  resourceRequest.setAttribute(TrashWebKeys.TRASH_ENTRIES,trashEntries);
  include(resourceRequest,resourceResponse,"/info_panel.jsp");
}

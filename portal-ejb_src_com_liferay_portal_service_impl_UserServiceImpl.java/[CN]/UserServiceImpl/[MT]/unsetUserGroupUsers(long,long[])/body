{
  if (!UserGroupPermission.contains(getPermissionChecker(),userGroupId,ActionKeys.UPDATE) && !UserGroupPermission.contains(getPermissionChecker(),userGroupId,ActionKeys.ASSIGN_USERS)) {
    throw new PrincipalException();
  }
  UserLocalServiceUtil.unsetUserGroupUsers(userGroupId,userIds);
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BookmarksFolder bookmarksFolder) method. Use update(BookmarksFolder bookmarksFolder, boolean merge) instead.");
  }
  return update(bookmarksFolder,false);
}

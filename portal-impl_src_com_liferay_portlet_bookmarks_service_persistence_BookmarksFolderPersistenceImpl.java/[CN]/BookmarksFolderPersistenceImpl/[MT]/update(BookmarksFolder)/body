{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(BookmarksFolder bookmarksFolder) method. To improve performance, call update(BookmarksFolder bookmarksFolder, boolean merge).");
  }
  return update(bookmarksFolder,false);
}

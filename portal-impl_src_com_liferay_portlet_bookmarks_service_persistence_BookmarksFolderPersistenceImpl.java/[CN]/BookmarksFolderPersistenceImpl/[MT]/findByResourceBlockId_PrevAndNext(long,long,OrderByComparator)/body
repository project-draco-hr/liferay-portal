{
  BookmarksFolder bookmarksFolder=findByPrimaryKey(folderId);
  Session session=null;
  try {
    session=openSession();
    BookmarksFolder[] array=new BookmarksFolderImpl[3];
    array[0]=getByResourceBlockId_PrevAndNext(session,bookmarksFolder,resourceBlockId,orderByComparator,true);
    array[1]=bookmarksFolder;
    array[2]=getByResourceBlockId_PrevAndNext(session,bookmarksFolder,resourceBlockId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

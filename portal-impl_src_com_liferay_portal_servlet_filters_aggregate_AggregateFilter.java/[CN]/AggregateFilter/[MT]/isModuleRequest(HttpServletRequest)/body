{
  String requestURI=request.getRequestURI();
  String contextPath=PortalWebResourcesUtil.getContextPath(PortalWebResourceConstants.RESOURCE_TYPE_JS);
  if (requestURI.startsWith(contextPath)) {
    return false;
  }
  return super.isModuleRequest(request);
}

{
  String[] names=null;
  String[] descriptions=null;
  boolean andOperator=false;
  if (Validator.isNotNull(keywords)) {
    descriptions=CustomSQLUtil.keywords(descriptions);
    names=CustomSQLUtil.keywords(names);
    classNameIds=CustomSQLUtil.keywords(classNameIds);
    structureKeys=CustomSQLUtil.keywords(structureKeys);
    storageTypes=CustomSQLUtil.keywords(storageTypes);
  }
 else {
    andOperator=true;
  }
  return countByC_G_CN_S_ST_D(companyId,groupId,classNameIds,structureKeys,storageTypes,names,descriptions,andOperator);
}

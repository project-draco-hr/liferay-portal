{
  int count=countByU_R_D(userId,read,deleted);
  if (count == 0) {
    return null;
  }
  List<UserThread> list=findByU_R_D(userId,read,deleted,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

{
  String categoryKey=(String)serviceReference.getProperty("panel.category");
  if (Validator.isNull(categoryKey)) {
    _log.error("Unable to register panel entry because of missing " + "service property \"panel.category\"");
  }
 else {
    emitter.emit(categoryKey);
  }
}

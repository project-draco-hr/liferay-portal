{
  StorageAdapterRegistry storageAdapterRegistry=StorageAdapterRegistryUtil.getStorageAdapterRegistry();
  Set<String> storageTypes=storageAdapterRegistry.getStorageTypes();
  Assert.assertEquals(2,storageTypes.size());
  Assert.assertTrue(storageTypes.contains("json"));
  Assert.assertTrue(storageTypes.contains(TestStorageAdapterImpl.STORAGE_TYPE));
}

{
  try {
    _persistence.countByU1_U2(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
    _persistence.countByU1_U2(0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

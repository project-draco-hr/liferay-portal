{
  if (template.equals(TEMPLATE_ABSTRACT) || template.equals(TEMPLATE_FULL_CONTENT)) {
    renderRequest.setAttribute(WebKeys.DYNAMIC_DATA_LISTS_RECORD,_record);
    String path="/html/portlet/dynamic_data_lists/asset/" + template + ".jsp";
    return path;
  }
 else {
    return null;
  }
}

{
  if (_log.isDebugEnabled()) {
    Thread.dumpStack();
  }
  Class<?> clazz=permission.getClass();
  String name=permission.getName();
  String actions=permission.getActions();
  if (Validator.isNotNull(actions)) {
    throw new SecurityException("Permission " + clazz.getName() + " attempted to "+ name+ " on "+ actions);
  }
 else {
    throw new SecurityException("Permission " + clazz.getName() + " attempted to "+ name);
  }
}

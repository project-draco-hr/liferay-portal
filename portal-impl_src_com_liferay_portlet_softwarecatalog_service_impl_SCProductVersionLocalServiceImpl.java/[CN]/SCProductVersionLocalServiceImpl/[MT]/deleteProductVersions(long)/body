{
  List<SCProductVersion> productVersions=scProductVersionPersistence.findByProductEntryId(productEntryId);
  for (  SCProductVersion productVersion : productVersions) {
    deleteProductVersion(productVersion);
  }
}

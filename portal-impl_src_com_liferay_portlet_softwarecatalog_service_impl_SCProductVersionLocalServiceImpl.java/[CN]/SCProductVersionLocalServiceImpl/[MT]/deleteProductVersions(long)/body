{
  Iterator itr=scProductVersionPersistence.findByProductEntryId(productEntryId).iterator();
  while (itr.hasNext()) {
    SCProductVersion productVersion=(SCProductVersion)itr.next();
    deleteProductVersion(productVersion);
  }
}

{
  ListType type=null;
  try {
    type=ListTypeServiceUtil.getListType(getTypeId());
  }
 catch (  Exception e) {
    type=new ListTypeImpl();
    if (_log.isWarnEnabled()) {
      _log.warn(e);
    }
  }
  return type;
}

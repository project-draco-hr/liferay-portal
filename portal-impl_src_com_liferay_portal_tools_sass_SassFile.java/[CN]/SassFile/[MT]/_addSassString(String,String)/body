{
  sassContent=sassContent.trim();
  if (sassContent.isEmpty()) {
    return;
  }
  SassString sassString=new SassString(_sassExecutor,fileName,sassContent);
  _fragments.add(sassString);
  sassString.call();
}

{
  if (_service == null) {
    _service=(RevisionService)PortalBeanLocatorUtil.locate(RevisionService.class.getName());
    ReferenceRegistry.registerReference(RevisionServiceUtil.class,"_service");
    MethodCache.remove(RevisionService.class);
  }
  return _service;
}

{
  if (!contains(permissionChecker,column,actionId)) {
    throw new PrincipalException.MustHavePermission(permissionChecker.getUserId(),ExpandoColumn.class.getName(),column.getColumnId(),actionId);
  }
}

{
  Destination destination=MessageBusUtil.getMessageBus().getDestination(DestinationNames.WORKFLOW_ENGINE);
  if ((destination != null) && (destination.getMessageListenerCount() > 0)) {
    return true;
  }
  return false;
}

{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof PortletCacheModel)) {
    return false;
  }
  PortletCacheModel portletCacheModel=(PortletCacheModel)obj;
  if ((id == portletCacheModel.id) && (mvccVersion == portletCacheModel.mvccVersion)) {
    return true;
  }
  return false;
}

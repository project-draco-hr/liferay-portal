{
  Random rand=new Random();
  char[] result=new char[length];
  for (int i=0; i < length; i++) {
    result[i]=_ALPHABET_AND_DIGITS[rand.nextInt(_ALPHABET_AND_DIGITS.length)];
  }
  return new String(result);
}

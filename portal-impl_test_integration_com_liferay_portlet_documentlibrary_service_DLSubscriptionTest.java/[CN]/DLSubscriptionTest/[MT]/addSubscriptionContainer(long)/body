{
  long classPK=containerId;
  if (containerId == DEFAULT_PARENT_CONTAINER_ID) {
    classPK=group.getGroupId();
  }
  SubscriptionLocalServiceUtil.addSubscription(TestPropsValues.getUserId(),group.getGroupId(),Folder.class.getName(),classPK);
}

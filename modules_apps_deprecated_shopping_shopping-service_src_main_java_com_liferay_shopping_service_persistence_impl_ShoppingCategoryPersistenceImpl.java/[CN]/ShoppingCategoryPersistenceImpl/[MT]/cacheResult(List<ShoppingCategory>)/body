{
  for (  ShoppingCategory shoppingCategory : shoppingCategories) {
    if (entityCache.getResult(ShoppingCategoryModelImpl.ENTITY_CACHE_ENABLED,ShoppingCategoryImpl.class,shoppingCategory.getPrimaryKey()) == null) {
      cacheResult(shoppingCategory);
    }
 else {
      shoppingCategory.resetOriginalValues();
    }
  }
}

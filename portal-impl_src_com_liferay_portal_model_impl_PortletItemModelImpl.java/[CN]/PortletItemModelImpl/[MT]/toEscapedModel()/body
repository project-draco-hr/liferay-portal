{
  if (isEscapedModel()) {
    return (PortletItem)this;
  }
 else {
    return (PortletItem)Proxy.newProxyInstance(PortletItem.class.getClassLoader(),new Class[]{PortletItem.class},new AutoEscapeBeanHandler(this));
  }
}

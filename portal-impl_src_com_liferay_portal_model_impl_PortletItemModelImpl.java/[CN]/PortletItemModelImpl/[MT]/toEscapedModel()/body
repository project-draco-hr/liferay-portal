{
  if (isEscapedModel()) {
    return (PortletItem)this;
  }
 else {
    PortletItem model=new PortletItemImpl();
    model.setEscapedModel(true);
    model.setPortletItemId(getPortletItemId());
    model.setCompanyId(getCompanyId());
    model.setUserId(getUserId());
    model.setUserName(Html.escape(getUserName()));
    model.setCreateDate(getCreateDate());
    model.setModifiedDate(getModifiedDate());
    model.setGroupId(getGroupId());
    model.setPortletId(Html.escape(getPortletId()));
    model.setClassNameId(getClassNameId());
    model.setName(Html.escape(getName()));
    model=(PortletItem)Proxy.newProxyInstance(PortletItem.class.getClassLoader(),new Class[]{PortletItem.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

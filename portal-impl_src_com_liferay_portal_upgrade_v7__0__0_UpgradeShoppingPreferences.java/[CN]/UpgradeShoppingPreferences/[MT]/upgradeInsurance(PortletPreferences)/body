{
  String insurance=portletPreferences.getValue("insurance",StringPool.BLANK);
  if (Validator.isNotNull(insurance)) {
    portletPreferences.setValues("insurance",StringUtil.split(insurance));
  }
}

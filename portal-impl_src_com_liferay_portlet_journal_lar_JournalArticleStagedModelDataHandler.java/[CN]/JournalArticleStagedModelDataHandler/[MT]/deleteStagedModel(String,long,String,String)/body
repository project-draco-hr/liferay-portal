{
  JournalArticle article=fetchExistingStagedModel(uuid,groupId);
  if (article == null) {
    return;
  }
  JSONObject extraDataJSONObject=JSONFactoryUtil.createJSONObject(extraData);
  if (Validator.isNull(extraData) || extraDataJSONObject.getBoolean("inTrash")) {
  }
 else {
    double version=extraDataJSONObject.getDouble("version");
    article=JournalArticleLocalServiceUtil.getArticle(groupId,article.getArticleId(),version);
  }
  JournalArticleLocalServiceUtil.deleteArticle(article);
}

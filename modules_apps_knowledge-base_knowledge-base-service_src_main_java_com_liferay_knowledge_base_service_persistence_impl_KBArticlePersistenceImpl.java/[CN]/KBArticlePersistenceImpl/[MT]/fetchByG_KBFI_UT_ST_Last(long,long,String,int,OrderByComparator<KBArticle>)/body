{
  int count=countByG_KBFI_UT_ST(groupId,kbFolderId,urlTitle,status);
  if (count == 0) {
    return null;
  }
  List<KBArticle> list=findByG_KBFI_UT_ST(groupId,kbFolderId,urlTitle,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

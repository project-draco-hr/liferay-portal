{
  int count=countByG_S_S(groupId,sections,status);
  if (count == 0) {
    return null;
  }
  List<KBArticle> list=findByG_S_S(groupId,sections,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

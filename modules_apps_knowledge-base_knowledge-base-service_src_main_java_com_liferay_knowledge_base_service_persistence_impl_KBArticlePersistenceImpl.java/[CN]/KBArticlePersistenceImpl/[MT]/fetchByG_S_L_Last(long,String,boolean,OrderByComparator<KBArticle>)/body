{
  int count=countByG_S_L(groupId,sections,latest);
  if (count == 0) {
    return null;
  }
  List<KBArticle> list=findByG_S_L(groupId,sections,latest,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

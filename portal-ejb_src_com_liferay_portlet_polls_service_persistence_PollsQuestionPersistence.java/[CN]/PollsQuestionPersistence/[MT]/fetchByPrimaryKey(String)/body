{
  Session session=null;
  try {
    session=openSession();
    return (PollsQuestion)session.get(PollsQuestionImpl.class,questionId);
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

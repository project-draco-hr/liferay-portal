{
  try (Repository repository=RepositoryCache.open(FileKey.exact(gitDir,FS.DETECTED))){
    if (UpToDateUtil.isClean(new Git(repository),relativePath) && !UpToDateUtil.hasChangedSince(repository,relativePath,since,ignoredMessagePattern)) {
      return true;
    }
    return false;
  }
 }

{
  Locale defaultLocale=LocaleThreadLocal.getDefaultLocale();
  Locale nullableLocale=null;
  try {
    String format=_languageImpl.format(nullableLocale,_LANG_KEY_WITH_ARGUMENT,"31");
    String expectedMessage=_languageImpl.format(nullableLocale,_LANG_KEY_WITH_ARGUMENT,"31");
    Assert.assertEquals(expectedMessage,format);
  }
  finally {
    LocaleThreadLocal.setDefaultLocale(defaultLocale);
  }
}

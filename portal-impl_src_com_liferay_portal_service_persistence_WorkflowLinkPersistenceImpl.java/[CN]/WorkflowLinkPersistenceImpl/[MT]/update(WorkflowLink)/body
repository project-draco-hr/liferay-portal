{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WorkflowLink workflowLink) method. Use update(WorkflowLink workflowLink, boolean merge) instead.");
  }
  return update(workflowLink,false);
}

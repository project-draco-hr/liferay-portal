{
  SRProductEntry productEntry=productVersion.getProductEntry();
  return permissionChecker.hasPermission(productEntry.getGroupId(),SRProductVersion.class.getName(),productVersion.getPrimaryKey(),actionId);
}

{
  List<JournalArticleImage> images=new ArrayList<JournalArticleImage>();
  for (  BinaryContent binaryContent : binaryContents) {
    images.add(((JournalImageContent)binaryContent).getJournalImage());
  }
  return images;
}

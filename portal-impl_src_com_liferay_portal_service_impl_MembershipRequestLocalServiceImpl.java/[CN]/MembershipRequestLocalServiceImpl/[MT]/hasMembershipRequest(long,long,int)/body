{
  List<MembershipRequest> membershipRequests=membershipRequestPersistence.findByG_U_S(groupId,userId,statusId);
  if (membershipRequests.isEmpty()) {
    return false;
  }
 else {
    return true;
  }
}

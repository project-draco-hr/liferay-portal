{
  List<Long> administratorIds=getGroupAdministratorIds(membershipRequest.getGroupId());
  for (  Long userId : administratorIds) {
    notify(userId,membershipRequest,PropsKeys.COMMUNITIES_EMAIL_MEMBERSHIP_REQUEST_SUBJECT,PropsKeys.COMMUNITIES_EMAIL_MEMBERSHIP_REQUEST_BODY);
  }
}

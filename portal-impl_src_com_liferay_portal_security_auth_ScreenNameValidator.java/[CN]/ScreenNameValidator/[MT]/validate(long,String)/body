{
  if (Validator.isNull(screenName) || Validator.isEmailAddress(screenName) || (screenName.equalsIgnoreCase("cyrus"))|| (screenName.equalsIgnoreCase("postfix"))|| (screenName.indexOf("_") != -1)|| (screenName.indexOf("/") != -1)) {
    return false;
  }
 else {
    return true;
  }
}

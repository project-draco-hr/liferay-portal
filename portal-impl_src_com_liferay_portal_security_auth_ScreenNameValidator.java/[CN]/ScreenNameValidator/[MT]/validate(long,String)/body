{
  if (Validator.isNull(screenName) || Validator.isEmailAddress(screenName) || (screenName.equalsIgnoreCase("cyrus"))|| (screenName.equalsIgnoreCase("postfix"))|| (screenName.indexOf(StringPool.UNDERLINE) != -1)|| (screenName.indexOf(StringPool.SLASH) != -1)) {
    return false;
  }
 else {
    return true;
  }
}

{
  ServletRequest req=pageContext.getRequest();
  req.setAttribute("liferay-ui:input-textarea:param",_param);
  req.setAttribute("liferay-ui:input-textarea:defaultValue",_defaultValue);
  req.setAttribute("liferay-ui:input-textarea:disabled",String.valueOf(_disabled));
  return EVAL_BODY_BUFFERED;
}

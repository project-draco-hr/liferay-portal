{
  try {
    return _groupLocalService.getUserPersonalSiteGroup(companyId);
  }
 catch (  PortalException pe) {
    _log.error("Unable to get user personal site group in company " + companyId,pe);
  }
  return null;
}

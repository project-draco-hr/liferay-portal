{
  resourceLocalService.addModelResources(proposal.getCompanyId(),proposal.getGroupId(),proposal.getUserId(),TasksProposal.class.getName(),proposal.getProposalId(),communityPermissions,guestPermissions);
}

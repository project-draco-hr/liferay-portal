{
  resourceLocalService.addResources(proposal.getCompanyId(),proposal.getGroupId(),proposal.getUserId(),TasksProposal.class.getName(),proposal.getProposalId(),false,addCommunityPermissions,addGuestPermissions);
}

{
  super.setUp();
  _userIds=new long[THREAD_COUNT];
  for (int i=0; i < THREAD_COUNT; i++) {
    User user=addUser("ResourceLocalServiceTest" + (i + 1),false,new long[]{TestPropsValues.getGroupId()});
    _userIds[i]=user.getUserId();
  }
}

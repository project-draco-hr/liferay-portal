{
  String versionNumber=getHeadVersionNumber(companyId,repositoryId,fileName);
  Entry fileEntry=getVersionedFileEntry(companyId,repositoryId,fileName,versionNumber);
  CMISObject cmisObject=fileEntry.getFirstChild(_cmisConstants.OBJECT);
  return cmisObject.getContentStreamLength();
}

{
  double versionNumber=getHeadVersionNumber(companyId,repositoryId,fileName);
  Entry fileEntry=getVersionedFile(companyId,repositoryId,fileName,versionNumber);
  CMISObject cmisObject=fileEntry.getFirstChild(_constants.OBJECT);
  return cmisObject.getContentStreamLength();
}

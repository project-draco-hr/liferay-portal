{
  _systemRootDir=getFolder(_session.getRootFolder(),PropsValues.DL_HOOK_CMIS_SYSTEM_ROOT_DIR);
  if (_systemRootDir == null) {
    _systemRootDir=createFolder(_session.getRootFolder(),PropsValues.DL_HOOK_CMIS_SYSTEM_ROOT_DIR);
  }
}

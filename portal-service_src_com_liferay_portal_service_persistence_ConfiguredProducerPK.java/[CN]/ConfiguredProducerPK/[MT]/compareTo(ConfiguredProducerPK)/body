{
  if (pk == null) {
    return -1;
  }
  int value=0;
  value=portalId.compareTo(pk.portalId);
  if (value != 0) {
    return value;
  }
  value=configuredProducerId.compareTo(pk.configuredProducerId);
  if (value != 0) {
    return value;
  }
  return 0;
}

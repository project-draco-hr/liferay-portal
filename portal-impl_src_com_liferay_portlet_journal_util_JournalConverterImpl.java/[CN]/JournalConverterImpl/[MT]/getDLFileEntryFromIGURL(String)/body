{
  Matcher m=_imageGalleryURLPattern.matcher(url);
  if (!m.find()) {
    return null;
  }
  long groupId=GetterUtil.getLong(m.group(2));
  return DLAppLocalServiceUtil.getFileEntryByUuidAndGroupId(m.group(1),groupId);
}

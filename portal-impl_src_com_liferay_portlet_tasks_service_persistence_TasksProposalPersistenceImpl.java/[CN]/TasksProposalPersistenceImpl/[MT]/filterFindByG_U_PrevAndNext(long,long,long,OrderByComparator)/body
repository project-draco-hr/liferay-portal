{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByG_U_PrevAndNext(proposalId,groupId,userId,orderByComparator);
  }
  TasksProposal tasksProposal=findByPrimaryKey(proposalId);
  Session session=null;
  try {
    session=openSession();
    TasksProposal[] array=new TasksProposalImpl[3];
    array[0]=filterGetByG_U_PrevAndNext(session,tasksProposal,groupId,userId,orderByComparator,true);
    array[1]=tasksProposal;
    array[2]=filterGetByG_U_PrevAndNext(session,tasksProposal,groupId,userId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

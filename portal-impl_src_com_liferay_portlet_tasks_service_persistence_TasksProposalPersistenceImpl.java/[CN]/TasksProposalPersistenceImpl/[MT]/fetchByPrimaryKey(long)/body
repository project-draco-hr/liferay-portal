{
  TasksProposal result=(TasksProposal)EntityCacheUtil.getResult(TasksProposalModelImpl.ENTITY_CACHE_ENABLED,TasksProposal.class,proposalId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      TasksProposal tasksProposal=(TasksProposal)session.get(TasksProposalImpl.class,new Long(proposalId));
      if (tasksProposal != null) {
        cacheResult(tasksProposal);
      }
      return tasksProposal;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (TasksProposal)result;
  }
}

{
  TasksProposal tasksProposal=(TasksProposal)EntityCacheUtil.getResult(TasksProposalModelImpl.ENTITY_CACHE_ENABLED,TasksProposalImpl.class,proposalId,this);
  if (tasksProposal == null) {
    Session session=null;
    try {
      session=openSession();
      tasksProposal=(TasksProposal)session.get(TasksProposalImpl.class,new Long(proposalId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (tasksProposal != null) {
        cacheResult(tasksProposal);
      }
      closeSession(session);
    }
  }
  return tasksProposal;
}

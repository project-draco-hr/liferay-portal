{
  for (  TasksProposal tasksProposal : tasksProposals) {
    if (EntityCacheUtil.getResult(TasksProposalModelImpl.ENTITY_CACHE_ENABLED,TasksProposal.class,tasksProposal.getPrimaryKey(),this) == null) {
      cacheResult(tasksProposal);
    }
  }
}

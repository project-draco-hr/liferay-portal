{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TasksProposal tasksProposal) method. To improve performance, call update(TasksProposal tasksProposal, boolean merge).");
  }
  return update(tasksProposal,false);
}

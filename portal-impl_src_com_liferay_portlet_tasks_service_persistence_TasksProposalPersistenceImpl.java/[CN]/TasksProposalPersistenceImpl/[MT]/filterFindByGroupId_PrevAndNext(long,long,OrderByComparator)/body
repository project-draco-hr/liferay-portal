{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByGroupId_PrevAndNext(proposalId,groupId,orderByComparator);
  }
  TasksProposal tasksProposal=findByPrimaryKey(proposalId);
  Session session=null;
  try {
    session=openSession();
    TasksProposal[] array=new TasksProposalImpl[3];
    array[0]=filterGetByGroupId_PrevAndNext(session,tasksProposal,groupId,orderByComparator,true);
    array[1]=tasksProposal;
    array[2]=filterGetByGroupId_PrevAndNext(session,tasksProposal,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

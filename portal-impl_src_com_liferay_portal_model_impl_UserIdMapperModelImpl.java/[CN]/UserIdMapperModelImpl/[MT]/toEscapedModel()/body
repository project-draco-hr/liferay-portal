{
  if (isEscapedModel()) {
    return (UserIdMapper)this;
  }
 else {
    return (UserIdMapper)Proxy.newProxyInstance(UserIdMapper.class.getClassLoader(),new Class[]{UserIdMapper.class},new AutoEscapeBeanHandler(this));
  }
}

{
  MBDiscussionPermission.check(getPermissionChecker(),permissionClassName,permissionClassPK,messageId,permissionOwnerId,ActionKeys.UPDATE_DISCUSSION);
  return mbMessageLocalService.updateDiscussionMessage(getUserId(),messageId,className,classPK,subject,body,serviceContext);
}

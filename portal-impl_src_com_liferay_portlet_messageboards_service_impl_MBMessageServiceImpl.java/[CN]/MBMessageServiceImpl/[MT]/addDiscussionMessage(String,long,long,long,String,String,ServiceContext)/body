{
  MBDiscussionPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),className,classPK,ActionKeys.ADD_DISCUSSION);
  return mbMessageLocalService.addDiscussionMessage(getUserId(),null,className,classPK,threadId,parentMessageId,subject,body,serviceContext);
}

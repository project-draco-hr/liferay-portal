{
  MBDiscussionPermission.check(getPermissionChecker(),permissionClassName,permissionClassPK,messageId,permissionOwnerId,ActionKeys.DELETE_DISCUSSION);
  mbMessageLocalService.deleteDiscussionMessage(messageId);
}

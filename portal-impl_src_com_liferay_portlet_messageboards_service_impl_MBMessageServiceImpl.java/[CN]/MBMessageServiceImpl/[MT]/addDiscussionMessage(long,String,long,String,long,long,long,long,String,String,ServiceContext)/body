{
  User user=getGuestOrUser();
  return addDiscussionMessage(user.getUserId(),groupId,className,classPK,permissionClassName,permissionClassPK,permissionOwnerId,threadId,parentMessageId,subject,body,serviceContext);
}

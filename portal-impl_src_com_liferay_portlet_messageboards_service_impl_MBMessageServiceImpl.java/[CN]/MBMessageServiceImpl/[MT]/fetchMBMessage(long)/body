{
  MBMessage mbMessage=mbMessageLocalService.fetchMBMessage(messageId);
  if (mbMessage != null) {
    MBMessagePermission.check(getPermissionChecker(),mbMessage,ActionKeys.VIEW);
  }
  return mbMessage;
}

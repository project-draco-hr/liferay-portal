{
  MBDiscussionPermission.check(getPermissionChecker(),permissionClassName,permissionClassPK,messageId,ActionKeys.UPDATE_DISCUSSION);
  return mbMessageLocalService.updateDiscussionMessage(getUserId(),messageId,className,classPK,subject,body,serviceContext);
}

{
  MBDiscussionPermission.check(getPermissionChecker(),groupId,className,classPK,ActionKeys.ADD_DISCUSSION);
  return mbMessageLocalService.addDiscussionMessage(getUserId(),groupId,className,classPK,threadId,parentMessageId,subject,body,themeDisplay);
}

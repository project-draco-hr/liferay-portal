{
  User user=getUser();
  MBDiscussionPermission.check(getPermissionChecker(),user.getCompanyId(),serviceContext.getScopeGroupId(),className,classPK,messageId,user.getUserId(),ActionKeys.UPDATE_DISCUSSION);
  return mbMessageLocalService.updateStatus(getUserId(),messageId,serviceContext);
}

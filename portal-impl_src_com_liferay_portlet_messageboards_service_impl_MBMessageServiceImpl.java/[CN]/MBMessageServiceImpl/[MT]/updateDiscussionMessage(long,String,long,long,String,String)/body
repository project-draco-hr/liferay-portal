{
  MBDiscussionPermission.check(getPermissionChecker(),groupId,className,classPK,ActionKeys.UPDATE_DISCUSSION);
  return MBMessageLocalServiceUtil.updateDiscussionMessage(getUserId(),messageId,subject,body);
}

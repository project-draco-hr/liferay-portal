{
  MBCategory category=mbCategoryPersistence.findByPrimaryKey(categoryId);
  return addMessage(category.getGroupId(),categoryId,subject,body,MBMessageConstants.DEFAULT_FORMAT,Collections.emptyList(),false,0.0,false,serviceContext);
}

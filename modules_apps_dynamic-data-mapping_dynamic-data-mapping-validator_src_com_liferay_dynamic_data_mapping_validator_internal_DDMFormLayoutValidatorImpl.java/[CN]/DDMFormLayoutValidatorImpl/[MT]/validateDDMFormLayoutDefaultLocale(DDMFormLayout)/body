{
  Locale defaultLocale=ddmFormLayout.getDefaultLocale();
  if (defaultLocale == null) {
    throw new DDMFormLayoutValidationException("The default locale property was never set for DDM form " + "layout");
  }
}

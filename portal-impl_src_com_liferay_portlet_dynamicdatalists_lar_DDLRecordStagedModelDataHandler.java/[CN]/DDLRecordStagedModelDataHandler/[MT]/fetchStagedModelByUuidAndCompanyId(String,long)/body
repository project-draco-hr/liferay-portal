{
  List<DDLRecord> records=DDLRecordLocalServiceUtil.getDDLRecordsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<DDLRecord>());
  if (ListUtil.isEmpty(records)) {
    return null;
  }
  return records.get(0);
}

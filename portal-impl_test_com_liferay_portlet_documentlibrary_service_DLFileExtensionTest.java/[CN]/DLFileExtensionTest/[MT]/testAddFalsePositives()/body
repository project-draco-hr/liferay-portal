{
  FileEntry fileEntry=addFileEntry(false,_FILE_NAME,_FILE_NAME);
  try {
    FileEntry tempFileEntry=addFileEntry(false,_STRIPPED_FILE_NAME,_STRIPPED_FILE_NAME);
    DLAppLocalServiceUtil.deleteFileEntry(tempFileEntry.getFileEntryId());
  }
 catch (  DuplicateFileException dfe) {
    fail("Could not create" + _FAIL_DUPLICATE_MESSAGE);
  }
  DLAppLocalServiceUtil.deleteFileEntry(fileEntry.getFileEntryId());
  fileEntry=addFileEntry(false,_STRIPPED_FILE_NAME,_STRIPPED_FILE_NAME);
  try {
    FileEntry tempFileEntry=addFileEntry(false,_FILE_NAME,_FILE_NAME);
    DLAppLocalServiceUtil.deleteFileEntry(tempFileEntry.getFileEntryId());
  }
 catch (  DuplicateFileException dfe) {
    fail("Could not create" + _FAIL_DUPLICATE_MESSAGE);
  }
  DLAppLocalServiceUtil.deleteFileEntry(fileEntry.getFileEntryId());
}

{
  Session session=null;
  try {
    session=openSession();
    return (Announcement)session.get(AnnouncementImpl.class,new Long(announcementId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}

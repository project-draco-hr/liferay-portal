{
  List<DeploymentDescription> defaultDeploymentDescriptions=super.generate(testClass);
  if ((defaultDeploymentDescriptions != null) && (defaultDeploymentDescriptions.size() > 0)) {
    return defaultDeploymentDescriptions;
  }
  ArrayList<DeploymentDescription> deploymentDescriptions=new ArrayList<DeploymentDescription>();
  WebArchive webArchive=WebArchiveBuilder.build();
  DeploymentDescription deploymentDescription=new DeploymentDescription(webArchive.getName(),webArchive);
  deploymentDescription.shouldBeTestable(true);
  deploymentDescriptions.add(deploymentDescription);
  return deploymentDescriptions;
}

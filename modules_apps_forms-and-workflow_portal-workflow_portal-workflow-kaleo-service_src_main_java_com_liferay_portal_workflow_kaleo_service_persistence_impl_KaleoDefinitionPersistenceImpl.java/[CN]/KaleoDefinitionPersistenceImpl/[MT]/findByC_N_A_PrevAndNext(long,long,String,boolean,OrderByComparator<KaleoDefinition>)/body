{
  KaleoDefinition kaleoDefinition=findByPrimaryKey(kaleoDefinitionId);
  Session session=null;
  try {
    session=openSession();
    KaleoDefinition[] array=new KaleoDefinitionImpl[3];
    array[0]=getByC_N_A_PrevAndNext(session,kaleoDefinition,companyId,name,active,orderByComparator,true);
    array[1]=kaleoDefinition;
    array[2]=getByC_N_A_PrevAndNext(session,kaleoDefinition,companyId,name,active,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

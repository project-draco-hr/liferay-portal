{
  ShoppingItem newShoppingItem=addShoppingItem();
  _persistence.clearCache();
  ShoppingItem existingShoppingItem=_persistence.findByPrimaryKey(newShoppingItem.getPrimaryKey());
  Assert.assertEquals(existingShoppingItem.getSmallImageId(),ReflectionTestUtil.invoke(existingShoppingItem,"getOriginalSmallImageId",new Class<?>[0]));
  Assert.assertEquals(existingShoppingItem.getMediumImageId(),ReflectionTestUtil.invoke(existingShoppingItem,"getOriginalMediumImageId",new Class<?>[0]));
  Assert.assertEquals(existingShoppingItem.getLargeImageId(),ReflectionTestUtil.invoke(existingShoppingItem,"getOriginalLargeImageId",new Class<?>[0]));
  Assert.assertEquals(existingShoppingItem.getCompanyId(),ReflectionTestUtil.invoke(existingShoppingItem,"getOriginalCompanyId",new Class<?>[0]));
  Assert.assertTrue(Validator.equals(existingShoppingItem.getSku(),ReflectionTestUtil.invoke(existingShoppingItem,"getOriginalSku",new Class<?>[0])));
}

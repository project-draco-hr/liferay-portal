{
  if (portletConfig instanceof PortletConfigImpl) {
    ConfigurationPortletPortletConfig cppc=new ConfigurationPortletPortletConfig((PortletConfigImpl)portletConfig);
    super.init(cppc);
  }
 else {
    super.init(portletConfig);
  }
}

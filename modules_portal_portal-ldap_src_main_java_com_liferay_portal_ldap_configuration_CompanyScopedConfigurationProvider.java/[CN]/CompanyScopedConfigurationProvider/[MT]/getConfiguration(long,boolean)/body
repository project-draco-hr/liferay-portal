{
  Dictionary<String,Object> properties=getConfigurationProperties(companyId);
  if (!useDefault && (properties == null)) {
    return null;
  }
  T configurable=Configurable.createConfigurable(getMetatype(),properties);
  return configurable;
}

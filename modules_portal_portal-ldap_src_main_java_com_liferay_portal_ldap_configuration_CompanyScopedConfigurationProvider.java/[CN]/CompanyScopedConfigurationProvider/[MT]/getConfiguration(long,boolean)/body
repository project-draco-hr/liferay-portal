{
  Dictionary<String,Object> properties=getConfigurationProperties(companyId,useDefault);
  if (properties == null) {
    return null;
  }
  T configurable=Configurable.createConfigurable(getMetatype(),properties);
  return configurable;
}

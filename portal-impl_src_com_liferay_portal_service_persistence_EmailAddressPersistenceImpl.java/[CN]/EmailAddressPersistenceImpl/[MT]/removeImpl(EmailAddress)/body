{
  emailAddress=toUnwrappedModel(emailAddress);
  Session session=null;
  try {
    session=openSession();
    if (emailAddress.isCachedModel()) {
      emailAddress=(EmailAddress)session.get(EmailAddressImpl.class,emailAddress.getPrimaryKeyObj());
    }
    session.delete(emailAddress);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(emailAddress);
  return emailAddress;
}

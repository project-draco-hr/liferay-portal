{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(EmailAddress emailAddress) method. To improve performance, call update(EmailAddress emailAddress, boolean merge).");
  }
  return update(emailAddress,false);
}

{
  EmailAddress emailAddress=(EmailAddress)EntityCacheUtil.getResult(EmailAddressModelImpl.ENTITY_CACHE_ENABLED,EmailAddressImpl.class,emailAddressId,this);
  if (emailAddress == _NULL_PLACE_HOLDER) {
    return null;
  }
  if (emailAddress == null) {
    Session session=null;
    boolean hasError=false;
    try {
      session=openSession();
      emailAddress=(EmailAddress)session.get(EmailAddressImpl.class,Long.valueOf(emailAddressId));
    }
 catch (    Exception e) {
      hasError=true;
      throw processException(e);
    }
 finally {
      if ((!hasError) && (emailAddress == null)) {
        EntityCacheUtil.putResult(EmailAddressModelImpl.ENTITY_CACHE_ENABLED,EmailAddressImpl.class,emailAddressId,_NULL_PLACE_HOLDER);
      }
 else {
        cacheResult(emailAddress);
      }
      closeSession(session);
    }
  }
  return emailAddress;
}

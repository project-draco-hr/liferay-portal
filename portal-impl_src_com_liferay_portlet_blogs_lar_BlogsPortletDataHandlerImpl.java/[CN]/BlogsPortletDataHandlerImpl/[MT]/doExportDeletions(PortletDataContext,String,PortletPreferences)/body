{
  Date startDate=portletDataContext.getStartDate();
  portletDataContext.addDeletionEntries(DeletionEntryLocalServiceUtil.getEntries(portletDataContext.getScopeGroupId(),startDate,BlogsEntry.class.getName()));
}

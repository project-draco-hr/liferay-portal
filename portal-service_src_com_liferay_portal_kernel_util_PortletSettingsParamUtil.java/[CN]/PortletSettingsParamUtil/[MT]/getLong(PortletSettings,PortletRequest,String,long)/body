{
  String portletSettingsValue=portletSettings.getValue(param,null);
  long getterUtilValue=GetterUtil.getLong(portletSettingsValue,defaultValue);
  return ParamUtil.get(portletRequest,param,getterUtilValue);
}

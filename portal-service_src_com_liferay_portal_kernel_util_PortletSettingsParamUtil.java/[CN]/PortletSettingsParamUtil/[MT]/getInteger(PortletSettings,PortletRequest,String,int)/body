{
  String settingsValue=portletSettings.getValue(param,null);
  int getterUtilValue=GetterUtil.getInteger(settingsValue,defaultValue);
  return ParamUtil.get(portletRequest,param,getterUtilValue);
}

{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof DDMDataProvider)) {
    return false;
  }
  DDMDataProvider ddmDataProvider=(DDMDataProvider)obj;
  long primaryKey=ddmDataProvider.getPrimaryKey();
  if (getPrimaryKey() == primaryKey) {
    return true;
  }
 else {
    return false;
  }
}

{
  DDMDataProviderCacheModel ddmDataProviderCacheModel=new DDMDataProviderCacheModel();
  ddmDataProviderCacheModel.uuid=getUuid();
  String uuid=ddmDataProviderCacheModel.uuid;
  if ((uuid != null) && (uuid.length() == 0)) {
    ddmDataProviderCacheModel.uuid=null;
  }
  ddmDataProviderCacheModel.dataProviderId=getDataProviderId();
  ddmDataProviderCacheModel.groupId=getGroupId();
  ddmDataProviderCacheModel.companyId=getCompanyId();
  ddmDataProviderCacheModel.userId=getUserId();
  ddmDataProviderCacheModel.userName=getUserName();
  String userName=ddmDataProviderCacheModel.userName;
  if ((userName != null) && (userName.length() == 0)) {
    ddmDataProviderCacheModel.userName=null;
  }
  Date createDate=getCreateDate();
  if (createDate != null) {
    ddmDataProviderCacheModel.createDate=createDate.getTime();
  }
 else {
    ddmDataProviderCacheModel.createDate=Long.MIN_VALUE;
  }
  Date modifiedDate=getModifiedDate();
  if (modifiedDate != null) {
    ddmDataProviderCacheModel.modifiedDate=modifiedDate.getTime();
  }
 else {
    ddmDataProviderCacheModel.modifiedDate=Long.MIN_VALUE;
  }
  ddmDataProviderCacheModel.name=getName();
  String name=ddmDataProviderCacheModel.name;
  if ((name != null) && (name.length() == 0)) {
    ddmDataProviderCacheModel.name=null;
  }
  ddmDataProviderCacheModel.description=getDescription();
  String description=ddmDataProviderCacheModel.description;
  if ((description != null) && (description.length() == 0)) {
    ddmDataProviderCacheModel.description=null;
  }
  ddmDataProviderCacheModel.definition=getDefinition();
  String definition=ddmDataProviderCacheModel.definition;
  if ((definition != null) && (definition.length() == 0)) {
    ddmDataProviderCacheModel.definition=null;
  }
  return ddmDataProviderCacheModel;
}

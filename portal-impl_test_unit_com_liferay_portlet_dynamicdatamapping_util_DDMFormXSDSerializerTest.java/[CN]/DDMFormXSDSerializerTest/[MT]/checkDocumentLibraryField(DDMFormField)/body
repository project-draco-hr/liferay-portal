{
  Assert.assertNotNull(documentLibraryField);
  Assert.assertEquals("document-library",documentLibraryField.getDataType());
  Assert.assertEquals("ddm-documentlibrary",documentLibraryField.getType());
}

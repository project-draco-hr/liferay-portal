{
  Map<String,String[]> parameterMap=liferayPortletURL.getParameterMap();
  String[] actionNameParameterValues=parameterMap.get(ActionRequest.ACTION_NAME);
  String actionNameParameterValue=StringUtil.merge(actionNameParameterValues);
  String[] mvcActionCommandNames=StringUtil.split(actionNameParameterValue);
  if (mvcActionCommandNames.length > 0) {
    String rootPortletId=PortletConstants.getRootPortletId(portletId);
    for (    String mvcActionCommandName : mvcActionCommandNames) {
      if (!_portletInvocationActionWhitelistActions.contains(getWhitelistValue(rootPortletId,mvcActionCommandName))) {
        return false;
      }
    }
    return true;
  }
  return false;
}

{
  String rootPortletId=PortletConstants.getRootPortletId(portletId);
  for (  String actionName : actionNames) {
    if (!whitelistActions.contains(getWhitelistValue(rootPortletId,actionName))) {
      return false;
    }
  }
  return true;
}

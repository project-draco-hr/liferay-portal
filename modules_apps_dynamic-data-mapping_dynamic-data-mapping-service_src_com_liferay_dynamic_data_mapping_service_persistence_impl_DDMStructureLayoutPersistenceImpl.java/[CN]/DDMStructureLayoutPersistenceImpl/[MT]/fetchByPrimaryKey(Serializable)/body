{
  DDMStructureLayout ddmStructureLayout=(DDMStructureLayout)EntityCacheUtil.getResult(DDMStructureLayoutModelImpl.ENTITY_CACHE_ENABLED,DDMStructureLayoutImpl.class,primaryKey);
  if (ddmStructureLayout == _nullDDMStructureLayout) {
    return null;
  }
  if (ddmStructureLayout == null) {
    Session session=null;
    try {
      session=openSession();
      ddmStructureLayout=(DDMStructureLayout)session.get(DDMStructureLayoutImpl.class,primaryKey);
      if (ddmStructureLayout != null) {
        cacheResult(ddmStructureLayout);
      }
 else {
        EntityCacheUtil.putResult(DDMStructureLayoutModelImpl.ENTITY_CACHE_ENABLED,DDMStructureLayoutImpl.class,primaryKey,_nullDDMStructureLayout);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(DDMStructureLayoutModelImpl.ENTITY_CACHE_ENABLED,DDMStructureLayoutImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return ddmStructureLayout;
}

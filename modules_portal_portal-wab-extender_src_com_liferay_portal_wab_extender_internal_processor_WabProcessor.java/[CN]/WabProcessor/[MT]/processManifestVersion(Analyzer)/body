{
  String manifestVersion=MapUtil.getString(_parameters,Constants.BUNDLE_MANIFESTVERSION);
  if (Validator.isNull(manifestVersion)) {
    manifestVersion="2";
  }
  analyzer.setProperty(Constants.BUNDLE_MANIFESTVERSION,manifestVersion);
}

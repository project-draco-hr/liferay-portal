{
  for (  String value : PropsValues.MODULE_FRAMEWORK_WEB_EXTENDER_DEFAULT_SERVLET_PACKAGES) {
    int pos=value.indexOf(StringPool.SEMICOLON);
    if (pos != -1) {
      value=value.substring(0,pos);
    }
    _importPackageNames.add(value.trim());
  }
}

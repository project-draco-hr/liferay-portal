{
  Set<String> packages=new HashSet<String>();
  for (  String interfaceName : interfaces) {
    packages.addAll(processReferencedDependencies(interfaceName.replace('.','/') + ".class",source));
  }
  return packages;
}

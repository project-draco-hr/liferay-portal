{
  Set<String> packageNames=new HashSet<String>();
  for (  String interfaceName : interfaceNames) {
    packageNames.addAll(processReferencedDependencies(source,interfaceName.replace(CharPool.PERIOD,CharPool.SLASH) + ".class"));
  }
  return packageNames;
}

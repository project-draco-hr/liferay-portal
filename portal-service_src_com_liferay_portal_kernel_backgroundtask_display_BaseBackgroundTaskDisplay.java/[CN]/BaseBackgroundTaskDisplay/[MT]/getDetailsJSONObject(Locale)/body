{
  JSONObject jsonObject=null;
  try {
    jsonObject=JSONFactoryUtil.createJSONObject(_backgroundTask.getStatusMessage());
  }
 catch (  JSONException e) {
    if (_log.isDebugEnabled()) {
      _log.debug(e,e);
    }
  }
  return jsonObject;
}

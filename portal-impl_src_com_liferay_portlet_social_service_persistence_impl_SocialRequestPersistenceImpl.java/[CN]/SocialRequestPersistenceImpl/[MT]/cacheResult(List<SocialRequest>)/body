{
  for (  SocialRequest socialRequest : socialRequests) {
    if (entityCache.getResult(SocialRequestModelImpl.ENTITY_CACHE_ENABLED,SocialRequestImpl.class,socialRequest.getPrimaryKey()) == null) {
      cacheResult(socialRequest);
    }
 else {
      socialRequest.resetOriginalValues();
    }
  }
}

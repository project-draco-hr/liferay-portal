{
  JournalStructure structure=journalStructureLocalService.getStructure(groupId,structureId);
  List<DDMTemplate> ddmTemplates=ddmTemplatePersistence.filterFindByG_CPK(groupId,structure.getPrimaryKey());
  return JournalUtil.toJournalTemplates(ddmTemplates);
}

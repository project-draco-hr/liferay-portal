{
  JournalTemplatePermission.check(getPermissionChecker(),groupId,oldTemplateId,ActionKeys.ADD_TEMPLATE);
  return journalTemplateLocalService.copyTemplate(getUserId(),groupId,oldTemplateId,newTemplateId,autoTemplateId);
}

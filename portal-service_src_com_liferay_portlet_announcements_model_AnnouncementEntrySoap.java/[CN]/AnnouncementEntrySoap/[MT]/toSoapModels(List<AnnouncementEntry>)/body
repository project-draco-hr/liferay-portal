{
  List<AnnouncementEntrySoap> soapModels=new ArrayList<AnnouncementEntrySoap>(models.size());
  for (int i=0; i < models.size(); i++) {
    AnnouncementEntry model=models.get(i);
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new AnnouncementEntrySoap[soapModels.size()]);
}

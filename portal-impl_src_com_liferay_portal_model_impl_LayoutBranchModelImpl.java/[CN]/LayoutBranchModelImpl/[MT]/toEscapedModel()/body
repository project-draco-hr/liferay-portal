{
  if (isEscapedModel()) {
    return (LayoutBranch)this;
  }
 else {
    return (LayoutBranch)Proxy.newProxyInstance(LayoutBranch.class.getClassLoader(),new Class[]{LayoutBranch.class},new AutoEscapeBeanHandler(this));
  }
}

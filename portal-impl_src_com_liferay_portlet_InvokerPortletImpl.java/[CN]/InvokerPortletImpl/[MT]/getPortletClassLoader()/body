{
  ClassLoader classLoader=(ClassLoader)_portletContextImpl.getAttribute(PortletServlet.PORTLET_CLASS_LOADER);
  if (classLoader == null) {
    classLoader=PortalClassLoaderUtil.getClassLoader();
  }
  return classLoader;
}

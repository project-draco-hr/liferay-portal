{
  ClassLoader classLoader=(ClassLoader)_portletContextImpl.getAttribute(PluginContextListener.PLUGIN_CLASS_LOADER);
  if (classLoader == null) {
    classLoader=PortalClassLoaderUtil.getClassLoader();
  }
  return classLoader;
}

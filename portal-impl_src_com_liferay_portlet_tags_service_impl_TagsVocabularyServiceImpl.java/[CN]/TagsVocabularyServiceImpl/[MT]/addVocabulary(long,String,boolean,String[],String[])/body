{
  PortletPermissionUtil.check(getPermissionChecker(),plid,PortletKeys.TAGS_ADMIN,ActionKeys.ADD_VOCABULARY);
  return tagsVocabularyLocalService.addVocabulary(getUserId(),plid,name,folksonomy,communityPermissions,guestPermissions);
}

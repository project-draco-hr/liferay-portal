{
  PortletPermissionUtil.check(getPermissionChecker(),serviceContext.getPlid(),PortletKeys.TAGS_ADMIN,ActionKeys.ADD_VOCABULARY);
  return tagsVocabularyLocalService.addVocabulary(getUserId(),name,folksonomy,serviceContext);
}

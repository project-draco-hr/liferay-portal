{
  TagsPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_VOCABULARY);
  return tagsVocabularyLocalService.addVocabulary(getUserId(),name,folksonomy,serviceContext);
}

{
  long templateId=ParamUtil.getLong(renderRequest,"templateId");
  if (templateId > 0) {
    DDMTemplate template=DDMTemplateLocalServiceUtil.getDDMTemplate(templateId);
    renderRequest.setAttribute(WebKeys.DYNAMIC_DATA_MAPPING_TEMPLATE,template);
  }
}

{
  try {
    return MBCategoryPermission.contains(themeDisplay.getPermissionChecker(),themeDisplay.getScopeGroupId(),_thread.getCategoryId(),ActionKeys.LOCK_THREAD);
  }
 catch (  Exception e) {
  }
  return false;
}

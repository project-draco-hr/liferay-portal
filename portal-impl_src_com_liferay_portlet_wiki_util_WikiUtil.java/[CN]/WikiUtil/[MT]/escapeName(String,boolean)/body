{
  if (escape) {
    return StringUtil.replace(name,WikiPageConstants.UNESCAPED_CHARS,WikiPageConstants.ESCAPED_CHARS);
  }
 else {
    return StringUtil.replace(name,WikiPageConstants.ESCAPED_CHARS,WikiPageConstants.UNESCAPED_CHARS);
  }
}

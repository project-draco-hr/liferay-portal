{
  Settings settings=SettingsFactoryUtil.getGroupServiceSettings(groupId,WikiConstants.SERVICE_NAME);
  ParameterMapSettings configurationSettings=new ParameterMapSettings(settings,request.getParameterMap());
  return new WikiSettings(configurationSettings);
}

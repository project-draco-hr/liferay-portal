{
  try {
    if (_wikiPortletInstanceSettings == null) {
      if (getPortletId().equals(PortletKeys.PORTLET_CONFIGURATION)) {
        _wikiPortletInstanceSettings=WikiPortletInstanceSettings.getInstance(getLayout(),getResourcePortletId(),getRequest().getParameterMap());
      }
 else {
        _wikiPortletInstanceSettings=WikiPortletInstanceSettings.getInstance(getLayout(),getPortletId());
      }
    }
    return _wikiPortletInstanceSettings;
  }
 catch (  PortalException pe) {
    throw new SystemException(pe);
  }
}

{
  if (isEscapedModel()) {
    return (LayoutRevision)this;
  }
 else {
    return (LayoutRevision)Proxy.newProxyInstance(LayoutRevision.class.getClassLoader(),new Class[]{LayoutRevision.class},new AutoEscapeBeanHandler(this));
  }
}

{
  if (isEscapedModel()) {
    return (LayoutRevision)this;
  }
 else {
    return (LayoutRevision)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

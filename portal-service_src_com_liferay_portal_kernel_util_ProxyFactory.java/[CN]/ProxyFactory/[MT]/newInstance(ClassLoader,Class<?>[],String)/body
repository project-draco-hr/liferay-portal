{
  Class<?> classObj=classLoader.loadClass(implClassName);
  Object instance=classObj.newInstance();
  return Proxy.newProxyInstance(classLoader,interfaceClasses,new ClassLoaderBeanHandler(instance,classLoader));
}

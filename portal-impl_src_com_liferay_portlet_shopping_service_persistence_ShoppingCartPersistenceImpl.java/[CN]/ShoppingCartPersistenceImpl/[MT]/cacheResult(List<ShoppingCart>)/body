{
  for (  ShoppingCart shoppingCart : shoppingCarts) {
    if (EntityCacheUtil.getResult(ShoppingCartModelImpl.ENTITY_CACHE_ENABLED,ShoppingCartImpl.class,shoppingCart.getPrimaryKey()) == null) {
      cacheResult(shoppingCart);
    }
 else {
      shoppingCart.resetOriginalValues();
    }
  }
}

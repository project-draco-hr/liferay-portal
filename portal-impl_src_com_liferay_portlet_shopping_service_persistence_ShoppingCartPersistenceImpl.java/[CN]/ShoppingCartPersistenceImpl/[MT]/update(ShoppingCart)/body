{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ShoppingCart shoppingCart) method. To improve performance, call update(ShoppingCart shoppingCart, boolean merge).");
  }
  return update(shoppingCart,false);
}

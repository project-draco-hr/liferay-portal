{
  try {
    DLServiceUtil.deleteFile(companyId,portletId,repositoryId,fileName,versionNumber);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}

{
  PropsUtil propsUtil=new PropsUtil();
  MockProps mockProps=new MockProps();
  mockProps.setProperty(PropsKeys.TRANSACTIONAL_CACHE_ENABLED,Boolean.toString(enabled));
  propsUtil.setProps(mockProps);
}

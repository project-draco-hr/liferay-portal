{
  ExpandoTable table=ExpandoTableLocalServiceUtil.addTable(ExpandoLocalServiceTest.class.getName(),_TABLENAME);
  assertNotNull(table);
  assertEquals(table.getClassName(),ExpandoLocalServiceTest.class.getName());
  assertEquals(table.getName(),_TABLENAME);
}

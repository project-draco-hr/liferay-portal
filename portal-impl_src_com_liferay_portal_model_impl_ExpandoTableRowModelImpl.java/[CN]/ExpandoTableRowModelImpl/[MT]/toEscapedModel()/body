{
  if (isEscapedModel()) {
    return (ExpandoTableRow)this;
  }
 else {
    ExpandoTableRow model=new ExpandoTableRowImpl();
    model.setEscapedModel(true);
    model.setRowId(getRowId());
    model.setTableId(getTableId());
    model=(ExpandoTableRow)Proxy.newProxyInstance(ExpandoTableRow.class.getClassLoader(),new Class[]{ExpandoTableRow.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}

{
  NumericRangeQuery numericRangeQuery=NumericRangeQuery.newIntRange(field,Integer.valueOf(startValue),Integer.valueOf(endValue),true,true);
  booleanQuery.add(numericRangeQuery,BooleanClause.Occur.SHOULD);
}

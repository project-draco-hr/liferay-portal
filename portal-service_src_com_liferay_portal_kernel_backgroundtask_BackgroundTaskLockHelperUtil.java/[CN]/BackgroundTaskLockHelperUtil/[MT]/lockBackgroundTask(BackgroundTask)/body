{
  String owner=backgroundTask.getName() + StringPool.POUND + backgroundTask.getBackgroundTaskId();
  return LockLocalServiceUtil.lock(BackgroundTaskExecutor.class.getName(),getLockKey(backgroundTask),owner);
}

{
  String owner=backgroundTask.getName() + StringPool.POUND + backgroundTask.getBackgroundTaskId();
  return LockHelperUtil.lock(BackgroundTaskExecutor.class.getName(),getLockKey(backgroundTask),owner);
}

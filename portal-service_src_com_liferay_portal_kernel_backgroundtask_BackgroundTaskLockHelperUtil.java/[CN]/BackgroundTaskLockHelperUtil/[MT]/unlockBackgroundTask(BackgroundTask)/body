{
  String owner=backgroundTask.getName() + StringPool.POUND + backgroundTask.getBackgroundTaskId();
  LockHelperUtil.unlock(BackgroundTaskExecutor.class.getName(),getLockKey(backgroundTask),owner);
}

{
  String owner=backgroundTask.getName() + StringPool.POUND + backgroundTask.getBackgroundTaskId();
  LockLocalServiceUtil.unlock(BackgroundTaskExecutor.class.getName(),getLockKey(backgroundTask),owner);
}

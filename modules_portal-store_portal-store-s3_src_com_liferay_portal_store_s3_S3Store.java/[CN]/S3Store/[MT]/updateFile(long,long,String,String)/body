{
  String oldPrefix=_s3KeyTransformer.getFileKey(companyId,repositoryId,fileName);
  String newPrefix=_s3KeyTransformer.getFileKey(companyId,repositoryId,newFileName);
  moveObjects(oldPrefix,newPrefix);
}

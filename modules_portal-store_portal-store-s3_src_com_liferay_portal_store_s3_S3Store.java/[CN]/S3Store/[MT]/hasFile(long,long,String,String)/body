{
  try {
    S3Object[] s3Objects=_s3Service.listObjects(_s3Bucket.getName(),getKey(companyId,repositoryId,fileName,versionLabel),null);
    if (s3Objects.length == 0) {
      return false;
    }
 else {
      return true;
    }
  }
 catch (  S3ServiceException s3se) {
    if (isS3NoSuchKeyException(s3se)) {
      return false;
    }
    throw new SystemException(s3se);
  }
}

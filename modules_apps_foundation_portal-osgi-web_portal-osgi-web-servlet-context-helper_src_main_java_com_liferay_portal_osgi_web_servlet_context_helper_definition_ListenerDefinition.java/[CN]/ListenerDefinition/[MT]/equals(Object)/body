{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ListenerDefinition)) {
    return false;
  }
  ListenerDefinition listenerDefinition=(ListenerDefinition)obj;
  if (Validator.equals(_eventListener.getClass(),listenerDefinition._eventListener.getClass())) {
    return true;
  }
  return false;
}

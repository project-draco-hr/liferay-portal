{
  String page=ParamUtil.getString(req,"page");
  String forward="portlet.mail.edit" + (Validator.isNotNull(page) ? StringPool.UNDERLINE + page : StringPool.BLANK);
  return mapping.findForward(getForward(req,forward));
}

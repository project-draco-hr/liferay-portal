{
  ApplicationContext applicationContext=new ArrayApplicationContext(PropsUtil.getArray(PropsKeys.SPRING_PARENT_CONFIGS));
  PortalApplicationContextUtil.setParentContext(applicationContext);
  return applicationContext;
}

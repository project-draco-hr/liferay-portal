{
  HttpServletRequest httpReq=PortalUtil.getHttpServletRequest(req);
  getArticle(httpReq);
  JournalArticle article=(JournalArticle)req.getAttribute(WebKeys.JOURNAL_ARTICLE);
  JournalUtil.addRecentArticle(req,article);
}

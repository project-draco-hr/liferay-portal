{
  HttpServletRequest httpReq=PortalUtil.getHttpServletRequest(req);
  getTemplate(httpReq);
  JournalTemplate template=(JournalTemplate)req.getAttribute(WebKeys.JOURNAL_TEMPLATE);
  JournalUtil.addRecentTemplate(req,template);
}

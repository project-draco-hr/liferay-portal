{
  HttpServletRequest httpReq=((ActionRequestImpl)req).getHttpServletRequest();
  getStructure(httpReq);
  JournalStructure structure=(JournalStructure)req.getAttribute(WebKeys.JOURNAL_STRUCTURE);
  JournalUtil.addRecentStructure(req,structure);
}

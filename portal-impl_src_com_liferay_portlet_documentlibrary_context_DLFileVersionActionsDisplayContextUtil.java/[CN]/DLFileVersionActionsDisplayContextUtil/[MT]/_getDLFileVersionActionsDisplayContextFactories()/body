{
  try {
    Registry registry=RegistryUtil.getRegistry();
    return registry.getServices(DLFileVersionActionsDisplayContextFactory.class,null);
  }
 catch (  Exception e) {
    throw new SystemException(e);
  }
}

{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Address address) method. To improve performance, call update(Address address, boolean merge).");
  }
  return update(address,false);
}

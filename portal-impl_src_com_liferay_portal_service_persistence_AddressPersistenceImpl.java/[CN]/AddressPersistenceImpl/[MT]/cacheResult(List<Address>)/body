{
  for (  Address address : addresses) {
    if (EntityCacheUtil.getResult(AddressModelImpl.ENTITY_CACHE_ENABLED,Address.class,address.getPrimaryKey(),this) == null) {
      cacheResult(address);
    }
  }
}

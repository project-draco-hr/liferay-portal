{
  elasticsearchConfiguration=Configurable.createConfigurable(ElasticsearchConfiguration.class,properties);
  String[] transportAddresses=StringUtil.split(elasticsearchConfiguration.transportAddresses());
  setTransportAddresses(new HashSet<>(Arrays.asList(transportAddresses)));
}

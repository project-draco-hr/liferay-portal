{
  if (!(o instanceof NonceDelayed)) {
    throw new IllegalArgumentException("Wrong Type : " + o.getClass());
  }
  long result=_createdMilliseconds - ((NonceDelayed)o)._createdMilliseconds;
  if (result == 0) {
    return 0;
  }
 else   if (result > 0) {
    return 1;
  }
 else {
    return -1;
  }
}

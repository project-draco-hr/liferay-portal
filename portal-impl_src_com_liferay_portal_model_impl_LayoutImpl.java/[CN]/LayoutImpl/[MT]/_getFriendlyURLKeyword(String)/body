{
  friendlyURL=friendlyURL.toLowerCase();
  for (  String keyword : _friendlyURLKeywords) {
    if (friendlyURL.startsWith(keyword) || keyword.equals(friendlyURL + StringPool.SLASH)) {
      return keyword;
    }
  }
  return null;
}

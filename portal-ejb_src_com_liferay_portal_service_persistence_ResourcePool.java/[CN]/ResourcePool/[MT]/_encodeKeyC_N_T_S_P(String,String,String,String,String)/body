{
  String key=GROUP_NAME + StringPool.POUND + companyId.toString()+ name.toString()+ typeId.toString()+ scope.toString()+ primKey.toString();
  if (_log.isDebugEnabled()) {
    _log.debug("Key " + key);
  }
  return key;
}

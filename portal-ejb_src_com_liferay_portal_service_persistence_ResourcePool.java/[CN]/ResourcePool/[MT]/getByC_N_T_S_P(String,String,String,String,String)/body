{
  String pk=_instance._getByC_N_T_S_P(companyId,name,typeId,scope,primKey);
  if (_log.isInfoEnabled()) {
    _log.info("Get C_N_T_S_P" + " " + companyId.toString() + " "+ name.toString()+ " "+ typeId.toString()+ " "+ scope.toString()+ " "+ primKey.toString()+ " is "+ ((pk == null) ? "NOT " : "")+ "in cache");
  }
  return pk;
}

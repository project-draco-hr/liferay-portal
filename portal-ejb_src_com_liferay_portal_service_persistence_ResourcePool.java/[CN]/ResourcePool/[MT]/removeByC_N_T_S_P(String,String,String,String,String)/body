{
  if (_log.isInfoEnabled()) {
    _log.info("Remove C_N_T_S_P" + " " + companyId.toString() + " "+ name.toString()+ " "+ typeId.toString()+ " "+ scope.toString()+ " "+ primKey.toString());
  }
  return _instance._removeByC_N_T_S_P(companyId,name,typeId,scope,primKey);
}

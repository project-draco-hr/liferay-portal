{
  Random random=new Random();
  for (int i=0; i < _taskIterationCount; i++) {
    MessageListener listener=_listeners[random.nextInt(_listeners.length)];
    if (_register) {
      _destination.register(listener);
    }
 else {
      _destination.unregister(listener);
    }
  }
  return null;
}

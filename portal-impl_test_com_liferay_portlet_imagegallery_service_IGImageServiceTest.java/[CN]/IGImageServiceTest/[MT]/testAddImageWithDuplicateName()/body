{
  String name="liferay.png";
  String description=StringPool.BLANK;
  File image=new File(getClassResource("com/liferay/portlet/imagegallery/dependencies/" + name).getPath());
  String contentType="png";
  String[] tagsEntries=null;
  boolean addCommunityPermissions=true;
  boolean addGuestPermissions=true;
  IGImageServiceUtil.addImage(_folder.getFolderId(),name,description,image,contentType,tagsEntries,addCommunityPermissions,addGuestPermissions);
  try {
    IGImageServiceUtil.addImage(_folder.getFolderId(),name,description,image,contentType,tagsEntries,addCommunityPermissions,addGuestPermissions);
    fail("Able to add two images of the name " + name);
  }
 catch (  DuplicateImageNameException dine) {
  }
}

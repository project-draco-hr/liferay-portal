{
  DLFileShortcut fileShortcut=getFileShortcut(fileShortcutId);
  DLFileShortcutPermission.check(getPermissionChecker(),fileShortcut,ActionKeys.UPDATE);
  dlAppHelperLocalService.restoreFileShortcutFromTrash(getUserId(),fileShortcut);
}

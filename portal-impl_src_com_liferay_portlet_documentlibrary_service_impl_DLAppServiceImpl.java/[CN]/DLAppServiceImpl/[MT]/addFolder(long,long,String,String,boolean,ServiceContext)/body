{
  if (defaultPermissions) {
    addDefaultPermissions(repositoryId,DLFolderConstants.getClassName(),serviceContext);
  }
  return addFolder(repositoryId,parentFolderId,name,description,serviceContext);
}

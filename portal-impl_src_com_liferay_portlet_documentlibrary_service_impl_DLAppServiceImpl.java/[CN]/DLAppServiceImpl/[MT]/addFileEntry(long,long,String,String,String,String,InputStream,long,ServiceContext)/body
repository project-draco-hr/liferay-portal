{
  if (is == null) {
    is=new UnsyncByteArrayInputStream(new byte[0]);
    size=0;
  }
  Repository repository=getRepository(repositoryId);
  return repository.addFileEntry(folderId,mimeType,title,description,changeLog,is,size,serviceContext);
}

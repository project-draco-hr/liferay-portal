{
  try {
    _persistence.countByC_FID(ServiceTestUtil.nextLong(),ServiceTestUtil.nextLong());
    _persistence.countByC_FID(0L,0L);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

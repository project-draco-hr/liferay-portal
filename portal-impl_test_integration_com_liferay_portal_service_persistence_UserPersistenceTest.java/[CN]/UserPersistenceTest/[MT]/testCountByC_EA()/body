{
  try {
    _persistence.countByC_EA(ServiceTestUtil.nextLong(),StringPool.BLANK);
    _persistence.countByC_EA(0L,StringPool.NULL);
    _persistence.countByC_EA(0L,(String)null);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

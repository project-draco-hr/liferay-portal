{
  try {
    _persistence.countByC_CD(ServiceTestUtil.nextLong(),ServiceTestUtil.nextDate());
    _persistence.countByC_CD(0L,ServiceTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  try {
    _persistence.countByC_MD(RandomTestUtil.nextLong(),RandomTestUtil.nextDate());
    _persistence.countByC_MD(0L,RandomTestUtil.nextDate());
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

{
  try {
    _persistence.countByC_SN(ServiceTestUtil.nextLong(),StringPool.BLANK);
    _persistence.countByC_SN(0L,StringPool.NULL);
    _persistence.countByC_SN(0L,(String)null);
  }
 catch (  Exception e) {
    Assert.fail(e.getMessage());
  }
}

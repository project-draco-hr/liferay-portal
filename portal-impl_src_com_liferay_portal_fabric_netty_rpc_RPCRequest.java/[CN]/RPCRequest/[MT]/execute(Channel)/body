{
  ChannelThreadLocal.setChannel(channel);
  try {
    sendRPCResponse(channel,new RPCResponse<T>(id,_processCallable.call(),null));
  }
 catch (  Throwable t) {
    sendRPCResponse(channel,new RPCResponse<T>(id,null,t));
  }
 finally {
    ChannelThreadLocal.removeChannel();
  }
}

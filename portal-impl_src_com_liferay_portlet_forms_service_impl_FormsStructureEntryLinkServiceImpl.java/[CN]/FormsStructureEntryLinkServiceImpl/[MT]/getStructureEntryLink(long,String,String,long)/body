{
  FormsStructureEntryPermission.check(getPermissionChecker(),groupId,structureId,ActionKeys.VIEW);
  return formsStructureEntryLinkLocalService.getStructureEntryLink(structureId,className,classPK);
}

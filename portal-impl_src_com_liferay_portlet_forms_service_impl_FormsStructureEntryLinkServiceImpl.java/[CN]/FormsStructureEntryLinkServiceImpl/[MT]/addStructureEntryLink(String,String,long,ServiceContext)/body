{
  FormsPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_STRUCTURE_ENTRY);
  return formsStructureEntryLinkLocalService.addStructureEntryLink(structureId,className,classPK,serviceContext);
}

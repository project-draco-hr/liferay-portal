{
  if (manager instanceof SchedulerEngine) {
    setSchedulerEngine((SchedulerEngine)manager);
  }
 else {
    if (_log.isErrorEnabled()) {
      _log.error("Manager type " + manager.getClass().getName() + " must implement "+ SchedulerEngine.class.getName());
    }
  }
}

{
  int count=countByG_U_C_C_T(groupId,userId,classNameId,classPK,type);
  List<SocialActivitySet> list=findByG_U_C_C_T(groupId,userId,classNameId,classPK,type,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

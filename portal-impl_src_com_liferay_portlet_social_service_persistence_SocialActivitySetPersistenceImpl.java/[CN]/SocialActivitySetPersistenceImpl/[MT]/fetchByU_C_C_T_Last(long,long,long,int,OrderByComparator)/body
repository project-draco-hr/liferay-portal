{
  int count=countByU_C_C_T(userId,classNameId,classPK,type);
  if (count == 0) {
    return null;
  }
  List<SocialActivitySet> list=findByU_C_C_T(userId,classNameId,classPK,type,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}

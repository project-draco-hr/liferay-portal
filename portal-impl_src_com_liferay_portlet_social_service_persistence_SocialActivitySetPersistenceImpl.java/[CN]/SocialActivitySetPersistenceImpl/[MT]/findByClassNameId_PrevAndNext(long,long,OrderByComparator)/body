{
  SocialActivitySet socialActivitySet=findByPrimaryKey(activitySetId);
  Session session=null;
  try {
    session=openSession();
    SocialActivitySet[] array=new SocialActivitySetImpl[3];
    array[0]=getByClassNameId_PrevAndNext(session,socialActivitySet,classNameId,orderByComparator,true);
    array[1]=socialActivitySet;
    array[2]=getByClassNameId_PrevAndNext(session,socialActivitySet,classNameId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

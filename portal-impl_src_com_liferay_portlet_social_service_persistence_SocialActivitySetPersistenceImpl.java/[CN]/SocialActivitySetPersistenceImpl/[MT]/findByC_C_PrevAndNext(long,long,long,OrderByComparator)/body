{
  SocialActivitySet socialActivitySet=findByPrimaryKey(activitySetId);
  Session session=null;
  try {
    session=openSession();
    SocialActivitySet[] array=new SocialActivitySetImpl[3];
    array[0]=getByC_C_PrevAndNext(session,socialActivitySet,classNameId,classPK,orderByComparator,true);
    array[1]=socialActivitySet;
    array[2]=getByC_C_PrevAndNext(session,socialActivitySet,classNameId,classPK,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}

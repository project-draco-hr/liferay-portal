{
  DDMForm ddmForm=createDDMForm(createAvailableLocales(LocaleUtil.BRAZIL,LocaleUtil.US),LocaleUtil.US);
  DDMFormField field=createTextDDMFormField("field");
  LocalizedValue tip=field.getTip();
  tip.addString(LocaleUtil.BRAZIL,"Dica para campo de texto.");
  tip.addString(LocaleUtil.US,"Tip for text field");
  List<DDMFormField> ddmFormFields=ddmForm.getDDMFormFields();
  ddmFormFields.add(field);
  DDMStructure structure=createStructure("Test Structure",ddmForm);
  Assert.assertEquals("Dica para campo de texto.",structure.getFieldTip("field",LocaleUtil.BRAZIL));
  Assert.assertEquals("Tip for text field",structure.getFieldTip("field",LocaleUtil.US));
}

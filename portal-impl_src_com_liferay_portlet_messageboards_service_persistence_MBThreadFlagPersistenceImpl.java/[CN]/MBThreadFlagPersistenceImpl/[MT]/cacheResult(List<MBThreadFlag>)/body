{
  for (  MBThreadFlag mbThreadFlag : mbThreadFlags) {
    if (EntityCacheUtil.getResult(MBThreadFlagModelImpl.ENTITY_CACHE_ENABLED,MBThreadFlagImpl.class,mbThreadFlag.getPrimaryKey(),this) == null) {
      cacheResult(mbThreadFlag);
    }
  }
}

{
  List<JournalFeed> feeds=JournalFeedLocalServiceUtil.getJournalFeedsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<JournalFeed>());
  if (ListUtil.isEmpty(feeds)) {
    return null;
  }
  return feeds.get(0);
}

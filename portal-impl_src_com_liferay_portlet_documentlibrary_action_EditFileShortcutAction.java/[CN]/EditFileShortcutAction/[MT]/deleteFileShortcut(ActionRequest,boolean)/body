{
  long fileShortcutId=ParamUtil.getLong(actionRequest,"fileShortcutId");
  if (moveToTrash) {
    FileShortcut fileShortcut=DLAppServiceUtil.moveFileShortcutToTrash(fileShortcutId);
    if (fileShortcut.getModel() instanceof DLFileShortcut) {
      TrashUtil.addTrashSessionMessages(actionRequest,(DLFileShortcut)fileShortcut.getModel());
    }
    hideDefaultSuccessMessage(actionRequest);
  }
 else {
    DLAppServiceUtil.deleteFileShortcut(fileShortcutId);
  }
}

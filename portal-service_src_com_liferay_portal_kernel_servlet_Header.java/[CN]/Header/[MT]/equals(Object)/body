{
  if (obj == null) {
    return false;
  }
  Header header=null;
  try {
    header=(Header)obj;
  }
 catch (  ClassCastException cce) {
    return false;
  }
  if (header.getType() != getType()) {
    return false;
  }
  return toString().equals(header.toString());
}

{
  ThemeDisplay themeDisplay=(ThemeDisplay)portletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  Company company=CompanyServiceUtil.updateLogo(themeDisplay.getCompanyId(),bytes);
  themeDisplay.setCompany(company);
}

{
  Boolean value=_organizationAdmins.get(organization.getOrganizationId());
  if (value == null) {
    value=Boolean.valueOf(isOrganizationAdminImpl(permissionChecker,organization));
    _organizationAdmins.put(organization.getOrganizationId(),value);
  }
  return value.booleanValue();
}

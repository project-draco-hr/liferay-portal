{
  Boolean value=_organizationOwners.get(organization.getOrganizationId());
  if (value == null) {
    value=Boolean.valueOf(isOrganizationOwnerImpl(permissionChecker,organization));
    _organizationOwners.put(organization.getOrganizationId(),value);
  }
  return value.booleanValue();
}

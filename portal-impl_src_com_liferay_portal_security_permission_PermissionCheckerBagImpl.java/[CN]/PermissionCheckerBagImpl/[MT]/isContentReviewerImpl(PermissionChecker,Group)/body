{
  if (permissionChecker.isCompanyAdmin() || permissionChecker.isGroupAdmin(group.getGroupId())) {
    return true;
  }
  long userId=getUserId();
  if (RoleLocalServiceUtil.hasUserRole(userId,group.getCompanyId(),RoleConstants.PORTAL_CONTENT_REVIEWER,true)) {
    return true;
  }
  if (group.isSite()) {
    if (UserGroupRoleLocalServiceUtil.hasUserGroupRole(userId,group.getGroupId(),RoleConstants.SITE_CONTENT_REVIEWER,true)) {
      return true;
    }
  }
  return false;
}

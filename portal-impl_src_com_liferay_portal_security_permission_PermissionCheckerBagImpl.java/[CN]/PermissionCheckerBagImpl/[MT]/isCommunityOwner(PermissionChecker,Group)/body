{
  Boolean value=_communityOwners.get(group.getGroupId());
  if (value == null) {
    value=Boolean.valueOf(isCommunityOwnerImpl(permissionChecker,group));
    _communityOwners.put(group.getGroupId(),value);
  }
  return value.booleanValue();
}

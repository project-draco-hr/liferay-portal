{
  Boolean value=_contentReviewers.get(group.getCompanyId());
  if (value == null) {
    value=Boolean.valueOf(isContentReviewerImpl(permissionChecker,group));
    _contentReviewers.put(group.getCompanyId(),value);
  }
  return value.booleanValue();
}

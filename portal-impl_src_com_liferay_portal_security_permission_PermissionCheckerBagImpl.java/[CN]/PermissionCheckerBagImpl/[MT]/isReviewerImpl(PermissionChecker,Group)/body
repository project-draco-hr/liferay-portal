{
  if (permissionChecker.isCompanyAdmin() || permissionChecker.isGroupAdmin(group.getGroupId()) || permissionChecker.isOmniadmin()) {
    return true;
  }
  if (group.isSite() && hasUserGroupRole(group)) {
    return true;
  }
  if (hasUserRole(group)) {
    return true;
  }
  return false;
}

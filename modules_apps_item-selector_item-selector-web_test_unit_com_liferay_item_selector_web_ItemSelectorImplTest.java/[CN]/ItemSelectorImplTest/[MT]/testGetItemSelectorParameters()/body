{
  Map<String,String[]> parameters=_itemSelectorImpl.getItemSelectorParameters("itemSelectedCallback",_mediaItemSelectorCriterion,_flickrItemSelectorCriterion);
  Assert.assertEquals("itemSelectedCallback",parameters.get(ItemSelectorImpl.PARAMETER_ITEM_SELECTED_CALLBACK)[0]);
  Assert.assertEquals(MediaItemSelectorCriterion.class.getName() + "," + FlickrItemSelectorCriterion.class.getName(),parameters.get(ItemSelectorImpl.PARAMETER_CRITERIA)[0]);
  Assert.assertNull(parameters.get("0_desiredReturnTypes"));
  Assert.assertNotNull(parameters.get("0_json")[0]);
  Assert.assertEquals(URL.class.getName(),parameters.get("1_desiredReturnTypes")[0]);
  Assert.assertNotNull(parameters.get("1_json")[0]);
  Assert.assertEquals(5,parameters.size());
}

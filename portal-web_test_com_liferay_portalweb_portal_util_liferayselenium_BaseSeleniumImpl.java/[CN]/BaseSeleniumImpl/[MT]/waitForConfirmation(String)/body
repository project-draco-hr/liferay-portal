{
  for (int second=0; ; second++) {
    if (second >= 30) {
      BaseTestCase.fail("timeout");
    }
    try {
      if (pattern.equals(getConfirmation())) {
        break;
      }
    }
 catch (    Exception e) {
    }
    Thread.sleep(1000);
  }
}

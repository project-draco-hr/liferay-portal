{
switch (JobState.VERSION) {
case 1:
    return _doSerialize1(jobState);
default :
  throw new IllegalStateException("Unable to find serialize method for JobState with " + "current version " + JobState.VERSION);
}
}

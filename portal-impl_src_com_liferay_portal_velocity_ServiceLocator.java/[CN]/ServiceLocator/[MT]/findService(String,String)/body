{
  if (serviceName.endsWith(_SERVICE)) {
    serviceName+=_VELOCITY;
  }
  Object obj=null;
  try {
    obj=PortletBeanLocatorUtil.locate(servletContextName,serviceName);
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return obj;
}

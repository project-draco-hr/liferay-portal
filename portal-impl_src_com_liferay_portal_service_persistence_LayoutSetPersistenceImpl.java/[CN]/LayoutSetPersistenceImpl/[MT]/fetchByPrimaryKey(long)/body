{
  LayoutSet result=(LayoutSet)EntityCacheUtil.getResult(LayoutSetModelImpl.ENTITY_CACHE_ENABLED,LayoutSet.class,layoutSetId,this);
  if (result == null) {
    Session session=null;
    try {
      session=openSession();
      LayoutSet layoutSet=(LayoutSet)session.get(LayoutSetImpl.class,new Long(layoutSetId));
      if (layoutSet != null) {
        cacheResult(layoutSet);
      }
      return layoutSet;
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
 else {
    return (LayoutSet)result;
  }
}

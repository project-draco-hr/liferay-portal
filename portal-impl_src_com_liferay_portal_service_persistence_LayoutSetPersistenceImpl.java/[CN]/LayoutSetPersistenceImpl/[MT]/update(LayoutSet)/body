{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(LayoutSet layoutSet) method. To improve performance, call update(LayoutSet layoutSet, boolean merge).");
  }
  return update(layoutSet,false);
}

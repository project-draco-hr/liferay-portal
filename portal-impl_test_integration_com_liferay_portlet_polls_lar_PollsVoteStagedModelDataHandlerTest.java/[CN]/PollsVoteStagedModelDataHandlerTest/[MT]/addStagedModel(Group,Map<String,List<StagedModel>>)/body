{
  List<StagedModel> dependentStagedModels=dependentStagedModelsMap.get(PollsQuestion.class.getName());
  PollsQuestion question=(PollsQuestion)dependentStagedModels.get(0);
  dependentStagedModels=dependentStagedModelsMap.get(PollsChoice.class.getName());
  PollsChoice choice=(PollsChoice)dependentStagedModels.get(0);
  return PollsTestUtil.addVote(group.getGroupId(),question.getQuestionId(),choice.getChoiceId());
}

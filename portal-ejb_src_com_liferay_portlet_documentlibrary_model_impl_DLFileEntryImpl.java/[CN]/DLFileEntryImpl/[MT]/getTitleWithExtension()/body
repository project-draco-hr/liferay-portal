{
  String titleWithExtension=getTitle();
  if (FileUtil.getExtension(titleWithExtension) == null) {
    titleWithExtension+=StringPool.PERIOD + FileUtil.getExtension(getName());
  }
  return titleWithExtension;
}

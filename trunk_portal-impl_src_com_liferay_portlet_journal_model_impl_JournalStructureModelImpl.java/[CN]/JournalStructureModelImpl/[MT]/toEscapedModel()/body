{
  if (isEscapedModel()) {
    return (JournalStructure)this;
  }
 else {
    return (JournalStructure)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

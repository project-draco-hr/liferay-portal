{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof Content)) {
    return false;
  }
  Content content=(Content)obj;
  if ((_contentId == content._contentId) && (_companyId == content._companyId) && Validator.equals(_portletId,content._portletId)&& (_groupId == content._groupId)&& (_repositoryId == content._repositoryId)&& Validator.equals(_path,content._path)&& Validator.equals(_version,content._version)&& (_size == content._size)) {
    return true;
  }
  return false;
}

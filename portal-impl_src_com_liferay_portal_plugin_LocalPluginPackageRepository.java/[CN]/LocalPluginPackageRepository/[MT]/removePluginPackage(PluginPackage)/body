{
  _pluginPackages.remove(pluginPackage.getContext());
  try {
    PluginPackageIndexer.removePluginPackage(pluginPackage.getModuleId());
  }
 catch (  SearchException se) {
    _log.error("Deleting index " + pluginPackage.getModuleId(),se);
  }
}

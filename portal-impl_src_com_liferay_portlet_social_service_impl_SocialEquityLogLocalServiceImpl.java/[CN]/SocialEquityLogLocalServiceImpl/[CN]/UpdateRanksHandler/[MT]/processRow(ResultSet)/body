{
  long equityUserId=rs.getLong(1);
  long groupId=rs.getLong(2);
  if (groupId == _groupId) {
    _rank++;
  }
 else {
    _groupId=groupId;
    _rank=1;
  }
  _updateRanksSetter.add(equityUserId,_rank);
}

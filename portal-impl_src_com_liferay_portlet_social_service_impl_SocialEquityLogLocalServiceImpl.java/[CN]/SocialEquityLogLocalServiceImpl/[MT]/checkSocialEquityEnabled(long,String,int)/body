{
  if (!socialEquityGroupSettingLocalService.isSocialEquityEnabled(groupId,Group.class.getName(),type)) {
    return false;
  }
  return socialEquityGroupSettingLocalService.isSocialEquityEnabled(groupId,className,type);
}

{
  List<SocialEquityLog> equityLogs=socialEquityLogPersistence.findByUserId(user.getUserId());
  for (  SocialEquityLog equityLog : equityLogs) {
    equityLog.setActive(false);
    socialEquityLogPersistence.remove(equityLog);
  }
}

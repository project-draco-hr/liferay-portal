{
  String sql=CustomSQLUtil.get(_UPDATE_SOCIAL_EQUITY_USER_PEQ);
  sql=StringUtil.replace(sql,new String[]{"[$GROUP_ID$]","[$USER_ID$]"},new String[]{String.valueOf(groupId),String.valueOf(userId)});
  runSQL(sql);
}

{
  if (!socialEquityGroupSettingLocalService.isEnabled(groupId,Group.class.getName(),type)) {
    return false;
  }
  return socialEquityGroupSettingLocalService.isEnabled(groupId,className,type);
}

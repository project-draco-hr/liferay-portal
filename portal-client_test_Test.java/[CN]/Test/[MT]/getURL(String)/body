{
  String url="http://localhost:8080/tunnel/axis/" + serviceName;
  if (true) {
    String userId="liferay.com.1";
    String password="qUqP5cyxm6YcTAhz05Hph5gvu9M=";
    url="http://" + userId + ":"+ password+ "@localhost:8080/tunnel/secure/axis/"+ serviceName;
  }
  return new URL(url);
}

{
  for (  SCProductVersion scProductVersion : scProductVersions) {
    if (entityCache.getResult(SCProductVersionModelImpl.ENTITY_CACHE_ENABLED,SCProductVersionImpl.class,scProductVersion.getPrimaryKey()) == null) {
      cacheResult(scProductVersion);
    }
 else {
      scProductVersion.resetOriginalValues();
    }
  }
}

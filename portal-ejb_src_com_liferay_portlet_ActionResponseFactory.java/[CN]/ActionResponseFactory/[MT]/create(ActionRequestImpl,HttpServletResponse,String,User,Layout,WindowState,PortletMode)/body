{
  ActionResponseImpl actionResponseImpl=(ActionResponseImpl)_instance._pool.borrowObject();
  actionResponseImpl.init(req,res,portletName,user,layout,windowState,portletMode);
  return actionResponseImpl;
}

{
  dbProperties=DBProviderTestUtil.readProperties(getTestPropertiesFileName());
  ShardExporter exporter=ShardExporterFactory.getShardExporter(dbProperties);
  dbProvider=(DBProvider)exporter;
  executeUpdate(dbProvider.getDataSource(),getCreateTableStatement());
}

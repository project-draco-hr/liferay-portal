{
  GroupPermission.check(getPermissionChecker(),liveGroupId,ActionKeys.UPDATE);
  return GroupLocalServiceUtil.addGroup(getUserId(),null,null,name,description,type,friendlyURL,active,liveGroupId);
}

{
  GroupPermission.check(getPermissionChecker(),liveGroupId,ActionKeys.UPDATE);
  return GroupLocalServiceUtil.addGroup(getUserId(),null,null,liveGroupId,name,description,type,friendlyURL,active);
}

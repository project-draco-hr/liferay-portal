{
  long companyId=CompanyThreadLocal.getCompanyId();
  ExpandoValue value=expandoValueFinder.fetchByTC_TC_TN_CN_C(companyId,classNameId,tableName,columnName,classPK);
  if (value != null) {
    deleteValue(value.getValueId());
  }
}

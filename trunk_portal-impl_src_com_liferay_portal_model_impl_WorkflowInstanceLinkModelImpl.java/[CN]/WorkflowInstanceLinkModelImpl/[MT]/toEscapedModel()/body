{
  if (isEscapedModel()) {
    return (WorkflowInstanceLink)this;
  }
 else {
    return (WorkflowInstanceLink)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}

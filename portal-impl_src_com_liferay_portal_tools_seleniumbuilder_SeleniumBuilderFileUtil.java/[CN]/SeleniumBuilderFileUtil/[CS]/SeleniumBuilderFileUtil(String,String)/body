{
  _baseDirName=baseDirName;
  _projectDirName=projectDirName;
  Properties properties=new Properties();
  try {
    String content=FileUtil.read(_projectDirName + "/test.properties");
    InputStream inputStream=new ByteArrayInputStream(content.getBytes());
    properties.load(inputStream);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  _componentNames=ListUtil.fromArray(StringUtil.split(properties.getProperty("component.names")));
}

{
  String bufferedExecutionMode=GetterUtil.getString(properties.get("buffered.execution.mode"));
  if (Validator.isNull(bufferedExecutionMode)) {
    throw new IllegalArgumentException("Must specify buffered.execution.mode: " + ClassUtil.getClassName(bufferedExecutionMode));
  }
  return bufferedExecutionMode;
}

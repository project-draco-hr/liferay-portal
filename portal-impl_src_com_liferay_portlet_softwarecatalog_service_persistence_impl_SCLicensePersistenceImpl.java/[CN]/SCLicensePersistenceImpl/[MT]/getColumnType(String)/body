{
  Integer type=SCLicenseModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SCLicenseModelImpl.TABLE_NAME);
  }
  return type;
}

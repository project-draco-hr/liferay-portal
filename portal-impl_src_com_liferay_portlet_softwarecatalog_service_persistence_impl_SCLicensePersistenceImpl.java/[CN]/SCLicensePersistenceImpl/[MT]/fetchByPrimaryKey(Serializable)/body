{
  SCLicense scLicense=(SCLicense)EntityCacheUtil.getResult(SCLicenseModelImpl.ENTITY_CACHE_ENABLED,SCLicenseImpl.class,primaryKey);
  if (scLicense == _nullSCLicense) {
    return null;
  }
  if (scLicense == null) {
    Session session=null;
    try {
      session=openSession();
      scLicense=(SCLicense)session.get(SCLicenseImpl.class,primaryKey);
      if (scLicense != null) {
        cacheResult(scLicense);
      }
 else {
        EntityCacheUtil.putResult(SCLicenseModelImpl.ENTITY_CACHE_ENABLED,SCLicenseImpl.class,primaryKey,_nullSCLicense);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(SCLicenseModelImpl.ENTITY_CACHE_ENABLED,SCLicenseImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return scLicense;
}

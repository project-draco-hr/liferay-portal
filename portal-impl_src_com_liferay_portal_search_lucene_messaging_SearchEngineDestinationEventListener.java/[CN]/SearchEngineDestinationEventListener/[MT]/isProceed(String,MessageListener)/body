{
  if ((!destinationName.equals(DestinationNames.SEARCH_READER)) || (messageListener == _searchReaderMessageListener) || !(messageListener instanceof SearchReaderMessageListener)) {
    return false;
  }
 else {
    return true;
  }
}

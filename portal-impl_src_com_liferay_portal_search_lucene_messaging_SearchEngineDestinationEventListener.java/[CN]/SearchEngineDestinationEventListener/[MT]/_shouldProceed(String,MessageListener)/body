{
  if (!destinationName.equals(DestinationNames.SEARCH_READER) || (messageListener == _defaultSearchReaderMessageListener) || !(messageListener instanceof SearchReaderMessageListener)) {
    return false;
  }
  return true;
}

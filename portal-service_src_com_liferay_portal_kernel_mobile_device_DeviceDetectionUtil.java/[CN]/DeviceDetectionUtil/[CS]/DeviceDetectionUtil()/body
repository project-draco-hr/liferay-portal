{
  Registry registry=RegistryUtil.getRegistry();
  _serviceTracker=registry.trackServices(DeviceRecognitionProvider.class,new DeviceRecognitionProviderServiceTrackerCustomizer());
  _serviceTracker.open();
}

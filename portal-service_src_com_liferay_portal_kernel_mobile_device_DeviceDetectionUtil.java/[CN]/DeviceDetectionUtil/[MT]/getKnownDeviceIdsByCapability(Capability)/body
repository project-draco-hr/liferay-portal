{
  KnownDevices knownDevices=_deviceRecognitionProvider.getKnownDevices();
  Map<Capability,Set<String>> capabilityDeviceIdsMap=knownDevices.getDeviceIdsByCapabilities();
  return capabilityDeviceIdsMap.get(capability);
}

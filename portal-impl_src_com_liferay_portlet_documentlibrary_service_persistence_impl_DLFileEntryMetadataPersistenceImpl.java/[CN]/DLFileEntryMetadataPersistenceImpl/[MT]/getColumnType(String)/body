{
  Integer type=DLFileEntryMetadataModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ DLFileEntryMetadataModelImpl.TABLE_NAME);
  }
  return type;
}
